# 그림 정책 — SVG 그래픽 리소스

이 게임의 그림은 **듀오링고풍 플랫 2D 벡터**다. 아바타·크리처·소품·아이콘 모두 같은 결이어야 한다.

지금 코드로 그리는 것(`avatar.js` · `creature.js` · `portrait.js` · `village.js`)도 이 기준을 따르고,
바깥에서 새 리소스를 만들 때도(생성 모델이든 사람이든) 아래 프롬프트를 그대로 쓴다.

관련 문서 — 글자는 `TEXT_POLICY.md`, 컴포넌트는 `UI_POLICY.md`.

---

## 1. 프롬프트

```
A cute human character in Duolingo illustration style, flat 2D vector,
clean smooth line art, constructed with simple rounded shapes,
vibrant solid flat colors, white background, no 3D depth,
no marionette joints, seamless limbs, minimal details
--no realistic, 3d render, marionette lines, textures
```

사람이 아닌 것(크리처·소품·배경)은 `A cute human character` 자리만 바꾸고 나머지는 그대로 둔다.

### 반드시 들어가는 말

`flat 2d vector illustration` · `duolingo style` · `simple rounded shapes` ·
`bold outlines` · `vibrant solid color palette` · `white background`

### 빼야 하는 것 (negative)

`3d` · `realistic anatomy` · `wooden doll joints` · `complex textures` · `detailed fingers`

### ⚠️ 비트맵을 들여올 때는 **배경을 구워 넣지 않는다** (PNG 는 투명 배경으로)

이 게임의 그림은 거의 다 SVG 라 이 문제가 없는데, **`logo.png` 하나가 불투명 팔레트
이미지**였다. 그 한 장 때문에 **스플래시만 테마를 못 타고** 고정 near-white 로 오래
남아 있었다 — 바탕을 테마 색으로 두면 로고가 사각형으로 깨져 보였기 때문이다.

- **드롭 섀도까지 구워져 있으면 더 나쁘다** — 「흰 것을 지운다」로 배경만 걷어 내면
  그림자가 회색 얼룩으로 남고, 어두운 테마에서는 그것이 **빛나는 자국**이 된다.
  그림자는 «검정 + 낮은 알파»로 풀려야 어느 바탕에서도 그림자로 보인다
- 이미 구워진 것을 되살리는 방법은 `tools/genlogo.js` 에 있다 (α 를 풀어 바탕을 빼낸다).
  **그래도 처음부터 투명하게 받는 편이 낫다** — 되살린 것은 근사값이다

---

## 2. 형태 규칙

| 축 | 규칙 |
|---|---|
| 얼굴·비율 | 큰 머리, **둥근 모서리의 직사각형과 원**으로 이루어진 몸체 (chibi proportions, oversized head) |
| 관절·사지 | 손가락은 생략하거나 **4개 이하**. 관절의 구분이 드러나지 않는 **단순한 덩어리** (fused seamless joints, mitten hands) |

**「관절이 드러나지 않는다」가 이 문서에서 가장 자주 깨지는 규칙이다.**
지금 아바타는 몸통·팔·허벅지·종아리를 **따로 그린 도형으로 겹쳐서** 만든다 —
그래서 겹침이 어긋나는 순간 그 자리가 곧바로 **꼭두각시 인형의 관절**로 보인다.
실제로 세 번 겪었다:

- 팔 위 끝이 어깨보다 아래에서 시작해 이음매에 계단이 졌다 (「승모근이 튀어나온 것 같다」)
- 어깨 곡선이 팔 윗머리보다 먼저 내려와 그 사이가 **3.75px 파였다** (「어깨에 살짝 홈이 생겨」)
- 몸통 옆선과 팔이 반대로 기울어 옆구리에 **1px 짜리 실틈**이 났다

셋 다 **눈으로는 「살짝 이상한데」 정도로만 보여서** 오래 남아 있었다.
그래서 검사기로 못을 박아 두었다 — `node tools/checkavatar.js` 의
「어깨 홈」 · 「몸통↔팔」 · 「목」 항목이 그것이다.
**몸의 기준값(`avatar.js` 의 `BODY`)을 건드렸으면 반드시 돌린다.**

### ⚠️ 덮는 조각의 «바깥 선»은 덮이는 것에서 **뽑는다** — 손으로 그리지 않는다

머리카락은 두개골을, 후드는 머리를, 옷은 몸을 덮는다. 그 바깥 선을 손으로 그리면
**덮이는 쪽을 한 번 고칠 때마다 어긋나고**, 어긋난 것이 화면에 오류 하나 없이 남는다.

초상화에서 실제로 그랬다 — 얼굴 타원의 꼭대기는 y=36 인데 머리카락 꼭대기가 y=18~22 라
**12~14px 이 떠 있었고**, 껍질이 정수리만 두꺼워(정수리 12~14px / 관자놀이 5~7px)
실루엣이 세로로만 늘어나 **「콘헤드」**가 됐다.

**덮이는 것과 «같은 중심·같은 rx» 에 두께만 더한 타원의 호**가 답이다
(`avatar.js` 의 `BANG_FLAT` · `portrait.js` 의 `crown()`). 같은 타원이면
**y=cy 에서 폭도 접선도 정확히 맞아 이음매가 «없다».**

- ⚠️ **두께는 «어디서나 비슷»해야 한다.** 한쪽만 두꺼우면 그 방향으로만 솟는다 —
  `checkportrait` 가 제일 두꺼운 데 ÷ 제일 얇은 데를 1.5배로 묶는다
- ⚠️ **모양의 차이는 «안쪽 선»으로 낸다** (머리라면 헤어라인). 바깥 호를 들어 올려
  차이를 내면 스타일을 바꿀 때마다 머리통의 «크기»가 같이 변한다
- **얹은 것(매듭 · 뻗친 가닥 · 어깨로 흐르는 몫)은 예외다** — 그것은 머리통이 아니다.
  `data-part` 를 갈라 두어 검사가 머리통만 보게 한다

### ⚠️ 옷이 덮어야 할 자리에 **아무것도 없으면 안 된다** (배경이 비친다)

**옷과 몸 사이에 «빈 자리»가 생기면 그 틈으로 배경이 그대로 비친다.** 마이 룸의 벽이
베이지라, 그 틈이 사람 눈에는 **살색 얼룩**으로 보인다 — 「공주 옷 어깨 선이 살짝
어긋난다」로 신고받은 자리가 그것이다.

이 그림에서 틈이 생기는 이유는 늘 같다: **몸통의 윗선은 평평한데(`torsoTopY`) 머리는
그보다 위에 있다.** 그래서 목의 양옆 — 턱과 어깨 사이 — 에 몸도 옷도 없는 띠가 남는다.
사람으로 치면 목에서 어깨로 내려오는 자리이고, **옷을 입었으면 옷이 덮고 있어야 할 자리**다.

- **메우는 것은 `avatar.js` 의 `neckGusset` 하나다.** 원피스가 있으면 그 색, 없으면 상의,
  아무것도 없으면 살색 (`crouchBack` 의 규칙과 같다)
- **윗변은 «턱선»이다** — 얼굴 타원의 아래 호를 몸통 좌표계로 옮긴 것(`neck.faceRx/faceRy`).
  평평한 선으로 메우면 목에서 멀어질수록 턱이 위로 달아나 **다시 4.5px 이 비친다**
- **턱보다 `GUSSET_RISE`(5) 위에서 자른다.** 턱선을 끝까지 따라가면 옷깃이 볼 옆까지
  기어올라 「얼굴을 감싼 깃」이 된다. ⚠️ 그 높이는 px 가 아니라 **얼굴에 대한 비율**이다
  (`GUSSET_RISE_K`) — px 로 박으면 머리가 작아질수록 이 조각이 상대적으로 넓어진다
- ⚠️ **맨몸에서는 윗변을 목에 붙인다** (`NECK_HUG` · 색이 `SKIN` 일 때만). 이 조각은
  역할이 둘이다: **옷을 입었으면 깃**이라 턱선의 현을 끝까지 써야 하고(좁히면 얼굴의
  아래 호와 어깨 사이에 배경이 쐐기처럼 남는다), **맨몸이면 살**이라 목과 한 덩어리로
  보여서 넓으면 목이 굵어 보인다. 맨몸에서 목 옆에 배경이 비치는 것은 **맞는 모양**이다 —
  공주는 드레스 깃이 높아 덮여 있을 뿐이고, 「턱밑 머리」가 오히려 그 틈을 4px 이상 요구한다
- ⚠️ **옆변은 어깨로 «흘러내리는 곡선»이다.** 네모로 잘라 내리면 어깨 위에 **각진 선반**이
  얹힌다 (그려 보고 되돌렸다). 몸통 윗선보다 아래(`GUSSET_DROP`)에서 옷 안으로 들어가게
  이으면 인트로 공주의 깃처럼 읽힌다
- ⚠️ **가운데는 `fill-rule="evenodd"` 로 도로 파낸다** — 안 파면 목이 통째로 옷 색이
  되어 «폴라»가 된다
- ⚠️ **옷을 입었으면 이 자리를 «메우지 않는다».** 목 옆에 **무엇을 그리든 목이 굵어
  보인다** — 옷 색·살색·턱 그늘색 세 판을 그려 보고 셋 다 신고받았다
  (「초록 기둥이 얼굴까지」 · 「동그랗게 파인 부분이 너무 작다」 · 「옷을 입으면 양옆으로
  목이 더 생긴다」). **맨몸과 똑같이 두는 것**이 이 그림에서 맞는 모양이고,
  `86a649a`(받침이 생기기 전)가 그랬다. 옷은 **제 넥라인**(`NECK_CUT`)을 그대로 그린다.
  · **깃이 높은 옷(터틀넥·폴로)만 메운다** — 거기서는 옷깃이 목까지 올라오는 것이 맞는
    모양이다. 판정은 `NECK_CUT` 표 하나(`pad == null`)를 본다
  · **검사 둘이 짝으로 지킨다** — 「목깃」이 «옷 색으로 메우는 판»을, 「맨몸↔옷」이
    «살색·그늘색으로 덮는 판»을 잡는다. 하나로는 셋을 다 못 잡는다
  · 「턱 밑 빈 자리」는 이제 **깃이 높은 옷에서만** 잰다 — 깃이 낮은 옷에서 그 틈은
    메울 것이 아니라 맞는 모양이기 때문이다

**새 옷·새 몸을 만들 때 지킬 것:**

1. **옷의 윗선을 몸통 윗선(`CLOTH_TOP_Y`)보다 위로 올리지 않는다.** 올리려면 그 위를
   무엇이 덮는지(머리·머리카락·옷깃)를 먼저 정한다
2. **넥라인은 몸통 윗선보다 위를 팔 수 없다** (`torsoTopAtX`) — 파면 옷도 몸도 없는
   자리가 생긴다. `NECK_CUT` 의 폭·깊이가 유일한 원본이고 검사기도 같은 표를 읽는다
3. **`node tools/checkavatar.js` 를 돌린다.** 「목깃」이 어깨선 위에서 옷이 목에 닿는지
   보고, 「살이 옷 밖으로」·「엉덩이·허벅지가 옷 옆으로」가 반대 방향(옷이 못 덮은 살)을
   본다. ⚠️ 얼굴이 아주 크면 턱이 어깨선 밑으로 내려가 그 줄의 살이 «볼»이 된다 —
   그때는 두 판이 같은 값을 내므로 거짓 통과가 아니다
4. **「턱 밑 빈 자리」가 이 규칙 자체를 지킨다** — 옷을 입힌 몸에서 **양옆이 막힌
   «투명한» 구간**을 직접 센다. 「옷깃 받침」은 옷이 **어깨 밖으로 나온** 쪽만
   보므로 **덜 덮는 쪽의 사고는 한 줄도 안 잡는다** (실제로 받침을 좁혀 놓았더니
   좌우 3.3~6.7px 이 비치는데도 통째로 통과했다)

### 옷 위에 얹는 띠는 **옷의 옆선을 따라간다**

벨트처럼 옷 위에 겹쳐 그리는 것은, 아래 옷이 **어디까지 넓은지**를 보고 그려야 한다.

허리춤은 `wR`(허리 폭)에서 시작해 `hR`(엉덩이 폭)로 벌어지는 큐빅이다.
벨트를 「허리 폭 + 1」로만 그렸더니 14px 아래에서는 옷이 이미 **3.5~5.5px 더 넓어**
띠 양옆에 옷색이 그대로 남았다 — 벨트가 허리를 못 두르고 가운데만 지나간 것처럼
보였다 (신고받았다).

- **같은 곡선을 두 번 적지 않는다.** `sideXAt()` 한 곳에서 풀고 벨트가 그것을 쓴다.
  좌표를 옮겨 적으면 옆선을 고쳤을 때 벨트만 옛 자리에 남는다
- **위아래 두 점을 직선으로 잇지 않는다.** 곡선의 현(弦)이라 가운데가 2px 안으로
  들어간다 — `sideRun()` 으로 훑어서 따라간다 (잰 값: 3.5/5.5 → 0/0.5, 남은 것은 안티앨리어싱)

### 옷에 뚫은 구멍은 **몸이 실제로 비어 있는 만큼만**

바지의 가랑이 홈처럼 **옷을 파내서 모양을 내는 자리**는, 파낸 폭이 그 높이의
**실제 몸 틈보다 넓으면 그 차이가 그대로 맨살로 보인다.**

반바지가 그랬다 — 홈이 폭 14(x 93~107) **고정**이었는데 그 높이의 다리 사이 틈은
`innerX` 로 재면 5 밖에 안 됐다. 남는 9px 이 허벅지 안쪽 살이었고,
반바지 색(`#ffc2a8`)이 살색과 가까워 **「바지가 갈라진 것」이 아니라 「맨살」로 읽혔다**
(플레이어가 신고했다).

규칙은 둘이다:

1. **구멍은 몸의 실제 틈을 따라간다** (`inseam()` → `innerX(y) - INSEAM_OVER`).
   숫자를 박아 두면 배율을 바꾼 몸에서 반드시 어긋난다
2. **모양을 내는 것은 구멍이 아니라 그늘로 한다.** 틈만큼만 파면 반바지가
   치마처럼 보이므로, 갈라진 느낌은 `shade(c, 34)` 로 **위에 덧그린다** (3장의 음영 규칙)

`tools/checkavatar.js` 의 「가랑이 홈에 살」이 이것을 지킨다.
**사보타주할 때는 구멍과 그늘을 같이 되돌려야 한다** — 구멍만 되돌리면 그늘이 덮어
정작 신고받은 반바지가 0건으로 나온다 (실제로 한 번 속을 뻔했다).

---

## 3. 그림자·그라데이션은 **쓴다**

프롬프트에서 `shading` · `shadows` · `gradient` 를 일부러 빼 두었다.
듀오링고 원본은 완전 평면이지만, **이 게임의 그림에는 이미 옅은 음영이 들어가 있다** —
빼면 오히려 어색해진다.

지금 쓰고 있는 것:

- `SKIN_SH` — 살색보다 한 단 어두운 색. 발등·뒤꿈치·턱 밑에 쓴다
- `neckG_*` 그라데이션 — 턱 밑 그늘이 **아래로 갈수록 사라지게** 한다.
  통짜 네모로 두면 턱과 몸통 사이에 어두운 띠가 딱 잘려 보였다
- `shade(color, n)` — 옷의 접힌 자리·단추·밑단 선

**규칙: 음영은 형태를 읽히게 하는 만큼만.** 입체를 만들려고 쓰지 않는다.
같은 살색 덩어리 둘이 겹쳤을 때 경계를 알려 주는 용도다.

---

## 4. 이 문서를 고쳤으면

정책과 검사기는 짝이다. 형태 규칙을 바꿨으면 `tools/checkavatar.js` 의 상수도 같이 본다
(`SHOULDER_DIP` · 목 길이 상·하한 · 커버리지 창).


---

## 사람이 서 있는 그림은 **사람이 가리는 자리를 «재서»** 그린다

NPC 대화 화면처럼 배경 위에 인물을 세우는 자리에서는, 물건을 어디에 두느냐가
그림의 절반이다. 여기서 두 번 틀렸다:

- **짐작으로 적지 않는다.** 「사람은 오른쪽 3분의 1에 선다」고 주석에 써 두고 물건을
  그 왼쪽에 놓았는데, 실제로 그림이 칠하는 자리는 **viewBox 300 중 x 211~276** 이었다 —
  여관의 창문 · 광산의 광차 · 탑의 아치창 · 우물의 나무통이 통째로 등 뒤에 있었다.
  **브라우저에서 한 번 재면 끝나는 일**이다 (폭을 여럿 두고 잰다)
- **바닥 그림자는 사람이 설 때만 그린다.** 빈 자리에 남은 그림자는 「뭔가 사라진 자리」다

## 방은 **그 방의 일**로 그린다 — 「선반 + 탁자 + 창문」은 아무 방도 아니다

대장간이 대장간 같지 않던 이유는 색이 아니라 **놓인 것**이었다. 방 하나를 그릴 때는
「여기서 무슨 일을 하는가」에서 물건 서넛을 고르고, 그중 **하나를 크게** 둔다:

- 대장간 → **화덕의 불**, 그다음 모루(뿔이 있어야 모루로 읽힌다)와 걸린 연장
- 여관 → **벽난로**, 술통, 차려진 밥상
- 광산 → **버팀목**(없으면 갱도가 아니다), 어두운 갱구, 광차, 매단 랜턴
- 탑 → **나선 계단**과 아치창. 위로 «올라가는» 방이다
- 폐허 → **바깥이 보여야** 무너진 것이다

⚠️ **벽과 바닥도 같이 갈린다** — 돌벽/회벽/바위/트인 하늘, 나무/돌/흙/물.
물건만 바꾸고 벽을 그대로 두면 「같은 방에 다른 가구」로 보인다.

⚠️ **하늘이 보이는 방은 위가 텅 빈다** — 물가가 그랬다. 먼 것(새·언덕)을 높은 자리에
하나 두면 채워진다. 반대로 실내는 **창문을 높은 자리(y28~80)** 에 두어 물건과 안 겹치게 한다.

---

## NPC 초상화의 얼굴 — 타원은 «도토리»다

「남자 NPC 들이 도토리 같다」고 신고받아 다시 그렸다. 원인이 넷이었고 **넷이 다 필요했다** —
하나만 고치면 나머지가 그대로 티가 난다:

1. **얼굴이 타원이었다** (세로/가로 1.15). 어느 각도로 봐도 사람 얼굴이 아니다.
   지금은 **광대에서 턱으로 좁아지는 윤곽** — 광대 반폭 25 · 턱 반폭 11 · 1.29
2. **눈썹도 코도 없었다.** 눈과 입만 있는 얼굴은 «아기»로 읽힌다.
   ⚠️ **코는 콧방울 그늘 한 조각뿐이다** — 만화에서 콧대 선 하나는 «흉터»로 보인다
   (4px 넘게 그었다가 그렇게 보였다)
3. **수염이 입을 덮고 있었다.** 옛 `full` 은 y70~108 을 통째로 덮는데 입은 y82~90 이라
   **입이 수염 «위에» 떠 있었다.** 지금은 **턱수염 → 입자리(살색) → 콧수염 → 입**
   순서로 그려 입술이 «살 위»에 앉는다
4. **머리가 바가지였다** — 돔 + 평평한 밑단. 버리는 열쇠 셋: ① 가르마(좌우 비대칭)
   ② 관자놀이가 파인 헤어라인 ③ 귀 앞까지 내려오는 옆머리(구레나룻)

⚠️ **눈·입의 자리(y66 · x50/70 · y≈84)는 한 글자도 안 옮겼다.** 표정 열여덟 × 열아홉이
그 좌표에 붙어 있어서, 옮기면 표정을 전부 다시 그려야 한다. 두개골만 갈았다.

⚠️⚠️ **눈썹은 «인물마다» 다르다** (`SPEAKERS` 의 `brows` · 일곱 가지).
한 벌로 두었더니 **여덟이 전부 같은 눈썹**이었고(「왜 모든 NPC 의 눈썹이 똑같아?」),
그전에는 굵은 직선 하나라 **여왕이 남자로 읽혔다.** 눈썹은 머리·수염과 같은 축이다 —
얼굴에서 «사람을 가르는 힘»이 제일 센 부품이라 한 벌로 두면 여덟이 형제가 된다.
가는 쪽(`thin`·`soft`)은 **광대 음영도 안 얹는다** — 둘 다 「어른 남자」를 만드는
마크라 짝으로 움직여야 한다.

⚠️ **부품을 늘리면 «같이 움직이는 것»을 찾아라.** 이번에 딸려 온 것 —
어깨를 넓히니(머리만 큰 아이로 보였다) **긴 머리 가닥이 허공에서 끊겼고**(가닥을 상자
밑까지 내렸다), 후드를 아치 하나로 두니 **머리띠로 보였다**(어깨까지 내렸다).

## 표정 (`portrait.js` · `data.js` 의 `BASE_MOODS`)

**표정을 하나씩 그리지 않는다.** 눈·입·눈썹을 부품으로 두고 **조합**한다 —
눈 하나를 늘리면 표정이 입 수만큼 늘고, 열 명이 한꺼번에 그 표정을 갖는다.

| 부품 | 어디에 | 지금 |
|---|---|---|
| 눈 `EYE` | `portrait.js` | 17가지 |
| 입 `MOUTH` | `portrait.js` | 16가지 |
| 눈썹 `BROW` | `portrait.js` | 7가지 (없는 것이 기본) |
| 이름 `BASE_MOODS` | `data.js` | 38가지 |

### ⚠️ 표정 계통이 **셋**이다 — 하나만 늘리면 나머지는 그대로다

같은 「표정」이라는 말이 세 곳에 있고 **서로 다른 표를 본다.**

| | 어디 | 누가 쓰나 |
|---|---|---|
| 초상화 | `portrait.js` + `BASE_MOODS` | 마을 NPC · 대사 · 컷씬 (이 절) |
| 인트로 그림 | `intro.js` 의 `poses` | 요정 대모 · 공주 (아래 항) |
| **마이 룸 아바타** | `avatar.js` 의 `AV_FACE` | **옷장에서 사람이 고르는 것** |

앞의 둘만 늘렸더니 **사람이 실제로 보는 옷장의 「표정」은 여섯 그대로였다** —
실제로 그랬다. 아바타 쪽은 규칙이 따로 있다 (`CLAUDE.md` 아바타 절):
눈·입 자리는 `AV` 다섯 숫자에서만 나오고, **표정 쪽에 좌표를 박지 않는다.**

- **이름은 감정이지 부품이 아니다.** 「눈웃음」이 아니라 「기쁨」이라야 사람마다
  다르게 그려도 대사가 같은 이름을 쓸 수 있다
- **눈썹은 없는 것이 기본이다.** 있는 쪽이 예외라야 얼굴이 안 시끄럽다.
  다만 **화남과 슬픔은 눈만으로 안 갈린다** — 그 둘은 눈썹 각도가 만든다
- ⚠️ **없는 부품 이름은 조용히 기본으로 떨어진다** (`EYE[m.eye] || EYE.normal`).
  오타를 내도 화면은 멀쩡하고 그 표정만 영영 기본 눈이 된다 —
  `tools/checktalk.js` 가 `portrait.js` 에서 부품 이름을 읽어 대조한다

### ⚠️ 인트로 그림을 쓰는 둘은 다르다

요정 대모와 공주는 **인트로에 이미 얼굴이 있다.** 부품으로 다시 그리면 방금까지
보던 사람과 다른 사람이 되므로(실제로 그랬다), `intro.js` 의 그림을 그대로 쓴다.

그래서 그 둘은 **이름만 늘려서는 표정이 안 는다** — 그림(`poses`)이 있어야 한다.
없는 포즈를 적으면 조용히 첫 포즈로 떨어진다 (checktalk 이 잡는다).
포즈를 늘릴 때는 **몸·머리는 그대로 두고 눈·입만** 바꾼다.

- 요정 대모 `fairyFace()` — idle · smile · glance · cross · laugh · wink · sad · think · proud
- 공주 `princessFace()` — puzzled · shy · smile · ask · dizzy ·
  laugh · wink · sad · cry · angry · sleepy · love · proud
