# 동행 크리처 — 기획 초안

크리처를 **데리고 탐험을 나가면 얻는 것이 늘어난다.**

> **초안이다.** 코드는 한 줄도 없다. 수치는 전부 **임시값**이고, 왜 그 값인지를
> 표 아래에 적어 두었다 — 바꾸기 전에 그 관계를 먼저 볼 것.
> 지금 코드에서 확인한 사실은 「지금 이렇게 되어 있다」로 표시했다.

---

## 한 줄

**크리처는 지금 방에 놓아 두는 장식이다.** 만들면 매력 총합이 오르고, 그게 전부다
(`charmBonus` — 나비 +2 · 개구리 +1 · 유니콘 +5).

여기에 **쓸 자리**를 준다. 한 마리를 골라 데리고 나가면 채집 결과가 달라진다.

```
조합 → 크리처 → 데리고 채집 → 재료가 더 나온다 → 더 좋은 조합
                    └→ 친밀도 ↑ → 그 크리처의 효과가 더 커진다
```

**정서는 「혼자가 아니다」다.** 이 게임의 폭식 판정 기준이 *혼자 먹었느냐*이고
(`EXERCISE.md` 7-3), 아우라의 주제가 *혼자 쌓은 것은 숫자일 뿐*이다
(`STORY.md` 카이로스). 동행은 그 줄 위에 있다 — 같은 채집인데 옆에 뭔가가 있다.

---

## 1. 지금 어떻게 되어 있나 (확인한 것)

| 것 | 지금 상태 |
|---|---|
| 크리처 | **세 마리뿐** (`data.js` — 반짝 나비 · 꽃개구리 · 유니콘) |
| 크리처 효과 | `charmBonus` 하나. `totalCharm()` 에 더해진다 |
| 중복 소유 | **된다.** `S.creatures` 는 배열이고 같은 id 가 여러 번 들어간다 (보너스도 그만큼 겹친다) |
| 채집 1회 | 재료 **반드시 1개** + 음식 12%(`FOOD_RATE`) + 특별한 재료 0.1%(`SPECIAL_RATE`) |
| 채집 비용 | AP 10 (`D.ENERGY.cost.gather`). 하루 1000 AP → **100번** |
| 탐험 갈래 | 필드 / 마을. **마을은 아직 껍데기다** — 이 시스템은 필드만 본다 |
| 옷 획득 | **없다.** `unlockCosmetic()` 은 있는데 개발용 버튼 말고는 아무도 안 부른다 |

### 그래서 「확률이 증가한다」가 무엇을 말하는가

**채집은 이미 100% 재료를 준다.** 그러니 「획득 확률」을 그대로 올릴 자리가 없다.
올릴 수 있는 것은 넷이다.

| 후보 | 지금 | 손대면 |
|---|---|---|
| **덤** — 재료가 하나 더 나온다 | 없음 | **이것이 주 효과다.** 「더 얻는다」가 가장 곧게 읽힌다 |
| 특별한 재료 | 0.1% | 배율만 곱하면 된다. 다만 `specials` 기록이 **행운 아우라**로 간다 (아래 6장) |
| 음식 | 12% | 손대기 쉬운데 **체감이 약하다** (아래 6장) |
| 옷 | 없음 | 이 문서 밖이다. 만들 것이 통째로 하나 더 있다 |

> **덤을 고른 이유.** 나머지 셋은 이미 있는 확률에 배율을 곱하는 것이라
> 화면에 아무 변화가 없다 — 0.1%가 0.3%가 된 것을 사람은 못 느낀다.
> 덤은 **토스트가 두 줄이 되고 가방 숫자가 눈에 띄게 는다.**

---

## 2. 동행 규칙

- **한 번에 한 마리.** 고르는 것이 이 시스템의 전부다
- **데려나가는 데 드는 것은 없다.** AP 도 안 들고, 크리처가 지치지도 않는다
- **방에 있는 것과 데리고 나간 것은 같다** — 매력 보너스는 그대로 붙어 있다
- 안 데려가도 된다 (`S.pal = null`). 지금과 똑같이 돌아간다

### 왜 비용을 안 붙이나

세 가지를 검토했고 셋 다 뺐다.

1. **크리처 피로도** — AP 위에 게이트가 하나 더 얹힌다.
   「채집하러 왔는데 못 한다」가 두 겹이 되면 코지 게임이 아니다.
   운동을 즉시 끝나게 만든 것과 같은 판단이다 (`EXERCISE.md` 4장)
2. **데려나가면 매력 보너스가 빠진다** — ⚠️ **이건 위험하다.**
   `isMapOpen(m)` 이 `totalCharm() >= m.unlock` 이고 `totalCharm()` 에 `charmBonus` 가
   들어 있다. 유니콘(+5)을 데리고 나가는 순간 **열려 있던 맵이 다시 잠긴다.**
   해금이 왔다 갔다 하는 것은 어떤 이유로도 하면 안 된다
3. **여러 마리 동행** — 다 데려가면 고를 것이 없어진다. 고르는 것이 전부인데

**대신 친밀도가 비용이 된다.** 갈아타면 쌓은 것이 그 자리에 남는다 — 잃지는 않지만
새로 고른 쪽은 처음부터다. 이것이 「아무나 데려가도 그만」을 막는다.

---

## 3. 친밀도 (`S.palBond`)

```js
PAL = {
  bondMax: 500,        // 여기서 효과가 최대
  bondPerGather: 1,    // 데리고 채집할 때마다
  bonusMin: 0.08,      // 친밀도 0   — 덤 확률
  bonusMax: 0.20,      // 친밀도 500 — 덤 확률
  homeZoneMul: 2,      // 선호 지대에서는 덤 확률 두 배
}
덤 확률 = lerp(bonusMin, bonusMax, bond / bondMax) × (선호 지대면 homeZoneMul)
```

- 크리처마다 따로 쌓인다 (`S.palBond = { butterfly: 320, frog: 12 }`)
- **중복 소유는 친밀도로 바꾼다.** 같은 크리처를 또 만들면 `bondMax / 5` 만큼 얹어 준다 —
  지금은 중복이 매력만 겹쳐서 두 마리째부터 만들 이유가 약하다
- 상한을 **꼭 둔다.** 덤 확률이 1.0 을 넘으면 AP 당 재료가 두 배가 되고,
  그건 사실상 AP 상한을 두 배로 올린 것이다 (다이아 충전의 가치가 같이 무너진다)

> **500 은 임시값이다.** 하루에 30번쯤 채집하는 사람 기준으로 **약 2주**,
> AP 를 다 쓰는 사람(100번) 기준으로 **닷새**다. 갈아타는 것이 아깝게 느껴지되
> 영영 못 갈아탈 만큼은 아닌 자리를 노렸다.

---

## 4. 크리처를 늘린다 — 지금 셋으로는 고를 것이 없다

**지대가 다섯인데 크리처가 셋이다.** 선호 지대를 붙이려면 최소 다섯,
「전 지대」 하나를 얹어 여섯~일곱이 맞다.

| 크리처 | 등급 | 선호 지대 | 특기 |
|---|---|---|---|
| 🦋 반짝 나비 | 기초 | 포근 평야 | 덤 확률이 가장 높다 (`bonusMax` +0.04) |
| 🐸 꽃개구리 | 기초 | 울창 숲 | 음식이 잘 나온다 (12% → 22%) |
| 🐿️ *도토리 다람쥐* | 기초 | 울창 숲 | 덤이 터지면 **두 개**를 물어 온다 (30%) |
| 🦉 *밤샘 올빼미* | 중급 | 뾰족 산악 | **밤(21~05시)에는 어디서든 선호 지대 취급** |
| 🐚 *소라게* | 중급 | 반짝 해안 | 채집 AP 를 1 깎아 준다 (10 → 9) |
| 🦎 *모래 도마뱀* | 중급 | 황량 황무지 | 그 맵의 특별한 재료 확률 ×2 |
| 🦄 유니콘 | 상급 | — (전 지대) | 특별한 재료 ×3 · 덤도 기초급 |

*기울임 = 새로 만들 것.*

**상급은 넓고 얕게, 기초는 좁고 깊게.** 유니콘은 이미 `charmBonus` 5 로 제일 세다 —
동행 효과까지 최고면 **유니콘 하나로 끝난다.** 그래서 유니콘은 「어디서나 무난」이고,
특정 지대에서는 그 지대의 기초 크리처가 더 낫게 잡았다.

### ⚠️ 새 크리처를 넣을 때

- **레시피 조합이 겹치면 조용히 덮어쓴다.** `RECIPE_MAP[recipeKey(inputs)] = result` 라
  나중 것이 이긴다 — 오류도 안 나고 앞 레시피가 사라진다.
  **`npm test` 에 중복 조합 검사가 지금 없다.** 크리처를 늘리기 전에 그것부터 만든다
- 재료는 **초반 맵에서 나오는 것**으로 짠다. 후반 맵 재료가 기초 크리처에 들어가면
  「기초인데 못 만든다」가 된다 (`CLAUDE.md` 6번)
- 이름은 `NAMES.en` 에도 같이 넣는다. 안 넣으면 영어 화면에 한글이 그대로 남는다

---

## 5. 화면

### 5-1. 동행 칸 → **탐험 화면 위쪽 한 줄**

```
┌─────────────────────────────┐
│ 🗺️ 탐험    [🦋 반짝 나비 ▸] │  ← 여기 (지금의 한 줄 설명 자리)
├─────────────────────────────┤
│ 🌾 평야 │ 🌲 숲 │ ⛰️ 산악 …  │  ← 지대 탭 (그대로)
│                             │
│ 🌻 햇살 언덕      [채집 ⚡10] │
```

- **세로를 안 먹는다.** `gatherSubText()` 가 쓰던 한 줄을 나눠 쓴다 —
  운동 버튼을 방 그림 위에 얹은 것과 같은 판단이다 (`UI_POLICY.md`)
- **누르면 고르기 시트**가 뜬다. 가진 크리처 + 「안 데려감」 한 칸
- 시트에는 크리처마다 **선호 지대 · 특기 · 친밀도 막대**를 보여 준다.
  지금 크리처 카드에는 `💖+2` 밖에 없어서 무엇이 다른지 읽히지 않는다
- 선호 지대 탭에 들어가 있으면 동행 칸에 **표시 하나**를 더 붙인다
  (여기가 그 아이 자리다 → 「두 배」가 켜졌다는 신호)

### 5-2. 채집했을 때

- 덤이 나오면 **토스트를 한 줄로 합친다** — 토스트를 두 번 띄우면 앞것이 잘린다
  (`got_item_food` 를 만들 때 겪은 것과 같다)
  > 🌿 이끼 · 🌿 이끼 — 🦋 반짝 나비가 하나 더 찾았다
- 크리처가 찾아 준 것에는 **그 크리처 이모지**를 붙인다. 안 붙이면
  「원래 두 개 나오는 건가?」로 읽혀서 동행의 효과가 안 보인다
- 카드 튀는 애니메이션(`.pop`)은 그대로 쓴다

### 5-3. 마이 룸 › 크리처 탭

- 지금 동행 중인 카드에 표시 하나 (👣). 여기서도 바꿀 수 있게 한다
- 친밀도 막대를 카드에 얹는다

---

## 6. ⚠️ 다른 시스템과 부딪히는 곳

**여기가 이 문서에서 제일 중요한 부분이다.** 수치 하나가 다른 시스템을 조용히 죽인다.

### 6-1. 음식 확률은 이미 남아돈다 — 꽃개구리의 특기는 체감이 약하다

지금 하루 100번 채집하면 음식이 **12개**쯤 나온다. 음식 하나가 포만감 15~50 이고
하루에 줄어드는 포만감은 **48~120** 이다. 이미 두세 배 남는다.

즉 **폭식은 음식이 없어서 일어나는 것이 아니라 「먹는 것을 잊어서」 일어난다.**
확률을 22%로 올려도 폭식은 그대로 일어난다.

- 하루 30번쯤 채집하는 사람에게만 체감이 있다 (3.6개 ≈ 79 포만감 = 딱 하루치)
- 그러니 이 특기는 **가볍게 하는 사람용**이라고 못 박아 두거나, 다른 것으로 바꾼다

### 6-2. 특별한 재료 배율 → 행운 아우라

`STORY.md` 의 표에서 **특별한 재료 `specials` → 행운 `luck`** 이다.
모래 도마뱀 ×2 · 유니콘 ×3 이면 그 기록이 두세 배 빨리 쌓인다.

카이로스가 **이정표만 노래하는** 방식이라 즉시 폭주하진 않는다 —
다만 이정표에 닿는 속도가 빨라진다. 카이로스를 만들 때 이 배율을 같이 볼 것.

### 6-3. 덤 = 사실상 AP 상한 증가

덤 20%는 「AP 가 20% 늘었다」와 같다. 그런데 AP 충전은 **다이아로 파는 것**이다
(`D.ENERGY.chargeCost = 1000`). 덤을 크게 잡으면 그 상품의 가치가 같이 내려간다.

- 지금 잡은 8~20%(선호 지대 최대 40%)는 「하루 한 번 더 조합할 수 있다」쯤이다
- **여기를 넘기기 전에 다이아 상점 값을 먼저 볼 것**

### 6-4. 소라게의 AP 할인은 다른 축이다

AP 10 → 9 는 **채집 횟수가 11% 늘어나는 것**이라 덤과 곱해진다.
둘 다 센 크리처를 만들면 안 된다 — 소라게는 덤을 기본값으로 둔다.

---

## 7. 세이브 · 번역 · 검사

### 세이브 — **`SAVE_VER` 를 올릴 필요가 없다**

```js
S.pal      = null;   // 동행 중인 크리처 id
S.palBond  = {};     // 크리처별 친밀도
```

둘 다 **없던 칸을 더하는 것**이라 `Object.assign(defaultState(), 저장값)` 이
기본값을 그대로 남긴다. 기본값을 *바꾸는* 것이 아니다 (`CLAUDE.md` 1번 · `EXERCISE.md` 1장).

- `S.pal` 에 **없는 크리처 id 가 남을 수 있다** — 초기화나 데이터 변경 뒤.
  읽는 곳에서 `S.creatures.includes(S.pal)` 로 한 번 거른다
- 새 크리처 레시피는 세이브에 영향이 없다 (`discovered` 에 안 들어간 것은 그냥 미발견)

### 번역

- 크리처 이름 넷 → `NAMES.ko` · `NAMES.en` 양쪽
- 새 UI 문자열: 동행 칸 · 「안 데려감」 · 특기 설명 · 친밀도 · 덤 토스트
- **특기 설명이 제일 길다.** 영어가 더 길어지니 265px 에서 먼저 재 볼 것

### 검사

| 무엇 | 어떻게 |
|---|---|
| 동행에 따라 결과가 **실제로** 달라지는가 | `Math.random` 을 갈아 끼우고 1만 번 돌려 덤 비율을 잰다 (`checktime.js` 가 `Date.now` 를 바꾸는 것과 같은 방식) |
| 덤 확률 상한 | 친밀도 만렙 × 선호 지대 × 최고 크리처에서 1.0 을 안 넘는가 |
| 없는 크리처를 동행 중일 때 | `S.pal` 에 안 가진 id 를 넣고 채집해도 안 죽는가 |
| 고르기 시트 | 평소 `display:none` 이라 검사에서 빠진다 — `checkui.js` 에 화면을 하나 더 넣는다 |
| 레시피 중복 조합 | **지금 없다.** 크리처를 늘리기 전에 만든다 |
| 번역 | `npm run test:i18n` (자동) |

---

## 8. 만드는 순서

**한 번에 다 만들지 않는다.** 1번만 넣고 실제로 재미있는지 먼저 본다.

1. **`S.pal` + `gather()` 의 덤 한 줄.** 효과는 덤 하나뿐, 크리처도 지금 셋 그대로.
   고르는 UI 도 없이 개발용 버튼으로 바꿔 가며 **이게 재미있는지만** 본다
2. 동행 고르기 시트 + 탐험 화면 한 줄
3. 친밀도 (`S.palBond`) · 선호 지대
4. **레시피 중복 조합 검사** → 그다음에 크리처 넷 추가 · 특기
5. 검사기 (위 표) · 문서 · 번역

> 1번에서 「별로다」가 나오면 2번부터는 안 만든다. 그때 다시 볼 것은
> **덤이 아니라 다른 축**이다 — 예를 들어 「그 크리처가 좋아하는 재료가 더 잘 나온다」
> (원하는 재료를 노려서 모을 수 있다 = 조합 목표가 생긴다) 쪽이 더 나을 수 있다.

---

## 9. 안 하기로 한 것

- **크리처 피로도** — 게이트가 두 겹이 된다 (2장)
- **데려나가면 매력이 빠지는 것** — 맵 해금이 왔다 갔다 한다 (2장) ⚠️
- **여러 마리 동행** — 고르는 재미가 사라진다
- **크리처 진화/성장** — 친밀도로 충분하다. 새 그림이 마리당 여러 장 필요해진다
- **마을에서의 동행** — 마을 안에서 할 일이 아직 없다 (`CLAUDE.md` 「아직 안 한 것」)
- **옷 획득 확률** — 그 시스템 자체가 없다. `unlockCosmetic()` 은 있는데
  아무도 안 부른다. 만든다면 **개성 아우라**가 담당하기로 되어 있는 자리다

---

## 10. 아직 안 정한 것

- **꽃개구리의 특기** — 음식은 체감이 약하다 (6-1). 그대로 둘지 바꿀지
- **친밀도 500** — 하루에 몇 번 채집하는 것을 표준으로 볼 것인가에 달렸다
- **동행을 바꾸는 데 제한을 둘 것인가** — 지금 초안은 언제든 무료다.
  「하루에 한 번」 같은 것을 붙이면 고르는 무게가 커지지만, 잘못 고른 것을
  못 되돌리는 답답함도 같이 온다
- **크리처가 화면에 보이는가** — 마이 룸에 이모지로만 있다.
  동행 중인 아이를 채집 화면이나 방 그림에 띄우면 훨씬 살아나지만,
  그림이 마리당 한 장씩 더 필요하다 (`avatar.js` 의 `ROOM_PROPS` 자리에 얹을 수는 있다)
