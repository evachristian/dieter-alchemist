# 텍스트 색상·폰트 정책

화면의 글자가 항상 읽히도록 지키는 규칙. `a11y.js` 의 `checkTextStyle()` 이
이 문서의 규칙을 그대로 코드로 옮겨 놓은 것이라, **정책을 바꾸면 검증기도 같이 고쳐야 한다.**

## 0. 검증하는 법

```
브라우저 콘솔에서
  checkTextStyle()             // 지금 보이는 화면 검사
  checkTextStyle({ all:true }) // 숨겨진 탭·모달까지 강제 검사
  checkLocked()                // 잠긴 콘텐츠가 흐리게 구분되는가 (UI_POLICY 7장)
  checkUI()                    // 모든 언어로 대비 + 레이아웃 + 잠금표현을 한 번에

또는 URL 뒤에 ?a11y=1 을 붙이면 로드 직후 자동 실행
  index.html?a11y=1
```

> `a11y.js` 에는 글자 대비(`checkTextStyle`) · 레이아웃(`checkLayout`) · 잠금 표현(`checkLocked`) 검증이 함께 들어 있다.
> 레이아웃 규칙은 [UI_POLICY.md](UI_POLICY.md) 참고.

위반이 있으면 콘솔에 표로 뜬다 — `선택자 / 텍스트 / 글자색 / 배경색 / 대비 / 크기 / 위반 사유`.
**색이나 글자 크기를 손댔으면 커밋 전에 한 번 돌려서 0건인지 확인할 것.**

---

## 1. 색상 토큰

글자에 쓸 수 있는 색은 아래가 전부다. 새 색을 즉석에서 만들어 쓰지 않는다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `--ink` | `#5a4a55` | **본문·제목·버튼 라벨.** 모든 표면에서 4.5:1 이상 |
| `--ink-soft` | `#6e5d69` | **보조 설명·캡션.** 흰색/크림/파스텔-1 위에서 4.5:1 이상 |
| `--ink-dim` | `#9a8a95` | **비활성·잠금 전용.** 읽어야 하는 글자에는 절대 금지 |
| `--text-pink` | `#9c3566` | 핑크 계열 강조 텍스트 |
| `--text-mint` | `#166b4c` | 민트 계열 강조 텍스트 |
| `--text-lav` | `#4f36a0` | 라벤더 계열 강조 텍스트 |
| `--text-warn` | `#8a5a2b` | 비용·주의 (⚡ 소모량 등) |

### 표면(배경) 토큰과의 조합

파스텔은 **배경 전용**이다. 파스텔-2 (`--pink-2` `--mint-2` `--lav-2`) 는 채도만 높을 뿐
명도가 밝아서, 그 위에 같은 계열의 밝은 색이나 흰색을 얹으면 대비가 1.2~1.8:1 까지 떨어진다.

| | `--card`/`--cream` | 파스텔-1 (`pink`/`mint`/`lav`) | 파스텔-2 (`pink-2`/`mint-2`/`lav-2`) |
|---|---|---|---|
| `--ink` | ✅ 7.8~8.3 | ✅ 6.4~6.6 | ✅ 4.6~5.5 |
| `--ink-soft` | ✅ 5.8~6.1 | ✅ 4.7~4.9 | ❌ 3.4~4.1 — 쓰지 말 것 |
| `--text-*` | ✅ 5.9~8.9 | ✅ 4.5~7.1 | ⚠️ 3.6~4.9 — 큰 글씨만 |
| `#ffffff` | ❌ | ❌ | ❌ **1.5~1.8 — 전면 금지** |

> **파스텔 배경 위에 흰 글씨는 금지.** 예전에 `.btn-primary` `.wr-tab.active`
> `.room-tab.active` `.wr-gift` 가 전부 흰 글씨였고 대비가 1.6:1 이었다. 전부 `--ink` 로 바꿨다.

흰 글씨는 `#14101f` 같은 **어두운 배경(인트로 대사창 등)에서만** 쓴다.

---

## 2. 대비 규칙 (WCAG 2.1 기준)

| 대상 | 최소 대비 |
|---|---|
| 일반 텍스트 | **4.5 : 1** |
| 큰 텍스트 (24px 이상, 또는 18.66px 이상이면서 bold 700+) | **3 : 1** |

- 배경은 **조상 요소까지 거슬러 올라가 합성한 실제 색**으로 계산한다.
  반투명 카드(`rgba(255,255,255,0.5)`) 위 글자도 그래서 정확히 잡힌다.
- `opacity` 로 흐린 글자는 **배경과 섞인 색**으로 계산한다.
  → 흐리게 만들고 싶으면 `opacity` 대신 `--ink-soft` 처럼 **색으로** 낮춘다.
- 그라데이션·이미지 배경, 그리고 **끝까지 불투명한 배경을 못 만나 흰색으로 가정한 경우**는
  `(추정)` 으로 표시된다. 이 경우 눈으로도 한 번 확인할 것.
- **안 보이는 글자는 재지 않는다.** 조상이 투명하면 자기 `opacity` 가 1이어도 화면에 없는 글자다.
  그런 요소는 위반이 아니라 `안보여서건너뜀` 수로 따로 알린다
  (예전에는 페이드 인 도중의 화면이 통째로 `대비 1.00` 위반으로 잡혔다).
- 검사 전에 끝이 있는 애니메이션·트랜지션을 끝 상태로 보낸다. 화면이 자리 잡기 전에 재서
  생기던 허위 위반이 이걸로 없어졌다. → [CLAUDE.md 2장](CLAUDE.md)

---

## 3. 폰트 규칙

| 항목 | 규칙 |
|---|---|
| 패밀리 | `body` 의 `'Noto Sans KR', 'Apple SD Gothic Neo', -apple-system, …` **상속만**. 개별 요소에서 재지정 금지 |
| 웨이트 | **400 / 500 / 700 / 800 / 900** 만. 그 외 값(600 등)은 로드하지 않아 브라우저가 합성해 뭉개진다 |
| 최소 크기 | **11px (0.72rem)**. 그보다 작은 글씨 금지 |

> 인트로/스플래시는 `style.css` 캐시와 무관하게 동작해야 해서 인라인 `<style>` 에
> 폰트 스택을 한 번 더 적어 둔다. 이건 의도된 예외다.

### 자주 쓰는 크기

| rem | px | 쓰임 |
|---|---|---|
| 0.72 | 11.5 | 캡션·배지·탭 라벨 (최소) |
| 0.78~0.85 | 12.5~13.6 | 보조 설명 |
| 0.9~1.0 | 14.4~16 | 본문·버튼 |
| 1.35 | 21.6 | 화면 제목 |

---

## 3-2. 방 배경 위의 글자 — 아웃라인 필수

마이 룸의 방 그림 위에 얹는 글자(시계 등)는 **흰 글자 + 검은 아웃라인**으로 고정한다.
`.on-room-bg` 클래스 하나만 쓰고, 화면마다 따로 색을 고르지 않는다.

**왜 대비 규칙으로는 안 되는가.** 방 배경은 한국시간 기준 시간대에 따라
새벽·낮·노을·밤으로 통째로 바뀌고, 한 화면 안에서도 벽·바닥·창문·햇살이 다 다른 색이다.
**어떤 색을 골라도 어느 시간대·어느 자리에서는 묻힌다.** 게다가 배경이 SVG 그림이라
검증기가 배경색을 읽을 수도 없다 — 그대로 재면 페이지 그라데이션(분홍)을 배경으로
잡아 흰 글자가 1.6:1 로 나온다. **실제로 안 보이는 게 아니라 잴 수 없는 것**이다.

그래서 이 자리에서는 대비 대신 **아웃라인이 가독성을 보장한다.** 배경이 무엇이든
글자 가장자리에 검은 테두리가 있으면 형태가 읽힌다.

```css
.on-room-bg {
  color: #fff;
  text-shadow: 1px 1px 0 rgba(0,0,0,0.92), -1px 1px 0 rgba(0,0,0,0.92),
               1px -1px 0 rgba(0,0,0,0.92), -1px -1px 0 rgba(0,0,0,0.92),
               1px 0 0 rgba(0,0,0,0.92), -1px 0 0 rgba(0,0,0,0.92),
               0 1px 0 rgba(0,0,0,0.92), 0 -1px 0 rgba(0,0,0,0.92),
               0 0 4px rgba(0,0,0,0.55);
}
```

- **`-webkit-text-stroke` 를 쓰지 않는다.** 획을 글자 안쪽으로도 먹어 들어가
  작은 글씨가 뭉갠다. 8방향 `text-shadow` 는 바깥으로만 번진다
- 마지막 `0 0 4px` 는 흐림만 주는 그림자다. 테두리 노릇은 못 하고 밝은 배경에서
  살짝 더 떠 보이게 하는 보조다 — **방향 수에 넣지 않는다**

**검증기가 이 정책을 강제한다.** `.on-room-bg` 안의 글자는 대비 검사에서 빼는 대신,
아웃라인이 **실제로 붙어 있는지**를 본다(서로 다른 방향 4개 이상). 없으면 위반이다.
그냥 예외로 빼기만 하면 아웃라인을 깜빡한 순간 아무도 못 잡는다.

```js
checkTextStyle()   // "방 배경 위인데 아웃라인이 없음 (테두리 방향 0개, 최소 4개)"
```

새로 방 배경 위에 글자를 올릴 때는 `.on-room-bg` 를 붙이기만 하면 된다.
정책 상수는 `a11y.js` 의 `roomBgSelector` · `outlineMinDirections`.

---

## 4. 검사 예외

아래는 검증에서 제외한다. 다른 이유로 예외를 늘리려면 **여기에 근거를 적고** 검증기의
`skipSelector` 도 같이 고칠 것.

| 예외 | 근거 |
|---|---|
| `.i-dot` `svg` `.avatar-svg` `.brew-emoji` `.spot-emoji` | 장식 그래픽 — 읽는 글자가 아님 |
| **이모지만 있는 칸** (예: 재료 구멍의 `🌿`) | 이모지는 색을 스스로 가진 그림 글자라 CSS `color` 가 적용되지 않는다. 대비를 재도 의미가 없다. 선택자가 아니라 **글자 내용**으로 판정한다(`pictogramOnly()`) — 새 이모지 UI 를 만들 때마다 예외를 추가하지 않아도 된다.<br>단 `✕` `→` `?` 처럼 **단색 기호**는 색이 먹으므로 그대로 검사한다 |
| `.wr-tab.dim` `[disabled]` `[aria-disabled=true]` | 완전 비활성 컴포넌트. WCAG 도 inactive 요소는 대비 요건에서 제외한다 |
| `[data-a11y-skip]` | 개별 예외 표시용 |

**잠긴 요소(`.locked`)는 예외가 아니다.** 컨텐츠 언락으로 잠긴 것은 채도를 낮추고
살짝 투명하게 처리하지만([UI_POLICY 7장](UI_POLICY.md)), 흐려진 뒤에도 대비 4.5:1 을
지켜야 한다 — 잠긴 카드에서 사용자가 읽어야 하는 정보가 바로 "언제 열리는가" 이기 때문이다.
그래서 잠긴 요소의 글자는 오히려 `--ink` 로 진하게 잡는다.

단, **잠겼다는 사실 자체는** 흐린 색이 아니라 자물쇠 아이콘(`.wr-lock`) 처럼
색에 의존하지 않는 수단으로 전달해야 한다.

---

## 5. 이 정책을 만들 때 실제로 고친 것 (63건 → 0건)

| 문제 | 원인 | 조치 |
|---|---|---|
| 레시피 `0 / 8` 배지가 안 보임 | 연녹색 배경(`--mint`)에 녹색 글씨(`--mint-2`) → **1.21:1** | `--text-mint` 로 교체 |
| 보조 텍스트 전반이 흐림 | `--ink-soft` 가 `#9a8a95` → 흰 배경에서 3.26:1 | `#6e5d69` 로 어둡게 (6.12:1) |
| 버튼·활성 탭 라벨 | 파스텔 위 흰 글씨 → 1.6:1 | 전부 `--ink` |
| 채집 `채집` 링크 | `--pink-2` 글씨 → 1.59:1 | `--text-pink` |
| `⚡10` 비용 | `#d98a3a` → 2.72:1 | `--text-warn` |
| 마법진 빈 슬롯 `+` | 어두운 배경 위 금색 → 1.92:1 | 밝은 금색 + 배경 더 어둡게 |
| AP 숫자·시계·아이템 이름 | 0.66~0.68rem (10.6~10.9px) | 0.72rem 로 상향 |
| 잠긴 레시피 행 | `opacity: 0.62` 로 흐려서 대비 미달 | 투명도 대신 배경/색으로 구분 |
| 재료 구멍의 빼기 배지 `✕` | `--pink-2` 위 흰 글씨 → **1.55:1** | 배경을 `--text-pink` 로 (6.74:1) |
| 잠긴 채집지·솥·옷 글자 | 흐리게 처리하면서 `--ink-soft`/`--ink-dim` 을 써 4.5:1 미달 | `--ink` 로 올리고 opacity 를 0.8 로 제한 (4.65:1) |
