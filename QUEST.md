# 퀘스트 시스템 — 기획

**5단계까지 만들었다.** 칩 · 진행도 · 보상 · 컷씬 · 스토리 다시보기 ·
비법서 장 배분 · **요리사 클레멘(부엌)** · **키워드와 마을 해금** · **NPC 여섯 배치**.
**아직 안 만든 것은 6단계다** — 거울 정령 · 엔딩.
**5단계는 끝났다** — 자리 · 대사 · 키워드 · 호감도 · 「주는 것」까지.
10장의 순서표에 어디까지 왔는지 적어 두었다.

### 1단계에서 기획과 달라진 것 하나

⚠️ **누적형의 `base`(시작할 때 값)를 안 쓴다.** 원래는 `S.record` 를 보고 시작
시점의 값을 빼려고 했는데, **그 일이 일어나는 자리에서 직접 세는 쪽**(`questBump`)이
더 간단하고 안전했다 — 「받은 뒤부터」가 저절로 지켜지고 기억할 값도 없다.
`S.quest.n` 하나가 지금 퀘스트의 진행 수다. `checkui` 가 `record.brews` 를 999 로
심어 놓고 **진행이 0인지**를 본다 (사보타주로 확인했다: `record` 를 보면 즉시 2/2 가 된다).

관련 문서 — 이야기는 `STORY.md`, 화면 규칙은 `UI_POLICY.md`, 비법서는 `UI_POLICY.md` 3-2d.

---

## 0. 왜 만드는가

지금 이 게임에는 **「다음에 뭘 하지」를 말해 주는 것이 없다.**

- 채집·조합·꾸미기·밭이 다 돌아가는데, 그걸 **왜** 하는지는 화면 어디에도 없다
- `STORY.md` 에 세계관·인물 여섯·3막이 다 있는데 **코드는 한 줄도 없다**
- 비법서의 장 배분(`PAGE_TIERS`)이 **매력 단계로 자동 지급**인데, 이건 임시 발판이다 —
  「요정 대모가 줬다」는 문장이 없이 서른 장이 툭 들어온다

퀘스트는 이 셋을 한꺼번에 맡는다. **이야기를 나르는 그릇이자, 다음 할 일이자,
비법서 장이 나오는 자리**다.

## 1. 제일 중요한 원칙 — 숙제가 되면 진다

본받는 모델이 **듀오링고**다. 누구나 쉽게 적응해야 한다.

| | |
|---|---|
| **기한이 없다** | 며칠 뒤에 들어와도 그대로 기다린다. 「놓쳤다」가 없다 |
| **실패가 없다** | 진행도는 뒤로 안 간다. 포기 버튼도 안 만든다 |
| **한 번에 하나** | 목록이 생기는 순간 그건 할 일 목록이고, 할 일 목록은 숙제다 |
| **길을 알려 준다** | 「무엇을」만 말하지 않고 **「어디서」**까지 말한다 (비법서와 같은 규칙) |

> ⚠️ **코지 게임에서 제일 위험한 것은 「해야 할 것이 쌓여 있다」는 느낌이다.**
> 퀘스트를 여러 개 동시에 주면 그 느낌이 곧바로 생긴다. 조건이 여럿 차면
> **큐에 쌓아 두고 하나씩** 내보낸다.

---

## 2. 화면

### 2-1. 퀘스트 칩 — 하단에 뜨는 NPC 얼굴

듀오링고의 「불붙은 짝 맞추기」 칩과 같은 자리. **탭 바 바로 위, 오른쪽.**

```
┌────────────────────────────────────┐
│                                    │
│           (화면 내용)               │
│                                    │
│                        ╭─────╮     │
│                        │ 🧚  │①    │  ← NPC 초상 + 진행도 링
│                        ╰─────╯     │
├──────┬──────┬──────┬──────────────┤
│ 마이룸│ 공방 │ 탐험 │ 랭킹         │
└──────┴──────┴──────┴──────────────┘
```

- **누가 주는 퀘스트인지가 얼굴로 보인다.** 요정 대모면 요정 대모, 클레멘이면 클레멘.
  `Portrait.bust()` 를 그대로 쓴다 (이미 인물 열 명의 초상이 있다)
- **진행도는 얼굴 둘레의 링**으로 두른다. 숫자를 읽지 않아도 얼마나 남았는지 보인다
- **모든 탭에서 보인다.** 퀘스트는 특정 화면의 것이 아니다

상태가 셋이고 **셋이 눈에 띄게 달라야 한다**:

| 상태 | 표현 | 누르면 |
|---|---|---|
| **새 퀘스트** | 반짝임 + `!` 배지 | 인트로 컷씬 → 수락 |
| **진행 중** | 링이 차 있음 | 지금 무엇을 얼마나 했는지 · 어디로 가면 되는지 |
| **완료** | 링이 꽉 참 + 붉은 배지 | 완료 컷씬 → 보상 |

> ⚠️ **칩 밑에 «갇히는» 것이 없어야 한다.** 스크린샷의 그 자리는 **옷장 칸 위**다.
> 칩은 떠 있으니 지금 무언가를 덮는 것은 정상이다 — 밀어 내리면 비켜난다.
> 진짜 문제는 **맨 아래까지 내려도 덮여 있는 것**이고, 그건 아무리 스크롤해도
> 못 누르는 버튼이다. 그래서 `body.has-quest` 가 화면 아래를 칩 높이만큼 비운다.
>
> **검사는 수치로 한다** — 비운 `padding-bottom` 과 「화면 바닥에서 칩 위까지」를
> 견준다. ⚠️ **눌러 보는 것만으로는 못 잡았다**: 지금은 맨 아래에 개발용 블록이
> 있어서 여유가 남아 돌고, 자리를 안 비우는 사보타주가 **통과했다.**
> 그 블록이 사라지는 날 조용히 깨질 검사였다.

> ⚠️ **퀘스트가 없을 때는 칩도 없다.** 회색으로 남겨 두면 「내가 뭘 안 한 건가」가 된다.

### 2-2. 컷씬

**초상화 + 말풍선.** 튜토리얼 다이얼로그와 **같은 부품을 쓴다** (`STORY.md` 「화면」).

```
┌────────────────────────────────────┐
│                                    │
│              ╭──────╮              │
│              │ 🧚   │              │  ← Portrait.bust(sp, mood)
│              ╰──────╯              │
│                                    │
│  ┌──────────────────────────────┐  │
│  │ 요정 대모                     │  │
│  │ 프린세스, 이걸 받아요.        │  │
│  │ 어머니가 남긴 비법서예요.     │  │
│  │                    ● ● ○  ▾  │  │
│  └──────────────────────────────┘  │
└────────────────────────────────────┘
```

- ⚠️ **손으로 그린 SVG 장면(`intro.js` 방식)을 퀘스트마다 만들지 않는다.**
  인트로 한 편에 장면 열넷 × SVG 함수가 들어갔다. 퀘스트가 스무 개면 그 방식은
  **끝나지 않는다.** 큰 그림은 **막이 바뀌는 자리에만** 쓰고, 나머지는 초상화로 간다
- 컷씬은 퀘스트마다 **둘까지**: 받을 때(`in`) · 낼 때(`out`). `out` 은 없어도 된다
- **인트로는 칩을 «처음» 누를 때만.** 진행을 확인하려고 누를 때마다 대사가 다시 나오면
  그건 방해다 — 두 번째부터는 바로 시트가 열린다
- **완료 컷씬이 먼저, 보상이 나중.** 보상 토스트가 마지막에 남아야 「받았다」로 끝난다.
  ⚠️ 컷씬이 도는 동안 또 누를 수 있으므로 `claiming` 으로 한 번만 지나가게 막는다
- ⚠️ **없는 표정을 쓰면 조용히 기본 얼굴이 된다.** 오류도 안 나고 화면도 멀쩡하다 —
  `checkdata` 가 `SPEAKERS` 의 `moods` 와 대조한다
- **말풍선의 이름은 「부르는 말」이다** (`speakerName()`) — 공주는 플레이어가 지은 이름으로,
  요정 대모는 「요정 대모」로 뜬다 (`STORY.md` 「호칭 규칙」)

### 2-3. 스토리 다시보기 (설정)

설정에 버튼을 하나 더한다 — 지금 `튜토리얼 인트로 다시보기` 바로 아래.

```
⚙️ 설정
  …
  [ 📖 스토리 다시보기 ]     ← 누르면 시트가 열린다

  ┌ 스토리 다시보기 ────────────────┐
  │ 1막 · 성을 나서다               │
  │   🧚 어머니의 비법서            │
  │   🧚 첫 물약                    │
  │   🍳 부엌의 불빛      ← 클레멘  │
  │ 2막 · 사람들 사이로             │
  │   🔒 아직 못 본 이야기 · 4      │
  └─────────────────────────────────┘
```

- **본 것만 보여 준다.** 안 본 컷씬을 제목까지 늘어놓으면 스포일러다 —
  「아직 못 본 이야기 · 4」로 **개수만** 알려 준다.
  **아직 아무것도 못 본 막은 통째로 안 내놓는다** — 막 제목만으로도 스포일러다.
  `checkui` 가 «못 본 컷씬의 제목이 화면 글자에 섞여 있는가»를 직접 찾는다
- 막으로 묶는다. `STORY.md` 의 3막이 그대로 소제목이 된다
- 다시 보기는 **보상을 다시 주지 않는다.** 컷씬만 재생한다

---

## 3. 데이터

### 3-1. 퀘스트 한 줄

```js
const QUESTS = [
  {
    id: 'q_book',                 // 세이브에 남는 열쇠 — **절대 바꾸지 않는다**
    npc: 'sp_althea',             // 칩에 뜰 얼굴 (SPEAKERS 의 id)
    act: 1,                       // 스토리 다시보기의 묶음
    at: { tier: 0, charm: 0 },    // 트리거 — 4장
    goal: { kind: 'brew', id: 'vitality', n: 3 },
    reward: {
      pages: ['potion:basic'],    // **비법서 장** — 지금의 PAGE_TIERS 를 여기가 가져간다
      crystal: 50,
      items: { dew: 5 },
    },
    cut: { in: 'q_book_in', out: 'q_book_out' },   // 컷씬 문구 열쇠
  },
];
```

- **`id` 는 세이브에 들어간다.** 옷·크리처 id 와 같은 규칙 — 한 번 나가면 안 바꾼다
- **`npc` 는 초상화 표(`SPEAKERS`)를 가리킨다.** 새 인물은 거기 한 줄만 늘리면 된다
- 이름·설명·대사는 전부 `i18n.js` 로 (`q_book_name` · `q_book_desc` · `q_book_in_1`…).
  **ko·en 둘 다** — `checki18n` 이 짝을 본다

### 3-2. 세이브

```js
quest: {
  active: null,      // 지금 하는 것 하나 (id 또는 null)
  n: 0,              // 지금 퀘스트의 진행 수 — **받은 뒤부터 센다** (아래 4-2)
  done: [],          // 마친 것 (순서대로)
  queue: [],         // 조건은 찼는데 아직 안 내보낸 것
},
seenCuts: [],        // 본 컷씬 (스토리 다시보기 목록 · 2단계)
```

**전부 없던 칸을 더하는 것이라 `SAVE_VER` 를 안 올려도 된다** —
`Object.assign(defaultState(), 저장값)` 이 빈 값을 그대로 남긴다
(`diary` · `petField` 와 같은 경우다).

---

## 4. 트리거

### 4-1. **`charmPeak()` 으로 판정한다**

> 트리거는 「**단계 N · 매력 총합 M 을 처음 달성한 시점**」이다.
> 매력 총합은 관리하면 오르고 안 하면 내려간다.

⚠️ **이 프로젝트에서 같은 사고를 세 번 냈다** — 맵 해금 · 밭 탭 · AP 상한.
전부 `totalCharm()`(지금 총합)을 보다가, **애착 크리처를 약한 것으로 바꾼 순간
열려 있던 것이 다시 잠기는** 버그였다.

**한 번 열린 퀘스트는 무슨 이유로도 닫히지 않는다.**

```js
function questReady(q) { return charmPeak() >= q.at.charm; }
```

- 매력이 내려가도 **이미 받은 퀘스트는 그대로 진행된다**
- 매력이 내려갔다 다시 올라도 **같은 퀘스트를 두 번 주지 않는다** (`done` 을 본다)

### 4-2. ⚠️ 누적형은 **받은 뒤부터** 센다

`S.record` 에는 이미 카운터가 여럿 있다 (`brews` · `drinks` · `creatures` · `raids`…).
그걸 그대로 보면 **이미 40번 조합한 사람에게 「5번 조합」을 주는 순간 완료**된다.
이야기가 통째로 스킵된다.

**그래서 `record` 를 안 본다.** 그 일이 일어나는 자리에서 `questBump()` 를 부르고,
`S.quest.n` 하나에 쌓는다 — 「받은 뒤부터」가 저절로 지켜지고 기억할 값이 없다.

```js
// 조합이 끝나는 자리에서
questBump('brew', result.id);
// 진행도는 그냥
S.quest.n
```

지금 걸어 둔 자리: 조합(물약·크리처) · 물약 마시기 · 채집.
**새 목표 종류를 만들면 그 일이 일어나는 자리에 `questBump` 를 한 줄 넣는다.**
`checkui` 의 「퀘스트완료」가 `record.brews` 를 999 로 심어 놓고 진행이 0인지 본다.

---

## 5. Objective (`goal`)

### 5-1. 종류

| `kind` | 뜻 | 세는 법 | 예 |
|---|---|---|---|
| `brew` | 특정 물약을 N개 **만든다** | 누적 | 생기 물약 3개 |
| `creature` | 특정 크리처를 N마리 **만든다** | 누적 | 불씨 도롱뇽 2마리 |
| `gather` | 특정 재료를 N개 **모아 온다** | 보유 (낼 때 **소모**) | 이슬풀 10개 |
| `drink` | 물약을 N개 **마신다** | 누적 | 아무 물약 5개 |
| `charm` | 매력 N 에 닿는다 | `charmPeak()` | 매력 35 |
| `visit` | 특정 맵에서 N번 채집한다 | 누적 | 햇살 언덕 5번 |
| `exercise` | 운동 N분 | 누적 (`record.exMin`) | 30분 |
| `farm` | 밭에서 N번 거둔다 | 누적 | 3번 |
| `kitchen` | 부엌에 N번 간다 | 누적 | 3번 |

### 5-2. ⚠️ 누적형을 기본으로 한다

**보유형(`gather`)은 진행도가 뒤로 갈 수 있다.** 「물약 3개 보유」로 두면 하나 마시는
순간 2/3 이 된다 — **1번 원칙(진행도는 뒤로 안 간다)을 어긴다.**

그래서 「만든다·마신다」는 전부 **누적**이고, **`gather` 하나만 보유형**이다.
`gather` 는 「갖다줘」라서 낼 때 소모하는 것이 자연스럽고,
**모자라면 완료 버튼이 회색으로 남을 뿐** 진행도가 뒤로 가진 않는다.

### 5-3. 어디로 가면 되는지까지 말한다

진행 중인 칩을 누르면 나오는 시트에 **비법서와 같은 안내**를 넣는다:

```
🧚 어머니의 비법서
   생기 물약을 3개 만들어 오세요.        2 / 3
   ─────────────────────────────────
   🌿 이슬풀 ×1   포근 평야 지대 · 햇살 언덕  ✅
   🍓 산딸기 ×1   울창 숲 지대            🔒 매력 38
```

`ingSource()` · `bestMapFor()` 가 이미 있다 — **비법서에서 만든 부품을 그대로 쓴다.**

> **길 잃음 방지가 제일 중요하다** (`STORY.md`). 무엇을 해야 할지 알아도
> 어디로 갈지 모르면 게임이 그 자리에서 멈춘다.

---

## 6. 보상 — **비법서 장이 여기서 나온다**

지금 `PAGE_TIERS`(매력 단계마다 자동 지급)는 **임시 발판**이라고 적어 두었다.
퀘스트가 그 자리를 가져간다.

```
지금:  매력 35 → 요정 단계 → 중급 물약 25장이 툭 들어온다
나중:  요정 대모가 「이걸 해 오면 다음 장을 주마」 → 해 오면 → 장을 준다
```

- 갈아 끼우는 자리는 **`pagesForTier()` 한 곳**이다 (그러라고 그렇게 잘라 두었다)
- 보상 종류: **비법서 장** · 현자의 결정 · 재료 · 옷 · (나중에) 키워드
- ⚠️ **키워드는 보상이 아니다** (`STORY.md`). 키워드는 이야기의 문이고,
  물건은 호감도·퀘스트가 준다. 섞으면 대화를 뒤지는 것이 파밍이 된다

### ⚠️ 넘어가는 길을 막지 않는다 — **퀘스트가 먼저, 단계 지급이 그물**

퀘스트가 비법서 장의 **유일한** 출구가 되면, 퀘스트 하나가 막히는 순간
게임 전체가 멈춘다. 그래서 같은 묶음을 **두 길**이 준다:

| | 주는 것 | 매력 |
|---|---|---|
| (자동) | 기초 물약 6 — **시작 밑천** | 0 |
| `q_first` → 그물 | 하급 물약 24 | 0 → 15 |
| `q_walk` → 그물 | 크리처 기초 12 | 6 → 35 |
| `q_bring`·`q_egg` → 그물 | 중급 물약 50 | 14·22 → 60 |
| `q_sip`·`q_bloom` → 그물 | 크리처 중급 12 · 상급 26+6 | 32·45 → 100 |

퀘스트를 하면 빠르고, 안 해도 **느릴 뿐 못 하게 되진 않는다.**

⚠️ **기초 물약 여섯 장은 그대로 자동이다.** 첫 퀘스트가 「생기 물약을 만들어라」인데
그 장이 없으면 시작조차 못 한다.

⚠️ **그물이 퀘스트보다 먼저 오면 안 된다.** 먼저 주면 퀘스트를 깨도 들어오는 장이
0이고 「받았는데 아무 일도 안 일어난다」가 된다. `checkdata` 가 둘의 순서를 붙들고,
보상 줄은 **아직 없는 장만** 센다 (미루다 그물이 먼저 준 경우에 거짓말을 안 하려고).

`checkdata` 가 같이 보는 것 하나 더 — **136장이 다 나오는가.** 어느 길로도 못 얻는
장이 있으면 그 레시피는 게임 안에 있는데 «영영 못 만드는» 것이 된다.

---

## 7. 이야기를 어떻게 나르는가

`STORY.md` 의 3막이 그대로 퀘스트의 등뼈다. **막이 곧 스토리 다시보기의 묶음이다.**

| 막 | 누가 | 대략 | 여는 것 |
|---|---|---|---|
| **1막 · 성 안** | 요정 대모(알테이아) | 튜토리얼 직후 ~ 매력 35 | 비법서 하급·중급 · 크리처 |
| **1막 · 부엌** | 요리사 클레멘 | 매력 15~ | 「혼자 먹은 밤」이 **함께한 식사**가 된다 |
| **2막 · 사람들 사이로** | 마을 NPC 넷 | 매력 35~100 | 마을 · 키워드 |
| **3막 · 거울 앞** | 거울 정령(유타르크) | 매력 100~ | 엔딩 |

`STORY.md` 의 「만드는 순서」와 같다: **요리사 → 튜토리얼 → 키워드 → NPC 넷 → 거울 정령.**

---

## 8. ⚠️ 이 설계의 약한 곳

솔직하게 적어 둔다. 만들기 전에 이걸 먼저 봐야 한다.

### 8-1. 대사 분량이 곧 작업량이다

초상화 열 명은 이미 있다. **없는 것은 글이다.**

```
퀘스트 20개 × 컷씬 2개 × 대사 4줄 × 2언어 = 320줄
```

`i18n.js` 가 지금 744줄인데 여기에 320줄이 더 붙는다.
**한 번에 다 쓰지 않는다** — 1막 다섯 개부터.

### 8-2. 퀘스트가 「해야 할 일」로 읽히는 순간 진다

칩이 늘 붉은 배지를 달고 있으면 그건 알림이지 초대가 아니다.
- 완료 배지는 **낼 것이 있을 때만**
- 진행 중에는 배지 없이 링만
- **퀘스트가 없으면 칩도 없앤다**

### 8-3. 매력이 「내려간다」는 설정이 아직 게임에 없다

트리거를 `charmPeak()` 으로 잡는 이유가 「매력이 내려갈 수 있어서」인데,
**지금 게임에서 매력이 내려가는 길은 애착 크리처를 바꾸는 것뿐이다**
(`totalCharm = beauty + charm + petBonus`, 앞의 둘은 안 줄어든다).

「관리 안 하면 내려간다」를 진짜로 만들려면 **방치 감소가 매력에도 걸려야** 하는데,
그건 코지 게임에서 위험한 축이다 — 며칠 못 들어왔다고 되돌아가면 다시 안 켠다.
**이 결정은 따로 해야 한다.** 퀘스트는 어느 쪽이든 `charmPeak()` 이라 안전하다.

### 8-4. 한 번에 하나 = 콘텐츠 소비가 느리다

큐에 다섯이 쌓여 있어도 하나씩 나온다. 잘 하는 플레이어는 「왜 안 주지」가 될 수 있다.
→ **완료하자마자 다음 것을 바로 내보낸다.** 하루 제한 같은 걸 두지 않는다.

### 8-5. 스토리 다시보기는 **보상 재지급 사고**가 나기 쉽다

컷씬 재생과 퀘스트 완료 처리가 한 함수에 있으면, 다시보기가 보상을 또 준다.
**컷씬 재생(`playCut`)과 보상 지급(`claimQuest`)을 처음부터 갈라 놓는다.**

---

## 9. 검사 계획

만들면서 같이 넣을 것. **각 항목은 사보타주로 「진짜 잡는지」 확인한다.**

| 검사 | 무엇을 | 어디에 |
|---|---|---|
| **트리거가 peak 인가** | 매력을 올렸다 내린 뒤에도 퀘스트가 살아 있는가 | `checkui` |
| **누적형의 base** | `record` 가 이미 큰 세이브에 퀘스트를 줘도 즉시 완료가 아닌가 | `checkui` |
| **한 번에 하나** | 조건이 셋 다 찼을 때 칩이 하나만 뜨는가 | `checkui` |
| **칩이 안 가린다** | 칩 자리에서 `elementFromPoint` 가 칩을 주는가 · 밑에 눌러야 할 것이 없는가 | `checkui` |
| **표 무결성** | 없는 NPC·재료·크리처·보상 id · 빠진 컷씬 열쇠 | `checkdata` |
| **문구 짝** | 컷씬·이름·설명의 ko/en | `checki18n` (이미 있다) |
| **다시보기가 보상을 안 준다** | 컷씬을 다시 재생해도 결정·장이 안 늘어나는가 | `checkui` |
| **퀘스트를 끝까지 눌러 본다** | 받기 → 목표 → 완료 → 보상 | `checkquest`(새 파일 · `checktut` 과 같은 방식) |
| **키워드 그래프가 안 막히는가** | 도달 불가능 · 죽은 키워드 · 순환 · 막다른 진행 | `checktalk` ✅ |
| **키워드를 끝까지 물어본다** | 부엌 → 굴뚝 → 사과밭 → 골짜기 → 쉼터 → 덤불 · 다시 물어도 두 번 안 준다 | `checkask` ✅ (`npm run test:ask`) |
| **호감도에 매력이 안 섞이는가** | 매력 999 로도 호감도가 0 인가 · 물약이 사라지는가 · 답례가 한 번인가 | `checkbond` ✅ (`npm run test:bond`) |

---

## 10. 만드는 순서

**1번만 끝나도 게임이 달라진다.** 컷씬 없이도 「다음에 뭘 하지」가 생긴다.

1. ~~**그릇**~~ ✅ — `QUESTS` 표(요정 대모 여섯) · 세이브 칸 · 칩 · 진행도 링 ·
   완료/보상 · 일지 한 줄. 시트에 NPC 한 마디가 들어가 있어 컷씬 전에도 말은 한다
2. ~~**컷씬**~~ ✅ — 초상화 + 말풍선(`playCut`) · `seenCuts` · 설정의 **스토리 다시보기**.
   1막 컷씬 열둘(퀘스트마다 인트로/완료) · 대사 스물여섯 줄 × 두 언어
3. ~~**1막 요정 대모 여섯** — 비법서 배분 이관~~ ✅ — 장은 이제 **퀘스트가 먼저**
   주고, 단계 지급이 **한 단계 늦게** 같은 묶음을 한 번 더 준다(그물)
4. ~~**요리사 클레멘**~~ ✅ — 부엌이 열리고 「혼자 먹은 밤」에 갈래가 생겼다.
   ⚠️ **부엌은 퀘스트와 상관없이 튜토리얼 직후부터 열린다** — 「혼자 먹은 밤」은
   지금까지 **피할 방법이 없는 페널티**였고(판정 기준이 「혼자 먹었느냐」인데 같이
   먹을 사람이 없었다), 그 해소를 «선택 콘텐츠»인 퀘스트 뒤에 숨기면 안 된다.
   · **아무것도 안 든다** — AP 도 재료도 결정도. 「온기만이 등가 교환의 밖에
     있다」(`STORY.md`)가 이 인물의 전부라, `checkui` 가 **0 깎였는지를 수치로** 잰다
   · 부엌에 간 날은 그 밤이 혼자가 아니다. 여러 밤을 정산할 때는 **그 밤 하나만** 뺀다
   · **시계를 옮겨 놓지 않으면 확인할 방법이 없다** — 「어제 갔고 오늘 날이 바뀌었다」를
     실제로 만들어야 갈래가 도는지 알 수 있다. `npm run test:time` 이 넷을 본다
5. ~~**마을 NPC 넷 + 키워드**~~ ✅ — 마을이 껍데기를 벗었다.
   키워드 열 · 물어볼 것 스물일곱 · **마을 다섯이 키워드로 열리고 NPC 여섯이 앉았다**
   · **시작은 부엌이다.** 마을이 전부 잠겨 있으니 시작 키워드(「정신적 허기」)를
     들고 갈 데가 클레멘밖에 없다 — 그가 첫 마을을 열어 준다.
     ⚠️ 그래서 `S.keywords` 의 기본값이 **빈 배열이 아니다**
   · **점수로 두 번 잠그지 않는다** (`STORY.md` 「마을 해금」). `isVillageOpen()` 은
     `S.villages` 만 본다 — 매력 조건을 여기에 또 걸지 않는다
   · **「물어볼 것」 줄은 말풍선 바로 밑이다.** 무대(그림+버튼) 아래로 내렸더니
     390px 화면에서 밀려 나갔다 — 지금은 이 줄이 이야기를 미는 «주된» 조작이다
   · **다시 물어도 된다.** 이미 물어본 것은 회색이 될 뿐 사라지지 않는다 —
     사라지면 「아까 뭐라고 했더라」를 확인할 방법이 없다. 주는 것만 한 번이다
   · **갈 곳만 알려 준다** — 새로 물어볼 것이 있는 마을 탭·건물·부엌 버튼에 점(●).
     무엇을 물을지는 안 알려 준다 (「길 잃음 방지가 제일 중요하다」)
   · **내보일 마을은 「건물이 있는 곳」이다.** 앞에서 몇 개를 잘라 내는 방식은
     목록 순서와 채운 순서가 어긋나는 순간 막힌다 — 가시덤불 마을을 채우려다
     안이 텅 빈 마을 둘이 자물쇠로 딸려 오는 것을 보고 바꿨다
   · 사슬은 거울 골짜기에서 **둘로 갈린다** — 유타르크가 「암살 의뢰」를 흘리면
     카이로스가 사냥꾼 쉼터를 열고, 슈타르크가 「왕자」를 주면 오릭스(문을 단 사람)가
     가시덤불 마을을 연다
   · **호감도 5단계** — 마을 NPC 여섯에게 **물약을 만들어 주면** 오른다.
     ⚠️ 매력·비주얼이 한 톨도 안 섞인다 (`checkbond` 가 999 로 올려 놓고 잰다)
   · **그들이 「주는 것」** — STORY 의 담당 표 그대로 상시 효과가 붙는다.
     ⚠️ **다섯 다 이미 있는 함수 한 곳에 곱해 붙인다** — 그래서 셋은 화면에
     저절로 보인다 (채집 버튼의 ⚡ · 운동 팝업의 단련 · 물약의 아우라)
   · **호감도가 이야기를 민다** (`ASKS` 의 `need: { bond: 3 }`) — 3단계가 되면
     여섯 줄이 더 열리고 그중 넷이 2막 키워드(🔮 유리관 · 🔒 엄마의 봉인)를 준다.
     재료로만 갚으면 호감도는 상점의 다른 얼굴일 뿐이다 (`STORY.md` 「신뢰에서야
     털어놓는 말」). 둘은 **아무것도 안 준다** — 털어놓는 것 자체가 값이다
     ⚠️ **잠긴 줄은 감추지 않고 🔒 로 보인다.** 대신 **길잡이 점에는 안 센다** —
     점을 따라갔는데 못 여는 것뿐이면 점이 거짓말이 된다
     ⚠️ **클레멘에게는 못 건다** (호감도가 없다) — `checktalk` 이 잡는다
6. **거울 정령 · 엔딩** — 지금 유타르크는 거울 골짜기에서 **답을 피하기만** 한다.
   「아름다움」에 답하는 것은 최후의 순간 딱 한 번이다 (`STORY.md` 「최후」)

---

## 11. 이 문서를 고쳤으면

- 데이터 모양을 바꿨으면 `tools/checkdata.js` 도 같이 본다
- 화면 규칙을 바꿨으면 `UI_POLICY.md` 에 항을 만든다 (여기는 기획, 거기는 규칙)
- 이야기 쪽은 `STORY.md` 가 원본이다. **대사와 인물은 거기서 뽑아 온다**
