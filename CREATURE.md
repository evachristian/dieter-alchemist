# 크리처 — 기획 초안 (2판)

크리처를 **키우고 · 데리고 나가고 · 재료로 되돌리는** 시스템.

> **1~6단계는 만들었다** (매력 무한 누적 제거 · 애착 크리처 한 마리 · 크리처 30종 · SVG 그림 ·
> 도감과 속성 탭 · 맵 51곳의 속성 · 동행과 덤 · 날씨 · 시간대 · 히든 등급 · 채집 단서 ·
> 먹이와 로열티·버프 · 어항).
> **남은 것은 크리처를 재료로 녹이기·생산·전투(7~9단계)다.**
> 아래에서 만든 부분은 「만들었다」로 표시했고, 나머지는 여전히 **초안**이다.
> 코드와 어긋나면 코드가 맞다. 수치는 전부 **임시값**이고, 왜 그 값인지를 표 아래에 적었다.
> 「지금 이렇게 되어 있다」는 코드를 읽고 확인한 사실이다.
>
> **1판(동행만 있던 것)에서 바뀐 것**: 크리처가 매력을 무한히 쌓던 것을 없앤다.
> 마이 룸에 두는 것은 **한 마리**뿐이고, 그 한 마리의 수치만 반영된다.

---

## 한 줄

```
가마솥에서 태어난다 → 마이 룸에 한 마리를 둔다(매력) → 다른 한 마리를 데리고 나간다(채집)
      ↑                        │                              │
      └── 남는 개체는 재료로 ──┘         먹이 → 로열티 ↑ → 더 얻는다
                                         특정 크리처는 매일 생산한다
```

**크리처가 몇 마리든 이득이 계속 쌓이는 구조를 없앤다.** 대신 **무엇을 고르느냐**가 남는다 —
방에 둘 한 마리, 데리고 나갈 한 마리, 재료로 녹일 한 마리.

---

## 0. ✅ 매력 누적을 없앤다 — **만들었다** (세이브 12)

예전에는 `totalCharm()` 이 **가진 크리처 전부**의 `charmBonus` 를 더했다. 중복까지 다 더해서,
같은 것을 여러 번 만들기만 해도 매력이 끝없이 올랐다. 지금은 **장착한 한 마리만** 본다.

```js
function totalCharm() {
  const pet = roomPet();                                   // 장착한 한 마리
  return S.stats.beauty + S.stats.charm + (pet ? pet.charmBonus : 0);
}
```

### ⚠️ 그냥 바꾸면 열려 있던 맵이 다시 잠긴다

`isMapOpen()` · `isZoneOpen()` · `leagueOpen()` 이 전부 매력 총합을 본다.
유니콘 셋을 가진 사람은 규칙이 바뀌는 순간 **−12** 다. 그래서 두 가지를 같이 넣었다.

**① 마이그레이션 — 잃는 만큼을 옮겨 적는다** (`migrate` 의 `from < 12`)

- 제일 센 크리처를 **자동으로 장착**한다. 고르라고 하면 그 전까지 매력이 비어 보인다
- 나머지가 갖고 있던 매력을 **`stats.charm` 에 더한다.** 지우는 게 아니라 옮기는 것이다
- 그래서 **불러온 순간의 총합이 정확히 같다.** 맵도 등급도 랭킹도 안 흔들린다

**② 해금은 「여태 닿은 최고 매력」으로 판정한다** (`charmPeak()`)

마이그레이션만으로는 부족하다. 그 뒤에 **센 크리처에서 약한 것으로 바꾸면** 총합이 내려간다 —
그게 이 시스템의 고르는 맛인데, 해금이 총합을 그대로 보면 맵이 도로 잠긴다.

```js
function charmPeak() {                     // 최고 기록은 세이브에 남긴다
  const now = totalCharm();
  if (now > (S.charmPeak || 0)) S.charmPeak = now;
  return S.charmPeak || 0;
}
```

**한 번 연 것은 무슨 이유로도 닫히지 않는다.** 총합·등급은 지금 값 그대로 보여 주되,
해금 판정만 최고 기록을 쓴다.

**검사** — `npm run test:save` 에 세이브 11 케이스를 넣었다. 유니콘 셋 + 나비를 가진
옛 세이브를 심고, 불러온 뒤 ①총합이 220 그대로인지 ②제일 센 것이 장착됐는지
③약한 것으로 바꿔도 **맵이 하나도 안 잠기는지**까지 실제로 눌러 본다.

> 서버 `charm` 컬럼은 **다음 저장 때** 새 값으로 바뀐다. 옮겨 적기가 있으니 총합은 같지만,
> 배포 뒤 랭킹을 한 번 확인할 것.

---

## 1. ✅ 마이 룸 — 애착 크리처 한 마리 (`S.petRoom`) — **만들었다**

- **마이 룸 › 인벤토리 › 🦋 크리처** 에서 크리처 블럭을 누르면 장착된다 (블럭이 곧 버튼이다)
- 장착한 한 마리만 **마이 룸 그림과 과시 카드에 나온다**
  (예전에는 가진 것 넷을 방 여기저기 흩어 놓았다)
- **그 한 마리의 수치만 반영된다** (매력 · 그리고 나중의 전투력)

### 어디에 있는가 — 땅 · 공중 · 물 (`move`) — **만들었다**

한동안 **서른 마리가 전부 허공에 떠 있었다.** 방에 놓는 크리처에 `float` 애니메이션을
하나로 걸어 놨기 때문이다. 유니콘이 네 발로 허공을 딛고 있었는데, 그림만 봐서는
「원래 그런 연출인가」 싶어 한참 안 보였다.

```
물고기는 물에 있다 — 어항에 들어간다        →  물  2
날개가 있으면 뜬다 (나비 · 박쥐 · 새 날개)   →  공중 8
나머지는 네 발로 선다                        →  땅  20
```

- **손으로 붙이지 않고 그림 부품에서 끌어낸다** (`moveOf()`, `tools/gencreature.js`).
  서른 마리에 하나씩 적으면 새 마리를 넣을 때 반드시 빠뜨리고, 빠뜨린 쪽은 조용히 땅이 된다
- ⚠️ **몸통(`body`)으로 가르면 틀린다.** 「햇살 암탉」은 `body` 가 `bird` 인데 날개가 없다 —
  암탉은 걸어 다닌다. 갈라야 하는 축은 **날개**다
- ⚠️ **물이 날개보다 먼저 걸려야 한다.** 물고기에는 지느러미(`wing: 'fin'`)가 달려 있어서
  날개 검사가 앞에 오면 물고기가 통째로 공중이 된다
- 화면에서는 클래스가 갈린다 (`.cr-ground` / `.cr-air` / `.cr-water`).
  땅에 선 것도 **숨 쉬듯 2px 만** 움직인다 — 아예 안 움직이면 붙여 놓은 스티커처럼 보인다.
  공중은 8px 이고 바닥에서 조금 더 띄운다 (같은 높이에서 뜨면 「땅에서 통통 뛰는 것」이 된다)

#### 어항 (`move: 'water'`)

물고기를 방에 두면 **어항이 같이 나온다.** 통은 안 움직이고 물고기만 좌우로 헤엄치다가
끝에서 몸을 돌린다(`scaleX(-1)`) — 안 돌면 뒤로 헤엄치는 것처럼 보인다.

- ⚠️ **어항을 방 배경(`roomScene`)에 그리면 안 된다.** 방 그림은
  `preserveAspectRatio="…slice"` 라 창 비율에 따라 확대·잘림이 달라지는데 크리처는
  DOM 요소로 퍼센트 자리에 놓인다. 좌표계가 둘이라 **비율이 바뀌면 물고기가 어항 밖으로
  새어 나간다.** 그래서 어항도 크리처와 **같은 상자 안에** 그린다 (viewBox 도 100×100 으로 같다)
- 앞뒤가 갈려야 유리 너머로 보인다 — `back`(유리통·물) 뒤 · 물고기 · `front`(테·반사) 앞
- 어항 안에서는 **바닥 그림자를 뺀다**(`draw(c, { noShadow: true })`). 물속에 그림자가 깔리면
  유리 위에 앉은 것처럼 보인다
- ⚠️ **여유가 2px 밖에 안 된다.** 부품을 손보거나 헤엄 폭을 늘리면 곧바로 지느러미가
  유리를 뚫는데, 눈으로는 「살짝 걸쳤나?」 정도로만 보인다.
  `tools/checkavatar.js` 가 **헤엄의 양 끝 자세를 직접 만들어** 유리(원)와 수면에 견준다 —
  애니메이션이 도는 것을 기다리지 않는다(어느 순간을 쟀는지 알 수 없다).
  물이 아닌 28마리에 어항이 안 붙는지도 같이 본다
- ⚠️ **치마와 겹치면 안 된다.** 다른 크리처는 치마 앞에 조금 걸쳐도 「앞에 선 것」으로
  읽히는데, 어항은 **속이 비치는 유리통**이라 치마가 통 안으로 들여다보여 그림이 깨진다.
  `left: 6%` 로 두었을 때 롱스커트와 **47px** 겹쳤다.

  그래서 왼쪽 끝이 아니라 **오른쪽 끝을 치마 옆선에 맞춘다** — 기준은 아우라의 왼쪽 끝이
  아니라 **가운데 선**이다. 아바타는 높이가 고정(312px)이라 폭도 179.3px 로 고정인데
  아우라는 좁은 화면에서 240px 밑으로 줄어들어(flex), 왼쪽 끝에서 재면 그만큼 어긋난다.

  그리고 **좁은 화면에서는 통도 같이 줄어야 한다**(`--bowl`). 오른쪽 끝을 붙박아 두면
  통이 클수록 왼쪽으로 밀려 화면 밖으로 나간다 (265px 에서 절반이 잘렸다).

  `checkavatar` 의 **「어항 자리」**가 화면 폭 여섯 가지 × 하의·원피스 전부를 돌며
  **치마와의 틈**과 **화면 밖으로 나갔는지**를 같이 본다.
  ⚠️ `checkui` 의 「어항」은 예전에 **`.char-aura` 안에 있는지**로 쟀는데,
  그 상자는 240px 이고 아바타가 179px 을 쓴다 — 96px 어항을 거기 가두면
  **반드시 치마와 겹친다.** 지금은 **방 그림(`.room-scene`)** 을 기준으로 본다

### 교체

다른 블럭을 누르면 그것이 장착되고 앞의 것은 자동으로 해제된다. **해제 버튼은 없다** —
누르는 곳이 하나면 「어떻게 바꾸지」를 고민할 일이 없다.

```
나의 애착 크리처가 반짝 나비에서 유니콘으로 교체되었어요.
```

- 조사는 `josa(name, '으로')` 가 「로 / 으로」를 골라 준다
- **처음 장착할 때**는 앞 부분이 없으므로 문구가 따로다 — 「유니콘을 애착 크리처로 삼았어요.」
- **같은 것을 다시 누르면 아무 일도 안 일어난다.** 토스트도 안 띄운다 —
  안 바뀌었는데 「교체되었어요」가 뜨면 거짓말이다
- 고른 칸은 **테두리와 「애착」 딱지**로 알린다. 목록을 훑을 때 글자보다 먼저 눈에 든다

### 도감 — **만들었다** (2단계)

- 인벤토리 › 크리처 탭이 곧 도감이다. **서른 칸이 늘 다 있다** —
  못 가진 것은 `❓` 와 `???` 로 자리를 남긴다 (이름도 안 알려 준다 — 레시피 북과 같은 규칙)
- **가진 것만 보여 주면 도감이 아니다.** 「무엇이 더 있는지」가 안 보이고,
  속성 탭도 쓸모가 없다 — 처음에는 모든 탭이 비어 있게 된다
- 못 가진 칸에도 **속성 딱지는 붙인다.** 무엇을 모으는 중인지 보인다
- 못 가진 칸은 흐리게 만든다. 값은 **잠긴 콘텐츠 공통 블록에서 온다** (`CLAUDE.md` 4번) —
  화면마다 따로 흐리게 만들지 않는다. `button` 이 아니라 `div` 라 눌리지도 않는다
- ⚠️ **다만 「잠금」이 아니라 「미발견」이다.** `checkLocked` 의 자물쇠 검사에는 **안 넣는다** —
  자물쇠는 「조건을 채우면 열린다」는 뜻인데 도감 빈칸은 「아직 안 만들어 봤다」는 뜻이고,
  그건 `❓` 와 `???` 가 이미 말한다. 미발견 레시피 줄(`.recipe-row.locked`)도 같은 이유로
  검사 대상이 아니다 — 그 선례를 따랐다. 그래서 클래스 이름도 `.unknown` 이다
- 레시피 북에 이미 「크리처」 카테고리가 있다 (`RECIPE_CATS`) — **겹친다.**
  레시피 북은 **만드는 법**(조합), 도감은 **가진 것과 그 아이의 수치**로 나눈다

---

## 2. ✅ 속성 — 크리처와 맵이 같은 표를 쓴다 — **만들었다** (속성 일치까지 확률에 붙었다)

```
불 ➔ 땅 ➔ 바람 ➔ 물 ➔ 불          (화살표 = 강하다)
빛 ↔ 암흑                          (서로 카운터)
```

여섯 속성이다. 크리처에도, **맵(필드)에도** 붙는다.

> **크리처 서른 종과 맵 쉰한 곳 모두 속성이 붙었다.**
> 크리처는 `tools/gencreature.js`, 맵은 `tools/genmapattr.js` 가 뽑는다.
> **3단계에서 「속성 일치」 하나만 확률에 붙었다** (`palBonusRate()`).
> ⚠️ **날씨 · 시간 · 로열티는 아직 안 붙었다** — 5장의 덧셈 식 중 두 항만 살아 있다.

### 채집에서는 「같으면 좋다」만 쓴다

- **물 속성 크리처를 늪 지대에 데려가면** 얻는 것이 는다
- **불 속성 크리처를 화산 지대에 데려가면** 얻는 것이 는다

**상성 순환(➔)은 채집에 안 쓴다.** 「불 크리처를 땅 맵에 데려가면 조금 낫다」까지 넣으면
플레이어가 표를 외워야 하고, 그 대가로 얻는 재미가 없다.
**순환은 나중의 전투·약탈에 쓴다** — 거기서는 상대가 있으니 표를 볼 이유가 생긴다.

### 맵에 속성을 붙이는 일 — **이름의 낱말에서 규칙으로 뽑는다** (`tools/genmapattr.js`)

쉰한 곳을 손으로 적지 않는다 (`CLAUDE.md` 6번). **`MAPS` 배열도 안 건드린다** —
이름·설명·재료 풀이 손으로 쓴 것이라 통째로 다시 쓰면 위험하다.
대신 `MAP_ATTRS`(맵 id → 속성) 표만 따로 뽑아 넣고 `mapAttr()` 이 찾아 쓴다.

```
물   저수지 늪 여울 파도 물거품 인어 서리 …
불   노을 해질녘 화산 잿 도마뱀 녹슨 소금
빛   햇살 별 반짝 등대 정령 달빛 신기루
암흑 안개 부엉이 거미 까마귀 뼈 가시 난파 동굴 호박
바람 바람 구름 절벽 메아리 덤불 갈매기 삭도
땅   (안 걸리면 여기)
```

> ⚠️ **순서가 곧 우선순위다.** 불 쪽에 `불` 을 넣으면 **「반딧불 늪가」가 불 속성이 된다.**
> 물이 먼저 걸리게 두었다.

배정 결과: `땅 12 · 물 9 · 암흑 9 · 빛 8 · 바람 7 · 불 6`.
생성기가 **분포까지 검사한다** — 여섯이 다 쓰이는가(안 쓰이면 그 속성 크리처 다섯 마리가
갈 곳이 없다) · 한 속성이 4곳 미만은 아닌가 · **한 지대가 한 속성으로 통일되지 않았는가**
(그러면 「지대 = 속성」이라 축이 겹친다).

화산은 걱정했던 것과 달리 이미 있었다 — `화산재 능선`(산악). 늪 둘도 물로 잘 걸린다.

### 화면

#### 속성은 **문자열로 표시한다** (정해짐)

이모지를 쓰지 않는다. **속성이 나오는 모든 곳에 이름을 글자 그대로 적는다.**

| 곳 | 어떻게 |
|---|---|
| 도감의 카테고리 탭 | `전체 · 불 · 땅 · 바람 · 물 · 빛 · 암흑` — **일곱 칸** |
| 크리처 도감 칸 | 이름 아래 작은 딱지. 못 가진 칸에도 붙는다 (무엇을 모으는 중인지 보인다) |
| 맵 카드 | 모서리에 작은 글자 배지 (`물`). 「특별한 맵」 배지와 같은 모양 |
| 크리처가 나오는 곳 전부 | 도감 · 동행 고르기 · 방 카드 |

**속성에는 누르면 뜨는 토스트가 없다.** 이름이 이미 화면에 있으니 눌러서 볼 것이 없다.
(날씨는 다르다 — 이모지 하나로는 「비가 추적추적」이 안 읽혀서 토스트가 남는다. 3장)

- **글자라서 언어를 탄다.** 속성 이름은 한 글자~두 글자인데(`불` `바람`)
  영어는 `Fire` `Water` `Light` 로 길다. **일곱 칸 × 영어**가 이 시스템에서
  가장 좁은 자리다 — 265px 에서 먼저 재고 시작한다
- `.cat-tabs` 는 원래 줄바꿈이라 **두 줄로 접히는 것은 위반이 아니다.** 접히게 둔다.
  다만 **세 줄까지 가면** 탭 줄이 화면의 3분의 1을 먹으므로 그때는 「전체」를 빼거나
  탭을 줄이는 것을 다시 본다
- `checkUI` 의 **라벨 2배 확대**가 이 탭에 그대로 걸린다. 이모지였다면 늘릴 글자가
  없어서 그냥 통과했을 자리다 — **검사를 실제로 받는 쪽을 고른 셈이다**
- 이름은 크리처 생성기가 `NAMES` 에 같이 넣는다 (`attr_fire` …). 속성 표가 데이터라서다
- ⚠️ **탭에 「5/30」을 같이 적으면 안 된다.** 칸 폭이 두 배가 되어 265px 영어에서
  **네 줄**이 됐다 — 탭 줄이 화면의 3분의 1을 먹는다. 가진 수는 탭 아래 한 줄에
  **지금 탭 것만** 적는다 (일곱 번 셀 필요가 없다). 지금은 한국어 390px 에서 한 줄,
  영어 265px 에서 두 줄이다
- **글자색은 고정(`--ink`)이고 배경만 속성 색을 옅게 깐다.** 여섯 색 위에 흰 글자를
  얹으면 대비를 여섯 번 다 맞춰야 하고 그중 하나는 반드시 걸린다 (`TEXT_POLICY.md`)

#### 동행 한 줄 — **만들었다** (3단계)

지대 탭 **위**에 한 줄(`#palRow`)을 두고, 누르면 고르기 시트(`#palPick`)가 뜬다.

```
🐾 동행 없음                      ← 아무도 안 데려갈 때 (.pal-off, 흐린 글자 하나)
🐾 화염 여우와 함께        [불]   ← 데려갈 때 (SVG 그림 · 이름 · 속성 딱지)
```

- **지대 탭 위에 둔다.** 아래에 두면 지대를 고를 때마다 자리가 밀려 보이고,
  「이 지대의 무엇」처럼 읽힌다. 동행은 지대와 상관없이 한 번 정하는 것이다
- 시트에는 **「혼자 간다」가 맨 위에** 있다. 이것이 없으면 한 번 데려간 뒤로
  **되돌릴 방법이 없다** — 크리처를 고르는 버튼만 있으면 「안 고르기」를 누를 데가 없다
- 크리처가 하나도 없으면 시트에 `pal_empty`(공방에서 만들 수 있어요)를 대신 띄운다.
  빈 시트는 고장으로 읽힌다
- ⚠️ **조사(`josa`)를 문자열에 박아 넣지 말 것.** `pal_row` 를 `{name} 과(와) 함께` 로
  두었다가 「화염 여우 과(와) 함께」가 그대로 화면에 떴다. 서른 종 중 **받침이 있는 이름과
  없는 이름이 섞여 있다**(불씨 도롱뇽 / 화염 여우) — `josa(name, '과와')` 를 지난다
- ⚠️ **덤 확률은 「계산값」만 보면 검사가 통과한다.** `palBonusRate()` 가 맞는 값을 돌려줘도
  `gather()` 가 그 값을 안 쓰면 표는 그대로 맞게 나온다. **`Math.random` 을 갈아 끼우고
  실제로 채집해 가방에 하나 더 들어오는지**까지 본다

#### 이모지는 날씨·시간대에만 쓴다

| 날씨 | | 시간대 | |
|---|---|---|---|
| ☀️ | 해가 쨍쨍 | 🌅 | 새벽 |
| 🌧️ | 비가 추적추적 | ☀️ | 낮 |
| 🌬️ | 바람 쌩쌩 | 🌆 | 해질녘 |
| 🌪️ | 모래 폭풍 | 🌙 | 밤 |
| 🌫️ | 안개 자욱 | | |
| ❄️ | 눈이 펄펄 | | |

**다른 데서 이미 쓰는 이모지와 겹쳐도 된다** (확인받음). ☀️ 는 맵 「햇살 언덕」이,
❄️ 는 재료 「설화 봉오리」가 이미 쓰고 있다. 안 겹치게 고르려면 뜻이 안 맞는 이모지를
억지로 붙여야 하는데 그게 더 나쁘다.

- 대신 **자리와 모양으로 역할을 가른다.** 재료는 **둥근 칩**(`.spot-chip`),
  날씨는 **카드 모서리의 작은 배지**다 — 「특별한 맵」 배지(`.spot-badge`)와 같은 언어
- 속성이 글자가 되면서 **한 카드 위의 이모지가 하나 줄었다.** 맵 이모지 · 재료 칩 ·
  날씨 배지 셋이면 아직 읽힌다

---

## 3. ✅ 맵의 날씨 — **만들었다** (4단계)

`비가 추적추적` · `해가 쨍쨍` · `바람 쌩쌩` · `모래 폭풍` … 맵마다 다르고 바뀐다.

- 맵 카드에 **날씨 이모지**. 누르면 토스트로 이름과 **편드는 속성**을 알려 준다.
  속성만 적어 두면 「이게 왜 좋은 건데」가 화면 어디에도 없다 — 표를 밖에서 외워 오라는 뜻이 된다
- 날씨가 채집 확률을 바꾼다 (5장)
- 이모지 표는 2장에 있다. **속성은 글자, 날씨는 이모지**다 —
  이모지 하나로는 「비가 추적추적」이 안 읽혀서 여기는 토스트가 남는다

**여섯 날씨가 여섯 속성을 하나씩 맡는다** (`D.WEATHERS`). 하나가 둘을 맡거나 비는 속성이
있으면 「내 크리처는 언제 좋은가」를 외워야 한다.

| ☀️ 불 | 🌧️ 물 | 🌬️ 바람 | 🌪️ 땅 | 🌫️ 암흑 | ❄️ 빛 |
|---|---|---|---|---|---|

- ❄️ 가 빛인 것은 눈밭이 볕을 되쏘기 때문이다. 억지스럽지만 여섯을 하나씩 맡기려면
  어딘가는 이렇게 이어야 한다 — 비는 쪽이 생기는 것보다 낫다
- ⚠️ **맵마다 나오는 날씨를 가리지 않는다.** 화산에 눈이 오는 것이 이상하긴 한데,
  가리기 시작하면 속성마다 「날씨가 맞을 확률」이 달라져서 어떤 크리처는 영영 손해다
- ⚠️ **이모지 배지에 `line-height: 1` 을 주면 안 된다.** 이모지는 글자보다 세로로 커서
  제 상자를 넘어가 잘린 것처럼 보인다. 크기도 속성 글자(0.7rem)에 맞추면
  🌫️ · 🌬️ 처럼 가로로 퍼진 것이 뭉개진다 — 재료 칩과 같은 0.9rem 을 준다
- ⚠️ **`<button>` 은 본문 폰트를 안 물려받는다.** `font-family: inherit` 을 빼먹으면
  Arial 로 떨어져 `checkTextStyle()` 이 「본문 스택 상속만 허용」으로 잡는다 (11건 × 두 언어)

### ⚠️ 날씨는 `Math.random()` 으로 정하면 안 된다

`render()` 는 아주 자주 불린다. 렌더마다 뽑으면 **화면을 다시 그릴 때마다 날씨가 바뀐다** —
채집 한 번 할 때마다 비가 왔다 갔다 한다.

**시간과 맵 id 로 결정론적으로 정한다.**

```js
// 3시간마다 바뀐다. 같은 시간·같은 맵이면 누가 언제 보든 같은 날씨다
function weatherOf(mapId, now) {
  const slot = Math.floor(now / (3 * 3600e3));
  return WEATHERS[hash(mapId + ':' + slot) % WEATHERS.length];
}
```

- 리그의 NPC 를 **주차·리그로 시드를 고정해** 만드는 것과 같은 방식이다 (`leagueNpcs`)
- `now` 는 반드시 **`nowDate()`** 를 지난다 — 그래야 `tools/checktime.js` 가
  시계를 옮겨 놓고 날씨 변화를 검사할 수 있다
- 세이브에 안 남긴다. 저장하면 기기마다 달라지고, 서버 동기화 충돌거리가 하나 는다

---

## 4. ✅ 시간대 — 아침 · 낮 · 해질녘 · 밤 — **만들었다** (4단계)

**탐험 탭 위쪽에 지금 시각을 보여 주기만 하면 된다.** 마이 룸 시계(`zoneTime()`)와 같은 모양.

**경계는 운동(`EX_WHEN`)과 어긋나지 않게 정했다** (14장의 열린 질문이었다).
채집의 시간대는 **운동의 세 구간을 더 잘게 자른 것**이다 — 낱말이 「해질녘」 하나 늘 뿐,
경계는 하나도 안 어긋난다. 따로 잡았다면 20시 30분에 「운동은 낮인데 채집은 밤」이 되어
설명이 두 벌이 됐을 것이다.

| 시간대 | 시각 | 이모지 | 운동 구간 | 편드는 속성 |
|---|---|---|---|---|
| 아침 | 05–11 | 🌅 | 아침 | 물 · 바람 |
| 낮 | 11–17 | ☀️ | 낮 | 불 · 땅 |
| 해질녘 | 17–21 | 🌆 | 낮 | 빛 |
| 밤 | 21–05 | 🌙 | 밤 | 암흑 |

- **넷에 여섯이라 딱 안 나뉜다. 긴 시간대에 적게 붙인다** — 밤이 여덟 시간으로 제일
  길어서 암흑 하나만 맡는다. 그래도 암흑이 33% · 빛이 17% 로 두 배 차이가 나는데,
  이 항은 +0.06 짜리라 기댓값 차이가 0.01 도 안 된다
- 시각은 전부 **`nowDate()`** 를 지난다 (`CLAUDE.md`). `tools/checktime.js` 가
  시계를 옮겨 놓고 **채집 시간대가 운동 구간을 넘지 않는지**까지 본다 —
  경계를 한 시간만 옮겨도 그 검사가 잡는다 (일부러 옮겨서 확인했다)
- **재료마다 잘 나오는 시간대**(재료에 `time` 축)는 아직 안 붙였다. 지금 시간대가 바꾸는
  것은 **동행의 덤·히든 확률뿐**이다

---

## 5. 확률 — 무엇이 무엇을 얼마나 바꾸는가

> **4단계에서 아래 표가 거의 다 붙었다.** 덤은 `기본 + 속성 + 날씨 + 시간` 네 항이,
> 히든은 `3 × 2 × 2` 배율과 맵별 등급이 살아 있다 (`PAL` · `PAL_SP` · `palMatch()` ·
> `palBonusRate()` · `specialRate()`, game.js).
> **로열티 항(0~0.10)만 아직 비어 있다** — 6단계다.
>
> 지금 닿을 수 있는 최고값: 덤 **0.30** (상한 0.45 는 로열티까지 붙어도 0.40 이라 여유가 있다),
> 히든 **12배** (초반 맵이면 0.5% → 6.0%).

### 지금

```
채집 1회 = 재료 1개 (100%) + 음식 12%(FOOD_RATE) + 히든 재료 0.1%(SPECIAL_RATE)
```

**재료는 이미 100% 나온다.** 그러니 「획득 확률」로 올릴 것은 셋이다.

| 무엇 | 지금 | 바뀌는 것 |
|---|---|---|
| **덤** (재료가 하나 더) | 없음 | 속성 일치 · 날씨 · 시간 · 로열티 |
| **히든 재료** | 0.1% 고정 | 위 넷이 **아주 크게** 바꾼다 |
| 음식 | 12% | 손대지 않는다 (6장) |

### 곱셈이 아니라 덧셈으로 쌓는다

```js
덤확률 = 기본(0.08)
       + (속성 일치 ? 0.10 : 0)
       + (날씨 맞음 ? 0.06 : 0)
       + (시간 맞음 ? 0.06 : 0)
       + 로열티 보정(0 ~ 0.10)
// 상한 0.45

히든배율 = 1
        × (속성 일치 ? 3 : 1)
        × (날씨 맞음 ? 2 : 1)
        × (시간 맞음 ? 2 : 1)
// 최대 12배
```

> **왜 덤은 덧셈이고 히든은 곱셈인가.**
> 덤은 자주 일어나는 일이라 곱하면 금방 1.0 을 넘는다 — 상한에 붙어 버려서
> 조건을 맞춘 보람이 사라진다. 히든은 0.1% 대라 12배를 해도 1.2% 이고,
> **「조건을 다 맞추면 확 오른다」가 이 시스템의 재미다.**

### ✅ 히든 재료 확률을 다양하게 — **만들었다** (4단계)

예전에는 `SPECIAL_RATE = 0.001` **하나로 51개 맵이 전부 같았다.** 등급을 나눴다.

| 등급 | 기본 확률 | 조건 다 맞췄을 때 | 어떤 맵 | 곳 |
|---|---|---|---|---|
| 흔한 히든 | 0.5% | 6.0% | 해금 매력 100 미만 | 20 |
| 보통 | 0.2% | 2.4% | 100~299 | 17 |
| 귀한 | 0.05% | 0.6% | 300 이상 | 14 |

- **등급을 데이터에 손으로 붙이지 않았다.** `맵의 해금 매력`에서 규칙으로 뽑는다
  (`D.SPECIAL_TIERS` · `specialTier()`). 51곳에 손으로 적으면 새 맵이 늘 때 반드시
  빠뜨리고, 빠뜨린 쪽은 조용히 제일 흔한 등급이 된다
- **초반 맵의 히든이 흔해지는 것은 일부러 그런 것이다.** 처음 몇 시간 안에
  「히든이라는 것이 있구나」를 한 번은 봐야 조건을 맞출 마음이 생긴다
- **`SPECIAL_RATE` 는 지웠다.** 쓰지 않는 값이 남아 있으면 다음 사람이 그것을
  진짜 확률로 읽는다

- ⚠️ **`specials` 기록은 행운 아우라로 간다** (`STORY.md` 표). 확률을 올리면
  행운이 그만큼 빨리 쌓인다. 카이로스가 **이정표만 노래하는** 방식이라 폭주하진 않지만,
  이정표에 닿는 속도가 빨라진다 — 카이로스를 만들 때 이 표를 같이 볼 것
- 지금 `SPECIAL_RATE` 를 쓰는 곳은 `gather()` 한 곳이다. 재료마다 다른 값을 주려면
  **재료 표(`INGREDIENTS`)에 `rare` 를 붙이고** 거기서 읽는다

---

## 6. ✅ 채집 결과의 「단서」 — **만들었다** (5단계)

채집 토스트가 「🌿 이끼」 한 줄이었다. 여기에 **왜 그게 나왔는지**를 붙였다.

```
🌾 밀 이삭 · 🌻 해바라기 씨
여기는 화염 여우에게 제 집 같은 곳이다. 그래서 하나가 더 딸려 왔다!
```

### 문장을 다 쓸 수는 없다 → **조각을 조립한다**

맵 51 × 날씨 6 × 시간 4 × 속성 6 = **7천 가지**가 넘고, 그것도 **두 언어**다.

```
{왜} + {결과}
"이런 날씨에 물방울 수달이 잘 돌아다닌다." + "가방이 조금 무거워졌다!"
```

조각 **24개**(왜 9 · 덤 3 · 히든 3 · 운 3 · 권유 3 · 혼자 3)에서 **36가지 문장**이 나온다
(`CLUE_WHY` · `gatherClue()`, game.js / 문장은 `clue_*`, i18n.js).

- ⚠️ **맵 조각은 안 만들었다.** 51곳의 산문을 두 언어로 쓰면 백 번 읽는 한 줄치고
  손이 너무 많이 간다 — 게다가 맵 이름은 방금 누른 카드에 이미 적혀 있다.
  지금은 날씨·시간대·속성만 말한다. 늘린다면 **지대(5곳)** 단위가 알맞다
- ⚠️ **맞은 것이 여럿이어도 하나만 말한다.** 셋을 다 늘어놓으면 토스트가 다섯 줄이 된다
- **조사는 `josa()` 로 붙인다.** 조각마다 필요한 것이 달라서(이/가 · 은/는 · 을/를)
  **세 벌을 미리 다 만들어 넘긴다**(`clueVars()`) — 조각에서 고르게 하면 빠뜨리는 데가 생긴다.
  영어 조각은 그것들을 아예 안 본다
- **조건이 안 맞았을 때가 더 중요하다.** 「다음엔 {속성} 속성을 데려와 봐」가
  **다음 채집의 이유**를 만든다 — 이 시스템에서 제일 값진 한 줄이다

### ⚠️ 매번 띄우지 않는다

**꾹 누르기 자동 채집**이 있다. 매번 두세 줄이 되면 잔소리가 된다.

| 언제 | 띄우는가 |
|---|---|
| 덤이 나왔을 때 | **반드시** — 그때가 「왜?」가 생기는 순간이다 |
| 히든이 나왔을 때 | **반드시** |
| 평범하게 하나 주웠을 때 | **조건이 하나도 안 맞았을 때만**, 그것도 `CLUE.miss`(14%)로 |
| 동행이 없을 때 | 위와 같은 빈도로 「누구든 데려와 보라」 |

덤이 나왔을 때는 단서가 **「하나 더 찾았어요」 줄을 대신한다.** 둘 다 두면 세 줄이 되는데,
단서가 이미 누가 왜 찾았는지를 말하고 있다.

### 토스트가 길어진다

- ⚠️ **`.toast.multi` 의 280px 로는 한 문장이 다섯 줄이 됐다.** 한국어는 `keep-all` 이라
  낱말 단위로만 꺾여서 좁으면 금세 세로로 길어진다. `min(340px, 88vw)` 로 넓혔다 —
  지금은 390px 에서 3줄, 265px 에서 4줄이다
- 재료 + 음식이 같이 나올 때 **한 줄로 합쳤던** 이유가 그대로 적용된다 —
  토스트를 두 번 띄우면 앞것이 잘린다
- `checkui` 에 `gather/채집단서` 를 넣었다. **줄 수도 같이 잰다** — 넓은 창에서는
  아무리 세로로 길어져도 화면 밖으로 안 나가서 「넘쳤는가」만 보면 통과한다
  (폭을 90px 로 줄여도 안 잡혔다. 줄 수를 보태니 13줄로 잡혔다)

---

## 7. ✅ 먹이 · 로열티 · 버프 — **만들었다** (6단계)

### 잡화 › 먹이 탭

```
잡화 ─┬─ 🧴 물약
      ├─ 🍞 음식     (사람이 먹는 것 — 포만감)
      └─ 🦴 먹이     (크리처가 먹는 것 — 버프 · 로열티)
```

세 칸이 되면서 265px·영어를 다시 쟀다 — `🧴 Potions · 🍞 Food · 🦴 Feed` 가 **한 줄**이다.

먹이 세 종은 채집에서 나온다 (`FEED_RATE = 0.09`, 음식 0.12 보다 낮다).

| 먹이 | 로열티 | 버프 | 나올 확률 |
|---|---|---|---|
| 🌿 들풀 뭉치 | +2 | 1시간 | 60% |
| 🫐 이슬 열매 | +6 | 3시간 | 30% |
| 🍬 별빛 사탕 | +15 | 8시간 | 10% |

⚠️ **셋의 값이 비례하지 않게 잡았다.** 들풀은 로열티당 싸고 버프가 짧다 —
로열티를 쌓을 때는 들풀, 지금 한 번 나갈 때는 사탕. 비례하면 고를 이유가 없다.

- 먹이를 누르면 **먹이주기 팝업**이 뜬다: 크리처 고르기 → 먹이 종류 → 수량을 **한 화면에**.
  단계를 나누면 「뒤로」가 필요해지고, 세 번 눌러야 한 번 먹인다
- 크리처 카드에도 **「먹이 주기」 버튼**을 둔다 (반대 방향 진입)
- ⚠️ **먹이 버튼을 카드 안에 못 넣는다.** 카드가 곧 `<button>` 이라 그 안에 또 버튼을
  두면 겹친 버튼이 되어 어느 쪽이 눌리는지 브라우저마다 다르다.
  격자 칸을 `.creature-cell` 로 한 번 감싸고 그 위에 얹었다

### 두 축을 나눈다

| 축 | 성격 | 효과 | 어디에 |
|---|---|---|---|
| **버프** | **일시적.** 시간이 지나면 원래대로 | 덤이 나올 **확률** +0.10 | `PAL.buff` |
| **로열티** | **영구.** 준 것이 쌓인다 (0~100) | 덤이 나올 때의 **개수** | `LOYALTY_STEPS` |

**이 둘을 하나로 묶으면 안 된다.** 버프만 있으면 먹이가 소모품일 뿐이고,
로열티만 있으면 「나가기 전에 먹인다」는 순간의 판단이 사라진다.

```
로열티  0~39 → 덤 ×1      40~79 → 덤 ×2      80~100 → 덤 ×3
```

- **로열티는 확률이 아니라 수량을 올린다.** 확률에는 이미 넷(기본·속성·날씨·시간)이
  붙어 있어서, 다섯 번째를 같은 자리에 얹으면 무엇이 효과가 있는지 아무도 모른다.
  개수는 토스트에 그대로 보인다
- ⚠️ **덤이 여럿일 때 단서가 「하나 더」라고 하면 거짓말이 된다.** 개수용 꼬리를
  따로 뒀다 (`clue_gotn_*`) — 로열티를 올려 놨는데 화면은 그대로면 올린 보람이 사라진다
- ⚠️ **버프는 이어 붙이지 않고 「지금부터 다시」다.** 쌓이면 사탕을 몰아 먹여
  하루짜리 버프를 만들 수 있는데, 그러면 「나가기 직전에 먹인다」가 사라진다
- **버프는 끝나는 시각 하나로만 산다** (`buffEnd`). 「몇 시간 남았다」를 카운트다운으로
  넣으면 앱을 꺼 둔 동안 시간이 안 가고 두 기기에서 다르게 흐른다.
  `nowDate()` 를 지나므로 `checktime.js` 가 시계를 옮겨 검사한다
- 버프가 걸려 있는 동안 **크리처 카드에 남은 시간**을 보여 준다.
  안 보여 주면 「먹였는데 아무 일도 안 일어났다」로 읽힌다
- 로열티도 카드에 **숫자와 「그래서 덤이 몇 개인가」를 같이** 적는다.
  숫자만 적으면 「42가 뭔데」로 끝난다

---

## 8. 생산하는 크리처

**하루에 한 번, 그 크리처가 만든 것이 인벤토리에 쌓인다.**

| 크리처 | 생산물 | 형태 |
|---|---|---|
| 🐐 염소 | 신선한 염소젖 | 음식 |
| 🐔 닭 | 달걀 | 음식 |
| 🐝 (예) 꿀벌 | 꽃꿀 | 채집 재료 |

- **생산 기록 버튼** — 최근 **5일치**까지 보여 준다
- 정산은 AP 의 자정 충전과 같은 자리에서 한 번에 한다 (`dayKey()`)
- ⚠️ **방치 상한을 둔다.** 한 달 만에 들어온 사람에게 30일치를 쏟으면
  「돌아왔더니 다 있네」가 되어 매일 들어올 이유가 없어진다.
  **5일치가 상한**이고, 기록도 딱 그만큼 보여 준다 — 상한과 기록 길이가 같으면
  「5일치까지만 쌓인다」가 설명 없이 읽힌다
- **장착한 한 마리만 생산하는가, 가진 전부가 생산하는가**를 정해야 한다.
  전부가 생산하면 0장에서 없앤 「무한 누적」이 생산물 쪽으로 되살아난다.
  → **장착한 한 마리 + 동행 한 마리**까지만 생산하는 쪽을 권한다

---

## 9. 크리처를 재료로 되돌린다

**크리처는 가마솥에서 태어난다. 그러니 가마솥으로 돌아갈 수도 있다.**

- **1개체는 못 녹인다.** 초과분(2번째부터)만 재료가 된다
- 남는 개체가 생기는 것이 지금은 「매력만 조금 더」였는데(그것도 0장에서 없앴다),
  이제는 **다음 크리처의 재료**가 된다 — 중복이 처음으로 쓸모를 갖는다

### ⚠️ 이게 생각보다 크게 건드린다

지금 조합 입력(`RECIPES[].inputs`)은 **재료 id 뿐**이고, 여러 곳이
`D.INGREDIENTS[id]` 를 **바로** 조회한다 — 가방 렌더 · 재료 칩 · `weightedPick` · 솥 슬롯.

크리처 id 가 입력에 섞이면 그 조회가 전부 `undefined` 가 된다.

**조회를 한 곳으로 모은다.**

```js
function itemOf(id) {           // 재료 → 크리처 순으로 찾는다
  return D.INGREDIENTS[id] || creatureResult(id) || null;
}
```

- **중복 조합 검사는 만들었다** (`tools/checkdata.js`, `npm test` 가 돌린다).
  `RECIPE_MAP[recipeKey(inputs)] = result` 라 같은 조합을 쓰면 **조용히 덮어쓰고**
  앞 레시피가 사라진다 — 오류도 안 나던 자리다. 크리처를 서른 종으로 늘리기 전에 먼저 만들었다
- 크리처를 녹일 때 **로열티와 버프도 같이 사라진다.** 확인 모달에서 그것을 말해 준다

---

## 10. 전투 · 약탈 — **아직 만들지 않는다**

나중에 「밭」과 「약탈」이 들어온다. 지금 하는 일은 **자리만 비워 두는 것**이다.

```js
// data.js 의 크리처에 붙여만 두고, 아직 아무도 안 읽는다
combat: { atk: 12, matk: 4, def: 8, mdef: 6 }
combatPower = atk + matk + def + mdef
```

- 판정은 「**지키는 크리처보다 보낸 크리처가 강해야 한다**」
- 로열티가 약탈 성공률을 올린다 (탐험에서 수량을 올리는 것과 같은 축)
- 속성 **순환**(불➔땅➔바람➔물➔불, 빛↔암흑)이 여기서 처음 쓰인다

> ⚠️ **약탈은 서버가 필요하다.** 지금 서버는 세이브 저장소 + 누적 매력 랭킹뿐이다.
> 남의 밭을 털려면 **남의 상태를 읽고 쓰는 것**이 필요하고, 그건 지금 구조에 없다.
> 리그가 아직 NPC 인 것과 같은 이유다 (`CLAUDE.md` 「아직 안 한 것」).
> **수치만 미리 넣어 두고, 밸런싱은 서버가 생긴 다음에 한다.**

---

## 11. 세이브

```js
S.petRoom  = null;   // 마이 룸 애착 크리처 id
S.petField = null;   // 동행 크리처 id
S.pets     = {};     // 크리처별 상태 { [id]: { loyalty, buffEnd, buff:{}, fed } }
S.produced = [];     // 생산 기록 (최근 5일치)
S.producedDay = '';  // 마지막으로 정산한 날
```

- **위 다섯은 없던 칸을 더하는 것**이라 그것만으로는 마이그레이션이 필요 없다.
  4단계도 마찬가지로 안 올렸다. 날씨·시간대는 **세이브에 남기지 않는다** — 남기면
  기기마다 달라지고 동기화 충돌거리가 하나 는다. 시각에서 매번 다시 뽑는다.
  실제로 3단계(`S.petField`)는 `SAVE_VER` 를 안 올렸다 — 옛 세이브에 그 칸이 없으면
  `defaultState()` 의 `null` 이 그대로 남아 「동행 없음」이 된다
- **하지만 `SAVE_VER` 는 올려야 한다** — 0장의 매력 옮겨 적기 때문이다.
  그건 「없던 칸 추가」가 아니라 **계산 규칙 변경**이다
- `S.petRoom` · `S.petField` 에 **없는 크리처 id 가 남을 수 있다**
  (초기화 · 데이터 변경 · 재료로 녹인 뒤). 읽는 곳에서 한 번 거른다
- 크리처를 재료로 녹였는데 그게 장착 중이었으면 **장착도 같이 풀어 준다**

---

## 12. 검사

| 무엇 | 어떻게 |
|---|---|
| **매력이 안 내려가는가** | 크리처 셋을 가진 옛 세이브를 심고 `totalCharm()` 과 열린 맵 수를 비교 (`test:save`) |
| 확률이 **실제로** 달라지는가 | ✅ (덤) `Math.random` 을 갈아 끼우고 1만 번 돌려 비율을 잰다. **계산값만 보면 안 된다** — 그 값이 `gather()` 까지 안 닿아 있어도 표는 맞게 나온다. 히든은 4단계 |
| 덤 확률 상한 | 조건을 다 맞춘 최고값이 상한을 안 넘는가 |
| 날씨가 **안 흔들리는가** | ✅ `checktime.js` — 같은 시각에 서른 번 물어도 같은 날씨인가 (여기서 `Math.random` 실수가 잡힌다). 일부러 `Math.random` 으로 바꿔 잡히는 것을 봤다 |
| 날씨·시간대가 **바뀌는가** | ✅ `checktime.js` — 하루 안에서 날씨가 여섯 가지 다 나오는가 · 같은 시각에도 맵마다 다른가(시드가 맵 id 를 타는가) · 8·14·19·23시의 시간대 |
| 시간대가 **운동과 안 어긋나는가** | ✅ `checktime.js` — 채집 시간대 하나가 운동 구간 둘에 걸치지 않는가. 경계를 한 시간만 옮겨도 잡힌다 |
| 크리처가 **땅에 서는가** | ✅ `gencreature.js --check` — 유니콘·개구리·암탉은 땅, 나비·봉황·해마는 공중. 규칙 한 줄이라 조용히 뒤집히기 쉬워서 못을 박아 뒀다 |
| 먹이 버프 만료 | ✅ `checktime.js` — 먹이면 걸리는가 · 만료 한 시간 전에는 살아 있는가 · 지나면 저절로 풀리는가 · 확률이 실제로 내려가는가(0.34→0.18) · **버프가 풀려도 로열티는 그대로인가** · 다시 먹여도 안 쌓이는가 |
| 로열티가 **개수**를 바꾸는가 | ✅ 실제 `gather()` 로 로열티 0·40·80 에서 가방이 2·3·4개가 되는 것까지 |
| 생산 상한 | 30일을 건너뛰어도 5일치만 들어오는가 |
| 레시피 **중복 조합** | ✅ `tools/checkdata.js` — 조합 중복 · 결과물 중복 · 없는 재료 · id 중복까지 |
| 없는 크리처를 장착 중일 때 | `S.petRoom` 에 안 가진 id 를 넣고 화면을 그려도 안 죽는가 |
| 단서 토스트 | ✅ `checkui` 의 `gather/채집단서` — 제일 긴 조각을 골라 띄우고 **줄 수까지** 잰다. 화면 밖인지만 보면 넓은 창에서 통과한다 |
| 새 화면들 | ✅ 먹이주기 팝업 · ✅ 동행 고르기 · ✅ 도감 탭 — 평소 `display:none` 이라 검사에서 빠진다 (`checkui.js` 에 넣는다). ⚠️ **동행 한 줄은 있을 때와 없을 때가 다른 화면이다** — 없을 때(`.pal-off`)만 재면 이름·속성 딱지가 든 줄을 한 번도 못 잰다 |
| 탭 폭 | **속성 탭 일곱 칸 · 잡화 탭 세 칸** — 265px 영어 · 라벨 2배 확대에서 몇 줄이 되는가 |
| 번역 | `npm run test:i18n` (자동) |

---

## 13. 만드는 순서

**한 번에 다 만들면 무엇이 재미있는지 알 수 없다.** 각 단계가 혼자서도 말이 되게 잘랐다.

| 단계 | 내용 | 왜 여기서 끊나 |
|---|---|---|
| ~~**1**~~ | ✅ **매력 누적 제거 + 마이 룸 한 마리 + 교체 토스트** — **만들었다.** 크리처 30종과 SVG 그림도 같이 들어갔다 | 세이브를 건드리는 유일한 단계다. 여기만 따로 배포해서 랭킹·해금을 확인한다 |
| ~~**2**~~ | ✅ **속성 표 · 맵 속성 배정(스크립트) · 도감과 속성 탭** — **만들었다** | 확률은 아직 안 바꾼다. **표시만** 해서 눈에 익게 한다 |
| ~~**3**~~ | ✅ **동행 고르기 + 덤 한 줄** (속성 일치만) — **만들었다** | 여기서 「재미있는가」를 판단한다. 아니면 4번부터 안 만든다 |
| ~~**4**~~ | ✅ **날씨 · 시간대 · 확률 표 전체 · 히든 확률 다양화** — **만들었다.** 크리처가 땅에 서는지 뜨는지(`move`)도 같이 들어갔다 | 3번이 재미있었을 때만 |
| ~~**5**~~ | ✅ **단서 문구 (조각 조립)** — **만들었다** | 4번이 있어야 할 말이 생긴다 |
| ~~**6**~~ | ✅ **먹이 · 로열티 · 버프** — **만들었다** | |
| **7** | ~~중복 조합 검사~~ ✅ (`tools/checkdata.js`) → 크리처를 **재료로 녹이기** | 검사는 먼저 만들었다 |
| **8** | 생산 | |
| **9** | (나중) 전투 · 약탈 — 서버가 생긴 뒤 | |

---

## 14. 아직 안 정한 것

- ~~**채집 시간대 경계를 운동(`EX_WHEN`)과 맞출 것인가**~~ → **정했다: 맞춘다.**
  운동의 세 구간을 그대로 두고 「낮」만 낮·해질녘으로 한 번 더 잘랐다 (4장)
- **생산을 장착한 한 마리만 할 것인가** (8장) — 전부 하면 무한 누적이 되살아난다
- **동행과 애착이 같은 마리여도 되나** — 지금 초안은 허용이다.
  막으면 「두 마리 이상 가져야 한다」는 압박이 생기는데, 그게 좋은지 나쁜지 아직 모르겠다
- **크리처를 몇 마리까지 늘릴 것인가** — 속성 여섯 × 등급 셋이면 18마리다.
  지금은 **셋**뿐이다. 이름·그림·조합·영어 이름이 마리당 한 벌씩 필요하다
- **크리처 그림** — 지금은 전부 이모지다. 생산·먹이·전투까지 붙으면
  이모지 하나로는 정보를 못 담는다

---

## 15. 1판에서 가져온 것 (여전히 유효)

- **음식 확률은 손대지 않는다.** 이미 남아돈다 — 하루 100번 채집하면 포만감 260쯤 들어오는데
  하루에 줄어드는 것은 48~120 이다. 폭식은 음식이 없어서가 아니라 **먹는 것을 잊어서** 일어난다
- **덤은 사실상 AP 상한 증가다.** 덤 20%는 「AP 가 20% 늘었다」와 같고,
  AP 충전은 다이아로 파는 상품이다 (`D.ENERGY.chargeCost`). 상한을 넘기기 전에 상점 값을 볼 것
- **상급 크리처는 넓고 얕게, 기초는 좁고 깊게.** 유니콘이 모든 축에서 최고면
  유니콘 하나로 끝난다 — 특정 속성 맵에서는 그 속성의 기초 크리처가 더 낫게 잡는다
