# 크리처 — 기획 초안 (2판)

크리처를 **키우고 · 데리고 나가고 · 재료로 되돌리는** 시스템.

> **초안이다. 코드는 한 줄도 없다.** 수치는 전부 **임시값**이고, 왜 그 값인지를 표 아래에 적었다.
> 「지금 이렇게 되어 있다」는 코드를 읽고 확인한 사실이다.
>
> **1판(동행만 있던 것)에서 바뀐 것**: 크리처가 매력을 무한히 쌓던 것을 없앤다.
> 마이 룸에 두는 것은 **한 마리**뿐이고, 그 한 마리의 수치만 반영된다.

---

## 한 줄

```
가마솥에서 태어난다 → 마이 룸에 한 마리를 둔다(매력) → 다른 한 마리를 데리고 나간다(채집)
      ↑                        │                              │
      └── 남는 개체는 재료로 ──┘         먹이 → 로열티 ↑ → 더 얻는다
                                         특정 크리처는 매일 생산한다
```

**크리처가 몇 마리든 이득이 계속 쌓이는 구조를 없앤다.** 대신 **무엇을 고르느냐**가 남는다 —
방에 둘 한 마리, 데리고 나갈 한 마리, 재료로 녹일 한 마리.

---

## 0. ⚠️ 가장 먼저 — 매력 누적을 없애면 세이브가 깨진다

지금 `totalCharm()` 은 **가진 크리처 전부**의 `charmBonus` 를 더한다. 중복까지 다 더한다.

```js
// 지금 (game.js)
const creatureBonus = S.creatures.reduce((sum, cid) => sum + charmBonus(cid), 0);
return S.stats.beauty + S.stats.charm + creatureBonus;
```

이걸 「장착한 한 마리만」으로 바꾸면 **기존 플레이어의 매력 총합이 내려간다.** 그런데

```js
function isMapOpen(m)  { return totalCharm() >= m.unlock; }
function isZoneOpen(z) { return totalCharm() >= D.zoneUnlock(z.id); }
```

**열려 있던 맵과 지대가 다시 잠긴다.** 유니콘(+5) 셋을 가진 사람은 −10 이다.
등급(`getTier`)도 내려가고, 랭킹 점수(`meta.charm` → 서버 `charm` 컬럼)도 같이 내려간다.

### 그래서 마이그레이션을 반드시 같이 쓴다

`SAVE_VER` 를 올리고 `migrate(st, from)` 에서:

1. **가장 센 크리처를 자동으로 장착**한다 (`st.petRoom`)
2. **잃는 만큼을 `st.stats.charm` 에 옮겨 적는다** — 한 번만

```js
if (from < N) {
  const all  = (st.creatures || []).reduce((s, id) => s + charmBonusOf(id), 0);
  const best = 가장 센 크리처;
  st.petRoom = best ? best.id : null;
  st.stats.charm = (st.stats.charm || 0) + Math.max(0, all - (best ? best.charmBonus : 0));
}
```

이러면 **마이그레이션 순간의 총합이 그대로다.** 맵도 등급도 안 흔들린다.
그 뒤로 새로 만드는 크리처는 장착한 한 마리만 반영된다.

- 기존 진행을 **지우지 않는다** — 옮겨 적는 것이다 (`CLAUDE.md` 1번)
- `npm run test:save` 에 **이 케이스를 넣는다**: 크리처 셋을 가진 옛 세이브를 심고,
  불러온 뒤 `totalCharm()` 이 그대로인지 · 맵이 안 잠겼는지
- 서버 `charm` 컬럼은 **다음 저장 때** 새 값으로 바뀐다. 한 번은 랭킹이 출렁인다 —
  옮겨 적기가 있으니 총합은 같지만, 그래도 배포 뒤 랭킹을 한 번 확인할 것

> **매력에서 크리처를 아예 빼는 안**도 검토했다. 그러면 마이그레이션이 훨씬 간단하다.
> 하지만 「방에 둔 아이가 나를 조금 더 빛나게 한다」가 이 게임의 정서에 맞아서 남겼다.

---

## 1. 마이 룸 — 애착 크리처 한 마리 (`S.petRoom`)

- **마이 룸 › 인벤토리 › 🦋 크리처** 에서 크리처 사각 블럭을 누르면 장착된다
- 장착한 한 마리만 **마이 룸 그림에 나온다**
  (지금은 `S.creatures.slice(0, 4)` 를 다 그린다 — 한 마리로 줄인다)
- **그 한 마리의 수치만 반영된다** (매력 · 그리고 나중의 전투력)

### 교체

다른 블럭을 누르면 그것이 장착되고 앞의 것은 자동으로 해제된다. **해제 버튼은 없다** —
누르는 곳이 하나면 「어떻게 바꾸지」를 고민할 일이 없다.

```
나의 애착 크리처가 반짝 나비에서 유니콘으로 교체되었어요.
```

- 조사 처리는 이미 있다 — `josa(name, '으로')` 가 「로 / 으로」를 골라 준다
- **처음 장착하는 경우**는 앞 부분이 없다 → 문구가 따로 필요하다
  (「유니콘을 애착 크리처로 삼았어요.」)
- **같은 것을 다시 누른 경우**는 아무 일도 일어나지 않는다.
  토스트도 안 띄운다 — 안 바뀌었는데 「교체되었어요」가 뜨면 거짓말이다

### 도감

- 인벤토리 › 크리처 탭이 곧 도감이다. **가진 것은 그림, 못 가진 것은 `❓`**
- 레시피 북에 이미 「크리처」 카테고리가 있다 (`RECIPE_CATS`) — **겹친다.**
  레시피 북은 **만드는 법**(조합), 도감은 **가진 것과 그 아이의 수치**로 나눈다
- 미발견 크리처의 조합은 레시피 북 규칙을 그대로 따른다 (발견 전에는 `❓`)

---

## 2. 속성 — 크리처와 맵이 같은 표를 쓴다

```
불 ➔ 땅 ➔ 바람 ➔ 물 ➔ 불          (화살표 = 강하다)
빛 ↔ 암흑                          (서로 카운터)
```

여섯 속성이다. 크리처에도, **맵(필드)에도** 붙는다.

### 채집에서는 「같으면 좋다」만 쓴다

- **물 속성 크리처를 늪 지대에 데려가면** 얻는 것이 는다
- **불 속성 크리처를 화산 지대에 데려가면** 얻는 것이 는다

**상성 순환(➔)은 채집에 안 쓴다.** 「불 크리처를 땅 맵에 데려가면 조금 낫다」까지 넣으면
플레이어가 표를 외워야 하고, 그 대가로 얻는 재미가 없다.
**순환은 나중의 전투·약탈에 쓴다** — 거기서는 상대가 있으니 표를 볼 이유가 생긴다.

### 맵에 속성을 붙이는 일

**지금 맵이 51개다.** 손으로 붙이지 않는다 (`CLAUDE.md` 6번) — 지대·이름·재료 풀을 보고
**스크립트로 배정**하고, 배정 결과를 표로 뽑아 사람이 한 번 훑는다.

- 지대별로 치우치게 잡는다: 해안 → 물이 많이, 황무지 → 불·땅이 많이
- **한 지대가 한 속성으로 통일되면 안 된다.** 그러면 「지대 = 속성」이 되어
  속성이라는 축이 따로 있을 이유가 사라진다
- 늪(`반딧불 늪가` · `청개구리의 늪`)은 물, 화산은… **지금 화산 맵이 없다.**
  황무지에 하나 만들거나, 불 속성을 `소금 평원` · `뼈의 골짜기` 쪽에 준다

### 화면

- **맵 카드에 속성 이모지**를 붙인다. 누르면 근처에 토스트로 속성 이름
  (재료 칩을 눌러 이름을 보는 것과 같은 짜임 — `poolChip`)
- **크리처가 나오는 모든 곳에 속성을 표시**한다 (도감 · 동행 고르기 · 방 카드)

#### 속성 탭 — **이모지만** (정해짐)

크리처 도감의 카테고리 탭은 **글자 없이 이모지 하나씩**이다.
전체 / 불 / 땅 / 바람 / 물 / 빛 / 암흑 = 일곱 칸.

**누르면 두 가지가 같이 일어난다** — 그 속성으로 걸러지고, **토스트로 속성 이름**이 뜬다.
글자를 뺐으니 이름을 볼 길이 그것뿐이다. 이미 고른 탭을 다시 눌러도 이름은 다시 뜬다
(「이게 뭐였더라」가 그 동작이다).

- **`aria-label` 이 필수다.** 글자가 없으면 화면 낭독기에는 아무것도 없는 버튼이다.
  재료 칩(`poolChip`)이 이미 같은 방식이다 — 그 짜임을 그대로 쓴다
- **고른 표시가 전부를 짊어진다.** 글자가 없으니 `.cat-tab.active` 의 대비만으로
  「지금 어디를 보고 있는지」가 읽혀야 한다. 이모지 폭에서 다시 볼 것
- 265px 에서는 **두 줄로 접힌다** (칸 하나가 대략 38px, 일곱이면 266px).
  `.cat-tabs` 는 원래 줄바꿈이라 넘침 위반은 아니다 — **두 줄을 허용한다**
- 이모지만 남으면 **영어에서도 폭이 같다.** 지금까지 탭이 걸리던 자리가
  통째로 사라진다 (`checkUI` 의 라벨 2배 확대도 늘릴 글자가 없다)

#### ⚠️ 이모지가 이미 다른 데서 쓰이고 있다

속성·날씨 후보 이모지를 데이터와 대조해 봤다. **대부분 이미 쓰인다.**

| 후보 | 이미 쓰는 곳 |
|---|---|
| 🔥 불 | 재료 부싯돌 · 불의 심장 · 온기 물약 |
| 🪨 땅 | 재료 동굴 이끼 · 흔들바위 조각 |
| 💧 물 | 재료 이슬 · 산안개 방울 |
| ✨ 빛 | 재료 별모래 · 맵 별헤는 평지 |
| ☀️ 맑음(날씨) | **맵 햇살 언덕** |
| 🌬️ 바람(날씨) | **맵 바람개비 밭** · 재료 영원의 서리꽃 |
| 💨 · 🌑 · 🐾 · 🌧️ | 안 쓰임 |

**여섯 속성과 여섯 날씨를 안 겹치게 고르는 것은 사실상 불가능하다.**
겹쳐도 되는 곳(도감 탭)과 안 되는 곳(맵 카드)을 나눈다.

- **도감 탭은 상관없다.** 그 화면에는 재료 이모지가 없다
- **맵 카드는 문제다.** 같은 카드에 맵 이모지 + 재료 칩이 이미 있어서,
  속성 🪨 와 「동굴 이끼」 칩이 나란히 놓이면 무엇이 무엇인지 안 읽힌다.
  ☀️ 는 **햇살 언덕 카드에서 맵 이모지와 같은 그림**이 된다
- → **모양과 자리로 가른다.** 재료는 **둥근 칩**(`.spot-chip`), 속성·날씨는
  **카드 모서리의 작은 배지**로 둔다. 「특별한 맵」 배지(`.spot-badge`)가
  이미 왼쪽 위에 있으니 같은 언어를 쓰면 된다 — 이모지가 같아도 역할이 안 헷갈린다

---

## 3. 맵의 날씨

`비가 추적추적` · `해가 쨍쨍` · `바람 쌩쌩` · `모래 폭풍` … 맵마다 다르고 바뀐다.

- 맵 카드에 **날씨 이모지**. 누르면 토스트로 설명 (속성 배지와 같은 짜임 — 2장)
- 날씨가 채집 확률을 바꾼다 (5장)
- **속성 이모지와 겹치면 안 된다.** 한 카드에 둘 다 붙는다 —
  바람 속성 💨 / 바람 날씨 🌬️ / 모래폭풍 🌪️ 처럼 갈라 둔다

### ⚠️ 날씨는 `Math.random()` 으로 정하면 안 된다

`render()` 는 아주 자주 불린다. 렌더마다 뽑으면 **화면을 다시 그릴 때마다 날씨가 바뀐다** —
채집 한 번 할 때마다 비가 왔다 갔다 한다.

**시간과 맵 id 로 결정론적으로 정한다.**

```js
// 3시간마다 바뀐다. 같은 시간·같은 맵이면 누가 언제 보든 같은 날씨다
function weatherOf(mapId, now) {
  const slot = Math.floor(now / (3 * 3600e3));
  return WEATHERS[hash(mapId + ':' + slot) % WEATHERS.length];
}
```

- 리그의 NPC 를 **주차·리그로 시드를 고정해** 만드는 것과 같은 방식이다 (`leagueNpcs`)
- `now` 는 반드시 **`nowDate()`** 를 지난다 — 그래야 `tools/checktime.js` 가
  시계를 옮겨 놓고 날씨 변화를 검사할 수 있다
- 세이브에 안 남긴다. 저장하면 기기마다 달라지고, 서버 동기화 충돌거리가 하나 는다

---

## 4. 시간대 — 낮 · 해질녘 · 밤 · 새벽

**탐험 탭 위쪽에 지금 시각을 보여 주기만 하면 된다.** 마이 룸 시계(`zoneTime()`)와 같은 모양.

| 시간대 | 대략 | 이모지 |
|---|---|---|
| 새벽 | 05–08 | 🌅 |
| 낮 | 08–17 | ☀️ |
| 해질녘 | 17–20 | 🌆 |
| 밤 | 20–05 | 🌙 |

- **밤에 잘 나오는 재료 · 낮에 잘 나오는 재료**가 있다 (재료에 `time` 축을 하나 붙인다)
- 운동의 `EX_WHEN`(아침/낮/밤)이 이미 같은 짜임이다 — **경계를 맞출지 따로 갈지 정해야 한다.**
  따로 가면 「운동은 밤인데 채집은 해질녘」이 되어 설명이 두 벌이 된다
- 시각은 전부 **`nowDate()`** 를 지난다 (`CLAUDE.md`)

---

## 5. 확률 — 무엇이 무엇을 얼마나 바꾸는가

### 지금

```
채집 1회 = 재료 1개 (100%) + 음식 12%(FOOD_RATE) + 히든 재료 0.1%(SPECIAL_RATE)
```

**재료는 이미 100% 나온다.** 그러니 「획득 확률」로 올릴 것은 셋이다.

| 무엇 | 지금 | 바뀌는 것 |
|---|---|---|
| **덤** (재료가 하나 더) | 없음 | 속성 일치 · 날씨 · 시간 · 로열티 |
| **히든 재료** | 0.1% 고정 | 위 넷이 **아주 크게** 바꾼다 |
| 음식 | 12% | 손대지 않는다 (6장) |

### 곱셈이 아니라 덧셈으로 쌓는다

```js
덤확률 = 기본(0.08)
       + (속성 일치 ? 0.10 : 0)
       + (날씨 맞음 ? 0.06 : 0)
       + (시간 맞음 ? 0.06 : 0)
       + 로열티 보정(0 ~ 0.10)
// 상한 0.45

히든배율 = 1
        × (속성 일치 ? 3 : 1)
        × (날씨 맞음 ? 2 : 1)
        × (시간 맞음 ? 2 : 1)
// 최대 12배
```

> **왜 덤은 덧셈이고 히든은 곱셈인가.**
> 덤은 자주 일어나는 일이라 곱하면 금방 1.0 을 넘는다 — 상한에 붙어 버려서
> 조건을 맞춘 보람이 사라진다. 히든은 0.1% 대라 12배를 해도 1.2% 이고,
> **「조건을 다 맞추면 확 오른다」가 이 시스템의 재미다.**

### 히든 재료 확률을 다양하게

지금은 `SPECIAL_RATE = 0.001` **하나로 51개 맵이 전부 같다.** 등급을 나눈다.

| 등급 | 기본 확률 | 조건 다 맞췄을 때 | 어떤 재료 |
|---|---|---|---|
| 흔한 히든 | 0.5% | 6.0% | 초반 맵 |
| 보통 | 0.2% | 2.4% | 중반 |
| 귀한 | 0.05% | 0.6% | 후반 · 상급 레시피 재료 |

- ⚠️ **`specials` 기록은 행운 아우라로 간다** (`STORY.md` 표). 확률을 올리면
  행운이 그만큼 빨리 쌓인다. 카이로스가 **이정표만 노래하는** 방식이라 폭주하진 않지만,
  이정표에 닿는 속도가 빨라진다 — 카이로스를 만들 때 이 표를 같이 볼 것
- 지금 `SPECIAL_RATE` 를 쓰는 곳은 `gather()` 한 곳이다. 재료마다 다른 값을 주려면
  **재료 표(`INGREDIENTS`)에 `rare` 를 붙이고** 거기서 읽는다

---

## 6. 채집 결과의 「단서」

채집할 때마다 지금은 「🌿 이끼」 한 줄이다. 여기에 **왜 그게 나왔는지**를 붙인다.

```
비 오는 늪지대의 밤은 평소보다 더 축축해서 반딧불 이끼가 잘 발견되곤 하지!
이런 축축한 밤에는 청개구리가 제격이야!
다음 축축한 밤에는 도롱뇽을 데려가는 게 좋을 듯?      ← 조건이 안 맞았을 때
```

### ⚠️ 문장을 다 쓸 수는 없다

맵 51 × 날씨 4 × 시간 4 × 속성 6 = **4,896가지**다. 그것도 **두 언어**로.

**조각을 조립한다.**

```
{날씨·시간 조각} + {맵 조각} + {결과 조각}
"비 오는 밤은"   + "늪지대가 유난히 축축해서"  + "{재료}가 잘 나오지!"
```

- 조각마다 서너 개씩 두고 무작위로 고른다 → 문장 수가 곱으로 늘어난다
- **조사는 `josa()` 로 붙인다.** 재료 이름이 들어가는 자리마다 필요하다
- **조건이 안 맞았을 때가 더 중요하다.** 「다음엔 ○○를 데려가 봐」가
  **다음 채집의 이유**를 만든다 — 이 시스템에서 제일 값진 한 줄이다
- 개그 톤을 섞되 **매번 웃기려 들지 않는다.** 백 번 보는 문구다

### 토스트가 길어진다

지금 채집 토스트는 한 줄이다. 단서까지 붙으면 두세 줄이 된다.

- 재료 + 음식이 같이 나올 때 **한 줄로 합쳤던** 이유가 그대로 적용된다 —
  토스트를 두 번 띄우면 앞것이 잘린다
- **265px 에서 먼저 재 볼 것.** 영어가 더 길다

---

## 7. 먹이 · 로열티 · 버프

### 잡화 › 먹이 탭

```
잡화 ─┬─ 🧴 물약
      ├─ 🍞 음식     (사람이 먹는 것 — 포만감)
      └─ 🦴 먹이     (크리처가 먹는 것 — 스탯 버프 · 로열티)
```

> ⚠️ **지금 잡화는 두 칸이고 균등 분할이다** (`UI_POLICY.md`).
> 세 칸이 되면 265px·영어에서 다시 재야 한다.

- 먹이를 누르면 **먹이주기 팝업**이 뜬다: 크리처 고르기 → 먹이 종류 → 수량
- 크리처 카드에도 **「먹이 주기」 버튼**을 둔다 (반대 방향 진입)

### 두 축을 나눈다

| 축 | 성격 | 효과 |
|---|---|---|
| **스탯 버프** | **일시적.** 시간이 지나면 원래대로 | 전투력 · 채집 보정. 나가기 직전에 먹인다 |
| **로열티** | **영구.** 준 것이 쌓인다 | 탐험에서 **얻는 수량**이 는다 |

**이 둘을 하나로 묶으면 안 된다.** 버프만 있으면 먹이가 소모품일 뿐이고,
로열티만 있으면 「나가기 전에 먹인다」는 순간의 판단이 사라진다.

- 버프 만료는 **`nowDate()`** 를 지난다 → `checktime.js` 가 시계를 옮겨 검사한다
- 버프가 걸려 있는 동안 **크리처 카드에 남은 시간**을 보여 준다.
  안 보여 주면 「먹였는데 아무 일도 안 일어났다」로 읽힌다
- 로열티는 **확률이 아니라 수량**을 올린다. 확률은 이미 넷(속성·날씨·시간·기본)이
  붙어 있어서, 다섯 번째를 같은 자리에 얹으면 무엇이 효과가 있는지 아무도 모른다

---

## 8. 생산하는 크리처

**하루에 한 번, 그 크리처가 만든 것이 인벤토리에 쌓인다.**

| 크리처 | 생산물 | 형태 |
|---|---|---|
| 🐐 염소 | 신선한 염소젖 | 음식 |
| 🐔 닭 | 달걀 | 음식 |
| 🐝 (예) 꿀벌 | 꽃꿀 | 채집 재료 |

- **생산 기록 버튼** — 최근 **5일치**까지 보여 준다
- 정산은 AP 의 자정 충전과 같은 자리에서 한 번에 한다 (`dayKey()`)
- ⚠️ **방치 상한을 둔다.** 한 달 만에 들어온 사람에게 30일치를 쏟으면
  「돌아왔더니 다 있네」가 되어 매일 들어올 이유가 없어진다.
  **5일치가 상한**이고, 기록도 딱 그만큼 보여 준다 — 상한과 기록 길이가 같으면
  「5일치까지만 쌓인다」가 설명 없이 읽힌다
- **장착한 한 마리만 생산하는가, 가진 전부가 생산하는가**를 정해야 한다.
  전부가 생산하면 0장에서 없앤 「무한 누적」이 생산물 쪽으로 되살아난다.
  → **장착한 한 마리 + 동행 한 마리**까지만 생산하는 쪽을 권한다

---

## 9. 크리처를 재료로 되돌린다

**크리처는 가마솥에서 태어난다. 그러니 가마솥으로 돌아갈 수도 있다.**

- **1개체는 못 녹인다.** 초과분(2번째부터)만 재료가 된다
- 남는 개체가 생기는 것이 지금은 「매력만 조금 더」였는데(그것도 0장에서 없앴다),
  이제는 **다음 크리처의 재료**가 된다 — 중복이 처음으로 쓸모를 갖는다

### ⚠️ 이게 생각보다 크게 건드린다

지금 조합 입력(`RECIPES[].inputs`)은 **재료 id 뿐**이고, 여러 곳이
`D.INGREDIENTS[id]` 를 **바로** 조회한다 — 가방 렌더 · 재료 칩 · `weightedPick` · 솥 슬롯.

크리처 id 가 입력에 섞이면 그 조회가 전부 `undefined` 가 된다.

**조회를 한 곳으로 모은다.**

```js
function itemOf(id) {           // 재료 → 크리처 순으로 찾는다
  return D.INGREDIENTS[id] || creatureResult(id) || null;
}
```

- 그리고 **중복 조합 검사가 지금 없다.** `RECIPE_MAP[recipeKey(inputs)] = result` 라
  같은 조합을 쓰면 **조용히 덮어쓰고** 앞 레시피가 사라진다. 오류도 안 난다.
  `npm test` 도 안 잡는다 — **크리처를 늘리기 전에 이 검사부터 만든다**
- 크리처를 녹일 때 **로열티와 버프도 같이 사라진다.** 확인 모달에서 그것을 말해 준다

---

## 10. 전투 · 약탈 — **아직 만들지 않는다**

나중에 「밭」과 「약탈」이 들어온다. 지금 하는 일은 **자리만 비워 두는 것**이다.

```js
// data.js 의 크리처에 붙여만 두고, 아직 아무도 안 읽는다
combat: { atk: 12, matk: 4, def: 8, mdef: 6 }
combatPower = atk + matk + def + mdef
```

- 판정은 「**지키는 크리처보다 보낸 크리처가 강해야 한다**」
- 로열티가 약탈 성공률을 올린다 (탐험에서 수량을 올리는 것과 같은 축)
- 속성 **순환**(불➔땅➔바람➔물➔불, 빛↔암흑)이 여기서 처음 쓰인다

> ⚠️ **약탈은 서버가 필요하다.** 지금 서버는 세이브 저장소 + 누적 매력 랭킹뿐이다.
> 남의 밭을 털려면 **남의 상태를 읽고 쓰는 것**이 필요하고, 그건 지금 구조에 없다.
> 리그가 아직 NPC 인 것과 같은 이유다 (`CLAUDE.md` 「아직 안 한 것」).
> **수치만 미리 넣어 두고, 밸런싱은 서버가 생긴 다음에 한다.**

---

## 11. 세이브

```js
S.petRoom  = null;   // 마이 룸 애착 크리처 id
S.petField = null;   // 동행 크리처 id
S.pets     = {};     // 크리처별 상태 { [id]: { loyalty, buffEnd, buff:{}, fed } }
S.produced = [];     // 생산 기록 (최근 5일치)
S.producedDay = '';  // 마지막으로 정산한 날
```

- **위 다섯은 없던 칸을 더하는 것**이라 그것만으로는 마이그레이션이 필요 없다
- **하지만 `SAVE_VER` 는 올려야 한다** — 0장의 매력 옮겨 적기 때문이다.
  그건 「없던 칸 추가」가 아니라 **계산 규칙 변경**이다
- `S.petRoom` · `S.petField` 에 **없는 크리처 id 가 남을 수 있다**
  (초기화 · 데이터 변경 · 재료로 녹인 뒤). 읽는 곳에서 한 번 거른다
- 크리처를 재료로 녹였는데 그게 장착 중이었으면 **장착도 같이 풀어 준다**

---

## 12. 검사

| 무엇 | 어떻게 |
|---|---|
| **매력이 안 내려가는가** | 크리처 셋을 가진 옛 세이브를 심고 `totalCharm()` 과 열린 맵 수를 비교 (`test:save`) |
| 확률이 **실제로** 달라지는가 | `Math.random` 을 갈아 끼우고 1만 번 돌려 덤·히든 비율을 잰다 |
| 덤 확률 상한 | 조건을 다 맞춘 최고값이 상한을 안 넘는가 |
| 날씨가 **안 흔들리는가** | 같은 시각에 `render()` 를 열 번 불러도 같은 날씨인가 (여기서 `Math.random` 실수가 잡힌다) |
| 날씨·시간대가 **바뀌는가** | `checktime.js` 로 시계를 옮겨 3시간 뒤 · 밤 · 새벽을 만든다 |
| 먹이 버프 만료 | 시계를 옮겨 놓고 스탯이 원래대로 돌아오는가 |
| 생산 상한 | 30일을 건너뛰어도 5일치만 들어오는가 |
| 레시피 **중복 조합** | **지금 없다.** 9장 — 크리처를 늘리기 전에 만든다 |
| 없는 크리처를 장착 중일 때 | `S.petRoom` 에 안 가진 id 를 넣고 화면을 그려도 안 죽는가 |
| 새 화면들 | 먹이주기 팝업 · 동행 고르기 · 도감 탭 — 평소 `display:none` 이라 검사에서 빠진다 (`checkui.js` 에 넣는다) |
| 탭 폭 | 잡화 탭 세 칸 — **265px 영어**에서 넘치는가 (속성 탭은 이모지만이라 언어를 안 탄다) |
| 이모지만 있는 탭 | `aria-label` 이 붙어 있는가 · 고른 탭이 대비로 구분되는가 (`checkTextStyle`) |
| 번역 | `npm run test:i18n` (자동) |

---

## 13. 만드는 순서

**한 번에 다 만들면 무엇이 재미있는지 알 수 없다.** 각 단계가 혼자서도 말이 되게 잘랐다.

| 단계 | 내용 | 왜 여기서 끊나 |
|---|---|---|
| **1** | **매력 누적 제거 + 마이 룸 한 마리 + 교체 토스트** | 세이브를 건드리는 유일한 단계다. 여기만 따로 배포해서 랭킹·해금을 확인한다 |
| **2** | 속성 표 · 맵 속성 배정(스크립트) · 화면 표시 | 확률은 아직 안 바꾼다. **표시만** 해서 눈에 익게 한다 |
| **3** | 동행 고르기 + **덤 한 줄** (속성 일치만) | 여기서 「재미있는가」를 판단한다. 아니면 4번부터 안 만든다 |
| **4** | 날씨 · 시간대 · 확률 표 전체 · 히든 확률 다양화 | 3번이 재미있었을 때만 |
| **5** | 단서 문구 (조각 조립) | 4번이 있어야 할 말이 생긴다 |
| **6** | 먹이 · 로열티 · 버프 | |
| **7** | **중복 조합 검사** → 크리처 추가 · 재료로 녹이기 | 검사가 먼저다 |
| **8** | 생산 | |
| **9** | (나중) 전투 · 약탈 — 서버가 생긴 뒤 | |

---

## 14. 아직 안 정한 것

- **채집 시간대 경계를 운동(`EX_WHEN`)과 맞출 것인가** (4장)
- **속성·날씨에 어떤 이모지를 줄 것인가** (2장) — 안 쓰이는 것은 💨 🌑 🐾 🌧️ 넷뿐이다.
  나머지는 겹치므로 **배지 모양으로 갈라야** 한다
- **생산을 장착한 한 마리만 할 것인가** (8장) — 전부 하면 무한 누적이 되살아난다
- **동행과 애착이 같은 마리여도 되나** — 지금 초안은 허용이다.
  막으면 「두 마리 이상 가져야 한다」는 압박이 생기는데, 그게 좋은지 나쁜지 아직 모르겠다
- **크리처를 몇 마리까지 늘릴 것인가** — 속성 여섯 × 등급 셋이면 18마리다.
  지금은 **셋**뿐이다. 이름·그림·조합·영어 이름이 마리당 한 벌씩 필요하다
- **크리처 그림** — 지금은 전부 이모지다. 생산·먹이·전투까지 붙으면
  이모지 하나로는 정보를 못 담는다

---

## 15. 1판에서 가져온 것 (여전히 유효)

- **음식 확률은 손대지 않는다.** 이미 남아돈다 — 하루 100번 채집하면 포만감 260쯤 들어오는데
  하루에 줄어드는 것은 48~120 이다. 폭식은 음식이 없어서가 아니라 **먹는 것을 잊어서** 일어난다
- **덤은 사실상 AP 상한 증가다.** 덤 20%는 「AP 가 20% 늘었다」와 같고,
  AP 충전은 다이아로 파는 상품이다 (`D.ENERGY.chargeCost`). 상한을 넘기기 전에 상점 값을 볼 것
- **상급 크리처는 넓고 얕게, 기초는 좁고 깊게.** 유니콘이 모든 축에서 최고면
  유니콘 하나로 끝난다 — 특정 속성 맵에서는 그 속성의 기초 크리처가 더 낫게 잡는다
