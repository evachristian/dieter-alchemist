# 다이어터 연금술사 — 작업 지침

코지 연금술 게임. 채집으로 재료를 모아 물약을 만들고, 마시면 캐릭터가 날씬해진다.
빌드 도구 없는 순수 HTML/CSS/JS + 세이브용 Express 서버.

**모든 코드 주석과 문서는 한국어로 쓴다.** (기존 코드가 전부 그렇게 돼 있다)

---

## 구조

```
dieter-alchemist/          ← 저장소 루트. Railway Root Directory 는 비워 둔다
├── index.html             게임 셸. 스플래시·인트로는 인라인 <style>/<script>
│                          (style.css 캐시가 깨져도 항상 뜨게 하려는 의도된 중복)
├── data.js                재료 111 · 채집지 51 · 레시피 136(크리처 30 · 밭 물약 6) · 솥 11 → window.GameData
├── game.js                상태·로직·렌더링. 세이브는 localStorage
├── sync.js                서버 동기화 → window.Sync
├── avatar.js              SVG 페이퍼돌 → window.Avatar
├── intro.js               튜토리얼 인트로 → window.Intro
├── tutorial.js            튜토리얼(인트로 다음) → window.Tut
├── portrait.js            인물 초상화 → window.Portrait
├── creature.js            크리처 SVG (부품 조합) → window.Creature
├── village.js             마을 지도·건물 안 → window.Village
├── i18n.js                한국어/영어 → window.I18N
├── sfx.js                 WebAudio 효과음(에셋 없음) → window.Sfx
├── a11y.js                검증기 → checkTextStyle / checkLayout / checkLocked / checkUI
├── style.css
├── HANDOFF.md             새 세션 인수인계 (배포 주소·시작 전 확인·남은 작업)
├── TEXT_POLICY.md         글자 색·폰트 정책
├── UI_POLICY.md           컴포넌트 정책 (탭·가변폭·잠금 표현 등)
├── ART_POLICY.md          그림 정책 (듀오링고풍 플랫 2D 벡터 · 생성 프롬프트)
├── STORY.md               세계관·인물·3막·키워드 시스템 (**아직 안 만든 것**)
├── ACTS.md                1~5막 퀘스트 16 · 컷씬 41 전체 표 (무엇이 어디에 붙어 있는지)
├── EXERCISE.md            운동·포만감·스태미나 시스템 (수치를 바꾸기 전에 읽을 것)
├── FARM.md                밭을 탐험의 세 번째 갈래로 키우는 기획 (**아직 안 만든 것**)
├── QUEST.md               퀘스트·컷씬·스토리 다시보기 (1~4단계까지 만듦)
├── CREATURE.md            크리처 — 1~9단계 (녹이기·생산·밭과 약탈까지). 밸런싱·연출까지 끝
├── tools/hooks/           git 훅 — 세션마다 `git config core.hooksPath tools/hooks`
└── server/
    ├── index.js           API + 게임 정적 서빙
    ├── store.js           Postgres / 파일 / 메모리 3단 폴백
    ├── battle.js          밭·약탈 규칙 (9단계). data.js 를 **그대로 읽는다**
    ├── test.js            npm test
    └── README.md          Railway 배포 안내
```

전역은 `window.*` 로 노출한다. 모듈 시스템·번들러 없음. `<script>` 순서가 곧 의존성 순서이고,
**sync.js 는 game.js 보다 먼저** 와야 한다 (game.js 의 `save()` 가 `Sync.push` 를 부른다).

---

## 저장소

`github.com/evachristian/dieter-alchemist` (**공개**). 기본 브랜치 `main`.

공개 저장소다 — **비밀값을 커밋하지 않는다.** 한 번 올리면 지워도 히스토리에 남는다.
접속 정보·토큰은 Railway 의 Variables 에만 두고, 저장소에는 참조만 적는다.

커밋 작성자는 **이 저장소에만** 따로 잡아 두었다 (`git config user.name/email`, `--global` 아님).
전역 설정과 다르니 `git config user.email` 로 확인하고 커밋할 것.

### ⚠️ 커밋 안 한 파일에 `git checkout <파일>` 을 쓰지 않는다

검사기가 진짜로 잡는지 보려고 **일부러 망가뜨렸다가 되돌릴 때** 쓰기 쉬운데,
그 파일에 아직 커밋 안 한 작업이 들어 있으면 **그것까지 같이 날아간다.**
실제로 `data.js` 와 `tools/gencreature.js` 의 4단계 작업을 한 번 통째로 잃었다.

되돌릴 것이 있으면 **먼저 복사해 두고 복사본으로 되돌린다.**

```bash
cp data.js /tmp/…/data.bak     # 망가뜨리기 전에
# … 일부러 고쳐서 검사기가 잡는지 본다 …
cp /tmp/…/data.bak data.js     # git checkout 이 아니라 이것으로 되돌린다
```

### 세션을 시작하면 훅부터 건다

```bash
git config core.hooksPath tools/hooks
```

**한 줄이지만 안 하면 조용히 어긋난다.** `.git/hooks` 는 저장소에 안 들어가서
새 세션마다 비어 있다 — 그래서 훅을 `tools/hooks/` 에 두고 이 설정으로 가리킨다.

지금 걸리는 것은 `post-commit` 하나다: **커밋할 때마다 세션 작업 브랜치를
`main` 과 같은 자리로 옮긴다.**

이 저장소에서는 `main` 에서 작업하고 `git push origin main:<브랜치>` 로 올린다.
그러면 원격 브랜치는 늘 맞는데 **로컬 브랜치 참조만 뒤처진다** —
「머지해줘」 할 때마다 안 맞아 보이던 자리가 그것이다 (실제로 두 번 겪었다).

훅은 **`main` 위에서 커밋할 때만** 움직이고, 그 브랜치에만 있는 커밋이 하나라도 있으면
건드리지 않는다. 밀어내지 않고 알리기만 한다.

---

## 반드시 지킬 것

### 1. 세이브 기본값을 바꾸면 마이그레이션도 같이 써야 한다

**이 프로젝트에서 같은 실수를 두 번 했다.** `defaultState()` 의 기본값만 바꾸면
**이미 플레이 중인 사람에게는 반영되지 않는다.** 불러올 때 `Object.assign(defaultState(), 저장값)`
이라 저장된 값이 기본값을 덮어쓰기 때문이다.

기본값을 바꿨다면:

1. `SAVE_VER` 를 올린다
2. `migrate(st, from)` 에 `if (from < N) { ... }` 를 추가한다
3. **기존 진행은 지우지 말고 없는 것만 채운다**

```js
if (from < 3) {
  if (!Array.isArray(st.discovered)) st.discovered = [];
  STARTER_RECIPES.forEach(id => { if (!st.discovered.includes(id)) st.discovered.push(id); });
}
```

버전은 반드시 **저장된 값**(`parsed.ver`)에서 읽는다. 병합 후의 `st.ver` 는
`defaultState()` 가 이미 최신 버전을 넣어 놔서 판별에 쓸 수 없다. (이것도 한 번 틀렸던 부분)

검사할 때는 **세이브가 없는 새 플레이어만 보지 말고** 예전 버전 세이브를 심어 놓고 확인할 것.

```bash
npm run test:save   # 옛 세이브를 심어 놓고 진행이 살아남는지 (tools/checksave.js)
```

**마이그레이션이 쓰는 값은 `load()` 위쪽에 둔다.** `load()` 는 game.js 를 끝까지 읽기 전에
불린다 — 아래쪽에 `const` 로 둔 표를 `migrate` 가 건드리면 `ReferenceError` 가 나고,
`load()` 의 `catch` 가 그것을 삼켜 **세이브가 통째로 기본값으로 되돌아간다.**
화면에는 아무 오류도 안 뜬다: 어제까지 키우던 캐릭터가 그냥 처음 상태로 보인다.
(세이브 9 를 쓰다가 실제로 만들었고, `checksave` 가 그때 생겼다)

### 2. 한국어를 고치면 영어도 같이 고친다

**문자열은 언제나 두 언어를 함께 넣는다.** 한국어만 추가하면 영어 화면에 한글이
그대로 남는데, 이건 눈에 잘 안 띈다 — 영어로 바꿔 놓고 그 화면까지 들어가 봐야 보인다.

더 중요한 이유는 **폭 검사**다. 영어는 대체로 한국어보다 길어서, 번역이 빠진 자리는
좁은 화면에서 깨지는지 한 번도 확인받지 못한 자리다. `checkUI()` 의 '라벨 2배 확대' 도
**있는 문자열만** 늘린다.

```bash
npm run test:i18n   # ko 에만 있는 키 + 영어 이름이 없는 데이터 id 를 전부 찾아 준다
```

`npm test` 가 이걸 먼저 돌린다. 빠진 게 있으면 서버 검사까지 가지 않고 멈춘다.

두 군데를 본다 — `STRINGS`(UI 문자열)와 `NAMES`(재료·맵·솥·레시피·옷·지대 이름).
데이터를 늘리면 `NAMES.en` 에도 반드시 같이 넣는다. 안 넣으면 `N()` 이 한국어를
그대로 돌려주기 때문에 **동작은 하는데 번역만 조용히 빠진다.**

개발용(임시) 문구도 예외가 아니다. 지워질 것이라도 화면에 있는 동안은 폭을 차지한다.

### 3. 화면을 손댔으면 검증기를 돌린다

브라우저 콘솔에서:

```js
await checkUI()   // 모든 언어 × 라벨 2배 확대로 대비·레이아웃·잠금 표현을 한 번에
```

`checkUI` 는 **async 다.** `await` 없이 부르면 결과 대신 Promise 가 찍혀 `{}` 로 보인다 —
0건으로 착각하기 쉽다. 개별 검사(`checkTextStyle()` `checkLayout()` `checkLocked()`)는 동기다.

0건이 아니면 커밋하지 않는다.

#### 측정 조건은 검증기가 스스로 맞춘다 (예전 78건 유령의 원인)

옛날에는 화면이 페이드 인 하는 도중에 재면 **대비 위반이 수십 건 허위로 잡혔다.**
`.screen` 의 `fade` 애니메이션이 불투명도 0 에서 시작하는데, 창이 숨겨지거나 백그라운드면
프레임 시계가 멈춰 **첫 프레임에 그대로 서 있는다.** 그러면 그 화면 글자가 통째로
`흰 글자 / 흰 배경 / 대비 1.00` 으로 보고됐다 — 마이 룸에서 78건.

지금은 검증기가 **재기 전에 끝이 있는 애니메이션·트랜지션을 모두 끝 상태로 보낸다**(`settle()`).
프레임이 안 도는 환경에서도 즉시 반영되므로 측정 조건이 결정적으로 정해진다.
무한 반복(장식용 반짝임)은 끝이 없으니 건드리지 않는다. 굳이 애니메이션을 살려 둔 채 재려면
`checkTextStyle({ settle:false })`.

같이 들어간 안전장치 — **0건이 항상 통과는 아니라는 것을 구분하기 위한 것들이다.**

- 조상이 투명하면 자기 `opacity` 가 1이어도 안 보이는 글자다. 그런 요소는 검사에서 빼고
  `안보여서건너뜀` 수로 따로 알린다. 지금 보고 있는 화면이 통째로 여기 잡히면 측정 조건을 의심할 것
- 창 폭·높이가 0 이면(패널이 접혔거나 헤드리스) **잴 수 없다고 알리고 `pass:false` 를 낸다.**
  이 상태에서 재면 모든 요소가 부모 밖으로 나간 것처럼 보여 넘침 22건이 허위로 잡혔다
- 배경색을 끝까지 못 찾아 흰색으로 **가정한** 경우는 반드시 `(추정)` 으로 표시된다.
  실측 흰색과 구분이 안 돼 오진의 빌미가 됐던 부분이다

수준이 `주의` 인 항목(라벨 잘림 등)은 위반 수에 들어가지 않는다. `위반` 만 0으로 만들면 된다.

#### 아무것도 없는 세이브는 검사망에 구멍을 남긴다

**가진 것이 없으면 그 칸은 아예 그려지지 않는다.** 재료가 없으면 가방 칸이, 물약이 없으면
선반 칸이 없다. 접혀 있는 것(채집 가방)은 `display:none` 이라 또 빠진다.
이 구멍에 재료 개수 글자(**1.4:1**)와 물약 카드의 `?`(**10.6px**)가 오래 숨어 있었다 —
화면은 통과인데 실제로 플레이하면 보이는 자리다.

```bash
FULL=1 node tools/checkui.js showcase atelier gather
```

`FULL=1` 이면 재료·물약·크리처를 채우고, 채집 가방과 **개발용(임시) 블록을 펼치고**,
마이 룸 하위 탭(옷 / 물약 / 크리처)을 하나씩 돌며 잰다.
**화면을 손댔으면 이것도 같이 돌린다.**

개발용 블록도 접혀 있으면 `display:none` 이라 통째로 빠진다. 출시 전까지는 사람이
실제로 보는 자리이므로 같이 잰다 — 펼쳐서 재기 시작하자마자 265px·영어 라벨에서
`.tune-label` 이 줄을 밀어내 가로 스크롤이 생기던 것이 잡혔다.

#### ⚠️ `checkUI()` 0건이 **시트 안까지 본 것은 아니다**

`checkLayout()` 은 정해진 선택자(버튼·칩·카드…)만 재는데, 모달 시트 속의 줄은 대개
그냥 `div` 라 대상이 아니다. 게다가 목록에 `overflow-y: auto` 를 주면 **가로 overflow 도
`auto` 로 계산돼** 넘침 검사에서 통째로 빠진다.

실제로 밭 시트의 줄을 일부러 `nowrap` 으로 만들어도 **모든 화면이 통과**로 나왔다.
그래서 `checkui.js` 에 `__cardFits(선택자)` 를 두고 **내용이 제 칸보다 넓은 요소**를
직접 찾는다 (같은 사보타주에서 19px · 172px 로 잡혔다).
**새 모달 시트를 만들면 `checkui` 에 그 시트를 넣고 이것도 같이 부른다.**

또 하나 — **값을 심어 놓고 여는 시트는 재기 직전에 아직 그대로인지 확인한다.**
`openFarm()` 처럼 열면서 서버에서 다시 받아 오는 화면은, 기다리는 사이에 심은 값이
날아가고 「지금은 볼 수 없다」 한 줄만 남는다. 그 상태의 0건은 아무것도 안 잰 것이다
(78건 유령과 같은 종류의 실수다).

고친 게 진짜 원인인지 보려면 **바꾼 것을 되돌려 같은 조건에서 한 번 더 재라.**
수치가 그대로면 원래 있던 문제다.

- 글자 색·크기 규칙 → `TEXT_POLICY.md`
- 탭·가변 폭·잠금 표현 → `UI_POLICY.md`
- 그림의 결·생성 프롬프트 → `ART_POLICY.md`
- **정책을 바꾸면 `a11y.js` 의 상수도 같이 고친다.** 문서와 검증기는 짝이다.

예외를 늘리기 전에 색을 고칠 수 있는지 먼저 보라. 지금까지 예외는 근거를 적고 최소한만 추가했다.

### 4. 잠긴 콘텐츠는 공통 규칙을 지난다

컨텐츠 언락으로 잠긴 것은 `style.css` 의 **잠긴 콘텐츠 공통 표현** 블록 하나만 쓴다
(`filter: saturate(0.15); opacity: 0.8`). 화면마다 따로 흐리게 만들지 않는다.
새 잠금 UI 를 만들면 그 블록의 선택자 목록에 추가하고, `checkLocked()` 로 확인한다.

⚠️ **값을 «베껴 쓰는» 것은 목록에 넣은 것이 아니다.** 비법서의 잠긴 재료 줄
(`.pg-row.locked`)이 「공통 표현을 쓴다」고 적어 놓고 실제로는 `filter`·`opacity` 를
그대로 복사해 두고 있었다 — 공통 블록을 고쳐도 그 줄만 옛 값에 남는 상태였고,
`a11y.js` 의 `lockedSelector` 에도 없어서 **`checkLocked` 가 한 번도 안 봤다.**
둘 다 목록에 넣어 고쳤다 (그 줄만 노린 사보타주로 잡히는 것을 확인했다).

⚠️ **잠금 검사는 채도와 불투명도가 «둘 다» 그대로일 때만 잡는다**(`&&`).
하나만 남아도 잠겨 보이기 때문인데, 그래서 **한쪽만 망가뜨리는 사보타주는 통과한다** —
검사기를 못 믿을 이유가 아니라 사보타주를 잘못 고른 것이다 (실제로 한 번 헛짚었다).

채도를 주로 쓰는 이유: `saturate()` 는 명도를 보존해 글자 대비를 떨어뜨리지 않는다.
`opacity` 는 대비를 그대로 깎으므로 0.8 까지만 쓰고, 잠긴 글자는 `--ink` 로 진하게 잡는다.

### 5. 파일을 고쳤으면 캐시 버스터를 올린다

`index.html` 의 `?v=YYYYMMDD<letter>` 를 일괄 치환한다. 안 올리면 사용자가 옛 파일을 계속 받는다.

```bash
sed -i 's/v=20260813a/v=20260813b/g' index.html
```

### 6. 데이터는 손으로 늘리지 말 것

재료·채집지·레시피는 규칙에 따라 생성했다. 대량으로 늘릴 일이 있으면
스크립트로 만들고 **중복 id · 중복 조합 · 없는 재료 참조 · 등급별 진입 점수**를 검사한다.
후반 맵에서만 나오는 재료가 초반 레시피에 들어가면 밸런싱이 깨진다.

커스터마이징 150벌(헤어 30 · 서클렛 20 · 귀걸이 30 · 목걸이 30 · 장갑 20 · 구두 20)도
그렇게 만들었다. `data.js` 와 `i18n.js` 의 `// <<<GEN:...` 구간은 **손으로 고치지 않는다.**

```bash
npm run gen:wardrobe    # 축 표(tools/genwardrobe.js) → data.js · i18n.js 를 다시 쓴다
npm run gen:creature    # 크리처 30종도 같은 방식이다 (tools/gencreature.js)
npm run gen:farm        # 특수 작물 6종 (tools/genfarm.js)
npm run gen:mapattr     # 채집지 51곳의 속성 (tools/genmapattr.js)
```

⚠️ **`npm test` 가 생성기 넷을 `--check` 로 먼저 돌린다** — 축 표를 고치고 다시
안 뽑았으면 거기서 멈춘다.

**크리처 서른 종도 축 표에서 나온다.** 속성 여섯 × 다섯 마리이고, 레시피 조합·수치·
그림 부품·영어 이름이 한 줄에서 같이 나온다. 여기서도 **옛 id 셋**(`butterfly` ·
`frog` · `unicorn`)은 절대 새로 뽑지 않는다 — 세이브에 들어 있다.

**`npm test` 가 `tools/checkdata.js` 를 돌린다.** 레시피 **중복 조합**(`RECIPE_MAP` 이
조용히 덮어써서 레시피 하나가 소리 없이 사라지던 자리) · 중복 결과물 · 없는 재료 참조 ·
id 중복을 본다.

축 표가 유일한 원본이라 중복 id·중복 조합이 애초에 생기지 않고, **한국어와 영어 이름이
같은 표에서 같이 나온다** — 한쪽만 늘어나는 일이 없다. `npm test` 가 맨 먼저
`genwardrobe.js --check` 를 돌려 생성 결과와 파일이 어긋났는지 본다.

**옛 id 는 절대 새로 뽑지 않는다.** 그 옷을 입고 있는 세이브가 있어서 id 가 바뀌면
착장이 통째로 날아간다. 생성기의 `LEGACY` 표가 '지금 모습 그대로 나오는 조합' 에
원래 id 를 붙이고, 생성 후 그 24개가 다 살아 있는지 다시 확인한다.

---

## 서버 동기화 (sync.js)

**로컬이 항상 진짜다.** 서버가 죽어도, 오프라인이어도 게임은 그대로 돌아간다.
서버는 기기를 바꾸거나 브라우저 데이터를 지웠을 때 되찾기 위한 사본이다.

- 저장할 때마다 `S.rev` 가 1씩 오른다. 서버에 더 큰 rev 가 있으면 `409` + 서버 세이브를 돌려주고,
  클라이언트가 그걸 채택한다 → **두 기기 동시 플레이 시 먼저 저장한 쪽이 이긴다**
- 저장은 3초 디바운스, 탭을 닫을 때는 `keepalive` 로 즉시 전송, 실패하면 지수 백오프
- ⚠️ **코드로 이어할 때는 «먼저 받아 보고, 있으면 갈아탄다»** (`Sync.peek()`).
  예전에는 형식만 보고 곧바로 신원을 갈아엎은 뒤 로컬 세이브를 지우고 새로고침했다 —
  코드에 오타가 하나 있으면 **되돌릴 방법 없이 빈손으로 새 게임**이 시작됐다.
  원래 신원까지 잃은 채로. `peek` 은 `me` 를 안 건드리고 물어보고, **누구의 세이브인지
  보여 준 다음에야** 갈아탄다. 404(없는 아이디) · 403(잘린 코드) · 형식 오류 ·
  못 닿음을 **갈라서** 말한다 — 하나로 묶으면 오프라인인 사람이 코드를 계속 다시 친다.
  `npm run test:code` 가 기기 둘을 띄워 실제로 옮겨 본다
- 로그인이 없어서 기기마다 무작위 아이디+비밀키를 만들고, 둘을 `.` 로 이은 것이 **복구 코드**다.
  구분자가 `.` 인 이유: 아이디·비밀키가 base64url 이라 `-` 와 `_` 를 포함한다. `-` 로 나누면
  아이디 한가운데가 잘려 **엉뚱한 신원이 조용히 만들어진다** (실제로 겪은 버그)
- **게임 초기화 시 서버 사본도 지운다.** 로컬만 지우면 다음 접속에 서버에서 되살아난다.
  지운 뒤에는 `wiped` 플래그로 이후 전송을 전부 막는다 — 지우는 순간 이미 날아가고 있던
  저장 요청이 나중에 도착해 세이브를 부활시킨 적이 있다

### ⚠️ 밭만은 반대다 — 서버가 정본을 갖는다 (크리처 9단계)

**게임의 다른 모든 것은 로컬이 진짜고 서버가 사본인데, 밭은 반대다.**
남이 내 밭에서 가져간 결과가 적히는 곳이라 세이브 안에 두면 **내가 다음에 저장하는
순간 없던 일이 된다.**

- `PUT /api/save` 는 밭을 **안 건드린다.** 파일·메모리 저장소는 레코드를 통째로 다시
  쓰므로 `put()` 에서 **밭을 옮겨 담아야 한다** — 빠뜨리면 저장할 때마다 밭이 사라진다.
  **컬럼을 늘릴 때와 같은 규칙이다: 저장소 3곳을 다 고친다**
- **판정도 서버가 한다.** 클라이언트가 보내는 것은 「누구의 밭을 턴다」 하나뿐이고,
  전투력·속성·로열티는 서버가 세이브에서 직접 뽑는다 (`server/battle.js`).
  수치는 `data.js` 를 그대로 읽는다 — 서버에 사본을 만들면 축 표를 다시 뽑을 때마다 어긋난다
- **수확·약탈은 `nonce` 로 멱등이다.** 응답을 못 받고 재시도했을 때 거둔 것이 사라지거나
  약탈권이 두 번 깎이면 안 된다 (이름 예약과 같은 이유)
- **남의 `playerId` 는 절대 안 내보낸다** — 그것이 곧 신원이다. 상대는 **이름**으로 가리킨다
- **밭을 못 받아 왔을 때 빈 밭으로 그리지 않는다.** 빈 밭으로 보이면 털린 줄 안다
- ⚠️ **한 화면 안에 정본이 둘이라는 것을 잊지 말 것.** 밭(칸·이삭·방패·약탈권)은
  서버가 정본이지만 **부대(`S.farmDef`·`S.farmAtk`)는 세이브 안에 있다.**
  방어대 줄을 서버가 준 값으로 그렸다가 **자리를 바꿔도 화면이 안 바뀌는** 버그를 냈다 —
  저장이 올라가고 다시 받아 와야 바뀌니 사람 눈에는 안 먹히는 버튼이다.
  서버 값은 「서버가 지금 무엇으로 지켜 줄지」이고, 화면에 보여 줄 것은 「방금 고른 것」이다

### 이름은 유일하다 (서버가 정한다)

**이름은 신원이 아니다.** 신원은 `playerId` 가 맡는다. 이름은 사람이 서로를 알아보는
딱지일 뿐이고, 세이브를 읽고 쓰는 열쇠는 여전히 `playerId` + `secret` 이다.

`playerId` **를 다른 것으로 바꾸지 말 것.** 복구 코드가 여기에 묶여 있어서, 새 UID 를
발급하면 기존 플레이어의 세이브 소유자와 복구 코드를 전부 갈아엎어야 한다.
이메일 로그인을 붙일 때도 계정에 `playerId` 를 연결하기만 하면 된다.

이름을 다루는 코드를 고칠 때 지킬 것:

1. **이미 남이 쓰는 이름(409)은 그 자리에서 막는다.** 다른 이름을 지으면 되는 일이다.
   **서버에 닿지 못한 경우는 막지 않는다** — 튜토리얼 마지막 관문에서 오프라인이라는 이유로
   게임을 시작조차 못 하게 하는 편이 훨씬 나쁘다. `nameClaimed=false` 인 임시 이름으로
   진행시키고, 서버에 닿았을 때 `ensureNameClaimed()` 가 확정한다.
   그사이 남이 가져갔으면 이름만 비우고 다시 짓게 한다 — **진행은 건드리지 않는다**
2. **'조회한 다음 쓰기' 로 유일성을 판정하지 않는다.** 그 사이에 남이 채 간다.
   Postgres 는 `name_key` 유일 인덱스 위반(`23505`)으로, 파일·메모리 저장소는
   검사와 쓰기 사이에 `await` 를 두지 않는 것으로 막는다 (노드는 단일 스레드다)
3. **같은 요청이 여러 번 와도 안전해야 한다.** 응답을 못 받고 재시도하는 일이 실제로 생긴다.
   같은 `playerId` 가 같은 이름을 다시 보내면 성공으로 친다
4. **대소문자만 다른 이름은 같은 이름이다.** 비교용 `name_key` 만 접고 표시용 원본은 남긴다
5. **저장이 예약된 이름을 덮어쓰지 않는다.** 보내온 이름이 남의 것이면 이름만 빼고 저장한다 —
   이름 때문에 진행을 잃는 것보다 낫다
6. 이름 규칙(`NAME_ALLOW` · `NAME_MAX_W`)은 `game.js` 와 `server/index.js` **양쪽에 있다.**
   클라이언트 검사는 안내용이고 서버가 다시 본다. 바꾸면 둘 다 고쳐야 한다
7. **재시도를 '이미 하는 중' 이라고 버리지 않는다.** `ensureNameClaimed()` 는 진행 중인
   시도가 있으면 그 결과를 같이 기다린다(`_claimTask`). 그냥 `return` 하면 그 한 번이
   실패했을 때 아무도 다시 시도하지 않아 **임시 이름이 영영 임시로 남는다** (실제로 겪었다)

### DB 컬럼은 state 의 사본이다

세이브 본체는 `state`(JSONB) 하나다. 그중 **이름·매력 총합·플레이 시간**만 조회용 컬럼으로
따로 뺐다 — 랭킹·통계를 낼 때 세이브를 전부 파싱하지 않으려는 것이다.

- 매력 총합은 크리처 보너스까지 더해야 해서 게임 규칙을 아는 **클라이언트가 `meta.charm` 으로 보낸다.**
  `totalCharm()` 을 고치면 컬럼 값의 의미도 같이 바뀐다. `meta` 가 없으면 서버가
  `state` 만으로 계산 가능한 값(비주얼+아우라)으로 떨어진다
- **컬럼을 늘리면 저장소 3곳(Postgres·파일·메모리)을 다 고쳐야 한다.** 하나만 고치면
  로컬 개발에서는 되는데 배포에서 안 되는(또는 그 반대) 일이 생긴다
- 운영 중인 테이블이므로 스키마는 `ADD COLUMN IF NOT EXISTS` 로만 붙인다.
  기존 행의 새 컬럼은 비어 있고, 그 플레이어가 다음에 저장할 때 채워진다

---

## 검사

```bash
npm install
npm start            # http://localhost:8080
npm test             # 생성 데이터 대조 + 번역 누락 + 인물·대사 표 + 서버 API 검사
npm run test:i18n    # 번역 누락만
npm run test:save    # 옛 세이브를 심어 마이그레이션 확인 (브라우저 필요)
npm run test:time    # 시계를 옮겨 놓고 포만감·스태미나·방치 감소 (브라우저 필요)
npm run test:tut     # 튜토리얼을 처음부터 끝까지 눌러 본다 (브라우저 필요)
npm run test:melt    # 중복 크리처를 솥에 넣어 녹여 본다 (브라우저 필요)
npm run test:wardrobe # 옷장 칸을 눌러 «그 자리»에 머무는지 (브라우저 필요)
npm run test:farm    # 밭에서 거두고 이웃 밭을 털어 본다 (브라우저 + 서버를 스스로 띄운다)
npm run test:ask     # 키워드를 물어 마을 여덟을 열어 본다 (브라우저 필요)
npm run test:name    # 여왕의 이름이 화면에 새는지 (브라우저 필요 · 절반은 npm test 가 돈다)
npm run test:bond    # 물약을 선물해 호감도를 올려 본다 (브라우저 필요)
npm run test:code    # 복구 코드로 다른 기기에서 이어해 본다 (브라우저 + 서버를 스스로 띄운다)
npm run test:lore    # 흐린 장 — 저어서 재료를 알아내 본다 (브라우저 필요)
npm run test:story   # 2~5막 퀘스트를 처음부터 끝까지 걸어 본다 (브라우저 필요)
npm run test:balance # 수치끼리의 약속 (npm test 가 이미 돌린다)
npm run sim:farm     # 밭·약탈 시뮬레이션 200명 × 30일 (수치를 바꿔 보는 도구)
npm run test:talk    # 키워드·대사 표를 걸어 본다 (npm test 가 이미 돌린다)
npm run test:data    # 생성 데이터 대조만 (npm test 가 이미 돌린다)
npm run gen:wardrobe # 커스터마이징 150벌을 축 표에서 다시 뽑는다
```

**눈으로 본 것을 수치로 바꿔 주는 도구 셋도 있다.** 「둥글지 않다」·「안 맞는다」 같은
말은 사람마다 다른 것을 가리키므로, 고칠 자리를 한 곳으로 좁힐 때 쓴다.

```bash
npm run side      # 옆선을 줄마다 재서 «볼록 / 직선 / 오목»으로 찍는다
npm run neck      # 목 옆선 — 옷의 목둘레와 신체 목둘레를 «화면에 찍힌 것»으로 견준다
npm run bundle    # 게임을 파일 하나짜리 HTML 로 묶는다 (서버 없이 열어 보기)
```

옷을 늘렸으면 (`WARDROBE` 의 상의·하의·원피스):

```bash
node tools/checkavatar.js   # 체형 5단계 × 모든 옷에서 살이 옷 밖으로 나오는지
```

### 아바타의 「긴 생머리 · 기본」은 **인트로 공주를 그대로 옮긴 것**이다

같은 사람인데 튜토리얼을 막 마치고 마이 룸에 들어서면 다른 사람이 서 있었다.
**두 얼굴 타원을 포개는 배율 하나가 유일한 원본**이고, 뒤통수·옆 가닥·앞머리 바깥 호를
전부 이 식으로 옮겨 왔다:

```
x = 100 + (x공주 − 150) × 33/34      y = 70 + (y공주 − 180) × 35/33
```

- ⚠️ **뒤통수는 여섯이 다 같은 타원이다** (`CROWN_LONG`). 예전에는 긴 생머리만 이것을
  쓰고 나머지 다섯은 `crownOf(40, 42)` 였는데, 그 타원은 밑끝이 턱(y105)까지 내려와
  **턱 밑에서 목을 파고들었다** — 얼굴이 끝나는 자리 아래에도 머리가 남아 목 옆에
  브라운이 닿았다. 정수리는 앞머리(`BANG_OUT`)가 만드니 뒤통수가 낮아져도 안 납작해진다.
  · ⚠️ **그 높이(턱, y84~90)는 픽셀로 못 잰다** — 거기서는 긴 생머리의 옆 가닥이
    «맞게» 턱을 따라 내려와서, 같은 잣대로 재면 멀쩡한 머리가 2px 으로 걸린다.
    그래서 「턱밑 머리」는 턱보다 아래(y96~)만 보고, 이 사고는 **구조로** 막았다
- **앞머리는 다섯이다** — 기본 · 사이드뱅 · 커튼뱅 · 시스루뱅 · **웨이브**.
  ⚠️ 「올 백」을 뺀 자리다: 이름과 달리 이마를 드러낸 적이 없어(헤어라인 y44)
  「기본」(y43)과 사실상 같은 그림이었다. 대신 **처음에 있던 「기본 앞머리」**
  (밑단이 봉우리 셋으로 물결지는 것)를 되살려 **「웨이브」**로 부른다 — 한눈에 갈린다.
  · ⚠️ **두 축에 같은 낱말이 있으면 이름이 겹친다** — 뒷머리 「웨이브」 × 앞머리
    「웨이브」가 「웨이브 웨이브」가 됐다. 생성기가 같은 라벨이면 한 번만 쓴다
  · ⚠️ **id 여섯(`hair_*_none`)이 사라졌다.** 그걸 입고 있던 세이브는 `getItem` 의
    맨 앞(시작 머리)으로 떨어진다 — 진행은 안 잃지만 착장만 바뀐다.
    **id 를 없애는 것은 이번처럼 「그 벌이 다른 벌과 구별이 안 될 때」만 한다**
- ⚠️ **앞머리의 바깥 윤곽은 «머리의 꼭대기»다** — 앞머리의 모양이 아니다.
  스타일을 가르는 것은 안쪽 변(헤어라인)뿐이고, **꼭대기는 뒷머리가 정한다**:
  긴 생머리만 공주처럼 봉긋하고(`BANG_OUT` · y23.3) 나머지 다섯은 **동그랗다**
  (`BANG_FLAT` — 뒤통수와 «같은 중심·같은 rx» 에 ry 만 늘린 타원의 호 · 꼭대기 y26).
  · ⚠️ 낮은 쪽을 y30 으로 뒀더니 **머리가 납작해 보인다**고 신고받았고, 손으로 그린
    곡선으로 올렸더니 이번엔 관자놀이 위에 **각이 졌다**. 같은 타원이면 y=cy 에서
    폭도 접선도 정확히 맞아 이음매가 «없다» — 다섯에는 이 방식이 맞는다
  · ⚠️ **긴 생머리만 이 방식을 안 쓴다.** 거기서는 정수리가 «공주의 선»이어야 해서
    타원으로 두면 둥글고 넓어진다 (그렇게 했다가 신고받았다)
  · ⚠️ **뒤통수를 높여서 봉긋하게 만들지 않는다** — 그러면 정수리가 타원이 되어
    공주보다 둥글고 넓어진다 (그렇게 했다가 「예전으로 되돌아간 것 같다」로 신고받았다).
    공주의 «선»을 지키려면 앞머리 쪽이어야 한다
  「기본」만 공주의 선(y23.3)으로 올리고 나머지 넷을 y29~30 에 두었더니, 긴 생머리의
  뒤통수(꼭대기 y29.7)가 혼자서는 정수리를 못 만들어 **앞머리를 바꾸는 순간 머리 위가
  납작해졌다** — 화면에서는 「뒷머리가 사라졌다」로 보였다.
  `checkavatar` 의 **「머리 꼭대기」**가 둘을 본다 — ① 같은 뒷머리 안에서 앞머리를
  바꿔도 꼭대기가 그대로인가 ② 긴 생머리만 봉긋한가. 사보타주 둘로 확인했다.
  ⚠️ **관자놀이(x70~82)에서 잰다** — 가운데를 재면 올림머리의 «매듭»이 걸려
  그 머리만 늘 제일 높게 나온다 (매듭은 머리통이 아니라 얹은 것이다)
- ⚠️ **밑단을 «톱니»나 «뾰족한 V» 로 파지 않는다** (커튼뱅 · 시스루뱅 · 기본 앞머리).
  `Q` 로 판 삼각 이빨은 골이 바늘처럼 서고, 가르마를 V 로 모으면 끝이 찌른다.
  **`S` 로 이어 붙이면** 앞 구간의 접선이 그대로 이어져 봉우리도 골도 둥글어지고,
  가르마 끝은 접선을 «수평»으로 눕혀 U 로 만든다
- **볼터치도 인트로 공주의 것이다** (`#ff9db4` · opacity 0.6 · rx8.25 ry5.52).
  ⚠️ **세로는 배율로 옮기지 않고 «입에서» 잰다**(`MY−2.5`) — 배율대로면 y76 이라
  눈 뒤로 들어간다. 가로도 마찬가지로 **얼굴 옆선에서 남는 여백**을 공주와 맞춰
  81/119 다 (배율대로 77.7 이면 얼굴 밖으로 나간다)
- ⚠️ **표정에 물음표(?)와 음표(♪)를 안 쓴다.** 나머지 기호(땀·💢·반짝·💤·눈물…)는
  «상태»를 그리는데 그 둘만 **말풍선처럼 «말»을 하고 있어서**, 얼굴이 짓는 표정이
  아니라 얹어 놓은 자막으로 읽혔다. 쓰던 여섯(다정·미심쩍음·활짝·궁금·해피·어리둥절)은
  이제 눈·눈썹·입만으로 간다. **다시 넣지 말 것 — 지웠다가 되돌린 자리다**
- ⚠️ **흰자위의 검은 테두리는 눈꺼풀 선이 있는 셋에만 있다** (토라짐 `jito` ·
  흘김 `jside` · 노려봄 `glare`). 나머지는 지웠다 — 눈이 커 보이고 부드러워진다.
  셋에 남긴 이유는 위에 굵은 눈꺼풀 선이 얹혀 있어서, 테두리를 지우면
  **눈 아래쪽 윤곽이 사라져 흰자위가 볼로 번지기** 때문이다 (미리 그려 보고 골랐다)
- ⚠️ **「활짝」은 `exp_happy` 다.** `exp_haha` 는 「박장대소」다 — 이름이 비슷해서
  그쪽을 고쳐 놓고 「안 고쳐졌다」는 말을 두 번 들었다. **id 가 아니라 «이름»으로 확인할 것**
- ⚠️ **「활짝」 하나만은 좌표를 직접 쓴다.** 그 표정이 곧 인트로 공주의 웃는 얼굴이라
  「거의 같다」로는 안 된다는 요청을 두 번 받았다. 아바타의 눈·입은 공주보다 8~10px
  아래에 붙어 있어서(`AV`), **부품만 같게 해서는 「배치가 다른 얼굴」이 된다.**
  공주의 세 줄(눈 둘 · 입)과 볼터치를 배율로 옮긴 값을 그대로 쓴다.
  **다른 표정에 이 방식을 퍼뜨리지 않는다** — 나머지 서른일곱은 `AV` 를 지난다
- ⚠️ **눈·입의 자리는 `AV` 다섯 숫자에서만 나온다** (L·R·Y·MX·MY). 표정 서른여덟이
  거기서 좌표를 받아 쓰므로 한 줄만 고치면 전부 따라온다 —
  **표정 쪽에 좌표를 박지 않는다.** 손으로 그린 여섯(윙크·해피·놀람·어리둥절·시크·기본)이
  87/113/89 를 박아 두고 있어서, 자리를 옮겼을 때 **그 여섯만 옛 자리에 남았다**.
  그중 「어리둥절」이 기본 얼굴이라 마이 룸에서 그대로 보였다.
  (자리 자체는 옮겼다가 **되돌렸다** — 지금 값이 원래 값이다. 여섯이 `AV` 를 지나게
  고친 것만 남겼고, 두 판을 그려 대조해 **롤백 전과 한 글자도 안 달라진 것**을 확인했다)
- ⚠️ **아바타에는 코도 없다.** 콧등의 그늘을 선 하나로 옅게 찍어 두었는데
  공주에게는 코가 없다 (`intro.js` 의 `princessFace` — 눈과 입뿐이다).
  코는 `head()` 한 곳에서 나오므로 거기서 빠지면 **표정 서른여덟이 한꺼번에** 따라온다 —
  표정 쪽에 그리기 시작하면 새 표정을 만들 때마다 빠뜨리게 된다
- ⚠️ **아바타에는 귀가 없다.** 예전에는 얼굴 옆에 `cx67·133 rx6 ry9` 로 붙어 있었는데
  공주에게는 없어서, 머리를 아무리 맞춰도 **볼 옆에 살색 혹 두 개**가 남았다.
  게다가 그 혹 때문에 얼굴 반폭이 33 이 아니라 39 로 잡혀 **머리를 공주보다 넓게
  그릴 수밖에 없었다**(뒤통수 44.7) — 머리만 커져서 더 달라 보였다. 귀를 빼면 둘 다 풀린다
- ⚠️ **옆 가닥은 얼굴 «뒤»에 둔다.** 앞으로 보내면 안쪽 변이 얼굴 안으로 15px 들어와
  있어서 눈과 볼을 덮는다 (공주도 뒤에 두고 겹치는 부분을 얼굴로 가린다)
- ⚠️ **앞머리도 공주의 선을 그대로 쓴다.** 「이음매가 수학적으로 맞게」 뒤통수와 같은
  타원의 호로 바꿨다가 되돌렸다 — 정수리가 공주보다 둥글고 넓어져 **「예전(뒤통수 44.7)
  으로 되돌아간 것 같다」**는 신고를 받았다. 이 얼굴에서 맞는 것은 «정확한 이음매»가
  아니라 «공주와 같은 선»이다
- ⚠️ **헤어라인만은 공주 값을 안 쓴다**(40.3 이 아니라 43). 40.3 에 두면
  `checkavatar` 의 「정수리에 살색」이 잡는데, 그 검사는 사이드뱅이 얼굴 꼭대기까지
  못 올라오던 것과 커튼뱅 가르마가 정수리에서 열리던 것을 **둘 다 실제로 잡아낸다**
  (사보타주로 확인했다). 「세로줄마다 머리가 먼저 나오는가」로 바꿔 보았는데
  **그 둘을 하나도 못 잡아서** 되돌렸다 — 뒤통수가 정수리를 덮고 있어서다.
  아바타는 공주보다 얼굴이 세로로 길어 이마가 원래 더 넓으므로 3px 은 그 몫이다.
  ⚠️ 한때 46.5 까지 내렸다가(「이마가 넓다」) 인트로와 어긋나 되돌렸다 —
  **둘을 동시에 만족시킬 수는 없다**
- ⚠️ **턱 밑에 «머리카락을 채워 넣지 않는다».** 목이 좁아지는 자리라 가닥과 목 사이로
  배경이 7~8px 비치는데, 그걸 「틈」으로 보고 가닥을 목 쪽으로 밀어 메웠다가
  **머리카락이 턱과 목 사이에 낀 것처럼 보인다**는 신고를 받았다.
  · 그 자리는 **원래도 더 넓게(23px) 비어 있었고** 아무도 문제 삼지 않았다 —
    공주는 드레스 깃이 높아 덮여 있을 뿐이다. 배경이 비치는 것이 맞는 모양이다
  · `checkavatar` 의 **「턱밑 머리」**가 이제 반대로 잰다 — 목 옆선에서 머리카락까지
    최소 4px. **재는 법을 두 번 고쳤다**: 「몇 px 떨어졌나」로만 재면 «닿았을» 때
    목과 머리가 한 덩어리가 되어 「머리카락이 아예 없다」로 통과하고, 「가운데 덩어리에
    머리색이 있나」로 재면 볼(얼굴)과 어깨가 걸린다. 지금은 **머리색이 아닌 칠만 따라
    걸어** 목의 옆선을 찾고, 그 덩어리가 목만 할 때(반폭 15px 이내)만 본다
- ⚠️ **부츠 목은 «종아리 모양»이다** (`calfPts` → `limbPath`). 폭 18짜리 네모 기둥이라
  종아리 150%(반폭 25.5)에서는 **다리가 부츠 밖으로 삐져나왔다**.
  `limbPath` 를 부츠 색으로 한 벌 더 그리고 **위를 네모로 잘라 내는 것**이 곧 부츠 입구다 —
  「재는 곡선과 그리는 곡선이 같아야 한다」(`thighOuterAt`)를 그대로 지킨 것이라,
  다리 배율을 어떻게 바꿔도 한쪽만 따라올 수가 없다.
  · **안쪽 변은 안 넓힌다**(`pad` 는 바깥에만). 다리와 같은 `innerX` 를 쓰므로 그 변에서는
    살이 비칠 수가 없고, 넓히면 두 짝이 다리 사이에서 겹쳐 한 덩어리가 된다
  · **입구 띠도 같은 폭을 따라간다.** 폭을 박아 두면 굵은 다리에서 띠만 짧아진다 —
    단 **입구가 «발 위»에 있을 때만이다**(`openUp`). 목이 5인 「로우」 다섯은 입구가
    발 타원(위 끝 `FY−7.6`) 안에 들어가서, 거기서 종아리 폭으로 그리면 **발보다
    안쪽으로 치우친 덩어리**가 얹힌다 (발은 발목 한가운데보다 6px 바깥이다 — `footX`).
    그때는 발 위에 곧은 선 하나로 긋는다. 발 타원은 배율을 안 타므로 폭을 박아도 된다.
    ⚠️ 띠를 **발보다 먼저** 그리면 로우는 통째로 가려져 **플랫과 구별이 안 된다**
    (그렇게 만들었다가 눈으로 보고 되돌렸다 — 살색 검사로는 안 잡힌다)
- ⚠️ **굽을 신으면 발이 앞으로 기울어 «세로로 긴» 타원이 된다** (`footShape`).
  굽 없이 선 발은 위에서 내려다보는 각이라 좌우로 넓은 타원인데, 하이힐은 발등이
  **사선으로** 떨어져서 앞에서 보면 길이가 그대로 보인다 — 좌우는 좁고 위아래가 길다
  (「하이힐인데 발등이 여전히 플랫하다」로 신고받았다).
  · **맨발(`legs`)과 신발(`renderShoes`)이 같은 함수를 지난다** — 한쪽만 바꾸면 맨발이
    신발 밖으로 삐져나온다 (종아리·부츠 목에서 두 번 겪은 것과 같은 사고다)
  · ⚠️ **하이힐은 유리구두 하나뿐이다** — 부츠(로우·앵클·롱부츠 광택)는 하이힐이 아니다.
    마감(광택)에 굽을 걸었다가 그 셋까지 하이힐이 되어 **되돌렸다.**
    그리는 쪽에도 `pitchOf` 가 목이 있으면 0 을 돌려주는 빗장이 남아 있는데,
    목이 있는 신발에 굽을 붙이면 **목의 안쪽을 좁아진 발이 못 덮어 배경이 쐐기처럼
    비친다** (그려 보고 알았다). 표 쪽은 `checkavatar` 가 막는다
  · **기울어진 발은 타원이 바닥까지 내려가 그것이 곧 앞코다** — 앞코를 따로 안 댄다.
    밑창은 **신발로 오려 낸다**(clipPath): 타원을 하나 더 얹으면 아래로 갈수록 급히
    좁아지는 자리라 어느 높이에서는 반드시 앞코 옆으로 삐져나온다
  · ⚠️ **기울어진 발은 발목 «밑»으로 모인다** (`FOOT_OUT_HEEL` 2 · 납작한 발은 6).
    좁아진 발(반폭 7.8)을 6px 이나 바깥에 둔 채로 두었더니 **발목 안쪽 밑에 배경이
    쐐기처럼 남아** 옆선이 y331 에서 104 → 109 로 툭 튀었다 —
    「발목이랑 발이 끊어져 있다」로 신고받았다. 굽을 신으면 발이 몸 밑으로 모이기도 한다.
    `checkavatar` 의 **「발목↔발」**이 스무 켤레 × 종아리 100·150% 에서
    **실루엣의 «안쪽» 옆선이 튀는 폭**을 잰다 (3px 까지 · 지금 최대 1px).
    ⚠️ 바깥 변은 안 잰다 (발은 발목보다 넓어 원래 벌어진다) · 발끝도 안 잰다
    (앞코가 오므라들며 안쪽도 같이 들어온다) — **이음매 구간 y324~336 만** 본다
  · `checkavatar` 의 「굽」이 **신발 상자(가로×세로)**를 재서 세로가 더 긴지 본다.
    ⚠️ **이 줄이 없으면 기울임을 통째로 지워도 통과한다** (사보타주로 확인했다 — 25×21)
  · ⚠️ **바닥에 닿았는지를 «색»으로 재지 않는다.** 펌프스가 바닥에 대는 것은 몸통보다
    어두운 «밑창»이라, 몸통 색을 찾던 옛 검사가 멀쩡한 그림을 「앞코가 떴다」로 잡았다.
    지금은 **바닥 언저리에서 한 발의 폭**을 잰다 (굽만 닿으면 2~3px · 앞코까지 닿으면 10px 넘는다)
- ⚠️ **목이 없는 구두는 발등이 드러난다** (`INSTEP_W`·`INSTEP_D`·`INSTEP_G` —
  신발 타원의 «비율»이다). 신발 타원 하나로 두면 발등 자리까지 통째로 신발이라
  「납작한 덩어리」로 보인다 — 신고받은 자리다. ⚠️ **px 로 박지 않는다**: 굽이 있으면
  타원이 통째로 달라져서 굽 있는 구두에서만 어긋난다. 밑변은 **3차 곡선**이다 —
  2차(`Q`)는 가운데만 뾰족하게 처져 렌즈 모양이 되고, 3차라야 가운데가 평평하게
  눌려 신발의 앞코 선처럼 보인다.
  · ⚠️ **입구를 얕고 넓게 두면 「납작한 뚜껑」이 된다** (「발등 곡선을 다듬어 달라」).
    고친 것은 **W 를 좁히고(.72→.68) D 를 깊게 한 것**(.60→.62)이다 —
    모서리가 타원의 더 아래로 내려가 옆에 신발의 «옆면»이 남고, 가운데가 더 파여
    밑변이 입구 선으로 읽힌다. **깊이÷폭 0.120 → 0.152.**
  · ⚠️ **모서리의 «각»이 원인이 아니었다.** 뿔처럼 보여서 각인 줄 알고 2차로
    굴려도 봤는데 면이 깎여 «각진 탭»이 됐고, 재 보니 지금 쪽이 오히려 더
    날카롭다(60° → 54°). 밑변의 «생김새»(`INSTEP_G`)도 정규화하면 거의 안 달라진다.
    **짐작으로 두 번 헛짚은 자리라, 고치기 전에 셋을 하나씩 떼어 그려 보고 잰다**
  · `checkavatar` 의 **「발등 깊이÷폭」**이 0.135 를 지킨다. ⚠️ 처음에 쓰려던
    「밑변이 얼마나 파였나」는 옛 모양 0.50 ↔ 지금 0.58 밖에 안 갈려서 버렸다 —
    **가르지 못하는 잣대는 무슨 값을 넣어도 통과한다.** 옛 값(W.72 D.60)으로
    되돌리는 사보타주로 확인했다 (평발 넷이 잡힌다).
  · ⚠️ **칠하는 색은 `SKIN`(다리)이 아니라 `SKIN_SH`(발)다.** 다리 색으로 칠했더니
    발 위에 «다른 색 띠»를 얹어 놓은 꼴이었다 — 드러나는 것은 다리가 아니라 발등이고,
    맨발도 그 색이라 그래야 한 몸으로 읽힌다
  · ⚠️ **구멍으로 뚫지 않고 덮는다.** 신발 타원(rx13)이 발 타원(rx12)보다 늘
    1~1.8px 넓어서, 뚫으면 그 사이로 **배경이 실오라기처럼 비친다**
  · ⚠️ **입구는 발 한가운데에 «대칭»으로 판다.** 발목 밑으로 옮겨 봤더니 바깥쪽 윤곽만
    남아 신발이 기울어 보였다 — 발이 발목보다 6px 바깥이라서다(`FOOT_OUT`).
    맨발도 그만큼 나가 있으니 발을 따르는 쪽이 이 그림의 결이다
  · ⚠️ **스트랩은 입구의 «아래쪽 3분의 2»를 지난다.** 한가운데에 두면 2px 짜리 선이
    입구를 거의 다 덮어 발등이 안 보인다 — `checkavatar` 가 실제로 잡았다
  · `checkavatar` 의 **「발등」**이 스무 켤레를 다 본다 — 목이 없는 다섯은 발색이
    드러나는가, 목이 있는 열다섯은 한 점도 안 드러나는가. **양쪽을 몇 켤레 쟀는지도
    같이 낸다** (한쪽이 0이면 그 방향은 아예 안 잰 것이다).
    사보타주 둘로 확인했다 (안 파면 5건 · 목이 있는 것까지 파면 15건)
- ⚠️ **굽(하이힐)은 발을 «띄워서» 만들지 않는다.** 발 몸통을 위로 올리고 밑에 기둥을
  세웠더니 `legs()` 가 그려 둔 **맨발(살색 타원 `cy=footY`)이 신발 밑으로 드러났다** —
  구두를 신었는데 발가락이 나와 있는 꼴이다. 신발은 맨발을 덮는 자리에 그대로 두고
  **굽만 바닥 아래로 더 내려 긋는다** (굽을 신으면 실제로도 키가 커지니 결도 맞는다).
  · 굽은 **축이 아니라 «조합 하나»가 정한다** — `genwardrobe` 의 `HEEL` 표에
    `'flat/gloss': 5` 한 줄이 전부다 (유리구두).
    ⚠️ 처음에는 마감(광택)에 걸었는데 **로우·앵클·롱부츠 광택까지 하이힐이 되어
    「그것들은 하이힐이 아니다」로 되돌렸다.** 축 어느 쪽에도 안 걸리는 값이라
    조합 키로 적는 표를 따로 둔다 — 축 표는 깨끗하고 예외는 한 줄로 «보인다».
    ⚠️ **목이 있는 조합에는 못 붙인다** (`checkavatar` 가 표를 본다) — 그리는 쪽의
    「부츠에 앞코를 따로 대는」 갈래는 그때 같이 지웠다
  · ⚠️ **앞코도 굽과 «같이» 바닥에 닿아야 한다.** 굽만 닿게 그렸더니 발끝이 5px 떠서
    「그 발로는 걸을 수 없다」고 신고받았다. 앞코는 **바깥쪽**, 굽은 **안쪽**으로
    물려 사이에 아치가 보이게 한다 — 반대로 두면 안짱다리로 보인다
  · ⚠️ **그림 상자 밑(`VB` 348)이 코앞이다.** 굽을 3px 만 더 늘려도 끝이 잘리는데,
    SVG 는 viewBox 밖을 아예 안 그리므로 **잘린 것이 티가 안 난다.**
    `checkavatar` 의 「굽」이 넷을 본다 — ① 발보다 아래로 나오는가 ② 상자 밑에
    안 닿는가 ③ **바닥 줄까지 앞코(신발 몸통 색)가 내려오는가** ④ 종아리가 달라지는가.
    거기에 **굽 있는 구두가 하나라도 있는가**도 본다 — 축 표에서 지우면 목록이 비어
    아무것도 안 재고 통과하기 때문이다 (「로우를 건너뛰던」 것과 같은 구멍이다).
    사보타주 넷으로 확인했다
- ⚠️ **굽을 신으면 종아리 모양이 달라진다** (`calfPts(tune, heel)`). 발끝으로 서는
  셈이라 장딴지가 당겨져 **위로 올라가고 조금 더 불룩해진다** — 하이힐을 신은 다리가
  예뻐 보이는 이유가 그것이다. 굽 5px(유리구두)을 1인분으로 잡고 그만큼 태운다.
  · **다리와 부츠 목이 같은 함수를 지난다** — 굽을 한쪽만 반영하면 부츠가 종아리를
    못 덮는다 (「종아리 밖으로 벗어난다」와 같은 사고다). 그래서 굽 값은
    `heelOf(신발)` 한 곳에서 나오고 `build()` 가 둘에 같이 넘긴다
  · ⚠️ **발목은 안 건드린다** — 발(`footX`)이 발목에서 나오므로 여기서 좁히면
    신발만 제자리에 남아 발목과 어긋난다
- ⚠️ **부츠 목은 옷 밑단 위로 안 올라간다** (`legHemY`). 발목까지 오는 청바지
  (`hemY` 332) 위로 목이 올라와 **바짓단 한가운데에 색이 다른 기둥**이 섰다.
  · **신발을 하의보다 아래로 내려서 고치지 않는다** — 그러면 바지가 발을 통째로 삼켜
    맨발도 구두도 안 보인다. 신발은 하의보다 «위»에 두어 **발끝이 바짓단 밑으로**
    나오게 하고, **목만** 밑단에서 자른다 (짧은 하의·치마에서는 목이 그대로 다 보인다)
  · `hemY` 가 없는 것(공주 드레스)은 바닥까지 오는 옷이라 999 로 친다 —
    `crouch()` 의 `legHem` 과 같은 규칙이다
  · `checkavatar` 의 **「부츠」**가 셋을 본다 — 목이 있는 구두 15켤레 × 종아리
    50·100·150% 에서 ① 부츠 구간에 살색이 0px 인가 ② 바짓단 위에 부츠 색이 0px 인가
    ③ **입구 띠가 보이는가**(장식이 없는 `plain` 셋에서 두 색 이상).
    사보타주 셋으로 확인했다 (네모 기둥 99px · 밑단을 안 보면 604px · 띠를 발 밑에 두면
    로우가 한 색).
    · ⚠️ **구간의 양 끝을 잘라 내지 않는다.** 2px 씩 뺐더니 목이 5인 「로우」 다섯은
      구간이 뒤집혀(y331~329) **통째로 건너뛰었다** — 0px 이 「통과」가 아니라
      「한 번도 안 쟀다」였다. 그래서 **몇 켤레를 쟀는지(45/45)도 같이 낸다**

브라우저 동작은 Playwright 로 확인해 왔다. 눈으로 보면 놓치는 것들을
자동 검사가 실제로 잡아냈다 — 어깨가 드레스 밖으로 나온 것, 카드가 58px 넘친 것,
마법진 구멍이 겹친 것, 초기화한 세이브가 되살아난 것.

자동화로 `checkUI()` 를 돌릴 때: 예전에는 창이 숨겨지면 `requestAnimationFrame` 이 멈춰
**영영 끝나지 않았다.** 지금은 숨겨진 창에서 프레임을 기다리지 않고 바로 진행한다
(측정 상태는 `settle()` 이 맞춰 주므로 결과는 같다). 숨긴 채로도 0.1초 안에 끝난다.

대신 **창 크기는 살아 있어야 한다.** 패널이 접혀 폭이 0 이면 검증기가 잴 수 없다고 알리고
`pass:false` 를 낸다 — 그 상태의 0건은 통과가 아니다.

### 시간이 흐르는 값은 시계를 옮겨 놓고 검사한다

포만감·스태미나·방치 감소는 **지금 화면만 보면 전부 통과로 보인다.** 「하루 뒤」를
실제로 만들어 봐야 굶었을 때 스태미나가 차 버린다든지, 방치 감소가 부를 때마다
두 번 깎인다든지 하는 것이 드러난다.

`tools/checktime.js` 가 페이지 안에서 `Date.now` 를 갈아 끼운다.
그래서 **시간은 전부 `nowDate()` 한 곳을 지나야 한다** — `new Date()` 를 여기저기서
부르면 시계를 옮겨 놓을 구멍이 없어진다. 실제로 `dayKey()` 가 그래서 밤 판정을 못
재고 있었다. **시간에 기대는 값을 새로 만들면 `nowDate()` 를 쓰고, 여기에 한 줄 늘린다.**

`Date.now` **한 곳만** 갈아 끼운다. `Date` 클래스까지 감싸면 `static now()` 가 자기를
다시 불러 **스택이 넘친다** — 검사 스크립트를 새로 쓸 때 실제로 겪었다.

지금 이 검사가 보는 것: 포만감 · 스태미나 · 방치 감소 · 혼자 먹은 밤 · 운동 시간대 ·
**채집 시간대**(운동 구간을 넘지 않는가) · **날씨**(같은 시각에 안 흔들리는가 ·
하루 안에서는 바뀌는가 · 같은 시각에도 맵마다 다른가) · **부엌에 간 밤**(혼자가
아닌 밤이 되는가 · 여러 밤 중 하나만 빠지는가 · 하루에 한 번뿐인가) ·
**로엔 제국력**(연도를 2027·2127 로 옮겨 놓고 227·327 이 나오는가 — 「올해」만 맞는
상수를 박아 두면 오늘은 통과로 보이고 새해 첫날에 조용히 틀린다).

**주장하기 전에 확인한다.** 특히 "고쳤다" 고 말하기 전에 실제로 그 경로를 실행해 볼 것.

---

## Railway 배포

자세한 절차는 `server/README.md`. 헷갈리기 쉬운 것만 적어 둔다.

| 항목 | 값 | 주의 |
|---|---|---|
| Root Directory | **비움** (`/`) | 저장소 루트가 곧 게임 폴더다. `server` 로 잡으면 안 된다 — Root Directory 는 **빌드에 포함되는 범위**라서 게임 파일이 안 올라간다. 예전에 게임이 하위 폴더였을 때 쓰던 `dieter-alchemist` 값도 이제는 틀린 값이다 |
| Start Command | `npm start` | |
| 포트 | 기본 8080 | Railway 가 도메인을 8080 으로 라우팅한다. 3000 이면 502 |
| `DATABASE_URL` | `${{Postgres.DATABASE_URL}}` | **Postgres 를 추가해도 자동으로 안 들어온다.** 참조 변수를 직접 만들어야 한다 |

배포 후 `/api/health` 의 `store` 값을 확인한다. `memory(휘발성)` 이면
**재배포할 때마다 모든 플레이어 세이브가 사라진다.**

---

## 경제 — AP · 현자의 결정 · 비법서

세 값이 서로 물려 있다. **하나를 바꾸면 `npm run test:balance` 가 나머지를 본다.**

| | |
|---|---|
| **채집 AP** | **지대마다 다르다** (`D.ZONES[].ap` → `zoneAp`). 평야 6 → 황무지 17. 화면 어디서도 `D.ENERGY.cost.gather` 를 직접 읽지 않는다 — `gatherCost(mapId)` 한 곳을 지난다. 직접 읽으면 **버튼에 적힌 값과 깎이는 값이 갈린다** |
| **AP 상한** | 매력 **단계**로 오른다 (`capPerTier` 200 · 새싹 1000 → 여신 1800). 판정은 `charmPeak()` 다 — 총합을 보면 애착 크리처를 바꾼 순간 상한이 내려가 **들고 있던 AP 가 갈 곳을 잃는다**. ⚠️ **자정 충전은 `energyCap()` 까지다**: 고정 `dailyFill` 로 자르면 늘어난 몫이 영영 안 찬다 |
| **현자의 결정** | **조합에 성공하면** 들어온다 (`brewReward` 5). ⚠️ 예전에는 조합 «실패»가 유일한 수급원이었는데 비법서가 실패를 없앴다 — 그대로 뒀으면 AP 충전도 밭 칸도 못 여는 게임이 됐다. **조합 값(25)보다 반드시 작아야 한다** |

**리그 사다리도 여기 물려 있다.** 한 주에 낼 수 있는 최고 점수는 AP 로 정해지는데,
상한이 오르고 초반 채집이 싸지면서 1312 → **2720점**이 됐다. 그래서 `leaguePace` 를
칸당 26 → **50** 으로 올렸다 (맨 위 1590점 = 상한의 58%). 40~70% 밖으로 나가면
`checkbalance` 가 실패시킨다.

## ⚠️ 전역 이름은 파일끼리 겹치면 안 된다

모듈 시스템이 없다. `<script>` 로 나란히 읽히므로 **`data.js` 의 최상위 `const X` 와
`game.js` 의 최상위 `const X` 는 같은 전역 하나**다. 겹치면 브라우저가
`Identifier 'X' has already been declared` 를 던지고 **그 파일이 통째로 실행되지 않는다.**

화면은 오류를 안 띄운다 — 그냥 빈 껍데기가 되고, 콘솔을 안 열면 원인을 못 찾는다.
`node --check` 도 못 잡는다: 파일 하나씩 보면 둘 다 멀쩡한 문법이다.

```bash
npm run test:globals   # npm test 가 이미 돌린다
```

(`PAGE_TIERS` 를 data.js 로 옮기면서 game.js 에 그대로 남겨 놓아 실제로 겪었다)

## 호감도 — 공방이 아니라 «주는 쪽»에서 오른다

⚠️ **매력·비주얼 수치를 호감도에 곱하지 않는다.** 예뻐질수록 남자들이 좋아해 주는
구조가 되면 `intro_6` 의 주제와 정면으로 부딪힌다 — 공주가 살을 빼는 이유가
남에게 잘 보이기 위해서가 되어 버린다. 그들이 끌리는 것은 외모가 아니라
**연금술사로서의 그녀**다. `npm run test:bond` 가 **매력을 999 로 올려 놓고
호감도가 한 톨도 안 움직이는지**를 수치로 잰다 (부엌의 「0 깎였는가」와 같은 검사다).

- **오르는 것은 만들어 준 물약뿐이다.** 마을 NPC 여섯의 「선물」에서 준다
- ⚠️ **값이 큰 물약이 아니라 «처음 주는 종류»가 크게 오른다**(`BOND_GAIN` 3 / 1).
  「좋은 것을 주면 많이 오른다」로 두면 후반 물약 하나가 초반 물약 스무 개를 덮어써서
  **초반 레시피가 통째로 죽은 콘텐츠**가 된다. 사람마다 좋아하는 «등급»이 따로 있어(+2)
  누구와 먼저 친해지는지가 점수 하나 없이 갈린다
- **물약은 사라진다** — 이것이 유일한 제동이다. 하루 몇 번 같은 제한은 안 건다
  (「오늘 여섯 명 다 돌아야 한다」가 되면 그게 숙제다)
- **내려가지 않는다.** 코지 게임에서 관리 압박은 독이다
- **클레멘에게는 호감도가 없다.** 그는 대가 없이 «주는» 쪽이고, 눈금을 붙이는 순간
  그것도 등가 교환이 된다
- 단계가 오르면 **그 사람의 자리에서 나는 재료**로 갚는다. ⚠️ 답례가 커지면
  「눌러서 재료 받기」가 되므로 `checkbalance` 가 **들인 AP 보다 작은지**를 지킨다
- **그들이 「주는 것」은 상시 효과다** (`BOND_GIVES` — STORY.md 의 담당 표 그대로).
  ⚠️ **다섯 다 이미 있는 함수 한 곳에 곱해 붙인다** — 새 경로를 만들면 화면에 적힌 값과
  실제로 먹는 값이 갈린다 (지대별 AP 에서 배운 것과 같다). 그 덕에 셋은 저절로 보인다:
  🛡️ 호위 → `gatherCost()`(채집 버튼의 ⚡) · 🍖 단백질 → `exCost()`(운동 팝업의 단련) ·
  🎻 노래 → 물약을 마실 때의 아우라. 안 보이는 둘(💎 보석 · 📖 지식)은 선물 시트에 적는다
  · ⚠️ **호위 할인이 지대 순서를 뒤집으면 안 된다.** `gatherCost` 에 평야 값 바닥을
    뒀지만 그건 마지막 안전장치이고, `checkbalance` 는 **표가 바닥에 안 닿는지**를 본다 —
    바닥을 씌워 놓고 재면 무슨 값을 넣어도 통과하는 「스스로 맞는」 검사가 된다
  · ⚠️ **리그 상한 계산도 이 할인을 봐야 한다** — 안 그러면 「맨 위 리그에 닿는가」가
    조용히 헐거워진다 (`checkbalance` 의 `apOfR` 이 제일 유리한 단계로 잰다)
  · **실반에게는 담당이 없다.** 그는 STORY 의 「남자 NPC 여섯」이 아니라 3막의
    농민 연합 쪽 인물이라 재료로만 갚는다 — 억지로 붙이면 여섯의 표가 흐려진다
- ⚠️ **하트(♥/♡)는 `checkTextStyle()` 이 안 본다** — 그림 글자의 대비는 재지 않는다는
  규칙에 걸린다. 실제로 3.56:1 짜리를 넣어도 `checkUI()` 는 0건이었다.
  그래서 `checkbond` 가 그 자리만 콕 집어 잰다 (정책을 느슨하게 푸는 대신)
- **단계가 오르면 «하는 말»이 달라진다** (`D.bondTalk` → `bt_<사람>_g/l<단계>`).
  인사말이 다섯 갈래로 갈리고, 「대화」의 **마지막에 그 단계의 잡담 한 줄**이 붙는다.
  눈금(♥)만 차오르고 말이 그대로면 호감도는 숫자놀이로 남는다
  · ⚠️ **외모를 칭찬하는 말을 한 줄도 안 쓴다.** 「예뻐졌네요」 하나면 「매력을 호감도에
    곱하지 않는다」가 글에서 무너진다 — 그들이 끌리는 것은 **연금술사로서의 그녀**다.
    `checkbond` 가 두 언어로 이 줄들만 훑어 외모 낱말을 잡는다 (수치 쪽 매력 999 검사와 짝이다)
  · ⚠️ **줄 목록은 `talkLinesOf()` 한 곳에서 나온다.** 화면과 `talkNext` 가 길이를
    따로 세면 붙인 줄에 **영영 못 닿는다** — 마지막 줄에서 대화가 끝나 버렸다 (실제로 그랬다)
  · 잡담을 늘렸으면 **표정도 같이** 늘린다 (`moods` 가 짧으면 그 줄만 기본 얼굴이 된다)
  · `checkbond` 가 셋을 본다 — 여섯×다섯이 다 있는가 · **단계끼리 겹치지 않는가** ·
    외모 낱말이 없는가. 키만 있고 내용이 같으면 화면에서는 안 달라진 것과 똑같다
- **호감도가 이야기를 민다** — 단계가 오르면 **그 사람이 새 키워드를 준다**
  (`ASKS` 의 `need: { bond: 3 }`). 물약을 만들어 준 값이 재료로만 돌아오면
  호감도는 상점의 다른 얼굴일 뿐이다. **신뢰에서야 털어놓는 말**이 있어야 사람이 된다
  · ⚠️ **클레멘에게는 `need.bond` 를 걸 수 없다.** 그에게는 눈금이 없어서 조건이
    영영 안 채워지고, 화면에는 자물쇠만 뜬 채 아무리 해도 안 풀린다 —
    조용히 막히는 종류다. `checktalk` 이 그것을 잡는다
  · **잠긴 줄은 감추지 않고 🔒 로 보여 준다.** 안 보이면 「없는 것」이고 보이면
    「아직 못 여는 것」이다. 대신 **길잡이 점(●)에는 안 센다**(`asksNew`) —
    점을 따라갔는데 못 여는 것뿐이면 점이 거짓말이 된다
  · 흐림은 **`style.css` 의 잠긴 콘텐츠 공통 표현**을 그대로 쓴다(`.ask-chip.locked`).
    `npm run test:ask` 가 세 가지를 갈라서 잰다 — 자물쇠가 보이는가 · 눌러도 안 들어오는가 ·
    3단계가 되면 풀려서 「유리관」이 들어오는가
  · ⚠️ **`checkui` 의 「물어볼것」 화면에 잠긴 칩이 하나는 있어야 한다.** 없으면 🔒 줄의
    대비·넘침을 아무도 안 재는 것이라 0건이 「재 본 적 없다」는 뜻이 된다 —
    그래서 그 화면이 유타르크의 호감도를 0으로 내려놓고 재고, **재고 나서 되돌린다**

## 부엌 — 요리사 클레멘 (STORY.md · QUEST.md 4단계)

**그는 대가 없이 준다.** AP 도 재료도 결정도 안 든다 —
「온기만이 등가 교환의 밖에 있다」가 이 인물의 전부다.
`checkui` 가 **0 깎였는지를 수치로** 잰다: 값을 받기 시작하면 인물이 무너진다.

⚠️ **부엌을 「선택 콘텐츠」 뒤에 숨기지 않는다.** 「혼자 먹은 밤」의 판정 기준은
처음부터 「혼자 먹었느냐」였는데 같이 먹을 사람이 없어서 **늘 참**이었다 —
**피할 방법이 없는 페널티**였다.

그래서 오래 「퀘스트와 상관없이 튜토리얼 직후부터 열려 있었다」. **지금은 첫 만남
컷씬(`c_meet_in`)을 본 뒤에 열린다**(`metClemen`) — 규칙이 바뀐 것이 아니라
**그 퀘스트가 첫 퀘스트가 됐기 때문**이다. 튜토리얼을 마치면 칩이 바로 떠 있고,
한 번 누르면 컷씬이 끝나면서 부엌이 나타난다. 선택 콘텐츠 뒤가 아니다.
· ⚠️ **「퀘스트를 끝내면」으로 두면 안 된다** — 그 퀘스트의 목표가 **부엌에서 같이
  먹기**라, 끝나야 열리게 하면 깰 방법이 없다 (물어보고 「받으면 열린다」로 정했다)

- 부엌에 간 날은 그 밤이 혼자가 아니다. 여러 밤을 정산할 때는 **그 밤 하나만** 뺀다
- **시계를 옮겨 놓지 않으면 확인할 방법이 없다** — `npm run test:time` 이 넷을 본다
  (안 갔을 때 · 갔을 때 · 사흘 중 하루만 · 하루에 한 번뿐인지)
- **폭식해도 그의 호감도는 안 깎는다.** 대사에 조건이 하나도 없다
- ⚠️ **부엌 버튼의 점(●)은 「오늘 밥」이다.** 「새로 물어볼 것」으로도 켜지지만
  그것은 **갈 수 있는 마을이 하나도 없을 때뿐이다**(`kitchenNews`).
  · 둘을 늘 겹쳐 두면 **밥을 먹어도 점이 영영 안 꺼진다** — 물어본 대답이 다음
    키워드를 주므로 「새로 물어볼 것」은 사실상 늘 하나 남아 있다.
    「다 먹었는데 레드닷이 안 사라진다」로 **실제로 신고받았다**
  · 그래도 안 지운 이유는 처음 그대로다: 마을이 전부 잠겨 있으면 이야기가 시작되는
    자리가 여기뿐이라 점이 꺼지면 갈 곳이 아예 안 보인다. 마을이 하나라도 열리면
    안내는 **마을 탭의 점**(`villageNews`)이 맡는다 — 첫 대답(`kw_hunger`)이 바로
    일곱 굴뚝을 여니 이 상태는 게임 첫날에만 있다
  · ⚠️ **클레멘만 아는 키워드를 만들면 안 된다.** 부엌은 마을이 아니라 마이 룸의
    버튼이라 `villageNews` 가 못 세고, 그러면 **아무도 안 가리키는 대답**이 된다.
    `checktalk` 이 그것을 잡는다 (사보타주로 확인했다)
  · ⚠️ **`doAsk` 가 `renderActBadges()` 를 부른다.** `renderKitchen()`·`renderGather()` 는
    **그 시트 안만** 다시 그려서, 안 부르면 점이 **켜진 채로 남아** 다음 `render()`
    까지 거짓말을 한다. `npm run test:ask` 가 **`render()` 를 안 부르고** 잰다 —
    먼저 그려 주면 안 그려도 통과하는 「스스로 맞는」 검사가 된다

## 마이 룸 — 방 안에서 하는 일 다섯은 «하나씩» 열린다

운동 · 흡입 · 부엌 · 수확 · 밭. 튜토리얼을 막 마친 화면에 다섯이 한꺼번에 서 있으면
무엇부터 눌러야 하는지 알 수가 없다. **처음에는 다 숨어 있다.**

- 조건은 **`actOpen()` 한 곳**에 모여 있다. `index.html` 은 `hidden` 만 달고 있고,
  켜는 것은 `renderActBadges()` 한 줄이다
- ⚠️ **「보이는가」와 「열리는가」를 따로 판정하지 않는다.** 갈라 두었더니 옛 세이브에서
  **버튼은 보이는데 눌러도 아무 일이 없었다** — 보임은 `roomActs` 를 보고 열림은
  `metClemen()` 만 봤다. `kitchenOpen()` 은 `actOpen('kitchen')` 을 그대로 돌려준다
- **부엌만 조건이 이야기다** (클레멘을 만났는가). 나머지 넷은 아직 조건이 없어서
  개발용 스위치(`S.roomActs`)로만 켠다 — 조건이 정해지면 `actOpen` 에 한 줄씩 붙인다
- ⚠️ **하던 사람에게서 뺏지 않는다** (세이브 14). 옛 세이브는 다섯을 다 쓰고 있었으므로
  마이그레이션이 그대로 열어 준다. ⚠️ 그 표(`ROOM_ACTS`)는 **`load()` 위쪽에 둔다** —
  `migrate` 가 쓰는 값이라 아래에 두면 `ReferenceError` 가 나고 세이브가 통째로
  기본값으로 되돌아간다 (1번 항의 그 사고다)
- ⚠️ **`checkui` 는 ver 8 짜리 옛 세이브를 심고 시작한다** — 그래서 평소 검사는
  «마이그레이션으로 다 열린» 상태만 본다. 새 플레이어의 화면은 「방버튼해금」이 따로
  잰다 (숨어 있는가 · 만나면 부엌만 나타나는가 · 보임과 열림이 갈리지 않는가 ·
  **컷씬을 끝까지 넘기면 버튼과 레드닷이 «그 자리에서» 나타나는가**).
  ⚠️ 마지막 하나는 `renderActBadges()` 를 손으로 불러 주면 안 된다 — 그러면
  「그리기가 빠진」 버그를 통째로 못 본다. 사보타주 넷으로 확인했다

## 공방 — 비법서는 답지가 아니라 «수수께끼»다 (흐린 장)

비법서가 재료 이름을 다 적어 주면 **발견이 없다** — 인터넷 공략을 그대로 따라 하는
것과 다를 바가 없다. 그렇다고 「맞혀 보세요」로 두면 벽이 된다: 평야 재료만 25가지라
2개 조합이 **300가지**이고, 재료가 12개인 레시피도 있다.

그래서 **맞히기 퀴즈가 아니라 「채워 가기」**다 (`data.js` 의 `LORE`).

- 한 장에서 가려지는 것은 **최대 두 칸**(`hiddenMax`). ⚠️ **한 칸은 언제나 보인다**
  (`keepShown`) — 재료 두 가지짜리를 둘 다 가리면 300가지에서 고르는 벽이 된다
- ⚠️ **틀려도 재료가 한 톨도 안 없어진다.** 이 한 줄이 부담을 없앤다 — 잃을 것이
  없으니 마음껏 넣어 볼 수 있고, 그래서 「실패」라는 말이 화면에 안 나온다.
  AP 만 든다 (그것도 안 들면 아무 생각 없이 누르게 된다)
- **하나만 맞아도 진도가 난다** → 몇 번 저으면 반드시 풀린다. **절대 안 막힌다**
- ⚠️ **밭 작물과 크리처는 안 가린다.** 채집으로 안 나와서(심거나 녹여야 한다)
  가려 놓으면 「어디서 구하는지」 자체를 알 수가 없다 — 수수께끼가 아니라 벽이다
- ⚠️ **튜토리얼이 가르치는 물약도 안 가린다**(`LORE.plain`). 배우는 중의 수수께끼는
  안내가 아니라 시험이다
- ⚠️ **수수께끼에 이모지를 넣지 않는다.** 「지대 + 이모지」로 좁히면 후보가
  평균 1.05개라 **답을 적어 주는 것**이 된다. 「지대 + 흔한 정도」라야 일곱쯤 남는다
- ⚠️ **가리는 칸은 레시피 id 로 «고정»한다.** 무작위면 어제 밝혀낸 칸이 오늘 다시
  가려진다 (날씨·일지와 같은 규칙이다)
- ⚠️ **모르는 칸을 채우는 것 자체가 「손으로 담기」**다. 예전 규칙(손으로 담으면
  `S.want` 를 놓는다)을 그대로 두면 넣는 순간 목표가 사라져 **영영 못 맞힌다** —
  아직 모르는 칸이 남아 있는 동안만 목표(`S.guess`)를 붙든다
- **추리가 싫은 사람의 우회로** — 어떤 재료를 `masteryAt`(30)개 모아 보면
  「잘 아는 재료」가 되어 **모든 장에서** 이름이 보인다. 채집만 해도 비법서가 밝아진다.
  ⚠️ 누적은 `S.gathered` 다 — `inventory`(지금 가진 것)를 쓰면 **쓰는 순간 숙련이
  도로 사라진다.** `addInv()` 한 곳에서 센다
- ⚠️ **하던 사람에게서 뺏지 않는다** (세이브 13). 이미 가진 장은 전부 밝혀진 것으로
  치고, 수수께끼는 그 뒤에 오는 장부터다 — 새 재미를 주려다 어제까지 만들던 물약을
  수수께끼로 바꾸면 그건 뺏는 것이다. `npm run test:lore` 가 옛 세이브를 심어 확인한다

## 비법서 장은 «맛보기 + 그물» 두 길로 나온다

퀘스트가 먼저 주고, **단계 지급(그물)이 한 단계 뒤에 같은 등급을 통째로** 준다.
퀘스트를 하면 빠르고, 안 해도 느릴 뿐 못 하게 되진 않는다.

⚠️ **한 퀘스트가 등급을 통째로 주지 않는다.** 첫 퀘스트(매력 0)가 하급 물약
**스물네 장**을 한꺼번에 주고 있었다 — 시작 밑천이 여섯 장인데 그 네 배가
첫 몇 분에 들어오는 셈이라:

- **흐린 장이 스물네 개** 동시에 열려 하나하나 알아내는 재미가 뭉개진다
- 그물이 한 단계 뒤에 주는 것이 **이미 다 가진 것**이라 빈손이 된다

지금은 등급을 등분해서 맛보기만 준다 (`pagesForSpec` 의 `#i/k`). 첫 퀘스트는
**여섯 장** — 시작 밑천과 같은 크기다. 덤으로 장이 하나도 없던 퀘스트 둘
(부엌·수프)에도 몫이 생겼다.

- ⚠️ **`#i` 는 예전부터 「절반」이다** (`PAGE_TIERS` 의 `potion:mid#0`). 그 뜻은
  안 바꾸고 `/k` 를 덧붙이는 것으로만 넓혔다 — 바꾸면 단계 지급이 통째로 어긋난다
- ⚠️ **그물은 잘게 쪼개지 않는다.** 거기까지 등분하면 퀘스트를 안 한 사람이
  영영 못 얻는 장이 생긴다
- `checkdata` 가 셋을 본다 — 136장이 다 나오는가 · 퀘스트가 그물보다 먼저인가 ·
  **한 퀘스트가 주는 장 수**(첫 퀘스트 8장 · 나머지 20장까지)

## 공방 — 비법서에 있는 것만 만들 수 있다

**레시피는 맞히는 것이 아니라 「장」을 갖는 것이다** (`UI_POLICY.md` 3-2d).
`S.discovered` 가 곧 가진 장이고, 뜻만 바뀌어서 **마이그레이션이 필요 없다.**

⚠️ **장이 없는 조합은 AP 를 쓰기 «전»에 막는다.** 먼저 깎고 나서 알려 주면
플레이테스터가 그만둔 그 좌절이 이름만 바꿔 돌아온다.

## 공방 — 몇 개 만들지 먼저 묻는다

「조합하기」를 누르면 **개수 패널**이 뜬다 (`openBrewQty`). 1개 / +1 / +10 / 최대 로
고르고, **그 개수만큼의 재료 소진량과 드는 AP** 를 그 자리에서 보여 준다.

- ⚠️ **관문을 다 지난 «뒤에» 묻는다.** 두 개 미만 · 흐린 장 · 없는 장을 먼저 거른다 —
  묻고 나서 「없는 장입니다」라고 하면 AP 를 먼저 깎는 것과 같은 종류의 실례다.
  **취소하면 아무 일도 없다** (AP 도 안 깎인다)
- ⚠️ **최대치는 재료와 AP 를 «둘 다» 본다** (`brewMax`). 재료만 보면 「최대」를
  눌렀을 때 도중에 AP 가 떨어져 절반만 만들어지고, 화면에 적힌 수와 실제가 갈린다
- ⚠️ **한 개밖에 못 만들면 안 묻는다.** 답이 하나뿐인 질문이라 손만 한 번 더 든다
- ⚠️ **튜토리얼 중에는 안 묻는다.** 거기서 가르치는 것은 「담고 → 젓는다」 한 동작이고,
  사이에 패널이 끼면 막이 기다리는 신호가 안 와 **3단계에서 갇힌다** (실제로 그랬다)
- 만드는 것은 `doBrew(n)` 하나다 — `brew()` 를 n 번 부르면 결과 모달이 n 번 뜬다.
  결과 모달에는 개수를 **×3** 으로 붙인다 (안 적으면 몇 개 나왔는지 알 길이 없다)
- ⚠️ **검사기가 `brew()` 를 부르는 자리는 패널을 지나야 한다** — 재료를 넉넉히 심어
  두면 `brew()` 는 묻기만 하고 «아직 안 만든다». 그대로 세면 「보상이 0」·「진행이 0」이
  나오는데, 그건 고장이 아니라 아직 안 누른 것이다 (`checkui` 세 곳 · `checklore` ·
  `checkfarm` 이 그랬다). 흐린 장을 «맞혀 보는» 자리는 예외다 — 거기서는 `brew()` 가
  수수께끼 갈래로 빠져서 패널까지 안 온다

### ⚠️ 검사기가 레시피를 채울 때는 **흐린 장을 먼저 밝힌다**

`fillFromRecipe()` 는 **아는 칸만** 담는다 (그것이 맞는 동작이다 — `LORE`).
그래서 「장을 가졌다」고 `S.discovered` 에만 넣어 두면 솥이 반만 차고, 검사는
「솥에 담긴 것이 레시피와 다르다」로 **엉뚱한 곳을 가리킨다.**

```js
D.hiddenOf(r).forEach(id => learnIng(r.result.id, id));   // 채우기 «전»에
```

`checkmelt` · `checkfarm` 이 이것 때문에 오래 빨간 채로 있었다 — 둘 다 흐린 장이
생기기 «전»에 쓴 검사라, 게임이 아니라 검사가 옛 규칙으로 돌고 있었다.
**여기서 볼 것이 수수께끼가 아니면 밝혀 놓고 시작한다.**

## 공방 — 고른 레시피는 계속 따라온다

레시피를 누르면 `S.want`(요구 재료 목록)가 남는다. **조합해도 지워지지 않는다.**

- 조합 뒤 `refillFromWant()` 가 남은 재료로 솥을 **다시 채운다** — 같은 것을 여러 번 만들 때
  매번 레시피를 다시 누르지 않아도 된다
- 재료가 떨어진 자리는 회색(`.c-slot.lack`)으로 남아 무엇이 없는지 보여 준다
- **손으로 담거나 빼거나 솥을 바꾸면 `S.want` 를 비운다.** 그 순간부터는 사람이 정한 것이
  기준이어야 하고, 그러지 않으면 다음 렌더에서 조용히 되돌아간다
- 솥은 `potFor(n)` 이 **재료 가짓수에 딱 맞는 가장 작은 것**으로 골라 준다.
  2가지 레시피에 12구 솥을 꺼내 놓으면 빈 구멍 열 개가 '더 넣으라'는 뜻으로 읽힌다

## 튜토리얼 (tutorial.js) — 새 플레이어가 반드시 지나는 외길

인트로가 끝나면 이어받아 열여덟 단계를 안내한다. **여기서 한 자리만 막혀도 새 플레이어는
게임을 시작조차 못 한다.** 그래서 규칙이 몇 가지 있다.

- **막의 구멍은 진짜 구멍이다.** 막을 이루는 `path` 하나에 `fill-rule="evenodd"` 로 안쪽
  사각형을 빼서, 칠해진 곳만 클릭을 먹고 구멍은 아래 버튼이 그대로 받는다.
  조각 네 개를 붙여 만들면 경계에서 클릭이 새고 화면이 움직일 때 어긋난다
- **대상은 요소가 아니라 선택자로 적는다.** `render()` 가 화면을 통째로 다시 그리므로
  요소를 붙들어 두면 다음 렌더에 문서에서 떨어져 나간다 (토스트에서 겪은 것과 같다)
- **막다른 길을 만들지 않는다.** 구멍 대상을 못 찾으면 막이 클릭을 먹지 않는 상태(`.loose`)로
  떨어져 아무 데나 누를 수 있다. 안내가 헛도는 편이 갇히는 것보다 낫다
- **구멍은 매 프레임 다시 잰다.** 한 번 재고 마는 것으로는 못 맞춘다 — 화면이 페이드 인
  하는 동안 `.screen` 이 `translateY(6px)` 로 밀려 있어서, 그때 잰 구멍이 **끝까지 6px
  어긋난 채로 남았다** (스크롤·리사이즈가 없으면 다시 잴 계기가 아예 없다).
  대신 값이 그대로면 DOM 을 안 건드린다
- **좌표계는 `#tut` 의 제 상자다.** `documentElement.clientHeight` 로 viewBox 를 잡으면
  그 값과 층의 실제 높이가 어긋나는 순간(주소창이 접히는 모바일) 그림이 통째로 늘어난다
- **막은 모달보다 아래에 깔린다**(`z-index: 40` < 모달 50). 위에 두면 튜토리얼 중에 뜨는
  조합 결과 팝업이 막에 덮여 「확인」이 안 눌린다 — **화면이 통째로 먹통이 된 것처럼 보인다.**
  그리고 팝업이 떠 있는 동안은 감추되 **추적 루프는 계속 돌린다** — 같이 멈추면
  팝업이 닫혀도 막이 안 돌아온다 (설정·확인 패널은 닫을 때 `render()` 를 안 부른다)
- **졸업할 때 바디파츠를 전부 상한(150%)으로 맞춘다** (`maxTune`). 이 게임은 «날씬해지는»
  이야기라 100% 로 출발하면 첫 물약부터 표준보다 마른 몸이 되어 **줄어들 자리가 없다.**
  ⚠️ `bodyTune` 은 세이브가 아니라 제 열쇠(`TUNE_KEY`)에 따로 산다 — 그래서
  마이그레이션이 아니라 «졸업할 때 한 번»이 맞는 자리다 (이미 졸업한 사람이 고른 값을
  나중에 덮으면 그건 뺏는 것이다). 상한은 부위마다 다르므로 **`tuneMaxOf` 를 지난다** —
  150 을 박아 두면 `TUNE_PARTS` 를 낮췄을 때 조용히 범위를 벗어난다.
  `npm run test:tut` 이 졸업 뒤의 값을 상한과 견준다 (사보타주로 확인했다)
- **`tutorialDone` 을 켜 주는 곳은 졸업 단계 한 곳뿐이다.** 이 문이 3구 무쇠 솥 · 크리처 탭 ·
  옷장 열두 칸을 연다. 튜토리얼이 끝났는데 안 켜져 있으면 `finish()` 가 한 번 더 확인한다
- **갈아입기는 졸업 뒤에 온다.** 튜토리얼 전의 마이 룸에는 인트로의 공주 그림이 서 있어서
  (`roomFigure`) 옷을 갈아입어도 화면이 하나도 안 변한다
- **졸업 선물은 신발이다** (`GIFT_SHOES` = `shoes_maryjane`). ⚠️ 원피스(`dress_onepiece`)를
  주던 자리다 — 공주는 이미 원피스를 입고 들어오는데 **발은 맨발**(`shoes_none`)이라,
  선물이 이미 채워진 자리에 하나를 더 얹고 빈 자리는 그대로 남았다.
  **채워야 할 자리를 채우는 쪽이 선물답다.** 선물을 바꾸면 대사(`tut_h5`)와
  안내(`tut_act_shoes`)도 두 언어로 같이 고치고, `checktut` 의 「선물받은 구두를 신었는가」도
  같이 고친다 (id 만 바꾸면 검사가 옛 선물을 계속 본다 — 사보타주로 확인했다)
- **한 번만 도는 효과는 `once()` 로 감싸고 표시를 세이브에 남긴다.** 안 남기면 새로고침으로
  같은 단계에 다시 들어설 때마다 첫 재료 주머니가 또 들어온다
- **말풍선의 이름은 설정상의 이름이 아니라 「부르는 말」이다** (`speakerName()`).
  공주는 **플레이어가 지은 이름**으로 뜨고(진짜 이름 「그위리엘」은 본인도 모른다),
  요정 대모는 인트로와 같은 `sp_fairy` 문자열을 쓴다. `SPEAKERS` 의 `name` 은
  설정상의 이름 그대로 두고 화면에서만 갈아 끼운다 — STORY.md 「호칭 규칙」
- 단계마다 `tab` 을 적어 그 화면으로 옮겨 놓는다. 새로고침하면 늘 마이 룸에서 다시 시작하므로,
  이게 없으면 '솥에 넣으세요' 라고 적힌 막이 마이 룸 위에 떠 있게 된다

```bash
npm run test:tut                       # 열여덟 단계를 진짜 마우스로 눌러 끝까지 가 본다
TUT=1 node tools/checkui.js            # 튜토리얼 화면의 대비·레이아웃
```

`checktut` 은 **`el.click()` 을 쓰지 않는다.** 그것은 `pointer-events` 를 무시해서 막이 뚫려
있는지 없는지를 전혀 검사하지 못한다 — `page.mouse.click` 으로 누르고, 막의 칠해진 자리인지는
`path.isPointInFill()` 로 판정한다.

**눌리는지만 보면 어긋난 구멍을 못 잡는다.** 여백이 8px 라 그만큼 밀려도 한가운데는 여전히
뚫려 있다. 그래서 막의 칠을 이분법으로 더듬어 네 변의 자리를 재고 `대상 + PAD` 와 견준다
(허용 1.5px). 위의 6px 사고가 이 검사 없이는 통과로 보였다.

## 퀘스트 — 초반에는 **손이 비지 않는다** · 점은 늘 켜져 있다

- ⚠️ **첫 퀘스트는 클레멘의 첫 만남**(`q_meet`)이다. 튜토리얼을 마치자마자 도착해
  있어야 졸업 직후가 「이제 뭘 하지」로 시작하지 않는다. 목표가 **한 번**인 이유는
  부엌이 하루 한 번이라서다 — n 을 키우면 첫 퀘스트가 며칠짜리가 된다
- ⚠️ **1막에서는 점수 문턱을 기다리게 하지 않는다** (`refreshQuests` 의 `EARLY_ACT`).
  하나를 끝냈는데 다음 `at`(6 · 10 · 14…)에 못 미치면 **퀘스트가 없는 구간**이 생기고,
  그 구간이 하필 게임을 막 시작한 자리다. 비었을 때만 표 순서대로 다음 것을 내보낸다.
  **`need` 는 절대 안 건너뛴다** — 점수가 아니라 이야기라, 건너뛰면 아직 만나지도
  않은 사람이 부탁을 해 온다. **2막부터는 안 민다** (`need` 가 이미 순서를 잡는다)
- ⚠️ **퀘스트가 있으면 칩의 점(●)은 늘 켜져 있다.** 예전에는 「아직 안 열어 본 것」에만
  찍었는데, 한 번 열면 꺼져서 **하던 퀘스트가 있다는 것 자체가 안 보였다**.
  다 찬 것에는 「!」 뱃지가 «대신» 붙는다 — `!full` 로 갈라서 둘이 같이 뜨지 않는다
- ⚠️ **「아직이에요」는 눌리는 버튼이다** (퀘스트 UI 공통 규칙 · `QUEST.md` 5-4).
  예전에는 `disabled` 라 눌러도 아무 일이 없었는데, 못 냈다는 것은 이미 막대와
  「0 / 1」로 적혀 있다 — 그런데도 누르는 이유는 **「그래서 뭘 하면 되는데」** 하나뿐이라
  반응이 없으면 고장으로 읽힌다. 누르면 **목표를 토스트로 버튼 바로 «위»에** 띄운다
  (`questNotYet`). 아래로 두면 「닫기」를 가려 다음 손짓을 막고, **시트는 안 닫는다** —
  닫으면 방금 읽던 목표 줄이 같이 사라져 토스트가 어디서 나온 말인지를 잃는다.
  · ⚠️ **잠긴 표현을 «값으로 베껴» 두지 않는다** — `.q-claim:disabled` 가
    `filter: saturate(0.15); opacity: 0.8` 을 그대로 적어 두고 있었다
    (`.pg-row.locked` 과 같은 사고다). 잠긴 것이 아니라 **눌리는** 버튼이다
  · ⚠️ **검사기가 「`disabled` 인가」를 보면 안 된다** — `checkui` 는 **눌러 보고**
    ① 보상이 안 나갔는가 ② 목표가 토스트로 떴는가를 본다. 토스트만 재면
    「가져가기」로 잘못 붙여 놔도 통과한다 (그쪽도 토스트를 띄운다). 사보타주 둘로 확인했다
- ⚠️ **내보냈으면 칩까지 `refreshQuests()` 가 그린다.** 부르는 쪽마다 따로
  `renderQuestChip()` 을 붙이고 있었는데, **켤 때는 `render()` 가 그보다 먼저**
  지나가서 **새로고침하면 하던 퀘스트의 칩이 사라졌다** — 퀘스트는 세이브에 멀쩡히
  살아 있고 화면에서만 없어지는 종류다
- ⚠️ **컷씬이 끝나면 화면을 다시 그린다**(`cutNext`). 본 컷씬이 화면을 열기 때문이다 —
  클레멘의 첫 만남이 **부엌 버튼을 여는데** 다시 안 그리면 아무것이나 화면을 다시
  그릴 때까지 버튼이 안 나타났다(**레드닷도 같이**). 판정(`seenCuts`)은 `playCut` 이
  시작할 때 이미 끝나 있어서 빠진 것은 「그리기」 하나였다. 사보타주로 확인했다
- ⚠️ **검사기가 「첫 퀘스트」에 기대고 있었다.** 표 맨 앞이 조합 퀘스트라고 보고
  `goal.id` 로 레시피를 찾던 자리가 넷 있었는데, 맨 앞이 부엌으로 바뀌자 통째로 터졌다
  (`checkui` 의 퀘스트점 · 퀘스트시트 · 퀘스트완료 · 컷씬).
  지금은 **조건에 맞는 퀘스트를 골라 세운다** — 「가진 장 중에서 고르지 않는다」와 같은 결이다.
  · ⚠️ 골라 세울 때는 **큐도 같이 비운다**(`S.quest = {active, n, done, queue}`).
    `active` 만 갈아 끼우면 그 id 가 큐에 남아 **보상을 받은 직후에 같은 퀘스트가 또 나온다**

## 퀘스트 — 2막부터는 **이야기가 연다** (매력이 아니라)

1막의 여덟은 전부 매력(`at`)으로 열린다. 2막부터 그러면 안 된다 — 마을과 키워드가
이야기로 열리는데 퀘스트만 점수로 열면 **「왜 안 열리지」가 두 벌**이 된다
(마을 해금의 「점수로 두 번 잠그지 않는다」와 같은 규칙).

그래서 `need` 를 둔다 — `{ kw }` · `{ village }` · `{ cut }`. **되돌아가는 값이
하나도 없는 것들만** 쓴다: 키워드·마을·본 컷씬은 한 번 얻으면 안 사라지므로
열렸던 퀘스트가 닫히지 않는다. `at` 은 **표 순서를 지키는 용도로만** 남긴다
(`checkdata` 가 오름차순을 본다).

- ⚠️ **목표를 「물어보기」로 두지 않는다.** 물어본 것은 한 번뿐이라, 퀘스트를 받기
  «전»에 이미 물어 놓았으면 그 뒤로 **영영 안 오른다** — 화면에 오류 하나 없이 막힌다.
  2·3막의 목표는 **「마을이 열렸는가」(`village`) · 「키워드를 가졌는가」(`keyword`)**
  같은 상태다. 뒤로 안 가고, 이미 이룬 것이면 그 자리에서 완료로 뜬다
- ⚠️ **키워드·마을이 들어오는 자리(`doAsk`)와 컷씬을 본 자리(`playCut`)에서
  `refreshQuests()` 를 부른다.** 안 부르면 다음에 매력이 오를 때까지 칩이 안 뜨는데,
  엔딩을 본 사람에게는 그 「다음」이 영영 안 올 수도 있다. **사보타주로 확인한 자리다**
- ⚠️ **여신(100) 위로 `at` 을 올리지 않는다.** 이야기가 아니라 점수를 더 모아야
  진행되는 것이 되고, `need` 가 이미 순서를 지킨다
- ⚠️ **2막 셋 말고는 비법서 장을 안 준다.** 여신에서 그물이 전부 주고 나면 줄 장이
  남지 않는다 — 억지로 붙이면 「받았는데 아무 일도 안 일어난다」가 된다. 결정과 재료로 갚는다
- **`q_seal` 이 공방을 5단계로 올린다** (`reward.room`). ⚠️ 그전까지 `roomLevel` 을
  올리는 길이 **개발용 스위치밖에 없어서**, 엔딩 조건(`sealReady`)이 정상 플레이로는
  영영 안 찼다 — 만들어 놓은 엔딩에 아무도 못 닿는 종류의 사고다.
  보상은 **내려가지 않게 `max` 로** 올린다 (보상이 진행을 되돌리면 그건 벌이다)

```bash
npm run test:story   # 2~5막을 진짜 화면에서 처음부터 끝까지 걸어 본다 (브라우저 필요)
```

`checkstory` 는 **키워드를 손으로 꽂지 않는다.** 꽂아 놓고 `refreshQuests()` 를 직접
부르면 「물어봤더니 칩이 떴다」가 아니라 「검사기가 칩을 띄웠다」를 재는 것이라,
`doAsk` 의 새로고침을 통째로 지워도 통과한다 (사보타주로 확인했다).
`checktalk` 은 **표에서** 같은 것을 본다 — `need` 와 목표가 가리키는 키워드·마을을
시작 키워드에서 실제로 걸어서 닿을 수 있는가.

## 스토리 다시보기 — **막마다 장면이 있어야 한다**

⚠️ **못 본 «컷씬»의 제목은 안 내놓는다** (`renderStory`) — 제목 하나가 곧 스포일러다.
그래서 **장면이 하나도 없는 막은 영영 안 보인다.** 2막을 만들 때 마을·키워드만 늘리고
컷씬을 안 만들었더니 다시보기가 **1막에서 3막으로 건너뛰었다** (「1막밖에 없다」).

### ⚠️ 막은 「🔒 몇 개」로 «전부» 서 있다 (감추지 않는다)

예전에는 아무것도 못 본 막을 **통째로 뺐다.** 그러면 다섯 막을 다 만들어 놓고도
화면에는 1막 하나만 서 있어서 **「없는 것」과 「아직 못 본 것」이 구별되지 않는다** —
같은 신고를 두 번 받았다 (「2~5막이 안 올라와 있어」).

잠긴 칩(`.ask-chip.locked`)에서 이미 정한 규칙을 그대로 쓴다:
**안 보이면 「없는 것」이고 보이면 「아직 못 여는 것」이다.**

- 막 «이름»(엔딩·에필로그)은 **하나라도 본 뒤에** 붙는다. 번호(4막)는 셋 넘는 막이
  있다는 것밖에 안 알려 주지만 이름은 그 자체가 이야기다
- **하나도 못 본 사람에게는 자물쇠 다섯 줄 대신 한 마디**(`st_empty`)를 준다 —
  누를 것이 하나도 없는 목록은 목록이 아니다

- 막을 새로 만들면 **컷씬을 최소 하나** 같이 만든다. `npm test` 는 이것을 안 잡는다 —
  「없는 것」이라 검사할 대상 자체가 없다
- 2막의 둘과 3막의 하나는 **마을에 처음 들어설 때** 뜬다 (`VILLAGE_CUT`). ⚠️ 마을이 «열리는»
  순간이 아니다 — 여는 것은 대답을 읽는 자리라, 거기에 얹으면 그 사람이 방금 한 말이 덮인다
- **엔딩도 다시보기에 들어간다** — `c_end_*` 넷이 **4막**, `c_epilogue` 가 **5막**이다.
  `renderStory` 는 `CUTS` 의 `act` 를 그대로 훑으므로 막을 늘려도 코드는 안 고친다.
  ⚠️ **엔딩을 4·5막으로 뺐더니 3막이 통째로 비었다** — 그때까지 act 3 는 전부 엔딩이었다.
  2막에서 겪은 것과 똑같은 구멍이라 3막 컷씬(`c_spire_in`)을 같이 만들었다
- 막 이름은 **번호 뒤에 덧붙인다**(`ACT_NAME` → 「4막 · 엔딩」). 번호만 적으면 STORY.md 의
  3막 구성과 어긋나 보이고, 이름만 적으면 사람이 센 순서를 잃는다.
  ⚠️ **번호를 문자열 안에 박지 않는다** — 막이 하나 늘면 통째로 어긋난다
- ⚠️ **`checkui` 의 「스토리다시보기」는 이름 붙는 막을 하나는 «본 것», 하나는 «못 본 것»으로 둔다.**
  1막 것만 심으면 「4막 · 엔딩」 같은 더 긴 머리글을 한 번도 안 재는 것이고, 이름 붙는 막을
  전부 심으면 「못 본 막의 이름이 새는가」를 한 번도 안 재는 것이다 — 어느 쪽이든 0건이
  통과가 아니라 「재 본 적 없다」가 된다 (영어는 더 길다). 사보타주 셋으로 확인했다:
  못 본 막을 빼면 「막이 2개 서 있다」 · 못 본 막에 이름을 붙이면 「번호만으로 안 서 있다」 ·
  자물쇠 줄을 없애면 「자물쇠 줄이 없다」. `.st-actname` 을 `nowrap` 으로 만들면
  `__cardFits` 가 253px 넘침을 잡는다

## 엔딩 — 방아쇠가 둘이고, 둘 다 이야기가 정한 것이다

컷씬 다섯이 **이어서** 돈다 (`playEnding()`). 하나씩 끊으면 사이에 화면이 돌아와
「거울에 금이 갔는데 채집하러 갈 수 있는」 상태가 된다.

| | 어디서 | 조건 |
|---|---|---|
| `c_end_seal` | 마이 룸에 들어설 때 | 「진짜 나」 + **공방 5단계** (`sealReady`) |
| `c_end_call` → `c_epilogue` | 거울못에 설 때 | 봉인이 풀려 있음 (`endingReady`) |

- ⚠️ **`render()` 안에서 트지 않는다.** 렌더는 수시로 돌아서 컷씬이 화면을 덮은 채
  다시 그려진다. 「마이 룸에 들어선다」는 `switchTab()` 한 자리에서만 판정한다
- ⚠️ **점수(매력)로 잠그지 않는다** — 마을 해금과 같은 규칙이다. 대신 **공방 5단계**가
  조건이라, 지금까지 조건조차 없던 마이 룸 업그레이드에 목적이 생긴다
- ⚠️ **칩(물어볼 것)으로 두지 않는다.** 마지막 순간은 공주 혼자라 다른 다섯 개와
  나란히 놓일 것이 아니다 (그리고 유타르크는 이미 여섯 칸이 찼다)
- **연출은 «줄»에 붙는다** — `lines` 의 셋째 칸(`['sp_yutark','true','crack']`).
  금(`crack`) → 깨짐(`shatter`) → 빛(`light`) → 남은 조각(`shard`).
  ⚠️ **깨지는 것은 「조각마다 비친다」고 «말하는» 줄에서다** — 한 줄 앞이면 대사가
  이미 지나간 일을 설명하는 꼴이고, 한 줄 뒤면 그의 마지막 말이 멀쩡한 거울 앞에서 나온다
  · ⚠️ **`setTimeout` 으로 조각을 붙이지 않는다** (밭 약탈 줄에서 배운 것과 같다) —
    검증기가 재는 순간에 아직 안 붙은 조각은 «없는 것»으로 세어 통과한다.
    CSS 애니메이션 + `animation-delay` 라야 `settle()` 이 끝 상태로 보내 준다.
    그래서 **평소 상태가 곧 끝난 상태**여야 한다 (조각은 흩어져 사라진 뒤가 기본)
  · **글자를 하나도 안 넣는다** — 장식이 대비 검사를 흔들지 않게
  · 연출이 있는 줄에서만 배경을 더 덮는다(`.cut.deep`). `.cut` 전체를 어둡게 하면
    1막 컷씬까지 같이 어두워진다
  · ⚠️ **`checkui` 의 「엔딩연출」은 연출이 «켜진» 줄에서 잰다** — 위의 「컷씬」 검사는
    1막이라 연출이 없고, 그 0건은 이 층을 한 번도 안 잰 것이다
- ⚠️ **여왕의 이름은 `c_end_call_3` 한 줄에서만 불린다.** `checkname` 의 허용 목록에
  그 키를 **줄 모양까지** 못 박아 뒀다 — 옆줄(`_4`)에 새면 그대로 잡힌다
- **지문을 인물 이름으로 띄우지 않는다.** 내레이터가 SPEAKERS 에 없어서
  (`checkdata` 가 없는 인물을 잡는다) 「거울이 깨지고…」 같은 줄은 **그녀가 본 것을
  말하는 대사**로 고쳐 썼다 — 새 장치를 만드는 것보다 글이 나아진다

## 아직 안 한 것

- **마을 여덟이 전부 키워드로 열린다** (`STORY.md` 「키워드 시스템」 · `QUEST.md` 5단계).
  1막에서 다섯, 2막에서 유리관 호수 · 은빛 갱도, **3막에서 여왕의 첨탑**.
  ⚠️ **점수로 두 번 잠그지 않는다** — `isVillageOpen()` 은 `S.villages` 만 본다.
  여기에 매력 조건을 또 걸면 왜 안 열리는지 플레이어가 알 수 없다.
  · 표는 `data.js` 의 `KEYWORDS`(열넷)와 **`ASKS`**(마흔일곱)다.
    ⚠️ STORY.md 의 예시 이름 `TALKS` 를 못 쓴다 — 인사말·대사 표가 이미 그 이름이고,
    모듈이 없어서 전역이 겹치면 그 파일이 통째로 안 돈다
  · **시작 키워드가 있다** — `S.keywords` 의 기본값이 `['kw_hunger']` 다.
    마을이 전부 잠긴 채로 「물어볼 것이 없다」가 되면 아무 데도 못 간다.
    **부엌(클레멘)은 늘 닿으므로** 거기서 첫 마을이 열린다
  · **다시 물어도 된다.** 이미 물어본 것은 회색이 될 뿐 사라지지 않고,
    주는 것(`gives`·`opens`)만 처음 한 번이다
  · **갈 곳만 알려 준다** — 새로 물어볼 것이 있는 마을 탭·건물·부엌 버튼에 점(●).
    무엇을 물을지는 안 알려 준다 (「길 잃음 방지가 제일 중요하다」)
  · 검사 둘 — `npm test` 가 `checktalk` 으로 **표를 걸어 보고**(도달 불가능 ·
    죽은 키워드 · 순환 · **막다른 진행**), `npm run test:ask` 가 **진짜 화면에서**
    부엌 → 굴뚝 → 사과밭 → 골짜기까지 눌러 본다.
    ⚠️ 진행이 막히는 버그는 화면에 아무 오류도 안 띄운다 — 플레이어는 그냥 그만둔다
  · **내보일 마을은 「건물이 있는 곳」이다** (`villagesShown()`). 예전에는 앞에서
    셋을 잘라 냈는데, 그러면 **목록 순서와 채운 순서가 어긋나는 순간 막힌다** —
    가시덤불 마을은 일곱 번째라 거기까지 늘리면 안이 텅 빈 마을 둘이 자물쇠로 딸려 왔다.
    ⚠️ 마을에 `spots` 를 넣으면 **그 순간 목록에 나타난다** — 여는 줄(`opens`)도 같이
    넣지 않으면 `checktalk` 이 「막다른 진행」으로 잡는다
  · **호감도 5단계**가 붙었다 (아래 항)
  · ⚠️ **한 사람이 반응하는 키워드는 여섯까지다** (`checktalk` 의 `ASK_MAX`).
    2막에서 다섯이 여섯이 됐는데, **막마다 한 칸씩 늘릴 수는 없다** —
    칩 줄이 화면을 먹고 「반응 없는 것 고르기」가 된다.
    그래서 3막의 것은 여기를 또 올리는 대신 **새 사람**(이그리트)이 받았다 —
    지금 여섯이 여섯 칸씩 차 있고 이그리트가 다섯이다. **다음 막도 같은 방식이다**
  · **2막은 「유리관」 하나에서 갈린다** — 실반에게 물으면 «어디에 있는지»(유리관 호수),
    오릭스에게 물으면 «누가 만들었는지»(은빛 갱도)가 열린다. 한 줄이 둘 다 열면
    나머지 한 사람에게 물을 이유가 사라진다.
    그 키워드 자체가 호감도 3단계에서만 나오므로 **2막은 사람과 친해진 뒤에 온다**
  · **3막은 「불로장생」을 슈타르크에게 가져가면 열린다** — 그는 원래 그녀가 고용한
    암살자라 성 안을 아는 유일한 사람이다. ⚠️ **첨탑도 매력 점수로 안 잠근다**
  · ⚠️ **여왕의 이름(이그리트)은 화면 어디에도 나오면 안 된다.** 마지막에 거울이
    그녀를 기억해 내며 부르는 이름이라, 그 전에 한 번이라도 뜨면 그 장면이 죽는다.
    설정값은 `SPEAKERS`·`NAMES` 에 그대로 두고 **`speakerName()` 이 「여왕」으로
    갈아 끼운다** (공주·요정 대모와 같은 방식). `tools/checkname.js` 가 둘로 지킨다 —
    **문자열 훑기**(브라우저 없이 · `checktalk` 이 `npm test` 마다 돌린다)와
    **진짜 화면 훑기**(`npm run test:name` · 두 언어로 대사·잡담·칩을 다 펼쳐 본다).
    ⚠️ 화면 쪽은 **「대화」 버튼을 눌러야** 잡담이 나온다 — 말풍선만 두드리면
    인사말에 머물러 잡담에 흘린 이름을 못 잡는다 (사보타주로 확인한 자리다)
  · **「진짜 나」를 유타르크에게 «칩으로» 가져가지 않는다** — 그것이 저주가 풀리는
    순간이라 엔딩의 몫이다. 3막에서는 실반·발렌만 받고, 유타르크에게 가는 것은
    **엔딩 컷씬**이 받는다 (아래 항)
  · ⚠️ **유타르크의 「봉인」 대답은 손대지 않는다** — 「봉인 때문에 그렇게 먹은 것이
    아닙니다」가 `STORY.md` 「봉인은 허기의 원인이 아니다」 그 자체다.
    폭식이 마법 탓이 되는 순간 애정결핍 이야기가 통째로 없어진다
- **본줄기는 1막부터 엔딩까지 이어졌고, 다섯 막에 퀘스트가 다 붙었다**
  (요리사 ✅ → 튜토리얼 ✅ → 키워드·1막 ✅ → 2막 ✅ → 3막 ✅ → 엔딩 ✅ →
  **2~5막 퀘스트 일곱 ✅**). 퀘스트 16 · 컷씬 41(막별 19·8·5·6·3)다.
  **호감도 5단계 대사와 그들이 「주는 것」 다섯도 다 붙었다** (`bt_*` 60줄 × 두 언어 ·
  `BOND_GIVES` 의 🛡️🍖🎻💎📖). 기본 잡담도 사람마다 다섯 줄까지 채웠다.
  막별 전체 표는 **`ACTS.md`** 에 있다. 남은 것은 연출이다 — `STORY.md` 에 아직 «기획만» 있는 것이 무엇인지는
  그 문서의 「만드는 순서」가 ✅ 로 표시한다. 거기 적힌 것을 '있는 기능' 으로
  착각하지 말 것 — 만들 때 그 문서에서 대사와 조건을 뽑아 쓴다

- **크리처는 `CREATURE.md` 의 1~9단계까지 됐다.** 서른 종을 만들고(`tools/gencreature.js`)
  이모지 대신 SVG 로 그리고(`creature.js`), 매력 무한 누적을 없애 **장착한 한 마리만**
  반영되게 했다 (세이브 12). 맵 51곳에 속성이 붙고, **동행 한 마리를 데려가면
  속성·날씨·시간대가 맞는 만큼 재료를 하나 더 준다.** 히든 재료 확률도 맵마다 갈렸다.
  **먹이를 주면 로열티(덤 개수)와 버프(덤 확률)가 붙는다.**
  **7단계로 중복 크리처를 재료로 녹인다** — 상급 조합에 같은 속성의 중급 크리처가
  하나 들어간다. **8단계로 하루에 한 번 생산한다** — 속성이 무엇을, 등급이 몇 개를
  정하고, 만드는 것은 **애착 한 마리 + 동행 한 마리**뿐이다.
  **9단계로 밭과 약탈이 들어갔다** — 여기서 **서버가 판정을 갖는다.**
    밸런싱도 연출도 끝났다 (`FARM.md` 9-1 · 9-2).
  · **해금은 `charmPeak()`(여태 닿은 최고 매력)으로 판정한다.** 애착을 약한 것으로 바꾸면
    총합이 내려가는데, `isMapOpen` 이 총합을 그대로 보면 열려 있던 맵이 다시 잠긴다.
    **한 번 연 것은 무슨 이유로도 닫히지 않는다**
  · **날씨를 `Math.random()` 으로 정하면 안 된다.** `render()` 마다 바뀌어 채집 한 번에
    비가 왔다 갔다 한다 — 리그 NPC 처럼 **시각과 맵 id 로 시드를 고정한다**(`weatherOf()`).
    세이브에는 안 남긴다. 남기면 기기마다 달라지고 동기화 충돌거리가 하나 는다
  · **채집 시간대는 운동(`EX_WHEN`)의 구간을 더 잘게 자른 것이다.** 경계를 따로 잡으면
    20시 30분에 「운동은 낮인데 채집은 밤」이 되어 설명이 두 벌이 된다.
    `npm run test:time` 이 이것을 검사한다 — 경계를 한 시간만 옮겨도 잡힌다
  · **크리처가 어디에 있는지는 `move` 가 정한다** — 물고기는 **어항**, 날개가 있으면 공중,
    나머지는 땅. 한동안 서른 마리가 전부 떠 있어서 유니콘이 허공을 딛고 있었다.
    물고기에는 지느러미(`wing: 'fin'`)가 있어서 **물이 날개보다 먼저 걸려야 한다**
  · **채집 토스트의 「단서」는 조각을 조립한 것이다** (`gatherClue()`). 조합이 7천 가지라
    문장을 다 쓸 수 없다. **매번 띄우지 않는다** — 꾹 누르기 자동 채집이 있어서
    매번 두세 줄이 되면 잔소리가 된다 (`CREATURE.md` 6장)
  · **솥에 넣는 것을 `itemOf(id)` 한 곳에서 찾는다.** 레시피 입력에 크리처 id 가
    섞이면서 필요해졌다 — 예전에는 가방·솥 구멍·레시피 줄이 저마다 `D.INGREDIENTS[id]`
    를 **바로** 읽어서, 크리처가 들어오는 순간 그 조회가 전부 `undefined` 가 됐다.
    개수는 `stockOf`(재료는 가방 · 크리처는 **초과분**), 소모는 `spendItem` 을 지난다
  · **마지막 한 마리는 못 녹인다**(`meltCount` = 가진 수 − 1). 그래서 로열티·버프가
    날아가거나 장착이 풀릴 일이 없다 — 기획 초안의 「확인 모달로 경고」보다 이쪽이 낫다.
    `npm run test:melt` 가 **진짜 화면에서 담고 조합해** 개체 수까지 센다
  · **생산은 평야·숲 재료만 준다.** 늦게 열리는 지대 재료를 매일 쥐여 주면
    **맵 해금이 의미를 잃는다** — 기초 크리처의 재료를 평야·숲으로 묶어 둔 것과 같은
    이유다. 생성기가 지대·히든·속성 중복을 다 검사한다
  · **방치 상한과 기록 길이를 같은 값으로 둔다**(`PRODUCE_DAYS` = 5).
    기록이 5줄인데 상한이 30이면 「5일치까지만 쌓인다」를 아무도 눈치 못 챈다.
    정산은 **AP 자정 충전과 같은 자리**에서 한다 — 날짜 판정이 두 벌이면 어긋난다.
    `npm run test:time` 이 30일을 건너뛰어 5일치만 들어오는지 본다
  · **먹이는 축이 둘이다.** 로열티는 영구히 쌓여 덤의 **개수**를 늘리고, 버프는 일시적으로
    덤의 **확률**을 올린다. 하나로 묶으면 「나가기 직전에 먹인다」는 판단이 사라진다.
    **로열티를 확률에 얹지 않는다** — 확률에는 이미 넷이 붙어 있어 다섯째는 아무도 못 느낀다.
    버프는 **끝나는 시각 하나**로만 산다 (카운트다운은 앱을 꺼 두면 안 흐른다)
- 아우라 중 **우아함**은 아직 효과가 붙어 있지 않다.
  **개성**(옷 획득 확률)과 **행운**도 수치만 오르고 아직 연결되지 않았다.
  **근성**은 운동이 붙었다 — 고강도 종목을 열고 근육량을 올린다 (`EXERCISE.md`)
- 이메일·소셜 로그인. 붙일 때는 지금의 `playerId` 를 계정에 연결만 하면
  기존 플레이어 진행이 그대로 넘어간다 (세이브 형식은 그대로, 소유자만 바꾸면 됨)
- **리그(주간 랭킹)의 상대가 아직 NPC 다.** 화면·정산·승급/강등은 다 돌아가는데,
  같은 조 11명은 주차·리그로 시드를 고정해 만든 NPC 다 (`leagueNpcs`).
  진짜 플레이어끼리 조를 짜려면 서버가 **주간 점수와 조 편성**을 들고 있어야 한다 —
  지금 서버에는 누적 매력 순위표(`GET /api/ranking`, `charm` 컬럼)뿐이다.
  붙일 때는 `leagueNpcs` 가 돌려주는 자리를 실제 사람으로 바꿔 끼우면 된다.
  · **밭(9단계)이 먼저 그 길을 냈다** — 서버가 남의 행을 골라 오고(`store.peers`)
    이름으로 상대를 가리키는 짜임이 이미 있다. 조 편성도 같은 방식으로 붙일 수 있다
- **밭을 키우는 기획이 `FARM.md` 에 있다.** 여신 단계(매력 100)에 탐험의 세 번째
  갈래로 열리고, 다섯 칸을 경작해 **채집으로는 안 나오는 특수 작물**을 기르며,
  크리처 다섯이 칸 하나씩을 지키고 다섯 판을 **자리 대 자리**로 붙는다.
  만드는 순서를 문서 끝에 적어 두었다 (그릇 → 탭 → 경작 → 물약 → 5대5 → 밸런싱).
  · **1단계(그릇)** — 서버의 밭 레코드가 `farm.plots[]` 가 됐고, 옛 모양
    (`farm.stash` 하나)은 읽을 때 첫 칸으로 옮겨 적는다(`migrateFarm`)
  · **2단계(탭)** — 탐험이 **필드/마을/밭 세 갈래**가 됐다. 밭은 여신 단계
    (매력 100 · 랭킹과 같은 문)부터 열리고, 판정은 `farmOpen()` → `charmPeak()` 다 —
    애착을 약한 것으로 바꿔도 **한 번 연 것은 안 닫힌다**.
    마이 룸의 🌾 는 여신부터 **그 탭으로 보낸다**(같은 것을 두 자리에서 안 보여 준다).
    시트와 탭은 **`farmHtml()` 하나가 그린다** — 두 벌이면 한쪽만 고치게 된다
  · **3단계(경작)** — 밭에 **칸 다섯**이 생겼다. 심으면 특수 작물이, 비워 두면
    크리처의 이삭이 쌓인다. 칸은 **현자의 결정**으로 연다 (AP 충전에 이은 두 번째 출구).
    특수 작물 여섯은 `tools/genfarm.js` 의 축 표에서 나온다 — **씨앗 칸을 안 만들고**
    이미 있는 재료를 낸다 (네 번째 가방이 생기지 않게).
    ⚠️ **재료·현자의 결정은 클라이언트가 깎는다.** 세이브가 클라이언트 정본이라
    서버가 깎아도 다음 저장에 되살아난다 — **서버가 판정을 갖는 기준은
    「남의 것을 건드리는가」다**(약탈이 그렇다). 다만 **다 자라는 시각은 서버가 잰다**
  · **4단계(물약)** — 특수 작물을 하나씩 먹는 **상급 물약 여섯**. 밭이 없으면 못 만든다.
    **여섯의 값이 일부러 같다** — 하나가 더 좋으면 나머지 다섯 작물이 죽은 콘텐츠가 된다.
    `checkdata` 가 **파일에 써진 것**을 본다 (작물이 채집 풀에 없는가 · 아무 레시피도
    안 쓰는 작물이 없는가 · 들어갈 솥이 있는가)
  · **5단계(5대5)** — 크리처 **다섯이 칸 하나씩을 지키고**, 쳐들어간 다섯과
    **자리 대 자리**로 붙는다. 세 판 이상 이겨야 가져간다.
    **총 전투력 합으로 붙이지 않는다** — 합으로 하면 고르는 일이 「합계 최대화」
    하나로 끝나고 속성 순환이 평균에 뭉개진다. **자리 번호 = 칸 번호**라
    1번을 이기면 상대의 1번 칸에서만 가져온다.
    ⚠️ **자라는 중인 칸은 이겨도 안 건드린다** — 남의 시간을 없애는 것은
    뺏는 것보다 나쁘다. ⚠️ **부대를 바꾸고 바로 쳐들어가면 서버는 옛 부대를 본다**
    (저장이 3초 디바운스다) — `doRaid` 가 먼저 `Sync.pushNow()` 로 올린다
    **밭을 처음 열면 센 다섯이 저절로 선다**(`autoTeam`) — 빈 밭은 그냥 털리는 밭이다.
    자리마다 눌러 그 자리로 열고, **이미 선 아이를 고르면 두 자리를 맞바꾼다**
    (막으면 순서를 바꿀 때마다 비우고 다시 넣게 된다)
  · **6단계(밸런싱·연출)까지 다 만들었다** (`FARM.md` 9-1 · 9-2)
- ~~**밭·약탈의 수치는 전부 임시값이다**~~ → **밸런싱을 잡았다** (`FARM.md` 9-1).
  「사람이 여럿 붙어 봐야 안다」고 적어 뒀는데 **사람이 있어야 아는 것은 접속 빈도뿐**이고
  나머지는 규칙에서 나온다. `npm run sim:farm` 이 200명을 30일 살게 해서 잰다 —
  **`server/battle.js` 의 진짜 함수를 부른다** (사본을 만들면 시뮬레이터만 옛 규칙으로 돈다).
  · ⚠️ 제일 큰 것은 수치가 아니었다: **남의 밭을 안 자라게 둬서 약탈이 30일 동안 0번**이었다.
    밭은 주인이 들어와야 자라는데 주인은 들어오면 곧바로 거둔다 — 저장된 밭은 늘 비어 있었다.
    안 자라게 둔 이유(「정산 시각이 남의 접속에 좌우된다」)는 틀렸다:
    `grow()` 는 시계를 `now - (elapsed % 24h)` 로 당겨 **위상을 보존한다**
  · 방패 2시간 → **4시간**(2시간이면 하루 12번까지 털린다) ·
    **「하루치는 못 가져간다」**(`RAID_FLOOR_DAYS` = 2일치)를 넣어 「아침에 빈 밭」을 없앴다
  · **수치끼리의 약속은 `tools/checkbalance.js` 가 본다** (`npm test` 가 돌린다).
    실패 보상 < 조합 값 · 가방 상한 = 밭 상한 · 바닥 < 상한 · 밭 물약 여섯이 같은 값 ·
    맨 위 리그에 닿는가 — 전에는 **주석에만** 적혀 있어 한쪽만 고치면 조용히 깨지던 것들이다
  · 남은 것은 **연출**이다 (이겼다/졌다가 토스트 한 줄)
- **리그 난이도(`leaguePace`)는 임시값이지만, 「빡빡하다」는 이제 틀렸다.**
  맨 아래 리그의 1위가 주 40점, 맨 위가 846점.
  ⚠️ 예전에는 「열심히 한 주의 상한이 600~700점이라 위쪽 리그는 빡빡하다」고 적어 두었는데,
  **밭 물약 여섯(매력 16)이 생기면서 상한이 1312점으로 올라갔다** — 맨 위 리그가 상한의
  64% 라 닿는다. `tools/checkbalance.js` 가 이 관계를 본다 (한 주 AP → 최고 효율 물약 → 점수).
  랭킹이 열리는 시점(매력 100)에는 맨 아래 리그부터 시작하므로 처음 몇 주는 쉽게 올라가고,
  랭킹이 열리는 시점(매력 100)에는 맨 아래 리그부터 시작하므로 처음 몇 주는 쉽게 올라간다.
  주에 한 칸씩이라 맨 위까지 32주다 — 이 곡선을 바꾸려면 여기를 본다
- **마이 룸 꾸미기.** 배경은 5단계로 나뉘어 있고(`avatar.js` 의 `ROOM_LEVELS`)
  소품마다 id 가 붙어 있다. 플레이어가 고른 소품 id 목록을 `roomScene(lv, extra)` 의
  둘째 인자로 넘기면 그대로 얹힌다 — 그릴 것은 있고, **올려 주는 조건과 고르는 화면이 없다.**
  지금은 개발용(임시)의 '🏠 방 배경' 으로만 단계를 바꿔 볼 수 있다 (`UI_POLICY.md` 3-3)
- 이모지 아트 → 실제 일러스트 교체
- **다이아 상점의 결제.** 수량·가격은 `D.SHOP`(data.js)에 있고 패널은 뜬다.
  누르면 '지금은 구매하실 수 없습니다' 로 끝난다 — `buyDiamond()`(game.js) 안쪽만 채우면 된다.
  현자의 결정이 모자란 경우도 전부 `openDiamondShop()` 으로 모여 있다
- **AP 충전 밸런스는 임시값이다.** `D.ENERGY.chargeCost`(1000 = 가득 채울 때) 와
  `failReward`(10). 실제로 내는 값은 **슬라이더로 고른 만큼**이다 (`costFor(n)` 이 비례로 나눈다).
  실패 보상이 조합 값(25 AP)을 넘으면 일부러 실패해서 AP 를 무한히 버는 고리가 생긴다 —
  수치를 바꾸기 전에 이 관계를 먼저 볼 것
