# 다이어터 연금술사 — 작업 지침

코지 연금술 게임. 채집으로 재료를 모아 물약을 만들고, 마시면 캐릭터가 날씬해진다.
빌드 도구 없는 순수 HTML/CSS/JS + 세이브용 Express 서버.

**모든 코드 주석과 문서는 한국어로 쓴다.** (기존 코드가 전부 그렇게 돼 있다)

---

## 구조

```
dieter-alchemist/          ← 저장소 루트. Railway Root Directory 는 비워 둔다
├── index.html             게임 셸. 스플래시·인트로는 인라인 <style>/<script>
│                          (style.css 캐시가 깨져도 항상 뜨게 하려는 의도된 중복)
├── data.js                재료 111 · 채집지 51(형 8) · 레시피 136(크리처 30 · 밭 물약 6) · 솥 11 → window.GameData
├── game.js                상태·로직·렌더링. 세이브는 localStorage
├── sync.js                서버 동기화 → window.Sync
├── avatar.js              SVG 페이퍼돌 → window.Avatar
├── intro.js               튜토리얼 인트로 → window.Intro
├── tutorial.js            튜토리얼(인트로 다음) → window.Tut
├── portrait.js            인물 초상화 → window.Portrait
├── creature.js            크리처 SVG (부품 조합) → window.Creature
├── village.js             마을 지도·건물 안 → window.Village
├── pumpkin.js             미니게임 · 호박 피하기 → window.Pumpkin
├── walnut.js              미니게임 · 호두 게임(합 10) → window.Walnut
├── rock.js                미니게임 · 돌깨기 게임(테트리스) → window.Rock
├── fish.js                미니게임 · 낚시(스타듀 밸리 식) → window.Fish
├── sparrow.js             미니게임 · 참새 쫓기(화살) → window.Sparrow
├── pinwheel.js            미니게임 · 바람개비 퍼즐(Arrows Puzzle 식) → window.Pinwheel
├── driller.js             미니게임 · 바위 부수기(미스터 드릴러 식) → window.Driller
├── i18n.js                한국어/영어 → window.I18N
├── sfx.js                 WebAudio 효과음(에셋 없음) → window.Sfx
├── a11y.js                검증기 → checkTextStyle / checkLayout / checkLocked / checkUI
├── style.css
├── HANDOFF.md             새 세션 인수인계 (배포 주소·시작 전 확인·남은 작업)
├── TEXT_POLICY.md         글자 색·폰트 정책
├── UI_POLICY.md           컴포넌트 정책 (탭·가변폭·잠금 표현 등)
├── ART_POLICY.md          그림 정책 (듀오링고풍 플랫 2D 벡터 · 생성 프롬프트)
├── STORY.md               세계관·인물·3막·키워드 시스템 (**아직 안 만든 것**)
├── ACTS.md                1~5막 퀘스트 17 · 컷씬 43 전체 표 (무엇이 어디에 붙어 있는지)
├── PLAYFLOW.md            **플레이 순서** — 인트로→튜토리얼→1~5막을 한 줄로 (퀘스트를 고치기 전에 볼 것)
├── EXERCISE.md            운동·포만감·스태미나 시스템 (수치를 바꾸기 전에 읽을 것)
├── FARM.md                밭을 탐험의 세 번째 갈래로 키우는 기획 (**아직 안 만든 것**)
├── QUEST.md               퀘스트·컷씬·스토리 다시보기 (1~4단계까지 만듦)
├── CREATURE.md            크리처 — 1~9단계 (녹이기·생산·밭과 약탈까지). 밸런싱·연출까지 끝
├── tools/hooks/           git 훅 — 세션마다 `git config core.hooksPath tools/hooks`
└── server/
    ├── index.js           API + 게임 정적 서빙
    ├── store.js           Postgres / 파일 / 메모리 3단 폴백
    ├── battle.js          밭·약탈 규칙 (9단계). data.js 를 **그대로 읽는다**
    ├── test.js            npm test
    └── README.md          Railway 배포 안내
```

전역은 `window.*` 로 노출한다. 모듈 시스템·번들러 없음. `<script>` 순서가 곧 의존성 순서이고,
**sync.js 는 game.js 보다 먼저** 와야 한다 (game.js 의 `save()` 가 `Sync.push` 를 부른다).

---

## 저장소

`github.com/evachristian/dieter-alchemist` (**공개**). 기본 브랜치 `main`.

공개 저장소다 — **비밀값을 커밋하지 않는다.** 한 번 올리면 지워도 히스토리에 남는다.
접속 정보·토큰은 Railway 의 Variables 에만 두고, 저장소에는 참조만 적는다.

커밋 작성자는 **이 저장소에만** 따로 잡아 두었다 (`git config user.name/email`, `--global` 아님).
전역 설정과 다르니 `git config user.email` 로 확인하고 커밋할 것.

### ⚠️ 커밋 안 한 파일에 `git checkout <파일>` 을 쓰지 않는다

검사기가 진짜로 잡는지 보려고 **일부러 망가뜨렸다가 되돌릴 때** 쓰기 쉬운데,
그 파일에 아직 커밋 안 한 작업이 들어 있으면 **그것까지 같이 날아간다.**
실제로 `data.js` 와 `tools/gencreature.js` 의 4단계 작업을 한 번 통째로 잃었다.

되돌릴 것이 있으면 **먼저 복사해 두고 복사본으로 되돌린다.**

```bash
cp data.js /tmp/…/data.bak     # 망가뜨리기 전에
# … 일부러 고쳐서 검사기가 잡는지 본다 …
cp /tmp/…/data.bak data.js     # git checkout 이 아니라 이것으로 되돌린다
```

### 세션을 시작하면 훅부터 건다

```bash
git config core.hooksPath tools/hooks
```

**한 줄이지만 안 하면 조용히 어긋난다.** `.git/hooks` 는 저장소에 안 들어가서
새 세션마다 비어 있다 — 그래서 훅을 `tools/hooks/` 에 두고 이 설정으로 가리킨다.

지금 걸리는 것은 `post-commit` 하나다: **커밋할 때마다 세션 작업 브랜치를
`main` 과 같은 자리로 옮긴다.**

이 저장소에서는 `main` 에서 작업하고 `git push origin main:<브랜치>` 로 올린다.
그러면 원격 브랜치는 늘 맞는데 **로컬 브랜치 참조만 뒤처진다** —
「머지해줘」 할 때마다 안 맞아 보이던 자리가 그것이다 (실제로 두 번 겪었다).

훅은 **`main` 위에서 커밋할 때만** 움직이고, 그 브랜치에만 있는 커밋이 하나라도 있으면
건드리지 않는다. 밀어내지 않고 알리기만 한다.

---

## 반드시 지킬 것

### 1. 세이브 기본값을 바꾸면 마이그레이션도 같이 써야 한다

**이 프로젝트에서 같은 실수를 두 번 했다.** `defaultState()` 의 기본값만 바꾸면
**이미 플레이 중인 사람에게는 반영되지 않는다.** 불러올 때 `Object.assign(defaultState(), 저장값)`
이라 저장된 값이 기본값을 덮어쓰기 때문이다.

기본값을 바꿨다면:

1. `SAVE_VER` 를 올린다
2. `migrate(st, from)` 에 `if (from < N) { ... }` 를 추가한다
3. **기존 진행은 지우지 말고 없는 것만 채운다**

```js
if (from < 3) {
  if (!Array.isArray(st.discovered)) st.discovered = [];
  STARTER_RECIPES.forEach(id => { if (!st.discovered.includes(id)) st.discovered.push(id); });
}
```

버전은 반드시 **저장된 값**(`parsed.ver`)에서 읽는다. 병합 후의 `st.ver` 는
`defaultState()` 가 이미 최신 버전을 넣어 놔서 판별에 쓸 수 없다. (이것도 한 번 틀렸던 부분)

검사할 때는 **세이브가 없는 새 플레이어만 보지 말고** 예전 버전 세이브를 심어 놓고 확인할 것.

```bash
npm run test:save   # 옛 세이브를 심어 놓고 진행이 살아남는지 (tools/checksave.js)
```

**마이그레이션이 쓰는 값은 `load()` 위쪽에 둔다.** `load()` 는 game.js 를 끝까지 읽기 전에
불린다 — 아래쪽에 `const` 로 둔 표를 `migrate` 가 건드리면 `ReferenceError` 가 나고,
`load()` 의 `catch` 가 그것을 삼켜 **세이브가 통째로 기본값으로 되돌아간다.**
화면에는 아무 오류도 안 뜬다: 어제까지 키우던 캐릭터가 그냥 처음 상태로 보인다.
(세이브 9 를 쓰다가 실제로 만들었고, `checksave` 가 그때 생겼다)

### 2. 한국어를 고치면 영어도 같이 고친다

**문자열은 언제나 두 언어를 함께 넣는다.** 한국어만 추가하면 영어 화면에 한글이
그대로 남는데, 이건 눈에 잘 안 띈다 — 영어로 바꿔 놓고 그 화면까지 들어가 봐야 보인다.

더 중요한 이유는 **폭 검사**다. 영어는 대체로 한국어보다 길어서, 번역이 빠진 자리는
좁은 화면에서 깨지는지 한 번도 확인받지 못한 자리다. `checkUI()` 의 '라벨 2배 확대' 도
**있는 문자열만** 늘린다.

```bash
npm run test:i18n   # ko 에만 있는 키 + 영어 이름이 없는 데이터 id 를 전부 찾아 준다
```

`npm test` 가 이걸 먼저 돌린다. 빠진 게 있으면 서버 검사까지 가지 않고 멈춘다.

두 군데를 본다 — `STRINGS`(UI 문자열)와 `NAMES`(재료·맵·솥·레시피·옷·지대 이름).
데이터를 늘리면 `NAMES.en` 에도 반드시 같이 넣는다. 안 넣으면 `N()` 이 한국어를
그대로 돌려주기 때문에 **동작은 하는데 번역만 조용히 빠진다.**

개발용(임시) 문구도 예외가 아니다. 지워질 것이라도 화면에 있는 동안은 폭을 차지한다.

### 3. 화면을 손댔으면 검증기를 돌린다

브라우저 콘솔에서:

```js
await checkUI()   // 모든 언어 × 라벨 2배 확대로 대비·레이아웃·잠금 표현을 한 번에
```

`checkUI` 는 **async 다.** `await` 없이 부르면 결과 대신 Promise 가 찍혀 `{}` 로 보인다 —
0건으로 착각하기 쉽다. 개별 검사(`checkTextStyle()` `checkLayout()` `checkLocked()`)는 동기다.

0건이 아니면 커밋하지 않는다.

#### 측정 조건은 검증기가 스스로 맞춘다 (예전 78건 유령의 원인)

옛날에는 화면이 페이드 인 하는 도중에 재면 **대비 위반이 수십 건 허위로 잡혔다.**
`.screen` 의 `fade` 애니메이션이 불투명도 0 에서 시작하는데, 창이 숨겨지거나 백그라운드면
프레임 시계가 멈춰 **첫 프레임에 그대로 서 있는다.** 그러면 그 화면 글자가 통째로
`흰 글자 / 흰 배경 / 대비 1.00` 으로 보고됐다 — 마이 룸에서 78건.

지금은 검증기가 **재기 전에 끝이 있는 애니메이션·트랜지션을 모두 끝 상태로 보낸다**(`settle()`).
프레임이 안 도는 환경에서도 즉시 반영되므로 측정 조건이 결정적으로 정해진다.
무한 반복(장식용 반짝임)은 끝이 없으니 건드리지 않는다. 굳이 애니메이션을 살려 둔 채 재려면
`checkTextStyle({ settle:false })`.

같이 들어간 안전장치 — **0건이 항상 통과는 아니라는 것을 구분하기 위한 것들이다.**

- 조상이 투명하면 자기 `opacity` 가 1이어도 안 보이는 글자다. 그런 요소는 검사에서 빼고
  `안보여서건너뜀` 수로 따로 알린다. 지금 보고 있는 화면이 통째로 여기 잡히면 측정 조건을 의심할 것
- 창 폭·높이가 0 이면(패널이 접혔거나 헤드리스) **잴 수 없다고 알리고 `pass:false` 를 낸다.**
  이 상태에서 재면 모든 요소가 부모 밖으로 나간 것처럼 보여 넘침 22건이 허위로 잡혔다
- 배경색을 끝까지 못 찾아 흰색으로 **가정한** 경우는 반드시 `(추정)` 으로 표시된다.
  실측 흰색과 구분이 안 돼 오진의 빌미가 됐던 부분이다

수준이 `주의` 인 항목(라벨 잘림 등)은 위반 수에 들어가지 않는다. `위반` 만 0으로 만들면 된다.

#### 아무것도 없는 세이브는 검사망에 구멍을 남긴다

**가진 것이 없으면 그 칸은 아예 그려지지 않는다.** 재료가 없으면 가방 칸이, 물약이 없으면
선반 칸이 없다. 접혀 있는 것(채집 가방)은 `display:none` 이라 또 빠진다.
이 구멍에 재료 개수 글자(**1.4:1**)와 물약 카드의 `?`(**10.6px**)가 오래 숨어 있었다 —
화면은 통과인데 실제로 플레이하면 보이는 자리다.

```bash
FULL=1 node tools/checkui.js showcase atelier gather
```

`FULL=1` 이면 재료·물약·크리처를 채우고, 채집 가방과 **개발용(임시) 블록을 펼치고**,
마이 룸 하위 탭(옷 / 물약 / 크리처)을 하나씩 돌며 잰다.
**화면을 손댔으면 이것도 같이 돌린다.**

개발용 블록도 접혀 있으면 `display:none` 이라 통째로 빠진다. 출시 전까지는 사람이
실제로 보는 자리이므로 같이 잰다 — 펼쳐서 재기 시작하자마자 265px·영어 라벨에서
`.tune-label` 이 줄을 밀어내 가로 스크롤이 생기던 것이 잡혔다.

#### ⚠️ `checkUI()` 0건이 **시트 안까지 본 것은 아니다**

**팝업은 전부 «바닥 시트»다** (`UI_POLICY.md` 3-2) — 화면 아래에 붙어 올라오고, 폭은 앱과
같고, 나가는 길은 테두리 없는 글자 한 줄(`.sheet-exit` · 「나가기」)이다. 새 시트를 만들면
`index.html` 의 다른 시트를 그대로 베낀다 — 폭·모서리·버튼을 따로 정하지 않는다.

`checkLayout()` 은 정해진 선택자(버튼·칩·카드…)만 재는데, 모달 시트 속의 줄은 대개
그냥 `div` 라 대상이 아니다. 게다가 목록에 `overflow-y: auto` 를 주면 **가로 overflow 도
`auto` 로 계산돼** 넘침 검사에서 통째로 빠진다.

실제로 밭 시트의 줄을 일부러 `nowrap` 으로 만들어도 **모든 화면이 통과**로 나왔다.
그래서 `checkui.js` 에 `__cardFits(선택자)` 를 두고 **내용이 제 칸보다 넓은 요소**를
직접 찾는다 (같은 사보타주에서 19px · 172px 로 잡혔다).
**새 모달 시트를 만들면 `checkui` 에 그 시트를 넣고 이것도 같이 부른다.**

⚠️ **말줄임표를 단 자리는 «일부러» 줄이는 자리라 빼고 본다.** 거기서는 `scrollWidth` 가
잘리기 «전»의 폭이라 늘 제 칸보다 넓은데, 화면에는 「🌬️..」 로 얌전히 선다 — 바람개비 밭
HUD 의 맵 이름이 265px 영어에서 **39px 넘침**으로 잡히고 있었다. 하필 style.css 에
「줄어드는 것은 이름이어야 한다」고 적어 둔 바로 그 자리다 (저장소에 말줄임표가 38곳이라
어디서든 터질 수 있었다). **셋이 «다» 있을 때만 건너뛴다** — `text-overflow: ellipsis` ·
`overflow-x: hidden` · `white-space: nowrap`. 그 셋이 곧 「여기는 잘려도 된다」는 선언이고,
그냥 `overflow: hidden` 으로 **말없이 자르는 것은 그대로 잡힌다** (사보타주로 확인했다 —
`.pw-name` 에서 말줄임표만 떼면 같은 39px 이 되돌아온다).

또 하나 — **값을 심어 놓고 여는 시트는 재기 직전에 아직 그대로인지 확인한다.**
`openFarm()` 처럼 열면서 서버에서 다시 받아 오는 화면은, 기다리는 사이에 심은 값이
날아가고 「지금은 볼 수 없다」 한 줄만 남는다. 그 상태의 0건은 아무것도 안 잰 것이다
(78건 유령과 같은 종류의 실수다).

고친 게 진짜 원인인지 보려면 **바꾼 것을 되돌려 같은 조건에서 한 번 더 재라.**
수치가 그대로면 원래 있던 문제다.

- 글자 색·크기 규칙 → `TEXT_POLICY.md`
- 탭·가변 폭·잠금 표현 → `UI_POLICY.md`
- 그림의 결·생성 프롬프트 → `ART_POLICY.md`
- **정책을 바꾸면 `a11y.js` 의 상수도 같이 고친다.** 문서와 검증기는 짝이다.

예외를 늘리기 전에 색을 고칠 수 있는지 먼저 보라. 지금까지 예외는 근거를 적고 최소한만 추가했다.

### 4. 잠긴 콘텐츠는 공통 규칙을 지난다

컨텐츠 언락으로 잠긴 것은 `style.css` 의 **잠긴 콘텐츠 공통 표현** 블록 하나만 쓴다
(`filter: saturate(0.15); opacity: 0.8`). 화면마다 따로 흐리게 만들지 않는다.
새 잠금 UI 를 만들면 그 블록의 선택자 목록에 추가하고, `checkLocked()` 로 확인한다.

⚠️ **값을 «베껴 쓰는» 것은 목록에 넣은 것이 아니다.** 비법서의 잠긴 재료 줄
(`.pg-row.locked`)이 「공통 표현을 쓴다」고 적어 놓고 실제로는 `filter`·`opacity` 를
그대로 복사해 두고 있었다 — 공통 블록을 고쳐도 그 줄만 옛 값에 남는 상태였고,
`a11y.js` 의 `lockedSelector` 에도 없어서 **`checkLocked` 가 한 번도 안 봤다.**
둘 다 목록에 넣어 고쳤다 (그 줄만 노린 사보타주로 잡히는 것을 확인했다).

⚠️ **잠금 검사는 채도와 불투명도가 «둘 다» 그대로일 때만 잡는다**(`&&`).
하나만 남아도 잠겨 보이기 때문인데, 그래서 **한쪽만 망가뜨리는 사보타주는 통과한다** —
검사기를 못 믿을 이유가 아니라 사보타주를 잘못 고른 것이다 (실제로 한 번 헛짚었다).

채도를 주로 쓰는 이유: `saturate()` 는 명도를 보존해 글자 대비를 떨어뜨리지 않는다.
`opacity` 는 대비를 그대로 깎으므로 0.8 까지만 쓰고, 잠긴 글자는 `--ink` 로 진하게 잡는다.

### 5. 파일을 고쳤으면 캐시 버스터를 올린다

`index.html` 의 `?v=YYYYMMDD<letter>` 를 일괄 치환한다. 안 올리면 사용자가 옛 파일을 계속 받는다.

```bash
sed -i 's/v=20260813a/v=20260813b/g' index.html
```

### 6. 데이터는 손으로 늘리지 말 것

재료·채집지·레시피는 규칙에 따라 생성했다. 대량으로 늘릴 일이 있으면
스크립트로 만들고 **중복 id · 중복 조합 · 없는 재료 참조 · 등급별 진입 점수**를 검사한다.
후반 맵에서만 나오는 재료가 초반 레시피에 들어가면 밸런싱이 깨진다.

커스터마이징 150벌(헤어 30 · 서클렛 20 · 귀걸이 30 · 목걸이 30 · 장갑 20 · 구두 20)도
그렇게 만들었다. `data.js` 와 `i18n.js` 의 `// <<<GEN:...` 구간은 **손으로 고치지 않는다.**

```bash
npm run gen:wardrobe    # 축 표(tools/genwardrobe.js) → data.js · i18n.js 를 다시 쓴다
npm run gen:creature    # 크리처 30종도 같은 방식이다 (tools/gencreature.js)
npm run gen:farm        # 특수 작물 6종 (tools/genfarm.js)
npm run gen:mapattr     # 채집지 51곳의 속성 (tools/genmapattr.js)
npm run gen:maptype     # 채집지 51곳의 «형» (tools/genmaptype.js)
```

⚠️ **`npm test` 가 생성기 다섯을 `--check` 로 먼저 돌린다** — 축 표를 고치고 다시
안 뽑았으면 거기서 멈춘다.

**크리처 서른 종도 축 표에서 나온다.** 속성 여섯 × 다섯 마리이고, 레시피 조합·수치·
그림 부품·영어 이름이 한 줄에서 같이 나온다. 여기서도 **옛 id 셋**(`butterfly` ·
`frog` · `unicorn`)은 절대 새로 뽑지 않는다 — 세이브에 들어 있다.

**`npm test` 가 `tools/checkdata.js` 를 돌린다.** 레시피 **중복 조합**(`RECIPE_MAP` 이
조용히 덮어써서 레시피 하나가 소리 없이 사라지던 자리) · 중복 결과물 · 없는 재료 참조 ·
id 중복을 본다.

축 표가 유일한 원본이라 중복 id·중복 조합이 애초에 생기지 않고, **한국어와 영어 이름이
같은 표에서 같이 나온다** — 한쪽만 늘어나는 일이 없다. `npm test` 가 맨 먼저
`genwardrobe.js --check` 를 돌려 생성 결과와 파일이 어긋났는지 본다.

**옛 id 는 절대 새로 뽑지 않는다.** 그 옷을 입고 있는 세이브가 있어서 id 가 바뀌면
착장이 통째로 날아간다. 생성기의 `LEGACY` 표가 '지금 모습 그대로 나오는 조합' 에
원래 id 를 붙이고, 생성 후 그 24개가 다 살아 있는지 다시 확인한다.

---

## 서버 동기화 (sync.js)

**로컬이 항상 진짜다.** 서버가 죽어도, 오프라인이어도 게임은 그대로 돌아간다.
서버는 기기를 바꾸거나 브라우저 데이터를 지웠을 때 되찾기 위한 사본이다.

- 저장할 때마다 `S.rev` 가 1씩 오른다. 서버에 더 큰 rev 가 있으면 `409` + 서버 세이브를 돌려주고,
  클라이언트가 그걸 채택한다 → **두 기기 동시 플레이 시 먼저 저장한 쪽이 이긴다**
- 저장은 3초 디바운스, 탭을 닫을 때는 `keepalive` 로 즉시 전송, 실패하면 지수 백오프
- ⚠️ **코드로 이어할 때는 «먼저 받아 보고, 있으면 갈아탄다»** (`Sync.peek()`).
  예전에는 형식만 보고 곧바로 신원을 갈아엎은 뒤 로컬 세이브를 지우고 새로고침했다 —
  코드에 오타가 하나 있으면 **되돌릴 방법 없이 빈손으로 새 게임**이 시작됐다.
  원래 신원까지 잃은 채로. `peek` 은 `me` 를 안 건드리고 물어보고, **누구의 세이브인지
  보여 준 다음에야** 갈아탄다. 404(없는 아이디) · 403(잘린 코드) · 형식 오류 ·
  못 닿음을 **갈라서** 말한다 — 하나로 묶으면 오프라인인 사람이 코드를 계속 다시 친다.
  `npm run test:code` 가 기기 둘을 띄워 실제로 옮겨 본다
- 로그인이 없어서 기기마다 무작위 아이디+비밀키를 만들고, 둘을 `.` 로 이은 것이 **복구 코드**다.
  구분자가 `.` 인 이유: 아이디·비밀키가 base64url 이라 `-` 와 `_` 를 포함한다. `-` 로 나누면
  아이디 한가운데가 잘려 **엉뚱한 신원이 조용히 만들어진다** (실제로 겪은 버그)
- **게임 초기화 시 서버 사본도 지운다.** 로컬만 지우면 다음 접속에 서버에서 되살아난다.
  지운 뒤에는 `wiped` 플래그로 이후 전송을 전부 막는다 — 지우는 순간 이미 날아가고 있던
  저장 요청이 나중에 도착해 세이브를 부활시킨 적이 있다

### ⚠️ 밭만은 반대다 — 서버가 정본을 갖는다 (크리처 9단계)

**게임의 다른 모든 것은 로컬이 진짜고 서버가 사본인데, 밭은 반대다.**
남이 내 밭에서 가져간 결과가 적히는 곳이라 세이브 안에 두면 **내가 다음에 저장하는
순간 없던 일이 된다.**

- `PUT /api/save` 는 밭을 **안 건드린다.** 파일·메모리 저장소는 레코드를 통째로 다시
  쓰므로 `put()` 에서 **밭을 옮겨 담아야 한다** — 빠뜨리면 저장할 때마다 밭이 사라진다.
  **컬럼을 늘릴 때와 같은 규칙이다: 저장소 3곳을 다 고친다**
- **판정도 서버가 한다.** 클라이언트가 보내는 것은 「누구의 밭을 턴다」 하나뿐이고,
  전투력·속성·로열티는 서버가 세이브에서 직접 뽑는다 (`server/battle.js`).
  수치는 `data.js` 를 그대로 읽는다 — 서버에 사본을 만들면 축 표를 다시 뽑을 때마다 어긋난다
- **수확·약탈은 `nonce` 로 멱등이다.** 응답을 못 받고 재시도했을 때 거둔 것이 사라지거나
  약탈권이 두 번 깎이면 안 된다 (이름 예약과 같은 이유)
- **남의 `playerId` 는 절대 안 내보낸다** — 그것이 곧 신원이다. 상대는 **이름**으로 가리킨다
- **밭을 못 받아 왔을 때 빈 밭으로 그리지 않는다.** 빈 밭으로 보이면 털린 줄 안다
- ⚠️ **한 화면 안에 정본이 둘이라는 것을 잊지 말 것.** 밭(칸·이삭·방패·약탈권)은
  서버가 정본이지만 **부대(`S.farmDef`·`S.farmAtk`)는 세이브 안에 있다.**
  방어대 줄을 서버가 준 값으로 그렸다가 **자리를 바꿔도 화면이 안 바뀌는** 버그를 냈다 —
  저장이 올라가고 다시 받아 와야 바뀌니 사람 눈에는 안 먹히는 버튼이다.
  서버 값은 「서버가 지금 무엇으로 지켜 줄지」이고, 화면에 보여 줄 것은 「방금 고른 것」이다

### 이름은 유일하다 (서버가 정한다)

**이름은 신원이 아니다.** 신원은 `playerId` 가 맡는다. 이름은 사람이 서로를 알아보는
딱지일 뿐이고, 세이브를 읽고 쓰는 열쇠는 여전히 `playerId` + `secret` 이다.

`playerId` **를 다른 것으로 바꾸지 말 것.** 복구 코드가 여기에 묶여 있어서, 새 UID 를
발급하면 기존 플레이어의 세이브 소유자와 복구 코드를 전부 갈아엎어야 한다.
이메일 로그인을 붙일 때도 계정에 `playerId` 를 연결하기만 하면 된다.

이름을 다루는 코드를 고칠 때 지킬 것:

1. **이미 남이 쓰는 이름(409)은 그 자리에서 막는다.** 다른 이름을 지으면 되는 일이다.
   **서버에 닿지 못한 경우는 막지 않는다** — 튜토리얼 마지막 관문에서 오프라인이라는 이유로
   게임을 시작조차 못 하게 하는 편이 훨씬 나쁘다. `nameClaimed=false` 인 임시 이름으로
   진행시키고, 서버에 닿았을 때 `ensureNameClaimed()` 가 확정한다.
   그사이 남이 가져갔으면 이름만 비우고 다시 짓게 한다 — **진행은 건드리지 않는다**
2. **'조회한 다음 쓰기' 로 유일성을 판정하지 않는다.** 그 사이에 남이 채 간다.
   Postgres 는 `name_key` 유일 인덱스 위반(`23505`)으로, 파일·메모리 저장소는
   검사와 쓰기 사이에 `await` 를 두지 않는 것으로 막는다 (노드는 단일 스레드다)
3. **같은 요청이 여러 번 와도 안전해야 한다.** 응답을 못 받고 재시도하는 일이 실제로 생긴다.
   같은 `playerId` 가 같은 이름을 다시 보내면 성공으로 친다
4. **대소문자만 다른 이름은 같은 이름이다.** 비교용 `name_key` 만 접고 표시용 원본은 남긴다
5. **저장이 예약된 이름을 덮어쓰지 않는다.** 보내온 이름이 남의 것이면 이름만 빼고 저장한다 —
   이름 때문에 진행을 잃는 것보다 낫다
6. 이름 규칙(`NAME_ALLOW` · `NAME_MAX_W`)은 `game.js` 와 `server/index.js` **양쪽에 있다.**
   클라이언트 검사는 안내용이고 서버가 다시 본다. 바꾸면 둘 다 고쳐야 한다
7. **재시도를 '이미 하는 중' 이라고 버리지 않는다.** `ensureNameClaimed()` 는 진행 중인
   시도가 있으면 그 결과를 같이 기다린다(`_claimTask`). 그냥 `return` 하면 그 한 번이
   실패했을 때 아무도 다시 시도하지 않아 **임시 이름이 영영 임시로 남는다** (실제로 겪었다)

### DB 컬럼은 state 의 사본이다

세이브 본체는 `state`(JSONB) 하나다. 그중 **이름·매력 총합·플레이 시간**만 조회용 컬럼으로
따로 뺐다 — 랭킹·통계를 낼 때 세이브를 전부 파싱하지 않으려는 것이다.

- 매력 총합은 크리처 보너스까지 더해야 해서 게임 규칙을 아는 **클라이언트가 `meta.charm` 으로 보낸다.**
  `totalCharm()` 을 고치면 컬럼 값의 의미도 같이 바뀐다. `meta` 가 없으면 서버가
  `state` 만으로 계산 가능한 값(비주얼+아우라)으로 떨어진다
- **컬럼을 늘리면 저장소 3곳(Postgres·파일·메모리)을 다 고쳐야 한다.** 하나만 고치면
  로컬 개발에서는 되는데 배포에서 안 되는(또는 그 반대) 일이 생긴다
- 운영 중인 테이블이므로 스키마는 `ADD COLUMN IF NOT EXISTS` 로만 붙인다.
  기존 행의 새 컬럼은 비어 있고, 그 플레이어가 다음에 저장할 때 채워진다

---

## 검사

```bash
npm install
npm start            # http://localhost:8080
npm test             # 생성 데이터 대조 + 번역 누락 + 인물·대사 표 + 서버 API 검사
npm run test:i18n    # 번역 누락만
npm run test:save    # 옛 세이브를 심어 마이그레이션 확인 (브라우저 필요)
npm run test:time    # 시계를 옮겨 놓고 포만감·스태미나·방치 감소 (브라우저 필요)
npm run test:tut     # 튜토리얼을 처음부터 끝까지 눌러 본다 · 지시문이 대상의 이름을 부르는가 (브라우저 필요)
npm run test:tuttext # 튜토리얼 지시문·대사의 글자 (npm test 가 이미 돌린다)
npm run test:melt    # 중복 크리처를 솥에 넣어 녹여 본다 (브라우저 필요)
npm run test:drink   # 물약을 꾹 눌러 연속으로 마셔 본다 · 「꿀꺽」이 한 병에 한 번인가 (브라우저 필요)
npm run test:emoji   # 다크에서 이모지가 «밝은 테마보다» 안 보이는 데가 없는가 (브라우저 필요)
npm run test:wardrobe # 옷장 칸을 눌러 «그 자리»에 머무는지 (브라우저 필요)
npm run test:farm    # 밭에서 거두고 이웃 밭을 털어 본다 (브라우저 + 서버를 스스로 띄운다)
npm run test:ask     # 키워드를 물어 마을 여덟을 열어 본다 (브라우저 필요)
npm run test:name    # 여왕의 이름이 화면에 새는지 (브라우저 필요 · 절반은 npm test 가 돈다)
npm run test:bond    # 물약을 선물해 호감도를 올려 본다 (브라우저 필요)
npm run test:code    # 복구 코드로 다른 기기에서 이어해 본다 (브라우저 + 서버를 스스로 띄운다)
npm run test:lore    # 흐린 장 — 저어서 재료를 알아내 본다 (브라우저 필요)
npm run test:page    # 비법서 «흘림» — 조합 네 번에 한 장이 오는가 (브라우저 필요)
npm run test:gather  # 히든 재료의 천장 — 연달아 헛걸음이면 반드시 나오는가 (브라우저 필요)
npm run test:story   # 2~5막 퀘스트를 처음부터 끝까지 걸어 본다 (브라우저 필요)
npm run test:balance # 수치끼리의 약속 (npm test 가 이미 돌린다)
npm run test:theme   # UI 색상 여섯 — 토큰 대비 표 + 여섯 × 화면 (브라우저 필요 · 절반은 npm test 가 돈다)
npm run sim:farm     # 밭·약탈 시뮬레이션 200명 × 30일 (수치를 바꿔 보는 도구)
npm run test:talk    # 키워드·대사 표를 걸어 본다 (npm test 가 이미 돌린다)
npm run test:data    # 생성 데이터 대조만 (npm test 가 이미 돌린다)
npm run test:walnut  # 호두밭 · 호두 게임을 진짜 마우스로 끌어 본다 (브라우저 필요)
npm run test:rock    # 바위산 · 돌깨기 게임을 진짜 마우스로 움직여 본다 (브라우저 필요)
npm run test:fish    # 낚시터 · 낚시를 진짜 마우스로 낚아 본다 (브라우저 필요)
npm run test:sparrow # 노을 밀밭 · 참새 쫓기를 진짜 마우스로 쏴 본다 (브라우저 필요)
npm run test:pinwheel # 바람개비 밭 · 퍼즐을 진짜로 풀어 본다 (브라우저 필요)
npm run test:driller # 소풍 바위 · 바위를 진짜로 파 본다 · 봇으로 속도를 잰다 (브라우저 필요)
npm run gen:maptype  # 채집지 51곳의 «형» (npm test 가 --check 로 돌린다)
npm run gen:logo     # 로고의 흰 배경을 빼낸다 (npm test 가 --check 로 «투명한가»를 본다)
npm run gen:wardrobe # 커스터마이징 150벌을 축 표에서 다시 뽑는다
```

**눈으로 본 것을 수치로 바꿔 주는 도구 셋도 있다.** 「둥글지 않다」·「안 맞는다」 같은
말은 사람마다 다른 것을 가리키므로, 고칠 자리를 한 곳으로 좁힐 때 쓴다.

```bash
npm run side      # 옆선을 줄마다 재서 «볼록 / 직선 / 오목»으로 찍는다
npm run neck      # 목 옆선 — 옷의 목둘레와 신체 목둘레를 «화면에 찍힌 것»으로 견준다
npm run bundle    # 게임을 파일 하나짜리 HTML 로 묶는다 (서버 없이 열어 보기)
```

옷을 늘렸으면 (`WARDROBE` 의 상의·하의·원피스):

```bash
node tools/checkavatar.js   # 체형 5단계 × 모든 옷에서 살이 옷 밖으로 나오는지
```

### 아바타의 살은 «부위마다 다르게» 찐다 (`FAT_W`)

몸무게(w · 0 날씬 ~ 1 통통)가 **몸 전체를 가로로 1.36배** 늘리던 것을 버렸다 — 그러면
통통한 몸이 「살찐 사람」이 아니라 **가로로 잡아 늘린 날씬한 사람**이다 (체형 다섯을
나란히 놓으면 실루엣이 옆으로 넓어질 뿐 «모양»이 안 바뀐다).

- 부위마다 몫이 다르다 — `avatar.js` 의 `FAT_W` (허리 .36 · 허벅지 .26 · 엉덩이 .24 ·
  팔 .18 · 종아리 .14). 어깨·발목·무릎은 거의 안 붙는다. 통째 배율(`bodyScaleX`)은
  어깨·가슴 몫 **1.12** 만 남았다
- **슬라이더 몫에 곱한다** (`fatOf = gainOf × weightK`). 슬라이더는 사람이 고른 체격,
  몸무게는 물약이 바꾸는 살 — 서로 다른 축이다. 졸업 직후 몸은 슬라이더 150% × 몸무게 1
- ⚠️ **몸무게를 `tuneOf` 에 섞지 않는다.** 세이브·화면의 % 는 사람이 고른 값 그대로여야 한다.
  대신 `build()` 가 `withWeight(tune, w)` 로 tune 에 실어 내려보내고, 살을 셈하는 자리
  (`fatOf` · `tuneMax` · `armK` · `hipSolve`)가 그것을 본다 — **옷도 `tuneMax` 를 지나므로
  저절로 따라온다.** 옷 쪽에서 굵기를 따로 셈하면 통통한 몸에서 살이 옷 밖으로 나온다
- **날씬(몸무게 0)은 한 픽셀도 안 바뀐다** — `checkavatar` 의 「상한 두께」·「슬라이더」가
  100%·날씬을 기준으로 재므로 거기서 잡힌다
- ⚠️ **팔은 0.28 로 시작했다가 0.18 로 내렸다** — 졸업 직후(150%)에는 이미 `TUNE_GAIN.arm`
  이 얹혀 있어(1.66배) 거기에 0.28 을 곱하면 소시지가 됐다. 그림을 보고 잡은 값이다
- ⚠️ **방의 크리처는 CSS 값이 아니라 그려진 치마 옆선을 재서 선다** (`game.js` 의
  `placePet()`). `right: 66.1px` 은 날씬한 몸에서 역산한 값이라 **통통한 새 플레이어의
  롱스커트 위에 19px 올라앉아 있었다** — 이번 일과 무관하게 원래 있던 버그인데
  `checkavatar` 의 「크리처 자리」가 `getBBox`(조각 «제» 좌표)로 재서 몸의 가로 변환을
  못 보고 한 번도 안 잡았다. 지금은 `getBoundingClientRect` 로 **체형 둘 다** 잰다.
  · **크기가 바뀌면 다시 잰다** — 좁은 화면에서 자리를 옮기면 `--pet` 도 바뀌어 겹치는
    높이 띠가 달라진다 (265px 의 어항이 커지며 엉덩이와 1.1px 겹쳤다). 물러나기만 하는
    되풀이라 반드시 멎는다. `placePet()` 을 끄면 19.9px × 여섯 폭으로 잡힌다
- ⚠️ **옷 검사는 «제일 넓은 몸»(슬라이더 전부 150 × 통통)에서도 돈다.** 그것이 곧 졸업
  직후 새 플레이어의 몸인데, `checkavatar` 는 배율을 **중간 체형에서만** 돌리고 있어서
  한 번도 안 쟀다 — 그 몸에서 **공주 드레스(시작 옷) 옆으로 엉덩이가 2px 나와 있었다.**
  · 커버리지·허리 검사가 양 끝 체형에서 「전부 150」을 같이 돌고, **「엉덩이·허벅지가 옷
    옆으로」**가 새로 생겼다 — 하의·원피스 전부 × 체형 0·1 × 기본·전부 150 에서, 맨몸의
    살색이 옷의 밑단 위에 남는가. 예전 「엉덩이가 하의 밖으로」는 하의만 · 날씬한 몸만 봤다
  · ⚠️ **허리 창에서는 팔·손을 지우고 잰다.** 몸통을 줄이면 팔이 창 안으로 들어오는데
    그것은 옷이 못 덮은 살이 아니다 — 안 지우면 몸통 50 에서 40쌍 전부가 「허리 살색」이다
  · ⚠️ **창의 위 끝은 몸의 세로 변환(`ky`)을 지난다.** 고정 y 로 두면 통통한 몸에서
    허리춤 위의 맨살 띠가 「하의 밖」으로 잡힌다 (실제로 그랬다)
  · **공주 드레스의 종은 허리·엉덩이 봉우리 높이에서 제 폭을 재서 모자란 만큼만 제어점을
    민다** (`sleevedDress` 의 `push`). ⚠️ **봉우리만 재면 안 된다** — 살이 허리에 제일 많이
    붙어서(`FAT_W.waist`) 통통 최대에서는 허리가 봉우리보다 넓고, 종은 위로 갈수록 좁다.
    안 모자라면 0 이라 날씬한 몸의 종은 한 글자도 안 바뀐다 (HEAD 와 path 를 대조했다)
- ⚠️ **엉덩이 슬라이더는 눈금의 절반이 몸에 들어간다** (`TUNE_SCALE.hip` 0.5 · 「지금
  엉덩이 50% 를 100% 라고」). 100% 아래의 엉덩이 폭은 `BODY.hipHalf` 가 아니라 **허벅지
  윗머리가 골반을 따라 들어오는 몫**(`HIP_PULL`)과 그것을 덮는 폭에서 나오므로, 상수를
  반으로 줄여서는 아무것도 안 바뀐다 — `hipHalf` 를 19 로 내려도 20~100% 가 그대로였다.
  그래서 `tuneOf` 에서 눈금 자체를 접는다. 새 20→150 은 옛 10→75 (봉우리 23.5 → 36 ·
  칸마다 살아 있다). 화면·세이브의 % 는 그대로고, `TUNE_GAIN.hip` 은 이제 닿지 않는다.
  `checkavatar` 「상한 두께」의 엉덩이 기대값이 1.12 → 1.16 이 된 것이 그 결과다.
  · ⚠️ **골반이 좁은 만큼 들어오는 몫(`hipPullK`)이 이제 기본 몸에도 걸린다** (0.75).
    허벅지 윗머리와 **무릎이 같은 값을 본다** — 무릎이 안 보면 허벅지 150% 에서 윗머리만
    들어오고 무릎은 남아 깔때기가 됐다 (「무릎이 허벅지를 따라가는가」가 잡았다).
    반대로 허벅지·종아리 50% 에서는 무릎이 **장딴지보다 가늘어질 수 없어서**(`kneeCalfCap`)
    몫이 44 → 59% 로 «오른다» — 그건 골반이 종아리를 못 줄이기 때문이라 구조적이다.
    그래서 그 검사는 **떨어지는 쪽만** 잡는다 (깔때기가 그쪽이다)
- ⚠️ **팔 100% 아래에서는 관절도 마디와 «같이» 가늘어진다** (`armWidths` 의 `jk`).
  「관절은 덜 붙는다」(`0.15+0.85k`)를 100% 아래에도 쓰면 팔 50% 가 어깨 5.7 · 팔꿈치 5.2 ·
  손목 4.7 의 **막대**가 된다 — 「팔을 낮추면 윗팔만 가늘어지고 아랫팔은 안 가늘어진다」로
  신고받았다. **소매 여유(`pad`)도 팔이 가늘어지면 같이 준다**(`padOf` · 바닥 절반) —
  3px 붙박이면 손목 반폭 2.2 에 3 이 얹혀 소매가 팔의 2.4배가 되고, 그 상수가 윗팔·아랫팔
  차이를 다 먹는다. 방 화면(공주 드레스)에서 보이는 것은 팔이 아니라 **소매**라 여기가
  자리였다. 100% 위는 한 글자도 안 바뀐다 (두 식이 100% 에서 같다)

### ⚠️ 옷이 덮어야 할 자리에 아무것도 없으면 **배경이 살색처럼 비친다**

몸통 윗선(`torsoTopY` 108)은 평평한데 머리는 그보다 위에 있어서, **목 양옆 — 턱과 어깨
사이 — 에 몸도 옷도 없는 띠**가 남는다. 방 벽이 베이지라 그 띠가 **살색 얼룩**으로 보였다
(「공주 옷 어깨 선이 살짝 어긋난다」로 신고받았다).

`avatar.js` 의 **`neckGusset`** 하나가 그 자리를 메운다 — 원피스가 있으면 그 색, 없으면
상의, 아무것도 없으면 살색. 윗변은 **턱선**(얼굴 타원의 아래 호를 몸통 좌표계로 옮긴 것)이고,
턱보다 `GUSSET_RISE` 위에서 자르며, 옆변은 어깨로 **흘러내리는 곡선**이다.
· ⚠️ 평평한 선으로 메우면 목에서 멀어질수록 턱이 위로 달아나 **4.5px 이 다시 비친다**
· ⚠️ 옆변을 네모로 떨구면 어깨 위에 **각진 선반**이 얹힌다 (그려 보고 되돌렸다)
· ⚠️ **옆변은 «가다가» 벌어져야 한다** (`GUSSET_FLOW` 0.5 — 제어점의 x 를 어깨 쪽으로
  민다). 제어점을 시작점과 «같은 x» 에 두면 접선이 수직이라, 띠가 어깨 바로 위까지
  같은 폭으로 내려가다 끝에서만 꺾인다. 띠의 폭은 머리를 따라 줄어드는데
  (`xc`·`half` 둘 다 `hk` 배) **높이는 거의 그대로라**(턱~어깨 24.8 → 28.4),
  바디파츠 최소에서 **띠가 «끈»이 된다** — 폭÷높이가 기본 0.293 → 최소 0.158 로
  반이 됐다 (「목 양옆의 초록 기둥이 이상하다」로 신고받았다). 도착 접선은 그대로
  수평이라 어깨와의 이음매는 안 바뀌고, x 는 세 점의 볼록껍질 안이라
  **어깨 끝(`sx`)을 넘을 수가 없다**
  · `checkavatar` 의 **「옷깃 띠」**가 잡는다 — **폭 자체로는 못 잡는다**(작은 몸에서
    좁은 것은 맞는 모양이다). 잡을 것은 «어디서 벌어지는가»라, 목 옆 띠(좌우 합)의
    폭이 **11줄 내려가며 늘어나는 배수**를 본다 (×1.5 이상 · 지금 1.60~6.00).
    ⚠️ **줄에서 옷 색의 «제일 바깥»을 재면 안 된다** — 어깨·소매가 같이 걸려 띠가
    아니라 드레스를 재게 된다 (그렇게 짰다가 0~59% 로 흔들렸다).
    ⚠️ **재는 줄을 11 에서 옮기지 말 것** — 16줄이면 수직으로 되돌려도 다 벌어져
    있어 «가르지 못하고», 8줄이면 멀쩡한 60% 가 걸린다. 사보타주로 확인했다
· ⚠️ 가운데는 `evenodd` 로 도로 판다 — 안 파면 목이 통째로 옷 색이 되어 폴라가 된다
· ⚠️ **어깨 끝을 넘으면 안 된다** (`neck.tip` ← `shoulderTipAbs`). 이 조각은 **목과
  마찬가지로 몸통 배율 그룹 «밖»**에 그려지는데, 바깥 끝을 `BODY.shoulderC`(배율이
  안 걸린 곡선)에서 바로 재고 있어서 **몸통을 어떻게 줄여도 늘 같은 자리**(32.57)에
  섰다. 바디파츠를 최소로 내리면 어깨는 반폭 15 로 좁아지는데 이 조각만 24.5 까지
  남아 **턱 밑에 좌우 10px 씩 뻗은 «날개»**가 생겼다 (「어깨 목 부분 오류」로 신고받았다).
  목이 `tipAbs` 로 이미 푼 문제라 **`shoulderTipAbs` 한 함수에서 둘이 같이 받는다** —
  두 벌로 두면 한쪽만 고쳤을 때 또 갈린다. 100% 위는 한 글자도 안 바뀐다
  (기본 어깨 136 > 받침 132.57 이라 한계에 안 닿는다 — 50·60% 에서만 걸린다)
· ⚠️ **실루엣으로 재면 못 잡는다** — 기본 체형에서는 머리 반폭이 36.5 라 32.57 짜리
  날개가 **머리카락에 통째로 숨는다.** `checkavatar` 의 **「옷깃 받침」**은 옷을 눈에
  띄는 색으로 물들여 **그 색만** 따라 재고(머리카락이 안 걸린다), 받침 꼭대기부터
  어깨 아래까지 **제 «바로 밑»보다 넓은 줄이 있는가**를 본다. ⚠️ 줄 번호를 박지
  않는다 — 몸통·체형을 바꾸면 어깨가 위아래로 옮겨 다녀 150% 에서 엉뚱한 데를 잰다
  (그렇게 짰다가 「옷을 못 찾았다」로 걸렸다). 몸통 6단계 × 체형 2 = 12조합을 다 쟀는지도
  같이 낸다. 사보타주로 확인했다 (한계를 빼면 50%·60% 에서 +10 · +7 · +5px)
· ⚠️ **높이는 px 가 아니라 «얼굴에 대한 비율»이다** (`GUSSET_RISE_K` = 5/34).
  5px 으로 박아 두었더니 **머리가 작아질수록 이 조각이 상대적으로 넓어졌다** — 턱은
  타원의 «맨 아래»라 5px 위의 현(弦) 폭이 `rx·√(1−((ry−5)/ry)²)` 인데, ry 가
  34(기본) → 17(최소)로 줄면 그 몫이 얼굴 반폭의 **0.52 → 0.71 배**가 된다.
  **맨몸에서는 이 조각도 살색이라 목과 한 덩어리로 보여서**, 바디파츠를 최소로
  내리면 **목이 굵어 보였다** (「목이 좀 두꺼운 것 같아」로 신고받았다).
  반대쪽도 틀려 있었다 — 150% 에서는 0.39 배라 **목이 가늘어 보였다** (아무도 신고를
  안 했을 뿐이다). 비율로 두면 어느 크기에서나 0.52 고, **기본(ry 34)에서 정확히
  5px 이라 100% 는 한 글자도 안 바뀐다** (그린 path 를 대조했다)
· ⚠️ **「목과 머리」는 이것을 못 잡는다** — 그 검사는 **얼굴만** 움직이고 나머지는
  1 로 고정한다. 바디파츠를 «다 같이» 내린 몸은 한 번도 안 쟀던 것이다.
  `checkavatar` 의 **「목과 몸」**이 여섯 단계를 다 같이 움직이며 **맨몸의**
  목/얼굴 비를 본다 (사람이 보는 것과 같은 조건이다 — 옷을 입히면 받침이 옷 색이 되어
  이 현상이 사라진다). ⚠️ 목은 **한 줄이 아니라 «평평한 구간»**으로 잰다 — 턱과 목이
  만나는 안티에일리어싱 한 줄을 집으면 작은 몸에서 7.5 대신 5.5 가 나온다.
  사보타주로 확인했다 (붙박이로 되돌리면 50% +37% · 60% +27% · 75% +16% · 150% −25%)
· ⚠️ **「목과 머리」는 «턱을 되짚지» 않는다 — 두 번 깨진 자리다.** 처음에는 「옷이 처음
  보이는 줄」로 턱을 잡았고(배율마다 105.5·126.5·153.5 로 널뛰었다), 다음에는 「받침이
  턱보다 `GUSSET_RISE` 위에서 시작한다」로 되짚었는데 **목깃을 없애자 그 전제가 깨졌다** —
  옷의 맨 윗줄이 이제 턱이 아니라 «옷의 제 어깨선»이라, 되짚은 자리가 살받침 한가운데로
  내려가 **목 대신 받침을 쟀다**(목 6.25 → 15.50). 게다가 배율마다 «고르게» 틀려서
  **통과로 찍혔다** — 0건이 통과가 아니던 자리다.
  지금은 「목과 몸」과 같은 방법이다: 턱 밑에서 **제일 좁은 줄**(세 줄의 가운뎃값)이 곧 목.
  · ⚠️ **아래 끝을 어깨선으로 막는다.** 여기서는 얼굴만 움직여 몸이 제자리라, 안 막으면
    어깨 반폭(≈36)을 목이라고 내놓는다 (「목과 몸」은 여섯이 같이 움직여 이 문제가 없다)
  · ⚠️ **맨몸으로 바꾸면 안 된다.** 몸통도 살색이라 얼굴을 줄이면 «가슴»이 얼굴보다
    넓어져 그쪽을 얼굴로 잡는다 (50·75% 가 통째로 건너뛰어졌다)
  · **새 잣대가 옛 그림에서 옛 값을 그대로 냈다** — main 에서 목 6.25 · 비 0.163 으로
    옛 검사기와 한 자리도 안 다르다. 잣대를 결과에 맞춘 것이 아님을 그것으로 확인했다.
    사보타주로도 확인했다 (`neckHalfOf` 에서 `hk` 를 떼면 +0.153 · +0.056 · −0.021)
· ⚠️ **맨몸일 때만 윗변을 목에 붙인다** (`NECK_HUG` 1.15 · `color === SKIN`).
  이 조각은 역할이 둘이라 **한 벌로 두면 반드시 한쪽이 틀린다**:
  · **옷을 입었으면 «깃»이다** — 턱선의 현을 끝까지 써야 한다. 좁히면 얼굴의 아래 호와
    어깨 사이에 **배경이 쐐기처럼** 남는다. (한때 두 경우 모두 좁혀 놓았는데
    `checkavatar` 가 통째로 통과했다 — 「옷깃 받침」은 받침이 어깨 «밖»으로 나온 것만
    보니까 **좁히는 쪽으로 틀리면 한 줄도 안 걸린다.** 실제로는 50~125% 에서
    좌우 3.3~6.7px 이 비치고 있었다)
  · **맨몸이면 «살»이다** — 목과 한 덩어리로 보여서, 얼굴 반폭의 0.52배가 목 옆에
    깔리면 **진짜 목(±4.5)이 ±8.4 짜리 기둥으로** 읽힌다 (「목이 좀 두꺼운 것 같아」로
    신고받았다). 그 바깥은 덮을 것이 **없는** 자리이고, 거기에 배경이 비치는 것이
    이 그림에서 맞는 모양이다 — 「턱밑 머리」가 오히려 그 틈을 4px 이상 요구한다
  가르는 줄은 **색 하나**다 (`clothColor` 는 입은 것이 없을 때만 `SKIN` 이다).
  옷을 입은 몸은 **그린 path 가 한 글자도 안 바뀐다** (여섯 배율에서 대조했다)
· ⚠️ **옷을 입었으면 이 자리를 «메우지 않는다».** 목 옆에 **무엇을 그리든 목이 굵어
  보인다** — 세 번 신고받고 세 판을 그려 보고 내린 결론이다:
  · 옷 색으로 메움 → 옷깃이 턱까지 올라와 기둥이 선다 (「초록 기둥이 얼굴까지 이어져
    올라온다」 · 「예전처럼 목깃이 없었으면」)
  · 위를 살색으로 덮음(`skinCap`) → 턱과 목이 한 덩어리가 되고, 조각이 어깨선 아래까지
    내려가 **옷의 제 넥라인까지 덮었다** (「목 두께에 비해 동그랗게 파인 부분이 너무 작다」)
  · 턱 밑 그늘색(`SKIN_SH`)으로 덮음 → 그래도 「옷을 입으면 양옆으로 목이 더 생긴다」
  그래서 **맨몸과 «똑같이» 둔다** — `86a649a`(받침이 생기기 전)와 같은 모양이고,
  목 옆에 배경이 비치는 것이 이 그림에서 맞는 모양이다 (「턱밑 머리」가 오히려 그 틈을
  4px 이상 요구한다). 옷은 **제 넥라인**(`NECK_CUT`)을 그대로 그린다.
  · ⚠️ **깃이 높은 옷(터틀넥·폴로)은 예전대로 메운다** — 거기서는 옷깃이 목까지
    올라오는 것이 맞는 모양이라, 안 메우면 옷과 목 사이로 배경이 비친다.
    판정은 **`NECK_CUT` 표 하나**다 (`pad == null` 이 곧 「목까지 올라온다」)
  · **검사 둘이 짝으로 지킨다** — 하나로는 셋을 다 못 잡는다:
    · 「목깃」 — 어깨선 바로 위 세 줄에서 **살이 끝나는 바로 그 픽셀이 옷인가**.
      **옷 색으로 메우는 판**을 잡는다 (0fb2f84 에서 8조합이 3/3)
    · 「맨몸↔옷」 — 어깨선 위에서 **옷을 입으면 턱 밑 살이 «더» 생기는가**.
      **살색·그늘색으로 덮는 판**을 잡는다 (둘 다 4.7~5.0px). ⚠️ **한쪽만 본다** —
      맨몸에는 받침이 살색으로 남아 있어(`NECK_HUG`) 옷 쪽이 조금 «가는» 것이
      지금 모양인데, 신고받은 것은 늘 「**더** 생긴다」였다
  · ⚠️ **「턱 밑 빈 자리」의 뜻이 바뀌었다** — 이제 **깃이 높은 옷에서만** 잰다.
    깃이 낮은 옷에서 그 틈은 «메울 것»이 아니라 **맞는 모양**이기 때문이다
· ⚠️ **좁히는 쪽의 사고는 `checkavatar` 의 「턱 밑 빈 자리」가 잡는다** — 옷 두 벌 ×
  바디파츠 여섯 단계에서 **양옆이 막힌 «투명한» 구간**을 직접 센다. 바깥 배경은 한쪽이
  그림 끝이라 저절로 빠지고, 얼굴·머리·옷 사이에 갇힌 구멍만 남는다 (지금 0.7px —
  안티에일리어싱이다). **맨몸은 안 잰다.** 사보타주로 확인했다 (옷에도 목에 붙이면 10건)
· ⚠️ **`neckHalfOf` 에 몸통 배율을 «또» 곱하지 않는다.** 어깨 몫에 `tuneOf(tune,'torso')`
  가 붙어 있어서, 바디파츠를 다 같이 내리면 얼굴 배율(`hk`)과 겹쳐 곱해져 **제곱으로**
  가늘어졌다 — 최소에서 −27% · 최대에서 +27% (「목 가운데 그림자가 가늘다」로 신고받았다).
  목은 **머리를 따라간다**: `hk` 하나만 곱하면 셋이 다 맞는다 — ① 몸 전체를 줄이면 목도
  «비례»해서 준다 ② 얼굴만 줄여도 목/머리 비가 그대로다 ③ 100% 는 한 글자도 안 바뀐다.
  대신 **몸통만 줄인 몸에서는 목이 안 가늘어진다** — 목은 어깨가 아니라 머리가 이고
  있는 것이라 그쪽이 맞는다 (물어보고 정했다). 「목과 몸」이 잡는다 (사보타주로 확인했다)
· ⚠️ **그 제곱 스케일을 펴자 «옷을 입은» 목이 최소에서 굵어졌다** (3.35 → 4.52 · +35%).
  맨몸은 받침을 목에 붙여 가늘어졌는데, **옷을 입으면 보이는 것은 받침이 아니라
  «목 자체»**라 반대로 갔다 — 「옷은 안 고쳐진 것 같다」로 신고받았다.
  `NECK_SLIM`(0.74)이 목 굵기를 통째로 줄인다: 최소 3.35(되돌림) · 기본 9.04 → 6.69.
  **한 곳이라 목·옷깃 입구(`neckCut`)·받침의 감싸는 폭(`NECK_HUG`)이 같이 따라온다**
· ⚠️ **「목과 몸」의 목 찾기를 «평평한 구간»으로 두지 않는다.** 받침의 옆변이 수직이라
  목이 «기둥»이었을 때만 맞는 방법이라, 옆변이 흐르게 고치자 **같은 값이 네 줄
  이어지는 자리가 사라져** 얼굴 폭(31.17)을 목이라고 내놓았다. 지금은 **세 줄의
  가운뎃값**으로 훑어 제일 좁은 곳을 잡는다 — 안티에일리어싱 한 줄도 그대로 걸러진다
· 규칙 전체는 **`ART_POLICY.md`** 의 「옷이 덮어야 할 자리에 아무것도 없으면 안 된다」

**⚠️ 이 조각을 손댈 때마다 검사기가 «같은 방식»으로 깨졌다 — 세 번이다.**
셋 다 **그림에서 턱이나 옷깃을 «되짚어» 자리를 잡고 있었기** 때문이다:
「옷이 처음 보이는 줄이 곧 턱」 → 받침이 생기면서 턱 옆을 옷깃으로 잡았고,
「받침이 턱보다 `GUSSET_RISE` 위」 → 목깃을 없애자 목 대신 살받침을 쟀고,
「옷이 처음 보이는 줄부터 어깨선까지」 → 그 구간이 통째로 비어 「잰 줄 0」이 됐다.

**되짚기를 쓰지 않는 잣대로 바꿔야 한다.** 지금 셋은 그렇게 돼 있다 —
「목과 머리」·「목과 몸」은 **제일 좁은 줄**을 찾고, 「목깃」은 **어깨선에서 되짚는다**
(`Avatar.CLOTH_TOP_Y` 는 상수라 그림이 바뀌어도 안 움직인다).
그리고 **몇 줄·몇 조합을 쟀는지를 늘 같이 낸다** — 0건이 「통과」가 아니라
「한 번도 안 쟀다」인 경우가 이 파일에서만 세 번 나왔다.

### 아바타의 「긴 생머리 · 기본」은 **인트로 공주를 그대로 옮긴 것**이다

같은 사람인데 튜토리얼을 막 마치고 마이 룸에 들어서면 다른 사람이 서 있었다.
**두 얼굴 타원을 포개는 배율 하나가 유일한 원본**이고, 뒤통수·옆 가닥·앞머리 바깥 호를
전부 이 식으로 옮겨 왔다:

```
x = 100 + (x공주 − 150) × 33/34      y = 70 + (y공주 − 180) × 35/33
```

- ⚠️ **뒤통수는 여섯이 다 같은 타원이다** (`CROWN_LONG`). 예전에는 긴 생머리만 이것을
  쓰고 나머지 다섯은 `crownOf(40, 42)` 였는데, 그 타원은 밑끝이 턱(y105)까지 내려와
  **턱 밑에서 목을 파고들었다** — 얼굴이 끝나는 자리 아래에도 머리가 남아 목 옆에
  브라운이 닿았다. 정수리는 앞머리(`BANG_OUT`)가 만드니 뒤통수가 낮아져도 안 납작해진다.
  · ⚠️ **그 높이(턱, y84~90)는 픽셀로 못 잰다** — 거기서는 긴 생머리의 옆 가닥이
    «맞게» 턱을 따라 내려와서, 같은 잣대로 재면 멀쩡한 머리가 2px 으로 걸린다.
    그래서 「턱밑 머리」는 턱보다 아래(y96~)만 보고, 이 사고는 **구조로** 막았다
- **앞머리는 다섯이다** — 기본 · 사이드뱅 · 커튼뱅 · 시스루뱅 · **웨이브**.
  ⚠️ 「올 백」을 뺀 자리다: 이름과 달리 이마를 드러낸 적이 없어(헤어라인 y44)
  「기본」(y43)과 사실상 같은 그림이었다. 대신 **처음에 있던 「기본 앞머리」**
  (밑단이 봉우리 셋으로 물결지는 것)를 되살려 **「웨이브」**로 부른다 — 한눈에 갈린다.
  · ⚠️ **두 축에 같은 낱말이 있으면 이름이 겹친다** — 뒷머리 「웨이브」 × 앞머리
    「웨이브」가 「웨이브 웨이브」가 됐다. 생성기가 같은 라벨이면 한 번만 쓴다
  · ⚠️ **id 여섯(`hair_*_none`)이 사라졌다.** 그걸 입고 있던 세이브는 `getItem` 의
    맨 앞(시작 머리)으로 떨어진다 — 진행은 안 잃지만 착장만 바뀐다.
    **id 를 없애는 것은 이번처럼 「그 벌이 다른 벌과 구별이 안 될 때」만 한다**
- ⚠️ **앞머리의 바깥 윤곽은 «머리의 꼭대기»다** — 앞머리의 모양이 아니다.
  스타일을 가르는 것은 안쪽 변(헤어라인)뿐이고, **꼭대기는 뒷머리가 정한다**:
  긴 생머리만 공주처럼 봉긋하고(`BANG_OUT` · y23.3) 나머지 다섯은 **동그랗다**
  (`BANG_FLAT` — 뒤통수와 «같은 중심·같은 rx» 에 ry 만 늘린 타원의 호 · 꼭대기 y26).
  · ⚠️ 낮은 쪽을 y30 으로 뒀더니 **머리가 납작해 보인다**고 신고받았고, 손으로 그린
    곡선으로 올렸더니 이번엔 관자놀이 위에 **각이 졌다**. 같은 타원이면 y=cy 에서
    폭도 접선도 정확히 맞아 이음매가 «없다» — 다섯에는 이 방식이 맞는다
  · ⚠️ **긴 생머리만 이 방식을 안 쓴다.** 거기서는 정수리가 «공주의 선»이어야 해서
    타원으로 두면 둥글고 넓어진다 (그렇게 했다가 신고받았다)
  · ⚠️ **뒤통수를 높여서 봉긋하게 만들지 않는다** — 그러면 정수리가 타원이 되어
    공주보다 둥글고 넓어진다 (그렇게 했다가 「예전으로 되돌아간 것 같다」로 신고받았다).
    공주의 «선»을 지키려면 앞머리 쪽이어야 한다
  「기본」만 공주의 선(y23.3)으로 올리고 나머지 넷을 y29~30 에 두었더니, 긴 생머리의
  뒤통수(꼭대기 y29.7)가 혼자서는 정수리를 못 만들어 **앞머리를 바꾸는 순간 머리 위가
  납작해졌다** — 화면에서는 「뒷머리가 사라졌다」로 보였다.
  `checkavatar` 의 **「머리 꼭대기」**가 둘을 본다 — ① 같은 뒷머리 안에서 앞머리를
  바꿔도 꼭대기가 그대로인가 ② 긴 생머리만 봉긋한가. 사보타주 둘로 확인했다.
  ⚠️ **관자놀이(x70~82)에서 잰다** — 가운데를 재면 올림머리의 «매듭»이 걸려
  그 머리만 늘 제일 높게 나온다 (매듭은 머리통이 아니라 얹은 것이다)
- ⚠️ **밑단을 «톱니»나 «뾰족한 V» 로 파지 않는다** (커튼뱅 · 시스루뱅 · 기본 앞머리).
  `Q` 로 판 삼각 이빨은 골이 바늘처럼 서고, 가르마를 V 로 모으면 끝이 찌른다.
  **`S` 로 이어 붙이면** 앞 구간의 접선이 그대로 이어져 봉우리도 골도 둥글어지고,
  가르마 끝은 접선을 «수평»으로 눕혀 U 로 만든다
- **볼터치도 인트로 공주의 것이다** (`#ff9db4` · opacity 0.6 · rx8.25 ry5.52).
  ⚠️ **세로는 배율로 옮기지 않고 «입에서» 잰다**(`MY−2.5`) — 배율대로면 y76 이라
  눈 뒤로 들어간다. 가로도 마찬가지로 **얼굴 옆선에서 남는 여백**을 공주와 맞춰
  81/119 다 (배율대로 77.7 이면 얼굴 밖으로 나간다)
- ⚠️ **표정에 물음표(?)와 음표(♪)를 안 쓴다.** 나머지 기호(땀·💢·반짝·💤·눈물…)는
  «상태»를 그리는데 그 둘만 **말풍선처럼 «말»을 하고 있어서**, 얼굴이 짓는 표정이
  아니라 얹어 놓은 자막으로 읽혔다. 쓰던 여섯(다정·미심쩍음·활짝·궁금·해피·어리둥절)은
  이제 눈·눈썹·입만으로 간다. **다시 넣지 말 것 — 지웠다가 되돌린 자리다**
- ⚠️ **흰자위의 검은 테두리는 눈꺼풀 선이 있는 셋에만 있다** (토라짐 `jito` ·
  흘김 `jside` · 노려봄 `glare`). 나머지는 지웠다 — 눈이 커 보이고 부드러워진다.
  셋에 남긴 이유는 위에 굵은 눈꺼풀 선이 얹혀 있어서, 테두리를 지우면
  **눈 아래쪽 윤곽이 사라져 흰자위가 볼로 번지기** 때문이다 (미리 그려 보고 골랐다)
- ⚠️ **「활짝」은 `exp_happy` 다.** `exp_haha` 는 「박장대소」다 — 이름이 비슷해서
  그쪽을 고쳐 놓고 「안 고쳐졌다」는 말을 두 번 들었다. **id 가 아니라 «이름»으로 확인할 것**
- ⚠️ **눈썹이 표정의 «뜻»을 뒤집는다 — 「기대」(`star`)에는 눈썹을 안 붙인다.**
  `b: 'up'` 을 달아 두었더니 공주·요정의 눈썹은 각이 커서(`pBrow('up')` 은 안쪽 끝을
  4px 꺾는다) **「미심쩍음」(`doubt`)과 똑같은 눈썹**이 됐고, 반짝이는 눈 위에 얹히니
  기대가 아니라 «능글맞은» 얼굴로 읽혔다 (「우와! 맛있겠다!」 하는 줄에서 신고받았다).
  · **기준은 아바타의 「기대」(`exp_star`)다** — 눈썹 없이 반짝임과 홍조로 그린다.
    같은 이름이 세 곳(아바타 · NPC 초상화 · 공주/요정 그림)에 있으니 **어느 것이 맞는
    얼굴인지는 이미 그려져 있는 쪽을 본다**
  · ⚠️ **공주(`PZ_FACE`)와 요정 대모(`FZ_FACE`)는 나란한 한 벌이다** — 한쪽만 고치면
    같은 이름이 두 얼굴이 된다. NPC 초상화(`portrait.js`)의 `BROW.up` 은 훨씬 완만해
    거기서는 문제가 안 났다 (재 보고 그대로 뒀다)
  · ⚠️ **이 종류는 자동 검사가 못 잡는다** — 「없는 표정」은 `checkdata` 가 잡지만
    「뜻이 안 맞는 얼굴」은 사람이 그려 보고 고르는 수밖에 없다. 표정을 손대면
    **그 이름과 이웃한 이름을 나란히 그려 놓고** 본다 (기대 ↔ 미심쩍음이 그랬다)
- ⚠️ **「수줍음」(`shy`)과 「부드러움」(`soft`)은 «입만» 다르다** (`princessFace`).
  눈(감은 호)·홍조·땀은 한 벌이고 입이 갈린다 — `shy` 는 **작은 ω**(`pMouth.w`),
  `soft` 는 **아래로 처진 한 줄**이다. 예전에는 둘이 한 그림이라 컷씬의
  「하지만 저는 드릴 수 있는 게 없는데…요…」에 처진 입이 나와 **시무룩해 보였다.**
  · **기준은 «아바타의 표정 목록»이다** — 「수줍음」(`exp_shy`)이 곧 그 얼굴이고,
    ω 입도 `avatar.js` 의 `MOUTH.w` 수치를 그대로 옮긴 것이다.
    같은 이름이 두 얼굴이 되면 안 된다
  · **볼의 세로 빗금**(`pBlushLines` ← 아바타의 `FX.blush`)은 **아바타에서
    `fx: 'blush'` 가 붙은 이름에만** 얹는다 — 「수줍음」과 **「부끄러움」** 둘뿐이다.
    홍조가 있다고 다 붙이면 「다정」·「기대」·「박장대소」까지 부끄러워한다
    · ⚠️ **px 가 아니라 «볼 반지름에 대한 비율»이다**(`BL`). 홍조가 두 크기로
      그려지기 때문이다 — 손그림은 rx 8 · ry 5, 부품 표(`pBlush`)는 rx 8.5 · ry 5.2.
      px 로 박으면 **큰 볼에서만 안쪽으로 몰린다**
    · ⚠️ **바깥 몫(`BL.OUT`)은 아바타 값(0.48)을 그대로 쓰면 안 된다.** 그러면
      제일 바깥 빗금이 볼 밖으로 나가 컷씬 크기에서 «흘린 자국»처럼 보인다
      (신고받았다). 셋이 다 볼 «안»에 드는 한계가 **0.34** 라 그 안쪽인 0.325 다 —
      한계는 「빗금 반높이(`H·ry`)가 그 x 에서의 타원 높이보다 작다」에서 나온다
  · ⚠️ **눈썹을 붙이지 않는다 — 붙였다가 되돌린 자리다.** 나는 `exp_shy` 의
    «표 값»(`b: 'sad'`)만 보고 「팔자 눈썹이 있다」고 판단했는데, **그려 보니
    화면에서는 눈썹이 안 보인다.** 표가 아니라 **그림**이 기준이다
  · ⚠️ **인트로 3·4는 `soft` 다** — 민망해하는 장면이라 «처진 입»이 맞는 얼굴이다.
    `shy` 로 두면 웃는 얼굴이 된다
  · ⚠️ **신고가 「A 로 바꿔 줘」인데 이미 A 이면, 그림을 고치기 «전»에
    「어느 A 를 말하는지」부터 확인한다.** 여기서는 그러지 않아 **두 번 고치고
    한 번 통째로 되돌렸다** — 사용자가 이미지를 보내 준 뒤에야 갈렸다
- ⚠️ **팔자(八) 눈썹은 좌우 대칭이 아니다 — 알고 있지만 «아직 안 고쳤다».**
  `pBrow`·`fBrow` 는 왼쪽에 `f=+1` · 오른쪽에 `f=-1` 을 넘겨 두 짝을 거울로 만드는데,
  `sad`·`angry`·`droop` 의 식은 **거울이 아니다**: 한쪽은 거의 평평하고 **다른 쪽만
  가파른 빗금**이 된다. 「걱정」·「서운함」·「대노」·「지침」이 다 한쪽으로 기운 얼굴이다.
  고치면 그 표정들의 «세기»가 같이 바뀌므로 **사람이 보고 정할 일**이라
  `HANDOFF.md` 「남은 작업」에 적어 뒀다 (고치는 식과 검사까지)
- ⚠️ **「활짝」 하나만은 좌표를 직접 쓴다.** 그 표정이 곧 인트로 공주의 웃는 얼굴이라
  「거의 같다」로는 안 된다는 요청을 두 번 받았다. 아바타의 눈·입은 공주보다 8~10px
  아래에 붙어 있어서(`AV`), **부품만 같게 해서는 「배치가 다른 얼굴」이 된다.**
  공주의 세 줄(눈 둘 · 입)과 볼터치를 배율로 옮긴 값을 그대로 쓴다.
  **다른 표정에 이 방식을 퍼뜨리지 않는다** — 나머지 서른일곱은 `AV` 를 지난다
- ⚠️ **눈·입의 자리는 `AV` 다섯 숫자에서만 나온다** (L·R·Y·MX·MY). 표정 서른여덟이
  거기서 좌표를 받아 쓰므로 한 줄만 고치면 전부 따라온다 —
  **표정 쪽에 좌표를 박지 않는다.** 손으로 그린 여섯(윙크·해피·놀람·어리둥절·시크·기본)이
  87/113/89 를 박아 두고 있어서, 자리를 옮겼을 때 **그 여섯만 옛 자리에 남았다**.
  그중 「어리둥절」이 기본 얼굴이라 마이 룸에서 그대로 보였다.
  (자리 자체는 옮겼다가 **되돌렸다** — 지금 값이 원래 값이다. 여섯이 `AV` 를 지나게
  고친 것만 남겼고, 두 판을 그려 대조해 **롤백 전과 한 글자도 안 달라진 것**을 확인했다)
- ⚠️ **아바타에는 코도 없다.** 콧등의 그늘을 선 하나로 옅게 찍어 두었는데
  공주에게는 코가 없다 (`intro.js` 의 `princessFace` — 눈과 입뿐이다).
  코는 `head()` 한 곳에서 나오므로 거기서 빠지면 **표정 서른여덟이 한꺼번에** 따라온다 —
  표정 쪽에 그리기 시작하면 새 표정을 만들 때마다 빠뜨리게 된다
- ⚠️ **아바타에는 귀가 없다.** 예전에는 얼굴 옆에 `cx67·133 rx6 ry9` 로 붙어 있었는데
  공주에게는 없어서, 머리를 아무리 맞춰도 **볼 옆에 살색 혹 두 개**가 남았다.
  게다가 그 혹 때문에 얼굴 반폭이 33 이 아니라 39 로 잡혀 **머리를 공주보다 넓게
  그릴 수밖에 없었다**(뒤통수 44.7) — 머리만 커져서 더 달라 보였다. 귀를 빼면 둘 다 풀린다
- ⚠️ **옆 가닥은 얼굴 «뒤»에 둔다.** 앞으로 보내면 안쪽 변이 얼굴 안으로 15px 들어와
  있어서 눈과 볼을 덮는다 (공주도 뒤에 두고 겹치는 부분을 얼굴로 가린다)
- ⚠️ **앞머리도 공주의 선을 그대로 쓴다.** 「이음매가 수학적으로 맞게」 뒤통수와 같은
  타원의 호로 바꿨다가 되돌렸다 — 정수리가 공주보다 둥글고 넓어져 **「예전(뒤통수 44.7)
  으로 되돌아간 것 같다」**는 신고를 받았다. 이 얼굴에서 맞는 것은 «정확한 이음매»가
  아니라 «공주와 같은 선»이다
- ⚠️ **헤어라인만은 공주 값을 안 쓴다**(40.3 이 아니라 43). 40.3 에 두면
  `checkavatar` 의 「정수리에 살색」이 잡는데, 그 검사는 사이드뱅이 얼굴 꼭대기까지
  못 올라오던 것과 커튼뱅 가르마가 정수리에서 열리던 것을 **둘 다 실제로 잡아낸다**
  (사보타주로 확인했다). 「세로줄마다 머리가 먼저 나오는가」로 바꿔 보았는데
  **그 둘을 하나도 못 잡아서** 되돌렸다 — 뒤통수가 정수리를 덮고 있어서다.
  아바타는 공주보다 얼굴이 세로로 길어 이마가 원래 더 넓으므로 3px 은 그 몫이다.
  ⚠️ 한때 46.5 까지 내렸다가(「이마가 넓다」) 인트로와 어긋나 되돌렸다 —
  **둘을 동시에 만족시킬 수는 없다**
- ⚠️ **턱 밑에 «머리카락을 채워 넣지 않는다».** 목이 좁아지는 자리라 가닥과 목 사이로
  배경이 7~8px 비치는데, 그걸 「틈」으로 보고 가닥을 목 쪽으로 밀어 메웠다가
  **머리카락이 턱과 목 사이에 낀 것처럼 보인다**는 신고를 받았다.
  · 그 자리는 **원래도 더 넓게(23px) 비어 있었고** 아무도 문제 삼지 않았다 —
    공주는 드레스 깃이 높아 덮여 있을 뿐이다. 배경이 비치는 것이 맞는 모양이다
  · `checkavatar` 의 **「턱밑 머리」**가 이제 반대로 잰다 — 목 옆선에서 머리카락까지
    최소 4px. **재는 법을 두 번 고쳤다**: 「몇 px 떨어졌나」로만 재면 «닿았을» 때
    목과 머리가 한 덩어리가 되어 「머리카락이 아예 없다」로 통과하고, 「가운데 덩어리에
    머리색이 있나」로 재면 볼(얼굴)과 어깨가 걸린다. 지금은 **머리색이 아닌 칠만 따라
    걸어** 목의 옆선을 찾고, 그 덩어리가 **목일 때만** 본다
  · ⚠️ **맨몸에서 잰다. 옷을 입히면 이 검사는 아무것도 못 본다** — 옷을 입으면
    목 양옆의 받침(`neckGusset`)이 옷 색이라 「머리색이 아닌 칠」을 따라 걷는 순간
    **목·받침·어깨가 한 덩어리**(반폭 18~35px)가 되고, 「목만 할 때만 본다」에 다
    걸려 **열다섯 줄이 전부 건너뛰어진다.** 받침이 생긴 날부터 여섯 머리가 모두
    「없음」인 채로 **한 번도 안 재고 통과**하고 있었다 (`git` 으로 다섯 커밋을
    거슬러 돌려 봐도 전부 「잰 줄 0」이었다). 사람이 이 현상을 보는 조건도 맨몸이다
  · ⚠️ **«목»의 경계를 px 상수로 박지 않는다** (예전에는 반폭 15px). 턱 밑은 목에서
    어깨로 **퍼져 나가는** 구간이라, 상수를 넉넉히 잡으면 이미 어깨가 된 줄까지
    「목」으로 센다 — 거기서는 어깨가 머리카락에 **닿는 것이 맞는데**(한 줄 뒤에서
    실제로 닿는다) 틈이 좁다고 잡혔다 (반폭 10 → 14 로 퍼지는 동안 틈이 6 → 3px).
    창 안에서 **제일 가는 줄**이 곧 목이고, 거기서 `NECK_SPREAD`(2px) 안에 드는
    줄만 본다 — 그림에서 바로 뽑으니 몸이 바뀌어도 따라온다
  · **몇 줄을 쟀는지를 머리마다 낸다**(`long 4px(10줄)` · `잰 줄 60`). 「없음」 자체는
    잘못이 아니다 — 올림머리·포니테일은 목 옆에 가닥이 «원래» 없다. 잘못은
    **한 줄도 못 찾은 것**이고, 그때만 실패시킨다 (그 한 줄이 없어서 위의 구멍이
    오래 살아 있었다). 사보타주 넷으로 확인했다 — 옷을 입은 채로 재기 · 가닥을
    6px 밀기(붙음) · 2px 만 밀기(3px) · 창을 어깨까지 넓히기(옛 오진이 되살아난다)
- ⚠️ **부츠 목은 «종아리 모양»이다** (`calfPts` → `limbPath`). 폭 18짜리 네모 기둥이라
  종아리 150%(반폭 25.5)에서는 **다리가 부츠 밖으로 삐져나왔다**.
  `limbPath` 를 부츠 색으로 한 벌 더 그리고 **위를 네모로 잘라 내는 것**이 곧 부츠 입구다 —
  「재는 곡선과 그리는 곡선이 같아야 한다」(`thighOuterAt`)를 그대로 지킨 것이라,
  다리 배율을 어떻게 바꿔도 한쪽만 따라올 수가 없다.
  · **안쪽 변은 안 넓힌다**(`pad` 는 바깥에만). 다리와 같은 `innerX` 를 쓰므로 그 변에서는
    살이 비칠 수가 없고, 넓히면 두 짝이 다리 사이에서 겹쳐 한 덩어리가 된다
  · **입구 띠도 같은 폭을 따라간다.** 폭을 박아 두면 굵은 다리에서 띠만 짧아진다 —
    단 **입구가 «발 위»에 있을 때만이다**(`openUp`). 목이 5인 「로우」 다섯은 입구가
    발 타원(위 끝 `FY−7.6`) 안에 들어가서, 거기서 종아리 폭으로 그리면 **발보다
    안쪽으로 치우친 덩어리**가 얹힌다 (발은 발목 한가운데보다 6px 바깥이다 — `footX`).
    그때는 발 위에 곧은 선 하나로 긋는다. 발 타원은 배율을 안 타므로 폭을 박아도 된다.
    ⚠️ 띠를 **발보다 먼저** 그리면 로우는 통째로 가려져 **플랫과 구별이 안 된다**
    (그렇게 만들었다가 눈으로 보고 되돌렸다 — 살색 검사로는 안 잡힌다)
- ⚠️ **굽을 신으면 발이 앞으로 기울어 «세로로 긴» 타원이 된다** (`footShape`).
  굽 없이 선 발은 위에서 내려다보는 각이라 좌우로 넓은 타원인데, 하이힐은 발등이
  **사선으로** 떨어져서 앞에서 보면 길이가 그대로 보인다 — 좌우는 좁고 위아래가 길다
  (「하이힐인데 발등이 여전히 플랫하다」로 신고받았다).
  · **맨발(`legs`)과 신발(`renderShoes`)이 같은 함수를 지난다** — 한쪽만 바꾸면 맨발이
    신발 밖으로 삐져나온다 (종아리·부츠 목에서 두 번 겪은 것과 같은 사고다)
  · ⚠️ **하이힐은 유리구두 하나뿐이다** — 부츠(로우·앵클·롱부츠 광택)는 하이힐이 아니다.
    마감(광택)에 굽을 걸었다가 그 셋까지 하이힐이 되어 **되돌렸다.**
    그리는 쪽에도 `pitchOf` 가 목이 있으면 0 을 돌려주는 빗장이 남아 있는데,
    목이 있는 신발에 굽을 붙이면 **목의 안쪽을 좁아진 발이 못 덮어 배경이 쐐기처럼
    비친다** (그려 보고 알았다). 표 쪽은 `checkavatar` 가 막는다
  · **기울어진 발은 타원이 바닥까지 내려가 그것이 곧 앞코다** — 앞코를 따로 안 댄다.
    밑창은 **신발로 오려 낸다**(clipPath): 타원을 하나 더 얹으면 아래로 갈수록 급히
    좁아지는 자리라 어느 높이에서는 반드시 앞코 옆으로 삐져나온다
  · ⚠️ **기울어진 발은 발목 «밑»으로 모인다** (`FOOT_OUT_HEEL` 2 · 납작한 발은 6).
    좁아진 발(반폭 7.8)을 6px 이나 바깥에 둔 채로 두었더니 **발목 안쪽 밑에 배경이
    쐐기처럼 남아** 옆선이 y331 에서 104 → 109 로 툭 튀었다 —
    「발목이랑 발이 끊어져 있다」로 신고받았다. 굽을 신으면 발이 몸 밑으로 모이기도 한다.
    `checkavatar` 의 **「발목↔발」**이 스무 켤레 × 종아리 100·150% 에서
    **실루엣의 «안쪽» 옆선이 튀는 폭**을 잰다 (3px 까지 · 지금 최대 1px).
    ⚠️ 바깥 변은 안 잰다 (발은 발목보다 넓어 원래 벌어진다) · 발끝도 안 잰다
    (앞코가 오므라들며 안쪽도 같이 들어온다) — **이음매 구간 y324~336 만** 본다
  · `checkavatar` 의 「굽」이 **신발 상자(가로×세로)**를 재서 세로가 더 긴지 본다.
    ⚠️ **이 줄이 없으면 기울임을 통째로 지워도 통과한다** (사보타주로 확인했다 — 25×21)
  · ⚠️ **바닥에 닿았는지를 «색»으로 재지 않는다.** 펌프스가 바닥에 대는 것은 몸통보다
    어두운 «밑창»이라, 몸통 색을 찾던 옛 검사가 멀쩡한 그림을 「앞코가 떴다」로 잡았다.
    지금은 **바닥 언저리에서 한 발의 폭**을 잰다 (굽만 닿으면 2~3px · 앞코까지 닿으면 10px 넘는다)
- ⚠️ **목이 없는 구두는 발등이 드러난다** (`INSTEP_W`·`INSTEP_D`·`INSTEP_G` —
  신발 타원의 «비율»이다). 신발 타원 하나로 두면 발등 자리까지 통째로 신발이라
  「납작한 덩어리」로 보인다 — 신고받은 자리다. ⚠️ **px 로 박지 않는다**: 굽이 있으면
  타원이 통째로 달라져서 굽 있는 구두에서만 어긋난다. 밑변은 **3차 곡선**이다 —
  2차(`Q`)는 가운데만 뾰족하게 처져 렌즈 모양이 되고, 3차라야 가운데가 평평하게
  눌려 신발의 앞코 선처럼 보인다.
  · ⚠️ **입구를 얕고 넓게 두면 「납작한 뚜껑」이 된다** (「발등 곡선을 다듬어 달라」).
    고친 것은 **W 를 좁히고(.72→.68) D 를 깊게 한 것**(.60→.62)이다 —
    모서리가 타원의 더 아래로 내려가 옆에 신발의 «옆면»이 남고, 가운데가 더 파여
    밑변이 입구 선으로 읽힌다. **깊이÷폭 0.120 → 0.152.**
  · ⚠️ **모서리의 «각»이 원인이 아니었다.** 뿔처럼 보여서 각인 줄 알고 2차로
    굴려도 봤는데 면이 깎여 «각진 탭»이 됐고, 재 보니 지금 쪽이 오히려 더
    날카롭다(60° → 54°). 밑변의 «생김새»(`INSTEP_G`)도 정규화하면 거의 안 달라진다.
    **짐작으로 두 번 헛짚은 자리라, 고치기 전에 셋을 하나씩 떼어 그려 보고 잰다**
  · `checkavatar` 의 **「발등 깊이÷폭」**이 0.135 를 지킨다. ⚠️ 처음에 쓰려던
    「밑변이 얼마나 파였나」는 옛 모양 0.50 ↔ 지금 0.58 밖에 안 갈려서 버렸다 —
    **가르지 못하는 잣대는 무슨 값을 넣어도 통과한다.** 옛 값(W.72 D.60)으로
    되돌리는 사보타주로 확인했다 (평발 넷이 잡힌다).
  · ⚠️ **칠하는 색은 `SKIN`(다리)이 아니라 `SKIN_SH`(발)다.** 다리 색으로 칠했더니
    발 위에 «다른 색 띠»를 얹어 놓은 꼴이었다 — 드러나는 것은 다리가 아니라 발등이고,
    맨발도 그 색이라 그래야 한 몸으로 읽힌다
  · ⚠️ **구멍으로 뚫지 않고 덮는다.** 신발 타원(rx13)이 발 타원(rx12)보다 늘
    1~1.8px 넓어서, 뚫으면 그 사이로 **배경이 실오라기처럼 비친다**
  · ⚠️ **입구는 발 한가운데에 «대칭»으로 판다.** 발목 밑으로 옮겨 봤더니 바깥쪽 윤곽만
    남아 신발이 기울어 보였다 — 발이 발목보다 6px 바깥이라서다(`FOOT_OUT`).
    맨발도 그만큼 나가 있으니 발을 따르는 쪽이 이 그림의 결이다
  · ⚠️ **스트랩은 입구의 «아래쪽 3분의 2»를 지난다.** 한가운데에 두면 2px 짜리 선이
    입구를 거의 다 덮어 발등이 안 보인다 — `checkavatar` 가 실제로 잡았다
  · `checkavatar` 의 **「발등」**이 스무 켤레를 다 본다 — 목이 없는 다섯은 발색이
    드러나는가, 목이 있는 열다섯은 한 점도 안 드러나는가. **양쪽을 몇 켤레 쟀는지도
    같이 낸다** (한쪽이 0이면 그 방향은 아예 안 잰 것이다).
    사보타주 둘로 확인했다 (안 파면 5건 · 목이 있는 것까지 파면 15건)
- ⚠️ **굽(하이힐)은 발을 «띄워서» 만들지 않는다.** 발 몸통을 위로 올리고 밑에 기둥을
  세웠더니 `legs()` 가 그려 둔 **맨발(살색 타원 `cy=footY`)이 신발 밑으로 드러났다** —
  구두를 신었는데 발가락이 나와 있는 꼴이다. 신발은 맨발을 덮는 자리에 그대로 두고
  **굽만 바닥 아래로 더 내려 긋는다** (굽을 신으면 실제로도 키가 커지니 결도 맞는다).
  · 굽은 **축이 아니라 «조합 하나»가 정한다** — `genwardrobe` 의 `HEEL` 표에
    `'flat/gloss': 5` 한 줄이 전부다 (유리구두).
    ⚠️ 처음에는 마감(광택)에 걸었는데 **로우·앵클·롱부츠 광택까지 하이힐이 되어
    「그것들은 하이힐이 아니다」로 되돌렸다.** 축 어느 쪽에도 안 걸리는 값이라
    조합 키로 적는 표를 따로 둔다 — 축 표는 깨끗하고 예외는 한 줄로 «보인다».
    ⚠️ **목이 있는 조합에는 못 붙인다** (`checkavatar` 가 표를 본다) — 그리는 쪽의
    「부츠에 앞코를 따로 대는」 갈래는 그때 같이 지웠다
  · ⚠️ **앞코도 굽과 «같이» 바닥에 닿아야 한다.** 굽만 닿게 그렸더니 발끝이 5px 떠서
    「그 발로는 걸을 수 없다」고 신고받았다. 앞코는 **바깥쪽**, 굽은 **안쪽**으로
    물려 사이에 아치가 보이게 한다 — 반대로 두면 안짱다리로 보인다
  · ⚠️ **그림 상자 밑(`VB` 348)이 코앞이다.** 굽을 3px 만 더 늘려도 끝이 잘리는데,
    SVG 는 viewBox 밖을 아예 안 그리므로 **잘린 것이 티가 안 난다.**
    `checkavatar` 의 「굽」이 넷을 본다 — ① 발보다 아래로 나오는가 ② 상자 밑에
    안 닿는가 ③ **바닥 줄까지 앞코(신발 몸통 색)가 내려오는가** ④ 종아리가 달라지는가.
    거기에 **굽 있는 구두가 하나라도 있는가**도 본다 — 축 표에서 지우면 목록이 비어
    아무것도 안 재고 통과하기 때문이다 (「로우를 건너뛰던」 것과 같은 구멍이다).
    사보타주 넷으로 확인했다
- ⚠️ **굽을 신으면 종아리 모양이 달라진다** (`calfPts(tune, heel)`). 발끝으로 서는
  셈이라 장딴지가 당겨져 **위로 올라가고 조금 더 불룩해진다** — 하이힐을 신은 다리가
  예뻐 보이는 이유가 그것이다. 굽 5px(유리구두)을 1인분으로 잡고 그만큼 태운다.
  · **다리와 부츠 목이 같은 함수를 지난다** — 굽을 한쪽만 반영하면 부츠가 종아리를
    못 덮는다 (「종아리 밖으로 벗어난다」와 같은 사고다). 그래서 굽 값은
    `heelOf(신발)` 한 곳에서 나오고 `build()` 가 둘에 같이 넘긴다
  · ⚠️ **발목은 안 건드린다** — 발(`footX`)이 발목에서 나오므로 여기서 좁히면
    신발만 제자리에 남아 발목과 어긋난다
- ⚠️ **부츠 목은 옷 밑단 위로 안 올라간다** (`legHemY`). 발목까지 오는 청바지
  (`hemY` 332) 위로 목이 올라와 **바짓단 한가운데에 색이 다른 기둥**이 섰다.
  · **신발을 하의보다 아래로 내려서 고치지 않는다** — 그러면 바지가 발을 통째로 삼켜
    맨발도 구두도 안 보인다. 신발은 하의보다 «위»에 두어 **발끝이 바짓단 밑으로**
    나오게 하고, **목만** 밑단에서 자른다 (짧은 하의·치마에서는 목이 그대로 다 보인다)
  · `hemY` 가 없는 것(공주 드레스)은 바닥까지 오는 옷이라 999 로 친다 —
    `crouch()` 의 `legHem` 과 같은 규칙이다
  · `checkavatar` 의 **「부츠」**가 셋을 본다 — 목이 있는 구두 15켤레 × 종아리
    50·100·150% 에서 ① 부츠 구간에 살색이 0px 인가 ② 바짓단 위에 부츠 색이 0px 인가
    ③ **입구 띠가 보이는가**(장식이 없는 `plain` 셋에서 두 색 이상).
    사보타주 셋으로 확인했다 (네모 기둥 99px · 밑단을 안 보면 604px · 띠를 발 밑에 두면
    로우가 한 색).
    · ⚠️ **구간의 양 끝을 잘라 내지 않는다.** 2px 씩 뺐더니 목이 5인 「로우」 다섯은
      구간이 뒤집혀(y331~329) **통째로 건너뛰었다** — 0px 이 「통과」가 아니라
      「한 번도 안 쟀다」였다. 그래서 **몇 켤레를 쟀는지(45/45)도 같이 낸다**

브라우저 동작은 Playwright 로 확인해 왔다. 눈으로 보면 놓치는 것들을
자동 검사가 실제로 잡아냈다 — 어깨가 드레스 밖으로 나온 것, 카드가 58px 넘친 것,
마법진 구멍이 겹친 것, 초기화한 세이브가 되살아난 것.

자동화로 `checkUI()` 를 돌릴 때: 예전에는 창이 숨겨지면 `requestAnimationFrame` 이 멈춰
**영영 끝나지 않았다.** 지금은 숨겨진 창에서 프레임을 기다리지 않고 바로 진행한다
(측정 상태는 `settle()` 이 맞춰 주므로 결과는 같다). 숨긴 채로도 0.1초 안에 끝난다.

대신 **창 크기는 살아 있어야 한다.** 패널이 접혀 폭이 0 이면 검증기가 잴 수 없다고 알리고
`pass:false` 를 낸다 — 그 상태의 0건은 통과가 아니다.

### 시간이 흐르는 값은 시계를 옮겨 놓고 검사한다

포만감·스태미나·방치 감소는 **지금 화면만 보면 전부 통과로 보인다.** 「하루 뒤」를
실제로 만들어 봐야 굶었을 때 스태미나가 차 버린다든지, 방치 감소가 부를 때마다
두 번 깎인다든지 하는 것이 드러난다.

`tools/checktime.js` 가 페이지 안에서 `Date.now` 를 갈아 끼운다.
그래서 **시간은 전부 `nowMs()` 한 곳을 지나야 한다** (`nowDate()` 도 그 밑이다) —
`Date.now()` 를 여기저기서 부르면 시계를 옮겨 놓을 구멍이 없어진다. 실제로 `dayKey()` 가
그래서 밤 판정을 못 재고 있었다.
**시간에 기대는 값을 새로 만들면 `nowMs()`/`nowDate()` 를 쓰고, 여기에 한 줄 늘린다.**

⚠️⚠️ **한때 이 약속이 «글로만» 있었다.** 주석에는 「전부 이 한 곳을 지난다」고 적혀
있었는데 실제로는 `Date.now()` 를 **스물다섯 곳**에서 직접 불렀고, 그중에 **몸의 시계**
(`tickBody` · `decayIdle` · 염색 남은 시간 · `daysPlayed`)가 있었다. 그래서 시계를
옮기면 **AP·생산·밤만 따라오고 포만감은 제자리**였다 — 개발용 「하루가 지나갔다고 치기」를
붙이면서 드러났다. 지금은 게임 시계가 `nowMs()` 하나다.

⚠️ **`devClock` 은 `load()` 보다 «위»에 둔다.** `defaultState()` 가 `nowMs()` 를 부르는데
`load()` 는 game.js 를 끝까지 읽기 전에 불린다 — 아래에 두면 `ReferenceError` 가 나고
`load()` 의 `catch` 가 그것을 삼켜 **세이브가 통째로 기본값으로 되돌아간다**
(1번 항의 그 사고다). 그래서 `nowMs()` 안에서 **`S` 를 안 읽는다** — 불러온 뒤에
`setDevClock(S.devClock)` 이 맞춰 주고, 서버에서 받아올 때(`adoptState`)도 같이 맞춘다.

⚠️ **게임의 시계가 «아닌» 것은 그대로 `Date.now()` 다** — 저장 실패 토스트의 제동 ·
캐시 버스터 · 밭 요청의 작업 번호 · **서버가 준 시각**(`FARM.now`) · 「마지막 저장 n분 전」.
현실 시계를 재는 자리라 개발용으로 옮겨 놓으면 오히려 거짓말이 된다.

### 개발용 「하루가 지나갔다고 치기」 (`devSkipDay`)

하루를 기다려야만 보이는 것이 여럿이다 — AP 자정 충전 · 포만감·스태미나 · 방치 감소 ·
혼자 먹은 밤 · 크리처 생산 · 날씨·시간대. 개발용(임시) 패널의 버튼 하나로 본다.

- ⚠️⚠️ **값을 심지 않고 «시계»를 옮긴다** (`S.devClock` · ms). 날짜에 기대는 값이
  예닐곱 군데라 하나씩 심으면 반드시 하나를 빠뜨리고, 그러면 **버튼이 만든 하루와
  진짜 하루가 다른 것**이 된다. 시계는 한 곳이라 거기를 옮기면 전부 따라온다
- ⚠️ **옮긴 몫은 세이브에 남는다** — 새로고침하면 되돌아가는 하루는 하루가 아니고,
  몸의 시각(`bodyTs` 등)이 이미 그 시계로 적혀 있어서 되돌리면 어긋난다.
  더하는 칸이라 `SAVE_VER` 는 안 올린다
- ⚠️ **정산은 `refreshEnergy()`** — 부팅·자정·복귀가 지나는 그 함수다. 여기서 따로
  깎으면 버튼으로 본 것이 진짜 경로가 아니게 된다
- ⚠️ **밭은 안 따라온다** — 자라는 시각은 서버가 잰다 (`FARM.now`)
- `npm run test:time` 이 넷을 본다 — 날짜가 «하루» 가는가 · AP 가 상한까지 차는가 ·
  **몸도 같이 가는가**(밤이 하나 지나간다) · 새로고침해도 시계가 그대로인가.
  · ⚠️⚠️ **「포만감이 조금이라도 줄었나」로 재면 못 가른다** — 시계를 반만 옮겨도
    버튼을 누르는 동안 흐른 몇 ms 만큼은 줄어서 **사보타주가 그대로 통과했다.**
    그렇다고 「하루치만큼 줄었나」로도 못 잰다: 포만감이 바닥나면 그 밤에
    `BINGE.fullnessBack`(70)까지 **도로 찬다.** 가르는 것은 **밤이 지나갔는가**다
  · ⚠️ 검사에서 `S.bingeDay` 를 0 으로 두지 않는다 — 「처음 들어온 사람」으로 보고
    기준만 잡고 돌아가서 밤이 하나도 안 세어진다. **오늘로** 세운다
  · 사보타주 넷으로 확인했다 (몸의 시계만 안 옮기기 · 시계 대신 값 심기 ·
    이틀 넘기기 · 정산 안 부르기)

`Date.now` **한 곳만** 갈아 끼운다. `Date` 클래스까지 감싸면 `static now()` 가 자기를
다시 불러 **스택이 넘친다** — 검사 스크립트를 새로 쓸 때 실제로 겪었다.

지금 이 검사가 보는 것: 포만감 · 스태미나 · 방치 감소 · 혼자 먹은 밤 · 운동 시간대 ·
**채집 시간대**(운동 구간을 넘지 않는가) · **날씨**(같은 시각에 안 흔들리는가 ·
하루 안에서는 바뀌는가 · 같은 시각에도 맵마다 다른가) · **부엌에 간 밤**(혼자가
아닌 밤이 되는가 · 여러 밤 중 하나만 빠지는가 · 하루에 한 번뿐인가) ·
**로엔 제국력**(연도를 2027·2127 로 옮겨 놓고 227·327 이 나오는가 — 「올해」만 맞는
상수를 박아 두면 오늘은 통과로 보이고 새해 첫날에 조용히 틀린다).

**주장하기 전에 확인한다.** 특히 "고쳤다" 고 말하기 전에 실제로 그 경로를 실행해 볼 것.

---

## Railway 배포

자세한 절차는 `server/README.md`. 헷갈리기 쉬운 것만 적어 둔다.

| 항목 | 값 | 주의 |
|---|---|---|
| Root Directory | **비움** (`/`) | 저장소 루트가 곧 게임 폴더다. `server` 로 잡으면 안 된다 — Root Directory 는 **빌드에 포함되는 범위**라서 게임 파일이 안 올라간다. 예전에 게임이 하위 폴더였을 때 쓰던 `dieter-alchemist` 값도 이제는 틀린 값이다 |
| Start Command | `npm start` | |
| 포트 | 기본 8080 | Railway 가 도메인을 8080 으로 라우팅한다. 3000 이면 502 |
| `DATABASE_URL` | `${{Postgres.DATABASE_URL}}` | **Postgres 를 추가해도 자동으로 안 들어온다.** 참조 변수를 직접 만들어야 한다 |

배포 후 `/api/health` 의 `store` 값을 확인한다. `memory(휘발성)` 이면
**재배포할 때마다 모든 플레이어 세이브가 사라진다.**

---

## 경제 — AP · 현자의 결정 · 비법서

세 값이 서로 물려 있다. **하나를 바꾸면 `npm run test:balance` 가 나머지를 본다.**

| | |
|---|---|
| **채집 AP** | **지대마다 다르다** (`D.ZONES[].ap` → `zoneAp`). 평야 6 → 황무지 17. **미니게임 맵은 거기에 「한 판 상한 × `miniApK`」를 곱한다**(아래 항). 화면 어디서도 `D.ENERGY.cost.gather` 를 직접 읽지 않는다 — `gatherCost(mapId)` 한 곳을 지난다. 직접 읽으면 **버튼에 적힌 값과 깎이는 값이 갈린다** |
| **AP 상한** | 매력 **단계**로 오른다 (`capPerTier` 200 · 새싹 1000 → 여신 1800). 판정은 `charmPeak()` 다 — 총합을 보면 애착 크리처를 바꾼 순간 상한이 내려가 **들고 있던 AP 가 갈 곳을 잃는다**. ⚠️ **자정 충전은 `energyCap()` 까지다**: 고정 `dailyFill` 로 자르면 늘어난 몫이 영영 안 찬다 |
| **비법서 장** | **조합에 성공하면** 흘러나온다 (`D.PAGE_DRIP` 4회에 한 장 · 아래 항). 매력 단계는 **천장**만 올린다 |
| **현자의 결정** | **조합에 성공하면** 들어온다 (`brewReward` 5). ⚠️ 예전에는 조합 «실패»가 유일한 수급원이었는데 비법서가 실패를 없앴다 — 그대로 뒀으면 AP 충전도 밭 칸도 못 여는 게임이 됐다. **조합 값(25)보다 반드시 작아야 한다** |

**리그 사다리도 여기 물려 있다.** 한 주에 낼 수 있는 최고 점수는 AP 로 정해지는데,
상한이 오르고 초반 채집이 싸지면서 1312 → **2720점**이 됐다. 그래서 `leaguePace` 를
칸당 26 → **50** 으로 올렸다 (맨 위 1590점 = 상한의 58%). 40~70% 밖으로 나가면
`checkbalance` 가 실패시킨다.

## 외부 비평(2026-09-11)에서 고친 것 — «약속»이 코드에서 어긋나 있던 자리들

코드를 통째로 Node VM 에 올려 재현한 비평을 받았다. 1장(구현 결함) 13개 중 10개를 고쳤고,
하나(1.3 두 기기의 최신성)는 사본만 남겼으며, 둘(1.8 개발용 API 기본값 · 1.9 경쟁
데이터의 서버 검증)과 2·3장의 설계 판단은
**결정이 필요한 것**으로 `HANDOFF.md` 에 남겼다. 고친 것마다 검사가 하나씩 생겼다 —
전부 **옛 코드에서 실제로 잡히는지**(사보타주) 확인했다.

- ⚠️ **`gathered` 는 재료별 누적 «객체» 하나다** (세이브 15). `defaultState` 에 같은
  이름이 두 번 있어서(객체 · 숫자) 뒤의 숫자가 이겼고, 채집의 `S.gathered++` 가 객체를
  NaN(→ 저장하면 null)으로 만들어 **숙련이 한 세션도 못 살아남았다.** 총 채집 횟수는
  `record.gathered` 다. ⚠️ **같은 키를 한 객체 리터럴에 두 번 쓰지 않는다** — 뒤의
  것이 조용히 이긴다. `checklore` 가 `gather()` 를 진짜로 30번 부르고 새로고침한다
  (값을 손으로 심는 검사만 있어서 채집 자체가 망가뜨리는 것을 한 번도 못 잡았다)
- ⚠️ **몸의 시간 정산은 `settleBody()` 하나를 지난다** — 포만감 → 방치 감소 → 밤.
  `refreshEnergy()` 가 `checkBinge()` 를 `tickBody()` «앞»에 불러서, 어제 배부른 채로
  저장하고 하루 뒤에 켜면 **밤을 지나쳤다** (같은 시간을 비웠는데 마지막 저장값에
  따라 결과가 달랐다). 부팅·자정·복귀가 같은 함수를 부른다. `checktime` 이
  `refreshEnergy()` 그대로와 **진짜 새로고침** 둘로 본다
- ⚠️ **주간 점수는 더하기 «전»에 주를 정산한다** (`addWeekScore` → `settleLeague`).
  정산이 랭킹 탭을 열 때만 있어서, 주가 바뀐 뒤 탭을 안 열고 마신 물약이 **지난주에
  적립됐다.** 경계 처리는 탭을 열었는지와 무관해야 한다
- ⚠️ **`save()` 는 로컬에 못 써도 행동을 끊지 않는다** — 알리고(`save_fail`) 서버에는
  올린다. `load()` 가 깨진 원문을 `…_broken` 에, `adoptState()` 가 덮이는 로컬을
  `…_prev` 에 남긴다. **조용히 새 게임이 시작되는 길을 다 막았다**
- ⚠️ **409 가 «더 새로운 로컬»을 버리지 않는다** (`sync.js` `flush`). 409 는 응답을
  잃은 재전송(같은 rev)에서도 온다 — 보내는 사이에 로컬이 더 나아갔으면 `serverRev`
  와 견줘 **남겨 두고 다시 보낸다.** `finally` 가 날아가는 동안 쌓인 것을 이어 보내고,
  요청마다 15초 한도가 있고, `pushNow()` 는 올라갔는지를 돌려준다 — **`doRaid` 는
  그것이 참일 때만 나간다** (옛 부대로 판정받지 않게). `checkfarm` ⑥c
- ⚠️ **밭 요청의 작업 번호는 확인될 때까지 남는다** (`opNonce` · `opRecover`).
  서버는 같은 nonce 에 같은 답을 주는데 클라이언트가 누를 때마다 새 번호를 만들어서,
  응답만 잃으면 **밭은 비고 가방은 그대로**였다. 번호를 로컬에 적어 두고, 다음에 밭을
  볼 때 같은 번호로 다시 물어 결과를 적용한다 — 수확·심기·칸·약탈 넷이 같은
  `apply*` 를 지난다. `checkfarm` ⑥b 가 **요청은 서버까지 보내고 응답만 끊어** 잰다
- ⚠️ **조회는 참여가 아니다** — `GET /api/farm` 은 `create=1` 일 때만 밭을 만든다.
  부팅이 조회를 하므로 게임을 켠 사람이 전부 목록에 올라 (이삭이 쌓이면) 털릴 수
  있었다 — 「안 연 사람은 안 뜬다」가 글로만 있었다. 부팅은 `refreshFarm(false)`
- ⚠️ **rev 검사는 쓰기 «안»에 있다** (`store.put` 이 `true/false` 를 돌려준다). API 가
  먼저 읽어 견주는 것은 안내다 — 두 요청이 같은 옛 행을 읽으면 둘 다 통과한다.
  **밭을 바꾸는 요청은 `store.transact`** 로 행을 잠근 한 거래다 (약탈은 두 행 — 예전에는
  피해자를 먼저 저장하고 공격자를 나중에 저장해서 둘째가 실패하면 한쪽만 바뀐 채 남았다).
  ⚠️ 저장소가 셋이라 **셋 다 같은 약속**이어야 한다 (`server/test.js` 가 파일·메모리를
  돌리고, pg 는 `WHERE saves.rev < EXCLUDED.rev` · `FOR UPDATE` 다 — 실 DB 경합은 못 돌려 봤다)
- **히든 재료에 천장이 생겼다** (비평 2.4 · `D.SPECIAL_TIERS.pity`). 그 맵에서 연달아
  100 / 250 / 1,000번 헛걸음이면 다음은 반드시다 — 기대값의 절반이라 꼬리만 자른다.
  `S.spMiss` 가 맵마다 세고, ❔ 힌트가 「N번 안에는 반드시」를 같이 말한다.
  `checkbalance`(rate × pity 0.3~0.8) · `npm run test:gather`(`Math.random` 을 «절대
  안 나오게» 두고 정확히 천장에서 나오는가). 자세한 것은 `CREATURE.md` 4단계
- **비밀키는 `X-Secret` 헤더로** (주소 쿼리는 접근 로그에 남는다 · 옛 클라이언트용으로 당분간
  같이 받는다) · `*.md` 는 정적 서빙에서 뺐다 · 초기화 때 **서버 사본을 못 지웠으면 한 번 더
  묻는다** (그냥 진행하면 서버의 옛 사본·이름은 남고 지울 열쇠는 사라진다) ·
  `checkname` 의 playwright 는 지연 require 다 (`npm test` 가 브라우저 없이도 돈다)

## ⚠️ 전역 이름은 파일끼리 겹치면 안 된다

모듈 시스템이 없다. `<script>` 로 나란히 읽히므로 **`data.js` 의 최상위 `const X` 와
`game.js` 의 최상위 `const X` 는 같은 전역 하나**다. 겹치면 브라우저가
`Identifier 'X' has already been declared` 를 던지고 **그 파일이 통째로 실행되지 않는다.**

화면은 오류를 안 띄운다 — 그냥 빈 껍데기가 되고, 콘솔을 안 열면 원인을 못 찾는다.
`node --check` 도 못 잡는다: 파일 하나씩 보면 둘 다 멀쩡한 문법이다.

```bash
npm run test:globals   # npm test 가 이미 돌린다
```

(`PAGE_TIERS` 를 data.js 로 옮기면서 game.js 에 그대로 남겨 놓아 실제로 겪었다)

### ⚠️ 검사기가 게임의 전역을 `window.X` 로 찾으면 «영영 안 온다»

`let`·`const` 는 **`window` 에 안 붙는다.** `S`(`game.js` 의 `let S = load()`)도,
`escHtml` 도 그렇다. 그런데 `window.X` 는 **오류가 아니라 `undefined`** 라,
그것으로 무엇을 «기다리면» 조건이 영영 참이 안 되고 **시간만 흘려보낸다.**

`checkask` 의 새로고침 자리가 그랬다 — 「기다려서 막아 뒀다」고 주석까지 적어 놓고
실제로는 `window.S` 를 봐서 **10초짜리 잠**이었고, 그 10초가 넉넉한 날만 통과했다.
「네 번에 한 번쯤 거짓으로 빨개진다」로 신고받은 자리다.

- **전역은 «이름»으로 찾는다** — `typeof S !== 'undefined'` (`checksave.js` 에 같은 주석이 있다)
- **볼 값 «그대로» 기다린다.** `S.villages.length > 0` 같은 헐거운 조건은 부팅 도중
  잠깐 서 있는 «덜 찬 상태»를 통과시켜, 이름을 고쳐도 같은 순간을 읽는다
- ⚠️ **기다림을 고쳤으면 «갈리는지»를 재라.** 여기서는 `revived` 가 참이 되기까지의
  시간으로 갈렸다 — 새 조건 **53ms** · 옛 조건은 **15005ms** 를 다 쓰고도 끝내 거짓.
  기다림은 통과/실패로는 안 보이므로 **시간을 재야 죽은 코드인 것이 드러난다**

## 호감도 — 공방이 아니라 «주는 쪽»에서 오른다

⚠️ **매력·비주얼 수치를 호감도에 곱하지 않는다.** 예뻐질수록 남자들이 좋아해 주는
구조가 되면 `intro_6` 의 주제와 정면으로 부딪힌다 — 공주가 살을 빼는 이유가
남에게 잘 보이기 위해서가 되어 버린다. 그들이 끌리는 것은 외모가 아니라
**연금술사로서의 그녀**다. `npm run test:bond` 가 **매력을 999 로 올려 놓고
호감도가 한 톨도 안 움직이는지**를 수치로 잰다 (부엌의 「0 깎였는가」와 같은 검사다).

- **오르는 것은 만들어 준 물약뿐이다.** 마을 NPC 여섯의 「선물」에서 준다
- ⚠️ **값이 큰 물약이 아니라 «처음 주는 종류»가 크게 오른다**(`BOND_GAIN` 3 / 1).
  「좋은 것을 주면 많이 오른다」로 두면 후반 물약 하나가 초반 물약 스무 개를 덮어써서
  **초반 레시피가 통째로 죽은 콘텐츠**가 된다. 사람마다 좋아하는 «등급»이 따로 있어(+2)
  누구와 먼저 친해지는지가 점수 하나 없이 갈린다
- **물약은 사라진다** — 이것이 유일한 제동이다. 하루 몇 번 같은 제한은 안 건다
  (「오늘 여섯 명 다 돌아야 한다」가 되면 그게 숙제다)
- **내려가지 않는다.** 코지 게임에서 관리 압박은 독이다
- **클레멘에게는 호감도가 없다.** 그는 대가 없이 «주는» 쪽이고, 눈금을 붙이는 순간
  그것도 등가 교환이 된다
- 단계가 오르면 **그 사람의 자리에서 나는 재료**로 갚는다. ⚠️ 답례가 커지면
  「눌러서 재료 받기」가 되므로 `checkbalance` 가 **들인 AP 보다 작은지**를 지킨다
- **그들이 「주는 것」은 상시 효과다** (`BOND_GIVES` — STORY.md 의 담당 표 그대로).
  ⚠️ **다섯 다 이미 있는 함수 한 곳에 곱해 붙인다** — 새 경로를 만들면 화면에 적힌 값과
  실제로 먹는 값이 갈린다 (지대별 AP 에서 배운 것과 같다). 그 덕에 셋은 저절로 보인다:
  🛡️ 호위 → `gatherCost()`(채집 버튼의 ⚡) · 🍖 단백질 → `exCost()`(운동 팝업의 단련) ·
  🎻 노래 → 물약을 마실 때의 아우라. 안 보이는 둘(💎 보석 · 📖 지식)은 선물 시트에 적는다
  · ⚠️ **호위 할인이 지대 순서를 뒤집으면 안 된다.** `gatherCost` 에 평야 값 바닥을
    뒀지만 그건 마지막 안전장치이고, `checkbalance` 는 **표가 바닥에 안 닿는지**를 본다 —
    바닥을 씌워 놓고 재면 무슨 값을 넣어도 통과하는 「스스로 맞는」 검사가 된다
  · ⚠️ **리그 상한 계산도 이 할인을 봐야 한다** — 안 그러면 「맨 위 리그에 닿는가」가
    조용히 헐거워진다 (`checkbalance` 의 `apOfR` 이 제일 유리한 단계로 잰다)
  · **실반에게는 담당이 없다.** 그는 STORY 의 「남자 NPC 여섯」이 아니라 3막의
    농민 연합 쪽 인물이라 재료로만 갚는다 — 억지로 붙이면 여섯의 표가 흐려진다
- ⚠️ **하트(♥/♡)는 `checkTextStyle()` 이 안 본다** — 그림 글자의 대비는 재지 않는다는
  규칙에 걸린다. 실제로 3.56:1 짜리를 넣어도 `checkUI()` 는 0건이었다.
  그래서 `checkbond` 가 그 자리만 콕 집어 잰다 (정책을 느슨하게 푸는 대신)
- **단계가 오르면 «하는 말»이 달라진다** (`D.bondTalk` → `bt_<사람>_g/l<단계>`).
  인사말이 다섯 갈래로 갈리고, 「대화」의 **마지막에 그 단계의 잡담 한 줄**이 붙는다.
  눈금(♥)만 차오르고 말이 그대로면 호감도는 숫자놀이로 남는다
  · ⚠️ **외모를 칭찬하는 말을 한 줄도 안 쓴다.** 「예뻐졌네요」 하나면 「매력을 호감도에
    곱하지 않는다」가 글에서 무너진다 — 그들이 끌리는 것은 **연금술사로서의 그녀**다.
    `checkbond` 가 두 언어로 이 줄들만 훑어 외모 낱말을 잡는다 (수치 쪽 매력 999 검사와 짝이다)
  · ⚠️ **줄 목록은 `talkLinesOf()` 한 곳에서 나온다.** 화면과 `talkNext` 가 길이를
    따로 세면 붙인 줄에 **영영 못 닿는다** — 마지막 줄에서 대화가 끝나 버렸다 (실제로 그랬다)
  · 잡담을 늘렸으면 **표정도 같이** 늘린다 (`moods` 가 짧으면 그 줄만 기본 얼굴이 된다)
  · `checkbond` 가 셋을 본다 — 여섯×다섯이 다 있는가 · **단계끼리 겹치지 않는가** ·
    외모 낱말이 없는가. 키만 있고 내용이 같으면 화면에서는 안 달라진 것과 똑같다
- **호감도가 이야기를 민다** — 단계가 오르면 **그 사람이 새 키워드를 준다**
  (`ASKS` 의 `need: { bond: n }`). 물약을 만들어 준 값이 재료로만 돌아오면
  호감도는 상점의 다른 얼굴일 뿐이다. **가까워져야 나오는 말**이 있어야 사람이 된다
  · ⚠️ **막는 문은 낮추고, 사람을 보여 주는 말은 깊은 자리에 남긴다.**
    여섯 줄이 다 **신뢰(50)** 에 걸려 있었는데, 그중 넷이 **2막의 문**이라
    1막을 끝낸 사람이 **저등급 물약 열 종**을 선물해야 2막이 시작됐다 — 그 구간에는
    퀘스트도 없어서 손이 통째로 비었다 (`PLAYFLOW.md` 8장). 그래서 **키워드를
    «주는» 넷만 친함(24)으로 내렸다**(다섯 종이다).
    **아무것도 안 주는 둘은 신뢰 그대로다** — 진행을 안 막으니 깊은 자리에 둬도
    아무도 안 갇히고, 여섯을 다 내리면 신뢰·각별한 사이에 걸린 말이 하나도 안 남아
    단계가 눈금만 된다 (「신뢰의 값이 정보만이면 호감도는 다시 상점이 된다」 · STORY.md)
    · `checkbalance` 가 둘을 못 박는다 — **2막의 문이 기초 등급(여섯 종) 안에서
      열리는가** · **주는 줄의 단계 ≤ 안 주는 줄의 단계**. ⚠️ 문턱을 **AP 로 재지
      않는다**: 드는 것은 시간이 아니라 물약의 «종류»다 (같은 것을 스무 개 줘도
      `again`(1)이라 거의 안 오른다). 사보타주 둘로 확인했다 — 옛 값으로 되돌리면
      「물약 10종」 · 둘을 뒤집으면 두 번째가 잡힌다
    · ⚠️ **검사기가 단계를 «3» 으로 박아 두면 안 된다.** `checkask` 가 그랬는데,
      그러면 표를 어느 값으로 내려도 3 이면 늘 풀려서 **문턱을 아무 데나 옮겨도
      통과했다.** 지금은 표에서 읽어 **한 점 앞에서는 잠겨 있고 그 단계에서
      풀리는지**를 본다 — `askLocked` 에 off-by-one 을 넣는 사보타주로 확인했다
      (옛 검사는 그것을 통과시킨다)
  · ⚠️ **클레멘에게는 `need.bond` 를 걸 수 없다.** 그에게는 눈금이 없어서 조건이
    영영 안 채워지고, 화면에는 자물쇠만 뜬 채 아무리 해도 안 풀린다 —
    조용히 막히는 종류다. `checktalk` 이 그것을 잡는다
  · **잠긴 줄은 감추지 않고 🔒 로 보여 준다.** 안 보이면 「없는 것」이고 보이면
    「아직 못 여는 것」이다. 대신 **길잡이 점(●)에는 안 센다**(`asksNew`) —
    점을 따라갔는데 못 여는 것뿐이면 점이 거짓말이 된다
  · ⚠️ **잠긴 칩을 누르면 그 사람의 «선물 시트»가 열린다** (`doAsk` 의 `askLocked` 갈래).
    막기만 하면 버그로 읽히는데, 예전에는 「더 친해져 보세요」 한 줄이 전부였다 —
    모자란 것은 호감도이고 호감도는 **물약을 «선물»해야** 오르는데 매력은 물약을
    «마셔서» 오르므로 **둘이 서로 다른 동작**이다. 그 연결이 화면 어디에도 안 적혀
    있어서 1막을 끝낸 사람이 「선물하기」를 스스로 찾아내야 2막이 시작됐다
    (`PLAYFLOW.md` 8장 「1막 → 2막에서 손이 빈다」).
    · **새 화면을 만들지 않는다** — 선물 시트에 이미 다 적혀 있다: ♥ 진행 ·
      **다음 단계까지 남은 점수** · 좋아하는 등급 · 줄 수 있는 물약이 오를 점수 순으로
    · 토스트에는 **몇 단계가 모자란지**를 적는다(`ask_locked_toast` 의 `{tier}`).
      **앵커를 안 준다** — 누른 칩은 이제 시트에 덮여 있어서 거기 붙이면 «안 보이는
      것»을 가리킨다. 기본 자리에 뜨면 방금 열린 시트의 머리말처럼 읽힌다
    · ⚠️ **검사가 「무슨 말이든 떴는가」를 보면 안 된다.** `tst.length > 0` 으로
      두었더니 **몇 단계 전의 「거울 골짜기로 가는 길이 열렸어요!」**를 읽고 통과하고
      있었다 — `doAsk` 는 새 키워드·마을 알림을 `setTimeout(…, 900 + i*700)` 으로
      미뤄 두므로 **앞 단계의 토스트가 뒤늦게 이 자리를 덮는다.** `checkask` 는
      누르기 «전»에 예약 토스트를 다 흘려보내고(토스트가 1.2초 동안 한 번도 안 뜨면
      다 흘러간 것이다) **기다리지 않고 바로** 읽는다. 사보타주 셋으로 확인했다 —
      시트를 안 열면 2건 · 토스트에서 단계 이름을 빼면 1건 · 아무 말도 안 하면 1건
  · 흐림은 **`style.css` 의 잠긴 콘텐츠 공통 표현**을 그대로 쓴다(`.ask-chip.locked`).
    `npm run test:ask` 가 세 가지를 갈라서 잰다 — 자물쇠가 보이는가 · 눌러도 안 들어오는가 ·
    3단계가 되면 풀려서 「유리관」이 들어오는가
  · ⚠️ **`checkui` 의 「물어볼것」 화면에 잠긴 칩이 하나는 있어야 한다.** 없으면 🔒 줄의
    대비·넘침을 아무도 안 재는 것이라 0건이 「재 본 적 없다」는 뜻이 된다 —
    그래서 그 화면이 유타르크의 호감도를 0으로 내려놓고 재고, **재고 나서 되돌린다**

## 부엌 — 요리사 클레멘 (STORY.md · QUEST.md 4단계)

**그는 대가 없이 준다.** AP 도 재료도 결정도 안 든다 —
「온기만이 등가 교환의 밖에 있다」가 이 인물의 전부다.
`checkui` 가 **0 깎였는지를 수치로** 잰다: 값을 받기 시작하면 인물이 무너진다.

⚠️ **부엌을 「선택 콘텐츠」 뒤에 숨기지 않는다.** 「혼자 먹은 밤」의 판정 기준은
처음부터 「혼자 먹었느냐」였는데 같이 먹을 사람이 없어서 **늘 참**이었다 —
**피할 방법이 없는 페널티**였다.

그래서 오래 「퀘스트와 상관없이 튜토리얼 직후부터 열려 있었다」. **지금은 첫 퀘스트를
받으면 열린다**(`c_meet_in` → `chefHired`) — 규칙이 바뀐 것이 아니라
**그 퀘스트가 첫 퀘스트가 됐기 때문**이다. 튜토리얼을 마치면 칩이 바로 떠 있고,
한 번 누르면 컷씬이 끝나면서 부엌이 나타난다. 선택 콘텐츠 뒤가 아니다.
· ⚠️ **「퀘스트를 끝내면」으로 두면 안 된다** — 그 퀘스트의 목표가 **부엌에서 같이
  먹기**라, 끝나야 열리게 하면 깰 방법이 없다 (물어보고 「받으면 열린다」로 정했다)
· ⚠️ **여는 컷씬(`c_meet_in`)에 클레멘은 안 나온다.** 예전에는 그것이 곧 첫 만남이라
  **부엌에 가기도 전에** 그가 이름을 대고 스프를 차려 놓았고, 뒤의 부엌 첫 방문
  (`c_clemen_meet`)이 **같은 장면을 한 번 더** 하고 있었다. 지금은 요정 대모가
  「식단을 위해 셰프를 고용했다」고 말만 하고(그래서 `q_meet` 의 `npc` 도
  **`sp_althea`** 다 — 아직 만나지도 않은 사람이 부탁을 해 오면 안 된다),
  **만나는 것은 부엌에서**다. 함수 이름도 `metClemen` → `chefHired` 로 바꿨다 —
  그 깃발이 뜻하는 것은 「만났다」가 아니라 「셰프가 왔다」이다
· ⚠️ **주는 이를 바꾸면 «완료» 컷씬도 같이 본다.** `c_meet_out` 은 공주·클레멘 둘뿐이라
  **요정 대모가 부탁해 놓고 다시 안 나왔다** — 퀘스트 칩·시트에 뜨는 얼굴과 보상을
  주는 얼굴이 달라진다. 밥상의 끝은 그대로 두고 마지막 한 줄만 그녀가 받는다
  (「식? 단?」을 여기서 받아 첫 컷씬의 농담도 닫힌다). `checkdata` 가 이제
  **퀘스트 열여섯의 `in`·`out` 에 주는 이가 한 줄이라도 있는지**를 본다 (사보타주로 확인했다)
· ⚠️ 컷씬 대사 안에서 **낱말 하나를 강조**할 수 있다 (`«클레멘»` · 영어는 `*Clement*`).
  표시는 원래부터 네 줄에 써 있었는데 **그리는 쪽이 그냥 글자로 뱉고 있었다** —
  「성 사람들 «전부»요?」가 화면에 겹화살괄호째 떴다. `cutLineHtml` 이 **접은 뒤에**
  표시만 `.cut-hi`(이름표와 같은 금색)로 바꾼다. ⚠️ **한쪽 언어에만 넣으면 안 된다** —
  그 언어에서만 금색이 되고 다른 쪽은 밋밋한데, 영어로 바꿔 그 컷씬까지 들어가 보기
  전에는 아무도 모른다 (`c_spire_in_3` 이 실제로 그랬다). `checkdata` 가 두 언어의
  **표시 수**를 견주고, 표시가 있는 줄이 하나도 없으면 그것도 알린다 (사보타주 둘로 확인했다)

- 부엌에 간 날은 그 밤이 혼자가 아니다. 여러 밤을 정산할 때는 **그 밤 하나만** 뺀다
- **시계를 옮겨 놓지 않으면 확인할 방법이 없다** — `npm run test:time` 이 넷을 본다
  (안 갔을 때 · 갔을 때 · 사흘 중 하루만 · 하루에 한 번뿐인지)
- **폭식해도 그의 호감도는 안 깎는다.** 대사에 조건이 하나도 없다
- ⚠️ **「오늘의 한 마디」는 날짜로 골라진다**(`diaryHash`) — 그냥 재면 다섯 줄 중 그날
  걸린 하나만 재는 것이라, **제일 긴 줄은 영영 안 재는 날이 생긴다.** `checkui` 의
  「부엌(제일 긴 한 마디)」이 그 줄로 못 박고 한 번 더 잰다 (언어마다 제일 긴 줄이 다르다).
  72자짜리 한 덩어리를 넣는 사보타주로 확인했다 — **평소 「부엌」은 통과하고 이쪽만
  267px 넘침으로 잡힌다**
- ⚠️ **「어머니」가 누구인지 밝혀 준다** — 클레멘의 「이거 공주님의 어머니께서
  좋아하시던 거예요」(`kt_say_5`)는 원래 「이거 어머니가…」였는데, 그러면 **말하는
  사람(클레멘) 자신의 어머니로 읽힌다.** 영어는 `Your mother` 라 처음부터 갈렸다 —
  한국어에만 있던 구멍이다. 그가 공주를 부르는 말은 「프린세스」와 「공주님」 둘 다 쓴다
  (`c_meet_in_2`)
- ⚠️ **부엌 버튼의 점(●)은 「오늘 밥」이다.** 「새로 물어볼 것」으로도 켜지지만
  그것은 **갈 수 있는 마을이 하나도 없을 때뿐이다**(`kitchenNews`).
  · 둘을 늘 겹쳐 두면 **밥을 먹어도 점이 영영 안 꺼진다** — 물어본 대답이 다음
    키워드를 주므로 「새로 물어볼 것」은 사실상 늘 하나 남아 있다.
    「다 먹었는데 레드닷이 안 사라진다」로 **실제로 신고받았다**
  · 그래도 안 지운 이유는 처음 그대로다: 마을이 전부 잠겨 있으면 이야기가 시작되는
    자리가 여기뿐이라 점이 꺼지면 갈 곳이 아예 안 보인다. 마을이 하나라도 열리면
    안내는 **마을 탭의 점**(`villageNews`)이 맡는다 — 첫 대답(`kw_hunger`)이 바로
    일곱 굴뚝을 여니 이 상태는 게임 첫날에만 있다
  · ⚠️ **클레멘만 아는 키워드를 만들면 안 된다.** 부엌은 마을이 아니라 마이 룸의
    버튼이라 `villageNews` 가 못 세고, 그러면 **아무도 안 가리키는 대답**이 된다.
    `checktalk` 이 그것을 잡는다 (사보타주로 확인했다)
  · ⚠️ **`doAsk` 가 `renderActBadges()` 를 부른다.** `renderKitchen()`·`renderGather()` 는
    **그 시트 안만** 다시 그려서, 안 부르면 점이 **켜진 채로 남아** 다음 `render()`
    까지 거짓말을 한다. `npm run test:ask` 가 **`render()` 를 안 부르고** 잰다 —
    먼저 그려 주면 안 그려도 통과하는 「스스로 맞는」 검사가 된다

## UI 색상 — 테마 여섯 벌 (`theme.js` · `style.css` 의 `:root[data-theme]`)

베이지 · **에크루(기본)** · 블루 · 핑크(예전 화면 그대로) · 퍼플 · **챠콜(하나뿐인 다크)**.
설정 시트의 컬러칩 여섯으로 고르고, 고른 것은 `<html data-theme="...">` **한 곳**이 말한다.

- ⚠️⚠️ **토큰 이름은 «색»이 아니라 «자리»다.** `--pink` 는 블루 테마에서 파랑이고
  챠콜에서 회색이다. 이름을 안 바꾼 이유는 `TEXT_POLICY.md`·`UI_POLICY.md` 가 그
  이름들로 **관계**를 적어 두었기 때문이다 — 「`--pink-2` 는 글자색으로 쓰지 않는다」는
  여섯에서 다 참이라 그 문장들이 그대로 산다. 자리는 셋이다:
  **주 강조**(`pink`) · **좋다**(`mint`) · **재화·보조**(`lav`)
- ⚠️ **「좋다」는 테마를 안 탄다** — 여섯에서 다 초록 쪽이다. 테마 색으로 같이 물들이면
  리그의 ▲올라감과 ▼내려감이 **같은 색**이 되어 뜻이 사라진다 (승패 `--blue`/`--red` 도 같다)
- ⚠️⚠️ **테마 블록에는 토큰을 «다» 적는다.** 한 줄이라도 빠지면 그 자리에 기본(에크루)
  값이 남는데, 챠콜에서 그것은 **밝은 판 위의 밝은 글자**라 화면에 오류 하나 없이
  안 읽히게 된다. `checktheme` 이 `<<<THEME:` 구간을 읽어 여섯을 대조한다
- ⚠️ **색을 «값으로» 박지 않는다.** `#fff`·`rgba(255,255,255,0.8)` 은 다섯 테마에서
  멀쩡하고 **챠콜에서만** 무너진다. 자리마다 토큰이 있다 — `--card` `--cream` `--tint`
  `--well` `--glass*` `--float` `--track` `--on-accent` `--halo` `--toast-*` `--mix-base`
  (표는 `UI_POLICY.md` 0장)
  · ⚠️ **`--float` 와 `--glass` 는 다른 자리다** — 유리는 «페이지 배경» 위, float 은
    «방 그림» 위다. 다크에서 방 버튼에 유리를 쓰면 밝은 방 그림이 그대로 비쳐
    **밝은 글자가 밝은 그림 위**에 앉는다 (찍어 보고 알았다)
- ⚠️⚠️ **`theme.js` 는 `<head>` 에서 «미루지 않고» 읽는다.** 문서 끝에 두면 기본 색으로
  한 번 그려진 뒤 얹혀서 **다크를 고른 사람에게 흰 화면이 한 번 친다.** 눈으로는 못 보므로
  `checktheme` 이 **`<body>` 가 붙는 순간의 `data-theme`** 를 잡아 잰다 (사보타주로 확인했다)
- ⚠️ **세이브에 안 넣는다** — 사운드·바디파츠와 같은 결이고, 무엇보다 `load()` 보다
  훨씬 먼저 읽어야 해서 세이브에 두면 첫 페인트에 늦는다. 그래서 `SAVE_VER` 도 안 올랐고,
  **하던 사람도 기본(에크루)으로 바뀐다** (출시 전이라 사람이 그렇게 정했다)
- ⚠️ **컬러칩에 칠하는 색만은 토큰이 아니라 값이다** — 보여 주는 것이 «지금 테마»가
  아니라 «누르면 될 색»이라 `var(--pink-2)` 로 두면 여섯이 전부 같은 색이 된다.
  그것이 진짜 그 테마의 주 강조색인지는 `checktheme` 이 style.css 와 대조한다

### ⚠️ 없는 토큰을 부르면 CSS 는 그 선언을 «통째로» 버린다

테마를 만들며 드러났다 — `var(--pink-1)` 이 **네 곳**에서 불리고 있었고 그 변수는
어디에도 없었다. 그래서 **고른 줄 넷(동행·먹이·크리처 카드)의 바탕이 아예 안 깔린 채로**
오래 있었다. 화면에는 오류가 안 뜨고 테두리만 남아서 「원래 그런 디자인」으로 보인다.
`checktheme` 의 「선언 안 된 토큰」이 이제 잡는다 — **JS 가 인라인으로 심는 것
(`--x` `--at` …)은 `.js` 를 훑어 알아낸다** (목록을 손으로 적으면 사본이 생긴다).
⚠️ **주석을 먼저 걷어 내고 훑는다** — 이 저장소의 CSS·JS 는 주석에 토큰 이름을 잔뜩
적어 두는데, 그대로 보면 「예전에 이런 사고가 있었다」고 **적어 놓은 이름까지** 잡는다
(위 사고를 주석에 기록하자마자 그 줄이 스스로 걸렸다).

### 버튼은 카드에서 «떠올라야» 한다 — 색과 그림자는 «따로» 잡는다

「눌리는 색」(`--pink-2`)은 기본 버튼 · 채집 버튼 · 고른 탭이 다 같이 쓴다. 처음에는
**카드와의 대비가 1.50~1.82** 뿐이라 버튼이 크림색 판에 묻혔다 (「버튼이 눈에 안 띈다」로
신고받았다).

⚠️⚠️ **한 번 여섯을 다 2.3 으로 올렸다가, 넷은 사람이 보고 되돌렸다.**
지금은 **베이지·챠콜만 깊은 값(2.3)** 이고 **에크루·핑크·블루·퍼플은 예전 값(1.50~1.82)**
이다. 되돌린 것은 **채움뿐**이고 **그림자는 그대로** 뒀다 — 「예전 색 + 새 그림자」가
그 넷의 모양이고, 그림자만으로도 버튼이 판에서 뜬다.
**수치가 낮다고 자동으로 틀린 것이 아니다 — 여기는 사람이 보고 정하는 자리다.**

- ⚠️ **한계를 잡는 것은 그 위의 글자다**(`--ink` ≥ 4.5). 깊게 가려면 글자를 먼저
  진하게 해서 여유를 만든다 — `--ink` 를 카드 위 8.2~8.8 → **10.5~10.8** 로 올렸다.
  **되돌릴 때 이것과 `--text-pink` 는 안 되돌렸다**: 버튼 색이 아니라 글자색이고,
  진해진 쪽이 읽기에 낫다
- ⚠️ 깊은 채움 위에서는 `--text-pink` 가 3:1 아래로 떨어진다 — **한쪽만 옮기면
  조용히 띠를 벗어난다** (표가 잡아 줬다)
- ⚠️ **컬러칩도 같이 옮긴다.** 칩은 「그 테마의 주 강조색 그 값」이라 안 고치면 화면과
  칩이 갈린다 — 올릴 때도 되돌릴 때도 `checktheme` 이 잡았다
- ⚠️ **그림자는 `--btn-shadow`** 다. 예전에는 `rgba(255,150,190,.4)`(분홍)이 박혀 있어
  블루·에크루 버튼 밑에 **분홍 안개**가 깔렸다 — 색을 고르는 기능이 생기는 순간 제일
  먼저 어긋나는 자리다.
  · ⚠️ **채도를 눌러야 그림자로 읽힌다**(58% 상한). 채움 그대로 쓰면 «네온 광채»가 된다
    (핑크에서 그랬다 — 찍어 보고 되돌렸다)
  · ⚠️ **다크만은 검정**이다. 색 그림자는 어두운 판 위에서 빛나는 것처럼 보인다
- ⚠️ **채집 버튼에는 그림자가 아예 없었다** — 카드에서 «유일하게 누르는 곳」인데
  평평한 알약이었다. 지금은 떠 있다

### ⚠️⚠️ 「버튼을 밝게」는 **테마마다 뜻이 반대다** — 재 보고서야 갈렸다

그라데이션 버튼(`.btn-flex` · `.wr-gift`)의 두 끝은 제 토큰이다(`--btn-grad-a/b`).
예전에는 재화 슬롯과 주 강조 슬롯을 그냥 갖다 썼는데, 그 둘이 닮은 테마에서는
명도차가 4~12 밖에 안 나 **버튼이 평평해 보였다** (에크루·챠콜에서 신고받았다).

「많이 밝아져도 괜찮다」는 말을 듣고 둘 다 밝혀 보려다 **수치가 갈라 줬다**:

- **챠콜은 말 그대로 밝힌다.** 카드(`#2b2f35`)와의 대비가 2.29 → **4.48** 이라 버튼이
  비로소 카드에서 떠오른다. 대신 `--ink` 가 밝은 끝에서 **2.41** 로 내려앉는다
- **에크루는 밝히면 «사라진다».** 카드가 흰색이라 밝힐수록 묻힌다 (1.46 → **1.14**).
  여기서 모자랐던 것은 밝기가 아니라 **채도**였다 — 회끼 도는 베이지(채도 15/18)를
  금빛(**36/60**)으로 올렸다. 글자는 7.83 / 5.14 라 손댈 것이 없다

**그래서 글자의 테(`--btn-halo`)는 챠콜 하나에만 켠다.** 나머지 다섯은 `transparent`
라 **한 픽셀도 안 바뀐다** (되돌려 둔 네 테마가 그대로 남는 이유다).

- ⚠️ **테의 «모양»과 «색»을 갈라 둔다** — 그림자 다섯 줄은 `--btn-ink-edge` 한 곳,
  색만 테마 블록의 `--btn-halo` 다. 두 버튼에 같은 다섯 줄을 적어 두면 한쪽만 고쳐 갈린다
- ⚠️⚠️ **「테를 둘렀으니 대비 검사는 면제」가 아니다.** 그림자는 `checkTextStyle()` 이
  대비로 **안 쳐 준다** (바람개비 HUD·퀘스트 숫자에서 두 번 배운 자리다) — 면제로 두면
  **「테를 선언해 놓고 안 그리는」 사보타주가 그대로 통과한다.** 그래서 둘이 짝이다:
  · `checktheme` ②가 **끝마다** 「글자가 제 힘으로 서거나, 테가 그 끝에서 4.5:1 이거나」
    를 본다. ⚠️ **테마 단위로 뭉뚱그리면 안 된다** — 챠콜은 «밝은 끝»에서만 테가 일하고
    어두운 끝에서는 글자가 제 힘으로 선다 (그렇게 짰다가 2.88 로 헛짚었다).
    **두 갈래를 각각 몇 끝 쟀는지 낸다** (지금 글자 11 · 테 1 — 한쪽이 0이면 안 잰 것이다)
  · `checktheme` ③의 **「버튼글자」**가 여섯에서 **진짜 픽셀을 떠서** 잰다.
    ⚠️ **재는 상자를 글자에 «붙인다»** — 버튼 상자를 통째로 찍으면 제일 밝은 점이
    둥근 모서리 «밖»의 카드라 테를 떼도 통과한다 (퀘스트 숫자에서 사보타주가 검사기의
    구멍을 찾아낸 그 자리다). `Range` 로 글자줄만 집고, **앞의 이모지는 뺀다** —
    제 색이 있어서 그것만으로 대비가 부풀어 오른다
- ⚠️ PNG 픽셀 읽기(`pngLums`·`pngLumGrid`)는 **`tools/pnglum.js` 한 곳**에서 온다 —
  검사기 셋이 같이 쓰므로 베껴 두면 한쪽만 고쳐 갈린다

### ⚠️⚠️ 챠콜의 «눌리는 색»은 회색도 산호도 아니라 **파랑**이다

두 번 옮긴 자리다. 이유가 서로 달라서 **둘 다 적어 둔다**:

1. **회색 → 산호** — 명암은 맞는데 「어포던스 유발이 잘 안 된다」로 신고받았다
   (퀘스트의 「보상 가져가요!」). 어두운 판 위의 **무채색은 아무리 밝혀도 «판의 일부»로
   보인다** — 밝기가 아니라 **채도**가 모자랐던 것이고, 에크루에서 이미 배운 교훈이다(위 항)
2. **산호 → 파랑** — **붉은 채움 위에서 길잡이 점(●)이 안 보인다.** 점은 `#e11d48` 이라
   산호(`#a24a33`)와 **ΔE 37** 밖에 안 갈렸다 (다음으로 낮은 테마가 63이다) —
   「붉은색 버튼을 쓰니까 레드닷이 눈에 띄지 않는다」로 신고받았다. 파랑에서 **95** 다

- **지금 값** — `--pink` `#354762` · `--pink-2` `#3565a8` · 그라데이션 `#7697c5 → #3c659e`
  (색상 215 로 넷을 맞췄다). ⚠️ **휘도를 산호와 «같게» 잡았다** — 넷 다 ±0.001 안이라
  `--ink`·`--text-pink`·카드와의 대비가 **소수점 둘째 자리까지 그대로**고, 그래서
  테 규칙(`--btn-halo`)도 대비 표도 한 줄 안 바뀐다.
  이 자리는 **한 줄로 여러 곳이 같이 바뀐다**: 기본 버튼 · 채집 버튼 · 고른 탭 ·
  아래 탭 · 리그의 ▼ 줄 · 선물하기 알약
- ⚠️ **채도는 유지한다** — 1번의 교훈이 그대로 살아 있어야 한다 (52 → 52).
  「빨강이 문제였으니 가라앉히자」로 가면 회색 시절로 되돌아간다
- ⚠️ **길잡이 점의 색을 옮겨서 고치지 않았다.** 점은 여섯 테마가 같이 쓰는 한 값이라
  (`.tab-dot` `#e11d48`) 거기를 건드리면 멀쩡한 다섯이 같이 움직인다 —
  어긋난 것은 «한 테마의 채움»이므로 그쪽을 옮기는 것이 맞다
- **`checktheme` 의 「길잡이 점」**이 여섯 채움을 **ΔE76** 으로 재서 50 아래면 실패다.
  ⚠️ **대비(휘도)로는 못 잡는다** — 점은 글자가 아니라 `checkTextStyle()` 의 검사망
  밖이고, 휘도만 보는 잣대는 **색이 같은 것을 못 본다** (이모지 테에서 배운 자리다).
  점 색은 style.css 에서 읽는다. 사보타주로 확인했다 (산호로 되돌리면 ΔE 37.2 로 잡힌다)
- ⚠️ **`--text-pink` 는 안 건드렸다** — 그것은 버튼이 아니라 **카드 위의 강조 글자**라
  따뜻한 오프화이트로 두는 쪽이 읽기에 낫다. 「버튼이 안 보인다」에 글자색까지 같이
  물들이면 고친 자리와 무너질 자리가 갈린다
- ⚠️ **컬러칩(`theme.js` 의 `sw`)도 같이 옮긴다** — 칩은 「그 테마의 주 강조색 그 값」이라
  안 고치면 화면과 칩이 갈린다 (`checktheme` 이 style.css 와 대조해 잡는다)

### ⚠️⚠️ 다크에서는 **어두운 이모지가 사라진다** — 둘레에 테를 두른다 (`--emoji-halo`)

「동행 크리처 없음」 앞의 🐾 가 윤곽조차 안 남는다고 신고받은 자리다.
**`checkTextStyle()` 은 이모지를 아예 안 잰다** — 「그림 글자의 대비는 재지 않는다」가
규칙이라(♥ 3.56:1 · ❔ 1.34:1 이 둘 다 0건이던 그 자리) 검사망 밖이고, 밝은 다섯에서는
그래도 됐다: 이모지는 대개 어두운 쪽이라 크림색 판 위에서 저절로 읽힌다.
**챠콜에서만 명암이 뒤집힌다.**

- **`--btn-halo` 와 «같은 규칙 하나»로 선다** — 모양은 `:root` 의 `--emoji-edge`,
  색은 테마 블록의 `--emoji-halo`. 밝은 다섯은 `transparent` 라 **한 픽셀도 안 바뀐다**
- ⚠️ **자리는 «재서» 골랐다** (`npm run test:emoji`) — 눈으로 고르면 목록이 끝없이 는다.
  세기도 **기준을 넘는 제일 약한 값**이다(번짐 없는 1px 테 · 알파 0.5).
  0.42 로 내리면 👗·❓ 가 다시 걸린다 — 값을 «보기 좋게» 고른 것이 아니다
  · ⚠️ 번짐(`0 0 3px`)을 섞으면 글자가 통째로 빛난다 (찍어 보고 되돌렸다).
    반대로 번짐«만» 두면 이모지 쪽이 모자라 다섯 건이 남는다 — 넷을 그려 보고 골랐다

#### ⚠️⚠️ 테는 «이모지만» 진다 — **글자에 걸면 같은 색이라 오히려 안 읽힌다**

처음에는 라벨 요소에 통째로 걸었다(`.room-tab` `.cat-tab` `.pal-name`). 「챠콜의 글자는
이미 밝아서 같은 색 테가 거의 안 보인다」고 적어 두었는데 **그게 틀렸다** — 챠콜의
글자색(`--ink` #e8e6e3)과 테 색(`--emoji-halo` rgba(240,234,228,.5))은
**사실상 같은 색**이라, 글자가 1px 씩 굵어지고 가장자리가 뭉개져 **읽기 나빠졌다**
(「버튼 텍스트가 버튼 음영 색이랑 동일하니까 가독성 떨어진다」로 신고받았다 —
「👗 옷」 탭과 「💇 헤어 3/30」 탭을 짚어 줬다).

그렇다고 통째로 떼면 **👖 👗 🦋 🌹 🐾 다섯이 다시 밝은 테마보다 못해진다**
(떼고 재 봤다 — 2.04 ↔ 4.12 · 2.19 ↔ 4.96 · 2.34 ↔ 5.74 · 2.85 ↔ 8.77 · 2.91 ↔ 7.56).
그래서 **이모지를 «제 요소»로 떼어 낸다** — 지금 서는 자리는 **글자가 없는 것뿐**이다:
`.tab-icon` `.creature-art` `.lg-face` `.em`.

- **떼어 내는 문은 `I18N.em(s)` 하나다** — 머리에 붙은 이모지를 `<span class="em">` 으로
  감싼 HTML 을 돌려준다. `I18N.apply()` 가 `data-i18n` 라벨에 그것을 쓰고
  (「🎒 잡화」 · 「🌿 필드」), 동적으로 짓는 탭은 **템플릿에 이미 이모지가 따로** 있어서
  (`${z.emoji} ${N(...)}`) 그 자리를 감싸기만 하면 된다
  · ⚠️ **`textContent` 는 한 글자도 안 바뀐다** (`<span class="em">🎒</span> 잡화`) —
    라벨을 글자로 읽는 검사(`checktut` 의 「지시문↔대상」 · `checktuttext`)가 그대로 돈다
  · ⚠️ `apply()` 가 **이모지로 시작할 때만** `innerHTML` 이다. 나머지는 예전 그대로
    `textContent` 고, 꼬리 글자는 접는다(`esc`)
  · ⚠️ **`.em` 은 인라인 `span` 이라 레이아웃이 안 바뀐다** — `.cat-tab`·`.room-tab` 은
    플렉스 «항목»이지 컨테이너가 아니다 (말줄임도 그대로 돈다)
- ⚠️ **목록에 «글자가 섞인» 선택자를 도로 넣지 말 것.** `checkemoji` 의 **「테를 두른 자리」**
  가 style.css 의 그 규칙에서 **선택자를 읽어** 훑는다 — 테를 두른 요소의 글자에서
  이모지·공백을 걷어 내고 남는 것이 있으면 실패다. **대비와 다른 축이라 짝으로 둔다**:
  대비만 재면 라벨 통째로 되돌려도 통과한다 (오히려 «올라간다»).
  **본 요소 수도 같이 낸다** — 0건이 「통과」인지 「한 번도 안 쟀다」인지를 가르려면 필요하다.
  사보타주로 확인했다 (라벨 셋을 도로 넣으면 스물몇 건)

#### 잣대는 «절대 3:1» 이 아니라 **「밝은 테마보다 못한가」**다

절대 기준으로 재면 **에크루가 71건 · 챠콜이 20건**이다 — 흰 판 위의 흰 자물쇠(🔒 1.03:1) ·
주황 딱지 위의 밤톨(🌰 1.04:1) 처럼 **밝은 테마에서도 원래 낮은 자리**가 잔뜩이고,
그것은 이번 신고와 아무 상관이 없다. 밝은 다섯은 오래 눈으로 보아 온 화면이라 그것이
곧 기준선이고, 고칠 것은 **판의 명암이 뒤집히며 원래 잘 보이던 것이 안 보이게 된** 자리다.
그래서 `checkemoji` 는 두 테마를 다 재서 견준다.

- ⚠️⚠️ **테는 글리프 «밖»에 그려진다** — `Range` 가 주는 상자만 재면 테가 통째로 빠지고,
  게다가 그 테가 배경을 재는 둘레 띠에 섞여 **배경이 밝아진 것처럼** 보인다.
  테를 두르자마자 🏠 이 오히려 «떨어졌다» (고쳐 놓고 나빠진 것처럼 보이던 자리다).
  상자를 `PAD` 만큼 넓혀 테까지 안에 넣고, 띠는 거기서 또 떨어뜨린다
- ⚠️ **이 잣대는 «색»을 못 본다** — 휘도만 재므로 산호 판 위의 파란 드레스처럼
  «색으로» 읽히는 것은 낮게 나온다. 그래서 절대 기준을 문으로 쓰지 않는 것이기도 하다
- 사보타주 셋으로 확인했다 — 테를 끄면 7건 · 알파를 0.42 로 내리면 2건 ·
  `PAD` 를 0 으로 두면 **멀쩡한 🏠 이 2.61 로 «떨어져»** 걸린다 (검사기 쪽 사고다)

### ⚠️ 잠긴 줄의 글자는 `--ink` 다 — 강조색을 두면 «한 테마에서만» 무너진다

잠긴 콘텐츠 공통 표현은 `opacity: 0.8` 로 대비를 그대로 깎으므로,
**「잠긴 요소의 글자는 `--ink` 로 진하게 잡는다」**가 원래 규칙이다. 그런데 비법서의
「어디서 나는가」 줄(`.pg-where`)만 강조색(`--text-lav`)을 그대로 쓰고 있었고,
**퍼플에서 4.49:1** 로 내려앉아 드러났다 — 다섯 테마에서는 4.6~5.1 이라 «간발로»
통과하고 있었을 뿐이다.

- ⚠️⚠️ **값을 한 테마만 밀어서 고치지 않는다.** 처음에 퍼플의 `--text-lav` 를 한 단
  내렸는데, 그건 증상을 덮은 것이다 — 원인은 **규칙을 안 지킨 자리** 하나였다.
  되돌리고 `.pg-row.locked` 의 글자를 `--ink` 로 잡았다 (여섯이 한꺼번에 풀린다)
- `checktheme` 의 표가 **`--ink` 를 «잠긴 채»(0.8 합성)로도** 잰다. ⚠️ 거기 넣을 것은
  **`--ink` 하나뿐**이다: 강조색과 `--ink-soft` 는 0.8 에서 3.8~4.4 로 떨어지는데,
  표에 넣어 값을 옮기는 것이 아니라 **그 색을 잠긴 자리에 두지 않는 것**이 답이다.
  넓게 잡았다가 일곱 건을 헛짚고 좁혔다 — **표는 「실제로 서는 것」만 담아야 잣대가 된다**
- 사보타주 둘로 확인했다 (잠긴 줄을 다시 강조색으로 · `--ink` 를 흐리게)

### 로고 화면(스플래시)도 테마를 탄다 — **로고를 투명 배경으로 구워서** 풀었다

오래 **여기만 고정 near-white(`#F6F6F8`)** 였다. `logo.png` 가 투명 영역이 아예 없는
불투명 팔레트 이미지(`tRNS` 가 없다)에 테두리까지 그림이 닿아 있어서, 바탕을 테마
색으로 두면 **로고가 사각형으로 깨져 보였기** 때문이다 (에크루에서 신고받았다).

지금은 `tools/genlogo.js` 가 그 배경을 **빼내서** 제약 자체를 없앴다. 스플래시는
`body` 와 **같은 그라데이션**(`--bg-1/2/3`)이고 글자는 `--ink` 다 — 같은 그림이라야
1.6초에 걸쳐 걷힐 때 색이 한 번 튀지 않는다.

- ⚠️⚠️ **「흰 것을 지운다」로는 안 된다 — 이 그림에는 «그림자»가 있다.** 산호색 그림과
  그 아래 부드러운 드롭 섀도가 흰 바탕에 구워져 있어서, near-white 만 지우면 그림자가
  **회색 얼룩**으로 남고 어두운 테마에서는 그것이 그림자가 아니라 **빛나는 자국**이 된다.
  그래서 픽셀마다 α 를 풀고 바탕색을 빼낸다 — `c = f·α + 배경·(1−α)` 를 `f` 에 대해 푼다
- ⚠️ **α 는 두 갈래의 «큰 쪽»이다.** 하나로는 둘 다 못 잡는다:
  **그림**은 채도(chroma)로, **그림자**는 중성색이라 chroma 로 안 잡히니
  「색이 0~255 안에 남으려면 적어도 이만큼은 덮여 있어야 하는가」(`floorOf`)로 잡는다.
  ⚠️ 둘 다 **잡티 바닥을 빼고 다시 편다** — 안 그러면 배경의 ±3 노이즈가 α 0.1 짜리
  유령이 되어 **어두운 테마에서 온 화면에 흰 점**으로 뜬다
- ⚠️⚠️ **`CH_FULL` 을 좁게 잡으면 안티에일리어싱이 통째로 날아간다 — 실제로 그랬다.**
  한때 **26** 이라 「chroma 26 만 넘으면 무조건 불투명」이었는데, 다 칠해진 그림의
  chroma 는 **90~128** 이다 — 즉 **30% 만 덮인 가장자리 칸이 100% 산호색**이 되어
  가장자리가 계단이 됐다 (「챠콜 테마에서 안티앨리어싱 안 먹는다」로 신고받았다).
  지금은 그림의 «속»에서 재서 **90** 이다 (반지름 2 안이 전부 그림인 칸 6만 개의
  chroma 5% 분위 · 중앙값 107) — 속의 95% 가 불투명하고 가장자리는 0~90 을 탄다
  · ⚠️⚠️ **밝은 테마에서는 이 사고가 «안 보인다».** α 를 무엇으로 잡든 **옛 배경에
    도로 얹으면 원본이 그대로 나오기** 때문이다 — `f·α + 배경·(1−α) = c` 는 α 를
    어떻게 고르든 성립하는 항등식이다. α 가 틀린 것은 **다른 바탕에 얹어야** 드러난다.
    그래서 「옛 화면과 같은가」는 이 종류를 **영영 못 잡는 잣대**다
  · ⚠️ **밝은 쪽은 α 에 안 센다.** 배경이 247 이라 위쪽 여유가 8 밖에 없어서,
    베벨 하이라이트(250)를 `floorOf` 에 넣으면 α 0.5 짜리 **흰 테**가 생긴다 —
    어두운 테마에서 글자 둘레가 은색으로 빛난다 (그려 보고 되돌렸다)
- ⚠️ **팔레트 + `tRNS` 로 쓴다.** 서로 다른 RGBA 가 236가지라 256 안에 들어가서
  RGBA(198KB) 대신 **77KB** 다 — 불투명하던 원본(101KB)보다도 작다. 스플래시는 제일
  먼저 받는 그림이라 이 차이가 그대로 첫 화면 시간이다.
  ⚠️ **기준 chroma 를 «이웃에서» 찾으면 팔레트가 터진다** — 그러면 같은 입력 색이
  자리마다 다른 α 로 나가 RGBA 가 3,590가지가 된다 (198KB). 기준이 **상수 하나**라야
  입력 색 하나가 출력 하나로 가서 256 안에 남는다
- **옛 화면과 한 픽셀도 안 달라진다** — 새 로고를 옛 고정색(`#F6F6F8`) 위에 얹어
  원본과 견주면 채널당 **최대 6.0 · 평균 0.62** 다 (사람 눈에 안 보이는 몫이고,
  `floorOf` 가 색이 넘쳐 잘리는 것을 막아 주어 **옛 파일보다 오히려 낫다** — 6.6 · 0.73)
- ⚠️ **두 번 구우면 그림자가 또 빠진다** — `genlogo` 는 **알파가 있으면 그 자리에서
  멈춘다**(`--force` 로만 넘긴다). 옛 불투명 그림은 `git show 70f5dbb:logo.png` 다
- ⚠️⚠️ **대체값은 `var()` «안»에 적는다** (인라인 `#splash`). 앞줄에 고정색을 두고
  뒷줄에 `var()` 를 쓰는 흔한 수법은 **안 통한다** — 없는 `var()` 는 그 선언을 «앞
  선언으로» 되돌리는 것이 아니라 속성을 통째로 initial(투명)로 만든다.
  스플래시를 인라인으로 둔 이유가 「style.css 가 없어도 뜬다」이므로,
  `var(--bg-1, #F6F6F8)` 처럼 **대체값을 안에** 달아 두면 그때는 예전 화면 그대로다
- **검사가 둘이고 보는 것이 다르다** — 한쪽만으로는 못 잡는다:
  · `npm test` 의 **`genlogo --check`** 가 **파일**을 본다 (알파가 있는가 · 모서리가
    비었는가 · 그림이 통째로 지워지진 않았는가 · **가장자리에 «중간 단계»가 있는가**)
    · ⚠️ 마지막 것이 안티에일리어싱을 지킨다 — 꽉 찬 칸마다 이웃 넷 중 제일 진한
      반투명 이웃을 보고 그 **10% 분위**가 64 이상인가 (옛 파일 **22** · 지금 **159**).
      사보타주 둘로 확인했다 (옛 로고를 되돌리기 · `CH_FULL` 을 26 으로)
  · `checktheme` 의 **「로고 화면」**이 **화면**을 본다 — ① 여섯이 서로 다른가
    ② 그 색이 정말 그 테마의 `--bg-1` 인가 ③ **로고 귀퉁이가 바탕과 같은가**(진짜 픽셀).
    ⚠️ ③ 이 없으면 **불투명 로고로 되돌려 놓아도 ①②는 그대로 통과한다** — 바탕은
    테마를 타는데 그 위에 흰 상자가 얹힌 화면이 0건으로 나온다
  · ⚠️ 색을 **글자로 견주지 않는다**: 토큰은 `#dcefff` 인데 화면이 내놓는 것은
    `rgb(220, 239, 255)` 라 **무슨 색이든 «없다»로 걸린다** (그렇게 짰다가 6건 났다)
  · ⚠️ **스플래시는 0.9초 뒤 걷히기 시작해 2.5초에 DOM 에서 «완전히 제거»된다** —
    다른 검사처럼 기다렸다 재면 늘 「못 찾았다」가 나온다. 여기만 **뜨자마자** 잰다
- ⚠️ **PNG 를 읽고 쓰는 것은 `tools/png.js` 한 곳이다.** `checkui`·`checktheme` 의
  휘도 읽기(`tools/pnglum.js`)도 그 위에 얹혀 있다 — 디코더가 두 벌이 될 뻔한 자리다

### ⚠️ `<button>` 은 `color` 를 «물려받지 않는다»

브라우저가 `color: buttontext`(검정)를 얹어서, 색을 안 적은 버튼은 검정 글자가 된다 —
밝은 테마에서는 크림 위 검정이라 **우연히 통과**하고 챠콜에서만 드러난다.
`.brewq-step` 이 1.81:1 이었고, 변종 없는 `.btn` **둘**(개수 패널의 「취소」 ·
혼자 먹은 밤의 「다음 밤 ▸」)은 배경까지 브라우저 기본(`#efefef`)이라 테마를 아예 안
타고 있었다. 지금은 **`.btn` 기반 규칙이 `--ink` 를 주고**, 변종 없는 `.btn` 은 두지
않는다 (셋 다 `checkui` 가 잡았다).
⚠️ **제 그림을 가진 어두운 장면의 버튼은 고정색으로 «명시»한다** — 혼자 먹은 밤의
버튼이 브라우저 기본에 기대고 있어서, `.btn` 기반이 색을 갖는 순간 **밝은 판 위
밝은 글자**(1.08:1)가 됐다. 기반을 고치면 **기대고 있던 것들이 드러난다**.

### ⚠️ 「핑크」는 토큰 값만 예전 그대로다 — 화면이 한 픽셀도 같지는 않다

하드코딩돼 있던 색을 토큰으로 옮기면서 달라진 자리가 있다. 되돌리려다 헷갈리지 않게
**`style.css` 맨 위 주석에 전부 적어 두었다** — 유리 불투명도 아홉을 셋으로 모은 것 ·
`--tint` 하나로 모은 것 · 표에 없던 강조 글자 넷을 `--text-pink` 로 · 리그 ▲▼ 줄 배경 ·
AP 슬라이더 손잡이 · 선물 딱지 · `.potion-why` 의 ? 글자 · 개발용 버튼 글자 ·
**없던 배경이 넷 생긴 것**(`--pink-1` 사고).

### `checkTextStyle()` 의 «흰색 가정»도 테마를 탄다

페이지 바닥이 그라데이션이라 화면 제목·설명 줄·아래 탭 라벨은 배경을 끝까지 못 찾고
**가정**으로 떨어진다. 그 가정이 흰색으로 박혀 있어서 **챠콜에서 멀쩡한 화면이 13건**
위반으로 잡혔다 (밝은 글자 위에 흰 배경을 가정하니 1.25:1). 지금은 **실제로 깔린
그라데이션의 스톱**을 읽고 그중 **제일 불리한 것**을 고른다 — 한 스톱만 고르면 다른
스톱에서 깨진 것을 놓친다. ⚠️ **토큰 이름(`--bg-1`…)으로 읽지 않는다**: 이름을 바꾸는
순간 조용히 흰색 가정으로 되돌아간다. `checkTextStyle()` 은 이제 **`checked`(잰 요소 수)**
도 같이 낸다 — 0건이 「통과」인지 「한 번도 안 쟀다」인지를 부르는 쪽에서 가르려면 필요하다.

```bash
npm run test:theme                                       # 짜임 + 토큰 대비 표 + 여섯 × 화면
FULL=1 THEME=charcoal node tools/checkui.js showcase atelier gather   # 한 테마를 통째로
```

`npm test` 는 `--static` 으로 **앞의 두 층만** 돌린다 (브라우저 없이).
사보타주 열하나로 확인했다 — 토큰 한 줄 빼기 · 칩 색 어긋내기 · 없는 토큰 부르기 ·
`theme.js` 를 문서 끝으로 옮기기 · 테마 선택자를 한 글자 틀리기 ·
**불투명 로고로 되돌리기** · **스플래시를 옛 고정 흰색으로** · **테마는 타되 엉뚱한 색으로** ·
**챠콜의 테를 안 켜기**(②③ⓐ 셋 다) · **테를 선언만 하고 안 그리기**(②는 통과 · ⓐ③이 잡는다) ·
**a11y 의 면제를 «무조건»으로 + 그림자 제거**(ⓐ가 조용해지고 ③만 잡는다 —
③이 왜 있어야 하는지가 이 한 줄이다).

## 탐험 화면 — 일지는 «제목 줄», 동행·시간대는 «제 줄» (`.gt-head` · `.pal-line`)

예전에는 일지가 제 줄을 통째로 쓰고 동행 줄이 **지대 탭 아래**에 따로 있어서
카드 목록이 그만큼 밀렸다. 한때 셋을 **한 줄**로 몰아 봤는데 265px·영어에서
일지(그때 이름 「Field Journal」)만으로 130px 이라 동행 줄이 50px 로 눌려
「No companion creature」가 석 줄이 되고 띠가 76px 로 부풀었다.

지금은 **둘로 나눈다** (사람이 그림으로 자리를 정해 줬다):

- **제목 줄**(`.gt-head`) — 「🧭 탐험」 왼쪽 · **📔 저널** 오른쪽(`margin-left:auto`).
  일지는 얇고 늘 같은 자리라 제목의 남는 폭에 얹는 것이 맞다
- **동행 줄**(`.pal-line`) — 동행 + 시간대가 **제 줄을 통째로** 쓴다.
  둘 다 「지금 어떤 조건으로 나가는가」라 나란히 서야 읽힌다.
  **자리는 «필드 판 안», 지대 칩과 카드 목록 사이**다 (사람이 그림으로 정해 줬다) —
  「이 지대의, 이 동행으로, 이 시간대에」가 위에서 아래로 한 흐름이 된다
- ⚠️ **265px 영어에서 제목이 「🧭」/「Explore」로 갈렸다** — `.gt-head .screen-title`
  에 `white-space: nowrap`, 머리에 `flex-wrap` 을 준다. 그래도 2배 라벨에서는
  일지 버튼이 2px 넘쳐서 `flex: 0 1 auto; min-width: 0` 으로 줄어들게 했다.
  **줄어드는 것은 일지여야 한다** — 제목은 여기가 어디인지를 말한다
- ⚠️⚠️ **동행·시간대는 «필드 갈래»의 것이라 필드 판 «안»에 산다.** 한때 탭 «위»에
  두었는데, 거기서는 세 갈래에 다 걸려서 마을·밭에서도 떴다 — 데려갈 곳이 없는 자리라
  **누르는 순간 뜻 없는 시트**가 열린다. 그때는 `renderPalRow()` 가 `hidden` 으로
  접었지만 그건 **JS 가 지키는 약속**이라, 부르는 것을 한 번 빠뜨리면 조용히 어긋난다.
  지금은 `#gatherPanel-field` 안이라 **갈래가 바뀌면 판째로 사라진다 — 구조가 지킨다**
  · **접는 줄을 지웠다** (`line.hidden = …`). 안 쓰는 규칙을 남겨 두면 다음 사람이
    그것을 「아직 필요한 것」으로 읽는다 — 짝 CSS(`.pal-line[hidden]`)를 지운 것과 같은 자리다
  · **여백을 새로 주지 않는다** — 위는 `.zn-tabs` 의 12px, 아래는 이 줄의 8px 이 맡는다
  · `checkui` 의 **「동행줄갈래」**가 **그려진 높이**로 잰다 — `hidden` «속성»으로 재면
    「속성은 붙었는데 화면에는 그대로」인 상태를 못 본다 (이 저장소에서 세 번 난 사고다).
    자리를 판 안으로 옮긴 지금도 **그대로 쓴다**: 잣대가 「어떻게 접는가」가 아니라
    「화면에 남는가」라 방법이 바뀌어도 따라온다.
    **통과했을 때도 잰 값을 낸다**(`field 42px · village 0px · farm 0px`) — 아무 줄도
    안 남기면 0건이 「접힌다를 한 번도 안 쟀다」와 구별이 안 된다.
    사보타주로 확인했다 (탭 «위»로 되돌리면 「village 갈래에 42px 남아 있다」)
- **「동행 없음」이 아니라 「동행 크리처 없음」이다**(`pal_row_none`) — 무엇이 없다는
  것인지가 그 줄만 봐서는 안 읽혔다

### 건물 안은 «바닥 시트»다 (`#npcSheet` · UI_POLICY 1-1 뒤)

마을 건물을 누르면 **지도 위에 시트가 올라온다.** 예전에는 `#villageBody` 를 통째로
갈아 끼워 **지도가 사라졌고**, 나가는 길도 제 화살표(`‹`)를 따로 만들어야 했다.

- ⚠️⚠️ **버튼 줄과 「나가기」는 굴러가는 자리 «밖»이다** (`#npcActs` · 카드가 세로 플렉스).
  시트에 들어가는 것이 그림 · 대사 · 칩이라 **사람마다 높이가 다르다** — 오릭스처럼
  칩이 일곱이면 1080×1920 폰(CSS 로 **360×640**)에서 시트가 105px 넘치고,
  그때 밀려나는 것이 하필 **누르는 자리**였다. 그림과 칩은 굴려도 되고 버튼은 안 된다
- ⚠️ **짧은 화면에서는 «그림»이 먼저 줄어든다** (`@media (max-height:760px)` · `26vh`).
  글과 버튼은 줄일 수가 없고, 그림은 `slice` 라 상자가 낮아지면 잘려서 들어맞는다
- ⚠️ **닫는 문은 `closeNpcSheet()` 하나다.** 시트는 `position: fixed` 라 탭을 옮겨도
  그대로 떠 있는데 `render()` 는 탐험 탭일 때만 `renderGather()` 를 부른다 —
  **그리기에 기대면 마이 룸 위에 대장간이 뜬다.** `switchTab()` 이 그 자리에서 닫는다
- ⚠️ **`#giftSheet` 는 이 시트 «위»에 뜬다** — 층을 style.css 에 적어 둔다(55).
  DOM 순서로 풀면 다음에 시트를 하나 더 넣을 때 또 깨진다 (밭 시트에서 배운 자리다)
- ⚠️ **검사기의 선택자가 통째로 옮겨 갔다** — `#villageBody .ask-chip` 등 스물여섯 줄이
  `#npcBody` 로 간다. 지도(`.vil-pin`)만 `#villageBody` 에 남는다
- `checkui` 의 **「건물안작은폰」**이 화면을 **360×640 으로 줄여** 칩이 제일 많은
  사람으로 열고, 버튼 줄과 「나가기」가 화면 안인지 본다.
  ⚠️⚠️ **여는 것과 재는 것을 갈라 놓는다** — 같은 `evaluate` 안에서 열고 바로 재면
  `sheetup` 첫 프레임의 `translateY(28px)` 가 상자에 얹혀 **멀쩡한 화면이 걸린다**
  (78건 유령과 같은 종류다). 사보타주로 확인했다 (세로 플렉스를 없애면 둘 다 화면 밖)

#### ⚠️ 선물 시트(`#giftSheet`)도 «같은 조리법»이다 — 붙박이 몫은 반드시 자란다

같은 사고가 선물 시트에도 있었다. 거기는 **물약 줄만** `max-height: 46vh` 로 묶어
두었는데, 그러면 카드 높이가 `붙박이 + 0.46vh` 이고 **붙박이 몫은 머리줄(♥ 진행 ·
좋아하는 등급 · 「그가 주는 것」)이 접히는 만큼 늘어난다** — 재 보니 **420px 아래에서
「나가기」가 화면 밖**이었다 (가로로 든 폰 640×360 에서 399 / 360).

지금은 건물 안 시트와 **글자 그대로 같은 네 줄**이다 — 카드 세로 플렉스 +
`overflow: hidden` · `#giftBody` 만 `overflow-y: auto`. **한 시트를 고칠 때 다른
시트도 같이 눈에 들어오게** 하려고 일부러 같은 모양으로 적었다.

- ⚠️ **`.gift-list` 에 `max-height`·`overflow` 를 도로 넣지 않는다** — 굴리는 자리가
  둘이 되어 손가락이 안쪽 목록에 갇힌다 (사보타주로 확인했다)
- ⚠️ **짐을 지는 것은 두 줄뿐이다** — `flex: 0 0 auto` 와 `min-height: 0` 은 지금
  한 픽셀도 안 바꾼다(떼고 재 봤다). 주석에 그렇게 적어 뒀다: **안 하는 일을 한다고
  써 두면 다음 사람이 그것을 믿고 원인을 엉뚱한 데서 찾는다**
- ⚠️⚠️ `checkui` 의 **「선물시트작은폰」**은 **360×640 에서만 재면 이 사고를 못 잡는다** —
  옛 CSS 도 거기서는 통과한다. **가르는 자리는 «낮은 화면»**이라 사람이 말한 폰과
  그것을 가로로 든 것(640×360)을 둘 다, **두 언어로**(영어 머리줄이 한 줄 더 접힌다),
  **선물할 수 있는 사람 여섯을 다** 돈다. 통과할 때도 몇 번 쟀는지를 낸다(24번)

### 맵 목록은 «한 덩어리»다 (`.gt-box`)

탭(필드/마을/밭) · 지대 칩 · 카드 목록은 **「여기서 갈 수 있는 곳」 하나**를 말하는
것들인데, 각자 화면 배경 위에 떠 있어서 **서로 다른 덩어리로 읽혔다**
(마이 룸 인벤토리에서 이미 겪고 `.room-inv` 로 정한 규칙이다 · 사람이 그림으로 짚어 줬다).

- ⚠️ **`.room-inv` 조리법을 «그대로» 못 베낀다.** 거기는 「바깥 카드 하나만 그림자를
  갖고 안쪽은 크림색 평면」인데, 여기서는 **크림색 + 그림자 없음이 이미 「잠김」의 뜻**이다
  (`.spot-card.locked`). 안쪽을 그렇게 깔면 **잠긴 카드와 안 잠긴 카드가 구별이 안 된다.**
  그래서 **안쪽은 한 글자도 안 건드리고 바깥 판만** 깐다
- ⚠️ **반투명 «흰색»이라야 한다.** 페이지 배경이 분홍→라벤더→민트 그라데이션이고
  `background-attachment: fixed` 라 **스크롤하면 판 뒤가 바뀐다** — 라벤더로 물들이면
  민트 구간에서 어긋난다 (넷을 진짜 화면에 입혀 찍어 놓고 골랐다)
- ⚠️⚠️ **음수 여백으로 판만 바깥으로 내보낸다 — 카드는 한 픽셀도 안 움직인다.**
  안쪽 여백만 주면 카드가 좌우로 좁아져 **설명이 접힌다**(「하늘을 그대로 비추는 저 /
  수지.」). 그리고 **음수 여백 = 안쪽 여백 + 테두리**(8 + 1)다 — 테두리 1px 을 빼먹어
  좌우 1px 씩 좁아졌더니 **그 2px 에 글줄이 바뀌었다.** 판을 씌우는 일이 글을 바꾸면 안 된다
  · 판을 껐다 켜서 **같은 화면에서 카드 폭을 견줘** 확인했다 (390·265·480px 에서
    358 = 358 · 233 = 233 · 448 = 448). 「고친 게 진짜인지 보려면 되돌려 한 번 더 재라」
  · `.screen` 의 좌우 여백 16px **안쪽으로만** 나가므로 가로 스크롤이 안 생긴다
    (세 폭에서 `scrollWidth` 넘침 0)

## 채집 — 맵마다 «형»이 있고, 형이 미니게임을 정한다

「AP 를 내면 재료 하나」만으로는 채집이 심심하다. 그래서 **맵에 형(field type)을 붙이고,
형이 「여기서 채집이 어떤 모양인가」를 정한다** — 그냥 줍는 곳(`field`)인지,
호두밭(`walnut` · 호두 게임)인지, 호박 밭(`pumpkin` · 호박 피하기)인지,
바위산(`rock` · 돌깨기 게임)인지, 낚시터(`fish` · 낚시)인지, 밀밭(`sparrow` · 참새 쫓기)인지,
바람개비 밭(`pinwheel` · 바람개비 퍼즐)인지, 소풍 바위(`driller` · 바위 부수기)인지.

- **속성과 다른 축이다.** 속성(`MAP_ATTRS`)은 「이 땅의 기운」이라 동행 크리처가 맞는지를
  정하고, 형은 채집의 «모양»이다. 한 맵에 둘이 같이 붙어 있다
- ⚠️ **미니게임을 «한 표»에서만 정한다** (`MAP_TYPES` → `FIELD_TYPES` → `D.fieldMini()`).
  예전에는 맵 줄에 `mini: 'pumpkin'` 을 손으로 적어 두었는데, 형이 생기면 그것이
  **두 번째 경로**가 되어 한쪽만 고치게 된다 (지대별 AP 에서 배운 것과 같다).
  그래서 `mini` 칸을 없앴다 — `gather()` 도 `D.fieldMini(mapId)` 한 줄만 본다
  · ⚠️ 칸을 없애면 **그 칸을 보던 조건이 «늘 참»이 된다.** `checkgather`·`checklore` 의
    `!m.mini`(미니게임 맵을 피하려던 조건)가 그대로 남아 아무것도 안 거르고 있었다 —
    우연히 첫 맵이 평범한 곳이라 통과했을 뿐이다. 둘 다 `!D.fieldMini(m.id)` 로 고쳤다
- ⚠️ **미니게임은 «이름이 그 게임인 곳»에만 붙는다** (`tools/genmaptype.js`).
  「호두 마루」에서 호두를 줍고 「파수꾼의 호박 밭」에서 호박을 피하고 「흔들 바위산」에서
  돌을 깨고 「거울 저수지」에서 낚고 「노을 밀밭」에서 참새를 쫓고 「바람개비 밭」에서
  바람개비를 날리고 「소풍 바위」를 판다 — 카드를 보면 무슨 게임인지 이미 알 수 있고,
  맵을 늘려도 규칙이 저절로 번지지 않는다. **지금 51곳 중 일곱뿐이다**
  (호두밭 1 · 호박 밭 1 · 바위산 1 · 낚시터 1 · 밀밭 1 · 바람개비 밭 1 · 소풍 바위 1).
  · ⚠️ **규칙은 «그 맵 하나를 가리킬 만큼» 좁아야 한다.** 바위산을 `바위` 로 잡으면
    「소풍 바위」·「파도 바위」·「인어의 바위」·「도마뱀 바위굴」까지 딸려 와 **다섯 곳**이
    된다 (그렇게 해 보고 `--list` 로 셌다). 지금은 `바위산` 과 `소풍 바위` 다 —
    **바위가 붙은 맵이 다섯인데 그중 둘에만** 게임이 붙어 있다
  · ⚠️ **한때 재료 풀로도 뽑았다** — 「열매가 셋 이상이면 과수원」. 그러니 아홉 곳이
    미니게임이 됐고(숲은 열 곳 중 아홉), 그 규모에서는 「과수원 형」이 사실상
    **「숲」의 다른 이름**이었다. 게다가 2분짜리를 아홉 곳에서 시키면
    「꾹 누르기 자동 채집」이 사실상 사라진다 — 코지 게임에서 그건 숙제다.
    **미니게임을 늘릴 때도 규칙에 한 줄을 적어 «어느 맵인지 눈에 보이게» 한다**
  · **미니게임 맵이 30% 를 넘으면 실패시킨다** — 위의 이유를 못 박아 둔 자리다
  · **미니게임마다 갈 곳이 하나는 있어야 한다** — 이름 규칙이 어긋나면 그 게임이
    붙은 맵이 없어지는데, 화면에는 아무 오류도 안 뜬다. 만들어 놓은 게임에 영영 못 닿는다
  · ⚠️ **미니게임 하나는 «처음부터» 열려 있어야 한다** (`unlock: 0` · 지금 호두 마루).
    미니게임 맵이 둘뿐이라 둘 다 매력을 모아야 열리는 곳이면 **새 플레이어는 한참 동안
    미니게임을 한 번도 못 본다** (사보타주로 확인했다)
  · ⚠️ **옛 배정을 새로 뽑지 않는다** (`PINNED` · genwardrobe 의 `LEGACY` 와 같은 규칙).
    둘 다 이미 미니게임이 붙어 나간 맵이라, 규칙을 고치다 형이 바뀌면
    **그 맵의 채집이 조용히 다른 게임으로 바뀐다**
- ⚠️⚠️ **튜토리얼이 가리키는 맵은 반드시 평범한 곳이어야 한다** (`checkdata`).
  9단계가 「가서 두 개 주워 오세요」인데 그 맵이 미니게임 맵이 되면 버튼을 눌러도
  **2분짜리 게임이 뜨고 튜토리얼은 그 자리에서 멈춘다** — 새 플레이어가 게임을
  시작조차 못 하는 종류다. `tutorial.js` 에 **써진 선택자**를 읽어 본다
  (`p_hill` 을 미니게임 맵으로 만드는 사보타주로 확인했다)
- **카드에 «어느 게임인지»를 적는다** (`FIELD_TYPES.tag`). 예전에는 「스페셜 맵」
  한 마디라 무엇이 특별한지를 **AP 를 내고 들어가 봐야** 알았다 — 2분짜리 게임이
  갑자기 뜨는 것은 안내가 아니다. `checkdata` 가 「미니게임인데 딱지가 없다」를 잡는다
- **그 딱지가 곧 ❔ 「규칙 보기」 버튼이다** (`.spot-badge` → `openMiniHelp(형)`).
  이름만으로는 어떻게 하는 게임인지 모르는데, **2분짜리를 AP 를 내고 들어가서야
  배우는 것은 안내가 아니다** — 그래서 **AP 를 내기 «전»에** 읽는 자리에 둔다.
  게임 «안»에 두지 않는 이유도 같다: 거기서 읽는 동안 시계와 산소가 그대로 흐른다
  · 시트는 다른 팝업과 같은 **바닥 시트**다 (`#miniHelpSheet` · 「나가기」)
  · ⚠️⚠️ **물음표는 «이모지가 아니라 글자»다** (`?` + 짙은 동그라미 · `.spot-badge > i`).
    처음에는 `❔`(U+2754)를 썼는데 그 글리프는 **흰/옅은 회색 물음표**라 연주황 딱지
    위에서 거의 안 보였다 — 「진짜 안 띈다」로 신고받았다. 더 나쁜 것은
    **그림 글자라 `checkTextStyle()` 이 대비를 안 잰다**는 것이다: 안 보이는 채로
    0건이 나왔다 (하트 ♥ 가 3.56:1 이어도 0건이던 것과 같은 자리다).
    진짜 `?` 글자로 바꾸자 **검사가 비로소 그것을 본다**(지금 6.46:1) —
    색을 딱지 색으로 돌리는 사보타주로 확인했다 (`선택자 i · 텍스트 ? · 대비 1.34`).
    **화면에서 뜻을 지는 기호는 이모지로 두지 않는다**
  · ⚠️ **줄에 요율·상한을 «숫자로 박지 않는다»** — `miniHelpVars()` 가 그 게임의
    모듈에서 읽어 `{per}`·`{max}`·`{lives}`·`{hits}` 로 넣는다. 「최대 18개」라고
    적어 두었다가 `REWARD_PER` 를 100 → 55 로 내린 날 **화면에 적힌 값과 받는 개수가
    갈렸고**, 호두밭은 상한이 20인데 규칙에 18이라고 적혀 있었다 (카드의 ⚡ 와 받는
    개수가 갈리던 `miniRewardMax` 와 같은 사고다). `checkdata` 가 **`{max}` 가
    없으면** 잡는다 (사보타주로 확인했다)
  · ⚠️ **「두 언어의 줄 수가 같은가」는 `checkdata` 에서 잴 수가 없다** — `I18N.t()` 가
    없는 열쇠를 **한국어로 떨어뜨려** 줘서 영어가 빠진 것이 안 보인다 (그렇게 짜 놓고
    사보타주가 그대로 통과했다). 그쪽은 `npm run test:i18n` 이 사전을 직접 읽어 잡는다
  · ⚠️ **`checkui` 는 일곱을 «다» 연다** — 하나만 재면 제일 긴 규칙(바위 부수기 여덟 줄)은
    한 번도 안 잰 것이 된다. `__cardFits` 도 같이 부른다 (시트 속의 줄은 그냥 `div` 라
    `checkLayout()` 의 선택자에 안 걸린다 — 밭 시트에서 배운 자리다)
- **그 딱지 옆에 📄 「지난 기록」이 붙는다** (`.spot-log` → `openMiniLog(맵)`).
  ❔ 가 「무슨 게임인가」라면 이쪽은 **「여기서 내가 얼마나 했나」**다 — 최근 열 판과
  총 판 수·최고 기록. 미니게임은 실력이 느는 것이 재미인데 **한 판이 끝나면 숫자가
  통째로 사라져서**, 나아지고 있는지를 알 방법이 없었다.
  · **머리말은 «맵 이름»이다** — ❔ 는 딱지 이름(「호두 게임」)이 맞지만 기록은
    「여기서 내가 한 일」이라 장소 이름이라야 어디 것인지 안 헷갈린다
  · ⚠️ **점수의 «단위»는 형마다 다르다** (`mr_unit_<형>` — 호두 «개» · 호박 «초» ·
    바위산 «줄» · 낚시 «마리» · 참새 «점» · 바람개비 «개» · 드릴러 «m»).
    한 낱말로 못 적으니 **단위는 읽을 때 붙이고 기록에는 «숫자»만 적는다** —
    적을 때 붙이면 요율을 고친 날 옛 줄만 옛 단위로 남는다.
    `checkdata` 가 형마다 그 열쇠가 있는지, `{n}` 이 들어 있는지를 본다
    (사보타주 둘로 확인했다 — 줄을 지우면 「열쇠가 그대로 뜬다」 · `{n}` 을 빼면
    「점수가 통째로 안 보인다」)
  · ⚠️⚠️ **미니게임은 끝낼 때 «점수»를 같이 돌려줘야 한다** (`cb({ picked, cleared, score })`).
    빠뜨리면 `miniLogAdd` 가 0 으로 적어 **모든 줄이 「0개」로 쌓인다** — 오류도 안 나고
    기록만 조용히 거짓말을 한다 (호박 밭이 실제로 그랬다 · 거기만 「버틴 초」라
    `S.lived` 를 따로 적어 두어야 했다). `checkdata` 가 형마다 그 파일의 **모양**을
    본다 — 값이 아니라 모양이라 사본이 생길 데가 없다 (사보타주로 확인했다)
  · ⚠️ **적는 문은 `miniLogAdd()` 하나이고, `finishMiniRun()` 에서만 부른다.**
    게임마다 제 자리에서 적으면 곧 일곱 벌이 되고, 한 벌만 빠져도 그 맵만 조용히
    기록이 안 쌓인다 (`finishMiniRun` 이 퀘스트를 세는 것과 같은 자리다)
  · **`S.miniLog` 는 «더하는» 칸이라 `SAVE_VER` 를 안 올린다** — 없으면 빈 객체로
    읽히고, 옛 세이브는 다음 한 판부터 쌓인다. 최근 열 판만 들고 있는다(`MINILOG_MAX`)
  · ⚠️⚠️ **`checkui` 는 값을 심어 `openMiniLog()` 를 «직접» 부른다** — 대비·넘침을
    재는 자리라 그것이 맞지만, 그러면 **📄 의 배선도 「기록이 실제로 적히는지」도
    한 번도 안 본다** (`Walnut.start()` 를 직접 부르면 안 되는 것과 같은 구멍이다).
    그 둘은 **한 판을 진짜로 노는 `checkwalnut`** 이 본다 — 판이 끝난 뒤 기록이
    한 줄 늘었는가 · 점수가 그 판의 점수와 같은가 · **카드의 📄 를 눌러** 열리는가 ·
    단위가 열쇠로 새지 않는가. 사보타주 셋으로 확인했다 (`miniLogAdd` 를 빼면 3건 ·
    `onclick` 을 끊으면 2건 · 단위를 지우면 1건)
  · ⚠️ **`checkui` 는 «두 모양»을 다 연다** — 기록이 있는 쪽과 **한 판도 안 한 쪽**
    (「아직 여기서 논 적이 없어요」). 빈 쪽만 재면 줄의 대비·넘침을 한 번도 안 잰
    것이고, 있는 쪽만 재면 새 플레이어가 처음 여는 화면을 한 번도 안 잰 것이다
  · ⚠️⚠️ **딱지가 둘이 되면서 오른쪽 배지(날씨·속성)와 «겹쳤다».** 둘 다 절대 배치라
    265px 영어에서 최대 40px 포개져 **📄 를 눌러도 날씨 배지가 클릭을 먹었다** —
    화면에는 두 개가 멀쩡히 서 있고 손가락만 안 먹는 종류다.
    `.spot-badges { max-width: calc(100% - 100px) }` + `.sb-name` 말줄임으로 막았다.
    `checkui` 의 **「딱지겹침」**이 지대 다섯을 돌며 **두 상자의 틈**을 직접 잰다
    (통과해도 「7장 · 제일 좁은 틈 22px」로 잰 것을 낸다). 사보타주로 확인했다
    (`max-width` 를 떼면 「p_windmill 가 11px 겹친다」)
- **끝난 뒤 처리는 `finishMiniRun()` 한 함수다.** 게임이 늘 때마다 `gather()` 에
  `if` 를 붙이면 곧 갈래가 다섯이 된다. ⚠️ 여기서 **퀘스트의 「채집 n번」을
  한 걸음 센다** — 안 세면 호두밭만 도는 사람은 `q_walk`(채집 8번)에서 영영 안 나아간다

### 호두 게임 (`walnut.js`) — 합이 10

숫자가 적힌 호두 **10×12 = 120개**가 깔리고, **드래그한 네모 안의 합이 정확히 10이면**
지워진다. 2분 동안 주운 호두가 곧 점수이고, **점수가 그대로 재료가 된다**
(`REWARD_PER` 5개마다 1개 · 상한 20). 붙어 있는 맵은 **「호두 마루」 하나**다.

- ⚠️ **한 게임에 이름이 하나여야 한다.** 원래 「사과 게임」(`apple.js` · `window.Apple` ·
  `ap_*` · `.ap-*`)이었는데, 호두밭 하나만 남기면서 **파일·전역·문자열 열쇠·CSS 를 다
  같이** 바꿨다 — 화면만 「호두 게임」으로 고치면 다음 사람이 두 이름을 오간다.
  · 덤으로 **`ap_*` 의 겹침이 풀렸다** — 행동력(AP)이 이미 `ap_full`·`ap_charge_*`·
    `.ap-chg-*` 를 쓰고 있어서 같은 앞가지가 두 뜻이었다. 지금은 `wn_*` · `.wn-*` 다
- ⚠️ **틀려도 잃는 것이 없다.** 합이 안 맞으면 잠깐 흐려질 뿐이다 — 흐린 장의
  「틀려도 재료가 한 톨도 안 없어진다」와 같은 결이다. 벌을 주면 2분 내내 손이 굳는다
- **지워진 칸을 건너뛰어 고를 수 있다** — 살아 있는 호두만 세므로 판이 비어 갈수록
  멀리 있는 것끼리 묶인다. 그것이 이 게임의 재미라 검사가 콕 집어 잰다
- ⚠️ **시작하자마자 막힌 판을 주지 않는다** (`MIN_MOVES` 6). 깔고 나서 «지울 수 있는
  네모»를 세어 모자라면 다시 깐다 — 2분짜리에서 첫 판이 막혀 있으면 그냥 빼앗긴 2분이다
- ⚠️ **큰 화면에서 호두를 더 주지 않는다.** 격자를 10×12 로 «고정»하고 칸 크기만
  줄인다 — 화면에 맞춰 늘리면 **화면 크기가 곧 보상**이 된다
- ⚠️ **프레임마다 다시 그리지 않는다.** 물리가 없어서 «바뀔 때만» 그린다 (호박 밭은
  매 프레임이 맞다 — 거기는 호박이 굴러온다)
- **드래그 중에 네모의 합을 띄운다.** 안 보여 주면 머릿속으로만 더해야 해서
  2분 동안 눈이 아니라 암산이 게임이 된다.
  · ⚠️ **어두운 알약 위에 얹는다.** 흰 글자로만 두었더니 네모 위쪽에 밝은 타일이
    오는 순간 **통째로 사라졌다** — 캔버스라 대비 검사가 못 본다
  · ⚠️ **칸을 가리지 않는 자리에 둔다.** 네모 바로 위에 얹었더니 **윗 칸의 숫자를
    덮었다** — 지금 고르려는 것을 가리는 표시다. 판이 가운데 정렬이라 아래에
    빈 띠가 남으므로 거기에 두고, 화면이 짧아 띠가 없을 때만 네모 위로 올린다

#### 타일은 «숫자가 적힌 호두»다

- ⚠️ **숫자가 주인공이다.** 껍데기의 무늬가 숫자와 겹치면 둘 다 안 읽힌다 —
  **넷을 진짜 칸 크기(38px)로 그려 놓고 골랐다** (짐작으로 고르지 않는다 ·
  발등 곡선에서 배운 것과 같다):
  · 동그라미 + 세로 이음매 → 이음매를 숫자가 덮어 **그냥 동그라미**가 된다
  · 타원 + 좌우 주름 · 반쪽 호두(뇌 모양) → 무늬가 숫자를 가로지른다
  · **호두 실루엣 + 반쪽 음영 + 이음매 «끝»** ← 이것
- **두 쪽을 «선이 아니라 면»으로 나눈다.** 왼쪽 반쪽만 조금 어둡게 칠하면 갈라진
  호두로 읽히면서 **숫자를 한 점도 안 가린다.** 이음매는 숫자가 앉지 않는 위아래 끝에만
- ⚠️ **꼭지를 한쪽으로 기울이지 않는다** — 벌어짐을 주면 실루엣이 **비뚤어 보인다**
  (0.18~0.45 를 그려 보고 대칭인 0 으로 되돌렸다)
- ⚠️ **한때 «그 맵에서 나는 것»(이모지)을 깔았다.** 과수원이 여덟 곳이던 때라
  「여기가 어디인지」가 필요했는데, 맵이 하나가 되면서 그 이유가 사라졌다 —
  호두밭에는 호두가 깔리는 것이 맞다 (`S.em` 과 `newEmojis` 를 통째로 지웠다)
- ⚠️ **결과 문구에 다른 열매 이름을 쓰지 않는다** — 화면에 호두를 깔아 놓고
  「사과 7개를 땄다」는 거짓말이다. `checkwalnut` 이 결과 글을 훑어 잡는다

### 돌깨기 게임 (`rock.js`) — 테트리스

돌조각이 내려온다. **한 줄이 다 차면 그 줄이 깨져 사라지고**, 2분 동안 깬 줄이 곧
점수이자 재료다 (`REWARD_PER` 한 줄에 1개 · 상한 18). 붙어 있는 맵은 **「흔들 바위산」 하나**다.

- **손가락 하나로 다 한다** — 끌면 옮기고, 톡 치면 돌리고, 아래로 쓸면 떨어뜨린다.
  ⚠️ **화면에 버튼을 깔지 않는다**: 판이 작아지고 엄지로 가릴 자리도 는다
- ⚠️ **일곱 조각을 알록달록하게 칠하지 않는다.** 여기서 쌓는 것은 블록이 아니라 «돌»이라
  무지개로 칠하면 바위산에 사탕이 쌓인다. 화강암·사암·점판암처럼 **돌의 결 안에서만** 가른다
- ⚠️ **떨어질 자리를 미리 보여 준다**(그림자). 안 보여 주면 빨라진 판에서
  「한 칸 옆이었는데」가 되풀이되어 손이 굳는다
- ⚠️ **끝에 가서 손을 못 쓸 만큼 빨라지지 않는다** (`DROP_START` 780 → `DROP_MIN` 420).
  코지 게임이라 「점점 어려워지는 맛」이면 되고, 벌은 이미 «시간»이 준다
- ⚠️ **바닥에 닿고도 `LOCK_MS` 만큼은 밀어 넣을 수 있다.** 그 틈이 없으면
  마지막 한 칸을 눈으로 보고도 못 넣는다
- **다음에 올 조각 하나를 미리 보여 준다** (판 오른쪽 위 · 「다음」 딱지와 함께).
  · ⚠️ **«종류»가 아니라 조각 그 자체를 들고 있는다**(`S.next = makePiece(...)`).
    종류만 들고 있다가 나올 때 새로 만들면 얼룩이 달라져 **보여 준 돌과 다른 돌**이
    내려온다. `checkrock` 이 **다섯 번 이어서** 「보여 준 것이 그대로 오는가」를 본다 —
    한 번만 보면 일곱 중 하나로 우연히 맞는다 (사보타주로 확인했다: 2/5회)
  · ⚠️ **놓을 자리를 «미리 떼어 놓는다»**(`fit` 의 `HUD_H`·`NEXT_K`·`NEXT_GAP`).
    남는 자리에 그리면 화면이 짧을 때 조각이 판을 덮거나 HUD 를 파고든다 —
    가운데 정렬로 되돌리면 띠가 **−9px 부터**가 되어 검사가 잡는다.
    띠의 높이가 칸 크기에 딸려 있으므로 한 식으로 푼다:
    `cell ≤ (h − 여백 − HUD − 안내줄 − 틈) / (ROWS + 2·NEXT_K)`
  · ⚠️ **판 «안»에 겹쳐 놓지 않는다** — 쌓인 돌과 섞여 「저건 뭐지」가 되고,
    HUD 줄에 끼우면 맵 이름·점수·시계를 밀어낸다 (이름이 긴 맵에서 먼저 터진다)
- **천장까지 차면 그 자리에서 끝난다** — 깬 줄만큼은 그대로 가져간다 (`rk_buried`).
  빈 판을 남은 시간 동안 보고 있을 이유가 없는 것과 같은 결이다

#### 돌이 «깨져» 보이게 하는 것 셋

줄이 사라지는 것만으로는 「줄이 찼다」가 안 읽힌다. 셋이 같이 있어야 돌로 보인다:

1. **파편** — 깨진 칸마다 `BURST_PER` 조각이 튀어 중력을 받고 사라진다
2. **먼지** — 그 «줄»에 밝은 띠가 한 번 인다.
   ⚠️ **파편만으로는 모자랐다** — 한 프레임을 멈춰 놓고 보니 파편이 그냥 «작은 돌»로
   보여 판이 지저분해진 것처럼 읽혔다 (찍어 보고 알았다). 먼지가 있어야 **어디가
   깨졌는지**가 보인다. 파편도 그때 같이 작게 줄였다
3. **흔들림** — 판이 `SHAKE_MS` 동안 흔들린다.
   ⚠️ **그리기에만 건다** — 칸 ↔ 화면 셈(`S.ox`)을 흔들면 손가락이 가리키는 칸이 어긋난다

- ⚠️ **돌의 얼룩은 씨앗이 정한다** (`rnd(seed, i)`). 매 프레임 새로 뽑으면 돌이
  지글거린다 — 그리고 **씨앗은 칸을 따라다닌다**: 줄이 깨져 내려앉을 때 얼룩이
  제자리에 남으면 위의 돌들이 통째로 다른 무늬가 된다 (호두 게임의
  「칸의 그림은 한 번 정해지면 안 바뀐다」와 같은 규칙이다)
- ⚠️ **회전 상태를 네 벌씩 적어 두지 않는다.** `n`×`n` 상자를 돌리는 식 하나만 두면
  한 벌만 고쳐서 어긋나는 일이 없다. 벽에 붙었을 때 한 칸 미는 «킥»까지만 넣는다

### 낚시 (`fish.js`) — 스타듀 밸리의 그 씨름

**기다림 → 입질 → 씨름 → 건져 올림**, 네 걸음이 2분 동안 돈다. 씨름은 스타듀 밸리의
것을 옮겨 왔다 — 세로 줄 안에서 «그물»이 중력에 눌려 내려오고, **화면을 누르고 있으면
올라간다.** 그물 «안»에 물고기를 붙들고 있으면 옆의 눈금이 차고, 놓치면 빠진다.
한 번 건질 때마다 재료 두 개 · 상한 18개. 붙어 있는 맵은 **「거울 저수지」 하나**다.

- ⚠️ **누르면 «올라가는» 것이 아니라 «위로 힘을 준다».** 바로 따라오게 하면 씨름이 아니라
  슬라이더가 된다 — 이 손맛이 스타듀 밸리 낚시의 전부다
  · ⚠️ **감쇠(`DAMP`)가 낮으면 «물 위의 배»가 된다.** 0.0016 에서는 손을 떼고도 0.6초를
    더 올라가서, 눈으로 보고 놓아도 이미 늦었다 (검사가 「놓으면 내려온다」로 잡았다).
    0.003 이면 되돌리는 데 0.2초라 「붙들고 있다」가 손에 남는다
  · ⚠️ **「꼭대기에 안 붙는다」를 «0.03 보다 크다»로 재면 못 가른다** — 그물은 제 높이의
    절반(0.12)보다 위로 못 올라가므로 **바로 붙어도 그 값**이다. 한계(`barH/2`)에
    닿았는지로 재야 슬라이더가 걸린다 (사보타주로 확인했다 — 옛 잣대는 통과시켰다)
- ⚠️ **빠지는 쪽이 차는 쪽보다 느리다** (4초 : 9.2초). 같게 두면 한 번 놓칠 때마다
  그만큼 되돌아가 2분이 «본전 찾기»가 된다 — 코지 게임에서 그건 벌이다
- ⚠️ **난이도는 「손익분기」 하나로 말할 수 있다** — 눈금이 안 줄어들려면 씨름하는 동안
  그물 안에 `CATCH_MS / (CATCH_MS + DRAIN_MS)` 만큼은 붙들고 있어야 하고, 그 아래면
  0 으로 떨어져 놓친다. **「어렵다」를 짐작으로 고치지 않고 사람 셋을 붙여 쟀다** —
  반응 속도만 다르게 준 봇 셋(150 · 280 · 420ms)을 진짜 게임에 2분 붙인다:
  · 옛 값(6400)의 분기는 **38.5%** 인데 보통 36% · 느긋한 35% 라 **평균이 분기 아래**였다
    (2분에 건짐 1 · 놓침 2~3). 「너무 어렵다」가 정확히 이 수치다
  · 넷을 같이 고쳤다 — `DRAIN_MS` 9200(분기 **30.3%**) · 그물 `BAR_H0` 0.24→**0.30**
    (좁히는 몫 `BAR_SHRINK` 0.012→0.008 · 바닥 0.15→0.22) · 물고기 속도 0.00042→0.00034 ·
    **마음 바꾸는 간격** 420~1100 → **620~1500ms**
  · 지금은 셋 다 **놓침 0** 이고, 2분에 건짐 15 / 8 / 5 다 — **잘하면 «더 많이», 못해도
    «잃지는 않는»** 모양이다 (상한 18개는 여전히 빠른 손만 닿는다)
  · ⚠️ **간격(`FSH_T_*`)이 속도보다 어렵게 만든다.** 420ms 마다 자리를 새로 뽑으면
    사람이 따라붙는 데 드는 시간(반응 + 그물을 미는 시간)보다 짧아, 쫓아가는 도중에
    이미 다른 데로 간다 — 아무리 잘해도 못 따라잡는 구간이 생긴다
  · ⚠️ **한 바퀴(`BITE_MIN`+`CATCH_MS`+`LAND_MS`)는 못 줄인다** — `checkbalance` 의
    「상한이 «쉬지 않고 건진 사람»의 것이다」가 9번 × 5.9초 ≥ 2분의 40% 를 지킨다.
    쉽게 만들 때는 **분기와 그물**을 건드린다 (그 셋은 그대로 둔다)
- ⚠️ **놓쳐도 잃는 것이 없다.** 건진 수는 그대로고 그저 다시 기다린다
  (흐린 장의 「틀려도 재료가 한 톨도 안 없어진다」와 같은 결이다)
- ⚠️ **아무 때나 눌러도 벌이 없다** — 입질이 아닐 때 누르면 아무 일도 안 일어난다.
  · ⚠️ **채는 문은 `hook()` 한 곳이다.** 손가락 쪽에서 `phase === 'bite'` 를 한 번 더
    보고 있었는데, 그러면 **문이 둘이라 한쪽 빗장을 빼도 다른 쪽이 막아 준다** —
    사보타주가 그대로 통과했다 (잠긴 표현을 「값으로 베껴」 두던 것과 같은 사고다)
- **씨름을 시작할 때 눈금이 0.4 차 있다** — 0에서 시작하면 손이 닿기 전에 놓친다
- **걸음마다 안내 한 줄이 바뀐다** (`fs_hint_*`). 넷을 한 줄에 다 적으면 아무도 안 읽는다.
  · ⚠️ `checkui` 는 **제일 긴 줄(씨름)로 못 박고 잰다** — 그냥 재면 그때 서 있던 걸음의
    문구만 재는 것이라 제일 긴 줄은 영영 안 잰다 (부엌의 「오늘의 한 마디」와 같은 구멍이다)

#### 물빛 — 「하늘을 그대로 비추는 저수지」

- 깊은 청록 그라데이션에 **비친 달과 별**, 그것을 흔드는 잔물결. 저수지가 거울이라는
  맵 설명을 그대로 그린 것이다
- ⚠️ **아래가 텅 비면 물이 아니라 그냥 «색»이다** — 갈대와 수련잎 실루엣을 깔았다
  (찍어 보고 넣었다). 물속에 잠긴 것이라 잎맥도 무늬도 없다
- ⚠️ **찌가 작으면 입질을 놓친다.** 7.5px 로 두었더니 390px 화면에서 점 하나였다 —
  **챌 순간을 알려 주는 것이 이 그림의 일**이라 크기가 곧 안내다 (11px 로 키우고,
  입질에는 기울고, **물결이 0.26초마다 계속 인다** — 한 번만 일면 늦게 본 사람은 못 본다)
- ⚠️ **물고기가 그물보다 작아야 한다.** 그물 폭의 0.66 으로 두었더니 꽉 차서
  「안에 붙들었다」와 「가장자리에 걸쳤다」가 **눈으로 안 갈렸다** (0.52 로 줄였다)
- **그물은 붙들고 있으면 금빛, 놓치면 크림빛**이다 — 「지금 되고 있다」를 색으로 말한다
- ⚠️ **캔버스를 «성기게» 훑어 세지 않는다.** 97픽셀마다 밝은 점을 세어 「스무 점 넘는가」로
  두었더니 그물·물고기가 어디 있느냐에 따라 19 ↔ 22 로 흔들려 **네 번에 한 번씩 거짓으로
  빨개졌다** (실제로 겪었다). 줄 상자를 **빽빽하게** 세면 875 ± 10 이라 흔들릴 수가 없다

### 참새 쫓기 (`sparrow.js`) — 노을 밀밭

참새가 밀 이삭에 앉아 쪼아 먹는다. **누른 자리로 화살이 날아가고**, 맞으면 「팡!」 하고
그 자리에서 사라진다. **30초** 동안 번 «점»이 곧 재료다 (`REWARD_PER` 12점마다 1개 ·
상한 6). 붙어 있는 맵은 **「노을 밀밭」 하나**다.

- ⚠️ **다섯 중 이것만 30초다.** 「2분은 길다」고 해서 줄인 자리인데, **한 판 최대를 같이
  내리지 않으면 나머지 넷이 죽는다** — 같은 AP 를 내고 4분의 1 시간에 같은 것을 받기
  때문이다. `checkbalance` 를 「같은 시간」에서 **「분당 같은 자리」**로 바꾼 것이 그 몫이다
- ⚠️ **남은 시간은 «숫자 + 막대»다** (`.sw-timer` · `.sw-tbar`). 30초짜리에서 작은 숫자
  하나로는 안 보인다 — 크게 띄우고 어두운 알약 위에 얹고, 마지막 10초(`HURRY_MS`)는
  붉게 뛴다 (「타이머 잘 보이게」로 신고받은 자리다)
- ⚠️ **틀려도 · 아무 데나 눌러도 잃는 것이 없다.** 빗나간 화살은 그냥 밭에 꽂히고,
  제 발로 날아간 참새는 「놓침」으로만 센다 — **번 점은 한 톨도 안 줄어든다**
  (흐린 장의 「틀려도 재료가 한 톨도 안 없어진다」와 같은 결이다)
- ⚠️ **연사를 막는 것은 «벌»이 아니라 활을 당기는 시간이다** (`SHOT_MS` 600).
  활이 «휘는 것»과 **시위에 걸린 화살**로 그것을 말한다 — 글자로 적지 않는다
- ⚠️ **처음에는 「쫓는다」였다** — 화살이 참새를 안 맞히고 밭에 꽂혀 놀래키는 그림이었다.
  「팡 하고 터지며 사라지게 · 멀티킬 이름을 띄우게」로 바뀐 것은 **사람이 내린 결정**이다.
  문구만 부드럽게 되돌리고 싶으면 `sw_multi_*` 와 `sw_hint` 두 줄이면 된다 (구조는 그대로다)

#### 멀티킬은 «제곱»으로 들어오고, 떼가 그것을 가능하게 한다

한 발에 n 마리면 **`n²` 점**이다 (1 · 4 · 9 · 16 · 25). 둘은 **한 벌이라 따로 못 만든다**:

- **제곱만 있고 떼가 없으면** 멀티킬이 순전히 운이라 제곱 보상은 아무도 못 닿는 장식이 된다
- **떼만 있고 제곱이 없으면** 떼는 그냥 «과녁이 많은 것»이라 노려서 모아 쏠 이유가 없다

그래서 **뒤로 갈수록 떼로, 그리고 크게 내려앉는다** (`FLOCK_BASE` 0.10 → `FLOCK_TOP` 0.60 ·
끝에는 다섯까지). ⚠️ **떼는 «한 발에 들어오도록» 모여 앉는다**(`FLOCK_R` 17 ≤ `HIT_R` 20) —
흩어 놓으면 보기에만 떼고 한 마리씩 맞히게 된다. 대신 앉자마자 저마다 폴짝 뛰어 흩어지므로
**내려앉는 그 순간**이 노릴 자리다.

- ⚠️ **상한은 «단발로는» 못 닿는다** — 30초에 (떼까지) 쉰 마리쯤 나오는데 상한이 72점이라,
  다 단발로 맞혀도 못 미친다. 「많이 쏘기」가 아니라 **「몰려 있을 때를 노리기」**가 실력이다.
  `checkbalance` 가 그 둘을 짝으로 못 박는다 — ① 단발로는 못 닿는가 ② 그래도 떼를 노리면 닿는가
- ⚠️ **떼를 세는 식을 «스폰 횟수»로 두면 안 된다** — 한 번에 여러 마리가 온다.
  검사가 `floor(DUR/SPAWN_MIN)` 로만 세다가 **「단발로는 못 닿는다」가 거짓말이 됐다**
  (37마리로 세는 동안 실제로는 쉰 마리가 나오고 있었다). 지금은 평균 떼 크기를 곱한다
- **봇 넷 × 세 판**으로 쟀다 — 평균 **6 / 5.7 / 4 / 2.7개**(빠른 손 · 보통 · 느긋한 · 설렁설렁).
  ⚠️ **한 판만 보고 조이지 않는다**: 떼는 운이 섞여서 같은 봇도 1 ↔ 6 으로 흔들린다.
  30초짜리라 그 편차는 오히려 다시 하게 만드는 쪽이다

#### 「팡!」 — 타격감은 셋이 같이 있어야 읽힌다

돌깨기의 「파편 · 먼지 · 흔들림」과 같은 자리다. 점수만 오르면 **참새가 그냥 사라진 것**이다:

1. **섬광** — 맞은 순간 한 번. ⚠️ 밭이 밝아서 옅게 두면 아무것도 안 터진 것처럼 보인다
2. **고리 둘** — 밖으로 퍼진다. ⚠️ 한 겹이면 그냥 동그라미다
3. **불똥 + 깃털** — 사방으로 튄다
4. **꽂힌 화살이 떤다**(`a.shake`) — 없으면 «놓인» 것으로 보인다

- **플로터**(`FLOAT_MS`)가 그 자리에 뜬다 — 하나면 `+1`, 둘 이상이면 `Double Kill` …
  `Penta Kill`. ⚠️ **캔버스 글자는 대비 검사가 못 본다** — 굵은 테두리를 둘러야 밭에 안 묻힌다
  (호두 게임의 「어두운 알약 위에 얹는다」와 같은 자리다)
- **콤보는 ×2 부터 뜬다** (×1 은 뜻이 없다). ⚠️ **빗맞히거나 `COMBO_MS` 쉬면 끊긴다** —
  안 끊으면 30초 내내 숫자만 오르는 장식이 된다
- ⚠️ **멀티킬 이름은 두 언어가 같은 낱말이다** — 게임에서 굳은 말이라 옮기면 오히려
  못 알아보고, 「더블 킬」은 화면 폭도 두 배다

#### 활과 화살 — 「힘이 하나도 없어 보인다」

선 하나에 삼각형을 붙인 것이 처음 모양이었고, 그대로는 이쑤시개로 보인다. 셋을 같이 줬다:

- **속도선** — 날아가는 쪽 뒤로 흐른다. 정지 그림이 아니라 «지금 가고 있는 것»이 된다
- **길고 «미늘» 있는 촉** — 끝만 뾰족한 것이 아니라 «파고드는» 모양이다. 위쪽만 밝게 해서 쇠붙이로
- **앞으로 갈수록 굵은 화살대** + 뒤로 누운 깃 셋
- 활은 **리커브**다 — 끝이 바깥으로 젖혀져야 «당겨진 힘»이 보인다.
  ⚠️ **짙은 테두리를 먼저 깔지 않으면 밝은 밭에 통째로 묻힌다** (찍어 보고 알았다).
  손잡이는 «감은 띠 둘»이다 — 덩어리 하나로 두면 혹으로 보인다

#### 난이도는 **세 번 헛짚고 나서** 수치로 찾았다 (2분짜리이던 때의 기록이다)

낚시에서 세운 규칙대로 **짐작으로 고르지 않고 사람 넷을 붙여 쟀다**
(반응 속도만 다른 봇 셋 + 「설렁설렁 두드리는 사람」). 세 판을 재고서야 원인이 나왔다:

1. **첫 판** — 셋 다 나온 참새 48마리를 통째로 쓸어 상한. 화살이 너무 싸서
   (`SHOT_MS` 300 → 2분에 400발) **조준이 필요 없었다**
2. **둘째 판** — 참새를 늘리고 활을 무겁게 했는데 **여전히 셋이 똑같았다**(명중률 50·50·49%).
   참새가 2.6초를 가만히 앉아 있으니 **0.15~0.42초짜리 반응 차이가 낄 자리가 없었다**
3. **셋째 판** — 폴짝거리게 했는데도 그대로였다. 진짜 원인은 **`HIT_R` 34px** —
   390px 화면에 참새 다섯이 깔리면 **아무 데나 쏴도 누군가는 걸린다**

`HIT_R` 을 참새 몸만 하게(20) 줄이고 한 화면의 참새를 넷으로 내리자 비로소 갈렸다.
⚠️ **봇은 «천장»이다** — 600ms마다 한 치도 안 쉬고 제일 가까운 참새를 쏜다.
사람은 그보다 한참 아래다.

##### ⚠️ 화살을 빠르게 하면 **반응 속도 축이 통째로 사라진다**

「화살 너무 느려」로 신고받아 `ARROW_SPD` 를 1.45 → **3.0** 으로 올렸다 (제일 먼 자리가
271 → 141ms). 손맛은 살아났는데 **난이도가 같이 무너졌다** — 느긋한(420ms)이 58 → 82마리로
올라 셋이 다시 똑같아졌다. 까닭은 한 줄이다: 조준이 어려웠던 이유가
**「반응 + 화살이 나는 시간」이 폴짝 간격보다 길어서**였는데, 화살이 빨라지며 그 합이
간격 안으로 들어왔다.

그래서 **화살은 빠르게 둔 채 폴짝을 당겼다**(`HOP_MS` 380 → **250** · `HOP_D` 42 → **56**).
지금은 **18 / 18 / 11 / 8개**(빠른 손 · 보통 · 느긋한 · 설렁설렁)로 **고치기 전보다 더 갈린다.**
⚠️ **손맛과 난이도를 같은 상수로 조절하지 않는다** — 화살 속도는 «손끝과 결과 사이»이고,
난이도는 «표적이 얼마나 움직이는가»다. 하나로 묶으면 한쪽을 고칠 때마다 다른 쪽이 따라 무너진다

- ⚠️ **상한은 «거의 다 쫓아야» 닿는다** — 18개 = 72마리이고 2분에 나오는 것이 여든 마리쯤이다.
  `checkbalance` 가 **「나오는 것의 절반은 쫓아야 한다」**(지금 72%)로 이 관계를 못 박는다.
  ⚠️ 이 검사의 분모는 **제일 촘촘할 때**(`SPAWN_MIN`)로 센다 — 넉넉한 쪽으로 세야
  「상한이 헐겁다」를 놓치지 않는다

#### 그림 — 「노을에 물드는 밀밭」

- ⚠️ **참새가 앉는 띠는 «이삭의 높이»여야 한다.** 밭을 화면 아래 20%에만 깔고 참새를
  0.40~0.78 에 두었더니 **이삭이 아니라 허공에 떠 있었다** (찍어 보고 알았다).
  지평선을 0.50 으로 올려 밭이 화면 절반을 차지하고, 그 띠가 뒤 겹 이삭의 머리와 겹친다
- ⚠️ **그래도 «띠 안 아무 데나»면 이삭이 없는 자리에 선다** — 참새는 **진짜 이삭 하나를
  골라 그 머리에 앉고**(`pickEar`), 폴짝도 **옆 이삭으로** 뛴다
- ⚠️ **줄기만 그으면 밭이 아니라 «막대기 몇 개»다.** 지평선 아래를 통째로 익은 빛으로
  깔고 그 위에 줄기가 선다. 이삭도 **길고 도톰하게 + 까끄라기 셋** — 작으면 밀이 아니라
  «민들레»로 보인다 (찍어 보고 고쳤다)
- ⚠️ **이삭의 높이는 «줄기 길이»가 아니라 «머리가 설 자리»로 뽑는다** — 길이로 뽑으면
  밑동이 어디냐에 따라 머리가 제멋대로 서서 참새가 앉는 띠와 안 맞는다
- ⚠️ **활이 작으면 통째로 묻힌다** — 밭이 밝아서 가는 선은 안 보인다 (찍어 보고 키웠다).
  안내 줄도 활과 겹쳐서 **104px 로 올렸다** (다른 미니게임은 16px 이다)
- ⚠️ **밭은 한 번 정하면 안 바뀐다** — 매 프레임 새로 뽑으면 지글거린다
  (돌깨기의 「얼룩은 씨앗이 정한다」와 같은 규칙이다)

#### 검사 — 사보타주 여섯으로 확인했다

`npm run test:sparrow` 가 스물여덟 줄을 본다. **여섯을 일부러 망가뜨려 다 잡히는 것을 봤다** —
화살을 즉시 맞게(2건) · 쿨다운 빗장 빼기(2건) · 손가락↔캔버스 셈을 40px 어긋내기(4건) ·
참새를 안 그리기(1건) · 깃털을 안 만들기(1건) · 제 발로 날아가면 쫓은 수를 깎기(1건).

- **그림은 «캔버스 픽셀을 읽어서» 잰다** — ① 하늘 띠가 노을빛인가(붉은 쪽 > 푸른 쪽 ·
  지금 12610/12610) ② **참새가 정말 그려지는가**. ⚠️ ②는 **참새 자리와 «빈 자리»를 같이
  재서 견준다**(104점 ↔ 0점) — 한쪽만 재면 무엇을 그려 놔도 통과한다
- **화살이 «날아가는 동안»은 시간으로 잰다** — 쏜 순간부터 맞을 때까지 141ms.
  「맞았는가」만 보면 즉시 맞게 고쳐도 통과한다 (즉시로 만들면 21ms 라 그대로 걸린다)
- ⚠️ **검사기에 수치를 옮겨 적지 않는다** — 재료가 나오는 마릿수도 `Sparrow.REWARD_PER`
  에서 셈한다. 처음에 10마리로 박아 뒀더니 `REWARD_PER` 를 4로 올리는 순간
  **「0개짜리 보상」으로 조용히 내려앉았다** (검사기의 제 방지 줄이 그것을 잡았다)

### 바람개비 퍼즐 (`pinwheel.js`) — 바람개비 밭

Steam 「Arrows Puzzle」을 옮겨 왔다. **바람개비는 «제가 도는 쪽»으로만 날아간다** —
누르면 그 방향으로 판을 빠져나가는데, 가는 길에 다른 바람개비가 하나라도 있으면 못 나간다.
**어느 것부터 치울지가 곧 퍼즐**이고, 한 수가 연쇄로 길을 연다. 판을 다 비우면 새 판이
깔리고, 2분 동안 치운 수가 재료다 (`REWARD_PER` 5개마다 1개 · 상한 18).
붙어 있는 맵은 **「바람개비 밭」 하나**다.

#### ⚠️⚠️ 판은 «역순»으로 깐다 — 그래야 **반드시 풀린다**

아무렇게나 깔면 **첫 수부터 아무것도 못 누르는 판**이 나온다 (서로가 서로의 길을 막는다).
화면에는 오류 하나 안 뜨고, 2분짜리에서 그건 그냥 빼앗긴 시간이다.

**치우는 순서의 «거꾸로»로 깐다** — 빈 판에서 시작해, **지금 놓아도 그 방향의 길이 비어
있는** 칸에만 하나씩 놓는다. 그러면 마지막에 놓은 것부터 거꾸로 치우는 순서가 언제나
답이 된다. **풀리는지 검사할 필요가 없다 — 만들 때부터 풀린다.**

- `checkpinwheel` 이 **판 스무 개를 끝까지 풀어 본다** — 열린 것을 아무거나 누르는
  «멍청한» 손으로도 다 비워져야 한다 (역순 생성이면 그것이 보장된다).
  사보타주(방향을 무작위로 뽑기)로 확인했다 — **스무 판이 전부 막힌다**
- ⚠️ **꽉 채우지 않는다**(`FILL` 0.62) — 빈 칸이 있어야 「길이 열린 것」이 처음부터
  몇 개 보이고, 그래야 첫 수가 막막하지 않다 (지금 스물여섯 개 중 열넷이 열려 있다)

#### 생명 셋 — 막힌 것을 세 번 누르면 끝난다 (`LIVES`)

⚠️⚠️ **여기는 규칙이 한 번 뒤집힌 자리다.** 처음에는 「막혀도 벌이 없다 · 흔들리기만
한다」였는데, **사람이 원본처럼 하트를 달라고 정했다.** 되돌릴 일이 아니다.

- ⚠️ **그래도 「틀려도 재료가 한 톨도 안 없어진다」는 그대로다** — 끝나도 **그때까지
  치운 것은 그대로 계산해서 가져간다**(`finish()`). 돌깨기에서 천장까지 차면 그 자리에서
  끝나되 깬 줄은 그대로 가져가는 것(`rk_buried`)과 **같은 자리**다.
  **잃는 것은 재료가 아니라 남은 시간이다** — 문구(`pw_dead` · `pw_lives_n`)도 그렇게 쓴다
- ⚠️ **깎는 문은 `loseLife()` 한 곳이다.** 손가락 쪽에서 또 깎으면 경로가 둘이 되어
  한쪽 빗장을 빼도 다른 쪽이 막아 준다 (낚시의 `hook()` 에서 배운 자리다)
- ⚠️ **마지막 하나를 잃어도 그 자리에서 `finish()` 하지 않는다** — 결과 판이 덮어써서
  방금 터진 하트도 공주도 한 프레임을 못 보고 사라진다 (그려 보고 알았다).
  `OOPS_MS` 만큼 「아이쿠」를 읽을 틈을 주고 끝내며, **그동안은 안 눌린다**(`dying`) —
  안 막으면 끝나는 중에도 점수가 오른다
- **하트도 「아이쿠」도 캔버스가 아니라 DOM 이다.** 캔버스 안은 `checkUI()` 가 아무것도
  못 보는 자리라, **글자가 있는 것은 DOM 에 두어야 대비·넘침이 재진다**
  · 하트가 사라지는 애니메이션은 `forwards` 다 — **평소 상태가 곧 끝난 상태**여야
    `settle()` 이 보낸 자리에서 제대로 재진다 (엔딩 연출의 조각과 같은 규칙이다)
  · 「아이쿠」 층은 평소 `display:none` 이라 **`checkui` 가 «일부러 켜 놓고» 잰다**.
    하트도 하나 꺼 놓고 잰다 — 다 켜진 줄만 재면 `.gone` 은 한 번도 안 본 것이 된다
- `npm run test:pinwheel` 이 여덟 줄을 본다 — 하트가 셋 떠 있는가 · 한 번 틀리면 하나
  주는가 · **화면의 하트도 같이 주는가**(상태와 그림이 안 갈리는가) · 공주가 얼굴과
  함께 한마디 하는가 · **둘째 번까지는 안 끝나는가** · 셋째에 끝나는가 ·
  끝나는 동안 더 안 눌리는가 · **그래도 치운 것은 그대로 받는가**.
  ⚠️ 생명 수를 검사기에 3 으로 박지 않는다 — `Pinwheel.LIVES` 에서 읽는다.
  사보타주 여섯으로 확인했다 (생명을 안 깎기 · 하트를 안 지우기 · 「아이쿠」를 안 띄우기 ·
  한 번에 끝내기 · 게임 오버면 재료를 안 주기 · `dying` 빗장 빼기)

#### 코지 게임으로 옮기며 바꾼 것

- **타이머가 또 하나의 제동이다** — 원본의 「시간 없는 모드」 대신 다른 미니게임 넷과
  같은 2분이다 (생각하는 게임이라 30초로는 한 판도 못 비운다)
- **판을 비우면 새 판**이 깔린다(`REFILL_MS`) — 한 판으로 끝나면 남은 시간이 빈다

#### 그림 — 「바람개비가 끝없이 돌아가는 밭」

- ⚠️ **방향을 말하는 것은 «날개»가 아니라 «화살촉»이다.** 도는 날개만으로는 어느 쪽인지
  안 읽히고, **퍼즐에서 방향이 안 읽히면 게임이 아니다.** 흰 바탕을 깔고 그 위에 크게
  얹어 날개와 층을 가른다 — 작게 뒀다가 찍어 보고 키운 자리다
- ⚠️ **색«만»으로 가르지 않는다** — 방향마다 색이 다르지만 화살촉이 따로 있다.
  색을 못 보는 사람에게도 방향이 보여야 한다 (`.ask-chip.fresh` 의 🆕 와 같은 규칙)
- ⚠️ **한낮의 푸른빛이다** — 노을 밀밭과 **일부러 반대로** 뒀다. 같은 평야 지대에 비슷한
  그림 둘이 서면 어디가 어딘지 헷갈린다. `checkpinwheel` 이 하늘이 푸른 쪽인지를 잰다
  · ⚠️ **그래서 여기만 HUD 글자가 안 읽혔다** — 다른 다섯은 흙빛·저녁·돌그늘·물빛·노을이라
    흰 글자가 그냥 읽히는데, 한낮의 `#7ec8f0` 위에서는 **1.84:1** 이었다
    (그림자는 대비가 아니다 — `checkTextStyle` 이 안 쳐 준다). 지금은 이름·점수·하트·
    안내 줄이 다 **어두운 알약 위에** 얹혀 있다 (호두 게임의 합계·참새의 시계와 같은 자리다).
    ⚠️ **알약이 넷이라 띠가 빡빡하다** — 265px·영어에서 이름이 「🌬️..」 까지 줄어 여백을
    조였다. 더 좁아지면 **줄어드는 것은 이름이어야 한다**: 남은 기회·점수·시계는
    «지금 벌어지는 일»이고 이름은 카드를 누를 때 이미 본 것이다
  · ⚠️⚠️ **1.84:1 이 오래 살아남은 이유는 `checkui` 에 이 화면 블록이 아예 없었기
    때문이다** (참새 쫓기도 같이 없었다). 붙일 때 안 만들어서 **HUD 도 안내 줄도 한 번도
    안 재고 있었다** — 0건이 「통과」가 아니라 **「한 번도 안 쟀다」**였다.
    **미니게임을 늘리면 `checkui` 에 그 화면 블록을 같이 만든다** (형 표와
    `checkbalance` 목록을 맞추는 것과 같은 자리다)
  · ⚠️ **`checkglobals` 에도 같은 구멍이 있었다** — 볼 파일 목록을 손으로 적어 두어서
    미니게임 셋(참새·바람개비·바위 부수기)이 **한 번도 안 보이고 있었다.**
    지금은 **`index.html` 의 `<script>` 를 그대로 읽는다** — 게임이 읽는 것과 검사가
    보는 것이 갈릴 수가 없다 (16개 → 20개 파일). 사보타주로 확인했다
- **판 뒤에 언덕 둘과 흐르는 바람 선** — 텅 비면 「밭」이 아니라 그냥 색이다 (낚시에서 배웠다)

#### 검사 — 사보타주 다섯 중 **둘이 검사기의 구멍을 찾아냈다**

`npm run test:pinwheel` 이 스물여덟 줄을 본다. 다섯을 망가뜨려 봤는데, 그중 둘이
**게임이 아니라 검사기가 틀렸다**는 것을 드러냈다 — 그래서 사보타주를 하는 것이다:

- ⚠️ **「하나가 치워졌다」로는 칸 셈을 못 가른다** — 손가락 → 칸 변환을 한 칸 어긋내도
  **옆 칸에 열린 것이 있으면 그것이 대신 날아가서 통과했다.**
  지금은 **누른 «그 칸»이 사라졌는지**를 본다
- ⚠️ **판이 빈 채로 픽셀 검사에 들어가면 «터진다»** — 새 판을 안 까는 사보타주에서
  `grid[0]` 을 읽다 크래시가 나 **그때까지 잰 것이 통째로 사라졌고, 실패가 한 줄도 안 남았다**
  (「크래시는 무엇이 틀렸나를 안 알려 준다」). 지금은 재기 전에 판을 새로 깔고,
  그래도 비어 있으면 **「아무것도 안 쟀다」로 실패시킨다**
- 나머지 셋도 다 잡힌다 — 역순 생성 버리기 · 막힌 것도 나가게 · 바람개비를 안 그리기
- **상한은 «판을 서너 번 비워야» 닿는다** (90개 ÷ 한 판 26개 = 3.5판).
  `checkbalance` 가 세 판 이상인지를 못 박는다 — 상한이 헐거우면
  「한 판 대충 치우고 꼭대기」가 된다 (바위산의 「판 하나보다 많이 깨야 한다」와 같은 자리다)

### 바위 부수기 (`driller.js`) — 소풍 바위 · 미스터 드릴러 식

**바위를 파고 내려간다.** 아래·왼쪽·오른쪽으로만 팔 수 있고 **위로는 못 간다.**
받치던 것을 파내면 위의 덩어리가 통째로 떨어지고, 떨어져 쌓인 것이 «같은 색 넷»을
이루면 사라지며 또 떨어진다 — **연쇄**가 곧 손맛이다. 2분 동안 내려간 깊이가 재료다
(`REWARD_PER` 55m 마다 1개 · 상한 18개 = 990m). 붙어 있는 맵은 **「소풍 바위」 하나**다.

#### ⚠️⚠️ 갱도는 «빈 칸 없이» 꽉 차 있다 — 빈 칸은 사람이 판 자리뿐이다

처음에는 `FILL` 0.86 으로 줄마다 구멍을 뚫어 두었다 (「빈 칸이 있어야 길이 보인다」).
그런데 **떠 있는 바위는 첫 낙하 틱에 그대로 무너진다** — 판에 들어서자마자
아무것도 안 눌렀는데 바위가 쏟아지고 넷이 사라지고 사람까지 내려갔다.
게다가 그 구멍이 곧 «공짜 길»이라 손가락을 대고 있으면 파지도 않고 내려가서,
**「롱프레스 시 너무 팍팍 부순다」·「진입하자마자 명령 안 했는데 부순다」로 신고받았다.**

**꽉 찬 판에는 받칠 것이 없는 바위가 하나도 없다** — 사람이 파기 전에는 한 칸도
안 움직이고, 무너지는 것은 전부 「내가 한 일」이 된다 (미스터 드릴러의 축 그대로다).
첫 줄만 비워 두는데 그것이 곧 **지표면**이다.

- ⚠️ **에어 캡슐은 칸 하나를 «대신» 차지한다.** 예전에는 「빈 칸에만 놓는다」였는데
  판이 꽉 차면 **캡슐이 영영 안 나온다** — 산소가 이 게임의 상한을 지키므로 그것이 곧
  「2분을 다 쓸 길이 없어지는」 것이다 (원본에서도 캡슐은 판 «안»에 박혀 있다)
- ⚠️ **`checkdriller` 의 「손을 대기 전에는 한 칸도 안 움직인다」가 «누계»로 잰다.**
  처음에는 1.2초 동안의 «차»로 쟀는데, 무너지는 것은 판이 열린 **첫 몇 백 ms 에
  이미 다 끝나** 있어서 **사보타주(구멍을 도로 뚫기)가 그대로 통과했다.**
  판을 연 뒤의 누계(낙하·연쇄·판 횟수·사람이 내려간 줄)를 보면 언제 들여다봐도 걸린다
- ⚠️ **파는 시간도 340 → 420ms 로 올렸다** (「팍팍」의 나머지 몫이다).
  손가락을 대고 있는 손이 봇으로 **13.6 → 11.6 m/s**, 판마다 흔들리던 폭도
  11.2~15.2 → **10.7~12.2** 로 좁아졌다

- ⚠️⚠️ **덩어리 하나가 「파는 단위 · 떨어지는 단위 · 사라지는 단위」를 다 겸한다**
  (`groupAt`). 셋을 따로 만들면 「파 보니 다른 것이 떨어지는」 그림이 나온다
- **끝나는 길이 셋이다** — ① 2분 ② 산소 0 ③ 생명 셋.
  ⚠️ **어느 쪽이든 판 만큼은 그대로 가져간다**(`finish()`) — 돌깨기의 `rk_buried` ·
  바람개비 밭의 생명과 같은 자리다. **잃는 것은 재료가 아니라 남은 시간이다**
- ⚠️ **산소가 깔리는 것보다 «부드러운» 벌이라 갈라 두었다** — 산소는 눈금이라 다가오는
  것이 보이니 0 이면 바로 끝내고, 깔리는 것은 순간이라 기회를 셋 준다

#### ⚠️⚠️ 상한을 지키는 것은 «시간»이 아니라 «산소»다 — 잣대를 두 번 틀렸다

「곧장 파면 몇 초」를 **식으로 계산해 검사까지 적어 놓았는데 두 번 다 사실이 아니었다.**
파는 단위가 «덩어리»라 한 번 파면 1~3칸을 내려가고, 연쇄가 터지면 통째로 낙하한다 —
`줄 수 × (DIG_MS + FALL_MS)` 는 게임을 **두 배 가까이 느리게** 잡고 있었다.
**사실이 아닌 잣대는 통과시켜도 아무것도 안 지킨 것이다.**

그래서 진짜 속도는 **`npm run test:driller` 가 봇을 붙여 잰다.**
`checkbalance` 에는 **식으로 반드시 참인 것만** 남겼다 (산소 한 통 < 판 길이).

⚠️⚠️ **「낙하만 해도 판의 5분의 1」은 지웠다 — 전제가 사라졌다.** 구멍이 있던 동안에는
「한 칸도 안 파고 통째로 떨어지기만 해도」가 말이 됐는데, **판을 꽉 채우면서 한 칸도
안 파면 한 줄도 못 내려가게 됐다.** 일어날 수 없는 일에서 뽑은 바닥은 **사실이 아닌
잣대**다. 약속(「상한이 진짜 일이다」)은 없앤 것이 아니라 **잴 수 있는 자리로 옮겼다** —
`checkdriller` 의 「상한에 닿으려면 **판의 절반**은 쉬지 않고 파야 한다」가 봇으로 재고,
그쪽이 훨씬 센 약속이다 (24초 → 60초 · 지금 86초 = 판의 72%).

- **봇은 천장이다** — 한 치도 안 쉬고 아래만 판다. 재 보니 **10.7~12.2 m/s**
  (구멍이 있던 판에서는 17~21 이었는데 그중 3분의 1은 파지도 않고 떨어진 몫이었다)
- ⚠️ **재는 동안 산소를 채워 준다.** 여기서 재는 것은 «파는 속도»이지 산소가 아니다 —
  안 채우면 판이 도중에 끝나 10.8 m/s 가 나왔고, 그건 「느리다」가 아니라 「중간에 끝났다」였다
- ⚠️ **20초는 재야 한다.** 12초로 재면 단단한 바위가 드문 «얕은 구간»만 재게 되어
  29 m/s 가 나온다 — 짧게 재는 것은 게임이 아니라 갱도의 «머리»를 재는 것이다
- 그래서 상한이 **990m** 다: 아래만 누르는 손은 **50초에 산소가 바닥나** 600m 남짓에서
  멈추고, **2분을 다 쓰려면 반드시 에어 캡슐을 주우러 돌아야 한다.** 그것이 곧 실력이다
  (미스터 드릴러의 축 그대로다).
  ⚠️ **한 판 상한(18개)은 안 건드린다** — 그것이 분당 밸런스에 걸려 있다.
  판을 꽉 채워 느려졌을 때 고칠 것은 «m 당 요율» 쪽이다 (100 → 55m).
  1,800m 그대로 두었으면 2분을 다 써도 못 닿는 «장식»이 됐다

#### 그림 · 조작

- ⚠️ **색«만»으로 가르지 않는다** — 칸마다 **점이 색 번호만큼** 찍힌다. 색을 못 보는
  사람도 「같은 것 넷」을 셀 수 있어야 퍼즐이 된다 (바람개비의 화살촉과 같은 규칙이다)
- ⚠️ **찍어 보고 한 번 가라앉혔다** — 처음 빛깔은 밝아서 바위가 아니라 «사탕»으로
  보였다 (돌깨기의 「바위산에 사탕이 쌓인다」와 같은 자리다)
- **단단한 바위는 남은 횟수를 «금»으로 보여 준다** — 몇 번 더 파야 하는지가 안 보이면
  그냥 벽이다. 부서질 때 산소 20% 를 먹는다 («뚫을까, 돌아갈까»가 여기서 나온다)
- ⚠️ **HUD 알약이 다섯이다**(이름·하트·깊이·시계 + 산소 막대) — 앱에서 제일 빡빡한 띠다.
  갱도는 위가 밝고 아래가 어두워 **배경이 판마다 달라지므로** 처음부터 알약을 깔았다
  (바람개비 밭에서 1.84:1 로 잡힌 뒤에 배운 것이다)

#### ⚠️ 검사기가 세 번 «가르지 못하는 잣대»였다 — 사보타주가 다 찾아냈다

이 게임은 **떨어지는 것이 모든 결과를 덮는다.** 그래서 「자리가 어떻게 됐나」로 재면
무엇을 망가뜨려도 통과한다:

- 「아래로 내려갔나」 → 손가락↔칸 셈을 40px 어긋내 **옆을 파게 해도**, 옆을 파면
  발밑이 비어 **떨어지면서 결국 내려간다.** 가르는 것은 **«칸(열)이 그대로인가»**다
- 「위로 안 갔나」 → 위로 팔 수 있게 해도 한 칸 올라가는 순간 **도로 떨어져** 끝난 자리가
  같다. 「제일 높이 올라간 자리」로 바꿨더니 이번엔 기준을 **누르기 한참 전의 줄**로 잡아
  그사이 떨어진 만큼이 여유가 됐다. 지금은 **「위를 누르면 조준(`aim`)조차 안 잡힌다」**를
  본다 — 누르는 내내 남아 있어 반드시 걸린다
- 「한 줄을 비워 놓고 재기」 → **위가 통째로 무너져** 심어 둔 칸 위에 쌓인다.
  지금은 **치우지 않고 단단한 것으로 «바꾸기만» 하고**, 심은 칸에 **표식**을 붙여
  그것만 따라보고, **바뀔 때까지 지켜본다**(정해진 시간에 한 번 재지 않는다)

**그리고 사보타주가 게임 버그도 하나 찾아냈다** — **무적인 동안 사람 칸에 들어온 바위를
안 치우면 사람이 바위 속에 박힌다.** 그 뒤로는 위에서 무엇이 떨어져도 «사람 칸에
들어오는» 일이 없어 **영영 안 깔리고**, 기회 셋 중 둘이 영영 안 줄어든다.
지금은 무적이어도 **치우기는 하고 기회만 안 깎는다.**

⚠️ **검사기가 사보타주에서 터지면 안 된다** — 위로 팔 수 있게 하자 사람이 갱도 밖으로
올라가 `rows[…]` 가 undefined 가 됐고, 검사기가 그 자리에서 터져 **그때까지 잰 것이
통째로 사라졌다.** 줄을 짚는 자리를 전부 막았다.


### 미니게임 맵의 AP 는 «한 판이 돌려주는 개수»에 비례한다

```
미니게임 AP = 지대 AP × 한 판 상한 × D.ENERGY.miniApK   (지금 0.2)
```

| 맵 | 지대 | 상한 | ⚡ | AP당 | 본전 |
|---|---|---|---|---|---|
| 노을 밀밭 (30초) | 평야 6 | 6 | **7** | 5.1배 | 19% |
| 파수꾼의 호박 밭 | 평야 6 | 17 | **20** | 5.1배 | 20% |
| 거울 저수지 | 평야 6 | 18 | **22** | 4.9배 | 20% |
| 바람개비 밭 | 평야 6 | 18 | **22** | 4.9배 | 20% |
| 호두 마루 | 평야 6 | 20 | **24** | 5.0배 | 20% |
| 소풍 바위 | 평야 6 | 18 | **22** | 4.9배 | 20% |
| 흔들 바위산 | 산악 11 | 18 | **40** | 5.0배 | 20% |

⚠️ **예전에는 미니게임 맵도 평범한 채집과 같은 값이었다** (평야 6). 한 판이 6~20개를
돌려주니 **AP당 6~20배**였고, 꾹 누르기가 420ms 에 하나(`HOLD_EVERY`)라 하루치 1000 AP 는
**70초면 다 쓴다** — 즉 하루 AP 를 다 쓰고 나서 미니게임을 따로 더 돌 수 있었다.
값을 AP 로 매겨 놓고 실제로는 «시간»으로만 받고 있던 셈이다.

⚠️ **그렇다고 AP당 효율을 평범한 채집과 «같게» 맞추면 안 된다** — 호두 20개가 꾹 누르기로는
8.4초인데 미니게임은 2분이다. 같은 값이면 아무도 안 한다. 지킬 것은 **「그냥 줍는 것보다
낫되, AP 가 값이기는 하다」** 사이다.

- 식이 깔끔하게 떨어진다 — 지대도 상한도 약분돼서 **배수 = 1/k · 본전 지점 = k** 다.
  여섯이 저절로 같은 자리에 선다 (표의 소수점 차이는 반올림 몫이다)
- **왜 0.2 인가** — 봇으로 잰 제일 못하는 손이 상한의 45% 쯤이다 (밀밭 설렁설렁 2.7/6 ·
  낚시 느긋한 10/18). 본전을 그 아래(20%)에 두면 **못한 판도 손해가 아니다** —
  「틀려도 재료가 한 톨도 안 없어진다」(흐린 장)의 연장이다
- ⚠️ **한 판 상한은 그 게임의 모듈(`REWARD_MAX`)이 유일한 원본이다.** data.js·game.js 에
  사본을 두면 보상을 고쳤을 때 **값만 옛 것으로 남아** 카드의 ⚡ 와 받는 개수가 갈린다.
  `game.js` 의 `miniRewardMax()` 가 `window[MINIS[형]].REWARD_MAX` 를 읽고,
  `checkbalance` 도 **모듈을 node 로 진짜 읽어** 같은 값을 본다 (정규식으로 긁으면
  호박 밭처럼 상한이 «식»인 것을 못 읽고, 무엇보다 사본을 보게 된다)
  · 그래서 **호박 밭에도 `REWARD_MAX` 를 만들었다** — 거기만 상한이 상수가 아니라
    `floor(DUR_MS/REWARD_EVERY) + CLEAR_BONUS` 다. 클리어 보너스도 `CLEAR_BONUS` 한 곳에서
    나온다 (예전에는 `pickItem(), pickItem()` 으로 박혀 있어 상한을 셀 수가 없었다)
- ⚠️ **평범한 채집 한 번보다 싸질 수 없게 바닥을 두었지만, 그 바닥에 «닿으면» 안 된다** —
  닿으면 `miniApK` 를 어떤 값으로 내려도 화면이 안 바뀌어 **무슨 값을 넣어도 통과하는
  「스스로 맞는」 검사**가 된다 (호위 할인의 평야 바닥과 같은 규칙이다). `checkbalance` 가 본다
- **호위 할인은 «평범한 채집 한 번»의 값에서 깎는다.** 미니게임 값은 그것을 배로 늘린
  것이라 할인도 같은 비율로 따라온다 — 미니게임 쪽에서 따로 깎으면 경로가 둘이 된다
- **`checkbalance` 가 넷을 본다** — ① AP당 1.2~8배 안인가 ② 본전 지점 ≤ 45% 인가
  ③ 값을 매기는 상한과 실제 한 판 최대가 같은가 ④ 바닥에 안 닿는가.
  ⚠️ 예전 줄은 「미니게임이 그냥 줍는 것보다 낫다」를 **개수만**(`g.max > 1`)으로 봐서,
  AP당 20배이던 동안에도 통과했다 — **얼마를 내는지는 한 번도 안 보던 검사다.**
  사보타주 넷으로 확인했다 (k=0 → 2건 · k=0.9 → 본전 90% · `REWARD_MAX` 를 안 내놓기 ·
  값과 실제 상한을 어긋내기)

### 미니게임 여섯은 «분당» 같은 자리에서 준다 (`checkbalance`)

미니게임은 「AP 한 번에 시간을 내고 재료를 많이 받는」 거래다. 한쪽이 너무 후하면
**그 맵만 돌게 되고** 나머지는 죽은 콘텐츠가 된다 (밭 물약 여섯을 같은 값으로 둔 것과
같은 이유다). 호박 밭 **17개** · 바위산 **18개** · 낚시터 **18개** · 바람개비 밭 **18개** ·
**소풍 바위 18개** · 호두밭 **20개** (여섯 다 2분) · **밀밭 6개(30초)**.

⚠️ **예전에는 「다 같은 시간을 쓴다」 + 「한 판 최대가 1.5배 안」 두 줄이었다.**
밀밭이 30초가 되면서 그 짝이 깨졌다 — **길이가 4배 다르면 두 줄이 동시에 참일 수가 없다:**
4분의 1 시간에 같은 것을 주면 나머지가 죽고, 시간에 딱 비례해 깎으면 「AP 한 번」의 값이
4분의 1 이 된다. 지금은 둘이다:

- **분당 수확이 1.5배 안** (8.5 ~ 12.0/분)
- **어느 하나가 다른 것을 «두 축 모두»에서 덮지 않는다** — 분당도 훨씬 낫고(1.25배 넘게)
  한 판 수확도 안 밀리면 그건 나머지를 죽이는 것이다. 한쪽만 나으면
  「빨리 끝나는 쪽 / 한 번에 많이 받는 쪽」이라는 **고를 거리**가 된다.
  ⚠️ 그 1.25배는 «값이 통과하도록» 고른 것이 아니다 — 지금도 호두밭이 낚시터보다
  분당 1.11배 · 한 판 1.11배로 조금씩 나은데 그 정도는 「덮는다」가 아니다.
  ⚠️ **처음에는 「한 판 최대가 3.2배 안」으로 적었다가 지웠다** — 20 ÷ 6 = 3.33 을
  통과시키려고 고른 값이었다. 그건 잣대를 결과에 맞춘 것이다

- ⚠️ **「한 판 최대」를 내는 식은 게임마다 다르다** — 버틴 시간(호박) · 판의 크기(호두) ·
  상한 자체(바위산) · **한 바퀴의 최소 길이**(낚시터는 입질 + 붙드는 시간 + 건지는 연출이
  한 바퀴라 2분에 스무 번이 한계다).
  그래서 **게임마다 한 줄씩 `checkbalance` 에 적고, 형 표와 그 목록이 어긋나면
  실패시킨다** — 새 미니게임을 붙이면서 밸런스를 안 보고 지나갈 수 없는 자리다
  (사보타주로 확인했다: 표에서 빼면 「미니게임 목록이 형 표와 같다」가 잡는다)
- **바위산은 상한을 «줄 수»로 지킨다** — 판 하나(16줄)를 통째로 비우는 것보다
  많이 깨야(18줄) 상한에 닿는다. 상한을 5로 내리면 이것과 「같은 자리」가 같이 잡힌다
- **낚시터는 «시간»으로 지킨다** — 상한 18개는 아홉 번이고, 한 바퀴가 최소 5.9초라
  **2분의 절반 가까이(53초)를 쉬지 않고 건져야** 닿는다

- ⚠️ **상한만 보면 요율이 안 보인다.** `REWARD_PER` 를 5 → 2 로 후하게 바꿔도 상한(20)이
  그대로라 「같은 자리」 검사가 **통과했다** — 호두 40개만 지워도 꼭대기가 되는데도.
  그래서 **「상한에 닿으려면 판의 절반은 지워야 한다」**를 따로 본다 (지금 83%)
- ⚠️ **수치를 검사기에 옮겨 적지 않는다** — `pumpkin.js`·`walnut.js` 에 써진 상수를
  그대로 읽어서 견준다 (사본을 만들면 검사기만 옛 값으로 남는다)

### ⚠️ 캔버스 안은 `checkUI()` 가 아무것도 못 본다

대비도 넘침도 잴 것이 없다. 그래서 미니게임이 **도는지**는 전용 검사에서만 잡힌다.

```bash
npm run test:walnut                     # 호두 게임 — 진짜 마우스로 끌어 본다 · 캔버스 픽셀도 잰다
npm run test:rock                       # 돌깨기 게임 — 끌고 · 돌리고 · 쓸어 본다
npm run test:fish                       # 낚시 — 채고 · 누르고 있어 본다
npm run test:sparrow                    # 참새 쫓기 — 진짜 마우스로 쏴 본다 · 캔버스 픽셀도 잰다
npm run test:pinwheel                   # 바람개비 퍼즐 — 판을 끝까지 풀어 본다
npm run test:driller                    # 바위 부수기 — 파고 · 깔리고 · 봇으로 속도를 잰다
FULL=1 node tools/checkui.js gather     # HUD·결과 화면(DOM)의 대비·넘침
```

- **`Walnut.start()`·`Rock.start()`·`Fish.start()`·`Sparrow.start()`·`Pinwheel.start()`·`Driller.start()` 를 직접 부르지 않는다.** 그러면 `gather()` 의
  「형 → 미니게임」 갈래를 통째로 건너뛰어 **그 줄을 지워도 통과한다.** 전용 검사는
  **채집 버튼을 누른다** (사보타주로 확인했다).
  · ⚠️ 잠긴 지대의 맵이면 **지대 탭도 옮겨야 카드가 뜬다**(`setGatherZone`) —
    흔들 바위산은 매력 132 짜리 산악 맵이라 평야 탭에서는 목록에 아예 없다
- **`_play()` 로만 재지 않는다.** 그러면 드래그 → 칸 좌표 변환이 틀려도 통과한다.
  **`page.mouse` 로 실제로 끈다** — 칸 좌표를 40px 어긋내는 사보타주로 확인했다
  (`checktut` 이 `el.click()` 을 안 쓰는 것과 같은 이유다)
- ⚠️ **뜻대로 안 됐으면 «거기서 죽지 말고» 그렇다고 알린다.** 갈래를 지우는 사보타주에서
  다음 줄이 `_state().grid` 를 읽다 터졌는데, **크래시는 무엇이 틀렸나를 안 알려 준다**
  (`checklore` 에서 배운 것과 같다).
  · **같은 자리를 `checkrock` 에서 또 만들었다** — 천장 판정을 지우자 게임이 안 끝나
    없는 「나가기」를 누르다 터졌고, **그때까지 잰 스물여덟 줄이 통째로 사라졌다.**
    끝났는지 보고 안 끝났으면 `done()` 으로 빠진다 (사보타주로 다시 확인했다)
- ⚠️ **보상 검사에 «0개짜리» 도피구를 두지 않는다.** 드래그 두 번은 점수가 4~7 이라
  재료가 0개인데, 「0개 이상 들어온다」는 **무슨 값을 넣어도 통과한다.** 지금은
  남은 수를 이어 두어 **반드시 재료가 나오는 점수까지 벌어 놓고** 잰다
- **결과 화면까지 열어 놓고 잰다** — 게임 중 화면만 재면 결과의 「나가기」와 재료
  알약은 한 번도 안 잰 것이 된다 (밭 시트에서 배운 것과 같다)
- **연출은 «상태 수»로 잰다** — 「점수가 올랐다」만 보면 돌이 그냥 사라져도 통과한다.
  `checkrock` 은 깨는 순간 **파편이 생기는지**, `BURST_MS` 뒤에 **사라지는지**를 같이 본다
  (사보타주 둘로 확인했다 — 파편을 안 만들면 0조각 · 안 지우면 50조각이 남는다)
- **그림은 «캔버스 픽셀을 읽어서» 잰다.** DOM 이 아니라 아무 검사도 안 보는 자리라,
  `getImageData` 로 칸 하나를 떠서 본다 — 호두는 ① 숫자와 껍데기의 **대비**(지금 8.0:1) ·
  ② 껍데기가 **두 쪽으로 갈리는가** · ③ **칸 모서리가 비어 있는가**(네모가 아니라 «알»인가).
  돌은 ① **결이 셋 이상인가**(한 색 네모가 아니다) · ② **모서리가 깎여 있는가** ·
  ③ **다시 그려도 얼룩이 그대로인가** (사보타주 셋으로 확인했다 — 네모로 칠하면 1가지 색 ·
  모서리 a255 · 얼룩을 매 프레임 뽑으면 「프레임마다 달라진다」).
  사보타주 셋으로 확인했다 (글자를 껍데기 색으로 1.4:1 · 음영을 지우면 0.0% · 네모로 칠하면 모서리 a255)
  · ⚠️ **처음에는 「왼쪽이 더 어둡다」로만 쟀다** — 음영을 통째로 지워 두 쪽을 같은
    색으로 만들어도 **안티에일리어싱 때문에 소수점 다섯째 자리에서 갈려 통과했다.**
    지금은 **사람이 볼 수 있는 만큼**(5% 이상) 벌어져야 한다 (지금 17.5%) —
    가르지 못하는 잣대는 무슨 값을 넣어도 통과한다

## 마이 룸 — 방 안에서 하는 일 다섯은 «하나씩» 열린다

운동 · 흡입 · 부엌 · 수확 · 밭. 튜토리얼을 막 마친 화면에 다섯이 한꺼번에 서 있으면
무엇부터 눌러야 하는지 알 수가 없다. **처음에는 다 숨어 있다.**

- 조건은 **`actOpen()` 한 곳**에 모여 있다. `index.html` 은 `hidden` 만 달고 있고,
  켜는 것은 `renderActBadges()` 한 줄이다
- ⚠️ **「보이는가」와 「열리는가」를 따로 판정하지 않는다.** 갈라 두었더니 옛 세이브에서
  **버튼은 보이는데 눌러도 아무 일이 없었다** — 보임은 `roomActs` 를 보고 열림은
  `chefHired()` 만 봤다. `kitchenOpen()` 은 `actOpen('kitchen')` 을 그대로 돌려준다
- ⚠️⚠️ **다섯이 다 같은 종류의 문이 아니다.** 붙일 자리를 고르기 전에 이것부터 갈라야 한다:

  | 종류 | 무엇인가 | 조건 |
  |---|---|---|
  | **이야기가 여는 것** | 🍲 부엌 · 🏃 운동 | 퀘스트 (`chefHired` · `questCleared('q_kitchen')`) |
  | **뱃지가 달린 기록** | 🍗 흡입 · 🧺 수확 | 그 **뱃지를 켜는 «내용물»** (`everBinged` · `hasProducer`) |
  | **점수 문** | 🌾 밭 | 여신(매력 100 · 리그와 같은 문 · `farmOpen`) |

  **가운데가 핵심이다** — 흡입·수확은 붉은 뱃지가 달린 버튼이라, 뱃지를 켜는 조건과
  버튼을 켜는 조건이 갈리면 반드시 둘 중 하나가 난다: 버튼이 **먼저** 열리면 누를 것이
  없는 빈 기록 화면이고, **나중에** 열리면 **뱃지가 갈 곳을 잃는다** (밤이 셋 쌓였는데
  알릴 자리가 없다 — 화면에는 오류 하나 안 뜬다). 그 둘의 내용물이 **마침 퀘스트의
  목표**라(`q_egg` = 크리처 한 마리), 퀘스트에 안 매달아도 자리는 「퀘스트 사이사이」다
- ⚠️ **🏃 운동은 `q_kitchen`(부엌 3번) «뒤»다 — 순서를 뒤집으면 벌이 먼저 온다.**
  운동은 포만감을 더 빨리 깎으므로, 부엌을 모르는 사람에게 먼저 주면 **혼자 먹은 밤이
  잦아지는데 고칠 방법은 안 배운 상태**가 된다 (「덜 먹는 게임이 아니라 혼자 먹지 않는
  게임이다」 · `EXERCISE.md`). 하루 한 번짜리 목표라 실제로 사흘이 걸린다
- ⚠️ **🍗 흡입을 `bingeCount()`(아직 «안 본» 밤)로 판정하지 않는다.** 그건 장면을 다 보면
  0 이 되는 큐라 **컷씬을 끝까지 넘기는 순간 버튼이 눈앞에서 사라진다** — 게다가 0 일 때
  누르면 「볼 것이 없다」(`bs_none`)고 말해 주게 돼 있으니 그 갈래가 영영 안 돈다.
  되돌아가지 않는 값(`S.record.aloneNights`)이어야 한다
- ⚠️ **🧺 수확을 `producers()`(애착·동행에 «세운» 아이)로 판정하지 않는다.** 크리처를
  얻어도 세우기 전에는 거짓이라, 버튼이 `q_egg` 가 아니라 **옷장을 열어 본 순간**에 뜬다.
  반대로 「`q_egg` 를 «끝내면»」으로도 안 둔다 — 얻는 순간 생산이 이미 시작되므로
  보상을 받기 전에 **뱃지만 켜질 수 있는 날**이 생긴다
- ⚠️ **🌾 밭에는 퀘스트 조건을 «또» 걸지 않는다.** 탐험의 밭 탭이 이미 여신을 문으로 쓰고
  마이 룸 🌾 는 그 탭으로 보내는 버튼이라, 여기에 하나 더 걸면 **한 것을 두 번 잠근다**
  (마을 해금에서 이미 정한 규칙). `actOpen('farm')` 이 `farmOpen()` 을 그대로 부른다
- ⚠️ **버튼이 «조용히» 나타나면 안 된다.** 부엌은 컷씬이 열어 줬다 — 운동·수확도
  **이미 있는 완료 컷씬에 줄 하나씩**을 붙였다 (`c_kitchen_out_3` · `c_egg_out_3` ·
  두 언어). 새 컷씬을 만들지 않는다. 흡입은 **붉은 뱃지 자체**가 알리는 것이 맞다
- ⚠️ **개발용 스위치(`S.roomActs`)는 그대로 둔다** — 조건보다 «먼저» 통과하므로
  옛 세이브는 한 글자도 안 바뀐다 (마이그레이션 14 가 다섯을 다 넣어 뒀다)
- ⚠️ **하던 사람에게서 뺏지 않는다** (세이브 14). 옛 세이브는 다섯을 다 쓰고 있었으므로
  마이그레이션이 그대로 열어 준다. ⚠️ 그 표(`ROOM_ACTS`)는 **`load()` 위쪽에 둔다** —
  `migrate` 가 쓰는 값이라 아래에 두면 `ReferenceError` 가 나고 세이브가 통째로
  기본값으로 되돌아간다 (1번 항의 그 사고다)
- ⚠️ **`checkui` 는 ver 8 짜리 옛 세이브를 심고 시작한다** — 그래서 평소 검사는
  «마이그레이션으로 다 열린» 상태만 본다. 새 플레이어의 화면은 「방버튼해금」이 따로
  잰다 (숨어 있는가 · 만나면 부엌만 나타나는가 · 보임과 열림이 갈리지 않는가 ·
  **컷씬을 끝까지 넘기면 버튼과 레드닷이 «그 자리에서» 나타나는가**).
  ⚠️ 마지막 하나는 `renderActBadges()` 를 손으로 불러 주면 안 된다 — 그러면
  「그리기가 빠진」 버그를 통째로 못 본다.
  · **다섯을 다 본다** — 조건을 하나씩 만들어 주고 **그 버튼만** 늘어나는지,
    그때마다 `actOpen()` 도 참인지(보임↔열림), 그리고 **`render()` 로** 나타나는지.
    **몇 개를 쟀는지를 통과할 때도 낸다**(`다섯을 다 쟀다 (5/5)`)
  · ⚠️ 흡입은 **일부러 «밤을 다 보고 난 뒤»의 자리에서 잰다**(`S.binges` 가 빈 채로) —
    그냥 밤을 하나 쌓아 놓고 재면 잘못된 조건(`bingeCount`)도 그대로 통과한다
  · ⚠️ 밭을 잠그려면 **매력까지 0 으로 내려야 한다** (`charmPeak()` 가 `totalCharm()` 을
    다시 올려 주므로 `S.stats`·`S.petRoom` 까지 비운다)
  · 사보타주 다섯으로 확인했다 — 운동 줄 빼기 · 흡입을 「안 본 밤」으로 · 수확을
    「세운 아이」로 · 밭을 개발용 스위치로만 · `renderShowcase` 의 그리기 빼기

## 잡화 — 물약은 꾹 누르면 계속 마신다 · 「꿀꺽」은 한 병에 한 번

물약이 스무 병 쌓인 사람은 스무 번을 눌러야 했다. 채집 버튼이 이미 푼 문제라
**같은 엔진을 쓴다** — 톡 누르면 한 병, 꾹 누르고 있으면 `HOLD_EVERY`(420ms)마다 한 병.

- ⚠️⚠️ **꾹 누르기 엔진은 «하나»다** (`startHold`/`stopHold`/`tapOnce`). 채집과 물약이
  각자 제 타이머를 두면 곧 두 벌이 되고 한쪽만 고쳐 어긋난다 — 손을 떼는 자리(document) ·
  스크롤 취소(`HOLD_MOVE`) · 이어 오는 click 버리기(`swallowTap`)가 전부 같은 규칙이다.
  한 걸음(`step`)은 **더 할 수 있으면 참**을 돌려준다 — 그래서 `drinkPotion()` 도
  `gather()` 처럼 불리언을 돌려주고, 마지막 한 병을 마시면 거짓이라 스스로 멎는다
- ⚠️ **즉시 마시지 않고 `HOLD_DELAY`(450ms)를 둔다** — 선반을 손가락으로 밀어 내릴 때
  누른 순간 마셔지면 스크롤만 해도 물약이 샌다 (채집의 AP 와 같은 이유다).
  첫 한 번은 onclick 에 맡긴다
- ⚠️ **손을 떼는 것은 document 에서 받는다** — `drinkPotion()` 안의 `render()` 가 카드를
  통째로 새로 그려서 카드에 붙인 pointerup 은 영영 안 온다 (채집에서 배운 함정이다)
- ⚠️⚠️ **「꿀꺽」은 `drinkPotion()` 한 곳에서만 난다.** 카드 쪽(pointerdown)에서 내면
  꾹 누르는 동안 **한 번밖에 안 들리고**, 못 마신 손짓에도 소리가 난다 —
  소리가 나는 자리는 **물약이 실제로 줄어드는 자리**여야 한다 (낚시의 `hook()` 과 같다)
  · 그래서 카드는 `data-nosfx` 다 (버튼음이 「꿀꺽」 앞에 겹쳐 울리지 않게).
    ⚠️ **`data-sfx` 를 적어 둔 요소는 조상의 `data-nosfx` 보다 앞선다** —
    안 그러면 소리를 끈 카드 «안»의 '?' 버튼까지 같이 벙어리가 된다 (`sfx.js`)
  · 소리는 **두 박자**다 (올라갔다 내려앉는다). 한 박자면 그냥 «툭» 하는 저음이라
    마시는 것으로 안 들린다. 다 합쳐 0.27초 — `HOLD_EVERY` 보다 짧아야 안 겹친다
- ⚠️ **꾹 누른다는 것을 화면에 적는다** (`hint_drink` · 두 언어). 탐험 화면의 설명 줄과
  같은 규칙이다 — 안 적으면 손가락을 떼기 전에는 아무도 그것을 알 길이 없다

```bash
npm run test:drink   # 진짜 마우스로 누르고 있어 본다 (브라우저 필요)
```

⚠️ **`el.click()` 으로는 아무것도 못 잰다** — pointerdown 을 제대로 안 보내서 꾹 누르기
갈래를 통째로 건너뛴다 (`checktut` 과 같은 이유다). `page.mouse` 로 눌러 본다.
⚠️ **선반은 화면 아래쪽이라 «보이는 데까지 굴려 놓고» 재야 한다** — 안 그러면 마우스가
빈 곳을 누르고 「아무 일도 안 일어난다」가 나온다 (실제로 일곱 건이 그렇게 빨개졌다).
⚠️ 끌기 검사는 **「한 병도 안 준다」로 재지 않는다** — 합성 마우스로 24px 끄는 것은
스크롤이 아니라 손을 뗄 때 click 이 그대로 온다. 지킬 것은 **연속 소비가 안 도는** 쪽이다.
사보타주 다섯으로 확인했다 — `onpointerdown` 빼기 · 소리를 카드로 옮기기 ·
`data-sfx` 우선을 없애기 · 움직임 취소 빼기 · pointerup 에서 안 멈추기.

## 공방 — 비법서는 답지가 아니라 «수수께끼»다 (흐린 장)

비법서가 재료 이름을 다 적어 주면 **발견이 없다** — 인터넷 공략을 그대로 따라 하는
것과 다를 바가 없다. 그렇다고 「맞혀 보세요」로 두면 벽이 된다: 평야 재료만 25가지라
2개 조합이 **300가지**이고, 재료가 12개인 레시피도 있다.

그래서 **맞히기 퀴즈가 아니라 「채워 가기」**다 (`data.js` 의 `LORE`).

- 한 장에서 가려지는 것은 **최대 두 칸**(`hiddenMax`). ⚠️ **한 칸은 언제나 보인다**
  (`keepShown`) — 재료 두 가지짜리를 둘 다 가리면 300가지에서 고르는 벽이 된다
- ⚠️ **틀려도 재료가 한 톨도 안 없어진다.** 이 한 줄이 부담을 없앤다 — 잃을 것이
  없으니 마음껏 넣어 볼 수 있고, 그래서 「실패」라는 말이 화면에 안 나온다.
  AP 만 든다 (그것도 안 들면 아무 생각 없이 누르게 된다)
- **하나만 맞아도 진도가 난다** → 몇 번 저으면 반드시 풀린다. **절대 안 막힌다**
- ⚠️ **밭 작물과 크리처는 안 가린다.** 채집으로 안 나와서(심거나 녹여야 한다)
  가려 놓으면 「어디서 구하는지」 자체를 알 수가 없다 — 수수께끼가 아니라 벽이다
- ⚠️ **튜토리얼이 가르치는 물약도 안 가린다**(`LORE.plain`). 배우는 중의 수수께끼는
  안내가 아니라 시험이다
- ⚠️ **수수께끼에 이모지를 넣지 않는다.** 「지대 + 이모지」로 좁히면 후보가
  평균 1.05개라 **답을 적어 주는 것**이 된다. 「지대 + 흔한 정도」라야 일곱쯤 남는다
- ⚠️ **가리는 칸은 레시피 id 로 «고정»한다.** 무작위면 어제 밝혀낸 칸이 오늘 다시
  가려진다 (날씨·일지와 같은 규칙이다)
- ⚠️ **모르는 칸을 채우는 것 자체가 「손으로 담기」**다. 예전 규칙(손으로 담으면
  `S.want` 를 놓는다)을 그대로 두면 넣는 순간 목표가 사라져 **영영 못 맞힌다** —
  아직 모르는 칸이 남아 있는 동안만 목표(`S.guess`)를 붙든다
- **추리가 싫은 사람의 우회로** — 어떤 재료를 `masteryAt`(30)개 모아 보면
  「잘 아는 재료」가 되어 **모든 장에서** 이름이 보인다. 채집만 해도 비법서가 밝아진다.
  ⚠️ 누적은 `S.gathered` 다 — `inventory`(지금 가진 것)를 쓰면 **쓰는 순간 숙련이
  도로 사라진다.** `addInv()` 한 곳에서 센다
- ⚠️ **하던 사람에게서 뺏지 않는다** (세이브 13). 이미 가진 장은 전부 밝혀진 것으로
  치고, 수수께끼는 그 뒤에 오는 장부터다 — 새 재미를 주려다 어제까지 만들던 물약을
  수수께끼로 바꾸면 그건 뺏는 것이다. `npm run test:lore` 가 옛 세이브를 심어 확인한다

## 비법서 장은 «퀘스트 한 장 + 조합이 흘리는 한 장»으로 나온다

⚠️⚠️ **한 번에 들어오는 장은 «한 장»이다 — 퀘스트도 그물도 예외 없다.**
예전에는 퀘스트가 3~16장을, 그물(단계 지급)이 매력 단계마다 등급을 통째로
(뮤즈에서 **50장**) 쏟아부었다. 그러면 셋이 한꺼번에 무너진다:

- **여러 장은 «숫자»가 되고, 한 장은 «이름»이 된다** — 「📖 비법서 6장」·「새 장이
  6장 늘었어요」에는 **무엇을 받았는지가 어디에도 안 나온다.** 한 장이면
  「'생기 물약' 장을 얻었어요」라고 쓸 수 있고, 그때부터 보상이 읽힌다
- **흐린 장이 한꺼번에 열리면 수수께끼가 뭉개진다** (`LORE` 의 그 재미다).
  이 이유는 원래 적혀 있었는데 **그물 쪽에는 같은 잣대를 한 번도 안 댔다**
- **보상의 크기가 이야기와 무관해진다** — `q_bloom` 이 16장이던 것은 그 퀘스트가
  커서가 아니라 «상급 물약의 절반이 거기 걸려서»였다

### 퀘스트 = 「이름 있는 한 장」 (열일곱 전부)

`QUESTS` 의 **`reward.page`**(레시피 id 하나)다. 어느 장인지는 **손으로 고른다** —
그 이야기가 가리키는 것으로. 등급 등분의 나머지가 아니다.
(부엌 첫 퀘스트 → 온기 물약 · `q_egg`「알」 → 햇살 암탉 · `q_seal`(무쇠 20) → 무쇠 물약)

- ⚠️ **`reward.pages`(여러 장)는 없앴다.** 칸 이름이 곧 규칙이다 — 되살리면
  `checkdata` 가 「아직 «여러 장»을 준다」로 잡는다
- ⚠️⚠️ **그 시점에 «만들 수 있는» 장이어야 한다.** 재료가 아직 안 열린 지대에서만
  나오면 받아도 못 만드는 죽은 장이다. **매력 100(여신)에서도 만들 수 있는 것은
  57장뿐**이라(맵이 매력 510 까지 열린다) 3~5막 퀘스트도 **중급까지만** 줄 수 있다 —
  상급을 주고 싶어도 못 준다. `checkdata` 가 재료 «닫힘»까지 돌려서 본다
- ⚠️ **열일곱이 서로 겹치면 안 된다** — 같은 장을 둘이 주면 한쪽은 빈손이다

### 그물 = 「단계가 천장 · 조합이 흘린다」

```
천장  = PAGE_TIERS (매력 단계가 등급을 «열어 둔다» · 주지는 않는다)
흘림  = 조합에 성공할 때마다 세고, D.PAGE_DRIP(4)회마다 «아직 없는 장 하나»
차례  = D.pageFlow() — 천장 오름차순 → id 순, 퀘스트 몫은 «맨 뒤»
```

**왜 조합인가** — 장은 조합에 쓰는 물건이고, 「만들다 보니 다음 장이 떠올랐다」가
연금술사의 이야기다. 게다가 **이미 있는 자리**다(`brewReward` 가 현자의 결정을 주는
그 한 곳) — 새 경로를 안 만든다. AP 25 가 드니 제동도 이미 있다.
하루 AP 의 3할을 조합에 쓰면 **하루 5.3장 · 113장에 21.5일**이다.

- ⚠️⚠️ **천장에 막히면 카운터를 «안 쓴다».** 밀림을 허용하면 단계가 오르는 순간
  밀린 몫이 **여러 장 한꺼번에** 터져 지금 고친 그 문제가 그대로 돌아온다.
  새싹에서는 기초 여섯 장이 이미 다 있어서 **실제로 막혀 있는 구간**이다
- ⚠️⚠️ **퀘스트 몫 열일곱은 흘림 차례의 «맨 뒤»다.** 안 그러면 흘림이 먼저 건네
  버려 **퀘스트를 깼는데 빈손**이 된다. 뒤로 «미는» 것이지 «빼는» 것이 아니라서,
  퀘스트를 한 줄도 안 해도 흘림이 결국 전부 준다 — **「그물을 없애지 않는다」**
  (퀘스트가 유일한 출구가 되면 하나가 막히는 순간 게임이 멈춘다)가 그대로 지켜진다
- ⚠️ **단계가 오르는 자리(`checkUnlocks`)에서는 장이 «안» 온다.** 거기서 주던 것을
  뺀 자리다 — 지금 단계가 하는 일은 천장을 올리는 것뿐이다
- ⚠️ **새싹(0)의 기초 물약 여섯 장만 자동이다** (`grantStarterPages`). 첫 퀘스트가
  「생기 물약을 만들어라」인데 그 장이 없으면 시작조차 못 한다
- ⚠️ **`S.pageDrip` 은 «더하는» 칸이라 `SAVE_VER` 를 안 올린다** (`miniLog` 와 같다).
  **누적 조합 수(`record.brewOk`)로 되짚지 않는다** — 그러면 옛 세이브가 켜자마자
  밀린 몫을 통째로 받아 지금 고친 그 문제가 그대로 돌아온다. 0 에서 센다
- ⚠️ **`pagesForSpec` 의 `#i/k`(등분)는 지웠다** — 퀘스트가 등분을 안 쓰게 되면서
  쓰는 데가 없어졌다 (`.ask-chip.on` 과 같은 규칙: 안 쓰는 규칙을 남기지 않는다)

### 검사

```bash
npm run test:page    # 조합을 «진짜 버튼으로» 저어 흘림을 잰다 (브라우저 필요)
```

- `checkdata` — 퀘스트가 한 장씩인가 · 겹치지 않는가 · **그 매력에서 만들 수 있는가** ·
  흘림이 136장을 다 덮는가 · **퀘스트 몫이 맨 뒤인가**.
  **몇 개를 쟀는지 통과할 때도 낸다** — 옛 검사가 `reward.pages` 를 훑고 있어서
  그 칸이 없어지자 **반복문이 0번 돌아 아무것도 안 재고 통과했다**
- `checkbalance` — 한 달 안에 다 나오는가 · 하루에 쏟아지지 않는가 ·
  퀘스트가 장의 주된 출구가 아닌가
- `checkpage` — 천장에 막히면 카운터가 안 오르는가 · 천장이 올라도 안 쏟아지는가 ·
  `PAGE_DRIP` 회째에 «한 장» 오는가 · 토스트가 **이름**을 부르는가 ·
  단계가 올라도 그 자리에서는 안 오는가 · 흘림만으로도 136장이 다 나오는가
- 사보타주 아홉으로 확인했다 — 여러 장으로 되돌리기 · 못 만드는 장 주기 ·
  맨 뒤로 미는 줄 빼기 · 천장 무시 · 밀림 허용 · 보상 줄을 숫자로 · 배선 끊기 ·
  요율 1 · 요율 20

## 공방 — 비법서에 있는 것만 만들 수 있다

**레시피는 맞히는 것이 아니라 「장」을 갖는 것이다** (`UI_POLICY.md` 3-2d).
`S.discovered` 가 곧 가진 장이고, 뜻만 바뀌어서 **마이그레이션이 필요 없다.**

⚠️ **장이 없는 조합은 AP 를 쓰기 «전»에 막는다.** 먼저 깎고 나서 알려 주면
플레이테스터가 그만둔 그 좌절이 이름만 바꿔 돌아온다.

## 공방 — 몇 개 만들지 먼저 묻는다

「조합하기」를 누르면 **개수 패널**이 뜬다 (`openBrewQty`). 1개 / +1 / +10 / 최대 로
고르고, **그 개수만큼의 재료 소진량과 드는 AP** 를 그 자리에서 보여 준다.

- ⚠️ **관문을 다 지난 «뒤에» 묻는다.** 두 개 미만 · 흐린 장 · 없는 장을 먼저 거른다 —
  묻고 나서 「없는 장입니다」라고 하면 AP 를 먼저 깎는 것과 같은 종류의 실례다.
  **취소하면 아무 일도 없다** (AP 도 안 깎인다)
- ⚠️ **최대치는 재료와 AP 를 «둘 다» 본다** (`brewMax`). 재료만 보면 「최대」를
  눌렀을 때 도중에 AP 가 떨어져 절반만 만들어지고, 화면에 적힌 수와 실제가 갈린다
- ⚠️ **한 개밖에 못 만들면 안 묻는다.** 답이 하나뿐인 질문이라 손만 한 번 더 든다
- ⚠️ **튜토리얼 중에는 안 묻는다.** 거기서 가르치는 것은 「담고 → 젓는다」 한 동작이고,
  사이에 패널이 끼면 막이 기다리는 신호가 안 와 **3단계에서 갇힌다** (실제로 그랬다)
- 만드는 것은 `doBrew(n)` 하나다 — `brew()` 를 n 번 부르면 결과 모달이 n 번 뜬다.
  결과 모달에는 개수를 **×3** 으로 붙인다 (안 적으면 몇 개 나왔는지 알 길이 없다)
- ⚠️ **검사기가 `brew()` 를 부르는 자리는 패널을 지나야 한다** — 재료를 넉넉히 심어
  두면 `brew()` 는 묻기만 하고 «아직 안 만든다». 그대로 세면 「보상이 0」·「진행이 0」이
  나오는데, 그건 고장이 아니라 아직 안 누른 것이다 (`checkui` 세 곳 · `checklore` ·
  `checkfarm` 이 그랬다). 흐린 장을 «맞혀 보는» 자리는 예외다 — 거기서는 `brew()` 가
  수수께끼 갈래로 빠져서 패널까지 안 온다

### ⚠️ 검사기가 레시피를 채울 때는 **흐린 장을 먼저 밝힌다**

`fillFromRecipe()` 는 **아는 칸만** 담는다 (그것이 맞는 동작이다 — `LORE`).
그래서 「장을 가졌다」고 `S.discovered` 에만 넣어 두면 솥이 반만 차고, 검사는
「솥에 담긴 것이 레시피와 다르다」로 **엉뚱한 곳을 가리킨다.**

```js
D.hiddenOf(r).forEach(id => learnIng(r.result.id, id));   // 채우기 «전»에
```

`checkmelt` · `checkfarm` 이 이것 때문에 오래 빨간 채로 있었다 — 둘 다 흐린 장이
생기기 «전»에 쓴 검사라, 게임이 아니라 검사가 옛 규칙으로 돌고 있었다.
**여기서 볼 것이 수수께끼가 아니면 밝혀 놓고 시작한다.**

## 공방 — 고른 레시피는 계속 따라온다

레시피를 누르면 `S.want`(요구 재료 목록)가 남는다. **조합해도 지워지지 않는다.**

- 조합 뒤 `refillFromWant()` 가 남은 재료로 솥을 **다시 채운다** — 같은 것을 여러 번 만들 때
  매번 레시피를 다시 누르지 않아도 된다
- 재료가 떨어진 자리는 회색(`.c-slot.lack`)으로 남아 무엇이 없는지 보여 준다
- **손으로 담거나 빼거나 솥을 바꾸면 `S.want` 를 비운다.** 그 순간부터는 사람이 정한 것이
  기준이어야 하고, 그러지 않으면 다음 렌더에서 조용히 되돌아간다
- 솥은 `potFor(n)` 이 **재료 가짓수에 딱 맞는 가장 작은 것**으로 골라 준다.
  2가지 레시피에 12구 솥을 꺼내 놓으면 빈 구멍 열 개가 '더 넣으라'는 뜻으로 읽힌다

## 튜토리얼 (tutorial.js) — 새 플레이어가 반드시 지나는 외길

인트로가 끝나면 이어받아 열여덟 단계를 안내한다. **여기서 한 자리만 막혀도 새 플레이어는
게임을 시작조차 못 한다.** 그래서 규칙이 몇 가지 있다.

- **막의 구멍은 진짜 구멍이다.** 막을 이루는 `path` 하나에 `fill-rule="evenodd"` 로 안쪽
  사각형을 빼서, 칠해진 곳만 클릭을 먹고 구멍은 아래 버튼이 그대로 받는다.
  조각 네 개를 붙여 만들면 경계에서 클릭이 새고 화면이 움직일 때 어긋난다
- **가리키는 것은 흰 장갑 손이다** (`tut-hand.png` · `HAND_IMG` · `.tut-hand`). 집게손가락이 «위»를
  가리키는 그림 하나를 두 자리에서 쓴다 — 구멍의 빈 쪽에서 두드리는 손(아래를 가리킬 때는
  `.down` 으로 뒤집는다)과, **눌러서 넘기는 대사**일 때 말풍선 오른쪽 아래 모서리에서 대사를
  두드리는 손(`.tut-more`). 지시를 따라야 넘어가는 단계에는 말풍선의 손이 없다 — 있으면
  눌러서 건너뛸 수 있는 것처럼 보인다.
  · 글자가 아니라 그림이라 대비 검사와 무관하다 — 예전 「▾」는 막 위에서 대비를 내려고
    검은 알약을 깔아야 했다. 이름은 `aria-label` 이 맡는다. 그림의 `?v=` 는 tutorial.js 의
    스크립트 태그에서 물려받는다 (`ASSET_Q`) — index.html 의 일괄 치환 한 번이면 된다
  · ⚠️ **가로 가운데 맞추기에 transform 을 안 쓴다** (`margin-left`). 두드리는 애니메이션이
    transform 을 통째로 덮어써서 손이 옆으로 튄다
  · 자리는 `place()` 가 잡고 크기는 `HAND_W`·`HAND_H`(40×60) 다 — CSS 의 `--tut-hand-w/h` 와 같은 값
- **대상은 요소가 아니라 선택자로 적는다.** `render()` 가 화면을 통째로 다시 그리므로
  요소를 붙들어 두면 다음 렌더에 문서에서 떨어져 나간다 (토스트에서 겪은 것과 같다)
- **막다른 길을 만들지 않는다.** 구멍 대상을 못 찾으면 막이 클릭을 먹지 않는 상태(`.loose`)로
  떨어져 아무 데나 누를 수 있다. 안내가 헛도는 편이 갇히는 것보다 낫다
- **구멍은 매 프레임 다시 잰다.** 한 번 재고 마는 것으로는 못 맞춘다 — 화면이 페이드 인
  하는 동안 `.screen` 이 `translateY(6px)` 로 밀려 있어서, 그때 잰 구멍이 **끝까지 6px
  어긋난 채로 남았다** (스크롤·리사이즈가 없으면 다시 잴 계기가 아예 없다).
  대신 값이 그대로면 DOM 을 안 건드린다
- **좌표계는 `#tut` 의 제 상자다.** `documentElement.clientHeight` 로 viewBox 를 잡으면
  그 값과 층의 실제 높이가 어긋나는 순간(주소창이 접히는 모바일) 그림이 통째로 늘어난다
- **막은 모달보다 아래에 깔린다**(`z-index: 40` < 모달 50). 위에 두면 튜토리얼 중에 뜨는
  조합 결과 팝업이 막에 덮여 「확인」이 안 눌린다 — **화면이 통째로 먹통이 된 것처럼 보인다.**
  그리고 팝업이 떠 있는 동안은 감추되 **추적 루프는 계속 돌린다** — 같이 멈추면
  팝업이 닫혀도 막이 안 돌아온다 (설정·확인 패널은 닫을 때 `render()` 를 안 부른다)
- **졸업할 때 바디파츠를 전부 상한(150%)으로 맞춘다** (`maxTune`). 이 게임은 «날씬해지는»
  이야기라 100% 로 출발하면 첫 물약부터 표준보다 마른 몸이 되어 **줄어들 자리가 없다.**
  ⚠️ `bodyTune` 은 세이브가 아니라 제 열쇠(`TUNE_KEY`)에 따로 산다 — 그래서
  마이그레이션이 아니라 «졸업할 때 한 번»이 맞는 자리다 (이미 졸업한 사람이 고른 값을
  나중에 덮으면 그건 뺏는 것이다). 상한은 부위마다 다르므로 **`tuneMaxOf` 를 지난다** —
  150 을 박아 두면 `TUNE_PARTS` 를 낮췄을 때 조용히 범위를 벗어난다.
  `npm run test:tut` 이 졸업 뒤의 값을 상한과 견준다 (사보타주로 확인했다)
- **`tutorialDone` 을 켜 주는 곳은 졸업 단계 한 곳뿐이다.** 이 문이 3구 무쇠 솥 · 크리처 탭 ·
  옷장 열두 칸을 연다. 튜토리얼이 끝났는데 안 켜져 있으면 `finish()` 가 한 번 더 확인한다
- **갈아입기는 졸업 뒤에 온다.** 튜토리얼 전의 마이 룸에는 인트로의 공주 그림이 서 있어서
  (`roomFigure`) 옷을 갈아입어도 화면이 하나도 안 변한다
- **졸업 선물은 신발이다** (`GIFT_SHOES` = `shoes_maryjane`). ⚠️ 원피스(`dress_onepiece`)를
  주던 자리다 — 공주는 이미 원피스를 입고 들어오는데 **발은 맨발**(`shoes_none`)이라,
  선물이 이미 채워진 자리에 하나를 더 얹고 빈 자리는 그대로 남았다.
  **채워야 할 자리를 채우는 쪽이 선물답다.** 선물을 바꾸면 대사(`tut_h5`)와
  안내(`tut_act_shoes`)도 두 언어로 같이 고치고, `checktut` 의 「선물받은 구두를 신었는가」도
  같이 고친다 (id 만 바꾸면 검사가 옛 선물을 계속 본다 — 사보타주로 확인했다)
- **한 번만 도는 효과는 `once()` 로 감싸고 표시를 세이브에 남긴다.** 안 남기면 새로고침으로
  같은 단계에 다시 들어설 때마다 첫 재료 주머니가 또 들어온다
- ⚠️ **튜토리얼 중의 채집은 «비법서에 있는 것»만 나온다** (`game.js` 의 `tutorialGatherPick`).
  첫 실습이 「두 개 주워 와서 솥에 넣는다」인데, 아무거나 주우면 그 둘이 가진 장(생기 물약 =
  산딸기 + 약초)을 못 이루어 **첫 실습이 「없는 장」으로 끝났다.** 졸업 전에는 그 맵에서
  «가진 장으로 만들 수 있는» 레시피의 재료만, 그중 **아직 없는 칸**을 먼저 준다 — 두 번
  주우면 반드시 한 장이 찬다. 히든 재료도 안 나오고 천장 셈도 안 한다. 그 맵에 만들 수 있는
  장이 없으면 비법서의 아무 재료, 그것도 없으면 평소대로다 (막다른 길을 만들지 않는다).
  졸업하면 한 글자도 안 건드린다. `checktut` 이 졸업 전에 서른 번 주워 보고(전부 장의
  재료인가 · 두 재료가 다 나오는가) 두 번째 조합까지 성공하는지를 본다 (사보타주로 확인했다)
- ⚠️ **손은 한 화면에 하나다** (`tapHandOn`). 말풍선의 「눌러서 넘기는」 손과 구멍 위의
  손이 같이 뜨면 새 플레이어는 **어디를 눌러야 하는지 두 곳을 놓고 고르게 된다** —
  「넣기만 하면 되는 거야?」를 읽는 중인데 「조합하기」 버튼에도 손이 얹혀 있었다 (신고받았다).
  가르는 줄은 하나다: **아직 읽을 대사가 남았으면 말풍선**, 마지막 대사까지 읽고
  **「어딜 눌러」라고 가르쳐 주면**(지시문 + `wait`) **그 UI 위**.
  · ⚠️ **두 곳에서 따로 판정하지 않는다** — `html()` 의 말풍선 손과 `place()` 의 구멍 손이
    각자 셈하면 어긋나는 순간 손이 둘이 되거나 하나도 없어진다. **함수 하나(`tapHandOn`)를
    둘이 같이 본다**
  · ⚠️ **예전에는 「한 번 만지면 접는다」였다**(`did['touch:<id>']` · `pointerdown` capture).
    만지기 «전»에는 여전히 둘이라 규칙이 아니라 임시방편이었다 — 통째로 지웠다.
    「보기만 하는」 구멍(위쪽 줄 · `wait` 가 없다)에는 이제 구멍 위의 손이 아예 없다
  · `checktut` 의 **「손」**이 단계·대사마다 본다 — ① 둘이 아닌가 ② 하나도 없지 않은가
    ③ 가리켜야 하는 자리에 **구멍 위의** 손이 있는가 ④ 아닌데 구멍 위에 있지 않은가.
    **몇 번 쟀는지와 두 갈래(구멍 위 · 말풍선)를 각각 몇 번 봤는지도 낸다** — 한쪽이 0이면
    그 방향은 아예 안 잰 것이다. 사보타주 둘로 확인했다 (늘 띄우면 「손이 둘」 ·
    늘 접으면 「지시문이 떴는데 그 자리에 손이 없다」)
- ⚠️ **구멍은 «대사마다» 다를 수 있다** (`holes` — `hole` 과 다른 칸이다).
  위쪽 줄 단계는 첫 줄이 ⚡ 행동력, 둘째 줄이 🌱 매력 총합을 가리킨다. 예전에는
  `hole` 에 둘을 같이 적어 **두 곳을 한꺼번에** 뚫어 놓았는데, 그러면 지금 말하는 것이
  어느 쪽인지 알 수가 없다 (손도 첫 번째 것에만 선다). `holes` 는 **대사 하나에 하나**다.
  · 이런 「가리켜 보여 주는」 단계에서는 **손이 구멍 위에 선다** — 「어딜 눌러」(`wait`)와
    같은 자리다. 손은 여전히 하나이므로 **말풍선에는 손이 없다.**
  · ⚠️ **그러면 넘기는 길이 화면에 없다** — 그래서 그 단계에서만 **막을 눌러도 넘어간다**
    (`skim`). 지시를 따라야 넘어가는 단계(`wait`)에는 절대 안 붙인다: 거기서 막이
    넘어가면 막이 막는 뜻이 없어진다. `checktut` 이 **진짜로 막을 눌러 보고**
    안 넘어가면 「갇힌다」로 잡는다 (사보타주로 확인했다)
  · ⚠️ **구멍 «안»도 같이 눌려야 한다** (`.tut-holetap`). 막은 칠해진 자리에서만
    클릭을 받는데(`visiblePainted`) 손이 가리키는 곳은 «진짜 구멍»이라 클릭이 그대로
    통과한다 — 그 대상(⚡ 행동력 · 🌱 매력 총합)은 눌러도 아무 일이 없는 표시라
    **손끝을 눌렀는데 반응이 하나도 없었다** (「손가락이 가리키는 곳을 눌러도 진행이
    안 된다」로 신고받았다). 구멍 위에 `pointer-events="all"` 짜리 **안 보이는 판**을
    덮어 클릭을 받아 준다.
    · ⚠️ **판에 `onclick` 을 따로 달지 않는다** — 막의 것과 겹쳐 **두 번 불려
      대사가 둘씩 넘어간다.** 클릭이 막까지 거품처럼 올라가게 두면 한 번이다
    · ⚠️ **`wait` 단계에는 절대 안 덮는다** — 거기서는 구멍 안의 «진짜 버튼»을
      눌러야 진행되므로, 판이 덮이면 튜토리얼이 그 자리에서 통째로 막힌다
      (지금 `wait` 13곳에 판 0 · 가리키는 단계에만 1)
    · 자리는 `place()` 가 구멍과 **같은 값**(`PAD`·`RAD`)으로 매 프레임 맞추고,
      `lastTap` 이 `lastD` 와 **같은 자리에서 같이** 비워진다 — 한쪽만 비우면
      다음 대사에서 판이 안 그려진다
    · `checktut` 이 **두 길을 대사마다 번갈아** 눌러 본다 (막의 칠 · 구멍 안) —
      각각 몇 번 눌렀는지도 낸다. 사보타주 둘로 확인했다 (판을 빼면 「구멍 안을
      눌러도 안 넘어간다」 · 판에 `onclick` 을 달면 「한 번 눌렀는데 2칸 넘어갔다」)
  · ⚠️ **`Tut.targets()` 로 「제 것을 가리키는가」를 재면 안 된다** — 구멍을 뚫는 쪽과
    대상을 알려 주는 쪽이 같은 함수라, 대사 번호를 통째로 무시해도 **둘 다 같이 틀려서
    통과한다**(「스스로 맞는 검사」). `checktut` 의 **「가리키는 단계」**는 **단계표에 적힌
    선택자**를 직접 읽어 ① 그 위에 구멍이 뚫렸는가 ② 손이 그 한가운데를 가리키는가를
    본다. 몇 줄을 쟀는지도 낸다. 사보타주로 확인했다 (번호를 무시하면 「.header-charm
    위에 안 뚫려 있다」 · 「202px 옆을 가리킨다」)
  · ⚠️ **막이 넘어가면 `blockedCheck` 이 헛짚는다** — 탭이 바뀐 것을 「막힌 자리가
    눌렸다」로 읽는다. 단계·대사가 «같이» 움직였으면 샌 것이 아니라 넘어간 것이다
- ⚠️ **대사 안에 플레이어 이름을 넣을 때는 «접어서» 넣는다** (`{name}` · `TALK_VARS`).
  졸업 대사(`tut_h2`)가 그 이름을 부르며 분홍으로 강조한다(`.tut-hi`) — 말풍선은
  `innerHTML` 로 그려지므로 **안 접으면 이름이 그대로 태그가 된다.**
  · ⚠️ **`game.js` 의 `escHtml` 은 최상위 `const` 라 `window` 에 안 붙는다** —
    `window.escHtml` 로 찾으면 늘 `undefined` 이고, `|| String` 같은 보조 길을 두면
    **한 글자도 안 접은 채 조용히 지나간다** (그렇게 써 놨다가 잡았다). 전역은 «이름»으로 찾는다
  · **값은 한 곳에서 모든 줄에 넘긴다** — 줄마다 골라 넘기게 하면 새 대사를 쓸 때마다
    빠뜨린다. `{name}` 이 없는 대사에 넘겨도 아무 일도 안 일어난다
  · `checkui` 의 **「15 졸업(이름 강조)」**이 대비를 재고, 이름에 태그를 넣어 **접히는지**
    본다 (사보타주로 확인했다 — 안 접으면 「태그가 그대로 살아났다」)
- **말풍선의 이름은 설정상의 이름이 아니라 「부르는 말」이다** (`speakerName()`).
  공주는 **플레이어가 지은 이름**으로 뜨고(진짜 이름 「그위리엘」은 본인도 모른다),
  요정 대모는 인트로와 같은 `sp_fairy` 문자열을 쓴다. `SPEAKERS` 의 `name` 은
  설정상의 이름 그대로 두고 화면에서만 갈아 끼운다 — STORY.md 「호칭 규칙」
- 단계마다 `tab` 을 적어 그 화면으로 옮겨 놓는다. 새로고침하면 늘 마이 룸에서 다시 시작하므로,
  이게 없으면 '솥에 넣으세요' 라고 적힌 막이 마이 룸 위에 떠 있게 된다
- ⚠️ **지시문은 구멍 대상의 «이름»을 부른다** — 화면에 적힌 그대로. 마이 룸 탭이
  「🧴 물약」에서 「🎒 잡화」로 바뀐 뒤에도 지시문은 「🧴 물약 칸을 누르세요」로 남아
  있었다: 구멍은 잡화 탭에 맞게 뚫려 있는데 글은 없는 칸을 불렀고, 눌러 보는 검사는
  **통과**였다 (구멍은 맞았으니까). 화면의 이름을 바꿨으면 그것을 부르는 지시문·대사도
  두 언어로 같이 고친다.
  · `checktut` 의 **「지시문↔대상」**이 단계마다 대상의 이름을 **화면에서 읽어**(aria-label ·
    이름 칸 · 제 글자 · 감싸는 카드·블록의 제목 · 옷장이면 펼쳐진 탭) 지시문에 그 낱말이
    있는지, 지시문의 이모지가 대상에 있는 그림인지를 **언어를 실제로 바꿔 가며 두 언어로**
    잰다. 몇 단계를 쟀는지도 같이 낸다 — 지시문이 있는 단계 수(13)에 못 미치면 실패다.
    사보타주 셋으로 확인했다 (옛 「🧴 물약 칸」 · 그림만 다른 「🧴 잡화 칸」 · 영어만 옛 문구)
  · `tools/checktuttext.js` 는 **브라우저 없이** 본다 (`npm test`) — 단계표의 키가 두 언어에
    다 있는가 · 「X 칸」/“the X section” 의 X 가 마이 룸 탭 이름인가 · 지시문의 따옴표
    이름(「생기 물약」)이 그 언어의 이름표에 있는가. 단계표는 `Tut.stepList()` 로 읽는다

```bash
npm run test:tut                       # 열여덟 단계를 진짜 마우스로 눌러 끝까지 가 본다 (지시문↔대상 · 두 언어)
npm run test:tuttext                   # 지시문·대사의 글자만 (브라우저 없이 · npm test 가 돌린다)
TUT=1 node tools/checkui.js            # 튜토리얼 화면의 대비·레이아웃
```

`checktut` 은 **`el.click()` 을 쓰지 않는다.** 그것은 `pointer-events` 를 무시해서 막이 뚫려
있는지 없는지를 전혀 검사하지 못한다 — `page.mouse.click` 으로 누르고, 막의 칠해진 자리인지는
`path.isPointInFill()` 로 판정한다.

**눌리는지만 보면 어긋난 구멍을 못 잡는다.** 여백이 8px 라 그만큼 밀려도 한가운데는 여전히
뚫려 있다. 그래서 막의 칠을 이분법으로 더듬어 네 변의 자리를 재고 `대상 + PAD` 와 견준다
(허용 1.5px). 위의 6px 사고가 이 검사 없이는 통과로 보였다.

## 퀘스트 — 초반에는 **손이 비지 않는다** · 점은 늘 켜져 있다

- ⚠️ **첫 퀘스트는 요정 대모가 준다**(`q_meet` — 「셰프를 고용했어요」). 튜토리얼을 마치자마자 도착해
  있어야 졸업 직후가 「이제 뭘 하지」로 시작하지 않는다. 목표가 **한 번**인 이유는
  부엌이 하루 한 번이라서다 — n 을 키우면 첫 퀘스트가 며칠짜리가 된다
- ⚠️ **1막에서는 점수 문턱을 기다리게 하지 않는다** (`refreshQuests` 의 `EARLY_ACT`).
  하나를 끝냈는데 다음 `at`(6 · 10 · 14…)에 못 미치면 **퀘스트가 없는 구간**이 생기고,
  그 구간이 하필 게임을 막 시작한 자리다. 비었을 때만 표 순서대로 다음 것을 내보낸다.
  **`need` 는 절대 안 건너뛴다** — 점수가 아니라 이야기라, 건너뛰면 아직 만나지도
  않은 사람이 부탁을 해 온다. **2막부터는 안 민다** (`need` 가 이미 순서를 잡는다)
- ⚠️ **1막과 2막 사이에 다리가 있다** (`q_gift`「그냥 하나씩」 · 클레멘 · at 50).
  `q_bloom`(매력 60)을 끝내도 `q_glass` 는 호감도가 있어야 열려서, 그 사이에
  **퀘스트가 하나도 없는 구간**이 있었다 — 매력은 물약을 «마셔서» 오르고 호감도는
  «선물»해야 오르는데 그 연결이 화면 어디에도 없었다 (`PLAYFLOW.md` 8장).
  · **목표가 곧 2막의 문이다** — 「아무하고나 «친함»(2단계)」(`goal.kind = 'bond'`).
    `checkbalance` 가 둘이 같은 단계인지 본다: 어긋나면 **깼는데 2막이 안 열리거나**
    (목표가 더 낮을 때) **열린 뒤에도 퀘스트가 안 끝난다**(더 높을 때)
  · ⚠️ **「선물 n회」로 두지 않는다.** 여섯 명에게 하나씩 흩어 주면 목표는 찼는데
    아무도 친함이 안 되어 **2막이 잠긴 채로 남는다** — 다리를 건넜는데 문이 닫혀
    있는 것은 손이 비는 것보다 나쁘다. **호감도 «단계»를 보는 상태형**이라 그 일이
    안 생기고, 호감도는 안 내려가므로 진행도도 뒤로 안 간다 (`questProgress` 의 `bond`)
  · ⚠️ **`giveGift()` 가 `refreshQuests()` 를 부른다.** `renderGift()` 는 그 시트 안만
    다시 그려서, 안 부르면 막대가 찬 뒤에도 칩이 **「!」 인 채로 남아** 다음 `render()`
    까지 거짓말을 한다 — `doAsk` 에서 배운 것과 같은 자리다
  · `npm run test:bond` 가 **진짜로 선물해서 걸어 본다** — 1막을 막 끝낸 자리에
    저절로 오는가 · 다섯 종을 주면 차는가 · **`render()` 없이** 칩이 「받아 가라」로
    바뀌는가 · **그 순간 🔒 유리관이 같이 열리는가**. 사보타주 둘로 확인했다.
    ⚠️ **`checkstory` 는 이것을 못 잰다** — 거기서는 1막을 통째로 「끝낸 것」으로
    심고 시작해서 이 퀘스트도 `done` 에 들어간다
- ⚠️ **퀘스트가 있으면 칩의 오른쪽 위 표시는 늘 켜져 있다.** 예전에는 「아직 안 열어
  본 것」에만 찍었는데, 한 번 열면 꺼져서 **하던 퀘스트가 있다는 것 자체가 안 보였다**.
  · **표시는 둘이고 뜻이 다르다** — `full` 로 갈라서 «절대 같이 안 뜬다»:
    아직 못 냄 → **정적인 「!」**(`.qc-badge`) · 다 참 → **숨 쉬는 붉은 점**(`.qc-dot`)
  · ⚠️ **둘을 반대로 두지 않는다 — 그렇게 돼 있다가 신고받은 자리다.** 늘 뛰는 점이
    「아직 못 냈다」를 가리키면 그건 길잡이가 아니라 **재촉**이고, 정작 받아 갈 것이
    생긴 순간에 조용한 「!」 로 바뀌어 **더 얌전해진다.**
    **움직이는 것은 «누르면 지금 무슨 일이 일어나는» 자리에만 준다**
  · ⚠️ **검사기가 «종류»만 세면 안 된다** — 둘을 맞바꿔 놔도 그대로 통과한다.
    `checkui` 의 「퀘스트점」이 **어느 쪽이 움직이는지**(`animationName`)까지 본다
- ⚠️⚠️ **칩 둘레의 맥박(`.quest-chip.fresh`)은 「인트로 컷씬을 아직 안 봤다」는 뜻이다.**
  예전에는 「아직 한 걸음도 안 뗐다」였는데, **상태형 목표**(마을·호감도·매력)는 한 걸음
  떼는 데만 며칠이 걸려서 **그동안 내내 뛰었다** — 위의 「움직이는 것은 «누르면 지금
  무슨 일이 일어나는» 자리에만」을 바로 그 칩이 안 지키고 있던 셈이다.
  안 본 인트로가 있으면 누르는 순간 **새 장면이 돌고**(`openQuest` 의 `playCut`),
  한 번 본 뒤로는 눌러 봐야 시트가 열릴 뿐이다.
  · ⚠️ **`!` 배지는 그대로 켜져 있는다** — 「하던 것이 있다」는 알림이라 꺼지면
    바로 위의 규칙이 무너진다. 빠지는 것은 **움직임뿐**이다
  · ⚠️ **인트로 칸(`q.cut.in`)부터 본다** — 없는 퀘스트를 `includes(undefined)` 로
    재면 «영영 안 본 것»이 되어 그 칩만 끝까지 뛴다 (지금 열일곱은 다 갖고 있고,
    `checkui` 가 없으면 「맥박을 잴 수가 없다」로 실패시킨다)
  · **`checkui` 의 「퀘스트점」이 이미 `seenCuts` 를 넣었다 뺐다 하던 자리**라 그 두
    상태에 한 줄씩 붙였다 — 안 봤으면 뛰는가 · 보고 나면 멎는가.
    사보타주 둘로 확인했다 (옛 규칙으로 되돌리면 「계속 뛴다」 · 늘 끄면 「안 뛴다」)
- ⚠️ **「아직 진행중이에요」는 눌리는 버튼이다** (퀘스트 UI 공통 규칙 · `QUEST.md` 5-4).
  예전에는 `disabled` 라 눌러도 아무 일이 없었는데, 못 냈다는 것은 이미 막대와
  「0 / 1」로 적혀 있다 — 그런데도 누르는 이유는 **「그래서 뭘 하면 되는데」** 하나뿐이라
  반응이 없으면 고장으로 읽힌다. 누르면 **목표를 토스트로 버튼 바로 «위»에** 띄운다
  (`questNotYet`). 아래로 두면 「나가기」를 가려 다음 손짓을 막고, **시트는 안 닫는다** —
  닫으면 방금 읽던 목표 줄이 같이 사라져 토스트가 어디서 나온 말인지를 잃는다.
  · ⚠️ **잠긴 표현을 «값으로 베껴» 두지 않는다** — `.q-claim:disabled` 가
    `filter: saturate(0.15); opacity: 0.8` 을 그대로 적어 두고 있었다
    (`.pg-row.locked` 과 같은 사고다). 잠긴 것이 아니라 **눌리는** 버튼이다
  · ⚠️ **검사기가 「`disabled` 인가」를 보면 안 된다** — `checkui` 는 **눌러 보고**
    ① 보상이 안 나갔는가 ② 목표가 토스트로 떴는가를 본다. 토스트만 재면
    「가져가기」로 잘못 붙여 놔도 통과한다 (그쪽도 토스트를 띄운다). 사보타주 둘로 확인했다
- ⚠️ **진행도 숫자는 막대 «위»에 얹힌다** (`.q-num` · 사람이 그림으로 자리를 정해 줬다).
  예전에는 막대 아래 제 줄을 쓰고 그 옆에 개발용 버튼이 알약으로 서 있었는데,
  숫자가 올라오면서 **그 줄이 통째로 없어졌다** — 개발용 버튼은 이제 막대 «아래»에
  **전체 폭**으로 선다.
  · ⚠️⚠️ **흰 테(`text-shadow`)를 두르지 않으면 채워진 구간에서 글자가 사라진다.**
    숫자는 가운데 고정인데 채움은 왼쪽에서 자라므로 절반쯤 차면 글자가
    크림색 트랙과 진분홍 채움 **양쪽에 걸친다** — `--text-lav`(#4f36a0)와
    `--text-pink`(#9c3566)는 둘 다 어두워 **1.5:1** 밖에 안 된다
  · ⚠️⚠️ **`checkTextStyle()` 은 이것을 영영 못 본다 — 이유가 «둘»이다.**
    ① 배경을 **조상**을 타고 올라가 찾는데 채움(`.q-bar span`)은 **형제**라
    늘 크림색 트랙만 보고 통과한다 ② **그림자는 대비로 안 쳐 준다**
    (바람개비 HUD 에서 배운 자리다). 그래서 `checkui` 의 **「퀘스트숫자」**가
    **채움을 100% 로 만들어 놓고 그 자리를 찍어 픽셀을 떠서** 잰다
    (지금 6.74:1 · 325점). 흰 테를 떼는 사보타주로 확인했다 (1.22:1)
  · ⚠️⚠️ **재는 상자를 글자에 «붙인다»** — `inset: 0` 으로 막대를 다 덮어 두었더니
    상자 안에서 제일 밝은 픽셀이 **둥근 모서리 «밖»의 흰 페이지 배경**이라,
    흰 테를 통째로 떼도 **7.88:1 로 통과했다.** 가운데에 붙여 놓으면 상자 안은
    글자와 채움뿐이다 — **사보타주가 검사기의 구멍을 찾아낸 자리다**
- ⚠️ **개발용(임시) 「(임시) 퀘스트 완료」 버튼** (`S.devQuestBtn` · 개발용 패널의
  「열기 스위치」 줄에서 켜고 끈다). 목표가 일곱 갈래라 «진행도를 심는» 방식으로는
  상태형 넷(`village`·`keyword`·`bond`·`charm`)이 안 차고, 마을을 열거나 호감도를 올려
  흉내를 내면 그건 퀘스트가 아니라 **게임 상태를 건드리는 것**이라 시험이 끝난 뒤에도 남는다.
  · **`questProgress()` 한 줄에서 접는다** (`st.devFull === q.id` 이면 `g.n`) —
    막대 · 「n / n」 · `questFull` 이 저절로 같이 따라온다 (경로가 둘이 될 데가 없다)
  · ⚠️ **퀘스트 id 로 묶는다.** `true` 하나로 두면 보상을 받은 뒤 «다음» 퀘스트까지
    다 찬 것이 된다 (`claimQuest` 가 비우지만 거기에만 기대지 않는다)
  · ⚠️ **보상까지 대신 받지 않는다** — 「가져가기」로 바꿔만 놓는다. 시험하고 싶은 것은
    대개 그 «뒤»(완료 컷씬 · 보상 · 다음 퀘스트)라, 여기서 받아 버리면 그 길을 건너뛴다
  · ⚠️⚠️ **스위치가 꺼져 있으면 마크업이 한 글자도 안 바뀐다.** 「켜면 뜨는가」만 재던
    동안에는 **스위치를 통째로 무시하는 사보타주가 그대로 통과했다** — 출시된 화면에
    개발용 버튼이 박힌 채로 0건이 나온다. 그래서 검사가 **양쪽**을 본다:
    「퀘스트시트」(스위치 꺼짐)가 **버튼이 «없는지»**를, 「퀘스트완료버튼」(켜짐)이
    **떠 있는지 · 눌러서 차는지 · 보상까지 받아 버리지 않는지**를 본다.
    ⚠️ **보상 여부를 진행도보다 «먼저» 본다** — 받아 버리면 `devFull` 이 같이 비워져
    진행도도 0 이라, 뒤에 두면 「눌러도 안 찬다」는 엉뚱한 말이 나온다 (사보타주에서 그랬다).
    사보타주 셋으로 확인했다 (아무 일도 안 하기 · 보상까지 받기 · 스위치 무시하기)
  · ⚠️ **자리도 같이 본다** — 막대 «아래»인가 · 막대와 **같은 폭**인가.
    ⚠️ 두 상자가 **둘 다 0 이면** 「버튼이 막대보다 좁다」가 «거짓»이 되어
    폭을 어떻게 망가뜨려도 지나간다 (시트가 아직 안 떠 있을 때다) — 못 잰 것을
    **먼저** 실패시키고, **통과할 때도 잰 값을 낸다**(`폭 440px (막대 440px) · 막대 아래 10px`)
  · ⚠️⚠️ **화면이 «안 바뀌는» 사보타주는 검사기를 한 번도 안 시험한 것이다.**
    폭을 줄여 보려고 `.q-devdone` 에 `width: 120px` 을 넣었는데 **두 번 다 통과**해서
    검사기를 의심했다 — 실은 전체 폭을 주는 것이 `.q-devdone` 이 아니라
    **`.modal-card .btn`**(선택자 둘)이라 사보타주가 **한 픽셀도 안 바꾸고 있었다.**
    같은 자릿수로 적자(`.modal-card .q-devdone`) 그 자리에서 잡혔다
    (`버튼이 전체 폭이 아니다 (120px · 막대 440px)`). **사보타주가 통과하면
    「그 사보타주가 정말 그림을 바꿨는지」부터 눈으로 확인한다**
  · **`S.devQuestBtn` 은 «더하는» 칸이라 `SAVE_VER` 를 안 올린다** (`miniLog` 와 같은 규칙)
- ⚠️ **내보냈으면 칩까지 `refreshQuests()` 가 그린다.** 부르는 쪽마다 따로
  `renderQuestChip()` 을 붙이고 있었는데, **켤 때는 `render()` 가 그보다 먼저**
  지나가서 **새로고침하면 하던 퀘스트의 칩이 사라졌다** — 퀘스트는 세이브에 멀쩡히
  살아 있고 화면에서만 없어지는 종류다
- ⚠️ **컷씬이 끝나면 화면을 다시 그린다**(`cutNext`). 본 컷씬이 화면을 열기 때문이다 —
  첫 퀘스트의 컷씬이 **부엌 버튼을 여는데** 다시 안 그리면 아무것이나 화면을 다시
  그릴 때까지 버튼이 안 나타났다(**레드닷도 같이**). 판정(`seenCuts`)은 `playCut` 이
  시작할 때 이미 끝나 있어서 빠진 것은 「그리기」 하나였다. 사보타주로 확인했다
- ⚠️ **검사기가 「첫 퀘스트」에 기대고 있었다.** 표 맨 앞이 조합 퀘스트라고 보고
  `goal.id` 로 레시피를 찾던 자리가 넷 있었는데, 맨 앞이 부엌으로 바뀌자 통째로 터졌다
  (`checkui` 의 퀘스트점 · 퀘스트시트 · 퀘스트완료 · 컷씬).
  지금은 **조건에 맞는 퀘스트를 골라 세운다** — 「가진 장 중에서 고르지 않는다」와 같은 결이다.
  · ⚠️ 골라 세울 때는 **큐도 같이 비운다**(`S.quest = {active, n, done, queue}`).
    `active` 만 갈아 끼우면 그 id 가 큐에 남아 **보상을 받은 직후에 같은 퀘스트가 또 나온다**

## 퀘스트 — 2막부터는 **이야기가 연다** (매력이 아니라)

1막의 여덟은 전부 매력(`at`)으로 열린다. 2막부터 그러면 안 된다 — 마을과 키워드가
이야기로 열리는데 퀘스트만 점수로 열면 **「왜 안 열리지」가 두 벌**이 된다
(마을 해금의 「점수로 두 번 잠그지 않는다」와 같은 규칙).

그래서 `need` 를 둔다 — `{ kw }` · `{ village }` · `{ cut }`. **되돌아가는 값이
하나도 없는 것들만** 쓴다: 키워드·마을·본 컷씬은 한 번 얻으면 안 사라지므로
열렸던 퀘스트가 닫히지 않는다. `at` 은 **표 순서를 지키는 용도로만** 남긴다
(`checkdata` 가 오름차순을 본다).

- ⚠️ **목표를 「물어보기」로 두지 않는다.** 물어본 것은 한 번뿐이라, 퀘스트를 받기
  «전»에 이미 물어 놓았으면 그 뒤로 **영영 안 오른다** — 화면에 오류 하나 없이 막힌다.
  2·3막의 목표는 **「마을이 열렸는가」(`village`) · 「키워드를 가졌는가」(`keyword`)**
  같은 상태다. 뒤로 안 가고, 이미 이룬 것이면 그 자리에서 완료로 뜬다
- ⚠️ **키워드·마을이 들어오는 자리(`doAsk`)와 컷씬을 본 자리(`playCut`)에서
  `refreshQuests()` 를 부른다.** 안 부르면 다음에 매력이 오를 때까지 칩이 안 뜨는데,
  엔딩을 본 사람에게는 그 「다음」이 영영 안 올 수도 있다. **사보타주로 확인한 자리다**
- ⚠️ **여신(100) 위로 `at` 을 올리지 않는다.** 이야기가 아니라 점수를 더 모아야
  진행되는 것이 되고, `need` 가 이미 순서를 지킨다
- ⚠️ **2막 셋 말고는 비법서 장을 안 준다.** 여신에서 그물이 전부 주고 나면 줄 장이
  남지 않는다 — 억지로 붙이면 「받았는데 아무 일도 안 일어난다」가 된다. 결정과 재료로 갚는다
- **`q_seal` 이 공방을 5단계로 올린다** (`reward.room`). ⚠️ 그전까지 `roomLevel` 을
  올리는 길이 **개발용 스위치밖에 없어서**, 엔딩 조건(`sealReady`)이 정상 플레이로는
  영영 안 찼다 — 만들어 놓은 엔딩에 아무도 못 닿는 종류의 사고다.
  보상은 **내려가지 않게 `max` 로** 올린다 (보상이 진행을 되돌리면 그건 벌이다)

```bash
npm run test:story   # 2~5막을 진짜 화면에서 처음부터 끝까지 걸어 본다 (브라우저 필요)
```

`checkstory` 는 **키워드를 손으로 꽂지 않는다.** 꽂아 놓고 `refreshQuests()` 를 직접
부르면 「물어봤더니 칩이 떴다」가 아니라 「검사기가 칩을 띄웠다」를 재는 것이라,
`doAsk` 의 새로고침을 통째로 지워도 통과한다 (사보타주로 확인했다).
`checktalk` 은 **표에서** 같은 것을 본다 — `need` 와 목표가 가리키는 키워드·마을을
시작 키워드에서 실제로 걸어서 닿을 수 있는가.

## 스토리 다시보기 — **막마다 장면이 있어야 한다**

⚠️ **못 본 «컷씬»의 제목은 안 내놓는다** (`renderStory`) — 제목 하나가 곧 스포일러다.
그래서 **장면이 하나도 없는 막은 영영 안 보인다.** 2막을 만들 때 마을·키워드만 늘리고
컷씬을 안 만들었더니 다시보기가 **1막에서 3막으로 건너뛰었다** (「1막밖에 없다」).

### ⚠️ 막은 「🔒 몇 개」로 «전부» 서 있다 (감추지 않는다)

예전에는 아무것도 못 본 막을 **통째로 뺐다.** 그러면 다섯 막을 다 만들어 놓고도
화면에는 1막 하나만 서 있어서 **「없는 것」과 「아직 못 본 것」이 구별되지 않는다** —
같은 신고를 두 번 받았다 (「2~5막이 안 올라와 있어」).

잠긴 칩(`.ask-chip.locked`)에서 이미 정한 규칙을 그대로 쓴다:
**안 보이면 「없는 것」이고 보이면 「아직 못 여는 것」이다.**

- 막 «이름»(엔딩·에필로그)은 **하나라도 본 뒤에** 붙는다. 번호(4막)는 셋 넘는 막이
  있다는 것밖에 안 알려 주지만 이름은 그 자체가 이야기다
- **하나도 못 본 사람에게는 자물쇠 다섯 줄 대신 한 마디**(`st_empty`)를 준다 —
  누를 것이 하나도 없는 목록은 목록이 아니다

- 막을 새로 만들면 **컷씬을 최소 하나** 같이 만든다. `npm test` 는 이것을 안 잡는다 —
  「없는 것」이라 검사할 대상 자체가 없다
- 2막의 둘과 3막의 하나는 **마을에 처음 들어설 때** 뜬다 (`VILLAGE_CUT`). ⚠️ 마을이 «열리는»
  순간이 아니다 — 여는 것은 대답을 읽는 자리라, 거기에 얹으면 그 사람이 방금 한 말이 덮인다
- **엔딩도 다시보기에 들어간다** — `c_end_*` 넷이 **4막**, `c_epilogue` 가 **5막**이다.
  `renderStory` 는 `CUTS` 의 `act` 를 그대로 훑으므로 막을 늘려도 코드는 안 고친다.
  ⚠️ **엔딩을 4·5막으로 뺐더니 3막이 통째로 비었다** — 그때까지 act 3 는 전부 엔딩이었다.
  2막에서 겪은 것과 똑같은 구멍이라 3막 컷씬(`c_spire_in`)을 같이 만들었다
- 막 이름은 **번호 뒤에 덧붙인다**(`ACT_NAME` → 「4막 · 엔딩」). 번호만 적으면 STORY.md 의
  3막 구성과 어긋나 보이고, 이름만 적으면 사람이 센 순서를 잃는다.
  ⚠️ **번호를 문자열 안에 박지 않는다** — 막이 하나 늘면 통째로 어긋난다
- ⚠️ **`checkui` 의 「스토리다시보기」는 이름 붙는 막을 하나는 «본 것», 하나는 «못 본 것»으로 둔다.**
  1막 것만 심으면 「4막 · 엔딩」 같은 더 긴 머리글을 한 번도 안 재는 것이고, 이름 붙는 막을
  전부 심으면 「못 본 막의 이름이 새는가」를 한 번도 안 재는 것이다 — 어느 쪽이든 0건이
  통과가 아니라 「재 본 적 없다」가 된다 (영어는 더 길다). 사보타주 셋으로 확인했다:
  못 본 막을 빼면 「막이 2개 서 있다」 · 못 본 막에 이름을 붙이면 「번호만으로 안 서 있다」 ·
  자물쇠 줄을 없애면 「자물쇠 줄이 없다」. `.st-actname` 을 `nowrap` 으로 만들면
  `__cardFits` 가 253px 넘침을 잡는다

## 엔딩 — 방아쇠가 둘이고, 둘 다 이야기가 정한 것이다

컷씬 다섯이 **이어서** 돈다 (`playEnding()`). 하나씩 끊으면 사이에 화면이 돌아와
「거울에 금이 갔는데 채집하러 갈 수 있는」 상태가 된다.

| | 어디서 | 조건 |
|---|---|---|
| `c_end_seal` | 마이 룸에 들어설 때 | 「진짜 나」 + **공방 5단계** (`sealReady`) |
| `c_end_call` → `c_epilogue` | 거울못에 설 때 | 봉인이 풀려 있음 (`endingReady`) |

- ⚠️ **`render()` 안에서 트지 않는다.** 렌더는 수시로 돌아서 컷씬이 화면을 덮은 채
  다시 그려진다. 「마이 룸에 들어선다」는 `switchTab()` 한 자리에서만 판정한다
- ⚠️ **점수(매력)로 잠그지 않는다** — 마을 해금과 같은 규칙이다. 대신 **공방 5단계**가
  조건이라, 지금까지 조건조차 없던 마이 룸 업그레이드에 목적이 생긴다
- ⚠️ **칩(물어볼 것)으로 두지 않는다.** 마지막 순간은 공주 혼자라 다른 다섯 개와
  나란히 놓일 것이 아니다 (그리고 유타르크는 이미 여섯 칸이 찼다)
- **연출은 «줄»에 붙는다** — `lines` 의 셋째 칸(`['sp_yutark','true','crack']`).
  금(`crack`) → 깨짐(`shatter`) → 빛(`light`) → 남은 조각(`shard`).
  ⚠️ **깨지는 것은 「조각마다 비친다」고 «말하는» 줄에서다** — 한 줄 앞이면 대사가
  이미 지나간 일을 설명하는 꼴이고, 한 줄 뒤면 그의 마지막 말이 멀쩡한 거울 앞에서 나온다
  · ⚠️ **`setTimeout` 으로 조각을 붙이지 않는다** (밭 약탈 줄에서 배운 것과 같다) —
    검증기가 재는 순간에 아직 안 붙은 조각은 «없는 것»으로 세어 통과한다.
    CSS 애니메이션 + `animation-delay` 라야 `settle()` 이 끝 상태로 보내 준다.
    그래서 **평소 상태가 곧 끝난 상태**여야 한다 (조각은 흩어져 사라진 뒤가 기본)
  · **글자를 하나도 안 넣는다** — 장식이 대비 검사를 흔들지 않게
  · 연출이 있는 줄에서만 배경을 더 덮는다(`.cut.deep`). `.cut` 전체를 어둡게 하면
    1막 컷씬까지 같이 어두워진다
  · ⚠️ **`checkui` 의 「엔딩연출」은 연출이 «켜진» 줄에서 잰다** — 위의 「컷씬」 검사는
    1막이라 연출이 없고, 그 0건은 이 층을 한 번도 안 잰 것이다

### ⚠️ 「컷씬」 검사는 **첫 퀘스트의 첫 줄** 하나만 잰다

대사가 마흔 컷에 백 줄이 넘는데 재는 것은 한 줄이라, **제일 긴 줄은 영영 안 잰다**
(부엌의 「오늘의 한 마디」와 같은 구멍이다). `checkui` 의 **「컷씬긴줄」**이 표에서
제일 긴 줄을 뽑아 세워 놓고 한 번 더 잰다 — 대사를 늘리면 저절로 따라온다.

- ⚠️ **언어마다 따로 세운다.** 말풍선은 `data-i18n` 이 아니라 `innerHTML` 로 그려져서
  `I18N.setLang()` 을 해도 **다시 안 그려진다** — 한 언어에서 세워 놓고 재면 다른 언어의
  제일 긴 줄은 한 번도 안 잰 것이 된다 (영어가 대체로 더 길다)
- ⚠️ **세운 줄이 «진짜로 그려졌는지» 글자 수로 확인한다.** 견줄 때는 **강조 표시를 떼고**
  센다 — `cutLineHtml` 이 `«…»`·`*…*` 를 금색 칸으로 바꾸며 그 기호를 지우므로,
  원문 길이 그대로 견주면 멀쩡한 줄이 늘 「두 자 모자란다」로 잡힌다 (실제로 그랬다)
- ⚠️⚠️ **`checkUI()` 만으로는 말풍선 «안»을 못 본다** — `checkLayout()` 은 정해진 선택자만
  재는데 대사 줄은 그냥 `<p>` 라 대상이 아니다. 안 끊기는 긴 낱말을 넣는 사보타주가
  **그대로 통과했다** (밭 시트에서 배운 자리다). `__fits('#cutScene')` 를 같이 부른다 —
  같은 사보타주가 265px 에서 「540px 넘쳤다」로 잡힌다
- **무엇을 쟀는지 통과할 때도 낸다** (`컷씬긴줄(en) — c_egg_out_3 · 100자`)
- ⚠️ **여왕의 이름은 `c_end_call_3` 한 줄에서만 불린다.** `checkname` 의 허용 목록에
  그 키를 **줄 모양까지** 못 박아 뒀다 — 옆줄(`_4`)에 새면 그대로 잡힌다
- **지문을 인물 이름으로 띄우지 않는다.** 내레이터가 SPEAKERS 에 없어서
  (`checkdata` 가 없는 인물을 잡는다) 「거울이 깨지고…」 같은 줄은 **그녀가 본 것을
  말하는 대사**로 고쳐 썼다 — 새 장치를 만드는 것보다 글이 나아진다

## 아직 안 한 것

- **마을 여덟이 전부 키워드로 열린다** (`STORY.md` 「키워드 시스템」 · `QUEST.md` 5단계).
  1막에서 다섯, 2막에서 유리관 호수 · 은빛 갱도, **3막에서 여왕의 첨탑**.
  ⚠️ **점수로 두 번 잠그지 않는다** — `isVillageOpen()` 은 `S.villages` 만 본다.
  여기에 매력 조건을 또 걸면 왜 안 열리는지 플레이어가 알 수 없다.
  · 표는 `data.js` 의 `KEYWORDS`(열넷)와 **`ASKS`**(마흔일곱)다.
    ⚠️ STORY.md 의 예시 이름 `TALKS` 를 못 쓴다 — 인사말·대사 표가 이미 그 이름이고,
    모듈이 없어서 전역이 겹치면 그 파일이 통째로 안 돈다
  · ⚠️⚠️ **첫 키워드(「정신적 허기」)는 «첫 퀘스트의 보상»이다** (`q_meet` 의 `reward.kw`).
    한때 `defaultState` 의 기본값이라 **튜토리얼을 막 마친 화면에서 부엌에 이미
    물어볼 것이 하나 떠 있었다** — 아직 클레멘을 만나지도 않았는데. 그 허기를
    알아차리는 자리가 곧 「같이 먹어 본 밥상」이라 순서를 이야기에 맞췄다 (사람이 정했다).
    **부엌(클레멘)은 늘 닿으므로** 거기서 첫 마을이 열리는 것은 그대로다
    · ⚠️ **막다른 길이 아니다** — `q_meet` 는 `at: 0` 에 `need` 도 없어 튜토리얼 직후
      저절로 서 있고, 목표(부엌 한 번)도 늘 할 수 있다
    · ⚠️ **`checktalk` 이 퀘스트도 «같이 걷는다».** 시작 목록만 들고 걷던 검사기는
      그 목록이 비는 순간 **마흔일곱 대답을 전부 「도달 불가능」**으로 잡는다.
      START 에 그 키워드를 손으로 적어 넣으면 **사본이 생겨** 퀘스트에서 빼도
      검사기만 옛 규칙으로 통과한다 — 그래서 **주는 자리를 그대로 걷는다**
      (조건이 채워진 퀘스트는 깰 수 있고, 깨면 `reward.kw` 가 들어온다).
      통과할 때도 **첫걸음이 어디서 나왔는지**를 같이 낸다
      (`시작 [(없음)] + 퀘스트 [kw_hunger←q_meet]`) — `시작 []` 만 찍으면 고장으로 읽힌다.
      사보타주로 확인했다 (퀘스트에서 빼면 「시작할 키워드가 하나도 없다」 + 도달 불가능 47줄)
    · ⚠️ **기본값을 없앴으니 «옛 세이브가 그것을 지키는지»가 곧 진행 전체다.**
      전에는 기본값이 구멍을 메워 줘서 아무도 안 봤는데, 이제 `keywords` 가 한 번이라도
      비워지면 **아무에게도 못 묻는 상태**가 된다 (화면에는 오류 하나 없다).
      `npm run test:save` 에 그 줄이 생겼다 (사보타주로 확인했다)
  · **다시 물어도 된다.** 이미 물어본 것은 회색이 될 뿐 사라지지 않고,
    주는 것(`gives`·`opens`)만 처음 한 번이다
  · ⚠️⚠️ **1막 사슬의 «속도»는 퀘스트가 잡는다** (`ASKS` 의 `need: { quest }` · 문 일곱).
    예전에는 1막 열셋에 **문이 하나도 없어서**, 묻는 데 AP 도 시간도 안 드는 것과 겹쳐
    **튜토리얼을 마친 사람이 한자리에 앉아 1막 이야기를 통째로** 걸어갈 수 있었다 —
    퀘스트는 매력 0 → 50 을 타느라 열흘쯤 걸리므로 **이야기가 먼저 끝나고 점수만 남는
    구간**이 생겼다 (「키워드가 너무 빠르다」로 신고받았다). 지금은 **퀘스트 하나에 문 하나**다
    (`q_walk`·`q_kitchen`·`q_bring`·`q_egg`·`q_soup`·`q_sip`·`q_bloom`).
    · ⚠️ **막는 것은 «구간의 머리» 한 줄뿐이다.** 뒤따르는 줄은 그 머리가 주는 키워드를
      이미 요구하므로 **저절로 같이 기다린다** — 「카이로스 + 독사과 → 사과밭」에 또 걸면
      같은 것을 두 번 잠그는 것이다. **일곱 줄로 열셋이 다 밀린다**
    · ⚠️ **앞뒤 양 끝은 안 잠근다** — 첫 줄(클레멘 + 허기)은 튜토리얼 직후의 유일한
      길이고, 마지막 둘은 이미 **매력 60**(`q_bloom`)이 문이라 충분히 느리다
    · ⚠️ **2막의 문(`need.bond`)에는 퀘스트를 «또» 걸지 않는다** — 두 번 잠그는 것이고,
      `checkbalance` 의 「2막 문이 기초 등급 안에서 열리는가」가 조용히 어긋난다
    · ⚠️ **첫 줄이 「키워드 + 마을」을 같이 주던 것을 갈랐다** — 2막에서 정한
      「한 줄이 둘 다 열면 나머지 한 사람에게 물을 이유가 사라진다」가 1막의 첫 줄에서만
      깨져 있었다. 이제 클레멘은 **마을만** 열고, 「아름다움」은 굴뚝의 **오릭스**가 준다
    · ⚠️⚠️ **키워드 ↔ 퀘스트가 «서로를 기다리는 고리»가 될 수 있다** — 퀘스트의
      `need.kw` 가 잠긴 대답의 키워드를 요구하면 영영 안 풀리는데 **화면에는 오류 하나
      안 뜬다.** `checktalk` 의 걸어 보기가 그대로 잡고(「«q_walk» 를 깰 수가 없다」),
      **이유를 갈라서 말한다** — 「키워드를 아무도 안 준다」로 적으면 엉뚱한 곳을 고치게 된다
    · ⚠️ **모자란 이유는 `askNeed(a)` 한 곳에서 낸다** (`null` · `{kind:'quest'}` ·
      `{kind:'bond'}`). 갈래마다 다시 판정하면 **화면이 여는 시트와 토스트가 갈린다**.
      잠긴 칩의 **툴팁도 여기를 지난다** — `D.askNeedBond(a)` 를 그냥 읽으면 퀘스트로
      잠긴 칩이 **「'모르는 사이' 단계가 되면」**이라고 말한다 (0번 단계라서다)
    · ⚠️ **이미 물어본 것은 안 잠근다** — 어제 들은 말이 오늘 자물쇠가 되면 그건 뺏는 것이다
    · ⚠️ **오릭스가 일곱 칸이 됐다** — `ASK_MAX` 를 6 → 7 로 올린 자리다. 대안을 다
      그려 보고 고른 마지막 수단이고(대사 한 벌을 죽이거나 2막 문의 선택지를 줄여야 했다),
      **「일곱은 한 명뿐」을 따로 못 박았다**(`ASK_OVER_MAX`). 규칙이 막으려던 것
      (칩 줄이 화면을 먹는다)은 **`checkui` 의 「물어볼것많은사람」이 265px 영어에서
      실제로 잰다** — 누구인지 표에서 읽으므로 칸이 옮겨 다녀도 따라온다.
      **다음에 모자라면 «새 사람»이 받는다** (3막의 이그리트가 그 선례다)
    · ⚠️⚠️ **잠금을 늘리면 «검사기가 볼 수 있는 화면»이 줄어든다.** `checkask` 는
      **문마다 ① 그 앞에서는 잠겨 있고 ② 그 퀘스트를 끝내면 풀리는지**를 보고,
      **몇 개를 쟀는지를 표와 견준다**(`퀘스트 문을 «다» 재 봤다 — 7 / 7`) —
      새 문을 붙이고 검사를 안 늘리면 0건이 「그 문은 한 번도 안 쟀다」가 된다.
      첫 문에서는 **눌러도 본다**(키워드가 안 들어오는가 · 어느 퀘스트인지 말해 주는가 ·
      퀘스트 시트로 데려가는가). 사보타주 둘로 확인했다 — `askNeed` 가 퀘스트를 안 보게
      하면 4건 · `doAsk` 의 퀘스트 갈래를 지우면 3건
  · ⚠️⚠️ **대답은 «컷씬»으로 돈다** (`askScene` → `playScene`) — 마흔일곱이 다
    **네 줄**이다: 공주가 묻고 → 그 사람이 답하고 → **공주가 되묻고** → 그가 닫는다.
    예전에는 인사말·오늘의 한 마디가 쓰던 말풍선을 대답이
    **갈아 끼웠는데**, 그러면 「방금 그 사람이 한 말」과 「늘 하는 말」이 같은 자리에서
    나와 구별이 안 되고, 화면을 떠났다 오면 조용히 사라져 무슨 말을 들었는지가 안 남았다.
    키워드를 가져가서 듣는 말은 이 게임의 «이야기»라 컷씬이 맞는 자리다 (사람이 정했다).
    · **표에 적힌 컷씬과 같은 길을 지난다** — `playScene(cut, then)` 하나다.
      장면 쪽에 칸 둘만 더 뒀다: `text`(줄마다 이미 번역된 글) · `noLog`(본 것으로 안 적는다).
      ⚠️ **`seenCuts` 에 안 적는다** — 마흔일곱이 거기 쌓이면 세이브만 불어나고,
      스토리 다시보기·`need.cut` 은 `CUTS` 표에 있는 것만 뜻하므로 적어 봐야 아무도 안 본다
    · **묻는 줄은 문자열 하나에서 나온다**(`ask_cut_q`) — 대답마다 질문을 따로 쓰면
      그만큼 번역이 빠질 자리가 늘고, 새 대답을 붙일 때 빠뜨리게 된다.
      키워드 이름을 `«…»`(영어는 `*…*`)로 감싸 `cutLineHtml` 이 금색으로 집게 한다
    · ⚠️⚠️ **이어지는 줄은 «같은 줄기»에서 나온다** (`more` → `ak_…_2` · `_3`).
      `more` 에는 **누가 · 어떤 얼굴로**만 적고 글은 번호가 데려온다 — 두 번째 줄부터를
      제 열쇠로 따로 적으면 **그 사람이 하는 말이 두 곳에 흩어져** 대답을 고칠 때
      한쪽만 고치게 된다. 지금 마흔일곱이 전부 네 줄이다
    · **세 번째 줄은 «공주»다.** 그 사람만 계속 떠들면 여러 줄이 돼도 그냥 «긴 대답»이고,
      대화가 아니다 — 공주가 되물어야 말이 오간다. `checkask` 가 **두 사람이 번갈아
      말하는가**(`new Set(who).size === 2`)를 부엌과 마을 양쪽에서 본다
    · ⚠️ **글의 기준은 인물이다** — STORY.md 의 말투를 그대로 따른다 (클레멘 존댓말 ·
      오릭스 반말 · 실반의 「…소」체 · 여왕은 「프린세스」). 그리고 **여왕의 이름은
      여기에도 못 쓴다** (`checkname` 이 문자열과 화면 양쪽에서 본다)
    · `checktalk` 이 셋을 본다 — 이어지는 줄의 **인물이 있는가 · 그 얼굴이 있는가 ·
      그 번호의 글이 있는가**. 거기에 **번호만 써 놓고 `more` 에 자리가 없는 줄**도
      잡는다 (써 놓고 버려진 줄이라 영영 화면에 안 뜬다). 사보타주 셋으로 확인했다
    · ⚠️ **토스트를 장면 «뒤»로 옮겼다.** 예전에는 900ms 뒤였는데 그 정도면 한 줄짜리
      대답이 읽히던 것이고 — 이제 그 자리에 컷씬이 덮여 있어 **토스트가 장면 위에 뜬다.**
      읽는 속도는 사람마다 달라 시간으로는 못 맞춘다: 「다 읽었다」는 신호는
      **장면을 끝까지 넘긴 것** 하나뿐이다
    · ⚠️ **`.ask-chip.on` 은 없앴다** — 「지금 떠 있는 대답」 자체가 없어졌다.
      안 쓰는 규칙을 남겨 두면 다음 사람이 그것을 고치려다 시간을 쓴다
    · ⚠️⚠️ **검사기가 「칩을 눌렀다」에서 멈추면 아무것도 안 잰 것이다.** 장면은
      첫 줄(공주의 질문)만 그려진 채로 서 있어서 **그 사람의 대답은 한 번도 화면에 안 뜬다** —
      `checkname` 이 그 상태로 「이름 안 샘」을 내고 있었다 (여왕의 이름을 대답에 심어도
      안 걸렸다). 지금은 **끝까지 넘기며 줄마다 훑고, 읽은 줄 수를 같이 낸다**
      (`물어본 것 5개(대사 20줄)`) — 첫 줄만 보게 되돌리면 5줄로 떨어져 바로 보인다.
      `checkui` 에도 **「키워드장면」 블록을 따로 뒀다** (위의 「물어볼것」 0건은
      이 층을 한 번도 안 잰 것이다).
      · ⚠️ **둘 다 «줄 수를 박지 않는다»** — 표에서 읽는다(`2 + more.length`).
        한때 `[1, 2]` 로 두 줄만 재고 있었는데, 이어지는 줄이 붙으면서 **뒤쪽이
        통째로 안 재지게 됐다** (거기가 보통 제일 긴 줄이다). 세 줄이 안 되면
        「이어지는 줄을 한 번도 안 쟀다」로 실패시킨다
      · ⚠️ **마을 쪽 검사가 돌려받을 것은 «둘째 줄»이다** — 마지막 줄은 그 사람의
        «마무리»라, 그것을 본 대답으로 착각하면 「오릭스가 유리관 이야기를 꺼낸다」
        같은 줄이 통째로 어긋난다 (실제로 다섯 줄이 그렇게 빨개졌다)
  · **갈 곳만 알려 준다** — 새로 물어볼 것이 있는 마을 탭·건물·부엌 버튼에 점(●).
    무엇을 물을지는 안 알려 준다 (「길 잃음 방지가 제일 중요하다」)
  · ⚠️⚠️ **점은 «윗단 갈래»까지 올라온다** (탐험의 필드/마을/밭 중 「마을」).
    안쪽(마을 탭·건물 명판)에만 찍으면 **«필드»에 서 있는 사람에게는 아예 안 보인다** —
    갈 곳을 알려 주는 점이 **갈 곳에 들어가야만 보이면** 뜻이 없다.
    판정은 `villageNews` 하나를 그대로 쓴다 (안쪽 점과 같은 함수라 갈릴 데가 없고,
    잠긴 마을은 거기서 이미 거짓이라 「잠긴 것은 길잡이 점에 안 센다」도 따라온다)
    · ⚠️ **켜는 줄과 «끄는 줄»을 같이 쓴다** — 다 물어본 뒤에도 남으면 그 점이
      거짓말이 된다 (부엌 점의 「다 먹었는데 안 사라진다」와 같은 자리다)
    · ⚠️⚠️ **라벨은 버튼이 아니라 «안쪽 span» 이 받는다.** `I18N.apply()` 가
      `data-i18n` 요소의 `textContent` 를 통째로 갈아 끼우므로, 버튼에 그대로 달아 두면
      찍어 둔 점이 **그 자리에서 사라진다.** 지금은 `setLang` 도 부팅도 곧바로
      `render()` 를 불러서 그 사고가 «안 보일» 뿐이다 — 그래서 `checkask` 는
      **`I18N.apply()` 를 «그리기 없이»** 불러서 잰다. `setLang('en')` 으로 재면
      다시 그려져 **무엇을 해 놔도 통과한다** (그렇게 짰다가 사보타주가 지나갔다)
    · `npm run test:ask` 가 셋을 본다 — 켜지는가 · 그리기 없이도 남는가 · 꺼지는가.
      사보타주 셋으로 확인했다 (끄는 줄 빼기 · 라벨을 버튼에 되돌리기 · 아예 안 찍기)
  · ⚠️ **점은 «누르면 할 일이 있는» 자리에만 찍는다.** 헤더 톱니에 「복구 코드를 아직
    안 봤다」는 뜻으로 점을 달아 뒀던 적이 있는데(`gearDot`·`S.codeSeen`), 설정을 한 번
    열기 전까지 안 꺼져서 **튜토리얼 첫 화면부터** 붉었다 — 아직 잃을 진행이 없는
    사람에게 백업을 채근하는 꼴이라 길잡이가 아니라 잔소리다 (「불필요한 레드닷」으로
    신고받아 통째로 지웠다). 「아직 안 본 것이 있다」는 점을 찍을 이유가 못 된다.
    `checkcode` 가 **톱니 버튼 안에 점 종류가 하나라도 있으면** 잡는다 — `gearDot`
    이라는 id 로만 재면 **이름만 바꿔 되살려도 통과한다** (사보타주로 확인했다)
  · ⚠️⚠️ **점의 자리는 «언제나 버튼의 우상단»이다 — `.tab-dot` 한 줄이 그 규칙 전부다.**
    예전에는 이 규칙이 «글 흐름 안»(`margin-left: 6px`)이라 라벨 뒤에 붙었고,
    칩만 `.ask-dot` 으로 우상단에 얹혀 있었다 — **같은 뜻의 점이 화면마다 다른 자리**에
    섰다 (「레드닷 위치가 버튼의 우상단으로 통일되는 룰」로 신고받았다).
    지금은 여섯이 다 거기서 나온다: 갈래 탭 · 마을 탭 · 건물 명판 · 물어볼 것 칩 ·
    부엌 버튼(`.act-dot`) · 퀘스트 칩(`.qc-dot`). **뒤의 둘은 «자리»만 덮어쓴다**
    · 점을 다는 버튼이 지킬 것 둘 — ① `position` 이 `static` 이 아니고
      ② 제 상자를 **안 자른다**. 말줄임 때문에 ②를 못 지키는 탭(`.cat-tab`·`.room-tab`)은
      **`.dot-host` 를 달고 말줄임을 «라벨»(`.tab-label`)로 옮긴다** — 통째로 없애면
      265px 영어에서 솥 이름이 41px 넘치던 그 사고가 되돌아온다
    · ⚠️ **`overflow` 되돌리기는 «더 힘센 선택자»로 적는다.** `.cat-tab`·`.room-tab` 의
      `overflow: hidden` 이 style.css 에서 **아래에** 있어, 그냥 `.dot-host` 로 적으면
      특정도가 같아 뒤엣것이 이긴다 — **점이 반달만 남았다** (찍어 보고 알았다)
    · ⚠️ **크기·색·테두리를 «베껴 쓰지 않는다».** `.tab-dot` 하나에서 나온다 —
      베낀 값은 공통 표현을 고쳐도 한쪽만 옛 값에 남는다
    · ⚠️ **잠긴 칩에는 안 붙는다** — 「잠긴 것은 길잡이 점에 안 센다」(`asksNew`)가
      화면에서도 같아야 한다. 점이 못 여는 것을 가리키면 그 점이 거짓말이 된다
    · ⚠️ **가로로 버튼 밖으로 나가면 안 된다**(`right: 0`). 절대 배치라도 오른쪽으로
      삐져나오면 `scrollWidth` 가 늘어 `__cardFits` 가 「3px 넘쳤다」로 잡는다 —
      버튼이 줄 끝에 설 수도 있으니 검사기가 맞다. 위로만 걸친다
    · **🆕 는 그대로 둔다**: 색·점만으로 가르면 색을 못 보는 사람에게는 아무 표시도
      없는 것과 같다 (`.ask-chip.fresh` 의 원래 주석 그대로다)
    · `checkui` 의 **「레드닷자리」**가 **자리마다** 잰다 — 점 가운데가 버튼의 오른쪽 위
      모서리에서 14px 안인가 · 그 버튼이 `static` 은 아닌가 · **제 상자를 안 자르는가**.
      ⚠️ 「붙어 있는가」만 보면 **잘린 점**을 못 본다 (상자 좌표는 그대로다) — 그래서
      셋을 같이 본다. **몇 가지 자리에서 쟀는지도 낸다**(지금 7). 사보타주 둘로
      확인했다 — 글 흐름으로 되돌리면 6건(23~120px) · `overflow` 되돌리기를 빼면 3건
    · `npm run test:ask` 가 셋을 본다 — 붙는가 · 이미 물어본 것·잠긴 것에는 없는가 ·
      삐져나오지 않는가. **새 칩과 물어본 칩을 각각 몇 개 쟀는지도 낸다**
      (한쪽이 0이면 그 방향은 아예 안 잰 것이다). 사보타주 셋으로 확인했다
  · 검사 둘 — `npm test` 가 `checktalk` 으로 **표를 걸어 보고**(도달 불가능 ·
    죽은 키워드 · 순환 · **막다른 진행**), `npm run test:ask` 가 **진짜 화면에서**
    부엌 → 굴뚝 → 사과밭 → 골짜기까지 눌러 본다.
    ⚠️ 진행이 막히는 버그는 화면에 아무 오류도 안 띄운다 — 플레이어는 그냥 그만둔다
  · **내보일 마을은 「건물이 있는 곳」이다** (`villagesShown()`). 예전에는 앞에서
    셋을 잘라 냈는데, 그러면 **목록 순서와 채운 순서가 어긋나는 순간 막힌다** —
    가시덤불 마을은 일곱 번째라 거기까지 늘리면 안이 텅 빈 마을 둘이 자물쇠로 딸려 왔다.
    ⚠️ 마을에 `spots` 를 넣으면 **그 순간 목록에 나타난다** — 여는 줄(`opens`)도 같이
    넣지 않으면 `checktalk` 이 「막다른 진행」으로 잡는다
  · **호감도 5단계**가 붙었다 (아래 항)
  · ⚠️ **한 사람이 반응하는 키워드는 여섯까지다** (`checktalk` 의 `ASK_MAX`).
    2막에서 다섯이 여섯이 됐는데, **막마다 한 칸씩 늘릴 수는 없다** —
    칩 줄이 화면을 먹고 「반응 없는 것 고르기」가 된다.
    그래서 3막의 것은 여기를 또 올리는 대신 **새 사람**(이그리트)이 받았다 —
    지금 여섯이 여섯 칸씩 차 있고 이그리트가 다섯이다. **다음 막도 같은 방식이다**
  · **2막은 「유리관」 하나에서 갈린다** — 실반에게 물으면 «어디에 있는지»(유리관 호수),
    오릭스에게 물으면 «누가 만들었는지»(은빛 갱도)가 열린다. 한 줄이 둘 다 열면
    나머지 한 사람에게 물을 이유가 사라진다.
    그 키워드 자체가 호감도 **친함(2단계)** 에서만 나오므로 **2막은 사람과 친해진 뒤에 온다**
  · **3막은 「불로장생」을 슈타르크에게 가져가면 열린다** — 그는 원래 그녀가 고용한
    암살자라 성 안을 아는 유일한 사람이다. ⚠️ **첨탑도 매력 점수로 안 잠근다**
  · ⚠️ **여왕의 이름(이그리트)은 화면 어디에도 나오면 안 된다.** 마지막에 거울이
    그녀를 기억해 내며 부르는 이름이라, 그 전에 한 번이라도 뜨면 그 장면이 죽는다.
    설정값은 `SPEAKERS`·`NAMES` 에 그대로 두고 **`speakerName()` 이 「여왕」으로
    갈아 끼운다** (공주·요정 대모와 같은 방식). `tools/checkname.js` 가 둘로 지킨다 —
    **문자열 훑기**(브라우저 없이 · `checktalk` 이 `npm test` 마다 돌린다)와
    **진짜 화면 훑기**(`npm run test:name` · 두 언어로 대사·잡담·칩을 다 펼쳐 본다).
    ⚠️ 화면 쪽은 **「대화」 버튼을 눌러야** 잡담이 나온다 — 말풍선만 두드리면
    인사말에 머물러 잡담에 흘린 이름을 못 잡는다 (사보타주로 확인한 자리다)
  · **「진짜 나」를 유타르크에게 «칩으로» 가져가지 않는다** — 그것이 저주가 풀리는
    순간이라 엔딩의 몫이다. 3막에서는 실반·발렌만 받고, 유타르크에게 가는 것은
    **엔딩 컷씬**이 받는다 (아래 항)
  · ⚠️ **유타르크의 「봉인」 대답은 손대지 않는다** — 「봉인 때문에 그렇게 먹은 것이
    아닙니다」가 `STORY.md` 「봉인은 허기의 원인이 아니다」 그 자체다.
    폭식이 마법 탓이 되는 순간 애정결핍 이야기가 통째로 없어진다
- **본줄기는 1막부터 엔딩까지 이어졌고, 다섯 막에 퀘스트가 다 붙었다**
  (요리사 ✅ → 튜토리얼 ✅ → 키워드·1막 ✅ → 2막 ✅ → 3막 ✅ → 엔딩 ✅ →
  **2~5막 퀘스트 일곱 ✅**). 퀘스트 17 · 컷씬 43(막별 21·8·5·6·3)다.
  **호감도 5단계 대사와 그들이 「주는 것」 다섯도 다 붙었다** (`bt_*` 60줄 × 두 언어 ·
  `BOND_GIVES` 의 🛡️🍖🎻💎📖). 기본 잡담도 사람마다 다섯 줄까지 채웠다.
  막별 전체 표는 **`ACTS.md`** 에 있다. 남은 것은 연출이다 — `STORY.md` 에 아직 «기획만» 있는 것이 무엇인지는
  그 문서의 「만드는 순서」가 ✅ 로 표시한다. 거기 적힌 것을 '있는 기능' 으로
  착각하지 말 것 — 만들 때 그 문서에서 대사와 조건을 뽑아 쓴다

- **크리처는 `CREATURE.md` 의 1~9단계까지 됐다.** 서른 종을 만들고(`tools/gencreature.js`)
  이모지 대신 SVG 로 그리고(`creature.js`), 매력 무한 누적을 없애 **장착한 한 마리만**
  반영되게 했다 (세이브 12). 맵 51곳에 속성이 붙고, **동행 한 마리를 데려가면
  속성·날씨·시간대가 맞는 만큼 재료를 하나 더 준다.** 히든 재료 확률도 맵마다 갈렸다.
  **먹이를 주면 로열티(덤 개수)와 버프(덤 확률)가 붙는다.**
  **7단계로 중복 크리처를 재료로 녹인다** — 상급 조합에 같은 속성의 중급 크리처가
  하나 들어간다. **8단계로 하루에 한 번 생산한다** — 속성이 무엇을, 등급이 몇 개를
  정하고, 만드는 것은 **애착 한 마리 + 동행 한 마리**뿐이다.
  **9단계로 밭과 약탈이 들어갔다** — 여기서 **서버가 판정을 갖는다.**
    밸런싱도 연출도 끝났다 (`FARM.md` 9-1 · 9-2).
  · **해금은 `charmPeak()`(여태 닿은 최고 매력)으로 판정한다.** 애착을 약한 것으로 바꾸면
    총합이 내려가는데, `isMapOpen` 이 총합을 그대로 보면 열려 있던 맵이 다시 잠긴다.
    **한 번 연 것은 무슨 이유로도 닫히지 않는다**
  · **날씨를 `Math.random()` 으로 정하면 안 된다.** `render()` 마다 바뀌어 채집 한 번에
    비가 왔다 갔다 한다 — 리그 NPC 처럼 **시각과 맵 id 로 시드를 고정한다**(`weatherOf()`).
    세이브에는 안 남긴다. 남기면 기기마다 달라지고 동기화 충돌거리가 하나 는다
  · **채집 시간대는 운동(`EX_WHEN`)의 구간을 더 잘게 자른 것이다.** 경계를 따로 잡으면
    20시 30분에 「운동은 낮인데 채집은 밤」이 되어 설명이 두 벌이 된다.
    `npm run test:time` 이 이것을 검사한다 — 경계를 한 시간만 옮겨도 잡힌다
  · **크리처가 어디에 있는지는 `move` 가 정한다** — 물고기는 **어항**, 날개가 있으면 공중,
    나머지는 땅. 한동안 서른 마리가 전부 떠 있어서 유니콘이 허공을 딛고 있었다.
    물고기에는 지느러미(`wing: 'fin'`)가 있어서 **물이 날개보다 먼저 걸려야 한다**
  · **채집 토스트의 「단서」는 조각을 조립한 것이다** (`gatherClue()`). 조합이 7천 가지라
    문장을 다 쓸 수 없다. **매번 띄우지 않는다** — 꾹 누르기 자동 채집이 있어서
    매번 두세 줄이 되면 잔소리가 된다 (`CREATURE.md` 6장)
  · **솥에 넣는 것을 `itemOf(id)` 한 곳에서 찾는다.** 레시피 입력에 크리처 id 가
    섞이면서 필요해졌다 — 예전에는 가방·솥 구멍·레시피 줄이 저마다 `D.INGREDIENTS[id]`
    를 **바로** 읽어서, 크리처가 들어오는 순간 그 조회가 전부 `undefined` 가 됐다.
    개수는 `stockOf`(재료는 가방 · 크리처는 **초과분**), 소모는 `spendItem` 을 지난다
  · **마지막 한 마리는 못 녹인다**(`meltCount` = 가진 수 − 1). 그래서 로열티·버프가
    날아가거나 장착이 풀릴 일이 없다 — 기획 초안의 「확인 모달로 경고」보다 이쪽이 낫다.
    `npm run test:melt` 가 **진짜 화면에서 담고 조합해** 개체 수까지 센다
  · **생산은 평야·숲 재료만 준다.** 늦게 열리는 지대 재료를 매일 쥐여 주면
    **맵 해금이 의미를 잃는다** — 기초 크리처의 재료를 평야·숲으로 묶어 둔 것과 같은
    이유다. 생성기가 지대·히든·속성 중복을 다 검사한다
  · **방치 상한과 기록 길이를 같은 값으로 둔다**(`PRODUCE_DAYS` = 5).
    기록이 5줄인데 상한이 30이면 「5일치까지만 쌓인다」를 아무도 눈치 못 챈다.
    정산은 **AP 자정 충전과 같은 자리**에서 한다 — 날짜 판정이 두 벌이면 어긋난다.
    `npm run test:time` 이 30일을 건너뛰어 5일치만 들어오는지 본다
  · **먹이는 축이 둘이다.** 로열티는 영구히 쌓여 덤의 **개수**를 늘리고, 버프는 일시적으로
    덤의 **확률**을 올린다. 하나로 묶으면 「나가기 직전에 먹인다」는 판단이 사라진다.
    **로열티를 확률에 얹지 않는다** — 확률에는 이미 넷이 붙어 있어 다섯째는 아무도 못 느낀다.
    버프는 **끝나는 시각 하나**로만 산다 (카운트다운은 앱을 꺼 두면 안 흐른다)
- 아우라 중 **우아함**은 아직 효과가 붙어 있지 않다.
  **개성**(옷 획득 확률)과 **행운**도 수치만 오르고 아직 연결되지 않았다.
  **근성**은 운동이 붙었다 — 고강도 종목을 열고 근육량을 올린다 (`EXERCISE.md`)
- 이메일·소셜 로그인. 붙일 때는 지금의 `playerId` 를 계정에 연결만 하면
  기존 플레이어 진행이 그대로 넘어간다 (세이브 형식은 그대로, 소유자만 바꾸면 됨)
- **리그(주간 랭킹)의 상대가 아직 NPC 다.** 화면·정산·승급/강등은 다 돌아가는데,
  같은 조 11명은 주차·리그로 시드를 고정해 만든 NPC 다 (`leagueNpcs`).
  진짜 플레이어끼리 조를 짜려면 서버가 **주간 점수와 조 편성**을 들고 있어야 한다 —
  지금 서버에는 누적 매력 순위표(`GET /api/ranking`, `charm` 컬럼)뿐이다.
  붙일 때는 `leagueNpcs` 가 돌려주는 자리를 실제 사람으로 바꿔 끼우면 된다.
  · **밭(9단계)이 먼저 그 길을 냈다** — 서버가 남의 행을 골라 오고(`store.peers`)
    이름으로 상대를 가리키는 짜임이 이미 있다. 조 편성도 같은 방식으로 붙일 수 있다
- **밭을 키우는 기획이 `FARM.md` 에 있다.** 여신 단계(매력 100)에 탐험의 세 번째
  갈래로 열리고, 다섯 칸을 경작해 **채집으로는 안 나오는 특수 작물**을 기르며,
  크리처 다섯이 칸 하나씩을 지키고 다섯 판을 **자리 대 자리**로 붙는다.
  만드는 순서를 문서 끝에 적어 두었다 (그릇 → 탭 → 경작 → 물약 → 5대5 → 밸런싱).
  · **1단계(그릇)** — 서버의 밭 레코드가 `farm.plots[]` 가 됐고, 옛 모양
    (`farm.stash` 하나)은 읽을 때 첫 칸으로 옮겨 적는다(`migrateFarm`)
  · **2단계(탭)** — 탐험이 **필드/마을/밭 세 갈래**가 됐다. 밭은 여신 단계
    (매력 100 · 랭킹과 같은 문)부터 열리고, 판정은 `farmOpen()` → `charmPeak()` 다 —
    애착을 약한 것으로 바꿔도 **한 번 연 것은 안 닫힌다**.
    마이 룸의 🌾 는 여신부터 **그 탭으로 보낸다**(같은 것을 두 자리에서 안 보여 준다).
    시트와 탭은 **`farmHtml()` 하나가 그린다** — 두 벌이면 한쪽만 고치게 된다
  · **3단계(경작)** — 밭에 **칸 다섯**이 생겼다. 심으면 특수 작물이, 비워 두면
    크리처의 이삭이 쌓인다. 칸은 **현자의 결정**으로 연다 (AP 충전에 이은 두 번째 출구).
    특수 작물 여섯은 `tools/genfarm.js` 의 축 표에서 나온다 — **씨앗 칸을 안 만들고**
    이미 있는 재료를 낸다 (네 번째 가방이 생기지 않게).
    ⚠️ **재료·현자의 결정은 클라이언트가 깎는다.** 세이브가 클라이언트 정본이라
    서버가 깎아도 다음 저장에 되살아난다 — **서버가 판정을 갖는 기준은
    「남의 것을 건드리는가」다**(약탈이 그렇다). 다만 **다 자라는 시각은 서버가 잰다**
  · **4단계(물약)** — 특수 작물을 하나씩 먹는 **상급 물약 여섯**. 밭이 없으면 못 만든다.
    **여섯의 값이 일부러 같다** — 하나가 더 좋으면 나머지 다섯 작물이 죽은 콘텐츠가 된다.
    `checkdata` 가 **파일에 써진 것**을 본다 (작물이 채집 풀에 없는가 · 아무 레시피도
    안 쓰는 작물이 없는가 · 들어갈 솥이 있는가)
  · **5단계(5대5)** — 크리처 **다섯이 칸 하나씩을 지키고**, 쳐들어간 다섯과
    **자리 대 자리**로 붙는다. 세 판 이상 이겨야 가져간다.
    **총 전투력 합으로 붙이지 않는다** — 합으로 하면 고르는 일이 「합계 최대화」
    하나로 끝나고 속성 순환이 평균에 뭉개진다. **자리 번호 = 칸 번호**라
    1번을 이기면 상대의 1번 칸에서만 가져온다.
    ⚠️ **자라는 중인 칸은 이겨도 안 건드린다** — 남의 시간을 없애는 것은
    뺏는 것보다 나쁘다. ⚠️ **부대를 바꾸고 바로 쳐들어가면 서버는 옛 부대를 본다**
    (저장이 3초 디바운스다) — `doRaid` 가 먼저 `Sync.pushNow()` 로 올린다
    **밭을 처음 열면 센 다섯이 저절로 선다**(`autoTeam`) — 빈 밭은 그냥 털리는 밭이다.
    자리마다 눌러 그 자리로 열고, **이미 선 아이를 고르면 두 자리를 맞바꾼다**
    (막으면 순서를 바꿀 때마다 비우고 다시 넣게 된다)
  · **6단계(밸런싱·연출)까지 다 만들었다** (`FARM.md` 9-1 · 9-2)
- ~~**밭·약탈의 수치는 전부 임시값이다**~~ → **밸런싱을 잡았다** (`FARM.md` 9-1).
  「사람이 여럿 붙어 봐야 안다」고 적어 뒀는데 **사람이 있어야 아는 것은 접속 빈도뿐**이고
  나머지는 규칙에서 나온다. `npm run sim:farm` 이 200명을 30일 살게 해서 잰다 —
  **`server/battle.js` 의 진짜 함수를 부른다** (사본을 만들면 시뮬레이터만 옛 규칙으로 돈다).
  · ⚠️ 제일 큰 것은 수치가 아니었다: **남의 밭을 안 자라게 둬서 약탈이 30일 동안 0번**이었다.
    밭은 주인이 들어와야 자라는데 주인은 들어오면 곧바로 거둔다 — 저장된 밭은 늘 비어 있었다.
    안 자라게 둔 이유(「정산 시각이 남의 접속에 좌우된다」)는 틀렸다:
    `grow()` 는 시계를 `now - (elapsed % 24h)` 로 당겨 **위상을 보존한다**
  · 방패 2시간 → **4시간**(2시간이면 하루 12번까지 털린다) ·
    **「하루치는 못 가져간다」**(`RAID_FLOOR_DAYS` = 2일치)를 넣어 「아침에 빈 밭」을 없앴다
  · **수치끼리의 약속은 `tools/checkbalance.js` 가 본다** (`npm test` 가 돌린다).
    실패 보상 < 조합 값 · 가방 상한 = 밭 상한 · 바닥 < 상한 · 밭 물약 여섯이 같은 값 ·
    맨 위 리그에 닿는가 — 전에는 **주석에만** 적혀 있어 한쪽만 고치면 조용히 깨지던 것들이다
  · 남은 것은 **연출**이다 (이겼다/졌다가 토스트 한 줄)
- **리그 난이도(`leaguePace`)는 임시값이지만, 「빡빡하다」는 이제 틀렸다.**
  맨 아래 리그의 1위가 주 40점, 맨 위가 846점.
  ⚠️ 예전에는 「열심히 한 주의 상한이 600~700점이라 위쪽 리그는 빡빡하다」고 적어 두었는데,
  **밭 물약 여섯(매력 16)이 생기면서 상한이 1312점으로 올라갔다** — 맨 위 리그가 상한의
  64% 라 닿는다. `tools/checkbalance.js` 가 이 관계를 본다 (한 주 AP → 최고 효율 물약 → 점수).
  랭킹이 열리는 시점(매력 100)에는 맨 아래 리그부터 시작하므로 처음 몇 주는 쉽게 올라가고,
  랭킹이 열리는 시점(매력 100)에는 맨 아래 리그부터 시작하므로 처음 몇 주는 쉽게 올라간다.
  주에 한 칸씩이라 맨 위까지 32주다 — 이 곡선을 바꾸려면 여기를 본다
- **마이 룸 꾸미기.** 배경은 5단계로 나뉘어 있고(`avatar.js` 의 `ROOM_LEVELS`)
  소품마다 id 가 붙어 있다. 플레이어가 고른 소품 id 목록을 `roomScene(lv, extra)` 의
  둘째 인자로 넘기면 그대로 얹힌다 — 그릴 것은 있고, **올려 주는 조건과 고르는 화면이 없다.**
  지금은 개발용(임시)의 '🏠 방 배경' 으로만 단계를 바꿔 볼 수 있다 (`UI_POLICY.md` 3-3)
- 이모지 아트 → 실제 일러스트 교체
- **다이아 상점의 결제.** 수량·가격은 `D.SHOP`(data.js)에 있고 패널은 뜬다.
  누르면 '지금은 구매하실 수 없습니다' 로 끝난다 — `buyDiamond()`(game.js) 안쪽만 채우면 된다.
  현자의 결정이 모자란 경우도 전부 `openDiamondShop()` 으로 모여 있다
- **AP 충전 밸런스는 임시값이다.** `D.ENERGY.chargeCost`(1000 = 가득 채울 때) 와
  `failReward`(10). 실제로 내는 값은 **슬라이더로 고른 만큼**이다 (`costFor(n)` 이 비례로 나눈다).
  실패 보상이 조합 값(25 AP)을 넘으면 일부러 실패해서 AP 를 무한히 버는 고리가 생긴다 —
  수치를 바꾸기 전에 이 관계를 먼저 볼 것
