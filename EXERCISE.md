# 운동 시스템 — 기획

`STORY.md` 의 「혼자 먹은 밤」(폭식)으로 가기 전에 **근성**부터 붙인다.
아우라 다섯 중 `grit`(근성)은 지금 **수치만 오르고 효과가 없다** — 운동이 그 자리다.

> **만들었다.** 아래는 실제로 들어간 것이고, 코드와 어긋나면 코드가 맞다.
> 수치는 전부 **임시값**이다 — 바꾸기 전에 봐야 할 관계를 각 표 아래에 적어 두었다.
> 폭식(「혼자 먹은 밤」)까지 들어갔다. 남은 것은 **요리사 클레멘** 쪽이다 — 7-3 끝 참고.

---

## 한 줄

**물약은 마법이고, 운동은 진짜 노력이다.** 둘 다 몸을 바꾸지만 드는 것이 다르다 —
물약은 재료와 AP, 운동은 **스태미나와 시간**이다. 그리고 스태미나는 **먹어야** 찬다.

```
음식 → 포만감 ↑ → 스태미나 상한 ↑ → 운동 → 근성 ↑ → 더 고강도 운동
                                            └→ 단련 ↑ → 체지방 ↓ · 체중 ↓ · 체형 날씬
방치 → 포만감 ↓ → 스태미나 안 참 → 운동 못 함 → 근성 ↓ · 단련 ↓ → 체형 통통
```

이 고리의 **반대쪽 끝이 폭식**이다. 포만감이 바닥인 채로 날이 바뀌면 혼자 먹는다
(`STORY.md` 「폭식 시스템의 판정 기준」) — 7-3 참고.

---

## 1. 먼저 정해야 하는 것 — 몸은 무엇으로 움직이는가

**지금 체중·체지방률·체지방량·근육량은 전부 `S.stats.beauty` 하나에서 계산된다.**
독립된 몸 상태가 없다.

```js
bodyLevel(beauty) = 1 - min(1, beauty / 60)     // 1 = 통통, 0 = 날씬
weightKg   = BMI(bodyLevel) × 키²
bodyFatPct = lerp(19%, 34%, bodyLevel)
muscleKg   = 체중 × lerp(35%, 27%, bodyLevel) + 근성 보정(최대 +3%p)
```

그래서 「운동하면 체지방이 준다」를 넣으려면 **몸을 움직이는 축을 하나 더** 놔야 한다.

### 결론 — `S.fit`(단련도)를 더한다

```js
// 몸이 보는 값. 매력 총합(비주얼 + 매력)은 건드리지 않는다.
function bodyPoint() { return Math.max(0, (S.stats.beauty || 0) + (S.fit || 0)); }
```

`bodyLevel` · `weightKg` · `bodyFatPct` · `muscleKg` 가 `beauty` 대신 이 값을 본다.

- **운동하면 `fit` 이 오른다** → 체지방률·체지방량·체중이 내려가고 체형이 날씬해진다
- **방치하면 `fit` 이 내려간다.** 음수까지 간다 — 그래야 방치가 물약으로 쌓은 진행을
  실제로 갉아먹는다. `bodyPoint()` 가 0 밑으로는 안 내려가므로 통통이 하한이다
- **매력 총합은 안 건드린다.** 운동으로 랭킹 점수가 오르면 리그 밸런스가 통째로 흔들린다.
  운동의 보상은 **몸**이지 점수가 아니다

> 다른 길도 있었다. 「운동이 비주얼을 조금 준다」로 하면 새 축이 필요 없다.
> 하지만 그러면 **운동이 물약의 싼 대체재**가 된다 — 하급 물약 한 병이 비주얼 1이라,
> 운동 한 번에 1만 줘도 채집·조합을 통째로 건너뛰는 길이 생긴다. 그래서 축을 나눴다.

**세이브**: `fit` 은 **새로 생기는 키**라 마이그레이션이 필요 없다
(`Object.assign(defaultState(), 저장값)` 이 기본값 0 을 그대로 남긴다).
`fullness` · `stamina` · `bodyTs` · `lastWorkoutTs` · `decayTs` · `foods` 도 같다.
`SAVE_VER` 를 올릴 이유는 **없다** — 기본값을 바꾸는 게 아니라 없던 칸을 더하는 것이다.

---

## 2. 포만감 (`S.fullness`)

`0 ~ 100`. **100 이 배부름, 0 이 굶주림.**

> 「허기」가 아니라 **「포만감」**으로 부르기로 했다. 높을수록 배부른 값에 「허기」라는
> 이름을 달면 「스태미나 상한 = 근육량 + 허기」가 거꾸로 읽힌다.

### 줄어드는 규칙

```js
FULLNESS = {
  max: 100,
  happyMax: 5.0,       // 행복 0   → 시간당 이만큼 준다 (하루 -120)
  happyMin: 2.0,       // 행복 1000 → 시간당 이만큼    (하루 -48)
}
시간당 감소 = lerp(happyMax, happyMin, happy / 1000)
```

- **행복이 높으면 천천히 준다(포만감), 낮으면 빨리 준다(공복감).**
  `STORY.md` 의 「정신적 허기는 애정결핍이다」가 수치로 나오는 자리다 —
  마음이 채워져 있으면 덜 먹어도 된다
- **운동하면 그만큼 더 준다** (종목별 `full`). 운동 → 배고픔 → 음식이 고리다
- 포만감이 **0 이면 스태미나가 안 찬다.** 굶으면 못 움직인다 —
  도중에 바닥나면 그 전까지만 회복한다(`tickBody` 의 `fedH`). 이 한 줄을 빼면
  **굶은 채로 오래 두는 것이 오히려 이득**이 된다

> ⚠️ 「행복이 낮으면 포만감」과 「행복이 낮으면 공복감」이 요구사항에 둘 다 적혀 있었다.
> 세계관(`STORY.md`)이 뒤쪽을 가리키므로 **행복이 낮으면 공복감**으로 잡았다.
> 반대로 갈 거라면 `FULLNESS.happyMin/happyMax` 두 줄만 바꾸면 된다.

---

## 3. 스태미나 — 시간이 채우고, 운동이 쓴다

```js
STAMINA = {
  base: 20,            // 아무것도 없어도 이만큼
  perMuscle: 1.0,      // 근육량 1kg 당
  perFull: 0.25,       // 포만감 1 당
  perHour: 6,          // 시간당 회복 (포만감이 0 이면 0)
}
상한 = round(base + muscleKg() × perMuscle + fullness × perFull)
```

지금 몸으로 계산하면 근육량 ≈ 15kg, 포만감 100 → **상한 ≈ 60**.
시간당 6 이면 **10시간에 가득** 찬다 → 하루에 두어 번 운동하는 리듬이다.

**AP 와 같은 방식으로 잰다** — 값을 계속 더하지 않고 `bodyTs`(마지막 계산 시각)를
두고 **볼 때 계산한다**. AP 의 자정 충전과 달리 연속이라 시각이 필요하다.

> 상한이 근육량을 보고, 근육량이 다시 근성을 보므로 **운동할수록 상한도 는다.**
> 이 되먹임이 너무 세면 후반에 무한 운동이 된다 — `perMuscle` 을 올리기 전에
> 근성 1000 에서의 상한을 먼저 계산해 볼 것.

---

## 4. 운동 종목 — 근성이 문을 연다

| 종목 | 필요 근성 | 스태미나/분 | AP/분 | 근성/분 | 단련/분 | 포만감/분 |
|---|---|---|---|---|---|---|
| 🚶 산책 | 0 | 1.0 | 1.0 | 0.4 | 0.010 | 0.3 |
| 🧘 스트레칭 | 50 | 1.5 | 1.5 | 0.6 | 0.015 | 0.4 |
| 🏃 달리기 | 150 | 2.5 | 2.5 | 1.0 | 0.030 | 0.7 |
| 🏋️ 근력 운동 | 350 | 3.5 | 3.5 | 1.4 | 0.045 | 1.0 |
| 🤸 서킷 | 600 | 5.0 | 5.0 | 2.0 | 0.070 | 1.4 |

**시간**은 `10 / 20 / 30 / 60분` 중에 고른다.

- 잠긴 종목은 **자물쇠로 보여 준다** — 무엇이 기다리고 있는지 알아야 근성을 올릴 이유가 생긴다
  (`UI_POLICY` 7장 「잠긴 콘텐츠 공통 표현」)
- **스태미나가 모자라면 그 시간을 못 고른다.** 상한이 60인데 서킷 60분은 300이 드니
  처음에는 산책 30분쯤이 한계다 — 「처음에는 고강도가 불가능하다」가 이렇게 나온다
- AP 는 하루 1000 이고 채집 10 · 조합 25 다. 운동에 **하루 200~300 AP** 쯤 쓰는 것이
  적당하다고 보고 잡은 값이다 (달리기 60분 = 150 AP)

### 보상

```
근성   += grit × 분          (아우라 grit, 상한 1000)
단련   += fit  × 분          (S.fit — 체지방·체중·체형이 따라 움직인다)
포만감 -= full × 분
스태미나 -= stam × 분
AP     -= ap   × 분
```

달리기 60분 = 근성 +60 · 단련 +1.8. **근성 600(서킷)까지 여러 주**가 걸린다 —
방치형의 호흡에 맞춘 것이고, 빠르게 하려면 `gritPerMin` 을 본다.

### 운동은 즉시 끝난다

실시간 타이머를 두지 않는다. 「운동 시작」을 누르면 **바로 정산하고 연출만 보여 준다** —
채집·조합과 같은 방식이라 새 개념이 안 늘고, 팝업이 커지지 않는다.
나중에 「30분 뒤에 돌아오세요」로 바꾸고 싶으면 `lastWorkoutTs` 옆에
끝나는 시각을 하나 더 두면 된다.

---

## 5. 방치하면 되돌아간다

```js
DECAY = {
  gritPerDay: 8,       // 마지막 운동에서 하루 지날 때마다
  fitPerDay: 0.6,      // 단련도 — 물약 진행을 갉아먹는다
  graceDays: 1,        // 하루는 봐 준다 (하루 못 들어왔다고 벌주지 않는다)
}
```

- 마지막 운동(`lastWorkoutTs`)에서 **`graceDays` 를 넘긴 날 수**만큼 깎는다.
  **`decayTs`(어디까지 반영했는지)를 같이 둔다** — 없으면 부를 때마다 또 깎인다
- `fit` 은 음수로 내려간다 → 체중·체지방이 늘고 체형이 통통해진다
- **깎는 것은 로그인할 때 한 번에** 계산한다 (AP 의 자정 충전과 같은 자리)

> 여기가 이 게임에서 **처음으로 수치가 내려가는 곳**이다. 조심해서 붙일 것 —
> 오래 쉬었다 돌아온 사람이 「내 캐릭터가 망가졌다」고 느끼면 그대로 떠난다.
> 그래서 상한을 둔다: 한 번에 깎이는 양은 **최대 7일치**까지다.

---

## 6. 음식 — 잡화 탭

인벤토리의 `🧴 물약` 탭을 **`🎒 잡화`** 로 바꾸고, 그 안에 하위 탭 둘을 둔다.

```
잡화 ─┬─ 🧴 물약   (지금 것 그대로)
      └─ 🍞 음식   (먹으면 포만감이 찬다)
```

**3단 탭이 아니다.** 마이 룸 › 옷 안에 이미 슬롯 탭(`.cat-tabs.wr-tabs`)이 있어서
「하위 탭 안의 하위 탭」은 이미 있는 짜임이다 — 같은 컴포넌트를 쓴다
(`UI_POLICY` 1장). 둘뿐이므로 **균등 분할 두 칸**이다.

| 음식 | 포만감 | 비고 |
|---|---|---|
| 🥣 죽 | +15 | |
| 🍞 빵 | +25 | |
| 🥗 샐러드 | +20 | 행복 +5 |
| 🍗 구운 고기 | +40 | 단련 -0.2 (기름지다) |
| 🍰 케이크 | +50 | 행복 +20 · 단련 -0.5 |

- **음식은 채집에서 재료와 함께 나온다** (`D.FOOD_RATE = 0.12` — 채집 100번에 12개쯤).
  재료를 대신 뺏지 않는다: 둘 다 나오고, 토스트도 한 줄로 합쳐서 알린다.
  요리사 **클레멘**이 들어오면 이 자리를 그가 가져간다 — 폭식 시스템의 반대편이라
  서사가 거기에 맞는다 (`STORY.md`)
- **절반 넘게 버려질 상황이면 안 먹는다.** 포만감 98 에서 케이크(+50)를 눌러 48을
  날리는 것이 화면에는 전혀 안 보인다 — 「가득일 때만 막기」로는 이 손해를 못 막는다
- 케이크처럼 **포만감은 많이 채우지만 단련을 깎는 음식**을 둔다. 그래야 「무엇을 먹느냐」가
  선택이 된다 — 안 그러면 제일 큰 것만 먹는다
- 폭식 시스템은 **이 표를 그대로 쓴다.** 혼자 먹는 밤에 케이크가 자동으로 들어간다

---

## 7. 화면 — 어디에 놓나

### 7-1. 「운동」 버튼 → **방 그림 오른쪽 아래**

```
┌─────────────────────────┐
│ 🌙 11:06 UTC+9          │  ← 시계 (왼쪽 위, 이미 있다)
│                         │
│          (아바타)        │
│                         │
│              [🏃 운동]  │  ← 여기
└─────────────────────────┘
       🌱 새싹  1
      ▾ 스탯 자세히 보기
```

**이유**

- **세로를 한 픽셀도 안 먹는다.** 방금 스탯을 접고 버튼을 6px 올린 화면이다 —
  여기에 가로 줄을 하나 더 얹으면 그 작업이 무의미해진다
- **시계와 대칭이다.** 방 그림 위에 얹는 것이 이미 있으므로 새 규칙이 아니다
  (`.on-room-bg` — 흰 글자 + 퍼지는 음영)
- 「방 안에서 하는 일」로 읽힌다. 나중에 🍽️ 식사 · 🪞 거울 · 🏠 꾸미기가
  **그 아래로 쌓일 자리**이기도 하다

**주의** — 방 배경 위 글자는 대비를 잴 수 없어 `.on-room-bg` 로 빼야 하는데,
예외는 최소한만 쓰는 것이 이 프로젝트의 규칙이다. **버튼은 명판으로 만든다** —
반투명 흰 알약 배경에 `--ink` 글자면 대비를 **실제로 잴 수 있다**
(`UI_POLICY` 마을 이름표와 같은 판단).

> 다른 후보와 왜 아닌지
> - 스탯 덩어리 안(「매력 과시」 옆) → **접으면 사라진다.** 늘 눌러야 하는 것이라 안 된다
> - 인벤토리 탭 줄 → 탭은 **보는 곳**이지 **하는 일**이 아니다. 성격이 다르다
> - 상단 바 → 265px 에서 이미 네 칸이라 넘친다

### 7-2. 「포만감 · 스태미나」 → **스탯 표 맨 위, 「지금 상태」 줄**

```
┌───────────────────────────┐
│  🌱 새싹        1          │  ← 매력 총합 (접어도 남는다)
├───────────────────────────┤
│  🍚 포만감 78/100  🏃 34/60 │  ← 여기 (새 줄, 두 칸)
├─────────────┬─────────────┤
│ 🪞 비주얼 12 │ 💖 아우라 30│
│ 체중  52.4kg │ 행복  120   │
│ …            │ …           │
└─────────────┴─────────────┘
```

**이유** — 포만감·스태미나는 **비주얼/아우라 표와 성격이 다르다.**
저 둘은 **쌓이는** 값이고, 이 둘은 **차고 주는** 값이다(AP 와 같은 부류).
같은 표에 섞으면 「체지방률 24%」 옆에 「포만감 78」이 나란히 서서, 무엇이 쌓이는 값이고
무엇이 지금 상태인지 안 읽힌다.

- **접으면 같이 감춘다.** 접기의 규칙은 「단계와 매력 총합만」이다 — 이미 정한 것을
  여기서 깨지 않는다. 대신 **운동 팝업에는 항상 크게** 나온다
- 두 칸은 `.stat-row`(2열 격자)를 그대로 쓴다. 새 컴포넌트가 아니다
- 265px 에서 「포만감 78/100」과 「스태미나 34/60」이 한 칸에 들어가는지 **반드시 잰다** —
  영어(`Fullness` / `Stamina`)가 더 길다

### 7-3. 운동 팝업

기존 `.modal` 을 쓴다 (조합 결과·확인 패널과 같은 것).

```
┌─────────────────────────────┐
│  🏃 운동                     │
│  🍚 78/100    🏃 34/60       │  ← 지금 상태를 크게
│                             │
│  [🚶 산책] [🧘 스트레칭]      │  ← 종목 (잠긴 것은 🔒 + 필요 근성)
│  [🏃 달리기] [🏋️ 근력 🔒350] │
│                             │
│  [10분] [20분] [30분] [60분] │  ← 스태미나가 모자란 칸은 잠긴다
│                             │
│  ⚡ 75 AP  ·  🏃 -75         │  ← 고른 만큼 계산해서 보여 준다
│  얻는 것: 근성 +30 · 단련 +0.9│
│                             │
│  [    운동 시작    ]         │
└─────────────────────────────┘
```

- **고르기 전에 드는 것과 얻는 것을 다 보여 준다.** AP 충전 슬라이더와 같은 방식이다
- 튜토리얼 막(`.tut`)보다 위에 뜬다 — `z-index` 는 `.modal`(50) 그대로

---

## 7-3. 혼자 먹은 밤 (폭식)

**판정 기준은 무엇을 먹었느냐가 아니라 「혼자 먹었느냐」다** (`STORY.md`).

```js
BINGE = {
  atFullness: 15,     // 날이 바뀔 때 포만감이 이보다 낮으면 혼자 먹는다
  fullnessBack: 70,   // 먹고 나면 이만큼까지 찬다 — 배는 부르다
  happy: -20,         // **이게 고리의 핵심이다**
  fit: -0.8,          // 체지방·체중이 는다
  grit: -8,           // 혼자 쌓은 것은 혼자 무너진다
  maxNights: 3,       // 오래 비웠어도 한 번에 이만큼까지만
}
```

- **날이 바뀔 때만 판정한다.** 낮에 포만감이 0 이 돼도 아무 일도 없다 —
  판정 시각은 AP 자정 충전과 같은 자리다 (`dayKey` 가 달라지는 순간)
- **막는 방법은 하나뿐이다: 자기 전에 먹어 둔다.** 그리고 행복이 높으면 포만감이
  천천히 줄어 저절로 막힌다 — 「마음이 채워져 있으면 덜 먹어도 된다」가 규칙이 된다
- 안 본 밤은 `S.binges` 에 **밤마다 한 칸씩** 쌓인다 (최대 5). 여러 밤을 하나로
  뭉치면 「그날 밤」이 사라진다
- 이대로 날이 바뀌면 먹게 되는 상태는 **미리 보인다**(`.now-val.low`).
  아침에 와서 「왜 깎였지」를 알게 되는 것보다 밤에 「먹어야겠다」가 낫다
- 플레이 기록에 **「혼자 먹은 밤」**으로 센다. 「폭식 12회」가 아니다 —
  이 게임은 벌하는 게임이 아니다

### 알리는 방식 — 토스트가 아니라 **「흡입」 컷씬**

그날 밤은 이 게임에서 제일 중요한 장면이라 흘러가는 토스트로 때울 수 없다.
그리고 **등을 돌린 뒷모습**이 이 장면의 전부라, 글자로는 전달되지 않는다.

- 마이 룸의 방 그림 오른쪽 아래, 「🏃 운동」 아래에 **「🍗 흡입」** 버튼이 있다
- 안 본 밤이 있으면 **붉은 뱃지에 몇 번인지** 적힌다. 레드닷 하나로는
  「하나 있나 보다」로 읽혀서 세 밤이 쌓인 것을 못 알린다
- 여러 밤이면 **먼저 물어본다** — 세 장면이 예고 없이 이어지면 갇힌 느낌이 든다
- **본 것은 지운다.** 그래서 뱃지가 준다. 볼 것이 없을 때 누르면
  「이미 벌어진 참사는 모두 목격하셨습니다.」
- 나가는 길은 **늘 오른쪽 위에** 있다 (`✕ 닫기`). 중간에 나가면 남은 밤은 그대로 남는다
- 우적우적은 CSS 무한 반복이다 — 끝이 없으므로 검증기의 `settle()` 이 안 건드린다

#### 우적우적 — 박자 하나에 부품 여섯

고개만 까딱이던 것을 **춘식이 고구마 먹방** 결로 다시 짰다. 부품마다 애니메이션을
따로 두되 **박자(`--cb-beat: 0.34s`)는 하나로 묶는다.** 제각기 다른 주기로 움직이면
씹는 것이 아니라 각자 떠는 것으로 보인다.

| 부품 | 주기 | 하는 일 |
|---|---|---|
| `.cb-head` | 1박 | 목을 축으로 고개를 까딱 |
| `.cb-body` | 1박 | 바닥을 축으로 눌렸다 편다 (스쿼시 & 스트레치) |
| `.cb-arm` | 1박 | 어깨를 축으로 팔꿈치가 들썩 |
| `.cb-bite` | 4박 (한 입) | 든 손이 입으로 갔다 돌아온다 |
| `.cb-food` | 4박 | 한 입 베어 물 때 잠깐 작아진다 |
| `.cb-spark` | 2박 | 「우적」 효과선. 좌우를 반 박자 어긋나게 |
| `.cb-crumb` | 4박 | 부스러기 셋이 시작 시각을 달리해 톡톡 튄다 |

지킬 것 넷:

1. **CSS `transform` 은 SVG `transform` 속성을 덮어쓴다.** 체형 배율(`scale(k,1)`)이
   걸린 그룹을 그대로 움직이면 통통한 몸이 갑자기 홀쭉해진다 — **한 겹 안에서** 움직인다
2. **`transform-origin` 은 조상 변환이 아니라 그 요소의 좌표계에서 풀린다.**
   머리가 `translate(0,26)` 안에 있으므로 축도 26 을 뺀 값으로 적는다
3. **든 것은 머리보다 뒤에 두면 안 된다.** 뒤통수에 가려 닭다리 끝만 삐죽 나온다 (실제로 그랬다)
4. **전부 `infinite` 여야 한다.** 끝이 있는 애니메이션은 `settle()` 이 끝 상태로 보내 버려서,
   검증기가 재는 순간 장면이 정지한다

흔들림에 약한 사람에게는 `prefers-reduced-motion` 으로 **정지 화면**을 준다.
장면의 뜻은 그림만으로도 전해진다.

**개발용(임시)에 「🌙 폭식 한 밤」 버튼이 있다.** 폭식은 날이 바뀔 때만 일어나서
그냥은 하루를 기다려야 본다. 이 버튼은 **실제 경로를 그대로 지난다** —
포만감을 비우고 판정 날짜를 어제로 돌린 뒤 `checkBinge()` 를 부른다.
이벤트를 손으로 만들어 넣으면 진짜 경로가 검사되지 않는다.

**뒷모습은 `Avatar.crouchBack()` 이 그린다** — 지금 그 캐릭터의 머리 모양·머리색·
옷 색·체형·**신발**을 다 따라간다. 신발은 색만이 아니라 목(`rise`)과 마감(`finish`)까지
아바타와 같은 필드를 쓴다 — 색만 바꾸면 유리구두를 신고도 맨발과 같은 모양이라
「신발이 안 그려진다」로 읽힌다. 맨발이면 뒤꿈치에 그늘을 하나 넣는다. 뒷머리(`hairBack`)는 원래 뒤통수를 그리는 함수라 그대로 쓴다.
다른 그림을 하나 더 그리면 「내 공주」가 아니라 삽화가 되어 버린다.
**얼굴은 안 그린다. 그게 이 장면의 전부다.**

```
도저히 참지 못하고 케이크를 폭풍 흡입했다. (1번)
행복 −20 · 근성 −8 · 단련 −0.80
배는 불렀는데, 채워지진…
```

무엇을 먹었는지는 **수치에 영향을 주지 않는다** — 판정 기준은 「혼자 먹었느냐」다.
음식 이름은 그 밤을 부르는 말일 뿐이고, 많이 채우는 것일수록 잘 나온다
(혼자 먹는 밤엔 큰 걸 먹는다).

### ⚠️ 덜 먹는 게임이 아니라 혼자 먹지 않는 게임이다

벌이 「많이 먹어서」로 읽히면 이 시스템은 실패한 것이다. 그래서 깎이는 것의 중심을
체지방이 아니라 **행복**에 뒀다 — 행복이 깎이면 포만감이 더 빨리 줄고, 그러면 또
혼자 먹는다. **그 나선이 이 시스템의 전부다.** 마지막 줄이 그걸 말한다:

> 배는 불렀는데, 채워지진 않았어요.

### 요리사 클레멘이 들어올 자리

지금은 **늘 혼자다.** 그가 들어오면 `checkBinge()` 에 「부엌에 갔었나」 갈래가 붙고,
갔던 밤은 폭식이 아니라 **함께한 식사**가 된다 — 포만감은 차고, 깎이는 것은 없다.
그리고 **폭식해도 그의 호감도는 안 깎는다.** 그것이 그의 이름이다
(`clementia` — 벌할 수 있는데 벌하지 않는 것).

---

## 8. 만든 순서 (그대로 갔다)

1. ✅ **몸의 축을 나눈다** — `S.fit` · `bodyPoint()`. 화면 변화 없음
2. ✅ **포만감 · 스태미나** — 값과 시간 계산, 스탯 표의 「지금 상태」 줄
3. ✅ **운동 팝업** — 종목·시간·정산. 여기서 처음으로 `fit` 과 `grit` 이 오른다
4. ✅ **잡화 탭 · 음식** — 물약 탭이 잡화가 되고 그 안에 물약/음식
5. ✅ **방치 감소** — 제일 마지막에 켰다
6. ✅ **혼자 먹은 밤(폭식)** — `STORY.md` 「폭식 시스템의 판정 기준」. 7-3 참고

### 만들면서 더 붙은 것

- **`S.decayTs`** — 방치 감소를 어디까지 반영했는지. 없으면 부를 때마다 또 깎인다
- **먹으면 절반 넘게 버려질 때는 안 먹는다.** 포만감 98 에서 케이크(+50)를 눌러
  48을 날리는 것이 화면에는 전혀 안 보인다 — 「가득일 때만 막기」로는 못 막는다
- **쉬는 동안 깎였으면 접속할 때 말해 준다.** 조용히 줄어 있는 것이 제일 나쁘다
- 잠긴 시간 칸에 **자물쇠 글자**를 넣는다. 색만으로 알리면 색약 사용자에게 안 전달된다
  (`checkLocked` 가 실제로 잡아냈다)

---

## 9. 검사

```bash
npm run test:time    # 시계를 옮겨 놓고 — 포만감·스태미나·방치 감소·단련·혼자 먹은 밤
npm run test:save    # 새 칸이 없는 옛 세이브가 기본값으로 뜨는지
node tools/checkavatar.js                  # bodyLevel 의 입력이 바뀌었다
FULL=1 node tools/checkui.js showcase      # 「지금 상태」 줄 · 잡화 두 칸 · 운동 팝업
```

**`SAVE_VER` 를 안 올렸다.** 기본값을 바꾼 게 아니라 없던 칸(`fit` `fullness`
`stamina` `bodyTs` `lastWorkoutTs` `decayTs` `foods`)을 더한 것이라,
`Object.assign(defaultState(), 저장값)` 이 기본값을 그대로 남긴다.

**시간에 기대는 것은 전부 `nowDate()` → `Date.now()` 하나를 지난다.** `new Date()` 를
여기저기서 부르면 시계를 옮겨 놓을 구멍이 없어진다 — 실제로 `dayKey()` 가 그래서
밤 판정을 못 재고 있었고, 그 한 곳을 고치니 AP 자정 충전과 리그 주차까지 같이 잴 수
있게 됐다. **시간에 기대는 값을 새로 만들면 `nowDate()` 를 쓸 것.**

**`tools/checktime.js` 가 이 시스템의 핵심 검사다.** 지금 화면만 보면 전부 통과로
보인다 — 「하루 뒤」를 실제로 만들어 봐야 굶었을 때 스태미나가 차 버린다든지,
방치 감소가 부를 때마다 두 번 깎인다든지 하는 것이 드러난다. 페이지 안에서
`Date.now` 를 갈아 끼우므로, 게임 코드가 `Date.now()` 를 지나기만 하면 다 잡힌다.
(셋 다 일부러 되돌려 보고 각각 다른 문구로 잡히는 것을 확인했다)

**FULL 준비에서 근성을 200 으로 잡아 둔 이유** — 전부 열려 있으면 잠금 표현이
검사에서 빠지고, 전부 잠겨 있으면 고른 칸의 대비를 못 잰다. 음식도 세 개 넣는다:
없으면 음식 칸이 아예 안 그려져 검사망에 구멍이 남는다 (CLAUDE.md 의 그 함정이다).

---

## 10. 아직 안 정한 것

- 근성이 열어 주는 것이 **운동 종목뿐인가** — 채집 성공률·AP 상한 같은 것도 볼지
- 요리사 **클레멘**이 들어오면 음식을 그가 준다 (지금은 채집)
- 운동 연출은 물약의 `playSlimFx` 를 그대로 쓴다. 따로 만들지는 정하지 않았다
