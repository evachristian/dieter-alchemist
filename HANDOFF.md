# 새 세션 인수인계

새 작업 세션(로컬이든 클라우드든)을 열었다면 **이 파일과 `CLAUDE.md` 를 먼저 읽는다.**
여기는 "어디서부터 시작하나" 만 적는다. 규칙의 본문은 `CLAUDE.md` 에 있다.

---

## 이 프로젝트가 뭔가

빌드 도구 없는 순수 HTML/CSS/JS 코지 연금술 게임 + 세이브용 Express 서버.
채집으로 재료를 모아 물약을 만들고, 마시면 캐릭터가 날씬해진다.

| 항목 | 값 |
|---|---|
| 저장소 | `evachristian/dieter-alchemist` (**공개**, 기본 브랜치 `main`) |
| 배포 | https://alchemist-production-7583.up.railway.app/ |
| 저장소 종류 | Postgres (`/api/health` 의 `store` 로 확인) |

**모든 코드 주석과 문서는 한국어로 쓴다.**

## 먼저 확인할 것

```bash
git config user.email                                              # 이 저장소만 따로 잡혀 있다
curl -s https://alchemist-production-7583.up.railway.app/api/health # store 가 memory 면 문제다
# ver 도 같이 본다 — index.html 의 캐시 버스터다. 손댄 판과 다르면 **배포가 안 된 것**이다
npm install && npm test
```

`store` 가 `memory(휘발성)` 로 뜨면 **재배포할 때마다 모든 플레이어 세이브가 날아간다.**
`server/README.md` 의 ③-2 (`DATABASE_URL` 참조 변수)를 빠뜨린 상태다.

## 반드시 지킬 것 (본문은 `CLAUDE.md`)

1. **`defaultState()` 기본값을 바꾸면 `SAVE_VER` 를 올리고 `migrate()` 도 같이 쓴다.**
   기본값만 바꾸면 이미 플레이 중인 사람에게는 반영되지 않는다. 이 프로젝트에서 두 번 겪었다
2. **화면을 손댔으면 브라우저 콘솔에서 `await checkUI()` 를 돌린다.** `await` 없이 부르면
   Promise 가 찍혀 0건으로 착각한다. `위반` 이 0건일 때만 커밋한다
3. **잠긴 콘텐츠는 `style.css` 의 공통 블록 하나만 쓴다.** 화면마다 따로 흐리게 만들지 않는다
4. **파일을 고쳤으면 `index.html` 의 `?v=` 캐시 버스터를 일괄로 올린다**
5. **`playerId` 를 다른 것으로 바꾸지 않는다.** 복구 코드가 여기 묶여 있다

정책 문서는 `TEXT_POLICY.md`(글자 색·폰트) 와 `UI_POLICY.md`(탭·가변폭·잠금 표현).
**정책을 바꾸면 `a11y.js` 의 상수도 같이 고친다** — 문서와 검증기는 짝이다.

## 세션을 옮길 때 안 따라오는 것

저장소에 없는 것은 이 셋뿐이고, 셋 다 없어도 작업에 지장이 없다.

- `.claude/settings.local.json` — 권한 허용 목록. 기기마다 달라서 `.gitignore` 에 있다
- 이전 대화 히스토리 — 이 파일과 `CLAUDE.md` 가 대신한다
- Railway 환경변수(`DATABASE_URL`) — Railway 대시보드에만 있다. 저장소에 넣지 않는다

## 창을 못 여는 환경에서 `checkUI()` 돌리기

**클라우드 세션에서도 돌아간다.** 브라우저 창이 없다고 검증기를 건너뛰지 않는다.

```bash
npm start &                                       # 서버가 떠 있어야 한다
node tools/checkui.js showcase atelier gather     # 탭을 옮겨 가며 검사
TUT=1 node tools/checkui.js                      # 튜토리얼 화면 (평소 검사에서 빠진다)
node tools/checktut.js                           # 튜토리얼을 끝까지 눌러 본다
```

`tools/checkui.js` 는 콘솔에서 `await checkUI()` 를 치는 것과 같은 검사를 헤드리스로 돌린다.
`pass` 가 전부 `true` 가 아니면 종료 코드 1 로 떨어진다.

걸리기 쉬운 것 셋:

- **크로미움 경로** — 클라우드 세션에는 `/opt/pw-browsers/chromium` 에 미리 깔려 있고,
  하네스가 있으면 알아서 그걸 쓴다. `playwright install` 을 돌리지 않는다 (받아 오지 못한다)
- **playwright 는 저장소 의존성이 아니다.** 게임 실행에 필요 없는 검사 도구라 넣지 않았다.
  없으면 `npm i -D playwright` 로 그 세션에서만 깔면 된다
- **창 크기를 반드시 준다.** 폭이 0 이면 검증기가 "잴 수 없다" 며 `pass:false` 를 낸다.
  하네스가 1280×900 을 지정하는 이유다 — **그 상태의 0건은 통과가 아니다**

배포본을 상대로 볼 수도 있다: `BASE=https://... node tools/checkui.js showcase`

## 최근에 들어간 것

- **튜토리얼**(`tutorial.js`) — 인트로 다음의 열여덟 단계. 막에 구멍을 뚫어 눌러야 할 곳만
  열어 주고, 마치면 `tutorialDone` 이 켜진다 (3구 무쇠 솥·크리처 탭·옷장 열두 칸이 열린다).
  규칙은 `CLAUDE.md` 의 「튜토리얼」 절. `npm run test:tut` 으로 끝까지 눌러 볼 수 있다
- **마을**(`village.js`·`portrait.js`) — 탐험 › 마을의 지도와 건물 안 NPC 화면.
  여는 조건은 아직 없고 개발용 스위치(전체 / 마을 하나씩)가 유일한 열쇠다
- **몸 비율 — 허리를 20px 올렸다** (`BODY.waistY` 184 → 164). 황금비(상체 1 : 하체 1.618)로
  가려던 자리는 145 인데 갈비뼈 한가운데라 못 쓰고 절충했다. **허리 아래는 전부 같이
  올라간다** — 엉덩이·허벅지·종아리·발·무릎을 k=1.1266 으로 늘렸고 바닥은 그대로라
  다리가 그만큼 길어졌다
- **어깨에서 팔로 파이던 홈을 없앴다** (`BODY.shoulderC`). 어깨 곡선이 팔의 둥근 윗머리보다
  먼저 내려와 **3.75px** 파여 있었다 — 「관절이 분리된 인형」처럼 보이던 자리다.
  어깨를 x 135 까지 평평하게 끌고 가 팔에 넘긴다. **옷의 어깨선도 이제 `shoulderC` 에서
  뽑는다**(`clothTopEdge`) — 손으로 박아 둔 옛 곡선 탓에 터틀넥에 30px 짜리 살색 띠가 떴었다.
  `checkavatar` 의 **「어깨 홈」**이 1px 을 넘기면 실패시킨다
- **어깨가 팔 밖으로 튀어나오던 「부리」도 없앴다.** 몸통·팔 배율을 **따로** 줄여야
  드러나는 자리라 오래 숨어 있었다 — 목의 벌어진 자락(몸통 50% 에서 6px)과
  몸통의 어깨 끝(팔 50% 에서 2.5px). 둘 다 어깨 쪽을 팔에 맞춰 좁혔다
  (`neckShape(…, maxR)` · `shoulderSquash`). **팔을 밖으로 밀면 안 된다** —
  팔은 허리에서 몸통과 1.6px 밖에 안 겹쳐서 곧바로 몸에서 떨어진다.
  `checkavatar` 의 **「어깨 부리」**가 몸통×팔 25조합을 돈다
- **어깨를 사선으로 눕히고 팔을 그 끝에 맞춰 내렸다** (위의 「평평하게 끌고 간다」를
  **대체하는 값이다**). 홈은 없앴지만 어깨선이 자로 그은 듯 평평해서 **어깨와 팔이
  거의 직각**으로 보였다 — 이제 `shoulderC` 가 목(108)에서 어깨 끝(120)까지 승모근처럼
  흘러내리고, 팔은 `armY` 112 → 114 로 내려 걸리고, 마개는 세로로 긴 타원이다
  (`ARM_CAP_H` = 1.5). **셋을 같이 안 고치면 전부 실패한다** — 곡선만 눕히면 「어깨 홈」
  1.5px, 팔을 108 까지 올리면 어깨 위에 혹, 116 까지 내리면 「어깨 부리」 1.75px.
  자세한 것은 `UI_POLICY.md` 9-0.
  같이 고친 것: 몸통 path 의 **왼쪽으로 되돌아오는 자리에 `133` 이 손으로 박혀 있었다** —
  `sc[2][1]` 에서 읽게 바꿨다(안 그러면 왼쪽 어깨만 옛 자리에 남는다). 팔이 짧아진 만큼
  (`armH` 102 → 100, 손목은 214 그대로) 소매 길이표(`SLEEVE_H`)와 손 크기(`HAND_W`)도 맞췄다.
  `checkavatar` 의 **「어깨 마개」**가 팔 위 끝에서 제 폭까지의 세로 길이를 팔 폭과 견준다
  (반원이면 0.88배 · 지금은 1.07배)
- **치마가 팔을 덮던 것을 고쳤다.** 팔은 몸통과 같은 층이라 하의보다 뒤였는데, 치마가
  허리에서 퍼지며 팔뚝과 손을 통째로 덮었다 — 긴 치마에서는 팔이 소매 끝에서 뚝 끊겨
  **겨드랑이에 살색 조각만** 남았다(두 신고가 같은 원인이었다).
  **허리 아래만 옷 위에 다시 찍는다**(`armsOverSkirt`) — 팔 전체를 올리면 맨팔이
  민소매 옷의 어깨끈을 덮는다. 공주 드레스는 종 안에 팔이 들어가 있어 예외다.
  같이 고친 것: **공주 드레스의 소매가 종보다 뒤로 갔다** — 앞에 있어서 손(살색
  동그라미)만 종 위에 동동 떠 있었다. `checkavatar` 의 **「치마가 팔을 덮음」**이 지킨다
  (**뒤로 보낸 것은 그 뒤에 되돌렸다** — 바로 아래 항목)
- **크리처를 재료로 녹인다** (`CREATURE.md` 7단계). **상급 크리처의 조합에 같은 속성의
  중급 크리처가 하나 들어간다** — 중복으로 나온 개체가 처음으로 쓸모를 갖는 자리다.
  넣는 것의 수는 그대로 넷이라 4구 솥을 그대로 쓴다. 무엇이 무엇을 먹는지는
  **손으로 안 적는다** — `gencreature.js` 의 `meltOf()` 가 속성에서 뽑는다.
  · **마지막 한 마리는 못 녹인다**(`meltCount` = 가진 수 − 1). 초안은 「녹이면 로열티도
    사라진다 + 확인 모달」이었는데, **못 하게 막는 편**이 낫다 — 로열티·버프가 날아가거나
    장착이 풀릴 일이 아예 없어진다
  · **조회를 `itemOf(id)` 한 곳으로 모았다.** 예전에는 가방·솥 구멍·레시피 줄이 저마다
    `D.INGREDIENTS[id]` 를 바로 읽어서, 크리처 id 가 섞이면 전부 `undefined` 가 된다.
    개수는 `stockOf`, 소모는 `spendItem`
  · **검사**: `npm run test:melt` 가 진짜 화면에서 담고 조합해 **개체 수까지** 센다.
    `checkdata` 는 크리처 입력을 알아보고 **자기보다 아래 등급인지**까지 본다.
    ⚠️ `checkui` 의 FULL 세이브에 **같은 크리처를 하나 더** 심는다 — 초과분이 없으면
    「녹일 수 있는 크리처」 칩을 한 번도 안 재게 된다
  · 같이 고친 것: 동행 고르기 검사가 **개체 수**로 칸을 세고 있었다(중복이 없던 시절이라
    우연히 맞았다). **종류 수**로 고쳤다
- **손이 팔에서 떨어져 나오던 것을 고쳤다** (`armPoint`). 손자리를 잡는 이 함수가
  `armShift`(몸통 배율만큼 팔을 옆으로 옮기는 값)를 **빼먹고** 있었다 — `armShape`
  는 그만큼 팔을 옮기는데 손만 제자리에 남아, **몸통이 100% 가 아니면 손이 팔에서
  최대 16px 떨어져** 옷 위에 동동 떴다. **100% 에서만 0 이라 기본 체형에서는 안 보여**
  오래 남았다 (몸통 50% 에서 팔과 겹침 0% · 100% 에서 11.5%).
  기준선 **둘 다** 옮겨야 한다 — 팔의 안쪽 변(`xin`)과 어깨 회전축(`pivot`).
  `checkavatar` 의 **「손이 팔에 붙어 있는가」**가 몸통×팔 15조합에서 **마스크 겹침**을
  잰다. 자리를 좌표로 견주지 않는 이유: 겹침으로 재면 **어느 값을 빼먹든** 잡힌다
- **살이 빠지면 옷이 안 맞던 것을 고쳤다 — 특히 어깨.** 옷을 통째로
  `wrapX(tuneMax(몸통·허벅지·종아리))` 한 번으로 늘리고 있었는데, `tuneMax` 가
  **1 을 바닥으로 깔아서** 살을 찌울 때만 따라오고 **뺄 때는 옷만 그대로 남았다**
  (몸통 50% 에서 몸의 허리 15 · 옷 33 · 어깨는 22px 차이). 같은 이유로 반대쪽도
  깨져 있었다 — 배율 하나가 옷 전체에 걸려 **허벅지를 키우면 어깨까지 두 배**가 됐다.
  이제 **좌표마다 제 몸 부위를 따라간다**: 어깨 `clothShoulderK`(squash × 몸통) ·
  허리 `clothWaistHalf`(허리 × 몸통 + 여유) · 엉덩이는 이미 몸을 따라간 값 ·
  **퍼짐만** 다리 배율을 탄다. 기본 그림은 그대로다 (공백 한 줄 빼고 바이트까지 같다).
  **검사가 왜 못 잡았는지**: 커버리지 검사가 **체형(몸무게)만** 돌고 슬라이더는
  한 번도 안 움직였다. 몸통 50/150 · 허벅지 200 · 종아리 200 · 허리 50 · 엉덩이 20 ·
  몸통50+팔50 을 같이 돌게 했다 — **고치기 전 37건**이 잡히고 지금은 0건이다.
  ⚠️ 몸통 창을 잴 때는 **팔·손을 지운다** (배율을 움직이면 팔이 창 안으로 들어온다)
- **공주 드레스를 입으면 손이 사라지던 것을 고쳤다.** 위에서 소매를 종 뒤로 보냈더니
  이번에는 **팔도 손도 통째로 사라져** 삼각뿔 하나가 됐다 — 이 종은 어깨에서 바닥까지
  내려오며 팔보다 **3~14px 밖으로** 퍼져 있어 어느 높이에서도 뒤가 안 비친다.
  다시 앞으로 꺼내되 **색은 드레스 그대로 두고 윤곽선만 두른다**(`armShape` 의 `outline`).
  선 없이 같은 색으로 얹으면 팔은 여전히 안 보이고 손만 보여 「살색 덩어리가 동동 뜬」
  그 모양으로 돌아가고, **한 톤 어둡게 칠하면 소매만 다른 옷**처럼 보인다 (그렇게
  한 번 올렸다가 되돌렸다). ⚠️ 조각마다 선을 그으면 팔꿈치에 가로줄이 그어진다 —
  같은 모양을 선 두께만큼 부풀려 밑에 깔고 그 위에 제 색을 덮는다.
  `checkavatar` 의 **「소매 윤곽선」**이 아랫팔 높이에서 옷보다 어두운 선을 센다
  (정상 6줄 · 지우면 0줄) — **「손이 옷에 안 덮이는가」로는 못 잡는다.**
  **검사 넷이 전부 통과했던 이유**: 손 검사가 죄다 **손만 따로 떼어** 재고 있어서,
  그려 놓고 옷이 덮으면 그대로 통과였다. 손에 `data-part="hand"` 를 붙이고
  **완성된 그림에서 그 자리가 아직 살색인지** 세는 검사를 더했다 (공주 드레스 0% → 89%).
  자세한 것은 `UI_POLICY.md` 의 「레이어 순서」
- **허리 → 엉덩이 → 허벅지를 접선이 이어지게 다시 그렸다.** 엉덩이 path 를 통째로
  `sx(kh,100)` 으로 늘리고 있어서 **이음매 점까지 같이 늘어났다** — 허리를 줄이거나
  엉덩이를 키우면 두 자리에 계단이 생겼다. 이제 세 점을 각자 제 배율로 절대 좌표에
  놓고 **세로 접선**으로 잇는다. 옆선 꺾임 최대 9.1 → 0.75.
  `checkavatar` 의 **「허리↔엉덩이↔허벅지」**가 배율 8조합을 돈다
- **엉덩이가 커질 때 옆으로 뾰족해지던 것도 고쳤다.** 엉덩이(198)에서 허벅지(214)까지
  16px 뿐인데 허리 50% · 엉덩이 150% 이면 가로로 28px 를 떨어뜨려야 해서, 기울기가
  3.1 까지 서 **날개처럼 뾰족**했다. `hipBlendY()` 가 **낙차에 비례해** 아래까지
  내려가 허벅지에 붙는다 (가랑이 선은 그대로 — 내리면 다리 사이가 막힌다).
  **바지·반바지도 같은 값을 본다.** 안 그러면 엉덩이가 바지 옆으로 나온다 —
  허리 아래는 커버리지 검사가 안 보던 자리라 **아무도 못 잡았다.**
  `checkavatar` 의 **「엉덩이가 하의 밖으로」**가 배율×하의 48조합을 돈다
- **엉덩이 슬라이더가 통째로 죽어 있던 것을 고쳤다.** 20%~150% 를 다 밀어도 좌우 폭이
  **38.0~38.6px 로 그대로**였다 — 허벅지 기본 폭을 올리면서(`LEG.hipW` 20 → 36)
  허벅지 윗머리(38)가 엉덩이(34)보다 굵어져, 「엉덩이는 허벅지보다 좁을 수 없다」는
  하한이 **배율을 통째로 삼켰다.** 셋을 같이 고쳤다:
  `BODY.hipHalf` 를 허벅지 윗머리와 같은 **38** 로(기본 그림은 바이트까지 그대로다) ·
  골반이 좁으면 **허벅지 윗머리도 절반만큼 같이 들어오게**(`HIP_PULL` — 무릎이
  허벅지를 따라가는 `KNEE_K` 와 같은 규칙) · 허리 하한은 **허리의 3/4 까지만**
  (`HIP_WAIST_MIN` — 허리 자체를 하한으로 두면 아래쪽 절반이 또 죽는다).
  이제 25.0 → 43.0px 로 움직인다.
  **검사가 왜 못 잡았는지도 같이 고쳤다** — 「200% 두께」가 100%·200% **두 점만** 봤고,
  게다가 엉덩이 슬라이더는 150% 까지라 **닿지도 못하는 자리**를 재고 있었다.
  이름을 「상한 두께」로 바꿔 **부위마다 제 상한에서** 재고(두께 배수는 그대로 1.11배),
  새 **「슬라이더」**가 하한~상한을 여러 칸 훑으며 칸마다 1px·전체 8px 이상 변하는지 본다.
  자세한 것은 `UI_POLICY.md` 7-2
- **엉덩이는 중력으로 처진다.** 가장 넓은 곳(`hipApexY`)이 `hipY` 보다 아래로 내려오고,
  허벅지에 붙는 높이(`hipBlendY`)는 낙차의 2.2배 — 아무리 커도 **무릎 10px 위**다.
  허벅지에 닿는 x 는 **「닿는 높이의」 허벅지 바깥 변보다 1px 안쪽**(`thighJoin`) —
  딱 맞추면 1px 턱이 져 가느다란 선이 보이고, **허벅지가 아래로 가늘어진다는 것을
  빼먹고 「가장 굵은 곳」으로 잡으면 그 사이로 세로 틈이 벌어진다**
  (기본 1.6px · 엉덩이 150% 에서 3.8px). `checkavatar` 의 **「엉덩이↔허벅지 틈」**이 지킨다.
  허벅지와 종아리의 **안쪽 변도 같은 값**이어야 한다 — 1px 어긋나 있어서 무릎에서
  다리 사이 틈이 4.3 → 6.3px 로 벌어졌다
- **다리를 원통에서 곡선으로 바꿨다.** 허벅지·종아리가 네모(rect)를 가로로 늘린 것이라
  굵게 하면 **무릎도 발목도 같이 굵어져 통나무**가 됐다. 이제 마디마다 「살이 붙는 곳」과
  「안 붙는 곳」이 나뉜다 — 허벅지는 위가, 종아리는 장딴지가 부풀고 **무릎·발목은 그대로**다
  (`LEG` · `limbPath`). 관절은 **중심선에서 잰 거리**로 잡는다 — 허벅지와 종아리의
  안쪽 변이 달라서 폭을 맞추면 무릎에서 1px 턱이 진다.
  **안쪽 변은 배율을 안 탄다** — 가운데를 축으로 하던 때는 허벅지 60% 에서 다리 사이 틈이
  4px → 16px 로 벌어졌다. 발·구두는 발목을 따라간다(`footX`).
  `checkavatar` 의 **「다리 사이 틈」**·**「발목」**이 배율 6단계를 돈다
- **팔도 원통에서 곡선으로 바꿨다.** 다리와 같은 규칙 — 살(어깨·팔뚝)은 배율을 타고
  **관절(팔꿈치·손목)은 덜 탄다** (`ARM_W` · `armHalf` · `armSegPath`).
  안쪽 변은 고정이라 굵기가 어떻든 팔이 몸통에서 안 떨어진다.
  손 위치(`armPoint`)도 프로필 중심선을 따라간다
- **방에 둔 크리처는 치마 옆에 세운다.** `left: 6%` 로 두었더니 롱스커트와 땅·공중이
  27px, 어항이 47px 겹쳤다. 크리처는 아바타보다 앞 층이라 치마 위에 올라앉는데,
  그러면 「방에 있는 짐승」이 아니라 **치마에 붙은 무늬**로 읽힌다
  (어항은 속이 비치는 유리통이라 더 나쁘다 — 치마가 통 안으로 들여다보인다).
  **오른쪽 끝을 아우라의 가운데 선에서 재어** 치마 옆선에 맞추고,
  좁은 화면에서는 크기도 같이 줄인다(`--pet`).
  `checkavatar` 의 **「크리처 자리」**가 땅·공중·물 × 하의·원피스 전부를 폭 6가지에서 돈다.
  ⚠️ `checkui` 의 「어항」이 **`.char-aura`(240px) 안에 있는지**로 재고 있었다 —
  거기 가두면 반드시 겹친다. 기준을 **방 그림(`.room-scene`)** 으로 바꿨다
- **배율이 살에 얹히는 정도가 부위마다 다르다**(`TUNE_GAIN` · `fatOf`).
  눈으로 맞추며 여러 번 옮겼다. **허벅지는 기본 폭 자체를 올렸고**(`LEG.hipW` 20 → 36 —
  「예전 150% 가 지금 100%」) 200% 의 두께는 그대로라 gain 을 1.6 → 0.444 로 같이 낮췄다.
  **100% 를 넘는 만큼(k−1)에만** 곱해서 **기본 체형은 한 톨도 안 움직인다.**
  ⚠️ `tuneOf` 를 그냥 바꾸면 안 된다 — 슬라이더 값 자체를 쓰는 곳(`tuneMax` · % 표시)이
  따로 있다. **살의 양을 셈하는 자리에서만** `fatOf` 를 쓴다.
  `checkavatar` 의 **「200% 두께」**가 부위별 배수를 박아 두고 100% 도 같이 본다
- **관절도 살을 타긴 탄다 — 훨씬 덜 탄다**(`KNEE_K`). 무릎을 아예 고정해 두었더니
  허벅지 200% 에서 42 → 17 로 좁아져 깔때기가 됐고, 종아리까지 200% 면 그 아래가 다시
  38 로 부풀어 **무릎만 잘록한 모래시계**가 됐다. 팔의 팔꿈치와 같은 몫(40%)을 태운다.
  **발목은 그대로다** — 「굵은 종아리에 가는 발목」은 일부러 그렇게 잡은 그림이다
- **장딴지가 가장 굵은 자리가 불어나는 양을 따라 내려간다**(`bellyYOf`). 붙박이면
  무릎에서 14px 안에 19px 을 붙여야 해서 **바깥으로 뾰족한 마름모**가 됐다.
  `checkavatar` 의 **「다리 옆선」**이 기울기(2까지)와 무릎이 굵어지는지를 같이 본다
  (2.67 → 1.58 · 무릎 0.75px → 7px)
- ⚠️ **엉덩이 옆선의 제어점 비율(0.45)을 「가로로 많이 가는 마디일수록 작게」 바꿔 보았다가
  되돌렸다.** 비율을 낮추면 마디 가운데의 S 는 펴지는데 **봉우리가 뾰족한 연**이 된다 —
  엉덩이 200% · 허리 50% 에서 더 나빠졌다. 0.45 는 그대로 두는 것이 낫다
- **엉덩이가 허벅지 윗머리를 감싼다** (`hipNeedHalf` · `thighDrawnAt`). 허벅지의 맨 위(186)는
  엉덩이 봉우리(198)보다 12px 위라, 반폭을 같게 맞춰도 그 높이에서는 옆선이 아직 좁아
  **허벅지의 둥근 윗머리가 혹처럼 튀어나왔다** (허벅지 200% 에서 4px).
  옆선을 따라가며 필요한 봉우리 폭을 되풀어 그만큼만 좌우로 퍼뜨린다.
  ⚠️ 덮을 것은 **그려진** 허벅지(둥근 마개 포함)이고, 양 끝은 **B 로** 잘라야 하며
  (높이로 자르면 반폭 70px 짜리 삼각형이 나온다), 얹는 폭에는 상한을 둔다
  (안 두면 옆구리에 선반이 생긴다). 낙차는 **배율이 만든 폭**으로만 잰다 (안 그러면 발산).
  `checkavatar` 의 **「허벅지 윗머리」**가 48조합을 돈다 (4px → 0px)
- **다리 안쪽 변도 곡선이다** (`INNER` · `innerX`). 바깥은 곡선인데 안쪽만 자로 그은
  세로선이라 다리 사이가 칼로 벤 틈처럼 보였다. 엉덩이 밑에서 거의 붙었다가 허벅지
  가운데에서 벌어지고, 무릎에서 모였다가 장딴지에서 좁아지고 발목에서 벌어진다.
  **배율은 안 탄다**(살을 빼도 다리는 안 벌어진다)**·허벅지와 종아리가 같은 함수를 지난다**
  (겹치는 7px 에서 턱이 안 지도록). `checkavatar` 의 **「다리 안쪽 변」**이
  꺾임·곧은 선인지·배율 흔들림 셋을 같이 본다
- **「발목」 검사가 조용히 죽어 있었다.** 고정 x(103)에서 「살인 동안」 밖으로 걸었는데,
  안쪽 변이 곡선이 되면서 그 자리가 다리 사이 틈이 되어 **첫걸음에 멈췄다** —
  모든 배율에서 같은 값을 돌려주어 통과로 보였다. 지금은 가운데에서 나가며
  **살에 들어갔다가 다시 나오는 자리**를 잡는다
- **배율의 하한도 부위마다 잡을 수 있다** (`TUNE_PARTS` 의 `min` · `tuneMinOf`).
  지금은 **엉덩이만 20%** 이고 나머지는 50% 다.
  ⚠️ **다만 엉덩이 슬라이더는 낮은 쪽에서 그림이 안 바뀐다.** 엉덩이 반폭은
  `max(34×배율, 허리반폭×몸통배율, 허벅지 윗머리)` 라, 기본 체형에서는 허벅지(38)가
  하한 노릇을 해서 **112% 아래로는 전부 같은 모양**이다. 엉덩이를 실제로 줄이려면
  허리·허벅지도 같이 줄여야 한다 (허리 50%·허벅지 50% 면 21.75px 까지 내려간다)
- **어깨 마개를 세로로 긴 타원으로 바꿨다**(`ARM_CAP_H`). 반원이면 팔 폭의 절반만에
  가로에서 세로로 돌아 **어깨와 팔이 거의 직각**으로 보였다. 꼭대기는 그대로라 혹은 안 생긴다.
  ⚠️ 팔 위 끝을 어깨선(108)까지 **올려 보았다가 되돌렸다** — 몸통 어깨는 x=133 에서
  이미 113 이라 팔이 어깨 위로 솟는다. ⚠️ `shoulderC` 를 **눕혀 보았다가 되돌렸다** —
  「어깨 홈」이 1.5px 로 한계를 넘는다. `checkavatar` 의 **「어깨 마개」**가 지킨다
  (반원 0.88배 → 지금 1.16배)
- **손을 만들었다** (`handShape`). 그전에는 손이 **아예 없어서** 팔이 손목에서 둥근
  마개로 뚝 끝났다 — 「잘린 것처럼 무섭다」는 신고. 손목보다 조금 넓은 벙어리장갑
  덩어리에 엄지 혹 하나다. 팔의 기울기(어깨+팔꿈치 회전의 합)를 받고 자리는 `armPoint`
  로 잡는다. **장갑도 손을 덮는다.**
  `checkavatar` 의 **「손」**이 손목 밑으로 내려온 길이와 바깥으로 부푼 폭을 잰다
- **팔 50% 에서 「손이 떨어져」 보이던 것을 고쳤다.** 손 크기에 **기본 손목의 0.85 를
  바닥**으로 깔아 뒀는데, 그 바닥은 하필 **팔 50% 에서만** 걸려 손만 33% 부풀었다
  (3.04 → 4.04). 가는 팔은 안쪽 변이 고정이라 **몸 실루엣 안으로 물러나 안 보이는데**
  그 공만 골반 밖으로 튀어나와 **팔에 안 달린 구슬**로 보였다.
  이제 손이 팔을 **따라가되 덜 따라간다**(`HAND_FOLLOW` 0.7 — 무릎·팔꿈치와 같은 규칙)
  그리고 **팔과 같은 그늘**을 손에도 깔아, 몸 위에 얹혀도 손으로 읽힌다.
  ⚠️ **그냥 팔을 그대로 따라가게 하면 안 된다** — 손이 손목보다 0.5px 밖에 안 넓어져
  다시 마개가 된다. 「손」 검사의 부푼 폭에 **위쪽 한계(1.3px)를 더했다** — 한계가
  아래밖에 없어서 「공」을 아무도 못 잡았다 (예전 1.75px)
- **몸통을 가늘게 했을 때 목이 파묻히던 것을 고쳤다** (`NECK_LIFT_TORSO`).
  목 길이는 **몸무게만** 보고 있었는데, 몸통 슬라이더를 줄이면 어깨 곡선이 가로로 눌려
  승모근이 목 바로 옆에서 솟는다 — 길이는 그대로인데 **목이 없어 보였다.**
  체형 축과 같은 방향으로 민다: 몸통 50→150% 에서 13.4 → 11.4 → 9.4 → 9.4px.
  `checkavatar` 의 **「목」**이 이제 **두 축**(체형 · 몸통 배율)을 돌고, 순서뿐 아니라
  **50% 가 150% 보다 2px 이상 긴지**도 본다 — 순서만 보면 전부 같은 값이어도 통과다
- **팔꿈치 이음매는 관절에 놓은 원 하나**로 메운다. 마디 둘을 그냥 맞대면 아랫팔이
  굽는 만큼 윗변이 기울어 쐐기 틈이 남는데, 예전에는 **윗팔을 고정된 반폭만큼 아래로
  늘려** 덮었다 — **그 조각은 굽힘을 안 타서** 팔꿈치 바깥에 네모난 턱이 삐져나왔다
  (가는 팔일수록 심했다). 회전축이 관절 한가운데라 **그 점을 중심으로 한 원은 굽혀도
  제자리다.** `checkavatar` 의 **「팔꿈치 턱」**이 팔 배율 5가지 × 좌우를 돈다
  (1.25px → 0.25px)
- **몸통과 팔을 그늘로 가른다** (`ARM_SHADE`). 같은 살색이라 겹치면 팔이 사라지는데,
  테두리는 어깨→팔 선을 끊어서 못 쓴다. **같은 모양을 몸 쪽으로 2.5px 밀어 한 단
  어둡게** 깔면 팔에 가려 안쪽 겨드랑이 선만 그늘로 남는다. 소매도 같이 따라온다
- **「어깨 부리」 검사를 다시 썼다.** 옆선이 되돌아오는지로 재면 **팔이 팔꿈치로
  가늘어지는 것까지** 부리로 잡는다 (팔 200% 에서 3px 헛나왔다).
  이제 **몸통·목과 팔을 따로 그려 같은 높이에서 견준다** — 이것이 진짜 규칙이다
- **하의 옆선은 몸의 엉덩이 곡선을 그대로 쓰고, 밑단에서 자른다**(`hipSideCurve`).
  밑단까지 억지로 눌러 담으면 하의가 몸보다 가팔라져 그 사이로 엉덩이가 비쳤다 (반바지)
- **`ART_POLICY.md`** — 그림의 결(듀오링고풍 플랫 2D 벡터)과 생성 프롬프트

## 남은 작업

`CLAUDE.md` 의 **"아직 안 한 것"** 절에 있다. 요약하면:

- 마을을 여는 조건 (`isVillageOpen()` 한 곳만 고치면 된다)
- 아우라 중 우아함·근성·개성·행운이 수치만 오르고 효과가 연결돼 있지 않다
- 이메일·소셜 로그인 (기존 `playerId` 를 계정에 연결만 하면 진행이 그대로 넘어간다)
- 리그(주간 랭킹)의 상대가 아직 NPC 다 — 서버가 주간 점수·조 편성을 들고 있어야 한다
- 이모지 아트 → 실제 일러스트 교체
