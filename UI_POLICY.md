# UI 컴포넌트 정책

컴포넌트를 새로 만들 때 지킬 규칙. 글자 색·폰트는 [TEXT_POLICY.md](TEXT_POLICY.md) 참고.

검증은 브라우저 콘솔에서 `checkUI()` — 모든 언어 × 라벨 2배 확대로
대비 · 레이아웃 · 잠금 표현을 한 번에 본다. 개별로는 `checkLayout()` `checkLocked()`.

---

## 1. 카테고리 탭 (`.cat-tabs` / `.cat-tab`)

가로로 늘어놓는 탭 줄. 현재 쓰는 곳:

| 위치 | 컨테이너 | 항목 |
|---|---|---|
| 마이 룸 › 옷 › 슬롯 탭 | `.cat-tabs.wr-tabs` | 헤어 / 헤어컬러 / 표정 / 문신 / 상의 / 하의 / 원피스 / 서클렛 / 귀걸이 / 목걸이 |
| 공방 › 레시피 북 › 등급 | `.cat-tabs.rb-tabs` | 기초 / 하급 / 중급 / 상급 (물약·크리처 공통) |

### 규칙

1. **여러 줄로 접힌다.** `flex-wrap: wrap`. 항목이 늘면 2줄·3줄·4줄로 내려간다.
   **가로 스크롤이 아니다** — 화면 밖에 숨는 탭이 없어야 한다.

2. 그래서 스크롤 장치(휠·끌기 핸들러, 선택 탭 정렬)는 **없다.**
   예전에는 한 줄 가로 스크롤이라 그것들이 필요했지만, 줄바꿈으로 바뀌며 전부 지웠다.
   다시 스크롤로 되돌린다면 이 문서와 `a11y.js` 의 `scrollerSelector` 를 같이 되돌려야 한다.

3. **넘치면 버그다.** `a11y.js` 의 `scrollerSelector` 는 비어 있다 —
   예전에는 `.cat-tabs` 를 '넘쳐도 되는 곳' 으로 빼 뒀는데, 줄바꿈이 된 지금은
   그 예외가 진짜 넘침을 가려 버린다. `checkLayout()` 이 그대로 잡게 둔다.

4. 상태별 스타일
   | 상태 | 스타일 |
   |---|---|
   | 기본 | `background: var(--cream)` · `color: var(--ink-soft)` |
   | 선택 | `background: var(--pink-2)` · `color: var(--ink)` — 흰 글씨 금지 |
   | 비활성 | `.dim` — `color: var(--ink-dim)` · `opacity: 0.7` |

### 새로 만들 때

```html
<div id="myTabs" class="cat-tabs"></div>
```
```js
el.innerHTML = ITEMS.map(x =>
  `<button class="cat-tab ${cur === x.id ? 'active' : ''}" onclick="pick('${x.id}')">${x.label}</button>`
).join('');
// 줄바꿈이라 따로 붙일 것이 없다. 항목이 늘면 알아서 다음 줄로 내려간다.
function pick(id) { cur = id; render(); }
```

여백만 다르면 `.cat-tabs` 옆에 전용 클래스를 하나 더 붙여 margin 만 지정한다 (`.wr-tabs`, `.rb-tabs` 처럼).

> **탭이 3~4개로 고정이라면** 줄바꿈 줄이 아니라 균등 분할 행
> (마이 룸의 `.room-tabs` — 옷/물약/크리처) 을 쓴다.

### 두 단으로 나누기 — 종류 먼저, 등급 다음

레시피 북은 **물약/크리처(균등 분할 행) → 등급(줄바꿈 칩)** 두 단이다.
한 줄에 다섯 개를 늘어놓았더니 무엇이 무엇의 하위인지 읽히지 않았다.

- 윗단은 마이 룸 인벤토리와 같은 `.room-tabs` 모양이고 라벨도 같은 개념을 쓴다
- 아랫단 칩 라벨은 **등급 + 종류**로 짓는다 (`기초 물약` · `기초 크리처`) —
  윗단에서 종류를 이미 골랐어도, 칩만 보고도 무엇의 등급인지 알 수 있어야 한다
- 진행도(`5 / 6`)는 제목이 아니라 **등급 칩 줄 오른쪽 끝**에 둔다.
  그 수는 '지금 보고 있는 등급' 의 진행이라 등급 옆에 있어야 뜻이 맞는다
- **등급 칸은 늘 네 개다.** 그 등급의 레시피가 아직 없으면 칩을 감추지 말고
  빈 안내(`empty_grade`)를 띄운다 — 칸이 들쭉날쭉하면 어디까지 있는지 알 수 없다

탐험도 같은 두 단이다: **필드/마을/밭(`.gt-tabs`, 균등 분할 행) → 지대·마을 이름(줄바꿈 칩)**.
⚠️ 윗단이 **세 칸**이 되면서 265px 영어가 훨씬 빡빡해졌다 — 칸을 늘리거나 라벨을 바꾸면
`checkui` 의 `gather/밭탭` 이 재는 「탭 줄이 한 줄에 들어가는가」를 반드시 다시 볼 것.
(밭 탭은 여신 단계 전에는 `hidden` 이라 두 칸이다)
윗단이 바뀌면 화면 안내 문구(`.screen-sub`)도 같이 바뀐다 — 필드는 채집 요령,
마을은 아직 열리지 않았다는 안내다. 갈래마다 할 일이 다른데 안내가 하나면 한쪽은 거짓말이 된다.

> **안 보는 쪽은 `display:none` 이라 검사에서 통째로 빠진다.**
> 두 단으로 나눌 때마다 `tools/checkui.js` 가 `FULL=1` 에서 양쪽을 다 돌게 해야 한다
> (마이 룸 하위 탭 셋, 탐험 갈래 둘이 그렇게 들어가 있다).

### 잠긴 채로 '지금 보고 있는' 탭 (`.cat-tab.locked.active`)

마을은 **잠겨 있어도 눌러서 볼 수 있다.** 지대·솥은 잠긴 것을 고를 수 없어서
`.locked` 와 `.active` 가 같이 붙는 경우가 없었는데, 마을에서 처음 생겼다.

- 두 규칙이 부딪힌다: `.active` 는 배경을 분홍(`--pink-2`)으로 칠하고,
  `.locked` 는 채도를 걷어낸다 → **글자 대비가 3.50:1 로 떨어진다** (checkUI 가 잡았다)
- 그래서 잠긴 팔레트(크림)를 지키고, **고른 표시는 테두리로** 한다
  (`box-shadow: inset 0 0 0 2px var(--ink-soft)`)
- 테두리 색을 `--ink-soft` 로 잡는 이유: `saturate(0.15)` 를 지나도 회색으로 남아 보인다.
  분홍 테두리는 채도가 걷히면 배경과 구별되지 않는다

**잠긴 것을 눌러 볼 수 있게 할지는 그 화면에 할 일이 있느냐로 정한다.**
지대는 탭을 옮기면 아래에 채집할 목록이 뜨므로 막고 조건만 알린다.
마을은 아직 할 것이 없어 읽어 보는 것 말고 할 일이 없으므로 막지 않는다.

### 마을 지도 (`.vil-map` · `.vil-pin`)

마을 갈래는 **탭이 곧 마을 전환**이다. 목록 → 들어가기 → 뒤로 하는 단을 두지 않는다 —
마을이 셋뿐이고 탭 줄이 그 셋을 다 보여 주므로, 한 단을 더 두면 같은 것을 두 번 고르게 된다.

- **지도는 화면 끝까지 나간다** (`margin: 0 -16px`). 지도는 카드가 아니라 창밖 풍경이라
  여백 안에 갇히면 작아 보인다
- **건물 이름표는 명판(`.vil-pin`)** 이다. 그림 위 흰 글자(`.on-room-bg`)로 하지 않는다 —
  배경이 있는 딱지라야 **대비를 실제로 잴 수 있다.** 예외는 최소한만 쓴다
- **명판은 좌우 끝에 붙인다.** 가운데에 띄우면 글자가 길어질 때(영어·2배 확대)
  지도 밖으로 밀려난다. 끝에 붙여 두면 늘어날 방향이 안쪽 하나뿐이라 안전하다
- **마을 설명은 지도에 겹쳐 놓지 않는다.** 겹쳐 두었더니 265px 거울 골짜기에서
  첫 명판과 부딪혔다 — 지도 **바로 위**에 띠로 두면 길이가 어떻든 겹칠 수가 없다
- **건물은 2열로 놓고, 지도 높이는 CSS 가 정한다.**
  한 줄에 하나씩 놓았더니 여덟 채가 세로로 늘어져 한 화면에 안 들어왔다 —
  **지도를 보려고 스크롤하는 것은 지도가 아니다.**
  그리고 높이를 SVG 비율에 맡기면 폭이 넓을수록 세로도 길어진다(390px 화면에서 576px).
  `.vil-map` 에 `height: clamp(...)` 를 주고 SVG 가 `preserveAspectRatio="none"` 으로 채운다 —
  viewBox 의 세로는 **그리기 좌표계일 뿐** 화면 높이가 아니다
- **이름은 두 줄까지 접고 그 이상은 자른다.** 한 줄이면 긴 이름이 다 잘리고,
  안 자르면 2배 확대에서 명판이 세로로 자라 아래 줄과 겹친다. 높이가 갇혀야 겹칠 수가 없다
- 건물 자리(x/y)는 **데이터 한 곳**에 있고 그림과 명판이 같은 좌표를 본다 — 어긋날 수가 없다

> ⚠️ **명판끼리 겹치는 것은 `checkUI` 가 못 본다.** 부모 밖으로 나가는 것이 아니라
> 형제끼리 포개지는 것이라 넘침 검사에 안 걸린다. 앱에서 유일하게 절대 위치로 놓는
> 화면이라, `tools/checkui.js` 가 마을에서만 **띠끼리 겹치는지 따로 본다.**

### 건물 안 — NPC 화면 (`.npc-*`)

장소 이름 → 대사 → 그림 + 버튼. 사람은 아직 없고 **자리만 잡아 둔 화면**이다.

> **오른쪽 끝은 언제나 「대화」다.**
> **「거래」가 없는 곳이 있다** (예: 병영에서 만난 왕자). 그때 「대화」가 가운데로 옮겨 가면
> **오른쪽을 눌렀을 때 화면마다 다른 것이 눌린다** — 손이 가는 곳이 바뀌면 안 된다.
>
> 그래서 버튼은 `flex: 1 1 0` 으로 **줄을 꽉 채워 나눠 갖는다.**
> 혼자면 줄 전체, 둘이면 반씩. 빈자리도 안 생기고 오른쪽 끝도 안 움직인다.
> `tools/checkui.js` 가 거래 있는 자리와 없는 자리를 둘 다 열어 **오른쪽 끝을 확인한다.**

- **「거래」는 기본으로 있고, 없는 곳만 데이터에서 뺀다** (`spot.trade === false`).
  있는 쪽이 훨씬 많을 것이라 없는 쪽을 적는 편이 표가 짧다
- **대사는 그림에 겹쳐 놓지 않는다.** 겹치면 대사가 길어질 때(영어·2배 확대)
  그림과 버튼을 덮는다 — 마을 설명에서 같은 문제를 이미 겪었다

---

## 1-2. 스페셜 맵 배지 (`.spot-badge`)

미니게임이 있는 맵(`map.mini`)은 카드 **왼쪽 위 모서리**에 "스페셜 맵" 배지를 단다.

- 배지를 다는 카드는 `.spot-card.special` 이 붙어 `position: relative` 가 된다
- 배지는 `position: absolute; left:-4px; top:-8px` — 카드 모서리에 걸치게 얹는다
- **`white-space: nowrap` 을 뺄 수 없다.** 두 줄로 접히면 카드 위로 삐져나온다
- 판정 기준은 데이터의 `mini` 키다. 화면에서 맵 id 를 하드코딩하지 않는다
- **글자는 진한 갈색(`#6b3d10`), 배경은 단색 주황(`#ffbc78`) — 5.53:1.**
  처음에는 흰 글자에 주황 그라데이션이었는데, 흰 글자는 어떤 주황에도 2.4:1 을 못 넘는다.
  그라데이션도 쓰지 않는다 — 검증기가 배경을 못 읽어 흰색으로 **추정**하면 실측 흰색과
  구분이 안 돼 오진의 빌미가 된다

이 배지는 **오래 검사망 밖에 있었다.** 배지가 붙는 맵(파수꾼의 호박 밭)이 해금 20점이라
새 세이브 화면에는 아예 그려지지 않았고, 그래서 `checkUI()` 가 볼 일이 없었다.
개발용 '모든 맵 오픈' 스위치를 켜고서야 대비 1.00 · 10.2px 두 건이 잡혔다.
**잠겨 있어 안 보이는 UI 도 언젠가는 보인다** — 스위치를 켜 놓고도 한 번 재라.

## 1-3. 한 덩어리로 묶기 (`.room-inv` · `.room-stats` · `.at-block`)

서로 관련된 것들이 낱개 카드로 흩어져 있으면 무엇이 한 묶음인지 안 보인다.
마이 룸의 **인벤토리**(탭·목록·안내)와 **능력치**(요약·상세·기록·과시)가 그래서 묶여 있다.

- **바깥 카드 하나만 그림자를 갖는다.** 안쪽 상자들은 `box-shadow` 를 빼고
  배경색(`--cream`)만으로 구분한다. 그림자가 겹치면 아무리 붙여 놔도 낱개로 읽힌다
- 바깥은 `background: var(--card); border-radius: var(--radius); box-shadow: var(--shadow); padding: 10px`
- 안쪽 상자는 `border-radius: 16px` — 바깥(20px)보다 작아야 겹쳐 보이지 않는다
- **안쪽 배경을 `--cream` 으로 바꾸면 `--cream` 짜리 구분선은 사라진다.**
  그런 선은 `rgba(154,138,149,0.25)` 처럼 배경보다 진한 것으로 바꾼다
- 묶음끼리는 같은 폭이어야 한다 (`.room-inv` 와 `.room-stats` 는 폭이 같다)
- **inset 그림자는 남긴다.** 바깥 그림자만 걷는 것이다 — 공방 레시피 줄의 왼쪽 초록 띠는
  `inset` 이라 '재료가 다 있다' 는 신호를 그대로 유지한다. 검사할 때도 `inset` 은 세지 않는다

쓰는 곳: 마이 룸 `.room-inv`(인벤토리) · `.room-stats`(능력치),
공방 `.at-block`(채집 가방 · 레시피 북) · `.dev-tools`(개발용)

### 지금 상태 (`.now-row`) — 포만감 · 스태미나

**비주얼/아우라 표와 성격이 다르다.** 저 둘은 **쌓이는** 값이고, 이 둘은 **차고 주는**
값이다(AP 와 같은 부류). 같은 표에 섞으면 「체지방률 24%」 옆에 「포만감 78」이
나란히 서서, 무엇이 쌓이는 값이고 무엇이 지금 상태인지 안 읽힌다.

- 스탯 표 **맨 위**에 두 칸으로 둔다 (`.stat-row` 를 그대로 쓴다 — 새 컴포넌트가 아니다)
- **접으면 같이 감춘다.** 접기의 규칙은 「단계와 매력 총합만」이다.
  대신 운동 팝업에는 **항상 크게** 나온다 — 접어 둔 사람에게는 거기가 유일한 자리다
- 눌러서 설명이 뜬다. 숫자만으로는 처음 보는 사람이 무엇인지 모른다

### 방 안에서 하는 일 (`.room-acts`)

🏃 운동처럼 **아바타에게 시키는 일**은 방 그림 오른쪽 아래에 쌓는다 (시계와 대칭).

- **세로를 한 픽셀도 안 먹는다.** 스탯을 접어 자리를 만든 화면에 가로 줄을 하나 더
  얹으면 그 작업이 무의미해진다
- **명판이다.** 방 배경 위 흰 글자(`.on-room-bg`)는 대비를 잴 수 없어 예외로 빼야
  하는데, 예외는 최소한만 쓴다 — 배경이 있는 알약이라야 대비를 실제로 잰다
  (마을 이름표와 같은 판단)
- 탭 줄에 넣지 않는다: **탭은 「보는 곳」이지 「하는 일」이 아니다.**
  스탯 덩어리 안에도 넣지 않는다 — 접으면 사라진다

**알림은 점이 아니라 숫자다** (`.act-badge`). 「🍗 흡입」에 안 본 밤이 몇 번인지 적는다 —
레드닷 하나로는 「하나 있나 보다」로 읽혀서, 세 밤이 쌓인 것을 못 알린다.
뱃지가 버튼 밖으로 삐져나오므로 `.room-acts` 의 오른쪽 여백을 그만큼 준다
(안 그러면 뱃지가 방 그림 밖으로 나간다).

> ⚠️ **`display` 를 주는 규칙에는 `[hidden]` 짝을 반드시 같이 쓴다.**
> 브라우저 기본 `[hidden]{display:none}` 은 사용자 에이전트 스타일이라
> **작성자 스타일에 무조건 진다.** `.act-badge{display:flex}` 하나 때문에
> `el.hidden = true` 를 해 놓고도 볼 것이 없는데 빨간 「0」이 붙어 있었다.
> 이 프로젝트에서 **두 번째**다 (먼저는 랭킹 탭이 '새싹' 단계에서 보였다).
>
> 이제 `checkLayout()` 이 **화면의 모든 `[hidden]` 요소**를 훑어, 속성은 켜져 있는데
> 실제로 그려진 것이 있으면 「숨김실패」로 잡는다 — 속성이 아니라 **그려진 크기**로 본다.

### 화면을 통째로 가져가는 장면 (`.binge-scene`)

「혼자 먹은 밤」처럼 **그림이 곧 내용**인 것은 토스트로 흘려보내지 않는다.
모달(50)·튜토리얼 막(40)보다 위(9400)에 뜨고, **나가는 길은 아래 버튼 하나**다
(키보드에서는 Esc). 오른쪽 위에 `✕ 닫기` 딱지를 뒀다가 뺐다 —
그림이 화면의 전부인 장면이라 흰 딱지가 눈에 먼저 걸렸다.

- 어두운 바탕이라 이 화면만 글자 색 규칙이 다르다 — 흰 글자에 어두운 판이다.
  `checkUI` 로 반드시 재고 넘어간다 (평소에는 `display:none` 이라 검사에서 빠진다)
- 볼 것이 여럿이면 **먼저 물어본다.** 예고 없이 이어지면 갇힌 느낌이 든다

### 인물 초상화 — 얼굴 뒤를 먼저 채운다 (`portrait.js`)

머리 부품은 안쪽을 파 놓은 띠(crescent)다. 얼굴 타원(`cx60 cy66 rx26 ry30`)보다
넓은 자리에서는 그 사이로 배경이 비쳐 **머리와 얼굴이 떨어져 보였다** —
관자놀이에서 10px 짜리 틈이 났고, 여섯 명이 그랬다.

그래서 머리 부품을 그리기 **전에** `HAIR_FILL` 로 얼굴 뒤를 한 번 채운다.
이 덩어리는 **모든 머리 모양의 실루엣 안쪽**에 들어가도록 잡혀 있어서, 새 머리를
늘려도 그대로 쓴다 — 그 부품이 안쪽을 얼마나 파든 상관없다.

`tools/checkavatar.js` 가 인물 전부를 **배경 판 없이(bare)** 그려서, 얼굴이 있는
가로 구간에서 「머리 → 얼굴」이 끊기지 않는지 본다. 판을 깔면 틈이 판 색으로 메워져
안 보이므로 반드시 bare 로 잰다.

#### 헤어라인 — 붙어 있다고 맞는 얼굴이 아니다

**앞머리(`front`)의 안쪽 선이 곧 헤어라인이다.** 예전에는 그 선이 `y=34` 라
얼굴 위끝(36)에 거의 안 걸쳐 있었다. 틈 검사는 통과했다 — 붙어 있기는 했으니까 —
그런데 얼굴의 45%가 맨이마라 **둥근 민머리처럼** 보였다.
「이마가 너무 크고 둥그렇게 보인다」는 말을 두 번 듣고서야 알았다.

눈이 `y=66`(위끝 60.8)이므로 헤어라인은 **얼굴 위끝과 눈 사이**에 와야 한다.
지금은 여덟 인물이 전부 이마 9.8~11.0px(얼굴 높이의 16~18%)이다.
너무 높으면 민머리, 너무 낮으면 헬멧이다 — 처음 고칠 때 물결머리 하나가
반대로 `y=56` 까지 내려가 눈썹에 닿았다.

`checkavatar.js` 가 **그린 path 를 직접 더듬어**(`isPointInFill`) 이마를 재고
6~18px 밖이면 잡는다. `HAIR` 표를 읽지 않는 이유는, 표만 고치고 그림을 안 고쳐도
통과해 버리기 때문이다.

**머리 장식도 헤어라인을 따라간다.** 서클렛을 정수리 쪽(`y=27`)에 둔 채 헤어라인만
내렸더니 발렌이 **노란 헬멧을 쓴 것처럼** 됐다. 지금은 헤어라인 바로 위(`y=46`)에 앉는다.

### 스탯 간단히 / 자세히 (`.room-stats.lite`)

능력치 덩어리는 세로를 많이 먹는데, 늘 보고 싶은 것은 **단계와 매력 총합** 둘뿐이다.
접으면 그 둘만 남고 신체·아우라·기록·과시가 통째로 사라진다 (390px 화면에서 334px 줄어든다).

- **접었을 때는 카드도 상자도 없다.** 배경·그림자·안쪽 상자를 전부 걷고 글자만 앱 배경 위에
  바로 앉힌다 — 테두리를 남기면 「접었는데도 여전히 덩어리」로 보여서 접은 보람이 없다
- **그 자리는 방 배경이 이어받는다.** 카드를 걷으면 앱 배경(그러데이션)이 그대로 드러나
  글자만 허공에 뜬다. 방 그림을 `ROOM_BLEED`(74px)만큼 아래로 늘여 글자가 방 바닥에 앉게 하고,
  글자는 그림 위 글자의 공통 규칙 `.on-room-bg`(흰 글자 + 퍼지는 음영)를 쓴다 —
  `applyStatsView()` 가 `#roomStats` 에 그 클래스를 붙인다.
  **CSS 로 상자만 늘이면 안 된다**: 방 그림은 `preserveAspectRatio="…slice"` 라
  상자가 세로로 길어지면 확대율이 올라가 **좌우가 잘린다**(창문·선반이 화면 밖으로 나간다).
  늘일 픽셀을 viewBox 단위로 환산해(`roomPadBottom`) `Avatar.roomScene(lv, extra, padBottom)` 에
  넘기면 **바닥을 진짜로 더 그린다** — 원근선과 창빛도 기울기를 그대로 이어 연장하므로 이음매가 없다.
  환산은 `늘일 px ÷ 확대율` 이다. 상자 높이로 되짚어 계산하면 **폭이 넓은 화면에서만**
  그림이 40px 쯤 아래로 미끄러진다 (좁은 화면에서는 멀쩡해서 눈으로는 안 잡힌다).
  `FULL=1 node tools/checkui.js showcase` 의 「방 배경 늘이기」가 확대율·좌우 잘림·세로 위치
  셋을 접기 전후로 견준다
- **버튼 글자는 「하면 일어날 일」을 적는다.** 접혀 있으면 `스탯 자세히 보기`,
  펼쳐져 있으면 `스탯 간단히 보기`. 화살표는 여는 쪽을 가리킨다 (`▾` / `▴`)
- **세이브에 넣지 않는다.** 진행이 아니라 이 기기에서 화면을 어떻게 볼지의 문제다 —
  개발용 블록의 접힘 상태와 같이 `localStorage` 에 둔다
- **검사는 두 상태를 다 잰다.** 접으면 글자가 카드 밖으로 나오므로 배경이 통째로 달라진다 —
  한쪽만 재면 다른 쪽은 검사에서 빠진다 (`FULL=1 node tools/checkui.js showcase`)
- **새로고침을 견디는지도 두 방향 다 본다.** 접힘만 확인하면
  「값이 있으면 무조건 접힘」 같은 구현이 통과해 버린다 — 그러면 **펼침을 저장하는 쪽이
  조용히 깨진다.** 같은 이유로 「기억하지 않고 그냥 변수」인 구현은 접힘 쪽에서만 잡힌다.
  둘 다 봐야 둘 다 잡힌다

---

## 1-3-2. '가진 것' 목록은 같은 네모 칸

채집 가방(`.ing-chip`) · 물약 선반(`.potion-card`) · 크리처(`.creature-card`) 는
**같은 모양**이다 — 3열 격자, 둥근 네모, 가운데 큰 그림, 아래 이름, **오른쪽 위에 개수**.

- 가방은 예전에 폭이 제각각인 알약 칩이 흘러가는 모양이었다. 25종만 넘어가도
  줄이 들쭉날쭉해 어디까지 봤는지 놓치기 쉬웠다
- 개수 색은 `--text-pink`. **`--pink-2` 는 글자색으로 쓰지 않는다** (크림 위 1.4:1)
- 이 칸들은 **가진 것이 있어야 그려진다** → 검사할 때 `FULL=1` 을 같이 쓴다 (2장 검증)
- 영원 염색약(`.dye-item`)도 같은 칸이다 — 그림 자리에 색 조각(`.dye-sw`)이 들어간다

## 1-3-3. 공방 — 레시피 줄과 재료 구멍의 상태

레시피 줄은 **세 가지**이고, 서로 달라 보여야 한다. 누르면 셋 다 무엇이 문제인지 알려 준다.

| 상태 | 모양 | 누르면 |
|---|---|---|
| 만들 수 있음 | 왼쪽 민트 띠 (`inset` 그림자) | 솥에 담긴다 |
| **고른 것** (`.on`) | 분홍 테두리 + 연분홍 바탕 + `✓` | (담긴 그대로) |
| 재료 부족 (`.short`) | 크림 배경 + `saturate(0.15)` | `채집 재료가 충분하지 않아요.` |
| 아직 모름 (`.locked`) | 연회색 배경, 물음표 | `아직 알 수 없는 레시피네요.` |

- **'못 만드는 것'과 '모르는 것'은 다르다.** 배경을 같게 두면 구분이 안 된다
- 모르는 줄의 **물음표 개수 = 재료 가짓수**. 몇 가지가 드는지는 알려 줘도 되는 정보이고,
  그래야 어떤 솥이 필요한지 가늠할 수 있다
- 채도만 낮춘다 — 명도가 그대로라 글자 대비가 떨어지지 않는다 (CLAUDE.md 4번)
- **고른 줄의 표시는 옷장·컬러칩과 같은 언어**다 (`--pink-2` 테두리 + 연분홍 바탕).
  모든 줄에 미리 투명 테두리를 줘 고를 때 줄 높이가 흔들리지 않게 한다.
  판정은 재료 목록(`D.recipeKey`)으로 한다 — 같은 조합의 레시피는 둘 있을 수 없다
- **줄여도 되는 쪽은 왼쪽 재료다.** `.recipe-in` 이 `flex: 1 1 auto; min-width: 0` 으로
  여러 줄로 접히고, 오른쪽 이름(`.recipe-out`)은 `nowrap` 으로 한 줄을 지킨다.
  재료가 12가지까지 늘면서 이름이 '사막의 / 별' 로 쪼개졌던 것을 이렇게 되돌렸다
  (20줄 전부 두 줄 → 0줄). 이름도 무한정 커지지 않게 `max-width: 52%` + 말줄임을 둔다
- **재료 이모지는 낱개로 눌린다** (`.ing-dot`) — 누르면 그 자리 위에 이름이 뜬다.
  줄 자체도 누르는 것이라 `event.stopPropagation()` 으로 갈라 놓는다.
  버튼은 inline-block 이라 제 콘텐츠 폭만큼 커지므로 `max-width: 100%` 로 막는다

재료 구멍(`.c-slot`)도 세 가지다.

| 상태 | 모양 | 누르면 |
|---|---|---|
| 담김 (`.filled`) | 민트 + `✕` 배지 | 뺀다 |
| 모자람 (`.lack`) | 회색 재료, **`✕` 없음** | `채집 재료가 부족해요.` |
| 빈칸 (`.empty`) | 점선 `+` | (없음) |

- `.lack` 은 **고른 레시피가 요구하는데 가방에 없는 재료**다. 뺄 것이 없으므로 `✕` 를 달지 않는다
- 구멍 번호와 `S.cauldron` 의 순서는 **다르다** (회색 자리가 사이에 낀다).
  뺄 때는 그 구멍 앞의 '실제로 담긴 것' 수를 세어서 지운다

## 1-3-6. 탐험 카드 (`.spot-card`) — **AP 를 쓰는 곳은 한 군데뿐이다**

| 누르는 곳 | 하는 일 |
|---|---|
| `.btn-gather` ('채집 ⚡N') | 톡 = 한 번 채집 / 꾹 = 연속 채집. **AP 를 쓰는 유일한 자리** |
| `.spot-chip` (재료 이모지) | 재료 이름 토스트 |
| 카드 몸통 | **아무것도 안 한다** (잠긴 카드만 예외 — 눌러서 해금 조건을 듣는다) |

### 지킬 것

- **AP 를 쓰는 조작은 눈에 보이는 버튼 안에만 둔다.** 예전에는 카드 아무 데나 눌러도
  채집돼서, 재료 이름을 보려다 · 목록을 밀어 내리려다 AP 가 샜다.
  안쪽 버튼마다 `stopPropagation` 을 다는 것으로 막으려 했지만, 그건 **막을 구멍이
  하나 늘 때마다 다시 막아야 하는** 방식이다. 카드가 애초에 안 받게 하는 편이 낫다
- 그래서 손가락을 부르는 표시(`cursor: pointer` · 눌림 효과)도 **버튼에만** 둔다.
  카드는 `cursor: default`, 잠긴 카드만 `pointer`
- 버튼은 **위아래로 46px** — 손끝이 덮는 크기(대략 44px)를 밑돌지 않게
- `.btn-gather` 는 카드와 같은 `touch-action: pan-y` 를 갖는다. 안 주면 버튼 위에서
  시작한 손가락으로 목록을 밀어 내릴 수 없다
- **채집 결과 토스트는 누른 버튼 옆에 띄운다**(`place: 'above'`). 화면 아래 기본 자리에
  뜨면 목록을 한참 내려온 상태에서 방금 누른 곳과 너무 멀어 눈이 따라가지 못한다.
  요소가 아니라 **선택자**를 넘긴다 — 바로 뒤의 `render()` 가 카드를 새로 그려서
  그 버튼은 문서에서 떨어져 나가고, 좌표가 0,0 이 되어 왼쪽 위 구석에 뜬다
- 이모지 칸도 `max-width: 100%` · `min-width: 0` · `overflow: hidden` — `.ing-dot` 과 같은 이유
- 특별한 재료 칸의 간격은 `margin` 이 아니라 부모의 `gap` 이 준다.
  `margin` 을 더하면 `max-width: 100%` 를 지켜도 그만큼 부모 밖으로 나간다 (2배 라벨에서 10px)
- `.spot-emoji` 에는 폭 상한이 있다. `flex: none` 인 채로 두면 라벨 2배 검사에서
  106px 까지 불어나 **가운데 정보 칸을 3.8px 로 짓눌렀다**

---

## 1-3-4. 튜토리얼 전에는 **가진 것만** 보여 준다

인트로가 끝나고 처음 들어온 마이 룸에는 **원피스 칸에 '공주 드레스' 하나**만 있다.

- 잠긴 칸 여덟 개가 늘어서 있으면 무엇을 하라는 화면인지 읽히지 않는다.
  잠긴 칸을 보여 주는 것은 '계속 하면 이런 게 나온다' 는 약속인데,
  아직 아무것도 해 보지 않은 사람에게는 그냥 벽이다
- **'없음' 도 뺀다** — 가진 옷이 한 벌뿐이라 벗을 이유가 없고, 눌러도 할 일이 없는 칸이다
- 탭의 보유 개수(`2/9`)도 적지 않는다. 한 칸만 놓고 '2/9' 라고 하면
  나머지 일곱이 어디 있는지 되묻게 된다
- 기준은 `S.tutorialDone` 하나다. 마치는 순간 전부 원래대로 돌아온다

## 1-3-5. 염색약 — 두 가지가 서로 다른 물건이다

| | 마법 염색약 🧪 | 영원 염색약 ♾️ |
|---|---|---|
| 무엇 | 아무 색이나 **24시간** | **그 색으로 영영** |
| 세는 법 | 개수 하나 (`S.dye`) | **색깔별 칸** (`S.dyeEver[색] = 개수`) |
| 고르는 화면 | 60색 팔레트 (`.wr-colors`) | **가진 것 목록** (`.dye-items`) |

- 영원 염색약은 **색이 곧 아이템**이다. 그래서 안 가진 색을 잠긴 점으로 보여 줄 이유가 없고,
  획득한 색(`S.unlocked`)과도 무관하다 — 염색약을 가졌으면 그 색을 쓸 수 있다
- 옷의 **원래 색과 같은 색은 팔레트·목록에서 감춘다.** 맨 앞의 '원래 색' 과 같은 것이
  두 번 나오기 때문이다 (색 값 `hex` 로 판정한다)
- **'원래 색' 을 누르면 이름을 알려 준다.** 색 조각만 보고는 무슨 색인지 알 수 없다.
  60색 중에 같은 색이 있으면 그 이름을, 없으면 그냥 '원래 색' 이라고 한다
- **마법 염색약이 걸려 있으면 '원래 색' 을 눌러도 되돌리지 않는다.** 24시간이 지나면
  저절로 풀리는 것이라, 여기서 지우면 쓴 염색약이 그냥 사라진다.
  대신 지금 무슨 색인지와 **언제 돌아오는지**를 두 줄로 알려 준다

## 1-6. 랭킹 — 주간 리그 (`#screen-league`)

듀오링고의 리더보드와 같은 짜임이다. 12명이 한 조로 한 주를 겨루고,
**1~3위 승급 · 4~8위 잔류 · 9~12위 강등**. 리그는 32개 (계열 8 × 단계 4).

### 화면 순서 — 위에서 아래로 읽히게

| 자리 | 무엇 | 왜 |
|---|---|---|
| `.lg-last` | 지난 주 결과 배너 | 켰을 때 **가장 먼저** 알아야 하는 것. 한 번 보면 닫는다 |
| `.lg-head` | 리그 배지 · 이름 · 규칙 · 사다리 · 남은 시간 | 지금 어디에서 무엇을 겨루는가 |
| `.lg-zone-head` | '지금 N위 — 승급 구간이에요' | 12줄을 눈으로 훑기 전에 **결론부터** |
| `.lg-list` | 12줄 순위표 | 구간이 바뀌는 자리에 선(`.lg-sep`)을 긋는다 |

### 지킬 것

- **구간을 색으로만 알리지 않는다.** 승급/강등은 순위 숫자 색 + 구간 경계선 +
  글로 쓴 라벨('잔류 구간' · '강등 구간')까지 셋으로 알린다 (7장과 같은 이유).
  지난 주 결과 배너도 색 + 화살표(▲▼＝) + 문장이다
- **내 줄은 배경색과 테두리를 같이 준다** (`.lg-row.me`). 색만 다르면 옷장 칸에서와
  같은 문제가 생긴다
- **글자 크기를 줄여 자리를 만들지 않는다.** 하단 탭이 셋에서 넷으로 늘 때
  `.tab-btn` 을 0.66rem 으로 줄였다가 10.6px 이 되어 최소 11px 에 걸렸다 —
  아이콘과 여백만 줄인다. 구간 라벨(`.lg-sep`)도 0.68rem(10.9px)에서 0.7rem 으로 올렸다
- 리그 이름은 **계열 이름 + 단계 숫자**로 조합한다. 32개에 번역을 따로 두면
  한쪽 언어만 빠지는 일이 반드시 생긴다 (2번). NPC 이름도 같은 이유로 앞말 × 뒷말이다
- 다음 리그 이름(`.lg-ladder-top`)은 길어지면 말줄임한다 — 사다리 막대를 밀어내지 않게

### 탭은 '여신' 단계부터 나타난다

잠긴 탭을 자물쇠로 보여 주지 않는다. 하단 탭은 **지금 갈 수 있는 곳**의 목록이고,
셋에서 넷으로 늘면 그것만으로 새로 열렸다는 것이 눈에 띈다.
`renderTabBar()` 가 `hidden` 을 켜고 끄며, `switchTab('league')` 은 잠겨 있으면 홈으로 돌린다 —
옛 세이브의 마지막 탭이 랭킹이면 빈 화면이 뜨기 때문이다.

> **`hidden` 속성만으로는 안 숨는다.** 브라우저 기본 `[hidden] { display: none }` 은
> 특정도가 낮아서 `.tab-btn { display: flex }` 에 진다 — 속성은 켜져 있는데 화면에는
> 그대로 보인다. 랭킹 탭이 '새싹' 단계에서 보이던 것이 이것이었고, `.tab-btn[hidden]` 을
> 따로 써서 고쳤다. **`hidden` 을 쓰는 곳에 `display` 를 지정했다면 짝을 맞춰 둘 것.**
> `tools/checkui.js` 가 속성이 아니라 **그려진 크기**로 이 상태를 검사한다 —
> 속성만 보면 통과하고 사람 눈에는 보인다.

---

## 1-4. 상단바 (`.app-header`)

한 줄에 왼쪽부터 **AP 알약 · 결정 알약 · 매력 칩 · 설정**이다.

- **재화 하나에 알약 하나.** `.ap-wrap`(⚡·게이지·`+`) · `.gem-wrap`(💎·개수·`+`) ·
  `.header-charm`(등급 뱃지·매력 총합). 같은 색·같은 둥근 모양·같은 높이(32px)라 한 식구로
  읽힌다. 묶지 않으면 낱개로 흩어져 무엇이 한 묶음인지 안 보인다 (1-3장과 같은 이야기다)
- 알약 안의 `+` 는 **'더 얻기'** 로 뜻이 같다 — AP 의 `+` 는 결정으로 충전,
  결정의 `+` 는 다이아 구매. 둘은 `.hdr-plus` 한 규칙이라 언제나 같아 보인다
- **알약을 누르면 그게 뭔지 알려 준다** — ⚡·게이지(`.ap-hit`)는 AP 안내,
  💎 는 무엇에 쓰는지. `+` 만 따로 눌리게 두어 안내와 행동이 섞이지 않는다
- **'AP' 글자는 없앴다.** 결정 개수가 들어오면서 게이지가 최소 폭(60px)까지 눌려
  `"1000/1000"`(54px)이 트랙을 꽉 채웠다. ⚡ 아이콘이 같은 뜻을 전하므로 글자를 빼고
  그 33px 을 게이지로 돌렸다 (375px 트랙 60 → 81px)
- 헤더의 수는 `fmtCount()` 로 **최대 5글자**까지만 (`9,999` · `99.9K` · `999K`).
  6글자가 되면 320px 에서 `+` 가 알약 밖으로 밀려난다. 정확한 값은 충전 패널이 보여 준다
- 게이지 트랙은 알약 안이라 밝게(`rgba(255,255,255,0.6)`) 잡는다.
  라벤더 위에 잿빛 트랙을 두면 탁해 보인다
- `.ap-wrap` 이 `flex: 1` 로 남는 자리를 먹고, 나머지 둘은 고정 폭으로 오른쪽에 붙는다
- **설정(`.hdr-gear`)은 줄 맨 오른쪽.** 자주 쓰는 것이 아니라 화면 구석에 둔다.
  가운데에 두면 두 알약 사이를 갈라 놓아 둘 다 읽기 나빠진다
- 이 줄은 줄어들 수 있는 것이 게이지뿐이라 좁은 화면에서 문제가 된다 → 2장 참고.
  알약이 셋이라 **세 단계로 물러선다** (숫자는 전부 재서 정한 값이다):
  | 폭 | 하는 일 |
  |---|---|
  | ≤380px | 헤더의 여백·간격·매력 칩을 조인다 (이게 없으면 375px 에서 `+` 가 10px 밖으로) |
  | ≤339px | 결정 **개수**를 감춘다 (💎 와 `+` 는 남는다) |
  | ≤299px | 결정 알약을 통째로 감춘다 (265px 에서 26px 모자란다) |

  감춰도 **없어지는 정보가 아니다** — AP 의 `+` 로 충전 패널을 열면 정확한 보유량이 나오고,
  모자라면 거기서 상점으로 이어진다

## 1-5. 아이콘 버튼 (`.icon-btn` · `.hdr-gear`)

동그란 아이콘 버튼은 **모두 같은 것으로 보여야 한다.** 지금은 둘이다 —
헤더의 설정과 마이 룸 제목 옆의 초기화. 크기·테두리·배경·그림자·아이콘 크기를
`.icon-btn, .hdr-gear` **한 규칙에서 같이** 정한다. 새 아이콘 버튼은 이 선택자에 더한다.

- 34px 원, 테두리 `1px rgba(154,138,149,0.28)`, 배경 `rgba(255,255,255,0.85)`,
  아이콘 19px. 좁은 화면(≤340px)에서는 **둘 다** 30px / 17px 로 줄어든다
- **아이콘은 이모지가 아니라 SVG 다.** 이모지는 플랫폼마다 색과 굵기가 달라
  ⚙️ 와 ↺ 를 짝으로 맞출 수가 없다 (⚙️ 는 컬러 그림, ↺ 는 가는 글자였다).
  `stroke="currentColor"` 로 그려 색은 버튼의 `color` 하나만 따라간다
- 버튼 안에 글자가 없으므로 **`aria-label` 은 필수**, `<svg>` 에는 `aria-hidden="true"`

## 2. 가변 폭 — 문구 길이가 달라져도 깨지지 않기

언어를 추가하면 같은 자리의 라벨 길이가 달라진다. `취소`(2자)가 `Cancel`(6자)이 되고,
독일어·러시아어를 넣으면 그보다 더 길어진다. **어떤 언어를 넣어도 요소가 서로 겹치거나
화면 밖으로 밀려나서는 안 된다.**

### 3단 방어

| 순서 | 동작 | 구현 |
|---|---|---|
| 1 | 줄바꿈하지 않는다 | `white-space: nowrap` |
| 2 | 공간이 모자라면 **줄어든다** | `flex: 0 1 auto` + `min-width: 0` |
| 3 | 그래도 모자라면 **말줄임** | `overflow: hidden` + `text-overflow: ellipsis` |

**절대 일어나선 안 되는 것:** 요소끼리 겹침 · 부모 밖으로 삐져나감 · 페이지 가로 스크롤.
라벨이 `…`로 잘리는 건 허용된 최후의 방어선이다 (검증기에서 `주의` 로만 뜬다).

### 3단으로 안 되는 줄 — 줄일 수도 말줄임할 수도 없을 때

2·3단계는 **글자**에만 통한다. 한 줄이 전부 고정 폭 요소이거나 내용 때문에 최소 폭을
가질 수밖에 없으면, 화면이 좁아지는 만큼 그대로 부모 밖으로 밀려난다.
헤더의 AP 줄이 그랬다 — `⚡`·게이지·`💎 개수`·`+` 중 줄어들 수 있는 건 게이지뿐인데,
게이지의 `min-width: 60px` 은 안에 겹쳐 쓰는 `1000/1000`(54px)을 담기 위한 값이라
더 줄일 수 없었다. 결과적으로 줄 끝의 버튼이 밖으로 나갔다.

이럴 때 쓰는 4단계는 세 가지다. **위에서부터 시도한다.**

| 4단계 | 쓰는 곳 | 구현 |
|---|---|---|
| 줄을 나눈다 | 탭 줄이 **아닌** 가로 줄 | `flex-wrap: wrap` (`.wr-foot` — 보유 개수 / 랜덤 획득) |
| 여백·글자를 조인다 | 아직 몇 px 모자랄 때 | `@media (max-width: 380px)` (헤더 전용) |
| 덜 중요한 것을 감춘다 | 그래도 안 될 때 | `@media (max-width: 339px / 299px)` (`style.css` 맨 끝) |

`.cat-tabs` 계열 탭 줄은 이제 **줄바꿈이 기본**이다 (1장 1번).
다만 줄바꿈은 **칩 하나가 줄보다 긴 경우**를 막지 못한다 — 그 칩은 그대로 부모 밖으로
나간다 (265px 영어에서 솥 이름이 41px). 그래서 `.cat-tab` 자체도 3단 방어를 갖는다.

감출 때는 **같은 뜻을 다른 수단으로도 알 수 있는 것**만 고른다. `AP` 글자를 뺀 건
바로 옆 `⚡` 아이콘이 같은 뜻을 이미 전하고 있어서고, 좁은 화면에서 결정 개수를 감추는 건
`+` 를 누르면 충전 패널에 그 값이 그대로 나오기 때문이다.
글자 크기를 줄이는 경우 [TEXT_POLICY.md](TEXT_POLICY.md) 의 **최소 11px** 을 넘지 않는다.

> 폭 기준을 뷰포트로 잡아도 되는 이유: `.app` 이 `max-width: 480px` 이라
> 화면 폭 == `min(뷰포트, 480)` 이다. 컨테이너 쿼리 없이 미디어 쿼리로 충분하다.

### `min-width: 0` 이 핵심

flex 아이템의 기본값은 `min-width: auto` 라서 **콘텐츠보다 작아지지 않는다.**
이걸 풀어 주지 않으면 `overflow: hidden` 을 걸어도 아이템이 줄지 않고 부모를 밀어낸다.
가로로 나란히 놓는 요소에는 반드시 `min-width: 0` 을 준다.

### 쓰는 법

가로 배치 줄에는 `.row-fit` 을 붙이면 자식들이 위 3단 방어를 자동으로 따른다.

```html
<div class="row-fit">
  <button class="btn">…</button>
  <button class="btn">…</button>
</div>
```

이미 적용된 곳: `.i-actions`(인트로 자동·다음) · `.cauldron-actions` · `.confirm-actions` ·
`.room-tabs` · `.set-opts` · `.wr-foot` · `.clock-bar` · `.spot-card`

### 앞머리는 정수리를 덮는다

얼굴은 타원(`cx100 cy70 rx33 ry35`)이라 **꼭대기가 y=35** 다. 앞머리가 거기까지
안 올라오면 정수리에 살색이 그대로 드러나 머리가 벗겨진 것처럼 보인다.
'이마 노출' 처럼 이마를 드러내는 것이 목적인 앞머리도 **정수리는 덮는다** —
드러나는 것은 헤어라인 아래(y≈42~)여야 한다.

사이드뱅이 y=42 까지밖에 안 올라와 정수리가 살색이었고, 커튼뱅은 가르마가 y=40 에서
열려 정수리가 갈라져 보였다. **둘 다 컨택트시트를 눈으로 훑을 때는 안 띄었다** —
`tools/checkavatar.js` 가 앞머리 종류마다 정수리(y34~42)의 살색 픽셀을 세어 잡는다.

### 검증

```
checkLayout()          // 지금 화면의 겹침 / 넘침 / 잘림 / 가로 스크롤 검사
checkUI()              // 모든 언어 × 라벨 2배 길이까지 자동으로 돌려서 표로 요약 (async)
checkUI({ stress: 3 }) // 3배 길이까지 견디는지
```

**두 가지를 같이 바꿔 가며 본다 — 화면 폭과 화면(탭).** 검증기는 *지금 보이는 것*만 재기
때문에, 창을 하나의 폭으로만 놓고 돌리면 좁은 화면에서만 나는 문제를 통째로 놓친다.
실제로 AP 줄이 밀려난 것도 **265px** 에서만 났다 (375px 에서는 0건).
`switchTab('gather'|'atelier'|'showcase')` · `setRoomTab('clothes'|'potions'|'creatures')`
로 화면을 옮겨 가며 폭 **375 / 320 / 265** 정도를 훑으면 충분하다.

`checkUI()` 는 등록된 모든 언어로 번갈아 바꿔 가며 **글자 대비(checkTextStyle) + 레이아웃
(checkLayout)** 을 함께 돌리고, 추가로 **라벨을 인위적으로 N배 늘린 상태**까지 검사한다.
아직 없는 긴 언어를 미리 흉내 내는 것이므로, 새 언어를 넣기 전에 이걸로 먼저 확인하면 된다.

> 이 검증을 처음 돌렸을 때 잡힌 실제 버그
> - `#clockKST` (시계) 가 `nowrap` 인데 줄어들지 못해 부모를 밀어냄 → 페이지 전체에 가로 스크롤
> - 탐험 카드의 채집 라벨(`채집 ⚡10`)이 `flex: none` 이라 카드를 58px 밖으로 밀어냄
>   (지금은 `.btn-gather` 버튼이다)
> - 헤더의 안내 버튼이 265px 에서 `.ap-wrap` 밖으로 8px — 줄 전체가
>   줄어들 수 없는 요소뿐이었다 (위 '3단으로 안 되는 줄')
> - `.cat-tab` 이 `flex: 0 0 auto` 라 긴 칩 하나가 부모 밖으로 41px (265px 영어)
> - `.ap-plus` 에 `min-width: 0` 이 없어, 라벨 2배 검사에서 알약 밖으로 1px
>   (고정 `width` 를 줘도 flex 아이템의 자동 최소 폭은 콘텐츠 기준이다)

폭을 바꿔 가며 돌리는 것은 `tools/checkui.js` 도 할 수 있다.

```bash
W=265 node tools/checkui.js showcase atelier gather
```

---

## 3. 아바타 옷 — 몸을 기준으로 그린다

체형(통통 ↔ 날씬)이 바뀌어도 옷이 몸을 덮어야 한다. 체형 변화는 **몸과 옷을 같은 그룹으로
함께 가로로 늘리는** 방식이라, 기본 좌표만 맞으면 어느 체형에서도 어긋나지 않는다.

### 규칙

1. 옷은 `avatar.js` 의 **`BODY` 상수를 기준으로** 그린다. 좌표를 눈대중으로 쓰지 않는다.

   | 값 | 뜻 |
   |---|---|
   | `torsoL` / `torsoR` | 몸통 최대 폭(어깨) |
   | `torsoTopY` | 몸통 맨 윗점(목 바로 아래). 옷의 어깨선은 `CLOTH_TOP_Y` 로 **이보다 위** |
   | `shoulderC` | 어깨 곡선의 제어점. **옷의 어깨선도 이 값에서 나온다**(`clothTopEdge`) |
   | `hipHalf` | 엉덩이 반폭. **허리·허벅지보다 좁아지지 않게 스스로 잡아 준다** |
   | `shoulderY` `waistY` `hipY` `ankleY` `footY` | 세로 기준선 |
   | `armX_L` `armX_R` `armY` `armH` `armW` | 팔 막대의 위치·크기 |
   | `armRot` `armPivotL` `armPivotR` `armPivotY` | 팔 회전 각도와 중심 |

2. **옷은 몸보다 한쪽당 `CLOTH_PAD`(3px) 넓게.** 같은 폭으로 그리면 경계에서 살이 비친다.
   **위쪽도 마찬가지다** — 옷의 어깨선(`CLOTH_TOP_Y`)은 몸통 맨 윗점(`torsoTopY`)보다 위여야 한다.
   예전에는 옷이 110 에서 시작해 몸통(108)보다 아래였고, 그 2px 사이로 살색 띠가 목 밑을
   가로질렀다. 상의·원피스·공주 드레스가 같은 값을 보므로 한 곳만 고치면 다 따라온다.

3. **소매는 팔과 같은 `rotate()` 를 쓴다.** 팔은 어깨를 중심으로 기울어져 있어서,
   기울기를 무시하고 세로로 그리면 어깨 위쪽이 삐져나온다.
   `sleeves(color, 길이비율)` 이 팔 좌표에서 소매와 손 위치를 계산해 준다.

4. **옷의 어깨선은 몸의 어깨 곡선(`BODY.shoulderC`)에서 뽑는다.** 손으로 박아 두면
   몸의 어깨를 고친 순간 조용히 어긋난다 — 실제로 어깨를 넓히자 옷은 옛 곡선 그대로라
   x=127 에서 **4px 짜리 살색 띠**가 어깨 위에 떴다 (터틀넥 30px · 블라우스 2px).
   `clothTopEdge()` 가 `shoulderC` 를 바깥으로 `SH_PAD`, 위로 1px 물려 그대로 따라간다.

5. **목에서 팔까지 실루엣 윗선은 다시 솟지 않는다.** 어깨 봉우리와 팔의 둥근 윗머리가
   따로 놀면 그 사이가 파여 **꼭두각시 인형의 관절**처럼 보인다 (`ART_POLICY.md` 2장).
   어깨 곡선이 팔 윗머리보다 **먼저 내려오면** 안 된다 —
   `checkavatar` 의 「어깨 홈」이 1px 을 넘기면 실패시킨다.

6. **어깨 쪽 어느 것도 팔의 바깥 변을 넘지 않는다.** 넘으면 부리처럼 뾰족한 턱이 진다.
   여기 걸리는 것이 둘 있고 **둘 다 몸통·팔 배율을 따로 줄여야 드러난다** —
   기본값에서는 팔이 늘 어깨보다 바깥이라 무엇이든 팔 밑에 숨는다.

   | 무엇 | 왜 넘는가 | 어떻게 막았나 |
   |---|---|---|
   | 목의 벌어진 자락 | 몸통 배율을 안 타서 어깨만 좁아진다 (몸통 50% 에서 **6px**) | `neckShape(…, maxR)` 로 어깨 끝까지만 벌린다 |
   | 몸통의 어깨 끝 | 팔이 가늘어지면 팔의 바깥 변이 안으로 들어온다 (팔 50% 에서 **2.5px**) | `shoulderSquash(tune)` 로 어깨를 팔에 맞춰 좁힌다 |

   ⚠️ **팔을 바깥으로 밀어서 맞추지 않는다.** 굵기 배율의 축을 바깥 변으로 옮기면
   어깨는 붙지만 **팔이 허리에서 몸통과 떨어진다** — 팔은 허리에서 몸통과 1.6px 밖에
   안 겹쳐서, 가늘어진 만큼 안쪽 변이 나가면 곧바로 배경이 비친다(팔 50% 에서 4.75px).
   폭이 15px 뿐이라 「허리에 닿으면서 어깨 끝까지 닿는」 팔은 만들 수가 없다.
   **어깨 쪽을 팔에 맞춘다.**

   `checkavatar` 의 「어깨 부리」가 몸통×팔 25조합을 돌며 1px 을 넘기면 실패시킨다.

7. **허리 → 엉덩이 → 허벅지는 접선이 이어져야 한다.** 셋은 배율이 서로 다르므로
   (허리·엉덩이·허벅지 슬라이더) **파츠를 통째로 늘리면 이음매까지 같이 늘어난다.**
   예전에는 엉덩이 path 를 `sx(kh, 100)` 으로 감싸서, 엉덩이만 키우면 위 이음매가
   몸통 옆선 밖으로 튀어나오고 아랫변이 허벅지보다 넓어져 **두 자리에 계단**이 생겼다.

   대신 **세 점을 각자 제 배율로 절대 좌표에 놓고 그 사이를 곡선으로 잇는다.**
   세 점 모두 **세로 접선**이라 어느 배율을 움직여도 꺾이지 않는다.

   | 점 | 높이 | 왜 세로인가 |
   |---|---|---|
   | 허리 | `waistY` | 실루엣의 가장 좁은 곳 — 몸통 옆선도 여기서 세로로 들어온다 |
   | 엉덩이 | `hipApexY(tune)` | 가장 넓은 곳. **중력으로 처져서** `hipY` 보다 아래다 |
   | 허벅지 | `hipBlendY(tune)` | 허벅지 바깥 변에 세로로 내려꽂아 그대로 다리로 이어진다 |

   ⚠️ **엉덩이가 커지면 뾰족해지는 게 아니라 처지면서 둥글어진다.**
   예전에는 엉덩이(198)에서 허벅지(214)까지 16px 안에 다 떨어뜨렸다 —
   허리 50% · 엉덩이 150% 이면 가로로 28px 라 기울기가 3.1 까지 서서
   **옆으로 뾰족한 날개**가 됐다. 실제 몸은 그렇게 안 생겼다.

   그래서 튀어나온 만큼(`hipDrop`) 두 가지가 같이 따라간다:

   | | 규칙 | 왜 |
   |---|---|---|
   | `hipBlendY` | `hipY + 낙차 × 2.2` (최대 **무릎 10px 위**) | 내려올 거리가 길어야 완만하다 |
   | `hipApexY` | `hipY + min(낙차 × 0.5, 내려갈 거리 × 35%)` | 중력으로 처진다 |

   가랑이 선(`hipBottom`)은 그대로다 — 그것까지 내리면 다리 사이가 막힌다.

   ⚠️ **허벅지에 닿는 x 는 「닿는 높이의」 허벅지 바깥 변보다 1px 안쪽**(`thighJoin`)이다.
   두 가지를 같이 지켜야 한다:
   - 딱 맞추거나 밖으로 물리면 그 높이에서 실루엣이 1px 턱을 져 **가느다란 선**이 보인다
   - **허벅지가 아래로 가늘어진다는 것**을 빼먹고 「가장 굵은 곳」으로 잡으면,
     닿는 높이에서는 허벅지가 이미 그보다 안쪽이라 그 사이로 **세로 틈**이 벌어진다
     (기본 1.6px · 엉덩이 150% 에서 3.8px). `checkavatar` 의 「엉덩이↔허벅지 틈」이 지킨다.

   **하의도 같은 값을 본다**(`hipApexY` · `hipBlendY` · `hipSideAt`) —
   안 그러면 넓은 엉덩이가 바지 옆으로 나온다. 밑단이 짧아 엉덩이가 다 내려오기 전에
   끊기는 옷(반바지)은 **끊기는 높이의 몸 폭까지 다리 통을 넓힌다**.

   ⚠️ **몸통 옆선의 마지막 제어점 x 는 허리와 같아야 한다.** `wR + 3` 처럼 바깥에 두면
   몸통은 안으로 기울어 들어오는데 엉덩이는 바깥으로 나가, 허리에 꺾임이 남는다.

   `checkavatar` 의 「허리↔엉덩이↔허벅지」가 배율 8조합에서 옆선의 기울기 변화를 재
   1.5 를 넘기면 실패시킨다 (고치기 전 최대 9.1).

8. **다리는 원통이 아니다.** 예전에는 허벅지·종아리가 네모(rect)를 가로로 늘린 것이라
   굵게 하면 **무릎도 발목도 같이 굵어져 통나무**가 됐다. 실제 여자 다리는
   **관절은 거의 그대로**고 살만 곡선으로 붙는다.

   | 마디 | 살이 붙는 곳 (배율을 탄다) | 안 붙는 곳 (배율을 **안** 탄다) |
   |---|---|---|
   | 허벅지 | 위(엉덩이 밑) `LEG.hipW` | 무릎 `LEG.kneeX` |
   | 종아리 | 장딴지 `LEG.bellyW` | 무릎 · 발목 `LEG.ankleX` |

   ⚠️ **관절은 「중심선에서 잰 거리」로 잡는다.** 허벅지와 종아리는 안쪽 변이 서로 달라서
   (2 / 3), 폭을 맞추면 무릎에서 바깥 변이 어긋나 1px 턱이 진다.
   관절도 **마디보다 굵어질 수는 없다** — 가늘게 만들면 같이 가늘어진다.
   발·구두는 **발목**을 따라간다(`footX`) — 종아리를 굵게 해도 발은 제자리다.

   ⚠️ **안쪽 변은 배율을 안 탄다** (`THIGH_GAP` · `CALF_GAP`). 파츠 가운데를 축으로 하면
   가늘게 만들 때 안쪽 변까지 밖으로 밀려 **다리 사이가 벌어진다**
   (허벅지 60% 에서 틈 4px → 16px). 체지방이 빠져도 살은 **바깥에서** 빠지고
   안쪽 틈은 그대로다 — 체지방 15~45% 사진이 그렇다.

   `checkavatar` 의 「다리 사이 틈」(흔들림 1.5px 까지)과 「발목」(굵어지면 안 된다)이 지킨다.

9. **팔도 원통이 아니다.** 윗팔·아랫팔이 둥근 네모(rect)를 가로로 늘린 것이라
   굵게 하면 팔꿈치도 손목도 같이 굵어졌다. 다리와 같은 규칙이다 —
   **살(어깨·팔뚝)은 배율을 타고 관절(팔꿈치·손목)은 덜 탄다**(`ARM_W` · `armHalf`).
   마디 사이를 에르미트로 이어 어깨·팔꿈치·손목에서 접선이 세로라 관절이 안 꺾인다.
   **안쪽 변은 고정**이라 굵기가 어떻든 팔이 몸통에서 안 떨어진다.
   손 위치(`armPoint`)도 프로필의 중심선을 따라간다 — 고정값에 두면 가는 팔에서 손만 바깥에 남는다.

7-0. **배율이 살에 얹히는 정도는 부위마다 다르다**(`TUNE_GAIN` · `fatOf`).
   슬라이더는 부위마다 0.5~2 로 같은데 **같은 200% 라도 자연스러운 두께는 다르다** —
   눈으로 맞추며 여러 번 옮겼다. 지금은 **허벅지의 기본 폭 자체를 올려 두었고**
   (`LEG.hipW` 20 → 36 — 「예전 150% 가 지금 100%」), 200% 의 두께는 그대로라
   `TUNE_GAIN.thigh` 를 1.6 → 0.444 로 같이 낮췄다. 엉덩이는 0.27, 종아리는 0.6 이다.

   ⚠️ **100% 는 어느 부위도 안 건드린다.** 100% 를 **넘는 만큼(k−1)에만** 곱한다:
   `k' = 1 + (k−1)·gain`. 100% 아래는 그대로 둔다 — 얇은 쪽은 이미 맞춰 놓은 그림이다.
   ⚠️ **`tuneOf` 를 그냥 바꾸면 안 된다.** 슬라이더 값 자체를 쓰는 곳(옷이 어느 파츠를
   따라갈지 고르는 `tuneMax` · 화면의 % 표시)은 사람이 고른 값 그대로여야 한다.
   **살의 양을 셈하는 자리에서만** `fatOf` 를 쓴다.

   `checkavatar` 의 「200% 두께」가 부위별로 **100% 대비 몇 배가 되는지**를 박아 두고
   (허벅지 1.44 · 엉덩이 1.11 · 종아리 1.59, ±0.06), **100% 가 기본 그림과 같은지**도 본다.

   ⚠️ **허벅지·엉덩이의 기본 폭을 바꾸면 치마 밑단도 같이 넓어진다** — 방에 둔 크리처의
   자리(`right: calc(50% + …)`)도 그만큼 옮겨야 한다. 「크리처 자리」가 잡아 준다.

7-0-1. **엉덩이의 가장 넓은 곳은 점이 아니라 짧은 곧은 구간이다**(`HIP_APEX_RUN`).
   위아래 두 마디가 봉우리에서 세로 접선으로 만나 수학적으로는 매끄러운데, 그 자리의
   **곡률 반지름이 `1.5·ha²/(h−w)`** 라 허리가 가늘고 엉덩이가 넓을수록 급격히 작아진다 —
   12px 쯤 되면 사람 눈에는 그냥 **뾰족한 모서리**로 읽힌다.

   ⚠️ **제어점 비율(0.45)로는 못 고친다.** 0.5 가 상한이라 반지름이 1.2배밖에 안 늘고,
   **낮추는 것은 더 나쁘다** — 마디 가운데의 S 는 펴지지만 봉우리가 **연처럼** 뾰족해진다
   (엉덩이 200% · 허리 50% 에서 실제로 더 나빠져 되돌렸다).

   그래서 봉우리를 **짧은 세로 직선**으로 벌린다. 그 구간의 곡률은 0 이라 어떤 배율에서도
   뾰족해질 수가 없고, 실제 몸도 가장 넓은 곳은 한 점이 아니라 한 뼘이다.
   길이는 **허리·허벅지보다 얼마나 나왔는지**에 비례하고, 위아래 마디를 반 넘게
   잡아먹지 않는다 (이을 곡선이 남아 있어야 한다).

7-1. **엉덩이는 허벅지의 「그려진」 윗머리까지 감싼다.** 허벅지의 맨 위(186)는 엉덩이의
   봉우리(198)보다 **12px 위**다. 반폭을 허벅지와 같게 맞춰 놔도 그 높이의 옆선은 아직
   허리에서 내려오는 중이라 좁아서, **허벅지의 둥근 윗머리가 혹처럼 튀어나왔다**
   (허벅지 200% 에서 4px). 옆선을 따라가며 「이 높이의 허벅지를 덮으려면 봉우리가 얼마나
   넓어야 하는가」를 되풀어 그만큼만 좌우로 퍼뜨린다(`hipNeedHalf`).

   ⚠️ **덮을 것은 「그려진」 허벅지다**(`thighDrawnAt`). 위 끝은 둥근 마개라 맨 윗줄에서는
   좁고 가장 굵어지는 자리는 186+r 쯤이다 — 186 에서 제 폭을 다 덮으려 들면 있지도 않은
   살을 덮느라 엉덩이가 크게 부푼다.
   ⚠️ **양 끝 언저리는 B(곡선이 끝점 쪽으로 간 비율)로 자른다.** 높이로 자르면 마디 길이가
   배율마다 달라져 어떤 조합에서는 끝까지 재게 되고, 나눗셈이 터져 **반폭 70px 짜리
   삼각형**이 나온다.
   ⚠️ **얹는 폭에 상한(`HIP_SPREAD_MAX`)을 둔다.** 못 덮을 자리를 만나면 이 식은 얼마든지
   커지는데, 커진 엉덩이는 봉우리에서 허벅지까지를 짧은 거리에 깎아 내리느라 옆구리에
   **선반(날개)** 을 만든다 — 사람 눈에는 그쪽이 더 나쁘다.
   ⚠️ **낙차(중력으로 처지는 양)는 「배율이 만든 폭」으로만 잰다.** 얹은 폭까지 넣으면
   넓어질수록 봉우리가 내려가고, 내려가면 위쪽 옆선이 좁아져 또 넓혀야 해서 **발산한다.**

   `checkavatar` 의 「허벅지 윗머리」가 허벅지×엉덩이×허리 48조합을 돌며, **엉덩이가
   실루엣을 맡는 구간**(허벅지 윗머리~봉우리)에서만 견준다 (4px → 0px).
   그 아래는 **일부러** 엉덩이가 허벅지 안쪽으로 물러나는 자리라 뺀다.

7-2. **⚠️ 부위끼리 하한 노릇을 하면 슬라이더가 조용히 죽는다.** 위 7-1 의 뒷면이다.
   엉덩이는 허벅지보다 좁을 수 없는데, 허벅지 기본 폭을 올리면서(`LEG.hipW` 20 → 36)
   **허벅지 윗머리(38)가 엉덩이(34)보다 굵어졌다** — 그때부터 `hipBaseHalf` 는 늘
   허벅지를 돌려줬고, **엉덩이 20%~150% 가 전부 38.0~38.6px** 로 그려졌다.
   슬라이더를 끝에서 끝까지 밀어도 좌우 폭이 하나도 안 변하는데 **검사는 전부 통과**였다.

   지킬 것 셋:
   · **하한이 되는 값과 배율의 기준값을 같은 자리에 둔다.** `BODY.hipHalf` 는
     허벅지 윗머리(`thighTop` 의 기본값)와 **같은 38** 이어야 한다. 낮으면 삼켜진다
   · **하한도 같이 움직이게 만든다.** 골반이 좁으면 허벅지 윗머리도 들어온다(`HIP_PULL`) —
     **무릎이 허벅지·종아리를 따라가는 것(`KNEE_K`)과 같은 규칙**이고, 관절이니 절반만
     따라간다. 무릎 쪽으로는 `thighOuterAt` 의 보간이 알아서 지운다.
     허리 하한도 **허리의 3/4 까지만**이다(`HIP_WAIST_MIN`) — 허리 자체를 하한으로 두면
     기본 허리(30)에서 슬라이더 아래쪽 절반이 죽는다
   · **양 끝만 재지 말 것.** 「상한 두께」는 100%·상한 두 점만 봐서 가운데가 통째로
     눌러앉아도 통과였다. 「슬라이더」가 하한~상한을 여러 칸 훑으며 **칸마다 1px 이상,
     전체 8px 이상** 변하는지 본다 (엉덩이 25.0→30.0→35.3→38.8→40.3→43.0).
     ⚠️ **팔을 같이 재면 안 된다** — 팔은 y 214 까지 내려와 골반 옆에 서 있어서
     몸 전체를 재면 엉덩이가 무슨 짓을 해도 늘 40.0px 이 나온다

   ⚠️ **재는 자리는 그 부위의 상한이다.** 엉덩이 슬라이더는 150% 까지인데 검사는
   200% 에서 재고 있었다 — **닿을 수 없는 자리를 지키느라** 사람이 쓰는 구간이
   죽어 있어도 통과였다. 두께 자체는 그대로다(1.11배) — 그 두께가 150% 에서 나온다.

8-1. **다리의 안쪽 변도 곡선이다.** 바깥 변은 마디마다 곡선인데 **안쪽 변만 자로 그은
   세로선**이라, 허리 아래부터 발목까지 145px 이 폭 하나 없이 내려와 **칼로 벤 틈**처럼
   보였다. 살이 붙은 자리는 안쪽에도 붙는다 — `INNER` 표(`avatar.js`)를 지난다:
   엉덩이 밑(내전근)에서 거의 붙었다가 → 허벅지 가운데에서 벌어지고 → 무릎에서 모였다가
   → 장딴지에서 다시 좁아지고 → 발목에서 발만큼 벌어진다.

   ⚠️ **배율을 안 탄다.** 여기에 배율을 태우면 살을 뺄 때 다리가 벌어진다 (위 8번과 같은 규칙).
   ⚠️ **허벅지와 종아리가 같은 함수를 지난다.** 둘은 7px 겹쳐 있어서 안쪽 변을 따로 잡으면
   겹치는 구간에서 안쪽에 있는 쪽이 이겨 이음매에 턱이 진다.

   `checkavatar` 의 「다리 안쪽 변」이 셋을 같이 본다 — **꺾임**(0.5px 까지) ·
   **곧은 선이 아닐 것**(엉덩이 밑↔발목 틈 차이 2px 이상 · 예전에는 0) ·
   **배율에 안 흔들릴 것**. 앞의 둘은 서로 반대라 하나만 재면 다른 하나를 못 잡는다.

8-2. **관절도 살을 타긴 탄다 — 다만 훨씬 덜 탄다.** 무릎을 아예 고정해 두었더니
   허벅지 200% 에서 42 → 17 로 좁아져 **깔때기**가 됐고, 종아리까지 200% 면 그 아래가
   다시 38 로 부풀어 **무릎만 잘록한 모래시계**가 됐다. 살이 찐 사람은 무릎도 굵다 —
   다만 허벅지·장딴지만큼은 아니다. **팔의 팔꿈치와 같은 몫(40%)** 을 태운다(`KNEE_K`).

   ⚠️ **발목은 그대로 둔다.** 「굵은 종아리에 가는 발목」은 일부러 그렇게 잡은 그림이다.
   ⚠️ 무릎은 **허벅지·종아리 중 굵은 쪽**을 따르되 **가는 쪽보다 굵어지지는 않는다** —
   허벅지만 200% 면 무릎은 종아리에 막혀 거의 그대로다.

8-3. **장딴지가 가장 굵은 자리는 불어나는 양이 클수록 아래로 내려간다**(`bellyYOf`).
   자리를 붙박아 두면 무릎(263)에서 장딴지(277)까지 **14px 안에** 살을 다 붙여야 해서,
   종아리 200% 에서는 그 사이에 19px 이 불어나 **바깥으로 뾰족한 마름모**가 됐다.
   엉덩이가 처지는 것과 같은 규칙이다 — 많이 나온 것은 내려갈 거리도 길다.
   기본 체형에서는 한 톨도 안 움직인다.

   `checkavatar` 의 「다리 옆선」이 허벅지×종아리 16조합에서 **옆선의 기울기**(|dx/dy|,
   6px 창)가 2 를 넘지 않는지와 **둘 다 200% 일 때 무릎이 3px 이상 굵어지는지**를 같이 본다
   (기울기 2.67 → 1.58 · 무릎 0.75px → 7px).

9-0. **어깨는 세 값이 같이 움직여야 둥글어진다** — 승모근의 기울기(`shoulderC`) ·
   팔이 매달린 높이(`armY`) · 팔 위 마개의 세로 비(`ARM_CAP_H`).
   예전에는 어깨선이 **자로 그은 듯 평평한 채로** 팔에 닿아 **어깨와 팔이 거의 직각**으로
   보였다. 지금은 목(108)에서 어깨 끝(120)까지 사선으로 흘러내리고, 팔은 그 끝에 맞춰
   내려 걸리고(`armY` 112 → 114), 마개가 세로로 길어 그 사선을 이어받는다.

   ⚠️ **셋 중 하나만 고치면 다 실패한다.** 세 번 다 겪었다.
   · `shoulderC` **만** 눕히면 몸통이 팔 마개보다 먼저 내려와 **「어깨 홈」이 1.5px** 로
     한계를 넘는다 — 팔 위 끝(`armY`)과 어깨 끝(`sc[2]`)을 같이 내려야 메워진다
   · 팔 위 끝을 **어깨선(108)까지 올리면** 어깨 위로 **혹이 솟는다.** 몸통의 어깨 곡선은
     평평해 보여도 x=133 에서 이미 113 까지 내려와 있다
   · 반대로 **116 까지 내리면** 몸통이 먼저 나와 **「어깨 부리」가 1.75px** 로 튄다.
     지금 값(114)은 그 사이를 훑어서 고른 자리다

   ⚠️ **마개가 반원이면(`ARM_CAP_H` = 1) 안 된다.** 팔 폭의 절반(7.5px)만에 가로에서
   세로로 돌아 **어깨선과 거의 곧은 팔이 작은 모서리 하나로** 만난다. 세로로 늘이면
   같은 폭을 **더 긴 거리에 걸쳐** 돌아 어깨가 둥글게 흘러내리고, 꼭대기는 그대로 `armY`
   라 어깨 위로 혹이 솟지도 않는다.

   ⚠️ **어깨 끝 좌표는 `sc[2]` 한 곳에서 읽는다.** 몸통 path 의 **왼쪽으로 되돌아오는
   자리**가 예전에는 `133` 을 손으로 박아 놓고 있었다 — `sc[2]` 를 내리는 순간
   **왼쪽 어깨만 옛 자리에 남을 뻔했다.**

   `checkavatar` 의 「어깨 마개」가 **팔 위 끝에서 제 폭이 다 나올 때까지의 세로 길이**를
   팔 폭과 견준다 (반원이면 0.88배 · 지금은 1.07배 · 1.0배 이상).
   「어깨 홈」·「어깨 부리」가 목→팔 실루엣이 다시 솟거나 튀어나오지 않는지 같이 본다.

9-2. **손은 벙어리장갑 한 덩어리다.** 예전에는 손이 **아예 없었다** — 팔이 손목에서
   둥근 마개로 뚝 끝나 「잘린 것처럼」 보였다 (소매를 그리는 옷 몇 벌만 소매 끝에
   살색 동그라미를 하나 얹고 있었다). 듀오링고풍이라 손가락은 그리지 않는다
   (`ART_POLICY`) — **손목보다 조금 넓은 덩어리에 엄지 혹 하나**면 손으로 읽힌다.

   ⚠️ **손목에 선을 긋지 않는다.** 잘린 것처럼 보이던 바로 그 모양이 된다.
   팔과 같은 살색이라 **실루엣으로만** 구분되므로 손목보다 넓은 것이 핵심이다.
   ⚠️ **팔의 기울기를 그대로 받는다** — 어깨(`armRot`)와 팔꿈치(`elbowRot`)의 **합**이다.
   ⚠️ 자리는 `armPoint` 로 잡는다. 고정값에 두면 가는 팔에서 **손만 바깥에 남는다.**
   ⚠️ **손은 팔을 따라 가늘어지되 덜 따라간다**(`HAND_FOLLOW` — 무릎·팔꿈치와 같은 규칙).
   아래위 양쪽에 함정이 있다:
   · 손목을 **그대로** 따라가면 팔 50% 에서 손이 손목보다 0.5px 밖에 안 넓어져 **다시 마개**다
   · **바닥을 크게 깔면**(예전의 `HAND_MIN` = 기본 손목의 0.85) 그 바닥은 하필
     **팔 50% 에서만** 걸려, 팔이 가장 가는 자리에서 손만 33% 부푼다(3.04 → 4.04).
     가는 팔은 안쪽 변이 고정이라 **몸 실루엣 안으로 물러나 안 보이는데** 그 공만
     골반 밖으로 튀어나와, **팔에 안 달린 구슬이 몸에 붙은 것**처럼 보였다
     (「몸통·팔 50% 일 때 손이 떨어짐」)
   ⚠️ **자리는 `armPoint` 가 잡는데, 그 안의 기준선 둘이 `armShape` 와 같아야 한다** —
   팔의 안쪽 변(`xin`)과 어깨 회전축(`pivot`). `armShift`(몸통 배율만큼 팔을 옆으로
   옮기는 값)를 `armPoint` 만 빼먹고 있어서, **몸통이 100% 가 아니면 손이 팔에서
   최대 16px 떨어져** 옷 위에 동동 떴다 (몸통 50% 에서 팔과 겹침 **0%**).
   **100% 에서만 0 이라 기본 체형에서는 안 보인다** — 그래서 오래 남았다.
   ⚠️ **손에도 팔과 같은 그늘을 깐다**(`ARM_SHADE`). 가는 팔은 몸 실루엣 안에 들어가는데
   같은 살색이라, 그늘이 없으면 손이 **어디서 시작하는지 안 보인다.**
   ⚠️ **장갑도 손을 덮는다** — 팔만 덮으면 장갑 아래로 살색 손이 삐져나온다.

   `checkavatar` 의 「손」이 맨팔(50·100·200%)·긴팔·장갑에서 **손목 밑으로 내려온 길이**와
   **바깥으로 부푼 폭**을 잰다 (손이 없으면 0.5px / 0px 로 잡힌다).
   부푼 폭은 **아래위를 다 막는다**(0.6~1.3px) — 위쪽 한계가 없어서 「공」을 못 잡았다.
   그리고 **손 마스크가 팔 마스크와 겹치는 몫**을 몸통×팔 15조합에서 잰다
   (제대로 붙어 있으면 배율과 무관하게 12% 안팎 · 6% 밑이면 실패).
   ⚠️ 자리를 **좌표로 견주지 않는다** — 겹침으로 재면 어느 값을 빼먹든(회전축이든
   기준선이든) 결과가 어긋나는 순간 잡힌다.
   ⚠️ `[data-part="arm"]` 안에 손이 **들어 있다** — 안 지우고 재면 늘 100% 로 나온다.

9-1. **팔꿈치 이음매는 관절에 놓은 원 하나로 메운다.** 마디 둘을 그냥 맞대면
   아랫팔이 굽는 만큼(`elbowRot` 9°) 윗변이 기울어 바깥쪽에 얇은 쐐기 틈이 남는다.
   예전에는 윗팔을 **고정된 반폭(`w/2`)만큼 아래로 늘려** 덮었는데,
   **그 늘린 조각은 굽힘을 안 탄다** — 팔꿈치 아래에 네모난 턱이 바깥으로 삐져나왔다.
   늘린 길이가 굵기와 무관한 고정값이라 **가는 팔일수록 심했다.**
   굽힘의 회전축이 관절 한가운데이므로 **그 점을 중심으로 한 원은 굽혀도 제자리다** —
   반지름을 팔꿈치의 반폭으로 잡으면 두 마디 어느 쪽에서 봐도 안쪽에 들어가 있다
   (원은 이차로 좁아지고 팔은 그보다 느리게 좁아진다).
   `checkavatar` 의 「팔꿈치 턱」이 팔 배율 5가지 × 좌우로 팔꿈치 둘레의 옆선 단차를 잰다
   (고치기 전 1.25px · 고친 뒤 0.25px = 래스터 눈금 · 0.5px 까지).

10. **몸통과 팔은 그늘로 가른다.** 둘 다 같은 살색이라 겹치면 팔이 사라진다.
    테두리를 두르면 어깨→팔로 이어지는 선을 끊어서 없앴는데, 그러자 구별이 안 됐다.
    **같은 모양을 몸 쪽으로 `ARM_SHADE`(2.5px) 밀어 한 단 어둡게 깔면**
    팔에 가려 **안쪽 겨드랑이 선만 그늘로 남는다** — 실루엣을 끊지 않는다.
    소매도 같은 함수를 지나므로 옷 색 그대로 따라온다.

11. **허리 아래는 하의가 덮는다 — 그리고 아무도 안 보고 있었다.**
   맨 위의 커버리지 검사는 **허리까지만** 본다. 그래서 엉덩이를 키우면 반바지 옆으로
   엉덩이가 삐져나오는데도 모든 검사가 통과였다.
   `checkavatar` 의 「엉덩이가 하의 밖으로」가 **엉덩이만 남긴 그림**의 픽셀을 찍어 두고,
   하의를 입힌 그림에서 그 자리가 아직 살색인지 본다 (배율×하의 48조합 · 2% 까지).

4. 몸통을 덮는 드레스는 `sleevedDress(색, 어두운색, 밑단Y, 긴소매여부)` 를 쓰면
   위 규칙이 자동 적용된다.

5. **옷을 늘릴 때 렌더러에 `kind` 분기를 새로 파지 않는다.** 실루엣이 정말 달라질 때만
   새 path 를 쓰고, 그 밖의 변주는 **아이템 필드**로 낸다. 아래가 지금 있는 축이다.

   | 칸 | 필드 | 값 |
   |---|---|---|
   | 상의·원피스 | `sleeve` | `none` `cap`(20) `short`(42) `half`(66) `long`(94) — 팔을 덮는 높이 |
   | 상의·원피스 | `neck` | `round` `v` `square` `polo` |
   | 상의 | `button` | 앞섶 단추 |
   | 원피스 | `puff` | 어깨 볼륨 |
   | 원피스 | `hemY` `flare` | 기장 · 퍼짐 |
   | 하의(치마) | `hemY` `flare` `balloon` `belt` | 246 무릎 위 · 268 무릎 · 288 무릎 아래 · 306 종아리 중간 · 330 발목 |
   | 하의(바지) | `hemY` `belt` | 반바지도 같은 실루엣이고 기장만 다르다 |
   | 장갑 | `len` | 팔 길이 대비 덮는 비율 (0.08~0.95) |
   | 구두 | `rise` | 발목 위로 올라오는 높이(px) |

   **넥라인은 실루엣이다 — 몸판의 윗변 자체를 판다.** (`clothTopEdge(kind)`)
   예전에는 선으로만 그렸다. 어떤 넥라인을 골라도 실루엣이 똑같고 그 위에 곡선 하나를
   얹을 뿐이라, 갈아입어도 목 부분이 하나도 안 변했다.

   파도 되는 이유는 **어깨를 안 건드리기 때문**이다. 파는 것은 가운데(`±w`)뿐이고
   어깨끈은 여전히 `CLOTH_TOP_Y` 에 있어서 몸통 옆·위를 그대로 덮는다.
   체형이 통통해져도 옷과 몸이 같은 배율로 늘어나므로 파낸 폭도 같이 커진다.

   | `neck` | 반폭 `w` | 깊이 `d` | 모양 |
   |---|---|---|---|
   | `round` | 13 | 13 | 대칭 3차 곡선. 가운데는 제어점의 3/4 만큼 내려오므로 제어점을 `d × 4/3` 로 잡는다 |
   | `v` | 13 | 26 | 직선 둘 |
   | `square` | 18 | 15 | **넓고 얕다** — 목(x 91~109)보다 조금만 넓게 파면 목이 그대로 이어져 굴뚝처럼 보인다 |
   | `polo` | — | — | 안 판다. 목을 덮는 통을 따로 얹는다 (`neckLine`) |

   폭·깊이 표(`NECK_CUT`)는 `avatar.js` 에만 있고 **커버리지 검사기도 그 표를 읽는다**
   (`window.Avatar.neckCutBox`). 두 곳에 적어 두면 어긋나고, 어긋나는 순간 검사가 헛돈다.
   표보다 크게 파면 그 바깥에서 살이 잡힌다 — 6px 넓게·8px 깊게 파 보면 전 옷에서 걸린다.

   **파낸 모서리는 몸통 윗선보다 아래에 둔다.** 몸통 윗선은 가운데(x=100)만 108 이고
   바깥으로 갈수록 내려간다 — 모서리를 어깨끈 높이(107)에 평평하게 두면 목 옆에
   **옷도 몸도 없는 홈**이 남아 배경이 비친다(라운드에서 3px 짜리가 났다).
   그래서 모서리 높이를 `torsoTopAtX(100 ± w) - 1` 로 잡는다. 넓게 파는 스퀘어일수록
   모서리가 더 내려온다 — `w` 를 바꿔도 알아서 따라온다.

   목도 `y 107` 아래에서 **어깨 쪽으로 벌어져** 그 모서리를 받쳐 준다.
   107 아래는 몸통도 같은 살색이라 어떤 모양이든 이음매가 안 보인다 —
   그래서 아래에서만 크게 벌린다. 위에서 벌리면 안 판 옷(폴라)의 어깨선 위로
   살이 1px 삐져나온다.

   **턱 밑 그늘은 아래로 갈수록 사라진다** (`neckG` 그러데이션, `SKIN_SH` → `SKIN`).
   끝나는 높이를 몸통 맨 윗선(`torsoTopY`)에 맞추는 것이 핵심이다 — 거기서 색이 `SKIN` 과
   같아지므로 몸통이 목을 덮는 자리에 경계가 아예 안 생긴다. 예전에는 통째로 `SKIN_SH` 인
   네모라, 턱과 몸통(둘 다 `SKIN`) 사이에 **어두운 띠가 딱 잘려** 보였다.
   그라디언트 id 에는 `roomScene` 과 같은 이유로 부를 때마다 꼬리표를 붙인다 —
   아바타를 둘 이상 한 화면에 그리면 뒤에 온 쪽이 앞 쪽 것을 써 버린다.

   **공주 드레스도 판다.** 실루엣이 달라 `sleevedDress()` 라는 다른 렌더러를 쓰지만,
   넥라인의 **가운데(`neckMid`)는 다른 옷과 같은 것**을 쓰고 어깨만 자기 것을 붙인다.
   대신 `intro.js` 의 공주도 같이 팠다 — 인트로와 마이 룸에 같은 공주가 이어서 나오므로
   한쪽만 파여 있으면 눈에 띈다. 인트로 쪽은 네 군데(먹는 중·반신·전신·초상화)가
   `princessNeckCut()` 하나를 같이 쓰고, 파낸 자리는 `princessNeckSkin()` 이 받쳐 준다
   (그 몸에는 아바타의 몸통 같은 것이 없어서, 안 받치면 배경이 그대로 비친다).

   `neck` 필드가 **없는** 옷은 파지 않고, 검사에서도 빼 주지 않는다 — 빼 주면
   그만큼 검사망이 성겨진다.

6. **색은 아이템이 아니라 따로 고른다.** 색만 다른 옷을 아이템으로 늘리지 않는다 —
   4열 그리드에 색 동그라미가 60개 늘어서면 서로 구분이 안 되고, 잠금·보유 개수도
   색 수만큼 부풀어 의미를 잃는다. 팔레트는 `D.COLORS`(60종), 고르는 칸은
   `D.COLORABLE_SLOTS` 다. 고른 색은 세이브에 **id 로** 넣고(`S.itemColor`),
   그릴 때만 hex 로 펴서 `Avatar.build(outfit.colors)` 로 넘긴다.

   - hex 를 저장하면 나중에 팔레트의 색을 조정해도 **이미 그 색을 고른 사람만 옛 색으로 굳는다**
   - `pick()` 은 카탈로그 원본을 고치지 않고 사본을 만든다. 원본을 물들이면
     옷장 목록·미리보기까지 같이 물든다
   - 목록의 색 동그라미는 **각자 자기 색**으로 칠한다. 염색이 옷에 붙으므로 칸마다
     색이 다르고, 목록 전체가 같은 색이 되는 일이 없다
     (칸에 붙어 있던 시절에는 지금 입은 것만 칠했다 — 안 그러면 60벌이 전부
     똑같은 검정 동그라미가 됐다)
   - 색 이름은 **고른 것 하나만** 글자로 쓴다. 60개에 다 이름을 달면 읽을 수 없는 크기가
     된다 — 나머지는 `aria-label` 로 읽어 준다
   - 선택 표시는 **두 겹 링**(진한 테두리 + 흰 링 + 분홍 링). 테두리 색 하나로 표시하면
     그와 비슷한 색 위에서 표시가 사라진다 (흰 링은 흰색 위에서 안 보인다)

### 레이어 순서

```
머리카락(뒤) → 다리 → 몸통·팔 → 상의 → 하의 → 신발 → 원피스 → 얼굴 → 앞머리
→ 장갑 → 문신 → 귀걸이 → 목걸이 → 서클렛
```

- **상의가 하의보다 뒤다.** 하의가 상의 밑단을 덮어 '넣어 입은' 모양이 된다.
  뒤집으면 상의 밑단이 치마 허리춤 위에 얹혀 빼 입은 것처럼 보인다
- **상·하의 둘 다 몸통보다는 앞이다.** 하의를 몸통 뒤로 보냈더니 몸통이 허리춤을 덮어,
  상의 밑단과 치마 사이로 살이 띠처럼 드러났다
- 상의 밑단(`hipY - 2`)이 하의 허리(`waistY`)보다 아래라 그 사이에 틈이 없다.
  **둘 중 하나를 옮기면 이 관계가 깨진다**
- **신발은 원피스보다 아래, 바지보다 위.** 원피스 위에 그리면 부츠 목이 치마를 뚫고 나오고,
  바지 아래에 그리면 바짓단이 부츠를 덮는다
- **허리 아래의 팔은 치마·바지보다 앞이다** (`armsOverSkirt`). 팔은 몸통과 같은 층에 있어
  원래 하의보다 뒤였는데, 치마가 허리에서 아래로 퍼지면서 팔뚝과 손을 통째로 덮었다 —
  긴 치마에서는 팔이 소매 끝 언저리에서 뚝 끊겨 **겨드랑이에 살색 조각만** 남았다.
  ⚠️ **팔 전체를 옷 위로 올리면 안 된다** — 어깨에서 맨팔이 민소매 옷의 어깨끈을 덮어
  버리고, 커버리지 검사의 몸통 창(x 72~128)에도 걸린다. **허리 아래만 다시 찍는다.**
  (공주 드레스는 예외다. 어깨에서 바닥까지 내려오는 종이고 팔은 그 안에 들어가 있다)
- **⚠️ 옷은 「통째로 늘리기」로 몸을 따라가지 않는다 — 조각마다 제 몸 부위를 따라간다.**
  예전에는 옷 전체를 `wrapX(tuneMax(몸통·허벅지·종아리))` 한 번으로 늘렸다. 두 군데가 깨진다:
  · `tuneMax` 는 **1 을 바닥으로 깐다** — 살을 찌울 때만 따라오고 **뺄 때는 옷만 그대로 남는다**
    (「살이 빠졌을 때 옷이 안 맞는다 · 특히 어깨」. 몸통 50% 에서 몸의 허리 15 · 옷 33)
  · 하나의 배율이 옷 전체에 걸린다 — **허벅지를 키우면 어깨까지 두 배**가 됐다
  지금은 좌표마다 제 배율을 탄다: 어깨 `clothShoulderK`(squash × 몸통) ·
  허리 `clothWaistHalf`(허리 × 몸통 + 여유) · 엉덩이 `clothHipHalf`(이미 몸을 따라간 값) ·
  **퍼짐(flare)만** 다리 배율(`tuneMax(허벅지·종아리)`)을 탄다.
  ⚠️ **옷 옆선의 가운데 제어점에 바닥을 깐다**(`clothSideC`). 허리가 어깨에 비해 아주
  가늘어지면(허리 50%) 옷이 몸보다 **먼저** 좁아져 어깨와 허리 사이에 살이 비쳤다.
  기본값에서는 예전 값(`wR + 4`)이 그대로 나와 그림이 안 바뀐다.
- **소매는 자기 옷의 몸판보다 뒤다. 공주 드레스만 앞이고, 대신 윤곽선을 두른다.**
  이 종은 팔보다 **3~14px 밖으로** 퍼져 있어서 뒤에 두면 소매도 손도 어느 높이에서도
  안 비친다 — **삼각뿔 하나**가 됐다(「공주 드레스 입으면 손 사라짐」).
  앞으로 꺼내되 **색은 드레스 그대로 두고 윤곽선으로만 구분한다**(`armShape` 의 `outline`).
  · **같은 색으로 선 없이** 얹으면 팔은 여전히 안 보이고 손만 보여, 초록 종 한가운데에
    **살색 덩어리 두 개가 동동 뜬 것**처럼 된다
  · **한 톤 어둡게 칠하면**(`c2`) 팔이 읽히기는 하는데 **소매만 다른 옷**처럼 보인다
  (인트로의 공주는 같은 색인데도 읽힌다 — 거기서는 팔이 종 **밖으로** 나가 있다)

  ⚠️ **조각마다 선을 그으면 안 된다.** 팔은 마디 둘 + 관절 원이라 각각에 선을 두르면
  **팔꿈치 이음매에 가로줄**이 그어진다. 같은 모양을 **선 두께만큼 부풀려 밑에 깔고**
  그 위에 제 색을 덮는다 — 삐져나온 만큼만 테두리로 남고 내부 이음매는 덮인다.
  ⚠️ **윤곽선과 그늘을 같이 쓰지 않는다** — 안쪽에 선이 두 줄이 된다.

  `checkavatar` 의 「소매 윤곽선」이 아랫팔 높이 두 줄을 가로로 훑어 **옷보다 어두운
  선이 몇 번 지나가는지** 센다 (정상 6줄 · 선을 지우면 0줄 · 4줄 이상).
  ⚠️ **「손이 옷에 안 덮이는가」로는 못 잡는다** — 선이 사라져도 손은 그대로 보인다.
- **⚠️ 「그렸다」와 「보인다」는 다르다.** 손 검사는 넷이나 있었는데 **전부 손만 따로 떼어**
  재고 있어서, 그려 놓고 옷이 그 위를 덮으면 그대로 통과였다. 손에 `data-part="hand"` 를
  붙이고 **완성된 그림에서 그 자리가 아직 살색인지** 센다 (공주 드레스 0% → 89%).
  ⚠️ 견주는 값은 **맨손 한 켤레의 넓이**다 — 손 자리 전체가 아니다. 옷에 따라 손이
  두 켤레분 자리에 그려져(몸이 그리는 팔 끝 214 · 소매 끝 206) 자리 전체를 분모로 잡으면
  **제대로 보이는 손도 53%** 로 나온다.

### 검증

```bash
node tools/checkavatar.js      # 서버가 떠 있어야 한다
```

⚠️ **체형(몸무게)만 돌면 안 된다 — 슬라이더도 같이 돈다.** 옷이 몸을 따라오는지를
슬라이더 쪽으로는 한 번도 안 재고 있어서, 「살이 빠지면 옷이 안 맞는다」가 통과였다
(고치기 전 37건 · 고친 뒤 0건). 중간 체형에서 몸통 50/150 · 허벅지 200 · 종아리 200 ·
허리 50 · 엉덩이 20 · 몸통50+팔50 을 돈다.
⚠️ **몸통 창을 잴 때는 팔·손을 지운다.** 배율을 움직이면 팔이 몸통 창 안으로 들어오는데
(몸통 50% 에서 안쪽으로 16px) 그 팔은 **옷이 못 덮은 살이 아니라 원래 밖에 있는 맨팔**이다.
창을 좁히는 것으로는 못 가른다 — 허리만 줄여도 같은 일이 난다.

체형 5단계 × 모든 상의·원피스에서 **살색 픽셀이 0개**여야 하는 구간 둘:

| 구간 | 범위 | 대상 |
|---|---|---|
| 몸통 | x 72~128 · y **108**(`BODY.torsoTopY`)~허리(`BODY.waistY`) · **넥라인이 판 자리는 뺀다** | 상의·원피스 전부 |
| 어깨/팔 | x 46~154 · y 112~소매가 끝나는 곳 | **소매가 있는 옷만** |

몸통 구간의 위끝은 **몸통의 맨 윗점**이다. 예전에는 112 에서 시작해서, 옷의 어깨선이
몸통(108)보다 2px 아래에 있어 목 밑을 가로지르던 살색 띠를 **통째로 지나쳤다.**
사람 눈에는 「1px 어긋난 선」으로만 보여 오래 남아 있었다.

허리 아래는 하의가 맡는다. 상의 밑단은 골반 바로 위(`hipY - 2`)까지 내려와
하의 허리춤을 덮으므로, 그 사이로 살이 띠처럼 보이지 않는다.

민소매는 팔 검사를 건너뛰고, 캡 소매는 **캡이 끝나는 곳까지만** 본다 —
짧게 설계한 소매를 '살이 나왔다' 고 잡으면 검사가 디자인을 막는다.

넥라인은 **파낸 자리를 빼고** 보되, 파낸 자리가 몸통이 받쳐 주는 높이 아래인지는
따로 잰다 — `isPointInFill` 로 **그린 path 를 직접** 재서 몸통 윗선과 견준다.
표(`neckCutBox`)만 읽으면 표는 그대로인 채 그림만 바뀌었을 때 검사가 통째로 헛돈다
(렌더 기반으로 짰다가 실제로 그렇게 새는 것을 봤다).

상의 × 하의 전 조합에서도 두 가지를 본다.

| 구간 | 범위 | 보는 것 |
|---|---|---|
| 허리 | x 72~128 · y 168~202 | 상의 밑단과 하의 허리춤 사이에 살이 보이는가 |
| 겹침 | x 88~112 · y 190~206 | 여기 보이는 것이 **하의 색인가** (상의를 빨강으로 물들여 센다) |

**살색 검사만으로는 레이어 순서를 못 잡는다** — 어느 쪽이 위에 있든 살은 안 보이기 때문이다.
그래서 순서는 색으로 따로 잰다. 순서를 뒤집어 보면 360조합 전부가 걸린다.

> 실제로 있었던 버그: 공주 드레스의 소매를 팔 기울기와 무관한 곡선으로 그려서
> 어깨 바깥쪽이 드러났다. 팔 좌표(`BODY.armX_*` + `rotate`)를 그대로 쓰도록 고쳐 해결.

> 검사기 쪽에서 겪은 함정 셋 — **0건이 통과가 아닐 수 있는 경우다.**
> - `build()` 는 등신 비율 때문에 몸 전체를 세로로 늘린다. 재는 창을 고정 좌표로 잡으면
>   통통한 체형에서 창이 턱·목에 걸려 **멀쩡한 옷까지 전부 걸린다.** 창도 같은 변환을 지나야 한다
> - **머리 쪽 창도 마찬가지다.** 앞머리 검사가 정수리를 `y34~42` 로 박아 두었는데,
>   목을 빼느라 머리를 6px 올리자 그 창이 정수리를 지나쳐 이마에 걸려
>   **멀쩡한 앞머리 셋이 전부 「정수리에 살색」으로 잡혔다.** 그린 머리의 실제 위치에서 잡는다
> - 살색 판정 폭을 넓게 잡으면 옷과 살의 **경계에서 섞인 픽셀**(안티에일리어싱)까지 살로 세어
>   어떤 옷이든 1~4px 씩 걸린다. 정확히 `#ffdcc4` 인 픽셀만 센다

### 목은 체형을 따라 길이가 변한다 (`NECK_LIFT`)

**머리와 몸은 축이 다른 변환을 탄다.** 머리는 `NECK_Y`(112)를, 몸은 바닥(342)을 축으로 삼는다.
그래서 체형을 움직이면 **턱과 어깨 사이가 저 혼자 벌어졌다 좁아진다** — 예전에는
통통 5.7px · 날씬 **2.7px** 이라 날씬한 몸에서 목이 사라지고 어깨가 솟아 보였다.

⚠️ **축을 턱(105)으로 옮기는 것으로는 안 고쳐진다.** 날씬 쪽은 2.7 → 2.9 로 그대로인데
(머리 배율이 0.979 라 축을 옮겨 봐야 0.15px 차이다) **통통 쪽만 5.7 → 3.6 으로 짧아진다.**
멀쩡하던 쪽을 나쁜 쪽에 맞추는 셈이다 — 실제로 해 보고 눈으로 확인했다.

그래서 **길이를 직접 주는 손잡이 하나**(`NECK_LIFT`)를 두고 날씬할수록 머리를 위로 올린다.
지금은 `12.7 → 11.1 → 9.4 → 7.4 → 5.7px` — **날씬할수록 길다**(4등신), 통통은 짧다(3등신).

⚠️ **처음에 6 으로 잡았더니 눈에는 그대로였다.** 숫자로는 2.7 → 8.7px 로 세 배가 됐는데도
「아직 안 고쳐졌다」는 말이 돌아왔다 — **머리카락이 어깨까지 내려와 목을 덮고 있어서**,
그만큼 올려서는 머리카락 밑으로 목이 나오지를 않았다.
**숫자가 나아진 것과 보이는 것이 다를 수 있다** — 이런 것은 반드시 그려서 눈으로 확인한다.

⚠️ **몸무게만 보면 안 된다 — 몸통 슬라이더도 살을 빼는 것이다**(`NECK_LIFT_TORSO`).
몸통을 가늘게 하면 어깨 곡선(`shoulderC`)이 **가로로 눌려** 승모근이 목 바로 옆에서
솟는다. 목 길이는 한 톨도 안 변했는데 **목이 어깨에 파묻힌 것처럼** 보였다
(몸통 50% 에서 특히 — 「몸통 % 가 낮아졌을 때 목이 너무 짧다」는 신고가 이것이다).
체형 축과 **같은 방향으로** 민다. 다만 몫은 작게(8) — 몸통은 굵기만 바꾸지 등신을 안 바꾼다.
지금은 몸통 50→150% 에서 `13.4 → 11.4 → 9.4 → 9.4px` (100% 이상은 안 건드린다).

`checkavatar` 의 「목」이 **두 축을 같이 돈다** — 체형 5단계와 몸통 배율 4단계.
순서(가늘수록 길다)뿐 아니라 **50% 가 150% 보다 2px 이상 긴지**도 본다.
순서만 보면 **전부 같은 값이어도 통과**라 예전 상태(9.4 → 9.4 → 9.4 → 9.4)를 못 잡는다.

### 목 굵기는 얼굴·어깨에서 나온다

**고정 폭이면 안 된다.** 머리는 등신 비율(`headK`)과 「얼굴」 배율을, 어깨는 체형과
「몸통」 배율을 탄다. 목만 고정이면 얼굴을 키웠을 때 목이 가늘어 보이고,
어깨를 넓혔을 때 목이 몸에 안 붙어 보인다.

```
목 반폭 = (얼굴 반폭 × 0.27 + 어깨 반폭 × 0.26) / 2
```

두 계수는 **지금 모습(반폭 9)에서 역산한 값**이라 기본값에서는 예전 그림과 같다.

### ⚠️ 목은 몸통 그룹, 머리는 머리 그룹 — 변환이 다르다

목 윗변을 `96` 으로 박아 두었더니, 머리를 `NECK_LIFT` 만큼 올리는 순간
**턱과 목이 벌어졌다**(w=0 에서 0.6px). 배경이 실처럼 비쳐 「얼굴과 목이 떨어졌다」가 된다.

`build()` 가 턱의 y 를 머리 변환으로 구한 뒤 **몸통 좌표계로 환산해서** 넘기고,
2px 겹치게 둔다. `checkavatar` 가 다섯 단계에서 「턱과 목이 겹쳐 있는가」를 본다.

### 팔에는 테두리를 두르지 않는다

맨팔 둘레에 살색 그림자선을 두르고 있었는데, 그 선이 어깨에서도 이어져
**팔이 몸에서 떨어져 붙인 것처럼** 보였다. **어깨 실루엣에는 테두리가 없으므로
없는 쪽으로 맞춘다** — 같은 살색끼리는 선 없이 이어지는 편이 몸으로 읽힌다.

> 테두리를 지우자 커버리지 검사에 `dress_maxi` 가 걸렸다. 그 선이 팔의 맨 바깥 1px 을
> 살색 아닌 색으로 덮고 있었을 뿐, 원래 있던 자리다. 소매 밑단은 둥글어서 바깥쪽이
> 높이 끝나므로 검사 창을 6px 물렸다.

### ⚠️ 몸통 옆선과 팔은 반대로 간다 — 옆구리에 실틈이 생긴다

몸통은 어깨에서 허리로 **좁아지고**, 팔은 `armRot` 만큼 바깥으로 **기울어** 내려간다.
그래서 y 170 언저리에 **1px 짜리 배경**이 실처럼 비쳤다. 팔을 몸통에 2px 더 물려서 막았다.

- 굵기 배율의 축은 **몸 쪽 가장자리**다. 팔 한가운데를 축으로 하면 가늘게 만들 때
  안쪽 변이 바깥으로 밀려 몸통에서 떨어진다 (팔 배율 0.5 에서 실제로 벌어졌다)
- `checkavatar` 가 **체형 21단계**를 훑는다. 5단계만 보면 그 사이에서 벌어지는 것을
  놓친다 — 실제로 0.2 와 0.25 에서만 벌어진 적이 있다

### ⚠️ 어깨를 기울이는 것으로는 못 고친다

어깨 윗선이 목 밑에서 수평으로 쭉 뻗다가 끝에서만 떨어져서, 작은 머리가
**넓은 판자 위에 얹힌 것**처럼 보인다. 그래서 목 옆에서부터 기울여 봤는데
(`shoulderC` 의 첫 제어점을 `[116,108]` → `[110,109]`),
**넥라인을 판 옷의 모서리가 몸통 위로 떠서 그 틈으로 살이 비쳤다** — 퍼프·슬립 드레스 좌우 1px.

넥라인은 x 118 언저리까지 파는데 그 안쪽 어깨선이 108 보다 내려가면
「옷도 몸도 없는 자리」가 생긴다. **어깨를 기울이려면 넥라인 표(`NECK_CUT`)도 같이 좁혀야 한다.**
지금은 안 한다 — 목 길이만으로 충분히 풀린다.

어깨 곡선의 제어점은 `BODY.shoulderC` **한 곳에서만** 정한다. 그리는 곳(`torsoArms`)과
재는 곳(`torsoTopAtX`)이 같은 값을 읽어야 한다 — 두 군데에 적어 두면 어긋나는 순간
넥라인 검사가 헛돈다.

`tools/checkavatar.js` 가 다섯 단계를 다 재서 **5~16px 사이인가**와
**날씬할수록 긴가**를 본다. 손잡이를 0 으로 되돌리면 8건이 잡힌다.

---

## 3-4. 커스터마이징 150벌 — 축 두 개를 곱한다

옷이 소매·넥라인·단추로 갈리듯, **헤어와 악세사리도 축 두 개로 갈린다.**
벌마다 그림을 하나씩 그리지 않는다 — 150벌에 새로 그린 조각은 25개다.

| 칸 | a축 (형태) | b축 (장식·마감) | 벌 |
|---|---|---|---|
| 헤어 | `back` 뒷머리 6 | `bang` 앞머리 5 | 30 |
| 서클렛 | `band` 띠 4 | `orn` 장식 5 | 20 |
| 귀걸이 | `form` 형태 6 | `charm` 장식 5 | 30 |
| 목걸이 | `chain` 줄 6 | `pend` 펜던트 5 | 30 |
| 장갑 | `len` 길이(숫자) 4 | `finish` 마감 5 | 20 |
| 구두 | `rise` 목 높이(숫자) 4 | `finish` 마감 5 | 20 |

### 지킬 것

- **렌더러는 축 하나당 함수 하나.** 벌을 늘려도 `avatar.js` 를 다시 안 건드린다.
  장식 조각(`charmShape`)은 서클렛·귀걸이·목걸이가 같이 쓴다
- **옛 세이브는 축 필드가 없다.** 각 렌더러는 `it.form || (it.kind === ... )` 처럼
  옛 `kind` 로 떨어지는 길을 갖고 있다. 이 길을 지우면 옛 착장이 통째로 사라진다
- 이름의 **b축 기본값은 이름에 안 붙는다** (일자뱅 · 원 · 민짜). 그래야
  `긴 생머리` / `긴 생머리 사이드뱅` 처럼 읽힌다.
  다만 축을 따로 고르는 칸(헤어)에는 그 기본값에도 부를 이름이 필요하다 → `secKo`/`secEn`
- 벌은 `tools/genwardrobe.js` 가 뽑는다 — `data.js` 를 손으로 고치지 말 것

### 목록은 4.5줄만 보이고 그 안에서 스크롤한다

한 칸에 30벌이면 4열 × 8줄이라 화면을 통째로 먹는다. `.wr-items` 에 상한을 걸고
안에서 스크롤한다. **반 줄을 남기는 이유**는 아래에 더 있다는 것이 보여야
스크롤할 생각이 들기 때문이다. 줄 높이(`--wr-row`)는 고정한다 — 칸 높이가 섞이면
'4.5줄' 이 몇 px 인지 정해지지 않는다.

### 칸에는 그림만 — 이름은 눌렀을 때 토스트로

30벌짜리 칸에서 `긴 생머리 시스루뱅` 같은 이름은 세 줄을 넘겨 잘렸다.
글자는 11px 밑으로 못 줄이므로(TEXT_POLICY 1) **그림만 남기고 이름은 눌렀을 때**
알려 준다. 칸 높이가 94 → 58px 로 줄어 한 화면에 훨씬 많이 들어간다.

- `aria-label` 에는 이름을 그대로 넣는다 — 화면 낭독기에는 그것이 유일한 단서다
- 잠긴 칸을 눌러도 이름을 알려 준다 (`{name} — 아직 못 얻었어요`)
- 토스트는 요소가 아니라 **선택자**로 붙인다. 뒤이은 `renderShowcase()` 가 칸을
  새로 그려 그 버튼이 문서에서 떨어져 나가면 좌표가 0,0 이 된다

### 헤어만 두 줄로 고른다 — 전체 실루엣 × 앞머리

**예외는 헤어 하나다.** 30벌을 한 줄에 늘어놓으면 `긴 생머리 시스루뱅` 과
`단발 시스루뱅` 이 서로 무엇이 다른지 훑어서는 안 읽힌다. 축이 두 개인 것을
화면에서도 두 줄로 쪼갠다 — **뒷머리 6 + 앞머리 5 = 11칸**이면 된다.

- 고른 둘이 만나는 벌이 실제로 입는 벌이다. **벌 id 는 그대로**라 세이브도 그대로고,
  마이그레이션도 필요 없다 (`hairItemOf(back, bang)` 이 찾아 준다)
- 칸이 11개뿐이라 **이름을 다시 적는다.** 4열이 아니라 3열이라 칸이 넓고,
  앞머리는 실루엣만으로는 고를 수 없다. 이름은 두 줄까지 (`.wr-nm`, 높이 고정)
- 그림은 **고르면 나올 모습 그대로** 그린다 — 나머지 축은 지금 쓰는 것을 물려받는다
- **앞머리 칸은 이마만 확대한다** (`hairIcon(it, color, 'face')`). 다섯 가지 차이는
  전부 이마(y29~60)에 있는데 머리 전체를 담으면 그 띠가 아이콘의 4분의 1로 줄어
  다섯 개가 사실상 같은 그림이 된다
- **잠금은 조합 단위다.** 뒷머리를 바꾸면 앞머리 칸의 잠금도 같이 달라진다 —
  가진 것은 `긴 생머리 시스루뱅` 이라는 한 벌이지 `시스루뱅` 이 아니다
- 축 이름(`D.HAIR_AXES`)도 `tools/genwardrobe.js` 가 벌 목록과 **같은 표에서** 뽑는다.
  벌 이름에는 기본 앞머리를 안 붙이지만(그냥 `긴 생머리`) 칸에는 부를 이름이
  있어야 해서, 축 표의 `secKo`/`secEn` 이 그 자리를 맡는다

### 헤어는 이모지 대신 실루엣을 그린다

30종에 겹치지 않는 이모지는 없다. `Avatar.hairIcon(it, color, crop)` 이 아바타와
**같은 `hairBack`/`hairFront` 로** 작게 그린다 — 머리 모양을 고치면 아이콘도 같이 바뀐다.
잘라 내는 범위(viewBox)는 어깨 높이까지다. 꼬리 끝까지 담으면 세로로 길어져
칸에서 폭이 24px 밖에 안 나오고, 그러면 앞머리가 구분되지 않는다.

나머지 다섯 칸은 이모지를 쓰고 **b축에서 가져오므로 칸 안에서 겹친다** (허용된 사항).

---

## 4. 확인(컨펌) 패널

- 바깥 영역(딤)을 터치하면 **취소와 동일하게** 닫힌다.
- 버튼은 `취소`(좌) / `확인`(우) 순서.
- 값을 치르는 확인에는 `showConfirm(문구, 콜백, html)` 의 셋째 인자로
  **'보유 / 지불' 표**(`.confirm-extra`)를 붙인다. 무엇을 얼마나 내는지 누르기 전에 보여야 한다.
  - 모자란 쪽은 `.cf-n.lack`(`--text-pink`)로 표시한다 — 색만으로 알리지 않고
    확인을 눌렀을 때 안내 토스트도 같이 뜬다
  - 재화 아이콘은 **누를 수 있다** — 무엇인지·어디서 얻는지 토스트로 알려 준다
- **문구는 언제나 `textContent`.** 이름처럼 사람이 넣은 값이 들어와도 태그로 해석되지 않게.
  표(html)는 코드가 만든 것만 넣는다
- **양을 고르는 패널은 고른 값이 보이게 한다.** AP 충전은 슬라이더(1 ~ 모자란 만큼)와
  게이지가 한 덩어리다 — 지금 AP(노랑) 옆에 **이번에 채울 몫**(분홍)을 이어 붙이고,
  슬라이더를 움직이면 게이지·충전량·지불이 같이 따라간다. 숫자만 있으면
  320 이 많은 건지 적은 건지 감이 오지 않는다
  - 모자란 표시(붉은 글씨)는 **보유가 아니라 지불** 쪽에 단다. 슬라이더를 내리면
    낼 수 있게 되는 값이라, 움직일 대상 옆에 있어야 읽힌다
  - 게이지·트랙 색은 패널 바탕(`--cream`)과 달라야 한다. 같으면 통이 안 보이고
    손잡이만 떠 있는 것처럼 된다 (실제로 그렇게 나왔다)
  - `input[type=range]` 에 `appearance:none` 을 주면 트랙과 손잡이를 **직접 그려야** 한다.
    `touch-action: none` 을 같이 준다 — 안 주면 좌우로 끌 때 화면이 같이 움직인다
  - **낼 값은 확인을 누른 시점에 다시 센다.** 패널이 떠 있는 동안에도 AP 는 변할 수 있고,
    고른 양이 남은 자리보다 크면 남은 자리까지만 채운다
- 고르고 나서 치르는 패널(다이아 상점)은 **고른 값의 가격을 확인 버튼의 라벨로** 쓴다
  (`showConfirm` 의 넷째 인자). 고르는 칩은 1장의 `.cat-tabs` 를 그대로 쓴다.
  **`closeConfirm()` 이 라벨을 되돌린다** — 안 되돌리면 다음 패널이 남의 가격표를 달고 뜬다

---

## 3-2. 한 마리는 **한쪽 부대에만** 선다

방어대와 공격대는 같은 크리처를 나눠 쓴다. **한 마리가 양쪽에 설 수는 없다.**

- 다른 부대에 선 아이는 **회색**(`.pal-item.off`)이고 **맨 아래로 내린다** —
  고를 수 있는 것이 위에 모여야 「누굴 넣지」가 한눈에 끝난다
- 딱지는 `.raid-tag.locked` 다. **`.bad` 를 쓰지 않는다** — 그건 이미
  「상성이 불리하다」는 뜻이라 한 클래스가 두 가지를 말하게 된다
- 누르면 **그냥 막지 않고 물어본다** — 「방어대에서 빼서 공격대로 데려갈래요?」.
  그냥 막으면 「왜 안 되지」로 끝나는데, 이 한 번의 물음이 규칙을 알려 준다
- ⚠️ **`autoTeam` 도 이 규칙을 따른다.** 안 그러면 두 부대가 «같은 다섯»이 되어
  고르기 화면이 통째로 회색이 된다
- ⚠️ **크리처가 다섯뿐이면 두 부대를 다 못 채운다.** 그것이 규칙의 값이다 —
  검사 하나가 이것 때문에 깨졌고, 고친 것은 규칙이 아니라 검사의 준비 상태였다

---

## 3-2a. 시트 위에 시트를 띄울 때 — **층을 적어 둔다**

`.modal` 은 전부 `z-index: 50` 이다. 둘이 같이 떠 있으면 **DOM 순서가 이긴다** —
「누가 나중에 열렸나」와는 아무 상관이 없다.

`#teamPick` 이 `#raidPick` 보다 `index.html` 앞쪽에 있어서, 이웃 밭에서 「바꾸기」를
누르면 부대 고르기가 **뒤로 떴다** (신고받았다). 사람 눈에는 「안 열렸다」로 보인다.

- **DOM 을 옮겨서 고치지 않는다.** 다음에 시트를 하나 더 넣으면 또 깨진다.
  **누구 «위에» 뜨는 것인지를 층으로 적는다** — 그것이 진짜 규칙이다
- 지금 층: `#plantPick`·`#teamPick` = 55 (밭·이웃 밭 시트 위) · `#confirmModal` = 60
- ⚠️ **`classList.contains('show')` 로 검사하면 안 된다** — 뒤에 떠 있어도 참이다.
  `checkui` 의 「겹친시트」는 `elementFromPoint` 로 **실제로 눌리는 쪽**을 본다

---

## 3-2c. 탐험 일지 (`#diaryModal` · `.di-*`)

탐험 탭 맨 위의 「📔 탐험 일지」로 연다. **수치가 아니라 문장이 쌓이는 곳이다** —
`S.record` 는 「몇 번 했나」를 세지만 여기는 「무슨 일이 있었나」를 적는다.

### 문장은 **읽을 때** 만든다

세이브(`S.diary`)에 들어가는 것은 **id 와 숫자뿐**이다. `{ k: 'di_creature',
v: { id: 'unicorn', attr: 'light' } }` 처럼. 문장은 `diaryLine()` 이 그때그때 만든다.

- **적을 때 `N()` 으로 풀어 넣으면 그때의 언어가 세이브에 굳는다.** 영어로 바꾼 뒤
  일지를 열면 예전 줄만 한국어로 남는다 — 화면 어디에도 오류가 안 뜬다
- **조사도 읽을 때 붙인다** (`{nj}` `{ij}` `{wj}`). 「유니콘를」이 되면 아무리
  귀여운 문장도 무너진다. `josa()` 는 «날것» 이름의 받침을 보므로 태그를 씌우기 전에 고른다
- 옛 세이브에 지금은 없는 열쇠가 남아 있으면 **그 줄만 빠지고** 일지는 그대로 열린다

### 한 줄은 **사실 + 꼬리말**이다

```
🛡️ 수수가 우리 밭에 왔다가 그냥 갔다. 우리 애들이 딱 막아 냈다! 오늘 저녁은 특식이다.
   요정 대모가 또 「그만 먹어요!」 하겠지. 오늘은 우리가 이겼으니까 봐줘요.   ← .di-tail
```

**본문이 「무슨 일이 있었나」라면 꼬리말은 「그래서 내 기분이 어땠나」다.**
웃음은 사건이 아니라 반응에서 나온다 — 「막아 냈다」는 사실이고, 웃긴 자리는 그다음이다.

- 꼬리말은 본문보다 **한 단 흐리게**(`--text-lav`) 둔다. 같은 색이면 사실과 농담이
  한 덩어리로 읽혀 둘 다 안 읽힌다
- **과녁은 늘 우리 쪽이다** — 잔소리하는 요정 대모 · 말 안 듣는 크리처 · 변명하는
  공주 본인 셋이면 충분하다. **남을 비웃는 농담은 안 만든다**: 코지 게임이고,
  이웃은 다음 주에 내가 털릴 사람이다 (약탈 한 마디와 같은 규칙 — `roundQuip`)
- **어느 꼬리말이 붙을지는 그 줄이 적힌 시각으로 고정한다.** `Math.random()` 이면
  일지를 열 때마다 말이 바뀌어 「내가 잘못 읽었나」가 된다 (날씨와 같은 이유)

### 한 날이 한 쪽 — 책장을 넘기듯 본다

날짜 여럿을 죽 늘어놓지 않는다. **`◀` 가 과거, `▶` 가 최근**이고, 끝에 닿은 쪽은
`disabled` 다 — 눌러도 아무 일 없는 버튼을 살려 두지 않는다. 열면 늘 0쪽(오늘)이다:
어제 넘겨 본 자리로 열면 놀란다.

⚠️ 265px 에서 화살표 44+44 에 여백 14 씩, 쪽수에 `min-width: 62` 를 주었더니
178px 이 되어 161px 칸을 **9px 넘쳤다.** 가운데 쪽수가 남는 자리를 갖고
(`flex: 1 1 auto; min-width: 0`) 여백을 줄여서 풀었다.

### 사건을 늘릴 때

1. `diaryAdd('di_새것', { … })` 를 그 일이 실제로 일어나는 자리에 넣는다
2. `i18n.js` 에 **ko·en 둘 다** 넣는다 — 본문과 꼬리말 `_t1`…`_tN` 까지
3. `game.js` 의 `DIARY_ICON` 에 아이콘을, `DIARY_TAILS` 에 꼬리말 수를 적는다

⚠️ **3번이 곧 검사 목록이다.** `checkui` 의 「탐험일지」가 `DIARY_ICON` 의 열쇠를
하나씩 심어 문장이 나오는지 보고, 「일지꼬리말」이 `DIARY_TAILS` 가 적어 둔 수만큼
문구가 있는지 본다 — 표에 넣기만 하면 빠뜨린 문구가 그 자리에서 잡힌다.
안 넣으면 **그 사건은 영영 검사받지 않는다.**

문구를 빠뜨렸을 때 화면은 멀쩡해 보인다: `diaryLine()` 이 빈 문자열을 돌려주고
렌더가 그 줄을 건너뛴다. **사건만 조용히 사라진다.** 꼬리말이 빠지면 더 안 보인다 —
그 줄은 본문만 남아 그냥 짧은 줄로 읽힌다.

⚠️ **사건 열쇠는 검사에서 «같은 날»에 몰아 심는다.** 한 날이 한 쪽이라 여러 날에
나눠 심으면 한 번에 한 쪽만 보이고, 나머지는 재지 않은 채 통과한다.

### 무엇을 적고 무엇을 안 적는가

**하루에 여러 번 하는 일은 안 적는다.** 물약은 마실 때마다가 아니라 **몸의 단계가
내려간 날**에만, 채집은 주울 때마다가 아니라 **히든 재료**에만 한 줄이 붙는다.
평범한 채집까지 적으면 일지가 통째로 채집 로그가 되어 진짜 사건이 묻힌다.

「내가 없는 동안 일어난 일」이 들어오는 자리는 **밭 기록 하나뿐이다**(`diaryFromFarm`).
나머지는 전부 내가 버튼을 눌러서 생긴 것이다.

### 날짜와 연호

머리에 **로엔 제국력**이 뜬다 — **현실 연도 − 1800**(2026 → 226 · 2127 → 327).
`eraYear()` 한 곳에서 나오고 `nowDate()` 를 지난다. `npm run test:time` 이
연도를 실제로 옮겨 보고 확인한다 — 226 을 상수로 박아 두면 오늘은 통과로 보이고
**새해 첫날에 조용히 틀린다.**

날은 **새것이 위**, 그 안은 **일어난 순서대로**다. 하루치는 아침부터 읽어야
이야기가 되고, 날은 오늘부터 봐야 찾기가 쉽다.

---

## 3-2b. 개발용(임시) 블록 — 컨트롤은 **두 가지뿐이다**

출시 때 통째로 지울 블록이지만, 그때까지는 **사람이 매일 보는 자리**다.
버튼이 스무 개 넘게 쌓이면서 무엇이 무엇인지 안 보이게 됐고, 정비했다.

| 종류 | 만드는 법 | 모양 | 뜻 |
|---|---|---|---|
| **실행** | `devAct()` · `devActs([...])` | 노란 카드, 폭을 나눠 갖는다 | 누르면 **지금** 일어난다 |
| **스위치** | `devSw()` · `devSws([...])` | 점선 칩(꺼짐) / 실선 노랑(켜짐) | 켜고 끄는 **상태** |

- **둘을 같은 모양으로 만들지 않는다.** 전에는 전부 전체 폭 버튼이라
  「모든 맵 오픈」이 상태를 바꾸는 것인지 한 번 일어나고 마는 것인지
  **눌러 보기 전에는 알 수 없었다**
- **하나에 한 줄씩 쓰지 않는다.** 실행은 `auto-fit minmax(128px, 1fr)` 로
  폭을 나눠 갖고, 스위치는 글자 폭 칩으로 흘려 담는다.
  탐험 블록이 530px → **315px** 가 됐다
- **같은 성격끼리 모은다.** 마이 룸의 AP 충전 버튼이 블록 한가운데 혼자 있었다 —
  실행은 실행끼리 맨 위에 모은다
- 묶음마다 `devGroup()` 으로 제목을 단다 (`▶ 실행` · `🔓 열기 스위치`)

**네 화면(마이 룸·공방·탐험·랭킹)이 같은 헬퍼를 쓴다.** 새 개발용 컨트롤을 넣을
때도 이 넷만 쓰면 저절로 같은 모양이 된다 — 직접 `<button class="btn btn-dev">`
를 쓰기 시작하면 다시 어긋난다.

---

## 3-3. 마이 룸 배경 (`avatar.js` 의 `roomScene(level, extra)`)

방은 **껍데기 + 소품** 두 겹이다. 단계별로 그림을 다섯 장 그리지 않는다 —
솥을 하나의 실루엣으로 그리는 것과 같은 이유다.

| 겹 | 무엇 | 어디 |
|---|---|---|
| 껍데기 | 벽 · 창문 · 바닥 | `roomScene()` 안에 직접. 단계에 따라 **색만** 바뀐다 (`ROOM_SKIN[lv]`) |
| 소품 | 이름이 붙은 조각 | `ROOM_PROPS[id](k)` — `k` 는 그 단계의 색 한 줄 |
| 구성 | 단계마다 어떤 소품을 놓나 | `ROOM_LEVELS[lv]` = 소품 id 목록 |
| 순서 | 겹치는 앞뒤 | `ROOM_Z` (뒤 → 앞). `@window` `@floor` `@beam` 은 껍데기가 끼어드는 자리 |

### 지킬 것

- **기본은 2단계다** (`ROOM_DEFAULT`). 1단계는 거미줄·균열까지 있는 '텅 빈 골방' 이라
  첫인상으로 쓰기엔 너무 휑하다 — 1단계는 아래로 내려갈 자리로 남겨 둔 것이다.
  숫자는 `avatar.js` 한 곳에만 두고 `game.js` 는 `roomDefault()` 로 읽는다
- **가운데(x 150~250)는 비워 둔다.** 아바타가 서는 자리다. 소품을 놓으면 몸에 가린다
- **위 단계가 아래 단계를 다 물려받지는 않는다.** 거미줄·균열은 방이 좋아지면 사라지고,
  작은 러그는 큰 카펫으로 바뀐다. `ROOM_LEVELS` 를 누적으로 쓰지 말 것
- **소품을 늘리면 `ROOM_Z` 에도 끼워 넣는다.** 목록에 없으면 아예 그려지지 않는다
  (`ROOM_Z` 가 그리는 순서이자 그리는 대상이다)
- **SVG 의 id 는 문서 전체에서 공유된다.** 그래서 `roomScene()` 은 부를 때마다
  그라디언트·클립 id 에 일련번호를 붙인다. 안 붙이면 방을 두 개 이상 한 화면에
  놓았을 때 뒤엣것이 앞엣것의 색을 그대로 써서 **단계별 색이 전부 같아진다**
  (5단계를 나란히 놓고 보다가 실제로 겪었다). 새 `<defs>` 를 넣으면 `ID()` 를 거칠 것

### 추후 '마이 룸 꾸미기' 를 붙일 자리

소품이 id 로 구분돼 있으므로, 플레이어가 고른 소품 id 목록을 `roomScene(lv, extra)` 의
**둘째 인자**로 넘기면 단계 기본 소품 위에 그대로 얹힌다. 단계 구성은 건드리지 않는다.

### 검증

단계를 바꿀 길이 아직 게임 안에 없다 — 마이 룸의 **개발용(임시) > 🏠 방 배경** 에서 고른다.
`S.roomLevel` 은 세이브에 들어간다 (튜토리얼 스위치와 같은 이유 — 진짜 진행 값이다).

---

## 3-2. 마법 솥 그림 (`cauldron.js`)

솥 11종을 하나씩 그리지 않는다. **실루엣은 하나**고 몸통·테두리 색과 장식만
데이터(`CAULDRONS[].art`)가 갈아 끼운다 — 옷과 같은 방식이다.

| 필드 | 값 |
|---|---|
| `art.body` | 몸통 3색 (밝은 → 어두운) |
| `art.trim` | 테두리·손잡이·다리 3색 |
| `art.deco` | `rust` 녹 · `shine` 광택 · `granite` 점박이 · `facet` 결정면 · `moon` 달 · `stars` 별 · `dragon` 용 · `runes` 룬 |

- **그라디언트 id 는 솥마다 다르게 짓는다** (`cd_gold_body` 처럼).
  한 문서에 같은 id 가 둘 있으면 브라우저는 **먼저 나온 것만** 쓴다 —
  대조표를 뽑았더니 열한 대가 전부 첫 번째 솥의 어두운 몸통색으로 나왔다.
  화면에는 한 대만 뜨지만, 두 대를 나란히 놓는 순간 같은 함정에 빠진다
- 장식의 점·별 자리는 **정해진 좌표**를 쓴다. 난수를 쓰면 다시 그릴 때마다 옮겨 다닌다
- 같은 솥이면 다시 그리지 않는다 (`dataset.pot` 비교) —
  매 렌더마다 갈아 끼우면 거품·반짝임 애니메이션이 처음부터 다시 돈다
- 움직이는 부분(`.bub` `.twinkle` `.brew-glow`)은 `style.css` 가 맡는다.
  클래스만 그대로 붙여 주면 애니메이션이 따라온다

---

## 4-1. 펼침 줄 (`.dye-bar`)

눌러서 아래를 펼치는 줄. 마이 룸의 **마법 염색약**이 이 모양이다.

- **줄 전체가 버튼이다.** 글자만 눌리면 어디를 눌러야 하는지 알기 어렵다
- **화살표로 열림/닫힘을 알린다** (`aria-expanded` + `▾` 회전).
  눌러도 아무 일이 없어 보이는 순간이 없어야 한다
- 줄 안의 `?` 같은 보조 버튼은 **`event.stopPropagation()`** 을 건다 —
  안내를 보려다 줄이 펼쳐지면 안 된다
- 쓸 수 없는 상태(염색약 0개)여도 **`aria-disabled` 를 달지 않는다.**
  눌리지 않는 게 아니라 **눌러서 이유를 듣는** 줄이다 (7장)
- `div[role=button]` 이므로 **Enter/Space 를 직접 받는다.** 진짜 `<button>` 이 아니면
  키보드로는 아무것도 안 된다

### 보유 개수는 한 곳에만

같은 화면에 '보유' 가 여러 군데 있으면 무엇의 수인지 헷갈린다.
옷 개수는 **고른 탭 안에**(`원피스 9/9`), 색 개수는 **컬러칩 아래**에 둔다.
목록 아래에 따로 있던 줄은 없앴다.

### 머리색도 염색이다 — '헤어컬러' 칸은 없앴다

예전에는 `헤어컬러` 라는 칸을 따로 두고 7색 중에서 골랐다. 옷은 60색 팔레트로
염색하면서 머리만 7색인 것이 앞뒤가 안 맞았고, 탭도 하나 더 먹었다.
지금은 헤어 아이템에 **원래 색(브라운)** 이 붙어 있고, 그것을 염색해서 머리색을 정한다 —
다른 칸과 완전히 같은 규칙이다 (`COLORABLE_SLOTS` 에 `hair` 가 들어 있다).

- 옷장 칸의 머리 실루엣도 **지금 머리색으로** 그린다. 염색해 놓고 목록만 브라운이면
  무엇을 고르는지 헷갈린다
- 옛 세이브에서 브라운이 아닌 머리색을 골라 뒀던 사람은 **가장 가까운 팔레트 색**으로
  옮기고 '영원 염색' 으로 잡아 뒀다 (세이브 9 마이그레이션)

### 두 염색약은 **조건이 다르다** — 한 조건으로 묶지 말 것

| | 쓸 수 있는 조건 | 언제 풀리나 | 세이브에 남는 것 |
|---|---|---|---|
| 🧪 마법 염색약 | 그 팔레트 색을 **얻어 뒀어야** 한다 | 24시간 뒤 | `dyeEnd[아이템 id]` |
| ♾️ 영원 염색약 | **그 색 염색약 자체가 아이템**이라 색을 따로 얻을 필요가 없다 | 안 풀린다 | `dyeForever[아이템 id]` |

### 염색은 **칸이 아니라 옷 한 벌**에 붙는다

세이브 11 에서 옮겼다. 그전에는 칸(`slot`)에 붙어 있어서 **장갑 하나를 물들이면
가진 장갑이 전부 그 색**이 됐다 — 염색약 한 개로 그 칸 전체를 칠하는 셈이라,
아이템을 늘려도 색이 개성이 되지 못했다.

- 세이브의 키는 `itemColor[아이템 id]` · `dyeEnd[아이템 id]` · `dyeForever[아이템 id]`
- 부르는 쪽은 여전히 **칸으로 물어본다**(`slotColor('glove')`). 칸 → 옷으로 옮기는 것은
  `wornId(slot)` 한 곳에서만 한다
- `expireDye()` 는 **입고 있지 않은 옷도** 본다 — 벗어 둔 사이에도 24시간은 흐른다
- 마이그레이션은 그때 **입고 있던 옷**에 옮겨 붙인다. 물들일 때 입고 있던 옷일
  가능성이 가장 높고, 무엇보다 화면이 그대로 유지된다

`dyedColorId(slot)` 한 곳에서만 판정하고, 아바타(`slotColor`)와 옷장에 적히는
색 이름이 **같은 판정을 쓴다.** 따로 판정하면 아바타는 물들었는데 이름은
'원래 색' 이라고 적힌다.

**기한(`dyeEnd`)만 보면 영원 염색약이 통째로 안 먹는다.** `applyDye` 가
영원 염색약을 쓸 때 기한을 지우기 때문이다 — 실제로 **여덟 칸 전부**에서
그랬다. 칠했다는 토스트는 뜨는데 아바타는 원래 색 그대로라 조용히 지나갔다.

`tools/checkavatar.js` 가 칸마다 **마법·영원·만료 세 가지를 그려 보고**
그 색이 SVG 안에 있는지 본다 — '색을 골랐다' 가 아니라 '그려졌다' 로 봐야 잡힌다.
그리고 물들인 뒤 **같은 칸의 다른 옷으로 갈아입어** 그 색이 따라오지 않는지도 본다 —
칸에 붙어 있던 시절의 버그는 갈아입어 봐야 드러난다.

---

## 4-2. 과시(공유) 카드

`매력 과시` 는 글자가 아니라 **방에 서 있는 아바타를 그린 PNG** 를 보낸다.
화면을 캡처하는 것이 아니라 **같은 SVG 를 캔버스에 다시 그린다** — 그래야 화면 밖으로
잘리지도, 개발용 블록이 딸려 오지도 않는다.

### 지킬 것

- **SVG 를 이미지로 만들 때 `width`/`height` 를 박아 넣는다.** `viewBox` 만 있으면
  브라우저가 기본 크기(300×150)로 그린다
- **비율은 `viewBox` 에서 읽는다.** 아바타는 200×348, 인트로 공주는 260×348 로 가로가 다르다.
  한쪽 비율로 못 박으면 다른 쪽이 좌우 여백을 두고 작게 그려진다
- 글자는 SVG 안이 아니라 **캔버스에 직접** 쓴다. data URI 안의 SVG 는 페이지 폰트를
  물려받지 못해 한글이 깨진다
- 방 배경은 CSS 와 같은 규칙(`xMid YMax slice`)으로 채운다

### 사용자 제스처 — 여기서 한 번 어긋난다

브라우저는 공유·클립보드를 **누른 직후에만** 허용하는데, 그림을 그리는 사이에 그 자격이
만료된다. 그래서 `ClipboardItem` 은 **기다리기 전에** 만들어 둔다 —
이 객체는 Blob 대신 **Promise 를 받아 주므로** 그림이 늦게 완성돼도 붙여넣기가 살아 있다.

순서는 ① 공유 시트(파일) → ② 이미지 복사 → ③ 내려받기 → ④ 글자.
`AbortError` 는 **실패가 아니다** — 사용자가 공유 창을 닫은 것이므로 다음 수단으로 넘어가지 않는다.

### 검증

`node tools/checkavatar.js` 가 카드도 같이 본다 — PNG 인가 · 너무 작지 않은가 ·
**방 영역에 색이 12가지 이상인가.** 마지막 것이 핵심이다:
SVG 래스터화는 조용히 실패해서, 그냥 그리면 배경만 칠해진 빈 카드가 나가도 아무도 모른다.
(래스터화를 일부러 깨 보면 `방 색 1가지` 로 잡힌다)

---

## 4-3. 튜토리얼 (`.tut-*` · `tutorial.js`)

인트로 다음에 딱 한 번 도는 안내. 화면을 반투명 검은 막으로 덮고 **눌러야 할 곳에만
구멍을 뚫는다.**

### 지킬 것

- **구멍은 진짜 구멍이다.** 막을 이루는 `path` 하나에 `fill-rule="evenodd"` 로 안쪽 사각형을
  빼서 만든다. 칠해진 곳만 클릭을 먹고(`pointer-events: fill`) 구멍은 아래 버튼이 그대로 받는다.
  **조각 네 개를 붙여 만들지 않는다** — 경계에서 클릭이 새고, 화면이 움직일 때 어긋난다
- **말풍선은 구멍의 반대쪽에.** 구멍이 화면 아래쪽이면 말풍선은 위(`.tut-talk.top`), 위쪽이면
  아래(`.bot`). 가리키는 곳을 자기가 덮으면 무엇을 누르라는 건지 안 보인다
- **화살표는 알약 위에 얹는다.** 막(SVG `fill`) 위에 글자만 띄우면 배경색을 잴 수가 없고,
  구멍 옆의 밝은 UI 위에서는 실제로 묻힌다 (TEXT_POLICY '방 배경 위의 글자' 와 같은 문제다)
- **불투명도로 강조하지 않는다.** '다음 ▾' 을 0.75까지 깜빡이게 했더니 대비가 4.29:1 로
  떨어졌다. 무한 반복 애니메이션은 `settle()` 이 끝 상태로 못 보내서 재는 순간마다 값이 다르다 —
  움직임만으로 신호를 준다
- **닷은 읽은 것과 안 읽은 것이 훑어서 구분돼야 한다.** 파스텔 두 가지(`--pink`/`--pink-2`)로
  나눴더니 크림 배경 위에서 셋 다 같은 분홍으로 보였다. 지금은 테두리만 있는 원 ↔ 꽉 찬 원이다
- **지시문(`.tut-act`)은 마지막 대사까지 읽은 뒤에만 남긴다.** 대사를 읽는 중에 같이 띄우면
  지금 무엇을 하라는 것인지 두 줄이 서로 다툰다
- **막다른 길을 만들지 않는다.** 구멍 대상을 못 찾으면 `.tut.loose` 로 떨어져 막이 클릭을
  먹지 않는다. 안내가 헛도는 편이 갇히는 것보다 낫다
- **막은 앱(최대 z 30)보다 위, 모달(z 50)보다 아래다** (`z-index: 40`). 모달 위에 두면
  튜토리얼 도중에 뜨는 조합 결과 팝업이 막에 덮여 「확인」이 눌리지 않는다 —
  플레이어에게는 **화면 전체가 먹통이 된 것**으로 보인다
- **팝업이 떠 있는 동안은 감추되 계속 지켜본다.** 감추면서 추적 루프까지 멈추면
  팝업이 닫혀도 아무도 다시 그려 주지 않는다 (설정·확인 패널은 닫을 때 `render()` 를 안 부른다)
- **구멍은 매 프레임 다시 잰다.** 한 번 재고 말면 못 맞춘다 — 화면이 페이드 인 하는 동안
  `.screen` 이 `translateY(6px)` 로 밀려 있어서 그때 잰 구멍이 끝까지 6px 어긋난 채로 남았다.
  좌표계는 `#tut` 의 제 상자이지 `documentElement` 가 아니다 (주소창이 접히는 모바일에서 갈린다)

### 검증

```bash
TUT=1 node tools/checkui.js      # 대비·레이아웃 (평소 검사에서 통째로 빠지는 화면이다)
node tools/checktut.js           # 열여덟 단계를 진짜 마우스로 눌러 끝까지 가 본다
```

`checktut` 은 **`el.click()` 을 쓰지 않는다.** 그것은 `pointer-events` 를 무시해서 막이
뚫려 있는지 없는지를 전혀 검사하지 못한다.

---

## 5. 토스트

- 아이콘 옆에서 뜨는 안내는 그 아이콘을 `anchor` 로 넘겨 근처에 띄운다.
- 줄바꿈이 있거나 22자를 넘으면 자동으로 여러 줄(`.toast.multi`)로 표시된다.
- 표시 시간 기본 1.8초, 긴 문구는 `toast(msg, anchor, ms)` 로 늘린다.

---

## 6. 버튼음

모든 버튼은 `sfx.js` 의 위임 리스너가 자동으로 소리를 붙인다. 개별 코드 불필요.

| 소리 | 대상 |
|---|---|
| `ui_tab` | 하단 탭 |
| `ui_confirm` | 주요 실행 버튼 (`.btn-primary`) |
| `ui_back` | 취소·닫기 (`.btn-ghost` `.set-x` `.i-skip`, 모달 딤) |
| `ui_tap` | 그 외 전부 |

개별 조정이 필요하면 `data-sfx="ui_confirm"`, 무음은 `data-nosfx`.

---

## 7. 잠긴 콘텐츠 (컨텐츠 언락 시스템)

아직 열리지 않은 것은 **한눈에 구분되어야 한다.** 자물쇠 아이콘 하나만 붙여 두면
목록을 훑을 때 열린 것과 섞여 보인다.

### 규칙

잠긴 요소에는 `.locked` 를 붙이고, `style.css` 의 **잠긴 콘텐츠 공통 표현** 블록
선택자 목록에 추가한다. 개별 화면에서 흐리게 만드는 코드를 따로 쓰지 않는다.

```css
.spot-card.locked,
.cat-tab.locked,
.wr-item.locked,
.room-tab.locked {
  filter: saturate(0.15);   /* ① 색 신호를 걷어낸다 */
  opacity: 0.8;             /* ② 살짝 뒤로 물러나게 한다 */
}
/* 색 견본만 예외 — 아래 '색 견본은 채도를 낮추지 않는다' 참고 */
.wr-color.locked { opacity: 0.78; }
```

**자물쇠는 CSS `::before` 로 붙이지 않는다.** 한 번 그렇게 했다가 `checkLocked()` 에
"자물쇠 표시가 없음" 으로 걸렸는데, 그 지적이 맞다 — 생성된 콘텐츠는 접근성 트리에
올라가지 않아 소리로 읽히지 않는다. 진짜 글자로 넣는다.

**`aria-disabled` 도 쓰지 않는다.** 잠긴 탭·카드는 눌리지 않는 게 아니라 **눌러서
해금 조건을 듣는** 버튼이다. `aria-disabled="true"` 를 달면 보조기술이 "동작하지 않는
버튼" 으로 알리고, 자동화 도구도 탭을 거부한다(실제로 Playwright 가 거부했다).
잠금 사실은 자물쇠 글자와 `aria-label` 에 담는다.

| 항목 | 값 | 이유 |
|---|---|---|
| 채도 | `saturate(0.15)` (최대 0.4) | **주된 수단.** `saturate()` 는 명도를 거의 보존해서, 색만 걷어내고 글자 대비는 떨어뜨리지 않는다 |
| 불투명도 | `0.8` (0.7 ~ 0.92) | 보조 수단. `opacity` 는 대비를 그대로 깎으므로 0.8 까지만 |
| 글자색 | `--ink` | 흐려진 만큼 진하게 잡아, 0.8 이 곱해진 뒤에도 4.5:1 을 넘긴다 (크림 배경에서 4.65:1) |
| 자물쇠 | 🔒 필수 | 색약 사용자에게는 채도 차이가 전달되지 않는다. 잠금 사실 자체는 **색이 아닌 수단**으로도 알려야 한다 |

> **해금 조건 문구는 예외적으로 굵게(`800`) 유지한다.** 잠긴 카드에서 사용자가
> 유일하게 읽어야 하는 정보가 "언제 열리는가" 이기 때문이다.

#### 예외 — 색 견본(`.wr-color.locked`)은 채도를 낮추지 않는다

옷 색 팔레트에서는 **색 자체가 내용**이다. 공통 표현의 `saturate(0.15)` 를 그대로 쓰면
60개가 전부 회색이 되어 **무엇을 얻게 되는지 볼 수 없다** — 잠긴 것을 알리려다
잠긴 것이 무엇인지를 지워 버리는 셈이다.

그래서 여기만 **불투명도(0.78) + 자물쇠**로 잠금을 알린다. `checkLocked()` 의 조건은
'채도 **또는** 불투명도 중 하나는 낮출 것' 이라 이대로 통과하고, 색에 기대지 않는
신호(자물쇠)도 그대로 남는다.

> 자물쇠 글자를 넣을 때 걸렸던 것 둘 —
> - `<button>` 은 본문 폰트를 **자동으로 물려받지 않는다.** `font-family: inherit` 를
>   빼먹으면 자물쇠가 Arial 로 떨어져 `checkTextStyle()` 이 60건을 잡는다
> - 자물쇠·보유 개수의 글자 크기는 **11px 이상**이어야 한다. `0.68rem` 은 10.88px 이라
>   반올림 없이 걸린다 (`minFontPx`)

### 이 규칙을 적용하지 않는 것

| 대상 | 이유 |
|---|---|
| `.recipe-row.locked` (레시피 `❓ ???`) | 언락 시스템이 아니라 **아직 발견하지 못한** 상태다. 조건을 채운다고 열리는 게 아니라 직접 조합해서 알아내야 한다. `? + ?` 라는 내용 자체가 이미 신호라 흐리게 만들지 않는다 |
| `.cat-tab.dim` | 잠김이 아니라 '해당 항목이 없음' 상태 |

### 검증

```
checkLocked()   // 잠긴 요소가 실제로 흐린가, 자물쇠가 있는가, 너무 흐리지는 않은가
checkUI()       // 대비·레이아웃과 함께 한 번에 (잠금표현위반 열)
```

`checkLocked()` 는 열린 형제 요소와 채도·불투명도가 **똑같으면** 위반으로 잡는다.
새 잠금 UI 를 만들고 공통 블록에 등록하지 않으면 여기서 걸린다.
